# 🧾 ModuleHub – 03 ContextBar / ActiveFilters

## Cel
Zdefiniować standard dla paska aktywnych filtrów oraz (jeśli występuje) kontekstowych akcji/bulk actions.

## Źródło prawdy
`wdrozenia/UI_UX_GOLDEN_STANDARD.md` (ActiveFilters + zalecenia filtrów)

## Wymagania MUST
- Filtry jako chips (możliwość usunięcia pojedynczego i „clear all”)
- Filtry nie mogą zmieniać danych „w tle” bez odświeżenia listy (spójne query)
- Brak mock fallbacków

