# ↕️ FilterableTable – 02 Sorting

## Cel
Ujednolicić sortowanie (domyślne, UI, parametry API).

## Kanon
- Domyślne sortowanie list: `updatedAt desc`
- UI: klik w nagłówek przełącza `asc/desc`
- API: `sort=field:dir` (np. `sort=updatedAt:desc`)

