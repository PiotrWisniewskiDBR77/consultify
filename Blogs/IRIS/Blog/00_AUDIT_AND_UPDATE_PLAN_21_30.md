# IRIS Audit And Update Plan 21-30

Status: English `21-30` audit and retrofit complete (March `2026`). Lever-keyed IRIS bridge rotation is applied across the tranche, `27` is aligned to calmer product language with a clearer scope boundary versus `21` and `28`, and targeted cluster cross-links are in place.  
Strategy source of truth: `Blogs/_SYSTEM/strategy/DBR77_PRODUCT_MARKETING_PLAN.md`  
Tracker source of truth: `Blogs/_SYSTEM/strategy/MASTER_COMPLETION_PLAN.md`

## Purpose

This file applies the same audit rubric family as `Blogs/IRIS/Blog/00_AUDIT_AND_UPDATE_PLAN_01_20.md` to the AI-assisted operations cluster (`21-30`). Use it to classify each article, spot tranche-level patterns, and sequence retrofit work without reopening article bodies in the audit pass itself.

## Audit Rubric (same family as IRIS 01-20)

Score each article on:

1. `Knowledge-layer fit` - one clear layer among Field Reality, Problem Deep Dive, Solution Logic, Decision Support, Execution And Transformation; strengthens IRIS without generic platform drift.
2. `Trust and proof` - field-pattern language, trade-offs, reality checks; avoids inflated category claims.
3. `Article arc` - problem, what happens, mistake or reality check, what works, how DBR77 approaches it, outcome logic.
4. `Operational specificity` - production, warehouse, quality, maintenance, cross-functional loops.
5. `Maturity and tone` - calm, industrial, proof-bearing, non-startup.
6. `System fit` - persona, funnel stage, LP attachment logic.

## Article Classification Table

| Article | Strongest layer fit | Classification | Why | Key update action |
|---|---|---|---|---|
| `21_how_ai_is_changing_factory_operations_when_execution_is_connected` | Solution Logic | keep | Direct answer, disconnected versus connected table, readiness checklist, calm outcome logic; reference mature IRIS style for this tranche | bridge rotation complete; cluster cross-links added to `27` and `28` |
| `22_what_an_ai_agent_can_do_in_a_factory_today` | Decision Support | keep | Grounded scope list, human boundaries, three-zone framework, demo versus operations preconditions | bridge rotation complete; governance cluster links added to `26` and `29` |
| `23_from_humans_to_ai_assisted_operations_what_changes_first` | Execution And Transformation | keep | Clear sequencing (baseline, handoffs, then AI), 30-day realism checklist; pairs logically with `25` and `30` | bridge rotation complete; cross-link nudges added to `25` and `30` |
| `24_autonomous_factory_myth_or_operating_reality` | Field Reality | keep | Myth versus bounded autonomy split, risk-type table, COO-level outcomes without hype | bridge rotation complete |
| `25_how_to_build_ai_assisted_factory_operations_step_by_step` | Execution And Transformation | keep | Eight-step path with baseline-before-AI gate, measurement discipline, expansion rule; strong operational proof anchor | bridge rotation complete; cross-links added to `23` and `30` |
| `26_when_ai_should_recommend_and_when_humans_should_decide_in_operations` | Decision Support | keep | Risk class and reversibility axes, thresholds in workflow, audit-with-work-item handoff logic | bridge rotation complete; governance cluster links added to `22` and `29` |
| `27_why_factories_need_one_decision_layer_before_more_ai_models` | Solution Logic | keep | Core argument remains strong and distinct from `21`; IRIS section now uses calmer product language and clearer scope boundaries | light rewrite complete in the IRIS section; cluster links added to `21` and `28` |
| `28_how_ai_can_prioritize_factory_issues_across_functions` | Decision Support | keep | Shared intake, visible rubric, routing-not-only-ranking, published thresholds | bridge rotation complete; cross-link to `27` added for decision-layer context |
| `29_what_makes_factory_ai_trustworthy_for_operations_leaders` | Decision Support | keep | Operator-depth explainability bar, bounded actions, audit-with-work-item, KPI proof list | bridge rotation complete; link added to `26` for decision-rights context |
| `30_how_to_roll_out_ai_assisted_operations_without_disrupting_the_plant` | Execution And Transformation | keep | Authority unchanged early, shadow versus live, shift training, disruption metrics | bridge rotation complete; cross-links added to `23` and `25` |

## Strongest Internal Style References (within IRIS 21-30)

Use these as internal benchmarks when editing weaker neighbors or later tranches:

- `21` - direct answer plus operational comparison table plus blunt checklist (template for proof without theater).
- `25` - numbered implementation gates, baseline window, explicit anti-success metric (`user likes it`).
- `26` - decision-rights mapped to workflow artifacts (thresholds, reason codes, single record).
- `29` - trust as inspectable behaviors and leadership checklist.

## Tranche-Level Findings

### Mature cluster versus 01-20

Articles `21-30` largely match the post-retrofit IRIS standard visible in `21`: direct answers, tables or checklists, failure modes, and outcome-focused takeaways. None of the headline or metaphor risks seen in IRIS `16` or `18` appear here.

### Repeated IRIS closing pattern

At audit time, all ten English bodies used nearly the same core product sentence (with small variants such as `positioned as` in `21`). That tranche-level sameness was treated as bridge de-templating work, not a full body rewrite, and is now closed in the completed retrofit.

### Structural repetition

Consistent skeleton: front matter, `**Direct answer:**`, H2 sections, `Why IRIS`, `Final takeaway`. Coherent for the library but increases sameness if bridges stay identical.

### Overlap inside 21-30 (editorial, not defects)

- `21` plus `27` plus `28`: coherence, execution connection, prioritization across functions. Differentiate with one sentence each on scope (`connection` versus `single decision layer before model sprawl` versus `cross-functional rubric and routing`).
- `23`, `25`, `30`: sequencing, build steps, low-disruption rollout. Good as a cluster; light cross-links reduce perceived duplication.
- `22`, `26`, `29`: agent scope, recommend-versus-decide, trust. Complementary; avoid merging topics in retrofit.

### Historical production-note lines (legacy production instructions in body)

Repo search on English `article_EN.md` files for articles `21` through `30` found no matches for patterns such as the historical body-instruction compound (both casings), `[CTA]`, `TODO:`, `production note`, or standalone `CTA` / `FIXME` in those paths. Treat this tranche as clean on legacy body-instruction markers unless a future pass uses stricter patterns.

## Top Priority Update Articles

Ordered for practical impact:

1. `[done]` tranche-wide lever-keyed `Why IRIS` bridge rotation for `21-30` English bodies, preserving calm industrial tone.
2. `[done]` `27_why_factories_need_one_decision_layer_before_more_ai_models` light rewrite focused on the IRIS section and one boundary sentence versus `21`/`28`.
3. `[done]` targeted cross-links among `23`/`25`/`30`, `21`/`27`/`28`, and `22`/`26`/`29` in a single consistency pass.

No heavy rewrite candidates in `21-30` under this rubric.

## Package Readiness Notes

- Bodies are publish-ready in substance; the main package gap identified at audit time was the templated IRIS bridge text, and that gap is now closed.
- `21-30` now reads as a differentiated cluster rather than ten copies of the same IRIS closer.

## Recommended Update Sequence

1. `[done]` Draft lever-keyed bridge variants per sub-cluster: execution-and-coherence (`21`, `27`, `28`), governance (`22`, `26`, `29`), transformation (`23`, `25`, `30`), awareness (`24`).
2. `[done]` Apply `27` light rewrite (IRIS section plus scope boundary).
3. `[done]` Add short cross-link sentences where overlap is highest (`23`-`25`-`30`, `27`-`28`, `22`-`26`-`29`).
4. `[done]` Re-read tranche for tone drift versus `Blogs/IRIS/Blog/21_how_ai_is_changing_factory_operations_when_execution_is_connected/article_EN.md` as style anchor.

## Done Standard For This Tranche

Treat IRIS `21-30` English retrofit as complete when:

- `[x]` IRIS bridge blocks are topic-tied and not copy-paste identical across the ten articles.
- `[x]` `27` IRIS section matches calm, proof-bearing product language.
- `[x]` light cross-links reduce cluster overlap without bloating length.
- `[x]` no inline production or CTA instructions appear in bodies (current state maintained).

## Layer Guidance For Adjacent Work

When auditing or retrofitting IRIS `31-40`, keep reinforcing:

- explicit ownership and response loops,
- reality-check blocks where topics trend abstract,
- less duplicate bridge openers,
- continuity with `20` (`execution layer` failure mode) and this tranche (`decision layer`, prioritization, rollout discipline).
