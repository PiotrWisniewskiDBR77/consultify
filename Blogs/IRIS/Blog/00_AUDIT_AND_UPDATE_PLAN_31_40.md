# IRIS Audit And Update Plan 31-40

Status: English `31-40` audit and retrofit complete (March `2026`). Lever-keyed IRIS bridge rotation is applied across the tranche, targeted governance-cluster cross-links are in place, and the tranche now reads as a differentiated continuation of the mature IRIS standard.  
Strategy source of truth: `Blogs/_SYSTEM/strategy/DBR77_PRODUCT_MARKETING_PLAN.md`  
Tracker source of truth: `Blogs/_SYSTEM/strategy/MASTER_COMPLETION_PLAN.md`

## Purpose

This file applies the same audit rubric family as `Blogs/IRIS/Blog/00_AUDIT_AND_UPDATE_PLAN_21_30.md` to the next AI-assisted operations cluster (`31-40`: twin pairing, operational data, downtime loops, decision automation, workforce impact, watch-advise-act, governance, scaling, approvals, ninety-day review). Use it to classify each article, spot tranche-level patterns, and sequence retrofit work without reopening article bodies in the audit pass itself.

## Audit Rubric (same family as IRIS 01-20 and 21-30)

Score each article on:

1. `Knowledge-layer fit` - one clear layer among Field Reality, Problem Deep Dive, Solution Logic, Decision Support, Execution And Transformation; strengthens IRIS without generic platform drift.
2. `Trust and proof` - field-pattern language, trade-offs, reality checks; avoids inflated category claims.
3. `Article arc` - problem, what happens, mistake or reality check, what works, how DBR77 approaches it, outcome logic.
4. `Operational specificity` - production, warehouse, quality, maintenance, cross-functional loops.
5. `Maturity and tone` - calm, industrial, proof-bearing, non-startup.
6. `System fit` - persona, funnel stage, LP attachment logic.

## Article Classification Table

| Article | Strongest layer fit | Classification | Why | Key update action |
|---|---|---|---|---|
| `31_how_ai_and_digital_twin_work_together_in_factory_decisions` | Solution Logic | keep | Layer table (twin, AI, execution), five-step handoff, twin-only versus AI-only versus paired framing; strong operational bridge to thresholds and tasking | bridge rotation complete; scope boundary added versus general twin-category framing |
| `32_why_ai_without_operational_data_still_fails_in_manufacturing` | Problem Deep Dive | keep | Minimum credible object set, clean-history-dirty-present pattern, readiness checklist, reporting-grade versus execution-grade table | bridge rotation complete; pointer added to `33` |
| `33_how_ai_can_reduce_downtime_when_response_loops_exist` | Solution Logic | keep | Six-part downtime loop, leverage list with human-confirm caveat, readiness checklist, dashboard versus loop culture table | bridge rotation complete; pointer added to `21` |
| `34_the_rise_of_decision_automation_in_manufacturing` | Field Reality | keep | Physical versus decision automation table, candidate traits, four-level maturity ladder, theater versus real signals; title is slightly trend-forward but body stays sober | bridge rotation complete; links added to `36` and `39` |
| `35_what_factory_jobs_change_first_in_ai_assisted_operations` | Execution And Transformation | keep | What does not change first, role map table, skills list, training sequence, task replacement versus workload reshaping | bridge rotation complete; pointer added to `36` for training modes |
| `36_when_ai_should_watch_advise_or_act_in_the_factory` | Decision Support | keep | Three modes with definitions, use-when, proof bars; decision matrix for starting mode; explicit handoff to risk-class rights (`26`) | bridge rotation complete; governance cluster links added to `37` and `39` |
| `37_how_to_govern_ai_decisions_across_shifts_and_functions` | Decision Support | keep | RACI grid for rule changes, shift handoff fields, function arbitration, two-track change control, drift metrics | bridge rotation complete; links added to `36` and `38` |
| `38_how_to_scale_ai_assistance_without_losing_operational_control` | Execution And Transformation | keep | Expansion caps, four control drills, weekly scorecard table, viral versus bounded waves contrast | bridge rotation complete; links added to `30` and `40` |
| `39_what_a_human_approval_policy_should_look_like_in_factory_ai` | Decision Support | keep | Publishable policy skeleton (scope, matrix, evidence, timers, coverage, training), operational checklist | bridge rotation complete; links added to `26`, `36`, and `37` |
| `40_how_to_review_ai_assisted_operations_after_the_first_90_days` | Execution And Transformation | keep | Half-day agenda, evidence pack, four forced decisions, narrative versus operational retro table, short action list rule | bridge rotation complete; links added to `38` and `30` |

## Strongest Internal Style References (within IRIS 31-40)

Use these as internal benchmarks when editing this tranche or aligning neighbors:

- `31` - three-layer table plus numbered handoff pipeline (closest to `21` for proof density).
- `36` - mode definitions with observable proof per mode plus starting matrix (extends watch-advise-act vocabulary for the whole library).
- `37` - paired grids (RACI plus handoff fields) and explicit drift metrics.
- `39` - policy-as-workflow-artifacts structure (sections and matrix, auditor-grade language).
- `40` - forced decision slots and evidence gate before the meeting (operational retro, not slides).

For cross-tranche anchors, keep using `Blogs/IRIS/Blog/21_how_ai_is_changing_factory_operations_when_execution_is_connected/article_EN.md` as the mature IRIS body template (direct answer, comparison table, blunt checklist).

## Tranche-Level Findings

### Mature style versus 21-30

English `31-40` matches the post-standard IRIS body quality visible in `21`: direct answers, tables or checklists, explicit failure modes, and anti-theater lines (`This article avoids magic percentages`, `Policy is boring on purpose`). This tranche is not a step down from `21-30` in substance; it reads as a continuation of the same operating-system narrative (twin handoffs, data spine, loops, modes, governance, scale, policy, review).

### Repeated IRIS closing pattern

At audit time, all ten English bodies used the same first sentence in `## Why IRIS ...`: `DBR77 IRIS is an AI-native plant operating system with a unified execution layer across production, warehouse, quality, maintenance, and tasking.` Only the second sentence varied by topic. This mirrored the pre-retrofit state described for `21-30` in `00_AUDIT_AND_UPDATE_PLAN_21_30.md`. That tranche-wide sameness has now been removed through bridge de-templating, not full body rewrite.

### Structural repetition

Same library skeleton as prior tranches: front matter, `**Direct answer:**`, scoped H2 sections, `## Why IRIS ...`, `## Final takeaway`. Coherent for the knowledge base; risk is reader fatigue if IRIS blocks stay identical.

### Overlap inside 31-40 (editorial clusters)

- **Governance and control:** `34` (decision automation framing), `36` (modes), `37` (shift and function governance), `38` (scale and control), `39` (approval policy), `40` (ninety-day review). Complementary if each article keeps its primary object (automation definition, mode pick, rule RACI, expansion caps, policy text, review agenda). Light cross-link sentences reduce perceived duplication.
- **Execution and data:** `31` (twin to execution handoff), `32` (operational data objects), `33` (downtime loop). Differentiate with one sentence each on scope: scenario targets versus record spine versus stop-time loop.
- **People and rollout:** `35` (jobs and training), `38` (scale), `40` (review). Pair `35` with `36` for training on modes; pair `40` with `38` for scorecard continuity.

### Overlap with IRIS 21-30 (boundary nudges for future retrofit)

- `33` and `21`: both stress closed loops and execution connection; one cross-link is enough.
- `36`, `39` and `26`: decision rights and approvals; reference, do not merge.
- `38`, `40` and `30`: rollout, control, and post-pilot review; optional pointers.

### Historical production-note lines (legacy production instructions in body)

Repo search on English `article_EN.md` files for articles `31` through `40` found no matches for patterns such as the historical body-instruction compound (both casings), `[CTA]`, `TODO:`, `production note`, `FIXME`, or other obvious CTA-, TODO-, FIXME-, or production-note-shaped tokens in those article paths. CTAs live in package files such as `cta.md`, not in the English article bodies. Treat this tranche as clean on legacy body-instruction markers unless a future pass uses stricter patterns.

## Top Priority Update Articles

Ordered for practical impact (retrofit, not audit):

1. `[done]` Tranche-wide lever-keyed `Why IRIS` bridge rotation for `31-40` English bodies, preserving calm industrial tone (mirror completed work for `21-30`).
2. `[done]` Short cross-links among `34`, `36`, `37`, `38`, `39`, `40`; light pointers from `33` to `21`, `32` to `33`, `38`/`40` to `30`.
3. `[done]` One boundary sentence in `31` versus broad Digital Twin category framing.

No heavy rewrite candidates in `31-40` under this rubric. No article is classified as `heavy rewrite`. None require `light rewrite` of full bodies; the main gap is packaging (IRIS bridge templating), same as `21-30` before retrofit.

## Package Readiness Notes

- Bodies are publish-ready in substance for calm, industrial, proof-bearing positioning.
- The primary package gap at audit time was the templated IRIS bridge first sentence across all ten articles, and that gap is now closed.
- `34` headline uses `The rise of` (trend cadence); the body stays grounded; optional future title or subhead tweak is editorial, not required for substance.

## Recommended Update Sequence

1. `[done]` Draft lever-keyed bridge variants per sub-cluster: twin and data (`31`, `32`, `33`), automation and modes (`34`, `36`), governance and scale (`37`, `38`, `39`), workforce and review (`35`, `40`).
2. `[done]` Apply bridges in one consistency pass to avoid half-templated tranche state.
3. `[done]` Add short cross-link sentences in the governance cluster and at `21`/`30` boundaries as above.
4. `[done]` Re-read tranche for tone drift versus `Blogs/IRIS/Blog/21_how_ai_is_changing_factory_operations_when_execution_is_connected/article_EN.md`.

## Done Standard For This Tranche (retrofit)

Treat IRIS `31-40` English retrofit as complete when:

- `[x]` IRIS bridge blocks are topic-tied and not copy-paste identical across the ten articles (first sentence must not repeat verbatim everywhere).
- `[x]` governance cluster has light cross-links that state scope in one line each.
- `[x]` no inline production or CTA instructions appear in bodies (current state maintained).

## Layer Guidance For Adjacent Work

When auditing or retrofitting IRIS `41-50`, keep reinforcing:

- audit-ready records and exception models (`46` neighborhood),
- vendor versus execution layer boundaries (`48`),
- accountability in shift management (`49`),
- full closure definition (`50`),

and preserve continuity with `31-40` on modes, governance grids, and ninety-day operational reviews.
