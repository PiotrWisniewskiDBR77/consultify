# Marketplace Audit And Update Plan 33-42

Status: Marketplace English `33-42` audit and retrofit complete.  
Strategy source of truth: `Blogs/_SYSTEM/strategy/DBR77_PRODUCT_MARKETING_PLAN.md`  
Tracker source of truth: `Blogs/_SYSTEM/strategy/MASTER_COMPLETION_PLAN.md`

## Purpose

This file applies the same audit rubric family as `00_AUDIT_AND_UPDATE_PLAN_01_12.md`, `00_AUDIT_AND_UPDATE_PLAN_13_22.md`, and `00_AUDIT_AND_UPDATE_PLAN_23_32.md` to English Marketplace articles `33` through `42`.

Use it to decide:

- which articles are already publish-strong on body content
- which need a light rewrite (if any)
- which need a heavier rewrite (if any)
- which cross-article patterns should be fixed as a package
- what to execute next after this audit

## Audit Rubric

Score every article against these dimensions (aligned with Marketplace `01-32`):

1. Knowledge-layer fit  
   - does the article clearly serve one of the `5` product knowledge layers (`Field Reality`, `Problem Deep Dive`, `Solution Logic`, `Decision Support`, `Execution And Transformation`)  
   - does it strengthen Marketplace positioning (sourcing, comparability, trust, delivery handoff) without drifting into generic automation blogging

2. Trust and proof  
   - practical sourcing, comparison, risk, and buying-workflow logic  
   - avoids inflated claims and vague automation rhetoric

3. Article arc  
   - real problem  
   - what actually happens  
   - common mistake or reality check  
   - what works  
   - how DBR77 approaches it (Marketplace bridge)  
   - outcome logic (bottom line)

4. Operational specificity  
   - close to plant friction, procurement mechanics, vendor comparison, acceptance, and document discipline

5. Maturity and tone  
   - calm, experienced, specific, manufacturer-first, sourcing-safe

6. System fit  
   - persona, funnel stage, and LP attachment logic remain credible

## Article Classification

| Article | Strongest layer fit | Classification | Why | Key update action |
| --- | --- | --- | --- | --- |
| `33_how_to_choose_the_right_internal_owner_for_an_automation_project` | Decision Support | keep | owner archetypes table, decision checklist, steering cadence, and good versus bad supplier-dialogue signals are governance-grade; direct answer is crisp | optional lever-key polish on Marketplace closing only (`33` already avoids the shared catalog disclaimer line; keep it distinct or align opener to job without pasting `34-42` template) |
| `34_when_to_run_a_paid_discovery_phase_before_full_automation_award` | Decision Support | keep | three trigger sets with concrete plant signals, paid-package structure, and weak versus strong discovery table match procurement reality | lever-keyed Marketplace closing; rotate or replace verbatim **not a robot catalog** line; cross-link to `29` (site visits) and `22` (momentum) where one sentence prevents duplicate reading |
| `35_what_change_order_risk_to_check_before_an_automation_project_starts` | Decision Support | keep | four rails, posture table, and one-hour pre-kickoff protocol are contract-adjacent and baseline-strong | lever-keyed Marketplace closing; cross-link to `20` (contract checks) and `32` (reopen before signing); optional nudge to `30` for handoff continuity |
| `36_how_to_set_acceptance_criteria_before_automation_delivery_begins` | Execution And Transformation | keep | object versus activity split, evidence table, dependency sequence, and approver map mirror commissioning reality | lever-keyed Marketplace closing; cross-link to `25` (FAT/SAT meaning), `17` (offer visibility), and `30` (selection-to-delivery handoff) |
| `37_when_an_incumbent_supplier_should_not_win_the_next_automation_project` | Decision Support | keep | four signals, pattern table, and bounded gate keep award logic inspectable without anti-relationship tone | lever-keyed Marketplace closing; cross-link to `23` (references), `24` (shortlist), and `27` (single sourcing) for sequencing |
| `38_how_to_keep_procurement_speed_without_losing_technical_quality` | Decision Support | keep | clarification log, comparability fields, deep-dive discipline, and reviewer calendar address speed-versus-quality without spreadsheet theater | lever-keyed Marketplace closing; cross-link to `14`/`15` (brief and RFQ), `22` (post-meeting momentum), optional `16` (supplier archetypes) |
| `39_what_a_good_internal_business_case_for_automation_should_make_visible` | Decision Support | keep | assumption ledger, visible elements, and pause rules align CFO and plant truth; directional-model framing is honest | lever-keyed Marketplace closing; cross-link to `31` (TCO) for assumption and evidence alignment |
| `40_how_to_prepare_operations_for_automation_go_live_before_installation_starts` | Execution And Transformation | keep | parallel readiness framing, tiered risk windows, and thirty-day ownership block match adoption reality | lever-keyed Marketplace closing; cross-link to `25`, `30`, and `36` for acceptance and handoff thread |
| `41_when_to_bundle_multiple_automation_needs_into_one_buying_process_and_when_not_to` | Decision Support | keep | coupling dimensions, comparability integrity test, and bundle versus split grid are portfolio-practical | lever-keyed Marketplace closing; cross-link to `21` (scoping) and `24` (supplier count and process design) |
| `42_how_to_run_a_final_internal_alignment_review_before_automation_kickoff` | Decision Support | keep | timeboxed agendas, mandatory one-page record, and kickoff delay rule are pre-mobilization strong | lever-keyed Marketplace closing; cross-link to `19` (pre-buy alignment), `30` (handoff), `35` (change-order rails), optional `32` |

No article in this band is classified **heavy rewrite** at audit time. Bodies match mature Marketplace style: direct answers, tables, bounded protocols, and calm manufacturer-first voice. The main gap versus the retrofitted `13-32` English state is **packaging repetition in Marketplace closing sections** for nine of ten articles.

## Strongest Internal Style References

When editing Marketplace English in this pre-kickoff and readiness band, benchmark against:

1. `36_how_to_set_acceptance_criteria_before_automation_delivery_begins/article_EN.md` for acceptance objects, evidence language, sequencing, and cross-function signatory mapping  
2. `35_what_change_order_risk_to_check_before_an_automation_project_starts/article_EN.md` for baseline rails, commercial posture fit, and bounded internal protocol  
3. `42_how_to_run_a_final_internal_alignment_review_before_automation_kickoff/article_EN.md` for timeboxed final gate, non-negotiable artifact, and escalation thresholds  
4. `38_how_to_keep_procurement_speed_without_losing_technical_quality/article_EN.md` for comparability-preserving speed rules and anti-pattern replacement table  
5. `41_when_to_bundle_multiple_automation_needs_into_one_buying_process_and_when_not_to/article_EN.md` for portfolio coupling logic and inspectability under a bundle  

Continue to treat `23-32` benchmarks (`25`, `30`, `31`, `20`, `19`, `22`) as adjacent style anchors for acceptance, handoff, TCO, contract, alignment, and meeting momentum.

## Recurring Issues In This Tranche

1. **Repeated Marketplace closing pattern**  
   Articles `34` through `42` each end `## What this means for DBR77 Marketplace` with a short bridge, then the line **Marketplace is not a robot catalog.**, then a follow-on sentence. The line is **verbatim identical across those nine** English bodies. Article `33` does **not** use that line; its Marketplace section is shorter and already somewhat job-specific. This matches the templating risk documented for `23-32` in `00_AUDIT_AND_UPDATE_PLAN_23_32.md` (since retrofitted there). Fix as a **single tranche pass**: lever-keyed openers and closings per article job (internal owner, paid discovery, change orders, acceptance, incumbent, procurement speed, business case, operations readiness, bundling, final alignment).

2. **Conceptual adjacency (intentional overlap)**  
   - `36` with `25`, `17`, and `30` on acceptance and offer comparability.  
   - `35` with `20` and `32` on baseline, contract, and late decision gates.  
   - `34` with `29` and `22` on discovery inputs and process momentum.  
   - `39` with `31` on economics visibility and assumption discipline.  
   - `37` with `23`, `24`, and `27` on evidence and sourcing path.  
   - `40` with `25`, `30`, and `36` on execution readiness.  
   - `41` with `21` and `24` on scope shape and supplier-count logic.  
   - `42` with `19`, `30`, and `35` on alignment, handoff, and pre-start risk.

3. **Maturity relative to older Marketplace waves**  
   This band is consistently strong on operational specificity, tables, and buyer-safe language. It reads as the same mature generation as `23-32` on body copy.

4. **Tone drift**  
   No material tone drift observed in body copy: voice stays calm, specific, and non-startup across the band.

5. **Inline production notes**  
   Repo search on English `article_EN.md` paths for folders `33_*` through `42_*` found **no** matches for the historical body-instruction compound (both casings), `[CTA`, `TODO`, `FIXME`, or `production note` (same pattern family as prior Marketplace audits).

## Top Priority Update Work

If execution time is limited, do this order:

1. `[done]` Tranche-wide lever-keyed rewrite of `## What this means for DBR77 Marketplace` sections across English `33-42`, including rotation or replacement of the shared **not a robot catalog** line in `34-42` so closings are topic-tied and not copy-paste identical. `33` received light alignment while staying distinct.  
2. `[done]` Light cross-link pass across the adjacency clusters listed above so one sentence reduces duplicate reading without noisy circular linking.  
3. `[done]` Persona and funnel-stage header lines still read true after bridge edits; no body rewrites were required for classification.

## Update Sequence For Marketplace 33-42

1. Marketplace bridge rotation pass for English `33-42` is complete.  
2. Targeted cluster cross-links were added after bridge stabilization.  
3. Sequence next English work using Marketplace `43-50` audit backlog and `Blogs/_SYSTEM/strategy/MASTER_COMPLETION_PLAN.md`.

## Done Standard For This Tranche Retrofit

Treat Marketplace English `33-42` retrofit as complete when:

- Marketplace closing sections are topic-specific and not identical across the band (including elimination of verbatim shared catalog disclaimer where replaced by job-bound language)  
- light cross-links support reading order without noisy circular linking  
- layer fit remains clear for each article  
- tone stays calm, practical, sourcing-safe, and proof-bearing end-to-end  
- no inline production-note residue appears in publish-ready English bodies
