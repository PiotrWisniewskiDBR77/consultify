/**
 * LEGACY ALIAS
 *
 * This file used to contain an older My Work router implementation.
 * To avoid route duplication and drift, it now re-exports the canonical router.
 *
 * Canonical SSOT: `server/src/routes/my-work.routes.ts`
 */
import myWorkRoutes from './my-work.routes.js';

export default myWorkRoutes;
