import express from 'express'; import request from 'supertest'; import { beforeEach, describe, expect, it, vi } from 'vitest'; import routes from '../admin/health-panel.routes.js';
const dbGet=vi.fn(); const dbAll=vi.fn(); let user:any={id:'u1',organizationId:'org-1',role:'admin'};
vi.mock('../../utils/DbPromise.js',()=>({get:(...a:any[])=>dbGet(...a),all:(...a:any[])=>dbAll(...a)}));
vi.mock('../../middleware/auth.middleware.js',()=>({verifyToken:(req:any,res:any,next:any)=>{if(!user)return res.status(401).end();req.user=user;next();}}));
vi.mock('../../middleware/admin.middleware.js',()=>({default:(_req:any,_res:any,next:any)=>next()}));
vi.mock('../../services/health/healthProbeService.js',()=>({HEALTH_PROBES:[],getCachedResults:vi.fn().mockResolvedValue([]),summarizeResults:vi.fn().mockReturnValue({}),isHealthPanelAllowedEnv:vi.fn().mockReturnValue(false),getProbeById:vi.fn(),runAllProbes:vi.fn(),runProbe:vi.fn(),cacheProbeResult:vi.fn()}));
const app=()=>{const a=express();a.use('/api/admin/health-panel',routes);return a;};
describe('health jobs route',()=>{beforeEach(()=>{vi.clearAllMocks();user={id:'u1',organizationId:'org-1',role:'admin'};dbGet.mockResolvedValue({role:'ADMIN',status:'ACTIVE'});dbAll.mockResolvedValue([]);}); it('requires auth',async()=>{user=null;expect((await request(app()).get('/api/admin/health-panel/jobs')).status).toBe(401);}); it('requires admin membership',async()=>{dbGet.mockResolvedValue({role:'MEMBER',status:'ACTIVE'});expect((await request(app()).get('/api/admin/health-panel/jobs')).status).toBe(403);}); it('uses token tenant and exposes no platform fields',async()=>{const response=await request(app()).get('/api/admin/health-panel/jobs?status=failed&limit=999');expect(response.status).toBe(200);expect(dbAll).toHaveBeenCalledWith(expect.stringContaining('WHERE organization_id = ?'),['org-1','failed',200,0],{fallback:false});expect(JSON.stringify(response.body)).not.toMatch(/host|cpu|memory|credential/i);});});
