Implementacja ROI-E001 (Case & Baseline) wg `docs/product/results-vnext/ROI_E001_DESIGN.md`. Status: schema (`server/migrations/20260815_rvn_roi_core.sql`), domenowy kod (`roiCaseCommands.ts`/`roiBaselineCommands.ts`/`roiRepository.ts`/`roiTypes.ts`) i warstwa API (`server/src/routes/resultsVnext/roi.routes.ts`, zamontowana w `Gateway.ts` pod `/api/vnext/results/roi`) landed i WPIĘTE. Zbudowane na fundamencie `platform/*` (RN-G1) i strukturalnym wzorcu domeny KPI (`kpi/kpiDefinitionCommands.ts`, `kpi/kpiDeviationCommands.ts`'s SAVEPOINT dedupe pattern, `kpi/kpiRepository.ts`). Poza zakresem tego epika (przyszłe ROI-E002…E008): economic model (Assumption/Cost/Benefit/Scenario/CalculationRun), Submit/Approve/Reject (`freezeRoiBaseline` jest już tu, cross-epic contract dla przyszłego `approveRoiCase`), Tracking/Benefits Realization, PIR, Finance seam, Teresa/legacy.
