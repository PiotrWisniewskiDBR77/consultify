Implementacja OKR-E001 (Program & Cycle) wg `docs/product/results-vnext/OKR_E001_DESIGN.md` — pierwszy epik domeny OKR, zupełnie nowej domeny w tym programie. Status: schema (`server/migrations/20260822_rvn_okr_program_cycle.sql`, 4 tabele `okr_vnext_*` — Decyzja P1: plik migracji `rvn_okr_`, tabele `okr_vnext_`), domenowy kod (`okrProgramCommands.ts`/`okrCycleCommands.ts`/`okrCycleScheduler.ts`/`okrRepository.ts`/`okrProgramTypes.ts`/`okrCycleTypes.ts`) i warstwa API (`server/src/routes/resultsVnext/okr.routes.ts`, zamontowana w `Gateway.ts` pod `/api/vnext/results/okr`) landed i WPIĘTE. Zbudowane na fundamencie `platform/*` (RN-G1) i strukturalnym wzorcu ROI-E001 (najświeższy precedens "pierwszy epik domeny") — z jedną świadomą różnicą (Decyzja P2/P4): brak wizerunku ABAC dla Program/Cycle, zapis chroniony `requireOrgRole('admin','superadmin')`, nie `resolveVisibility()`. `publishProgram` jest pierwszym product-facing writerem `rvn_platform_visibility_policies` (nowy prymityw `publishVisibilityPolicy` w `platform/visibilityResolver.ts`, Decyzja P5). Poza zakresem tego epika (przyszłe OKR-E002…E008): `okr_vnext_sets`/Objective/KeyResult/Alignment/CheckIn/Review/Reflection — `'okr_set'` w `RVN_RESOURCE_TYPES` i `okr_set.published` w `EVENT_TYPE_CONSUMER_GROUPS` pozostają nietknięte, zarezerwowane dla OKR-E002. Scheduler (`okrCycleScheduler.ts`) to czyste, w pełni testowane funkcje wywoływalne bezpośrednio (Decyzja P10) — brak realnego wpięcia do crona, tak samo jak `outboxDrain.ts`.
