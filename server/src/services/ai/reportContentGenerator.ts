/**
 * Report Content Generator
 *
 * Not implemented in this codebase. Export an explicit marker instead of a self-loading wrapper.
 */
const reportContentGenerator = { __unavailable__: true } as const;

export default reportContentGenerator;
