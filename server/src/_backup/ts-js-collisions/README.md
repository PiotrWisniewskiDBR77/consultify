# Backup: TS/JS collision moves

This directory stores `.js` (and “copy” files like `file 4.js`) that were moved out of `server/src/` runtime paths to ensure `tsx` dev runs the TypeScript source of truth.

Do not delete these files without confirming they are not referenced anywhere.

