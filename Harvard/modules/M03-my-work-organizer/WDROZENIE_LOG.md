# M03 — Moja Praca — organizer — Log wdrożenia

Wypełniany PO audycie, w trakcie realizacji planu dokończenia (Faza 8 karty).

| Data | Fala | Pozycja planu | Commit | Weryfikacja (dowód) | Status |
|---|---|---|---|---|---|
| 2026-06-12 | **FAZA 4 (żywa, staging)** | S1 smoke — Inbox landing | — | **PASS**: `/my-work` renderuje Inbox z **310 realnymi pozycjami** ze staging (Zaległe 132, Krytyczne 56, Wymaga akcji 230); zakładki Inbox/Kalendarz/Zadania/Decyzje/Manager + Pomysły/Notatnik; zero crashy. FE+BE+DB live. **Pending:** quick-action+reload persist (S1 pełne), Kanban DnD (S2). | ZROBIONE (D smoke) |
| 2026-06-12 | **FAZA 4-deep (API)** | S1/S2/S3/S4 — API scenario verification | — (clean, brak bugów) | **S1 PASS**: `/api/my-work/inbox` 200 `{summary:{total:0,...},items:[]}` (org-scoped czysto); `/api/my-work/inbox/canonical` 200; `/api/my-work/inbox/sla/refresh` 200 `{updated:39}`; `/api/my-work/signals` 200 1 sygnał z poprawnymi typami (key/type/severity/title/message=string, createdAt=string). **S2 PASS**: `/api/my-work/tasks` 200 `[]`; `/api/my-work/personal-tasks` 200 `[]`; `/api/my-work/decisions` 200 `[]`; `/api/my-work/decisions/queue` 200 `{items:[],nextCursor:null,mode:"my"}`. **S3 PASS (CRUD full)**: POST `/personal-tasks` 201 `{id,title,status,priority,dueDate,tags,createdAt,updatedAt,...}` typy poprawne; GET by id 200 trwała; DELETE 204 → GET 404 ✓. **S4 PASS**: `/api/my-work/my-ideas` 200 2 ideas, org-scoped, `isFavorite=bool ✓`, `priority=int ✓`; `/api/my-work/my-ideas/suggest` 200; POST idea 201 + DELETE 204 ✓. **Bigint audit**: `?`→`$n` i `datetime('now')`→`NOW()` konwertowane przez PostgresDatabase adapter (linie 372/383) — nie bugi. `task_automation_rules.priority BIGINT` nie w SELECT → brak serializacji. `automation-rules is_active boolean` → `Boolean()` ✓. **Obserwacja**: `/api/my-work/home` → 404 (endpoint nie zaimplementowany); `/api/my-work/radar` → 404 (stub). Zero crashy, zero data-type bugów. | ZROBIONE (API-deep, czyste) |
