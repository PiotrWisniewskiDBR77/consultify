# M14 — Wdrożenie — Log wdrożenia

Wypełniany PO audycie, w trakcie realizacji planu dokończenia (Faza 8 karty).

| Data | Fala | Pozycja planu | Commit | Weryfikacja (dowód) | Status |
|---|---|---|---|---|---|
| 2026-06-12 | Fala 2 [INTEGRACJA] | #6 — eksport sygnałów ROI do M15 + deep-link Execution→Results | _ten commit_ | Nowy `executionResultsBridge.ts`: `budget_health` sygnał do `v8_kpi_signals` (KPI z `initiative_id`, dedup pending, AMBER=medium/RED=critical) wpięty w `createBudgetEntry`(ACTUAL)+`deleteBudgetEntry` (fire-and-forget); `budget_health` dodany do `KpiSignalTypeValues`; FE: `BudgetControlPanel` widok inicjatywy → przycisk „Zobacz w Rezultatach" → `/benefits?initiativeId=` (ResultsHub czyta param, `ResultsHub.tsx:146`); i18n PL/EN; testy 6/6 PASS (`executionResultsBridge.test.ts`), resultsROI 99/99, tsc zielony. Żywy dowód (zmiana budżetu → sygnał w M15 UI) = FAZA C | ZROBIONE (kod) / żywa weryfikacja PENDING |
| 2026-06-12 | **FAZA 4 (żywa, staging)** | S1 smoke — Executive dashboard | — | **PASS**: `/implementation` renderuje Zestawienie/Wdrożenie/Raportowanie/Zarządzanie z **realnymi danymi** (51 inicjatyw, 9 zablokowanych, 20 brak dat); zero crashy. FE+BE+DB live. **Znalezisko (deep-link #6):** przycisk „Zobacz w Rezultatach" → `/benefits` → redirect `/chat`, bo `MODULE_BENEFITS:'closed'` (`betaAccess.ts:39`). Deep-link strukturalnie poprawny; do otwarcia Rezultatów beta albo oznaczenia przycisku. | ZROBIONE (D smoke) |
| 2026-06-17 | **Fala Faza 2 — L-04 martwy kod + L-08 testy** | L-04 (D-02→DP-7) + L-08 (S2 kanban-DnD + S6 raporty) | `a493d1ccd9` + `acdec5339d` | **L-04 NAPRAWIONA:** 4 martwe komponenty (PeopleChangeWorkspace, RiskSignalsPanel, DelayDetectionPanel, ReportCompactPanel) + 3 testy — grep src/ = 0 referencji; barrel `Execution/index.ts` oczyszczony; −2356 linii. **L-08 NAPRAWIONA:** S2 kanban-DnD (`executionInitiativesKanban.dnd.test.tsx`) 5/5 PASS — onStatusChange/readOnly/card-on-card; S6 raporty live-data (`reportContentGenerator.liveData.test.ts`) 10/10 PASS — RAG/risks/summary/polish/markdown. **Bonus:** `ai-feedback.routes.ts` + `ai-nudges.routes.ts` — `notConfigured()` ← `code:'FEATURE_UNAVAILABLE'`; testy aiFeedback/aiNudges isolated via `vi.doMock()` (4/4 PASS) `d755f85981`. | ZROBIONE (L-04 + L-08 zamknięte) |
| 2026-06-12 | **FAZA 4-deep (API)** | S1/S2/S3/S4/S5 — API scenario verification | `c4bfe757ca` | **S1 PASS**: healthScore=53 dynamiczny z realnych danych (różny per projekt), endpoint `/api/execution/:projectId/health`. **S2 PASS**: 42 inicjatywy z pełną strukturą. **S3 PASS**: Action Queue 23 items, filtruje po projekcie, 5 typów zdarzeń. **S4 PARTIAL**: RAID OK; Decisions: `blockedItemsCount` był stringiem→naprawiony na `Number()` (commit `c4bfe757ca`). **S5 PASS**: 4 Rollout subroute aktywne, tabele zmigrowały. **Bug naprawiony:** BUG-M14-01 (`blockedItemsCount` bigint→string `'0'`→ `Number()`). **Obserwacja:** budgetHealth=0 zamiast null przy braku danych (semantyczny, low). | ZROBIONE (API-deep) |
