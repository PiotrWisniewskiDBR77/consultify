# M01 — Czat — Log wdrożenia

Wypełniany PO audycie, w trakcie realizacji planu dokończenia (Faza 8 karty).

| Data | Fala | Pozycja planu | Commit | Weryfikacja (dowód) | Status |
|---|---|---|---|---|---|
| 2026-06-11 | quick-fix (Krok 4, reguła 2) | Fala 1 #4 — crash hasła share | _ten commit_ | `share.routes.ts:592` `hashPasscode`→`await scryptHash` (niezdefiniowana funkcja → ReferenceError); zgodne z wzorcem :304/:417 | ZROBIONE |
| 2026-06-12 | **FAZA 4 (żywa)** | Scenariusze krytyczne S1, S2 | — (weryfikacja) | **S1 PASS** (live, staging): nowa rozmowa `/chat/698823ea…` → prompt → **streaming SSE** (Teresa odpowiedziała + grounding „10 sources") → **reload → obie wiadomości trwałe**. **S2 PASS**: „/" → menu slash-komend (Utwórz tabelę/Generuj obraz…), intercept intencji żywy. Smoke: render OK, `/api/conversations` 200, zero błędów konsoli. **Pending:** S3 (załącznik), S5 (share/revoke), S6 (Canvas handoff), S7 (głos). | ZROBIONE (D częściowa) |
| 2026-06-12 | **FAZA 4-deep (żywa)** | S5 share/revoke + 2 bugi prod | `9cd8fd0d2f` | **S5 ✅ PASS** (live): create 200 → public view 200 (wiadomości renderują) → revoke 200 → re-view **410**. Naprawione 2 realne bugi prod (`share.routes.ts`): (1) publiczny widok 500 — `JSON.parse` na JSONB metadata → `parseMaybeJson`; (2) **SECURITY** — `is_active` bigint→string `"0"` truthy → rewokowane share dalej dostępne → `flagOn`. Patrz [[finding_pg_bigint_jsonb_serialization]]. | ZROBIONE |
| 2026-06-12 | **FAZA 4-deep (żywa)** | S3 ingest załącznika | — (weryfikacja) | **S3 ingest ✅ PASS** (live): `POST /api/ai/attachments/ingest` (CSV) → **201**, `extractionStatus:"extracted"`, `totalChunks:1`, wiersze `knowledge_docs`+`knowledge_chunks` zapisane, org-scoped. `embeddedChunks:0` = **limit env staging** (brak `OPENAI_API_KEY` → `generateEmbedding` zwraca `null`, czysta degradacja, chunk i tekst zapisane); retrieval-half wymaga klucza embeddings (prod ma). **Retrieval env-gated, nie bug.** | ZROBIONE (ingest) |
| 2026-06-12 | **FAZA 4-deep — SECURITY** | Cross-org write IDOR (rodzina hard-cap P0) | `f847243008` | `POST /api/ai/memory/project/:projectId/decision` NIE miał guarda org (GET/DELETE miały) → user z org A mógł wstrzyknąć pamięć decyzji do projektu org B (zatruwa grounding czatu). Dodany guard org (404/403, `String()`-coerced); `recordDecision→recordProjectMemory` wstawiał bez tenant-checku. **Live: POST na nieistniejący projekt → 404 przed zapisem.** Domyka ostatnią krawędź IDOR-a (read+delete już były). | ZROBIONE |
