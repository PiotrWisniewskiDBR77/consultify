# M16 „Finanse" — AUDYT WDROŻENIOWY (2026-06-24)

> Audyt analogiczny do M14: identyfikacja REALNYCH funkcji (kod + żywa weryfikacja, nie dokumentacja) jako podstawa do programu budowy/dokończenia. Ugruntowany na żywym module (org a3e05d4a, staging-trolley) — nie na starym audycie.

## STATUS PRAWDY (zweryfikowane live 2026-06-24)
- Żywy moduł = route `/finance` → `src/views/EconomicsView.tsx` (wrapper, 25 lin.) → **`src/components/Economics/FinanceHub.tsx` (2382 lin.)**. Beta-gate `MODULE_ECONOMICS` (closed) + `ProductionModuleGate` + runtime flag `useV8FeatureFlag('finance')`.
- **Moduł JEST zbudowany i DZIAŁA live** dla org a3e05d4a: renderuje 6 zakładek, `/api/v8/finance/dashboard` → 200 z realnym pipeline (ingestion 2, avg confidence 0.91, gate-pass 100%, escalations 1, unlinked 86), tabela Statements pokazuje „DBR77 Manufacturing FY2025 PLN Approved 2 docs". **Stary audyt MODULE_08 = 47/100 jest MOCNO NIEAKTUALNY** (jak 06-03 przy M14).
- **Architektura = V8-first + legacy-fallback („split-brain")**: primary `/api/v8/finance/*` (`server/src/routes/v8/finance.routes.ts`, 2250+ lin., DB-backed, LLM-enhanced), fallback `/api/economics/*` (`economics.routes.ts`, 2503 lin.). `shouldFallbackToLegacyFinance(error)` przełącza na 404/501/400. To główny strukturalny dług.
- **Dane = SEED stagingowy, nie organiczne** (model „DBR77 Staging Finance Model — seeded to populate the Finance module", 1-2 statements). Czyli: kod żyje, ale NIE jest ograny realnym ruchem klienckim → ryzyko ukrytych braków na ścieżkach poza seedem.
- **Testy = praktycznie ZERO** (1 plik `financeModelLabels.test.ts`). To największa luka realizacyjna vs M14 (gdzie dobiliśmy ~330 testów).
- Prod (centerbeam) NIETKNIĘTY. SSOT split-brain: `evidence/144-v81-finance-split-brain-map.md`.

## INWENTARZ FUNKCJI — 6 zakładek (zweryfikowane: renderują się live)

| # | Zakładka | Komponent (plik) | Backend (endpoint) | DB | Stan realny |
|--|--|--|--|--|--|
| 1 | **Statements** (import Excel/PDF/CSV) | `Finance/FinancialStatementPackWorkspace` + `FinancialStatementImportWizard` | `POST /v8/finance/statements/upload-and-analyze` → `/detect` → `/extract` → `/map` → `/confirm` | financial_statement_packs/_statements/_lines/_mapping_candidates/_ingest_runs | 🟢 DB-backed, 4-krok wizard, LLM-mapping; live: 1 Ready statement, ale „—CF" (brak Cash Flow) + Escalations 1 |
| 2 | **Models** (modele finansowe) | `Economics/FinanceModelDocumentView` + `modals/CreateModelModal` | `GET/POST /v8/finance/models` · `/compute` · `/events` · `/validations` · `/outputs` · `/approve` | financial_models/_events/_outputs/_validations/_versions | 🟢 DB-backed + V8 engine (forecast P&L/BS/CF, walidacje balansu); live: 1 seed-model |
| 3 | **Analysis** (analiza finansowa) | `Benefits/FinancialAnalysisWorkspace` + `CreateAnalysisModal` | `GET/POST /v8/finance/analyses` · `/run` · `/ratios` · `/initiative-proposals` · `/initiatives` | financial_analyses/_ratios/_insights (legacy: digitization_analyses) | 🟡 DB-backed V8 + legacy fallback `/economics/analyses` (pusty live=0); ratios ROI/NPV/IRR; generuje inicjatywy→M13 |
| 4 | **Prediction** (modele + budżety) | `Benefits/BudgetWorkspace` + `FinanceModelDocumentView` | `GET /v8/finance/budgets` · legacy `/economics/budgets` | ai_budgets/budget_lines/budget_scenarios | 🟡 Hybryda V8+legacy; budżety działają; scenariusze budżetowe = legacy/placeholder (do potwierdzenia) |
| 5 | **Enterprise valuation** (wycena) | `Benefits/ValuationWorkspace` + `CreateValuationModal` | `GET /v8/finance/valuations` (read) + `POST/PUT /economics/valuations` (write) | valuations/valuation_scenarios | 🟡 Split: read V8, write legacy (DCF/multiples/WACC) — kandydat na konsolidację |
| 6 | **Investment analysis** | `Benefits/FinancialAnalysisWorkspace` (współdzielony, filtr `investment_case`) | jak Analysis (filtr typu) | jak Analysis | 🟡 Nie odrębny — filtr nad Analysis (NPV/IRR/payback/go-no-go) |
| + | **V8 Lane (P05)** | `FinanceLanePanel` | `POST /v8/finance/lane/start` · `/advance` · `/mutation-audit` · `/kpi-coherence` | finance_lane_runs (+audit) | 🟡 Eksperymentalny workflow import→analysis→mutation→readback; „Gate pass 100%" w UI |
| + | **V8 Dashboard** (command-row metryki) | FinanceHub command row | `GET /v8/finance/dashboard` | agregat | 🟢 Live: ingestion/linkage/escalations/gate-pass |

## RELACJE MIĘDZYMODUŁOWE
- **M16→M13**: `POST /v8/finance/analyses/{id}/initiatives` — analiza generuje inicjatywy (linkage). Live: **Unlinked 86** = 86 niepowiązanych pozycji (luka w domknięciu linkage).
- **M16↔M14/M15**: współdzielone workspace'y w `components/Benefits/` (BudgetWorkspace, FinancialAnalysisWorkspace, ValuationWorkspace) — finanse i benefity dzielą kod. KPI-coherence w V8 Lane.
- **FinancialEngine** (z Business Plan generatora [[project_business_plan_generator]]) — wspominany, ale NIE wyeksponowany jako route finansowy; do ustalenia czy/jak spięty.

## GŁÓWNE LUKI / DŁUG (kandydaci do programu budowy)
1. **🔴 ZERO testów** — moduł 2382-lin. + 2 route'y po ~2300 lin. praktycznie bez pokrycia. Najpilniejsze: unit (FinancialEngine/compute, ratios, mapping) + integration (route'y) + E2E (import→confirm, model→compute, analysis→initiatives).
2. **🟠 Split-brain V8/legacy** — dwa równoległe systemy (`v8/finance` vs `economics`). Trzeba: zmapować co tylko-legacy (valuations write, budget scenarios), zdecydować konsolidację/retire, usunąć martwe ścieżki (jak `Implementation/` w M14).
3. **🟠 Niedomknięty pipeline danych** — live: „—CF" (brak Cash Flow w statemencie), **Escalations 1** (nieobsłużona), **Unlinked 86** (linkage initiatives↔finance niedomknięty). To realne stany do obsłużenia w UI/logice.
4. **🟡 Dane tylko-seed** — żaden tab nie jest ograny realnym importem klienckim; ścieżki błędów (Rejected/Recovery Queue) niezweryfikowane live.
5. **🟡 Valuation/Investment/Prediction** — częściowo współdzielone/legacy; do potwierdzenia czy każda metoda (DCF/multiples/WACC, NPV/IRR, scenariusze budżetowe) realnie liczy, czy jest skorupą.
6. **🟡 Gating produkcyjny** — `MODULE_ECONOMICS` closed + V8-flag per-org: do ustalenia docelowa polityka dostępu (kto widzi finanse na prod).

## CO WYMAGA DECYZJI PIOTRA (zanim program ruszy)
- **D1 — Split-brain**: konsolidujemy do V8 i wycofujemy legacy `/economics/*`, czy utrzymujemy fallback? (rekomendacja: V8 jako SoT, legacy→retire po teście parytetu).
- **D2 — Zakres v1**: które z 6 zakładek są „must-finish" na v1 (rekomendacja: Statements+Models+Analysis rdzeń; Valuation/Investment/Prediction = pełne dokończenie w fali 2).
- **D3 — Dane testowe**: czy zaseedować realistyczny komplet (pełny P&L+BS+CF, kilka modeli/analiz) do live-verify, jak przy M14.
- **D4 — FinancialEngine**: czy spinamy z generatorem biznesplanu (jeden silnik liczbowy) czy zostają osobno.

## METODA (jak przy M14)
Pętla weryfikacji gotowa: lokalny FE :3000 → backend :3001 (trolley), org a3e05d4a (finance V8 ON, seed). Program: per-zakładka live-verify end-to-end → identyfikacja realnych braków → dobudowa za flagą/additive → testy (unit+integration+Playwright) → deploy demo. Bramki odbioru 8/8 jak w `M14-STAN-PRACY-ODBIORY.md`.

## NASTĘPNY KROK
Po decyzjach D1–D4 → zbuduję `M16-STAN-PRACY-ODBIORY.md` (tablica zbiorcza zadań w falach, analogicznie do M14) i ruszamy per-zakładka z live-verify + testami. Domyślnie (bez decyzji) zaczynam od **live-verify Statements→Models→Analysis end-to-end** (najpierw odkryć realne braki na rdzeniu, zanim dobudowujemy).
