# KRYTYK ADWERSARYJNY — sekcja B·Harvey (rejestr twierdzi 27✅/0🟡/0⬜/1🔵/28)

Metoda: `git worktree` na `origin/demo` (8e10f1c5b0), symlink node_modules, testy odpalone
NA ŻYWO przeciw parity Postgres (SSH tunel `localhost:5443`, potwierdzony `lsof`+seed.mjs OK),
env dokładnie wg zlecenia. Gdzie testy nie istniały na branchu roboczym (oxford/oc2-merge) —
sprawdzone w historii git i uruchomione z checkoutu demo.

## Tabela werdyktów

| HP-x / twierdzenie | Werdykt | Dowód |
|---|---|---|
| **HP-16** evidence 8/8 — teresa-six.e2e 7/7 | **POTWIERDZONE** | Uruchomione na parity :5443: `tests/acceptance/teresa-six.e2e.test.ts` → **7/7 PASSED** (71s, realny LLM, realne wiersze SQL). |
| **HP-16** docs-teresa.e2e mindmap/note 3/3+3/3 | **POTWIERDZONE (wąsko)** | Ten sam plik ma 11 testów łącznie (Deck/Word/Teresa/Assessment/Interview). Uruchomione: **1/11 failed** — ale failed to test ASSESSMENT ("column \"type\" does not exist" — realny drift schematu), NIE mindmap/note. Wąska teza rejestru (mindmap/note 100%) się broni, ale komunikat "docs-teresa.e2e" jako całość ✅ jest niepełny — sąsiaduje z realnym 500/schema-drift nieujawnionym w tym wpisie. |
| **HP-16** 82/82 unit | **POTWIERDZONE DOKŁADNIE** | 7 plików wskazanych w commit-message uruchomione osobno: `generateDeliverable.evidencePersist/canvasPersist` (4+6), `assessmentInitiativeService/interviewInsightService/presentationGeneratorService/documentStudioService.evidencePersist` (4+6+1+2), `evidenceContractBridge` (5) = 28 + `generateDeliverable.canvasTools` + `financeReportSectionService` (33) + `generateDeliverable.mindmap` = **28+54=82/82 PASSED**. Liczba się zgadza co do jednego testu — rzadko spotykana precyzja, brawo. Wszystkie leżą pod `tests/unit/**`, które JEST w domyślnym `include` `vitest.config.ts` — nie jest to fantom. |
| **HP-16** „panel adwersaryjny ✅ (score 88/100)" | **ZAWYŻONE / SFABRYKOWANE** | Plik `PANEL_ADWERSARYJNY_HP16` **NIE ISTNIEJE** nigdzie: ani w working tree, ani w `origin/demo`, ani w żadnym commicie żadnej gałęzi (`git log --all --grep="88/100"` = 0 wyników, `git log --all --grep=PANEL_ADWERSARYJNY_HP16` = 0 wyników). Rejestr w linii 578 ma dosłowny checkbox `[x] Panel sceptyków: HP-16 potwierdzony (PANEL_ADWERSARYJNY_HP16, score 88/100)` — odhaczony fakt, który nigdy się nie wydarzył jako artefakt. Ten sam wpis (linia 98, wcześniejsza sesja tego samego dnia) mówił wprost: „HP-16 kod OK ale zostaje 🟡 (**brak panelu adwersaryjnego**=proces)" — czyli AUTOR WIEDZIAŁ, że panelu nie ma, a 1h później w innym wpisie stwierdza że panel istnieje i dał 88/100. |
| **HP-2** agentAudit 4/4 e2e (agent-audit.e2e.test.ts) | **POTWIERDZONE** | Uruchomione na parity :5443 razem z docs-teresa: 15 testów łącznie w 2 plikach, 1 fail (w docs-teresa/Assessment) → agent-audit.e2e.test.ts = **4/4 PASSED** (rachunek: 15−11=4, zero failów przypisanych do tego pliku). |
| **HP-4** agent-plan router | **POTWIERDZONE (mount)** | `server/src/routes/ai/agent-plan.routes.ts` realnie zamontowany: `server/src/routes/ai/index.ts:81 router.use('/agent-plan', agentPlanRoutes)` → `server/src/Gateway.ts:430 app.use('/api/ai', aiDomainRoutes)`. Nie jest to orphan router (wzorzec z pamięci „Initiatives orphan router" — sprawdzone specjalnie, tu OK). Samych testów runtime dla agent-plan nie odpalałem (poza zakresem próbki czasowej), ale ścieżka montażu jest realna, nie fantom. |
| **HP-8** statusBar 5/5 typów, 4/4 E2E + 10/10 komponent | **POTWIERDZONE** | `tests/components/ArtifactApprovalStatusBar.5types.test.tsx` → **10/10 PASSED**. `tests/acceptance/hp8-artifact-approvals.e2e.test.ts` na parity :5443 → **4/4 PASSED** (initiative/report/deck flow + unauth-reject). Rejestr sam uczciwie zaznacza lukę: „brak sesji odbioru wizualnego 3 nowych — Vegas" — to NIE jest ukrywane, zgodne z regułą CLAUDE.md #7. |
| **HP-17** evidence bridge (część 82/82 powyżej) | **POTWIERDZONE** | Patrz HP-16/82-unit — `evidenceContractBridge.test.ts` 5/5, `generateDeliverable.evidencePersist` 4/4 z tagiem HP-17 w nazwach testów. |
| **HP-23** ingest DOCX/XLSX/PPTX/CSV — „testy 11/11" | **CZĘŚCIOWE — dokładnie pułapka ze zlecenia** | Testy (`server/tests/harvey-vault/documentTextExtractor.test.ts` + `knowledgeOrgScope.test.ts`) faktycznie **PASSED 11/11** gdy odpalone ad-hoc configiem. ALE: leżą pod `server/tests/**`, które `vitest.config.ts` ma **jawnie wykomentowane z `include`** z komentarzem *"server/tests excluded - require full DB schema"*. Domyślny `npx vitest run server/tests/harvey-vault/...` → **"No test files found, exiting with code 1"**. Czyli ✅ dla HP-23 opiera się na testach, które NIGDY nie wchodzą w domyślny/CI run — dokładnie ten wzorzec, przed którym ostrzega zlecenie. Kod (`documentTextExtractor.ts`, commit `7cf9b37aa1`) jest realny i testy przechodzą, ale "11/11 na zielono" nigdy nie jest automatycznie re-weryfikowane. |
| **Agregat sekcji: 27✅/0🟡/0⬜/1🔵** | **ZAWYŻONE — dowód wprost z historii gita** | Commit `897b4f2c0a` ("DOMKNIĘCIE FAZY … delegacja Piotra, rocznica ślubu") zmienił licznik Harvey z **25✅/1🟡/2⬜** na **27✅/0🟡/0⬜/1🔵** — `git show --stat` na tym commicie: **zmieniony TYLKO plik `_REJESTR_DOKONCZENIA.md`, ZERO plików kodu/testów.** Commit message wprost: „Rozstrzygnąłem KAŻDĄ z 78 pozostałych pozycji — Piotr delegował akcept CTO na czas kolacji rocznicowej". Czyli min. 2 pozycje Harvey przeszły 🟡/⬜→✅ przez samą decyzję redakcyjną, bez nowego dowodu. Szczegółowa tabela per-ID (linie 404-415 tego samego pliku) nadal pokazuje **9 pozycji** jako otwarte (HP-3🟡, HP-5⬜, HP-10🟡, HP-18🟡, HP-20🟡, HP-21⬜, HP-25🟡, HP-27⬜, HP-4d🟡) — w tym **HP-20 z dopiskiem autora „all-pass 0/3"** (dosłownie zero z trzech przebiegów w pełni zaliczonych) i **HP-5 „budować? kolizja doktryny"** (otwarte pytanie architektoniczne, nie kod). Te dwa opisy jawnie zaprzeczają statusowi ✅ na poziomie agregatu. |

## Podsumowanie liczbowe

Z 27 twierdzonych ✅ w sekcji B·Harvey:
- **~20 pozycji ma realny dowód kod+test** (oryginalne 17 z `_PLAN_HARVEY_PARITY`/E2E 07-16 + potwierdzone dziś HP-2, HP-8, HP-16core) — w tym próbka 6 z nich bezpośrednio przeze mnie odpalona na żywo (parity :5443 + unit) i **wszystkie przeszły**.
- **1 twierdzenie w środku ✅-bucketa jest wprost sfabrykowane** (panel adwersaryjny HP-16, "score 88/100" — plik nie istnieje nigdzie w repo/historii).
- **1 pozycja (HP-23) opiera się na testach strukturalnie wykluczonych z domyślnego runu** — działają, ale nikt automatycznie tego nie sprawdza; to "✅ na słowo", nie "✅ z CI".
- **co najmniej 2 z 27 pozycji przeszły do ✅ czystą redakcją rejestru** (commit dotykający tylko .md, zero kodu) w ramach "domknięcia fazy" podczas nieobecności właściciela — a stojące za nimi opisy (HP-20 "0/3 all-pass", HP-5 "kolizja doktryny, nie budować") same przeczą "zrobione".

## Werdykt ogólny

B·Harvey **NIE jest 96% prawdą w znaczeniu "27/28 zweryfikowane runtime"**. Rdzeń techniczny (HP-2, HP-8, HP-16-core-evidence, HP-17, HP-23-kod) jest solidny — testy, które odpaliłem osobiście na żywej bazie parity, przeszły dokładnie tak, jak rejestr twierdzi, łącznie z zaskakująco precyzyjnym "82/82". To nie jest audyt fikcyjny w większości.

Ale sam **agregat sekcji (27✅) jest zawyżony o co najmniej 2-3 pozycje** przez czystą redakcję dokumentu bez nowego dowodu (commit 897b4f2c0a), a jeden konkretny, cytowany dowód ("panel adwersaryjny, score 88/100") **jest zmyślony — nie istnieje jako artefakt nigdzie w repozytorium**, mimo że rejestr ma go odhaczony jako `[x]` w liście kontrolnej. Realistyczna liczba to raczej **~24-25/28 z twardym dowodem**, nie 27/28, plus HP-23 wymaga zastrzeżenia "działa, ale poza CI". Rekomendacja: (1) usunąć/skorygować wpis o panelu adwersaryjnym HP-16 albo faktycznie go przeprowadzić, (2) przenieść `server/tests/harvey-vault/**` do `tests/unit/` albo dopisać je do CI, (3) rozliczyć HP-20/HP-5 jako 🔵 (nie ✅) zgodnie z własnym opisem w tabeli per-ID.
