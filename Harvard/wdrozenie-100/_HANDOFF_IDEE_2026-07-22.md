# HANDOFF — IDEE (4 narzędzia canvas) · 2026-07-22
**Dla świeżej sesji.** Kontekst poprzedniej się zamknął. To jest jedyny punkt wejścia — czytaj od góry.

## 0. GDZIE JEST PRACA
- **Worktree:** `.worktrees/audyt-idee` · **gałąź:** `audyt-idee-2026-07-22` (od `origin/demo`). **NIC NIE WYPCHNIĘTE, nic na demo.**
- Wszystkie dokumenty niżej są NA TEJ GAŁĘZI (untracked), nie na origin/demo.

## 1. CO JUŻ ZROBIONE — 3 fixy kodu (zweryfikowane renderem, czekają na akcept Piotra → deploy)
| Plik | Fix | Dowód |
|---|---|---|
| `src/components/MyWork/MyWorkHub.tsx` | **IDE-001 root(a): okno budowy honoruje wybór narzędzia** (dodane `data.initialTool=startTool` + `setIdeaActiveTool(startTool)` w `handleStartupTemplateSelect`; omija no-op `patchIdeaWorkspaceState`). | render: Whiteboard/Table/Process otwierają się poprawnie |
| `server/src/routes/my-work.routes.ts` | Lista Idei zwraca realny `preferredTool` (subquery z `my_idea_maps`) — badge „Tool" był „Recommendation map" dla wszystkich. | render listy + API 200 |
| `server/src/services/ai/canvasGraphLlm.ts` | **Jakość treści Teresy**: Process Flow→realny proces (nie meta-prezentacja); Table→tryb B tabela porównawcza + luz bramki (`derivedComparisonColumns>=2`); Mind Map→answer-first; wszystkie→zakaz zmyślania dat. | 2 przebiegi generatorów PRZED/PO |

**IDE-001 root(b) NIEZROBIONE:** 3 tryby startu (Szablon/Blank/AI) są w praktyce identyczne (szablon nigdy nie wybrany, AI-kickoff nie odpala). Osobny temat (IDE-004…009).

## 2. SSOT REDESIGNU IDEE (czytaj OBOWIĄZKOWO)
`Harvard/wdrozenie-100/_ANALIZA_IDEE_4_NARZEDZIA_2026-07-22.md` — kryteria **K1–K9** (K8=elegancja lidera, K9=dynamika: zoom/pan/drag/add/type/font/kolor), macierz PRZED, podział WSPÓLNE/OSOBNE, **PLAN REALIZACJI Faza 0–5**.
Werdykt Piotra: „4 środowiska dramatyczne — ani ładne, ani użyteczne, ani sterowalne". Cel: **każde narzędzie eleganckie i dynamiczne jak LIDER swojego rynku** (Mapa→Whimsical · Whiteboard→Miro/FigJam · Proces→Lucidchart · Tabela→Airtable), na WSPÓLNEJ powłoce (SPEC-A).

Checklisty Faza 0 (parytet lidera per narzędzie): `_FAZA0_PARYTET_{WHITEBOARD_MIRO,TABELA_AIRTABLE,MINDMAP_WHIMSICAL,PROCESS_LUCIDCHART}.md` (pisane przez agentów 07-22).

## 3. NASTĘPNY KROK
Plan Faza 0 (parity checklisty) → **Faza 1: prototyp WSPÓLNEJ POWŁOKI** (Menu 1 + wzorzec górnego paska + uproszczony prawy panel + wspólny baseline prawego-kliku + tokeny bez crimson) → wstępny OK Piotra (reguła #7: prototyp PRZED budową) → render → akcept. Równolegle Faza 2: prymitywy interakcji (silnik płótna). Kolejność narzędzi w Fazie 3: Whiteboard + Tabela najpierw (najboleśniejsze).

## 4. HARNESS RENDERU — JAK RENDEROWAĆ TE NARZĘDZIA BEZ LOGOWANIA PIOTRA (krytyczne)
Reguła #7 wymaga, żebyś renderował SAM. Sposób (sprawdzony 07-22):
1. **Backend E2E** (read-only, chroni dane demo): skopiuj `.env.staging.local` z GŁÓWNEGO checkoutu do worktree, dopisz `NODE_ENV=staging`, `JWT_SECRET=dev_render_dummy_secret_min_32_chars_1234567890`, `E2E_MODE=true`. Odpal `npm run dev:staging:ro` (backend 3001 + frontend 3000). `DB_READONLY=1` blokuje zapisy → collab/presence rzucają czerwone toasty „Disconnect/Presence failed" = ARTEFAKT renderu, NIE produkt.
2. **Auth = wbudowany bypass E2E** (`server/src/middleware/auth.middleware.ts` ~1037): token JWT z claimem `{e2e:true, id, organizationId}` (podpisany DOWOLNYM sekretem — E2E nie weryfikuje podpisu) jest akceptowany. Mintuj `jsonwebtoken.sign(...)`. Wstrzyknij przez Playwright `addInitScript` → `localStorage['token']` (+`refreshToken`). Przeglądarka Claude (mcp) BLOKUJE wstrzyknięcie tokenu — używaj **Playwright/chromium z `@playwright/test`** w node_modules worktree (node-skrypt, własny chromium).
3. **CZYSTY user do renderu:** `id=0fe55f96-6965-4970-a291-3018549e6fb9`, `org=7504ff08-826e-4747-86ec-9b044911b623` (membership `ACTIVE`), ma po 1 idei na narzędzie: mindmap `fb9b7358-24e1-4dc3-907e-787f45dd1657`, whiteboard `7681386f-8ae5-4545-a32d-dbdf701485ba`, process_flow `64ed2e0e-8ab3-47bb-9766-e0e1f5402c66`, table `df41b69d-8cb8-4a6e-ae2a-eaf2d1dd871c`.
   - ⚠ PUŁAPKA: user `d2b6a316` (piotr@dbr77) ma membership `active` (małe) — `assertIdeaMembership` sprawdza `='ACTIVE'` case-sensitive → mapa GET **404** → powłoka pokazuje szkielet mindmap = FAŁSZYWY „zawsze mindmap". To 2 z 1140 wierszy (anomalia danych, nie bug kodu). NIE renderuj na tym userze.
4. Wyłącz onboarding: `localStorage['consultify_onboarding_done:<uid>']='true'`. Otwieranie narzędzia: deep-link `/my-work/ideas/<id>/workspace/<tool>` (mindmap/whiteboard/process_flow/table).
5. **iCloud psuje HMR Vite** — po edycji frontendu często trzeba RESTART frontendu (`pkill vite`, `rm -rf node_modules/.vite`, restart) — sprawdzaj `curl localhost:3000/src/.../Plik.tsx | grep <twój-marker>`.
6. Uruchamianie generatorów Teresy (jakość treści): node-skrypt `tsx` w `server/`, importuje `canvasGraphLlm.js`, ładuje env, wywołuje `generate{Mindmap,ProcessFlow,Whiteboard,Table}Graph(intent,title,true)`. Ustaw `DB_STATEMENT_TIMEOUT=60000` (inaczej timeout llm_providers → null → szkielet, fałszywy defekt).

## 5. TWARDE FAKTY RUNTIME (zweryfikowane, Faza 1 audytu — patrz `_AUDYT_IDEE_2026-07-22.md`)
- **Teresa tworzy REALNIE** wszystkie 4 (canvasGraphLlm + generate_deliverable, materializeOrThrow → my_idea_maps). Flagi `ENABLE_TERESA_MINDMAP`/`_CANVAS_TOOLS` default ON.
- **Kolaboracja REALNA** (WS `/ws/collab/:ideaId`, presence/graph_patch/komentarze/snapshoty).
- **Persist canvas** zahartowany (reload-race/anti-wipe). Wspólny wyścig `POST /map/sync` (my-work.routes.ts:4718) nieatomowy — potencjalny cichy data-loss przy współpracy (do naprawy).
- **Interceptory czatu** (`UnifiedChatPanel.tsx:3122/3153/3244`) przechwytują „stwórz mapę/proces/tablicę" PRZED LLM → no-op poza otwartym narzędziem. Do naprawy (zawężić regex).
- **Tabela dwutorowa:** IdeaTableTool (graf) + tablePlatform (tp_tables) mostek; flaga `tablePlatformMetadataFirst` TWARDO zablokowana (default off + allowLocalOverride false + CLEARED_FLAG_OVERRIDES + 0 callerów serwera + 0 wierszy feature_flags) → relacyjny silnik + jego realtime = martwy dla usera.
- **Storage** `STORAGE_PROVIDER` default `local` (dysk Railway efemeryczny) → obrazy Whiteboard giną przy redeployu, o ile demo nie ma s3.
- **LLM tier:** aktywny tylko `STANDARD/openrouter/anthropic/claude-sonnet-4`; generatory żądają `premium` (wszystkie premium is_active=false → fallback). OK jakościowo (Sonnet 4), ale do świadomości.
- `trolley.proxy.rlwy.net` = baza WSPÓLNA demo+staging (DATABASE_URL w głównym `.env.staging.local`). Prod=centerbeam — NIE ruszać.

## 5b. PARTIA PO W TOKU (agenci równolegli, 07-22 popołudnie)
- **PO/1 zrobione i zacommitowane** na gałęzi `audyt-idee-2026-07-22` (commit `63b32f416a`): Mind Map orb zdjęty (K8), trackpad-pan włączony (K9, `useIdeasToolDefaults.ts`).
- **3 agenci PO w izolowanych worktree'ach** (każdy commit na SWOJĄ gałąź, NIE push, bez renderu — port był zajęty). GAŁĘZIE DO INTEGRACJI:
  1. ✅ Whiteboard prawy-klik — gałąź `worktree-agent-acfd08ac847f8e43e` commit `457f2753fb` (pliki: `IdeaCanvasContextMenu.tsx` + `IdeaWhiteboardTool.tsx`). Dodane: Edytuj/Duplikuj/Kopiuj/Na-wierzch/Pod-spód/Lock/Usuń nad sekcją AI; reużyte handlery paska selekcji; NOWE `bringSelectedToFront`/`sendSelectedToBack`; naprawiona synchronizacja selekcji prawym-klikiem.
  2. ✅ Table prawy-klik — gałąź `worktree-agent-aab28db457aa7d60b` commit `e30de81093` (5 plików: `table/ViewRouter.tsx`, `useTablePlatformIntegration.ts`, `TableDataProvider.tsx`, `IdeaTableTool.tsx`, test). Zbudował kebab wiersza OD ZERA (8 pozycji) + handlery duplicate/delete/insert. ⚠ UWAGA DO RENDERU: agent stwierdził, że menu wiersza NIE istniało w `PlatformGridView` — ale zrzut RC-table.png z rana POKAZAŁ menu (Edit/Add note/Dup/Delete). Rozbieżność → przy renderze SPRAWDŹ czy nowy kebab nie DUBLUJE istniejącego z innej ścieżki widoku; zweryfikuj który widok realnie renderuje. Insert-nad/pod = kolejność tylko client-side (brak pola `order` w backendzie).
  3. ✅ Process toolbar — gałąź `worktree-agent-a4ba735dbf206d5e6` commit `d3303857d5` (plik: `ProcessFlowToolbar.tsx`). Ściana 3-kol → minimalistyczny pasek + overflow „Więcej"; guidance truncate+tooltip; wyczyszczony dług crimson `c-accent`→navy.
- **CO ROBI ŚWIEŻA SESJA:** `git worktree list` + `git branch` → znajdź gałęzie agentów (nazwy typu `claude/*`); dla każdej: przejrzyj diff, zintegruj do gałęzi roboczej IDEE, **wyrenderuj PRZED/PO harnessem (§4), zrzut** → dopiero potem Piotr ocenia (reguła #7). Żaden fix agenta NIE był renderowany — wymaga wizualnej weryfikacji przed pokazaniem.

## 6. REGUŁY (nienaruszalne)
Nic na demo bez akceptu Piotra na zrzutach. Reguła #7: prototyp→OK→render-sam→accept (Piotr NIGDY pierwszym testerem wizualnym). Baza gałęzi = origin/demo. Zero crimson w nowym kodzie. Weryfikuj realny runtime, nie flagi.

## 7. ZADANIE #1 WYKONANE (2026-07-22, sesja "drugi plan taryfowy") — 3/3 gałęzie zintegrowane + render
Wszystkie 3 gałęzie agentów PO scherry-pickowane na `audyt-idee-2026-07-22` (commity `955e5ae400` whiteboard, `f7f4efdf6f` process, `3ca23776ec` table). Konflikty były w każdej (i18n `t()`-wiring i crimson-fixy wylądowały PO forkach agentów) — rozwiązane ręcznie, zachowując OBIE strony (nowa funkcja agenta + interim-fixy z HEAD). esbuild czysty na wszystkich plikach.

**Render harness odpalony i zweryfikowany** (Playwright/chromium, user `0fe55f96`/org `7504ff08`, backend `E2E_MODE=true DB_READONLY=1`, motyw wymuszony przez `localStorage['consultify-storage']={state:{theme}}` — `colorScheme` samego Playwrighta NIE wystarcza, appka czyta zustand-persist, nie `prefers-color-scheme`):
- **Whiteboard prawy-klik** ✅ czysto zweryfikowany light+dark: menu Edytuj/Duplikuj/Kopiuj/Warstwa(przód/tył)/Zablokuj/Usuń nad AI-akcjami (Expand/Challenge), zero crimson, zero duplikacji.
- **Process Flow toolbar** ✅ czysto zweryfikowany light+dark: pasek build-palette+Lane/Insert/Split+Undo/Redo+Save+"More", overflow "Więcej" z Analyze/Manage/Convert, zero crimson.
- **Table row-kebab** ✅ zweryfikowany wizualnie IZOLOWANYM harnessem (light+dark) — I ZŁAPAŁ REALNY CRASH. Kontekst: w apce `PlatformGridView` renderuje się TYLKO gdy `tablePlatformMetadataFirst` znajdzie żywą platform-bazę, a `tp_bases` istnieją tylko dla syntetycznych QA-workspace'ów (`qa-ws-*`/`odbior-ws-0001`) — ŻADEN realny `my_idea_maps.id` nie ma dopasowanej `tp_bases.workspace_id` (sprawdzone SQL), więc `usePlatform=false` dla KAŻDEJ idei w produkcji → kebab był nieosiągalny przez działającą appkę. Zamiast czekać na decyzję o flagach, użyłem gotowego harnessu `dev-render/` (izolowany Vite `:3020`, bez bazy/logowania — „wzór harness EV" z reguły #7): wyeksportowałem `PlatformGridView` (pure-presentational) i zmountowałem go z mock TableNode[]/fields w nowym ekranie `dev-render/screens/idea-table-tool-kebab.tsx` (zarejestrowany w `main.tsx`). **Render ujawnił `ReferenceError: isPl is not defined`** — kebab agenta używa `isPl` w ~20 miejscach, ale `PlatformGridView` destrukturyzował tylko `t` z useTranslation; `isPl` był niezdefiniowany → crash przy PIERWSZYM mountcie. Latentne dokładnie dlatego, że komponent jest martwy in-app. **Fix** (commit `7cb32c31f2`): `const { t, i18n } = useTranslation(); const isPl = i18n.language?.startsWith('pl')` — jak rodzic ViewRouter. Po fixie kebab renderuje się czysto light+dark: 8 pozycji w grupach (Edytuj/Dodaj notatkę · Wstaw nad/pod · Duplikuj/Kopiuj · Rozwiń rekord · Usuń wiersz), separatory OK, „Usuń wiersz" w danger-red (semantyka delete — dozwolone), zero crimson-CTA. Potwierdzone też: STARY (legacy graph) row-menu w `IdeaTableTool.tsx` (Edit/Add note/Duplicate/Delete) renderuje się poprawnie i BEZ duplikacji obok nowego — integracja nie zepsuła istniejącej ścieżki. Odkrycie po drodze: `useTablePlatformIntegration.ts` miał JUŻ zaimplementowane (ale niepodłączone) `handleDeleteRow`/`handleDuplicateRow` — scaliłem z wersją agenta (position-aware insert + `onSelectionChange` cleanup + sufiks „(copy)"), usuwając duplikat logiki.
- **Commity Table**: `3ca23776ec` (integracja kebaba) + `7cb32c31f2` (fix crash isPl — FORWARD-PORTOWALNY na demo/Londyn niezależnie) + `d949b77ca6` (dev-render harness — tylko tooling, NIE potrzebny na demo).
- **UWAGA co do demo**: nowy Table-kebab, nawet po akcepcie zrzutów, NIE będzie widoczny userom dopóki `tablePlatformMetadataFirst` zostanie OFF (flaga `allowLocalOverride:false`, silnik metadata-first uznany za niegotowy). To osobna decyzja produktowa (włączyć flagę + zasiedzić realne platform-bazy). Fix crasha (`7cb32c31f2`) warto forward-portować NIEZALEŻNIE — chroni przed wywaleniem się ekranu w dniu włączenia flagi.
- Serwery render-harnessu zatrzymane po zakończeniu. `.env.staging.local` w worktree zostawiony (gitignored). Harness dev-render: `npx vite --config dev-render/vite.config.ts --port 3020` → `?screen=idea-table-tool-kebab&theme=light|dark`.

## 10. INTEGRACJA FALA 1+2 (2026-07-22 noc) — gałąź `idee-fala12-integracja`
5/5 gałęzi scalonych bez konfliktów (70 commitów od origin/demo): guard + procwb + table + mindmap + menu1. **Strażnicy na zintegrowanym drzewie: check-artefakt AKTUALNIE 7 vs baseline 274** (sweep zbił dług o 267; zostały udokumentowane wyjątki), check-triada ✓, check-list-canon ✓. Worktree audyt-idee stoi na tej gałęzi.
**NASTĘPNY KROK (przed pokazaniem Piotrowi):** (1) debug 2 problemów renderu Z7 z §9 (pasek mels nie montuje się z `?ff_melsCanvas=1` na deep-linku workspace — dev-render `melscanvas-workspace&ff_melsCanvas=1` działał PRZED zmianami Z7, więc podejrzany nr 1 = zmiany w IdeaCanvasMelsView/TopBar/chips; ErrorBoundary „Something went very wrong!" niedeterministyczny — złap pageerror stack); (2) render PRZED (origin/demo) / PO (integracja) 4 narzędzia × 2 motywy harnessem §4; (3) komplet zrzutów → akcept Piotra → promocja skillem → baseline strażnika → 0 → matryca (oś Grafika/Powłoka w górę).

## 11. DEBUG RENDERU — twarde fakty (2026-07-22 późna noc, gałąź idee-fala12-integracja)
1. **Menu 1 (Z7) DZIAŁA w izolacji**: dev-render `?screen=melscanvas-workspace&ff_melsCanvas=1` renderuje pełny pasek (Idee › tytuł · chip „Kształtuje" · „Zmiany w kolejce" · Teresa · Konwertuj + Menu 3). UI jest dobre.
2. **In-app deep-link `/my-work/ideas/<id>/workspace/<tool>` NIE montuje paska** ani przez `?ff_melsCanvas=1`, ani przez `localStorage['ff.mels_canvas']='1'` (fallback istnieje w melsCanvasFlag.ts). Mindmap renderuje legacy bez błędu; **whiteboard/process/table rzucają ErrorBoundary „Something went very wrong!" KONSEKWENTNIE na gałęzi integracyjnej** (na render-menu1 było niedeterministycznie) → podejrzenie: któryś sweep (procwb/table) wprowadził runtime-błąd niewidoczny dla esbuilda (np. klasa w template stringu / zmieniona stała konsumowana logicznie). Wcześniej na tply origin/demo te same deep-linki działały (tplat3-*.png).
3. **NASTĘPNE KROKI DEBUGU** (kolejność): (a) złap pełny stack — Playwright `page.on('console')` z msg.args()/stackiem albo grep źródła po "Something went very wrong" i dodaj log błędu z boundary; (b) bisekcja: render na gałęzi `fala2-procwb-2026-07-22` solo i `fala2-table-2026-07-22` solo — który wprowadza crash; (c) po znalezieniu — punktowy fix, esbuild, re-render; (d) czemu deep-link nie montuje mels: sprawdź czy trasa workspace montuje IdeaMapWorkspace czy inny host (grep route'ów po 'workspace/'), i gdzie woła isMelsCanvasEnabled().
4. Serwery: `npm run dev:staging:ro` w worktree audyt-idee (gałąź idee-fala12-integracja), harness §4. Zrzuty debugu: scratchpad/idee-render/m2-*.png.

## 12. RENDER Z7 KOMPLETNY — Menu 1 zaakceptowane wizualnie (2026-07-22 noc, gałąź idee-fala12-v2)
Oba blockery §11 to fałszywe alarmy (bisekcja): (a) crash = DB_READONLY harnessu → unhandled reject autosave → Vite overlay, NIE bug produktu; drobna luka (brak .catch) i tak utwardzona (SHA d8e1ed518b); (b) „pasek się nie montuje" = E2E-user renderował po angielsku („Convert" ≠ „Konwertuj"), montaż był poprawny cały czas.
**Znaleziony i naprawiony REALNY bug integracji:** mindmap w powłoce mels dziedziczył `variant={mapOpen?'overlay':'embedded'}` ze starej ścieżki legacy — fullscreen overlay zakrywał Menu 1. Fix: `!melsCanvasEnabled` w warunku (mindmap embedded w mels, overlay tylko w legacy) — 1 plik, 3 linie, esbuild+strażnicy OK.
**Render finalny 4×2 (PL, ff.mels_canvas=1, harness §4)**: wszystkie 8 kombinacji `mels=true konwertuj=true err=0`. Zrzuty: scratchpad/idee-render/v3-{mindmap,whiteboard}-{light,dark}.png + v2-{process_flow,table}-{light,dark}.png. Menu 1 wygląda identycznie na 4 narzędziach: ← Idee › ikona-typu · tytuł · chip cyklu życia (Iskra/Rośnie/...) · wskaźnik zapisu (Draft lokalny/Zmiany w kolejce) · Teresa · kebab ⋯ · Konwertuj ▾; Menu 3 osobnym paskiem pod spodem z akcjami widoku.
**Integracja finalna:** gałąź `idee-fala12-v2` (baza `fix-fala12-crash`, 7/7 gałęzi robocze scalone: Z8 panel, Z14 snapping, Z16 tablenav, Z17 connector, Z18 sync-atomic, Z20 interceptory, + fix mindmap-embedded). Jeden konflikt merge (Z14×Z17 w IdeaProcessFlowTool.tsx, oba dokładały rozłączne propsy do ReactFlow) rozwiązany ręcznie zachowując obie zmiany. Strażnik: 7 vs baseline 274. Wszystkie multi-touch pliki esbuild-czyste.
**GOTOWE DO POKAZANIA PIOTROWI** — to pierwszy komplet zrzutów tej partii wymagający jego akceptu przed promocją na demo (reguła #7 spełniona: prototyp z OK → build → mój render → czyste zrzuty → TERAZ akcept).

## 13. PROMOCJA NA DEMO — Menu 1 (Z7) + Fala 1/2/3/4 (2026-07-22 noc)
Piotr zaakceptował Menu 1 na 4 narzędziach (Mind Map/Whiteboard/Process Flow/Table) na zrzutach light+dark. Po drodze złapał realny bug na renderze whiteboard — panel sesji zderzony z lewym railem (naprawione, patrz commit w §12/whiteboard). Promocja wg skilla `consultify-promocja-demo`: pre-flight 0 konfliktów, izolowany worktree `/private/tmp/promote-demo`, twarda weryfikacja 122 plików (zgodne z oczekiwanym zakresem), esbuild 118/118 plików FE + 1 BE czyste, 3 strażnicy zielone (check-artefakt 7/274, check-triada, check-list-canon).
**Push:** `89a555ed41` na `origin/demo`. Podczas monitoringu zdrowia zaobserwowana seria równoległych BUILDING/REMOVED w Railway (17:45–19:57) — to współbieżna aktywność INNEJ sesji na tej samej gałęzi demo (nie mój push zepsuł coś); potwierdzone: mój commit jest aktualnym czubkiem `origin/demo` (nic nie weszło po nim), health-check chwilowo `404 Application not found` = Railway w trakcie serii builów, nie awaria.
**Zawartość promocji:** Menu 1 (Z7, 4 narzędzia) + crimson-sweep (274→7) + prawy panel 5 sekcji (Z8) + snapping/prowadnice (Z14) + nawigacja klawiaturą Table (Z16) + connector magnetyczny Process (Z17) + atomowy map/sync (Z18) + interceptory czatu (Z20).
**Punkt cofnięcia PRZED promocją:** `7d6a4af465`. Tag `demo-safe-2026-07-22` do re-tagowania na `89a555ed41` po potwierdzeniu live.
