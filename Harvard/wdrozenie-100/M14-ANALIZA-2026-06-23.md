# M14 „Wdrożenie" — ANALIZA (stan zweryfikowany 2026-06-23)

> Metoda: 3 agentów-rozpoznawczych (kod / baseline-audyt / backend-dane) + **weryfikacja bezpośrednia nośnych twierdzeń** (lekcja z M13: audyty/agenci bywają nieaktualni). Route `/implementation`. Poprzedni audyt: `docs/audit/2026-06-03/(deep/)MODULE_06_realizacja.md` (71/100).

## 1. Tożsamość — co NAPRAWDĘ jest M14
- Route `/implementation` (`AppView.IMPLEMENTATION`) → renderuje **`ExecutionHub`** (`src/components/Execution/ExecutionHub.tsx`, AppRoutes.tsx:1869). Alias `/execution`.
- **NIE** renderuje `src/components/Implementation/` — patrz F1 (martwy duplikat).
- Backend: rodzina routerów `execution.routes`, `raid.routes`, `rollout.routes`, `status-reports.routes`, `v8/execution-control.routes` + `ExecutionController`.

## 2. Werdykt
Najbogatszy funkcjonalnie moduł wykonawczy nad inicjatywami (M13). **Backend w większości REALNY + DB-backed + org-scoped.** Audyt 06-03 dał 71/100; **część findingów już naprawiona** (stan dziś > snapshot). Główny dług = **martwy duplikat-kod** + UX tabel + i18n + handoff do M15.

## 3. Zakres (13 pozycji — z baseline audytu)
Executive Dashboard (health/KPI live) · Portfel: Tabela + Kanban + Timeline · Action Queue · RAID (5 typów CRUD) · Rollout: Master Plan + KPI + Risk + Change + Closure (5 rejestrów) · Raporty (presety, live-data, dystrybucja) · Manager / People-Change (lanes, V8-gated).

## 4. Backend / dane — REAL vs MOCK (zweryfikowane przez agenta C)
| Widok | Endpoint(y) | Dane | Tabela DB |
|---|---|---|---|
| Executive Dashboard | `GET /api/execution/:p/summary,/health`, `/execution/escalations,/calendar` | REAL | initiatives, tasks, decisions, raid_items |
| Portfel | `/execution/:p/blockers`, `POST /:p/gate-check` | REAL | initiatives, tasks |
| Action Queue | `/v8/execution-control/manager/lanes/action-queue/*` | REAL | tasks, decisions |
| RAID | `GET/POST/PUT/DELETE /api/raid` + `/raid/scoring/*` | REAL | raid_items, raid_appetite_thresholds |
| Rollout | `/api/rollout/{kpis,risks,changes,closures}` (+history) | REAL | rollout_kpis/_history/_risks/_changes/_closures |
| Status Reports | `/api/status-reports/initiative/:id/*` (generate/approve/publish) | REAL | status_reports, report_section_history, report_distributions |
| Manager | `/v8/execution-control/manager/lanes/:id/*` | REAL (people-change lane → empty) | initiatives, tasks, decisions, users |
| Execution Control | `/v8/execution-control/{risk,delay,budget,capacity}` | REAL | signals, budget_transactions |

**Relacja M13↔M14:** współdzieli `initiatives`/`tasks`/`decisions` (czyta live z DB, nie kopiuje); M13 prowadzi PLANNING→READY, M14 EXECUTING→BLOCKED→DONE. RAID/rollout/status_reports = osobne tabele M14.

## 5. FINDINGI (priorytet, zweryfikowane)
- 🔴 **F1 — Martwy duplikat `components/Implementation/` (9 komponentów, ~190KB).** BudgetTrackingView, CapacityView, ChangeRequestModal, DecisionBoard, ExecutiveDashboard, ExportControls, InitiativeKanban, RAIDLog, StatusReportBuilder. **ZERO importów** (potwierdzone grep-em; jedyne „Implementation" w repo to niezwiązany `MyWork/ImplementationIdeasSection`). Porzucona równoległa implementacja — każdy ma realne endpointy, nigdy nie wywołane. Dezorientuje (duplikat RAIDLog/ExecutiveDashboard vs żywe w ExecutionHub), dług bundle/maintenance. **→ usunąć** (wzorzec „dwóch implementacji" jak Gantty M13).
- 🟡 **F2 — UX tabel Rollout:** 5 rejestrów = surowy `<table>` zamiast `FilterableTable` (brak preview/filtry/sort/kebab/bulk; niezgodne z `TABLE_AND_PREVIEW_CANON §27`).
- 🟡 **F3 — i18n:** ~141 brakujących kluczy PL + hardcoded etykiety presetów raportów (`'Weekly'` itd.).
- 🟡 **F4 — Handoff M14→M15 (06→07) martwy:** brak „View in Results →" CTA z Rollout/delivery do M15 (M15 beta-closed).
- 🟡 **F5 — Raporty PDF = client-side `window.print`** — brak server endpointu + audit trail.
- ⚪ **F6 — work_queue:** router eksportowany w `routes/index.ts`, tabela `work_queue` nie istnieje; FE nie woła. Zweryfikować montaż → jeśli niewołany, usunąć martwy stub.
- ⚪ **F7 — Możliwy dodatkowy martwy klaster Execution:** `ExecutionView`/`FullExecutionDashboardWorkspace` + `BenefitsTracker`/`CorrectiveActions`/`KPIDashboard` (stash z sesji Londyn próbował je usunąć jako „dead Execution cluster"). Wymaga osobnej weryfikacji żywotności (referencje w AppRoutes.tsx).
- 🟢 **Już naprawione vs audyt (stan > 71/100):** `ImplementationView.tsx` usunięty; `rollout.routes` `NOW()`=0 (SQLite-crash P0 zniknął).

## 6. Testy
Doc `Harvard/Testy manualne/TESTY_M14_WDROZENIE.md` (~58–62 scen manualnych, 8 obszarów + cross-module + dead-code grepy). FE: 2 smoke (`RolloutTab.smoke`, `ManagerApproval.smoke` — 2 mogą failować z UI-drift). **BRAK** backend testów `rollout.routes` (CRUD ×5) — priorytet.

## 7. Rekomendowany plan (fale — do akceptacji)
- **F0 — Higiena martwego kodu (szybkie, wysokowartościowe):** usuń `components/Implementation/` (9) + zweryfikuj/usuń work_queue + zweryfikuj klaster Execution (F7). Czyści dezorientację przed właściwą pracą.
- **F1 — Dane/UX:** Rollout 5 tabel → `FilterableTable` (§27) + baner degradacji błędów (`health`/`action-queue`).
- **F2 — i18n:** ~141 kluczy PL + presety raportów.
- **F3 — Handoff + raporty:** M14→M15 „View in Results" CTA + server PDF endpoint.
- **F4 — Testy:** `rollout.routes.test.ts` (BE CRUD ×5) + napraw 2 smoke.

**Decyzja CEO:** M14 „do 100%" pełną serią fal (jak M13).

---

## 8. POSTĘP PROGRAMU (2026-06-23) — i KLUCZOWY WYNIK: audyt 71/100 mocno nieaktualny

Weryfikacja kodu (nie ufanie staremu audytowi) pokazała, że **większość fal była już zrobiona** od czasu audytu 06-03. Realny stan M14 ≫ 71/100.

| Fala | Stan | Dowód |
|---|---|---|
| **F0 — higiena martwego kodu** | ✅ DONE (`5dc7d21090`) | Usunięto `components/Implementation/` (9 komp.) + workqueue (route+service+validator+3 ref) = **5597 linii**; tsc FE+backend 0; żywy moduł 26/26 testów. F7 (FullExecutionView/pilot) ODROCZONY (żywy + stash Londyn). |
| **F1 — Rollout→FilterableTable + banery degradacji** | ✅ JUŻ ZROBIONE (przed sesją) | RolloutTab: 5 rejestrów CRUD już na `FilterableTable` (canon §27, linie 1089/1141/1182/1250) + load-error banner (1011). Banery degradacji L-02 w ExecutionHub: health(2963)/tasks(3244)/decisions(3425)/action-queue(3721). Pozostał 1 surowy `<table>` = `DerivedRiskTable` (read-only sygnały V8, drugorzędne — akceptowalne). |
| **F2 — i18n etykiety presetów raportów** | ✅ DONE (`5d591130e4`) | `execution.reports.preset.*` PL+EN (Weekly/Monthly/Bi-weekly/On demand/Sponsor → t()). Pełny sweep ~141 kluczy = osobny program i18n. |
| **F3a — handoff M14→M15 „View in Results"** | ✅ ISTNIEJE | `BudgetControlPanel.tsx:312-317` → navigate `/benefits` + i18n `execution.budget.viewInResults`. |
| **F3b/F5 — server PDF endpoint** | 🔴 REALNA LUKA | Brak `/api/.../export/pdf`; raporty = client-side `window.print` (`executionReports.ts:358 exportReportPDF`). Wymaga budowy endpointu generacji PDF + audit trail. |
| **F4 — testy backendu `rollout.routes`** | ✅ DONE (`a66b629f46`) | `tests/integration/routes/rollout.routes.test.ts` — 14 testów (org-scoping każdego query, 404, KPI history-on-change, gating MANAGE_ROLLOUT, 5 rejestrów). Pokrycie 0→14, w ścieżce CI. |
| **Już naprawione vs audyt** | ✅ | `ImplementationView` usunięty; `rollout.routes NOW()`=0 (SQLite-crash zniknął). |

**Pozostała realna luka do 100%:** **F3b — server PDF endpoint** (raporty = client-side `window.print`). To **enhancement, nie defekt** (print-to-PDF działa); server-side = audit trail + spójny rendering + załącznik email. Budowa = lib PDF (puppeteer/pdfkit) + rendering raportu — decyzja: w zakresie „100%" v1 czy v1.1?

**Bilans M14 (2026-06-23):** F0 ✅ · F1 ✅ (już-zrobione) · F2 ✅ · F3a ✅ (istnieje) · F4 ✅ · **F3b → v1.1** (decyzja CEO 2026-06-23: client-side `window.print` = działający MVP; server-PDF jako osobny enhancement v1.1).

## ✅ M14 — DOMKNIĘTE INŻYNIERSKO (2026-06-23)
Wszystkie fale realizacyjne zamknięte; jedyny odłożony element (F3b server-PDF) świadomie → v1.1. Pozostają (po stronie nie-inżynierskiej / osobne): **F7** klaster pilot-Execution (żywy + stash Londyn — osobna, ostrożna konsolidacja); **→F/→UI** odbiory Piotra na demo; pełny i18n-sweep (~141 kluczy = osobny program). Stan M14 ≫ audytowe 71/100.
