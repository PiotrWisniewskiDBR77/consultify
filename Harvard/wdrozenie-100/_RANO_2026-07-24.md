# Rano — co się wydarzyło w nocy (2026-07-23/24)

## 1. WYNIK: 5,9 → 7,1. **Nie osiągnąłem 9,5.**

| Sędzia | Start | Teraz | Δ |
|---|:--:|:--:|:--:|
| Grafika | 6,0 | **7,1** | +1,1 |
| Merytoryka | 5,4 | **6,6** | +1,2 |
| IT / architektura | 6,3 | **7,6** | +1,3 |
| **ŚREDNIA** | **5,9** | **7,1** | **+1,2** |

Prosiłeś o 9,5. Brakuje **2,4 punktu**. Piszę to na początku, bo to najważniejsza informacja.

**Dlaczego nie dojechałem:** start był niżej, niż zakładaliśmy (5,9, nie ~8), bo sędziowie mieli
polecenie zaniżać przy wątpliwości i wyszły rzeczy, których wcześniejsze audyty nie widziały —
m.in. **11 atrap AI udających działanie**. Jedna runda naprawcza dała +1,2. Do 9,5 potrzeba
jeszcze 2 takich rund. Nie ściemniam wyniku, żeby zmieścić się w mandacie.

**Dobra wiadomość:** sędzia grafiki podsumował stan tak — *„zostały już tylko braki jednoklasowe,
mechanika jest zrobiona, brakuje domknięcia powtarzalnych detali, a nie żadnej przebudowy"*.
Największa pojedyncza pozycja (+1,6) to **trzy linie kodu w trzech kartach**.

---

## 2. CO NAPRAWIONE (wszystko na gałęzi, NIC na demo)

### Produkt przestał kłamać użytkownikowi — to najważniejsze
Znaleźliśmy i wycięliśmy **11 atrap**: kod, który udawał, że AI zadziałało.
- „Generuj plusy/minusy" wpisywał **zawsze te same 6 zaszytych fraz**
- „Komentarz AI" doklejał zmyślony tekst **podpisany „AI Assistant"** i pokazywał sukces
- „Skróć" ucinał zdanie przez `slice(65%)` — **niszczył treść użytkownika**
- 6 generatorów w Zadaniu udawało pracę modelu przez `setTimeout(1200–1800)` + zaszytą treść
  identyczną dla każdej karty
- Generator RACI po awarii wstawiał **pierwsze 4 osoby z listy** i meldował sukces
- 2 przyciski „AI" w modalach to był czysty `toast()` bez logiki (podłączone do realnego AI)
- Komponent dzielony przez **12 ekranów** miał własną atrapę
- **5 kolejnych w module Inicjatyw** — z komentarzem w kodzie `// Simulate a short delay for "thinking"`

Przyczyna wspólna: endpoint `/ai/chat` zwracał strukturę bez pola, które czytało **15 miejsc w kodzie**.
Nic nie rzucało błędu — 200, pustka, gałąź awaryjna. Tokeny spalone, wynik wyrzucony.

Weryfikacja nie jest deklaracją: agenci symulowali awarię AI i mierzyli **sumę kontrolną znaków**
pola przed i po. Identyczna. Treść nietknięta, błąd uczciwy.

### Reszta
- **Kontrast**: 2,46 → 7,26:1 (nagłówki), 3,47 → 17,06:1 (Inicjatywa), czcionki 8 px → 11 px
- **Tryb Podgląd**: Inicjatywa 5 edytowalnych pól → 0; Insight 22 uchwyty → 0
- **Polonizacja**: pasek stanu, „Findingi"→„Ustalenia", surowe klucze i18n, ~42 wtręty w Narzędziu
- **Backend**: cichy sukces przy zerowym zapisie → 404; brak walidacji → schemat z limitami;
  wyścig utraty zapisu → porównanie-i-podmiana
- **Rubryka oceny AI**: 41 kryteriów dostało progi („SPEŁNIONE GDY / NIESPEŁNIONE GDY") zamiast
  samych nazw, doktryna (piramida · MECE · kwantyfikacja z założeniami · falsyfikowalność ·
  uczciwa niepewność). **Rubryka jest teraz ostrzejsza niż treść, którą ocenia** — łapie własne dane demo
- **Testy Insightu**: 26 błędów → 0 (5/7 zielonych; 4 czerwone to wcześniejsze defekty, nie regresja)
- **Treść demo**: liczby uzgodnione między kartami, każda z podstawą wyliczenia i warunkiem falsyfikującym
- **`expectedOutcome`**: łańcuch przerwany w 3 miejscach (serwer nie zwracał, nie przyjmował, front nie mapował)

---

## 3. ★ TRZY RAZY POSTAWIŁEM BŁĘDNĄ DIAGNOZĘ — i przekazałem ją dalej jako fakt

To najważniejsza rzecz, jakiej się dziś nauczyłem o własnej pracy.

| Co ogłosiłem | Prawda | Czym mnie zmylił pomiar |
|---|---|---|
| „`dark:via-*` nie działa, 20 miejsc do naprawy" | **Działa** | escapowany dwukropek w wyszukiwaniu |
| **„Kanwa nie podąża za sekcją"** — wpisałem to jako pozycję nr 1 planu z wyceną +1,5 | **Podąża, wszystkie 24 sekcje** | `innerText` nie pokazuje treści pól formularza |
| „Animacja utyka na 0,26, karta trwale przygaszona" | **Kończy się w <1 s** | trafienie w środek przejścia |

Za każdym razem ten sam schemat: narzędzie skłamało, wynik brzmiał spójnie z resztą obrazu,
uwierzyłem i **powiedziałem Ci to jako fakt**. Dwie z tych diagnoz odwołali sami sędziowie.

**Reguła, którą wpisałem do dziennika:** zanim ogłoszę defekt systemowy, muszę zweryfikować go
drugą, niezależną metodą. Przy sporze między pomiarami rozstrzyga zrzut ekranu, nie liczba z DOM.

Powiązane lekcje z tej samej rodziny:
- `esbuild` **nie łapie błędów składni JSX** — zielona bramka ≠ ekran wstanie
- **„Zero konfliktów" w scaleniu nie znaczy „bezpiecznie"**: dwie gałęzie naprawiały ten sam błąd,
  git policzył to jako dwie delecje i **usunął import używany w 14 miejscach**, bez ostrzeżenia
- **Mój podwójny import wyłączał harness całkowicie** przez kilka godzin — czyli blokował regułę
  „nigdy nie jesteś pierwszym testerem" całej flocie agentów

---

## 4. DECYZJE, KTÓRE CZEKAJĄ NA CIEBIE

1. **Migracja bazy dla Insightu** — plik `931_…sql` gotowy, **nieuruchomiony**. W kodzie jest
   automatyczny guard dorabiający kolumnę przy pierwszym zapisie. Puścić ręcznie i zdjąć guard,
   czy zostawić automat?
2. **Ślad audytowy AI zniknął** — przepięcie na `/ai/generate` sprawiło, że wywołania AI nie trafiają
   już do rejestru w panelu administratora. **Nasza regresja z tej nocy**, do naprawy.
3. **Nagłówek „Scenariusze AI (real-time)"** stoi nad polami wypełnianymi ręcznie — obietnica,
   której produkt nie dotrzymuje.
4. **„Konwertuj na inicjatywę" jest w dwóch miejscach** (nagłówek = kreator, kafel = od razu tworzy).
5. **Zielone CTA** („Zatwierdź decyzję", „Ukończ") — kontrast 4,35:1, poniżej normy, i tylko 2 z 6 kart
   tak robią. Kanon mówi „CTA neutralne".
6. **Koszt tokenów wzrósł** o ~2,5 tys. znaków na każde kliknięcie „Analizuj z AI" (cena za ostrzejszą
   rubrykę). Do Twojej oceny, czy warta.
7. **Poza kartami**: „Eksport danych" w ustawieniach melduje *„otrzymasz e-mail"* i **nie robi nic**
   (`ProfileModule.tsx:262`). Osobna atrapa, poza zakresem nocy.

---

## 5. STAN TECHNICZNY
- Gałąź `odbior/hub-2026-07-23`, **nic nie promowane**, demo nietknięte
- Bezpieczny punkt: tag `demo-safe-2026-07-23` = `9b143bc913`
- Audyt środowiska 18/18, bramki zielone, harness na `localhost:3000/odbior.html`
- Fala 2 w toku: 5 atrap Inicjatywy · uchwyty w Podglądzie · Menu 2 Narzędzia
- Pełny dziennik z pomiarami: `_PETLA_NOCNA_9_5_2026-07-23.md`

## 6. CO BYM ZROBIŁ DALEJ (rekomendacja)
Dwie rundy po ~1,2 pkt każda domkną 9,5. Kolejność wg zwrotu:
1. Domknąć falę 2 (w toku) — uchwyty, atrapy Inicjatywy, Menu 2 Narzędzia
2. Ślad audytowy AI (nasza regresja) + angielszczyzna w Insighcie i Powiadomieniu
3. Jedna szerokość powłoki (dziś cztery różne: 1488/1332/1496/1364) + braki sekcji panelu
4. `StandardArtifactShell` — 590 martwych linii: podpiąć albo skasować
5. Przepiąć 4 karty na wspólną powłokę (źródło wszystkich rozjazdów)

---

## 7. DOPISANE NAD RANEM (fala 2, częściowo domknięta)

### Zamknięte po napisaniu raportu
| Naprawa | Pomiar |
|---|---|
| Uchwyty w Podglądzie — Decyzja | 6 → **0** |
| Uchwyty w Podglądzie — Inicjatywa | 24 → **0** |
| Zielone CTA „Zatwierdź decyzję" | 4,35:1 → **17,09:1** (light), 17,77:1 (dark) |
| „Zgłoś do recenzji" aktywny w Podglądzie | usunięty |
| Dwa kebaby na ekranie (Inicjatywa) | scalone w jeden — **nie usunięte**, bo żadna pozycja nie dotyczyła karty (Nowe zadanie/decyzja/RAID, Eksport, Archiwizuj); usunięcie skasowałoby zdolności |
| Tytuł centrum ≠ nawigacja | zsynchronizowane |
| `expectedOutcome` (Zadanie) | łańcuch przerwany w **3 miejscach** (serwer nie zwracał kolumny, nie przyjmował jej, front nie mapował) — naprawione, 1166 zn. treści widoczne |
| Pewność w Powiadomieniu | `0.82%` → **82%** (nowa funkcja formatująca, 16 przypadków) |
| Liczniki Insightu | usunięta **możliwość** rozjazdu (jedno źródło), nie poprawione ręcznie |
| Testy Insightu | 26 błędów → **0** (5/7 zielonych) |
| Sprzeczność 96 vs 71 min | trzy stany rozdzielone; koszt przeliczony od realnego nadmiaru: 1,74 → **0,53 mln PLN** |

### ★ DWUNASTA ATRAPA — pasek zatwierdzenia zmyśla „Szkic"
`ArtifactApprovalStatusBar` przy braku rekordu spada na `state ?? 'draft'` — czyli **pokazuje „Szkic"
na każdym artefakcie, który nie ma rekordu zatwierdzenia**, niezależnie od realnego statusu.
Flaga `ff_artifactApprovalUi` jest domyślnie **WŁĄCZONA**, więc to było widoczne na żywo:
Decyzja „Szkic" przy statusie „Oczekująca", Inicjatywa „Szkic" przy „W realizacji".
Agent zabramkował to po stronie wywołań (pasek nie montuje się poza szkicem) — **właściwa naprawa
to prop stanu zamiast `?? 'draft'`**, żeby pasek pokazywał prawdę, a nie znikał.

### Zostało zablokowane zakresem (pliki zajęte przez innych agentów)
- **Chip „Edytowane" widoczny w Podglądzie** — wymaga jednej linii w `NModeCardState.tsx`
- **Ten sam zielony CTA 4,35:1 siedzi w `TaskDetailView.tsx:4956`** — ta sama jedna linia
- **Uchwyty w Podglądzie w Zadaniu** (`TaskDetailView.tsx:5500`) — `readMode={readMode}`

To trzy jednolinijkowe naprawy czekające tylko na zwolnienie plików.

---

## 8. FALA 2 SCALONA I DOMKNIĘTA (dopisane 00:5x)

### Co scalone do `odbior/hub-2026-07-23`
| Gałąź | Konflikty | Uwaga |
|---|---|---|
| `fix/piec-jednolinijkowych` | brak | `expectedOutcome` front+backend, pewność `82%`, liczniki Insightu z jednego źródła, guard `useFixedT`, sprzeczność 96/71 min |
| `fix/readmode-nav-decyzja-inicjatywa` | brak | uchwyty Decyzja/Inicjatywa, neutralne CTA, kebaby scalone |
| `fix/narzedzie-menu2-panel-jezyk` | brak | Menu 2 „Sekcje", panel 5→4 sekcje, ~90 podmian językowych |
| `fix/atrapy-inicjatywa-analysis` | brak (auto-merge `pl/translation.json`) | scalona **dwa razy** — agent skończył pracę PO pierwszym merge'u i dołożył 3 commity (pętla renderu `LogicAnalysis`, i18n paneli, `<Toaster/>` w harnessie) |

**Zero konfliktów w całej fali** — cztery gałęzie, rozłączne pliki poza `pl/translation.json`
(auto-merge, `JSON.parse` na pl i en zielony po każdym scaleniu).
Pułapka z fali 1 (dwie gałęzie kasujące ten sam podwójny import → git usuwa jedyną linię) **nie
powtórzyła się**: `dev-render/vite.config.ts` ma dokładnie jeden `import fs`, oba pluginy
`pluginUwag()`+`odbiorPlugin()`, `main.tsx` ma `React.lazy`, `PanelUwag` i wymuszanie motywu z URL.

★ **Lekcja operacyjna:** „gałąź istnieje" ≠ „gałąź skończona". Worktree agenta był `locked` i miał
niescommitowane zmiany w chwili merge'u. Gdyby nie drugi merge, do hubu nie weszłaby naprawa pętli
renderu, która **wywalała `<LogicAnalysis>` po kliknięciu „Wykryj zależności"**. Przed scaleniem
sprawdzaj `git log` gałęzi PONOWNIE po zakończeniu pracy, nie tylko na starcie.

### Trzy naprawy jednolinijkowe — ZAMKNIĘTE (commit `656bfabc1d`)
| Naprawa | Stan |
|---|---|
| `TaskDetailView` — `readMode` do `NModeLeftNav` | zrobione, 8 uchwytów → **0** |
| `TaskDetailView` — CTA „Ukończ" `bg-emerald-600` | → `bg-c-text`, **4,35:1 → 17,09:1** |
| `NModeCardState` — chip „Edytowane" w Podglądzie | nowy prop `hideBadge` |

**Dlaczego `hideBadge`, a nie rozszerzenie `hideActions`** (odstępstwo od zlecenia, świadome):
`hideActions` NIE jest równoważne Podglądowi — Decyzja wiąże je z `isDecisionStageLocked`,
Inicjatywa z `!canEditCards`. Rozszerzenie zdejmowałoby badge także w trybie Edycja przy
zablokowanym etapie. Nowy prop jest wpięty jako `hideBadge={readMode}` w Zadaniu, Decyzji (4 karty)
i Inicjatywie. Insight ma własny pasek stanu (`InsightSectionCardHeader`, nie `NModeCardState`) —
tam pasek znika w `readOnly`. **Tryb Edycja bez zmian.**

### Pomiary z własnego renderu (port 3160, playwright, 6 kart × light/dark × Edycja/Podgląd)
| Karta | Uchwyty w EDYCJI | Uchwyty w PODGLĄDZIE | Badge w Podglądzie | CTA „Ukończ" |
|---|---|---|---|---|
| Decyzja | 6 | **0** | 0 | — |
| Zadanie | 8 | **0** | 0 | **17,09:1** light · **17,77:1** dark |
| Powiadomienie | 0 | **0** | 0 | — |
| Insight | 0 | **0** | 0 | — |
| Narzędzie | 0 | *brak pstryczka* | — | — |
| Inicjatywa | 0 | **0** | 0 | — |

Wynik identyczny w light i dark. Cel „0 uchwytów w Podglądzie" — **osiągnięty na wszystkich kartach,
które mają tryb Podgląd**. Zrzuty obejrzane oczami (Zadanie light+dark w obu trybach, Decyzja dark,
Inicjatywa light+dark, Insight light, Narzędzie light).

★ **Korekta metody pomiaru — pierwszy pomiar był BŁĘDNY i pokazywał „8 uchwytów, nie naprawione".**
Harness startuje karty w trybie **Edycja**, nie Podgląd. Liczenie uchwytów zaraz po `goto` mierzy
Edycję, gdzie uchwyty są POPRAWNE. Tryb Podgląd trzeba **kliknąć**
(`[role="radio"][aria-label="Podgląd"]`). Bez tego kliknięcia pomiar oskarżyłby własną, działającą
naprawę. Wykrycie „czy jestem w Podglądzie" po tekście strony też nie działa — słowo „Podgląd"
stoi na pstryczku w obu trybach; rozstrzyga `aria-checked`.

### Weryfikacja po scaleniu
- `node scripts/odbior-audyt.mjs` → **18/18 gotowych**, bez regresji (przed i po ostatnim merge'u)
- `check-artefakt.sh` ✓ (crimson 7, baseline 274) · `check-list-canon.sh` ✓ · `check-triada.sh` ✓
- `vitest run tests/unit/services/cardAnalysisRubric.test.ts` → **18/18** (pełny vitest nieuruchamiany)
- Gałąź: **85 commitów ponad `origin/demo`**, nic nie wypchnięte, demo nietknięte

### Otwarte (znalezione przy oglądaniu, NIE naprawiane — poza zakresem zlecenia)
1. **Podgląd nie jest spójny między kartami.** Zadanie i Decyzja chowają akcje („Akcje ukryte
   w trybie podglądu"). Inicjatywa zostawia „Utwórz wariant" i nagłówkowe „Oznacz jako ukończone",
   Insight zostawia „Zgłoś do recenzji", przełącznik stanu (Szkic/Generowanie/Ukończone/
   Opublikowano/Błąd) i „Oznacz gotowe". To ta sama klasa wady, którą właśnie zamknęliśmy
   w Zadaniu — tyle że w prawym panelu.
2. **Karta Narzędzia nie ma pstryczka Edycja/Podgląd.** Może tak być z projektu (obiekt biblioteczny
   do czytania), ale wtedy odbiór powinien to nazywać, a nie zostawiać jako lukę.
3. **Powiadomienie loguje `EMPTY_LLM_RESPONSE`** w konsoli przy starcie (harness nie ma LLM) —
   ścieżka awaryjna, ale błąd leci przy samym wejściu na ekran, bez akcji użytkownika.
4. Dwunasta atrapa (`ArtifactApprovalStatusBar`, `?? 'draft'`) — nadal zabramkowana po stronie
   wywołań, nie naprawiona u źródła.
