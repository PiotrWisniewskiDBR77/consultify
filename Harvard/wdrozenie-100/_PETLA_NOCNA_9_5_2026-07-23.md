# PĘTLA NOCNA → średnia 9,5 (2026-07-23/24)

> **Mandat właściciela (idzie spać ~23:50):** doprowadzić do średniej **≥ 9,5** we wszystkich
> parametrach — czyli wejść na dziesiątki w części ocen. System **TRZECH SĘDZIÓW**:
> (1) Grafika, (2) Merytoryka, (3) Rozwiązanie IT / backend / architektura.
> Każdy ocenia swój obszar, **średnia z całości ≥ 9,5**. Pracować w pętli aż osiągnięte. ~6 h.
>
> ## ★ REGUŁA NIENARUSZALNA (nie łamię nawet dla mandatu)
> **NIC na demo bez akceptu właściciela.** Właściciel śpi → **PRACUJĘ NA GAŁĘZI, NIE PROMUJĘ.**
> Rano akcept → promocja. (CLAUDE.md #5/#7.) Ostatni stan zaakceptowany i LIVE:
> tag `demo-safe-2026-07-23` = `9b143bc913`.
>
> **Gałąź robocza pętli:** `odbior/hub-2026-07-23` (worktree `.worktrees/odbior-hub`).
> Na starcie pętli hub == origin/demo (ahead 0). Środowisko odbioru: `localhost:3000/odbior.html`
> — NIE dotykać tego portu; agenci renderują na 308x i sprzątają po sobie.

---

## 1. SYSTEM TRZECH SĘDZIÓW

Trzej **niezależni, adwersaryjni** agenci. Każdy ocenia **6 kart N** (Decyzja · Zadanie ·
Powiadomienie · Insight · Narzędzie · Inicjatywa) w SWOIM obszarze, skala 0–10 z krokiem 0,5.

| Sędzia | Ocenia | Twarde kryteria |
|---|---|---|
| **G — Grafika** | wygląd i spójność wizualna | zgodność ze standardem n-Type (Menu 1/2, panel, kolejność sekcji), tokeny `c-*` zamiast surowych, crimson tylko semantyka krytyczna, AI = fiolet, kontrast light+dark (WCAG), gęstość, brak pustych ramek/rozjazdów, animacje |
| **M — Merytoryka** | wartość treści dla konsultanta | czy karta niesie treść na poziomie doradczym, jakość promptów i kryteriów „Analizuj z AI" per typ, czy pola mają sens biznesowy, brak lorem/atrap, język PL poprawny, czy AI ocenia wg realnych kryteriów artefaktu |
| **IT — Architektura** | jakość rozwiązania | brak duplikatów komponentów, jeden wzorzec zamiast N wariantów, kontrakt danych, brak martwego kodu i fantomowych flag, backend addytywny i wstecznie zgodny, obsługa błędów, brak atrap udających funkcje, dług techniczny |

**Dyscyplina oceny (wpisana w prompt sędziego):**
- Oceniaj jak recenzent, który MA ZNALEŹĆ powody do niższej oceny. Domyślnie zaniżaj przy wątpliwości.
- **10 = nie ma się do czego przyczepić** w tym obszarze. 9 = drobiazgi kosmetyczne. 8 = realny brak.
- Każda ocena < 10 MUSI mieć **konkretny, wykonalny** powód (plik/miejsce/co zmienić). Ocena bez
  uzasadnienia jest nieważna.
- Weryfikuj w **realnym runtime** (render + DOM + kod), nie z dokumentacji. Audyty starzeją się.
- Zwracaj JSON: `{karta, ocena, braki:[{co, gdzie, jak_naprawic, waga}]}`.

**Wynik rundy:** średnia z 18 ocen (6 kart × 3 sędziów). Cel: **≥ 9,5**.

---

## 2. PĘTLA (jedna runda ≈ 60–75 min)

1. **Pomiar** — 3 sędziów równolegle → oceny + lista braków (JSON).
2. **Bramka** — jeśli średnia ≥ 9,5 **i** żadna pojedyncza ocena < 9,0 → KONIEC pętli.
3. **Naprawa** — fala robotników na TOP brakach, **rozłączne pliki** (jeden agent = jedna karta
   lub jeden obszar współdzielony). Priorytet: najniższe oceny × waga braku.
4. **Scalenie + weryfikacja** — merge do hubu, `scripts/odbior-audyt.mjs`, bramki
   (`check-artefakt`/`check-list-canon`/`check-triada`), render własnymi oczami.
5. **Dziennik** — dopisz rundę do §4 tego pliku. Wróć do 1.

**Higiena floty:** baza gałęzi = **hub** (`odbior/hub-2026-07-23`), NIGDY wąska gałąź robocza
(lekcja z 23.07 — agent na wąskiej gałęzi raportuje fałszywe „brak komponentu"). Rozłączne pliki
między agentami jednej fali. Render na portach 308x, NIE 3000. commit-per-krok, **NIE push**.

---

## 3. ZAKRES PRAC (co realnie podnosi oceny)

**Wiadome przed startem (z dziś):**
- Merytoryka to najsłabsza oś — treść AI nie była dziś ruszana. Największy zapas punktów.
- IT: uśpione crimsony w kreatorze decka (dziesiątki), martwe klasy Tailwind spoza skali,
  endpoint `/api/ai/chat` zwraca inny kształt niż czyta go ~15 miejsc w `src/`.
- Grafika: 18/18 w audycie środowiska, ale audyt mierzy próg „nadaje się do odbioru", nie „10/10".
- Insight: edytowalne pola + backend — w toku (gałąź `feat/insight-edytowalne-pola`).

**Zakazy nocne:** żywa baza (migracje tylko przygotować + zgłosić), promocja na demo,
przełączanie flag wizualnych na żywo, wymyślanie treści tam gdzie brak modelu danych
(zgłaszać zamiast tworzyć atrapy).

---

## 4. DZIENNIK RUND

| Runda | Godz. | Śr. G | Śr. M | Śr. IT | ŚREDNIA | Co naprawione |
|---|---|---|---|---|---|---|
| **baseline** | ~01:15 | **6,0** | **5,4** | **6,3** | **5,9** | pomiar wyjściowy, komplet 3 sędziów |

**Baseline — oceny per karta**

| Karta | Grafika | Meryt. | IT | Najcięższy zarzut |
|---|:--:|:--:|:--:|---|
| Decyzja | 7,5 | 6,0 | 5,0 | 11 martwych wywołań AI + zaszyte fallbacki („Skróć" tnie zdanie w połowie) |
| Zadanie | 7,5 | 5,5 | 5,5 | 2 przyciski AI = czysty `toast()`; zaszyty komentarz „AI Assistant" |
| Powiadomienie | 6,5 | 4,5 | 6,5 | surowy enum „AI RISK DETECTED"; chip 2,58:1; cichy no-op zapisu |
| Insight | 5,0 | 5,0 | 7,0 | surowy klucz i18n jako tekst; brak „Analizuj z AI" przy wejściu; testy 7/7 czerwone |
| Narzędzie | 5,0 | 6,5 | 7,5 | panel 3/7 sekcji kanonu; czerwień dekoracyjna; font 8 px |
| Inicjatywa | 4,5 | 5,0 | 6,0 | **Podgląd ≡ Edycja** (5 pól edytowalnych); 11 655 linii w pliku |
| Powłoka | — | — | 5,5 | `StandardArtifactShell` = 0 konsumentów; 4 karty sklejają powłokę ręcznie |
| Backend | — | — | 7,0 | `/ai/chat` nie zwraca `.text` → 15 martwych wywołań w `src/` |

**★ ROOT CAUSE #1 (odzysk +1,5):** `POST /api/ai/chat` zwraca orkiestrator `{role,intent,prompt}` — **bez `text`**. Walidator nie `strict` ⇒ 200, front czyta `undefined`, wchodzi w fallback. Tokeny spalone. Działający: `/ai/generate` → `{text}`. Wzorzec poprawny już w repo: `services/cardAnalysis/cardAnalysisService.ts:340`, `AIFieldEnhancer`, Inicjatywa.

**★ ATRAPY do wycięcia (nie fallbacki — usunięcie):** zaszyte 6 fraz plusów/minusów; komentarz podpisany „AI Assistant" + `toast.success` w `catch`; `slice(0,65%)` jako „Skróć"; 2 przyciski = `toast()`; `AttachmentsLinksCanvas:697` — nic się nie dzieje, bez błędu (3 karty).

**★★ ROOT CAUSE #2 — NAJWIĘKSZE ODKRYCIE NOCY (odzysk +1,5):**
**Kanwa NIE PODĄŻA za wyborem sekcji w Menu 1 — wszystkie 6 kart.** Nav podświetla nową sekcję,
toolbar zmienia przycisk, a centrum zostaje na poprzedniej. Odtworzone realnymi kliknięciami po
przeładowaniu, z czekaniem 8–12 s. Zmierzone pary: Decyzja(Konsekwencje→pokazuje Zakres),
Decyzja(Załączniki→RACI), Zadanie(Lista kontrolna→Pomysły), Zadanie(Zależności→Pomysły),
Powiadomienie(Analiza AI→Co się dzieje), Inicjatywa(Kryteria sukcesu→Opis i kontekst).
**Konsekwencja: większość zadeklarowanej treści NIGDY nie dociera do użytkownika** — dlatego
Merytoryka dostała 5,4 (sędzia nie mógł zobaczyć tego, co karty deklarują). Dla Inicjatywy 6 z 9
kryteriów właściciela jest niesprawdzalnych. NAJTAŃSZY punktowo remont w całym obszarze.
→ Naprawa w fali 2 (wszystkie 6 plików kart zajęte przez falę 1).

### Fala naprawcza 1 (7 agentów, rozłączne pliki)
IT: `fix/atrapy-ai-decyzja` · `fix/atrapy-ai-zadanie` · `fix/powiadomienie-i-backend`
Grafika: `fix/grafika-inicjatywa` · `fix/grafika-narzedzie` · `fix/grafika-insight` (baza HUB) · `fix/grafika-wspolne`
Merytoryka: `fix/rubryka-analizy` (definicje kryteriów + kotwice + DOKTRYNA) · `fix/tresc-demo-doradcza` (mock-dane)

### Fala 2 — do zrobienia po scaleniu fali 1
1. **Kanwa podąża za sekcją** (+1,5, wszystkie karty) ← najwyższy priorytet
2. „Źródła i założenia" puste mimo liczb w treści (+0,7)
3. Reszta ponglish/enumów w kartach zajętych falą 1 (+0,4)
4. Rozbicie `InitiativeDocumentView.tsx` (11 655 linii) (+0,5)
5. `StandardArtifactShell` — podłączyć albo skasować (+1,0)
6. Przepiąć 4 karty na `NModeShell` (+1,0)
7. Czerwony pakiet testów InsightViewer 7/7 (+0,5)


### Przed pierwszą rundą — Insight backend WCIĄGNIĘTY (gałąź `feat/insight-edytowalne-pola`, 6 commitów)
- **Edytowalne pola per sekcja** (decyzja właściciela): `InsightSectionManualField` = `AutoFitTextarea`
  + AI (fiolet) + `previewMode`. Draft lokalny — nieudany zapis NIE kasuje tekstu.
- **Serwer, addytywnie:** `sectionOverrides` na Insight, `PATCH /api/v8/interview/insights/:id`,
  **scalanie częściowe** (dwie osoby redagujące różne sekcje nie kasują sobie pracy — świadomie
  inaczej niż `sectionCompletions`, które podmienia całą mapę).
- ★ **MIGRACJA 931 NIEURUCHOMIONA** — `ALTER TABLE interview_insights ADD COLUMN IF NOT EXISTS
  section_overrides TEXT` (metadanowa, bez locka, wszystkie wiersze NULL). W kodzie jest dodatkowo
  *lazy guard* (auto-ALTER przy pierwszym zapisie, wzór 1:1 z istniejącego `section_completions`).
  **DO DECYZJI WŁAŚCICIELA RANO:** puścić 931 ręcznie i zdjąć guard, czy zostawić lazy.
  Dopóki nie promuję — DDL nie wykona się na demo.
- **Rezultaty**: kafle downstream z centrum → prawy panel, sekcja na kanonicznej pozycji 5.
- **„Analizuj z AI"**: wcześniejszy audyt był NIEAKTUALNY — panel strukturalny działał. Realny defekt
  był inny: `writableFieldIds=[]` + `applyChange=()=>false` ⇒ „Zastosuj" trwale wyszarzone. Teraz
  ręczna treść sekcji jest zapisywalna, więc „Zastosuj" działa naprawdę.
- **4 defekty znalezione RENDEREM, nie audytem:** dwa surowe klucze i18n (`targetMeta.decision`,
  `panelResults*`), rozpychanie panelu w wariancie compact, ramka edycyjna widoczna w Podglądzie.

**Do decyzji właściciela rano (zgłoszone, nie zmyślone):**
1. „Konwertuj na inicjatywę" istnieje teraz w DWÓCH miejscach (nagłówek = kreator, kafel Rezultaty =
   bezpośredni POST). Lista 04 §6.3 dopuszcza kafel „jeśli nie pozostaje w nagłówku". Nie usunąłem
   sam — to odebranie działającej zdolności.
2. Czy ręczna treść ma WYPIERAĆ blok AI w Podglądzie/eksporcie/PDF? Dziś leży NAD nim, nie zamiast.
3. Lazy-ALTER vs ręczna migracja (wyżej).
4. `„Historia / AI"` → `„Historia"` w panelu Insightu (lista 04 §6.2) — poza zakresem tamtego zadania.

---

## 5. RANO DLA WŁAŚCICIELA
- Ten plik = stan. Oceny sędziów = pomiar (uzasadnienia per brak).
- Wszystko na gałęzi hubu, **nic nie promowane**. Po akcepcie → promocja procedurą
  `consultify-promocja-demo` + re-tag `demo-safe`.
- Jeśli któryś obszar nie dobił 9,5 — jawnie napisane dlaczego i czego brakuje (zwykle: decyzja
  właściciela albo backend/model danych).


---

## 6. STAN FALI 1 (aktualizowany na bieżąco)

### ZAMKNIĘTE (gałęzie gotowe do scalenia, NIE pushowane)

| Gałąź | Efekt (zmierzony w runtime) |
|---|---|
| `fix/grafika-inicjatywa` | **Podgląd ≡ Edycja NAPRAWIONE** — w Podglądzie 0 edytowalnych select/date, 0 uchwytów, 0 AI-przy-polach. Kontrast **3,47 → 17,06:1** (light), 14,89 (dark). Pasek „Draft" do `statusBar`. „Analizuj z AI" obecne w 3 stanach. Data jako tekst zamiast `dd/mm/yyyy`. Menu 2 tło przezroczyste. Panel 217→92 px. „Forkuj"→„Utwórz wariant" |
| `fix/grafika-insight` (baza HUB) | „Analizuj z AI" bezwarunkowo (było ukryte w Podglądzie = przy wejściu). Uchwyty **11 → 0** w Podglądzie. Menu 2 zrównane z Menu 1 co do piksela. Kontrast 4,21→8,33 / 4,08→6,50. „Findingi"→„Ustalenia". ★ Sprawdził, co hub już naprawił — **4 z 9 zadań były zrobione, nie dublował** |
| `fix/atrapy-ai-decyzja` | **11 martwych wywołań `/ai/chat` → `/ai/generate`.** Wycięte WSZYSTKIE zaszyte atrapy (6 fraz plusów/minusów, komentarz „AI Assistant"+`toast.success`, `slice(0,65%)` jako „Skróć", 2 reguły przypomnień, 9 zdań scenariuszy). Usunięte 25 kluczy i18n obsługujących kłamstwa, dodane 20 uczciwych błędów. **Weryfikacja symulacją awarii AI: treść bit-w-bit nietknięta** |
| `fix/rubryka-analizy` | 38 kryteriów × `definition` + `failsWhen` (PL+EN). Kotwice `completeness` (4 pasma z warunkiem liczbowym). **Blok `=== DOKTRYNA ===`** (piramida · MECE · kwantyfikacja z założeniami · falsyfikowalność · uczciwa niepewność + reguła językowa + kontrola spójności liczb). Powiadomienie: tautologia `gaps/risks` → 5 realnych osi. Dedup przed limitem 6. Sortowanie wagą. Prompt 2772→9230 zn., progi 0→10. **Test 18/18** |
| (nadzorca) | `dev-render/vite.config.ts` — podwójny `import fs` (mój artefakt scalania), naprawione |

### W TOKU (7 agentów)
`fix/atrapy-ai-zadanie` · `fix/powiadomienie-i-backend` · `fix/grafika-narzedzie` · `fix/grafika-wspolne` · `fix/tresc-demo-doradcza` · `fix/panel-akcji-kolor` · `fix/atrapa-aifieldenhancer`

### ★ ODKRYCIA WYMAGAJĄCE DECYZJI WŁAŚCICIELA (zebrane, NIE rozstrzygnięte)
1. **Nagłówek „Scenariusze AI (real-time)"** stoi nad polami wypełnianymi ręcznie — obietnica, której produkt nie dotrzymuje. Proponowane: „Scenariusze konsekwencji".
2. **3 handlery AI w Decyzji są martwe** (zero wywołań w drzewie renderu): `generateProsConsForAlternative`, `suggestRemindersAI`, `suggestEscalationsAI`. Naprawione, ale niepodpięte — podpiąć czy skasować?
3. **Karta wywiadu NIE MA przycisku analizy** — sędzia IT się mylił. `InterviewWorkspace.tsx` nie importuje `useCardAIAnalysis` ani `Menu2AIButton` (grep: 0). Potwierdza decyzję właściciela: wywiad nie jest kartą N.
4. **Koszt promptu wzrósł o ~2,5 tys. znaków/wywołanie** (blok doktryny; łącznie ~9 tys. przy limicie 30 tys.). Jeśli budżet AI napięty — można ciąć, ale wracają wady językowa i liczbowa.
5. **`severity` bez wartości**: agent świadomie NIE odrzuca pozycji (odrzucona znika bez śladu ⇒ użytkownik czyta krótszą listę jako „karta czystsza niż jest"). Zamiast tego `low` + jawny dopisek + koniec listy.
6. Dwa kebaby na ekranie (Menu 1 + Menu 2) — wymaga zmiany `NModeHeader`, kontrakt powłoki.
7. `initiatives.fork2` = „Forkuj" — klucz współdzielony, zmiana wychodzi poza kartę.

### FALA 2 — kolejność po scaleniu fali 1
1. ★ **Kanwa podąża za sekcją** (+1,5, wszystkie 6 kart) — najwyższy priorytet, najtańszy remont
2. „Źródła i założenia" puste mimo liczb w treści (+0,7)
3. `StandardArtifactShell` — podłączyć albo skasować (+1,0)
4. Przepiąć 4 karty na `NModeShell` (+1,0)
5. Czerwony pakiet testów InsightViewer 7/7 (+0,5)
6. Rozbicie `InitiativeDocumentView.tsx` (11 655 linii) (+0,5)

---

## 7. ★ MÓJ BŁĄD, KTÓRY BLOKOWAŁ REGUŁĘ #7 CAŁEJ FLOCIE

`dev-render/vite.config.ts` — **podwójny `import fs from 'fs'`** (artefakt mojego scalania hubu).
Skutek nie był kosmetyczny: **harness dev-render NIE STARTOWAŁ WCALE.** Czyli każdy agent bazujący
na `origin/demo` miał zablokowaną weryfikację wzrokiem — regułę, na której stoi cała dyscyplina
(„właściciel nigdy nie jest pierwszym testerem wizualnym"). Dwaj agenci zgłosili to niezależnie,
trzeci musiał naprawić u siebie, żeby cokolwiek zobaczyć.

Naprawione w hubie (`1 import fs`). **`origin/demo` NADAL MA 2** — wchodzi z następną promocją.
Lekcja: po każdym scaleniu **uruchom harness**, nie tylko esbuild. Błąd składni w konfiguracji
dev-only nie zapala żadnej bramki, a wyłącza jedyne narzędzie weryfikacji wizualnej.

## 8. ZAMKNIĘTE — fala 1 (8/12)

| Gałąź | Efekt zmierzony |
|---|---|
| `fix/grafika-wspolne` | pasek stanu PL (Szkic/W recenzji/Zgłoś do recenzji); kontrast nagłówków **2,46→7,26:1** light, **2,52→10,82:1** dark, 10→11 px; uchwyty w Podglądzie Insight **22→0**; odstęp M1↔M2 0→8 px; Menu 1 62→60 px. ★ Pomiar „było" wykonany samodzielnie przez cofnięcie plików do `origin/demo` |
| `fix/atrapy-ai-zadanie` | **6 generatorów udawało AI przez `setTimeout(1200–1800)` + zaszytą treść identyczną dla każdej karty** (opis, rezultat, checklista, pomysły, ryzyka, alternatywy) — wycięte. Fallback RACI wstawiał pierwsze 4 osoby z listy i meldował SUKCES — wycięty. 2 przyciski-atrapy PODŁĄCZONE (nie usunięte) z uzasadnieniem. Weryfikacja przełącznikiem `ai=ok\|down`: przy awarii treść bit-w-bit nietknięta |
| `fix/powiadomienie-i-backend` | współdzielona atrapa `AttachmentsLinksCanvas:697` (3 karty) → widoczny banner błędu zamiast ciszy. Backend: cichy 200 przy 0 wierszy → **404**; `/worksheet` bez walidacji → zod `.strict()` + limit 20 tys. zn.; read-modify-write → **compare-and-swap** (transakcja niemożliwa: pula nie przypina klienta). Union `NModeArtifactType` poszerzony o `notification`/`tool` — rzutowanie usunięte |

### ★ Wymaga JEDNEJ LINII w 4 kartach (fala 2)
Decyzja, Zadanie, Powiadomienie, Inicjatywa renderują `NModeLeftNav` **bezpośrednio, z pominięciem
`NModeShell`** — poprawka uchwytów w Podglądzie ich nie dosięga (6/8/—/24 uchwyty zostają).
Naprawa: `readMode={readMode}` przy `<NModeLeftNav>`. Pliki były zajęte przez innych agentów.
To ten sam dług, co „4 karty sklejają powłokę ręcznie" z werdyktu sędziego IT.

### Nowe defekty znalezione po drodze (nie z werdyktów)
- Ciemny motyw Decyzji: kafelek wybranego typu relacji ma biały spód i biały podtytuł — nieczytelny.
- `KnownToolDetailView.tsx:107` — zbędny cast `'tool' as unknown as NModeArtifactType` (union już go ma).
- Skala defektu localStorage **mniejsza** niż w moim briefie: ekran NIE robi się pusty (fallback `extras`),
  realny skutek to śmieciowe pozycje w pickerze. Agent uczciwie skorygował moje zawyżenie.

## 9. `fix/tresc-demo-doradcza` — ZAMKNIĘTE (9/12)

11 poprawek treści do poziomu doradczego. Wszystkie liczby uzgodnione MIĘDZY kartami:
przezbrojenie 96→84→71→cel 45 min (opis · KPI-1 · historia · kryteria), marża 4 500 PLN/h
(ZAŁOŻENIE, kontroling VII 2026) → 1,74 mln PLN/rok, warianty Decyzji 168/74/9 tys. i 6/3/1 tydz.,
stawka zwłoki 32 tys./tydz. spójna między Decyzją a Powiadomieniem (77 tys. za 12 dni).
Każda liczba ma podstawę wyliczenia i warunek falsyfikujący („co obaliłoby tę diagnozę").

### ★ DEFEKTY KOMPONENTÓW znalezione przez pracę nad treścią (fala 2)

1. **★★★ `TaskDetailView.tsx` — `loadTask()` NIE MAPUJE `expectedOutcome`.** Stan `useState('')`
   (l. 524), zapisuje go wyłącznie generator AI (1989, 4366). **Pole nie renderuje się mimo
   poprawnych danych** — to dlatego sędzia merytoryki widział placeholder zamiast kryteriów
   akceptacji. Naprawa: `setExpectedOutcome(task.expectedOutcome || '');` + dopisać do
   dirty-check i payloadu zapisu. JEDNA LINIA.
2. **★★★ `DecisionDetailView.tsx` — opcje nie renderują liczb.** `estimatedCost`,
   `estimatedDuration`, `confidence`, `impactScore`, `isRecommended` są w danych, nie ma dla nich
   miejsca w widoku. Karta o trade-offach bez jednej liczby na ekranie.
3. **★★ `InitiativeDocumentView.tsx` — etykieta kłóci się z nazwą pola.** Karta „OPIS ROZWIĄZANIA"
   (6464-6497) podpięta pod `problemDefinition.rootCause` (hydratacja 2306). Ekran mówi
   „rozwiązanie", pole mówi „przyczyna źródłowa" — każdy autor treści musi o tym wiedzieć.
   Potrzebne osobne `proposedSolution`.
4. **★ `InsightViewer` — trzecia kolumna „Odczytu" nieosiągalna dla treści PL.** `hiddenSignals`/
   `opportunityReadout` (1903-1925) filtrują nagłówki wyrażeniem WYŁĄCZNIE angielskim
   (`opportunit|strength|theme|trend`, `observation|between|pattern|signal`). W mocku udało się
   trafić przypadkiem („Trendy…" łapie `trend`) — wzorce wymagają polskich odpowiedników.
5. **★ `InsightViewer` — „Podsumowanie" bierze pierwszy akapit `content`** zamiast pola
   `executiveSummary`; gdy treść zaczyna się nagłówkiem, sekcja pokazuje samo słowo „Kontekst".
6. **★★ i18n:** legenda Narzędzia mówi 6 etapów, kafle i licznik mówią 5. Anglicyzmy widoczne na
   zrzutach: „Findingi", „OFFICIAL ANSWERS/ISSUES/RISKS/SIGNALS/OPPORTUNITIES", „opportunities",
   „action planów", „downstream", „lineage".

## 10. `fix/grafika-narzedzie` + `fix/atrapa-aifieldenhancer` — ZAMKNIĘTE (11/12)

**Narzędzie:** panel 3→**5 sekcji kanonu** (Akcje·Właściwości·Powiązania·Źródła i założenia·Rezultaty),
Rezultaty `defaultOpen:false`, czerwień dekoracyjna → neutralny kafel + ikona (2 warianty + 2 miejsca,
gdzie crimson był KATEGORIĄ DANYCH), kontrast light 19→3 naruszeń (reszta w `NModeLeftNav`),
czcionki <11 px **27→0**, chipy EN→PL, przełącznik bez skutku UKRYTY.
★ **Komentarze i Historia NIE dodane — brak źródła danych**, zweryfikowane w `knownTools.routes.ts`
(tylko `GET /` i `GET /:toolType`; zero POST, zero tabeli komentarzy, jeden znacznik `createdAt`).
Kanon: „lepiej brak niż pusty akordeon udający funkcję". Zgodne z decyzją właściciela (biblioteka read-only).

**AIFieldEnhancer (12 ekranów):** `fallbackRefineText` usunięta w całości (63 linie) — to ona podawała
jako propozycję AI `slice(0,65%)`, „It is hereby noted that…", stały akapit. Oba `catch { text='' }`
GUBIŁY kod błędu i to pusty wynik odpalał atrapę. Dowód nietykalności treści: **suma kontrolna znaków
identyczna** przed/po awarii (856589=856589), nie „wygląda tak samo".
★ Znalazł drugą stronę tej samej choroby: prompt WMAWIAŁ modelowi `Status: draft` i `Priority: medium`,
których karta nie ma — usunięte.

### ★ LEKCJE WERYFIKACJI (obie z tej nocy, obie kosztowne)
1. **`esbuild` NIE łapie błędów składni JSX** (`{/* */}` w gałęzi ternary) — złapał dopiero prawdziwy
   render przez Babel. Bramka „esbuild PASS" nie jest dowodem, że ekran wstanie.
2. **Pomiar w jednym viewporcie kłamie.** Łatka Menu 2 wyrównywała szerokie okno i psuła wąskie
   (`-mx-6` wypychało M2 na x=0, gdy oba menu były już zgodne). Agent ją WYCOFAŁ — dobra decyzja.

### Fala 2 — dopisane
- **Menu 2 rozjazd = jedna linia w `NModeShell`** (`px-6` WEWNĄTRZ limitu dla M2 `:119` vs NA ZEWNĄTRZ
  dla M1 `:102`). Klasami z wnętrza karty się tego nie wyrazi — warunek zależy od szerokości dziadka.
  Wspólne dla 6 kart. Tło paska (`bg-white/95`, `:64`) — uwaga: pasek jest `sticky`, przezroczystości
  nie zdejmować w ciemno.
- **Szerszy audyt wzorca „lokalna atrapa zamiast AI"**: grep `fallback` w `src/` z oceną, które gałęzie
  PRODUKUJĄ TREŚĆ (atrapa), a które tylko trzymają stan UI (poprawne). Naprawiliśmy 3 miejsca, ale
  nie wiemy, ile zostało.
- `AI_BUDGET_EXHAUSTED`/`ACCESS_BLOCKED` nie dochodzą jako kod — `src/services/api.ts` rzuca goły
  `Error(message)` bez `err.data` dla 403.

## 11. `fix/panel-akcji-kolor` — ZAMKNIĘTE (12/12, fala 1 KOMPLETNA)

7 tonów pastelowych → **1 neutralny**; rozróżnienie typu przez KSZTAŁT IKONY, nie kolor.
Żaden ton nie niósł semantyki (7 równorzędnych akcji „utwórz X"); jedyna realna semantyka
(kafel zablokowany) wyrażona przez `disabled`+tooltip. Kontrast etykiet **2,56→7,24:1** light,
**3,49→9,07:1** dark, 10→11 px. Rozpychanie wariantu compact **47 px → 0** (sprawdzone też
skrajnie długą etykietą wstrzykniętą kontrolnie). Klucz `targetMeta.decision` naprawiony.
Znalezione po drodze: **oba modale miały min. 2,14:1** → 10,91:1.

### ★★ KOREKTA MOJEJ WŁASNEJ DIAGNOZY — „animacja wejścia"
Przez cały dzień powtarzałem (także właścicielowi i w instrukcjach dla agentów): *„karty mają
animację wejścia opacity 0.18→1, poczekaj ~2,5–5 s przed pomiarem"*. Agent panelu zmierzył coś
innego: w `karta-insight` **przodek panelu UTYKA na `opacity ≈ 0.26`** (inline `opacity: 0`),
czyli animacja **nigdy się nie kończy** — całe centrum karty zostaje trwale przygaszone.
Występuje IDENTYCZNIE przed jego zmianą (nie regresja), do zrzutów musiał wymuszać `opacity:1` przez JS.

**To znaczy, że moja rada „poczekaj i będzie dobrze" była błędna** — czekanie nie pomaga, bo to nie
jest animacja w trakcie, tylko zacięty stan. Do zweryfikowania w fali 2 na wszystkich kartach:
czy ekran po 5 s ma `opacity=1`, czy utyka. Jeśli utyka — użytkownik widzi przygaszoną kartę
ZAWSZE, a nie tylko przez chwilę po wejściu.

### Do decyzji właściciela (dopisane)
- **Oba modale mają powłokę na sztywno ciemną** (`bg-navy-900`) — nie idą za motywem aplikacji.
  Agent naprawił tylko kontrast (jasny tekst na ciemnym), NIE zmieniał języka projektowego.
  Kanonicznie powinny jechać na tokenach `c-*` i być jasne w jasnym motywie — zmiana wizualna, czeka.
- **`TEXT_L1` w `src/styles/typography.ts` kończy się `dark:text-slate-500`** → 3,40:1 na wymuszonym
  granacie. To SSOT typografii — jeśli inne komponenty używają go na ciemnym tle, mają ten sam defekt.
  Agent obszedł lokalnie, SSOT nietknięte. Warte osobnego sweepu.

---

# RUNDA 2 — WYNIKI

| Sędzia | R1 | R2 | Δ |
|---|:--:|:--:|:--:|
| Grafika | 6,0 | *w toku* | — |
| Merytoryka | 5,4 | **6,6** | +1,2 |
| IT / architektura | 6,3 | **7,6** | +1,3 |

## ★★ ODWOŁANIE „NAJWAŻNIEJSZEGO DEFEKTU NOCY" — KANWA DZIAŁA

**Bug „kanwa nie podąża za sekcją" NIE ISTNIEJE.** Sędzia merytoryki sam go odwołał w rundzie 2:
zmierzył 24 sekcje w 6 kartach — **wszystkie przełączają treść poprawnie**, unikalny hash centrum,
treść zgodna z etykietą. Wszystkie 6 par z rundy 1 przechodzi.

**Przyczyna błędnej diagnozy (wpadł w nią dwa razy, drugi raz dziś):** treść kart siedzi
w `<input>`/`<textarea>.value`, a **`innerText` tego NIE POKAZUJE**. Do tego stały nagłówek,
nawigacja i prawy panel dominują odczyt strony ⇒ sekcje „wyglądają" na identyczne i puste.
Dopiero zrzut ekranu + odczyt `.value` pokazały 168 000 PLN, podstawę wyliczenia i warunek falsyfikujący.

**Konsekwencje dla nas:**
- Ocena 5,4 z rundy 1 była **zaniżona przez wadę pomiaru, nie wadę produktu**.
- Pozycja „+1,5 za naprawę kanwy" z planu fali 2 — **SKREŚLONA**, nie ma czego naprawiać.
- ★ **Metoda pomiaru:** `innerText` nie wystarcza do oceny kart z polami formularza.
  Mierz `.value` pól + zrzut ekranu. Ta sama pułapka co „esbuild nie łapie JSX" i „jeden viewport kłamie".

## ★ REGRESJA, KTÓRĄ SAMI WPROWADZILIŚMY TEJ NOCY
Migracja `/ai/chat` → `/ai/generate` **wycięła ślad audytowy AI**. `AIAuditLogger.logSuggestion`
jest wołany WYŁĄCZNIE z `/chat` (`ai.routes.ts:5910`); `/generate` (`:5829-5881`) nie loguje nic.
⇒ każda akcja AI w 6 kartach zniknęła z rejestru widocznego w SuperAdmin. Cicha regresja governance —
nic się nie psuje widocznie, żadna bramka nie świeci. **Do naprawy w fali 2.**

## ★ 5 ŻYWYCH ATRAP, KTÓRYCH RUNDA 1 NIE ZNALAZŁA (Inicjatywa)
`Initiatives/Analysis/{Resources,Feasibility,Completeness,Timeline,Logic}Analysis.tsx` — przycisk
`Sparkles`, spinner „Analyzing…", `setTimeout` 600–800 ms udający myślenie, **zero wywołań API**.
Komentarz w kodzie: `// Simulate a short delay for "thinking"`. Klikalne w produkcie
(`AppRoutes:1840` → `InitiativesHub:1538` → `PortfolioAnalysisView`).
Dlatego Inicjatywa spadła 6,0 → 5,0 mimo napraw. → agent `fix/atrapy-inicjatywa-analysis` w toku.

Poza zakresem, do rejestru: `useNotificationPreferences.ts:56-141` (atrapa, backend ISTNIEJE),
`views/settings/ProfileModule.tsx:262-278` — „Eksport danych" melduje *„otrzymasz e-mail"* i nie robi NIC.

## TOP jednolinijkowe naprawy (największy stosunek zysku do ryzyka)
1. `TaskDetailView.loadTask()` — brak `setExpectedOutcome(task.expectedOutcome || '')`. Najlepszy blok
   falsyfikowalnych kryteriów w całej partii jest **niewidoczny**. Zgłoszone w R1, nadal nie zrobione.
2. Powiadomienie — **„Pewność: 0.82%"** zamiast 82% (błąd formatowania ×100).
3. Insight — liczniki sprzeczne na jednym ekranie: `ISSUES/RISKS 10` vs nawigacja `5` vs panel `Ustalenia 5`.
4. `NModeCardState.tsx:79` — `i18n.getFixedT` bez guardu ⇒ **10 czerwonych testów** Insightu.
5. Inicjatywa — opis mówi „96 min" (czas teraźniejszy), własna tabela KPI mówi 71 ⇒ **koszt bezczynności
   zawyżony ~3,3×**. Rubryka łapie to jako naruszenie `doktryna-liczby` — cytuje ten przypadek dosłownie.

★ **Rubryka jest teraz ostrzejsza niż treść, którą ocenia** — łapie własną treść demo
(`required-action.failsWhen` oblewa listę Powiadomienia; `doktryna-liczby` łapie 96 vs 71).
Narzędzie pomiarowe przestało być wąskim gardłem.

## RUNDA 2 — KOMPLET

| Sędzia | R1 | R2 | Δ |
|---|:--:|:--:|:--:|
| Grafika | 6,0 | **7,1** | +1,1 |
| Merytoryka | 5,4 | **6,6** | +1,2 |
| IT | 6,3 | **7,6** | +1,3 |
| **ŚREDNIA** | **5,9** | **7,1** | **+1,2** |

Per karta (G / M / IT): Decyzja 8,0/8,0/8,0 · Zadanie 7,5/6,0/8,0 · Powiadomienie 7,0/5,5/7,5 ·
Insight 7,0/5,5/8,0 · Narzędzie 6,5/8,0/9,0 · Inicjatywa 6,5/6,5/5,0 · Powłoka —/—/7,0 · Backend —/—/8,0

## ★★ DRUGIE ODWOŁANIE MOJEJ DIAGNOZY — animacja NIE utyka
Wpisałem wcześniej, że przezroczystość „utyka na 0,26 i karta jest trwale przygaszona", i że moja
wcześniejsza rada „poczekaj 5 s" była błędna. **To też było nieprawdą.** Sędzia grafiki próbkował
co 300 ms przez 8,1 s: najgłębsze przygaszenie to 0,628 (Inicjatywa) przy 0,6 s, **wszystkie karty
na 1,0 po 0,9 s**. Po 6 s zero elementów kontenerowych poniżej 1,0. Obserwacja „0,26" była trafieniem
w środek przejścia. **Metoda „poczekaj ~2 s i mierz" jest poprawna.**

★ Sędzia grafiki **odrzucił też własny pierwszy pomiar kontrastu** — liczenie tła przez uśrednienie
przystanków gradientu dało 27 fałszywych alarmów (rzekome 1,06:1 na kaflach bez zarzutu). Wiarygodny
wynik daje kolor tekstu z computed style zestawiony z **dominującym pikselem ze zrzutu**.

### ★ TRZY FAŁSZYWE DIAGNOZY W JEDNEJ DOBIE — wzorzec do zapamiętania
1. „`dark:via-*` nie działa" — mylił mnie escapowany dwukropek w grepie. **Działa.**
2. „Kanwa nie podąża za sekcją" (+1,5 w planie!) — `innerText` nie pokazuje `.value` pól. **Podąża.**
3. „Animacja utyka na 0,26" — trafienie w środek przejścia. **Kończy się w <1 s.**
Za każdym razem: narzędzie pomiarowe kłamało, wynik brzmiał spójnie z resztą obrazu, uwierzyłem
i **przekazałem dalej jako fakt**. Reguła na przyszłość: zanim ogłosisz defekt systemowy, zweryfikuj
go DRUGĄ, niezależną metodą. Zrzut ekranu rozstrzyga spór między pomiarami DOM.

## Fala 2 — w toku
`fix/atrapy-inicjatywa-analysis` (5 żywych atrap) · `fix/piec-jednolinijkowych` (expectedOutcome,
0.82%, liczniki Insight, guard i18n, 96 vs 71) · `fix/readmode-nav-decyzja-inicjatywa` (uchwyty +1,6,
CTA, sprzeczne stany) · `fix/narzedzie-menu2-panel-jezyk` (Menu 2, ~42 wtręty EN)

### Do fali 3 (nie zaczęte)
- **Ślad audytowy AI** — `/generate` nie loguje do `AIAuditLogger` (nasza regresja, cicha)
- Uchwyty w Zadaniu (`TaskDetailView.tsx:5500`) — plik był zajęty
- Angielszczyzna Insight (5 etykiet + chip `AI-draft`) i Powiadomienia (`AI RISK DETECTED`, `Kategoria: ai`)
- Zepsute cytaty `". ` w Insighcie (naprawa upstream w parserze, nie w widoku)
- Niespójna szerokość powłoki: 1488 / 1332 / 1496 / 1364 + rynna 0 px w Powiadomieniu
- Braki sekcji panelu: Powiadomienie (Komentarze), Decyzja/Zadanie (Rezultaty)
- `StandardArtifactShell` 590 l. martwych — podpiąć albo skasować
- 4 karty poza `NModeShell`; 6 rozjazdów flagi `cardContract`
- Puste sekcje treści: Zadanie 4/8, Inicjatywa (Wpływ finansowy, OKR, Hipoteza, Wnioski)
