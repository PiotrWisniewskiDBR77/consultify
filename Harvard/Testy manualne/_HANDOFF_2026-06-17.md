# HANDOFF — System testów manualnych Consultify (2026-06-17)

> **Dla kolejnego agenta:** to jest punkt wejścia. Przeczytaj ten plik w całości, potem `_INDEX.md`, `_AUDYT_ZGODNOSCI.md`, `_BACKLOG_FINDINGI_APLIKACJI.md`. Wszystko żyje w `Harvard/Testy manualne/`.
> **Status:** system testowy KOMPLETNY i CZYSTY. Następny etap = WYKONYWANIE testów (nie pisanie).
> **Repo:** `/Users/piotrwisniewski/Documents/Antygracity/DRD/consultify` · branch `Londyn`
> **Autor pracy:** sesja Claude 2026-06-15…17 (Piotr = product/strategy, Claude = CTO).

---

## 1. O co chodzi (kontekst)

Consultify to aplikacja konsultingowa AI (React/TS frontend + Node/Express backend + Postgres na Railway). Ma **27 modułów + 1 aneks (A1)** wg realnego Sidebara — mapa: `Harvard/podzial/_MODULE_MAP_V2.md`.

Celem tej pracy było zbudowanie **kompletnego systemu testów manualnych E2E dla całego programu** — „ostatnie testowanie" przed wdrożeniem, pokrywające wszystkie funkcje, workflow i wielkie ścieżki produktowe (Wywiad → Wnioski → Inicjatywy → Wdrożenie → Rezultaty). Folder docelowy: **`Harvard/Testy manualne/`**.

---

## 2. Co powstało (stan na 2026-06-17)

### Folder `Harvard/Testy manualne/`
- **28 plików testów** `TESTY_<Mxx>_<NAZWA>.md` (M01–M27 + A1) — ~**1 954 scenariusze** łącznie. Wszystkie śledzone w gitcie.
- **`_INDEX.md`** — indeks-master: tabela 28 modułów, statusy, konwencja formatu, legenda `[MANUAL]/[FLAG]/[DB]`, linki do audytu/backlogu.
- **`_AUDYT_ZGODNOSCI.md`** — raport niezależnego audytu zgodności (werdykty + 6 wzorców systemowych).
- **`_BACKLOG_FINDINGI_APLIKACJI.md`** — realne defekty KODU wykryte przy audycie (P1 bezpieczeństwo + dług). NIETKNIĘTE — do osobnej naprawy.
- **`WYNIKI_M01-M04_2026-06-16.md`** — wyniki pierwszego wykonania testów M01–M04 (handoff agent już je przeszedł).
- **0 duplikatów** (wcześniej było 32 śmieci `… 2.md` z synchronizacji iCloud — usunięte).

### Materiał źródłowy (grounding, NIE edytować bez powodu)
- `Harvard/podzial/_MODULE_MAP_V2.md` — mapa 27+1 modułów (route, gating, inwentarz).
- `Harvard/wdrozenie-100/M*.md` — teczki per moduł z sekcją **F·EPIKI** (z nich brano pokrycie). UWAGA: bywają nieaktualne vs kod.
- `Harvard/modules/M*/KARTA_AUDYTU.md` — pełne audyty per moduł.
- `Harvard/podzial/inventory/INV_*.md` — inwentarze funkcjonalności.
- `Harvard/RAPORT_TESTOW_M01_M02_2026-06-14.md` — wcześniejszy raport żywych testów M01/M02.

---

## 3. Chronologia — co dokładnie zrobiono (etapy)

1. **Utworzenie folderu** `Harvard/Testy manualne/` i przeniesienie tam 4 istniejących wzorcowych testów (M01–M04) przez `git mv`. Zbudowanie `_INDEX.md`.
2. **Napisanie 24 brakujących testów** (M05–M27 + A1) — równolegli agenci, każdy zakotwiczony w teczce `wdrozenie-100` + inwentarzu + REALNYM kodzie. Każdy test ma cross-module journeys (główna ścieżka: M10→M13→M14→M15).
3. **Niezależny audyt adwersarialny** wszystkich 24 testów — osobny agent per moduł, weryfikujący twierdzenia o kodzie bezpośrednio w repo (grep/Read). Werdykt wyjściowy: **3 NIEZGODNE (M08, M13, M22) + 21 zgodnych-z-uwagami**.
4. **Naprawa 3 NIEZGODNYCH** (M08, M13, M22) — przepisanie warstwy faktów z kodu.
5. **Domknięcie pozostałych 21** (3 fale × 7 agentów) — wszystkie uwagi DO_POPRAWY zastosowane, każda zweryfikowana w kodzie.
6. **Czyszczenie** 32 duplikatów `… 2.md`.
7. **Backlog findingów aplikacji** spisany osobno (decyzja Piotra: nie ruszać kodu teraz).

**Wynik końcowy: 28/28 testów CZYSTYCH.** Warstwa faktów (endpointy/ścieżki/numery linii/flagi/statusy) przepisana z realnego kodu jako SSOT. Nieaktualne „known-bugi" przeklasyfikowane na testy regresji.

---

## 4. KLUCZOWA NAUKA (czytaj, zanim cokolwiek zmienisz)

**Wzorzec porażki:** agenci piszący specy odtwarzali endpointy/ścieżki/linie z DOKUMENTÓW (`wdrozenie-100`, KARTA) zamiast z KODU → fałszywe FAIL-e (zły endpoint) i nieaktualne known-bugi (bug już naprawiony cytowany jako P0).

**Zasada nadrzędna:** każdy endpoint/ścieżkę/numer linii w teście POTWIERDŹ w kodzie (`grep` w `src/services/api.ts` + `server/src/routes/`). Dokumenty bywają stale. Audyt domykający kilkukrotnie korygował SAM AUDYT, ufając kodowi.

**Prod caution:** backend dev (:3001) może wskazywać na PRODUKCYJNĄ bazę Railway (centerbeam). Przed JAKIMKOLWIEK zapisem danych potwierdź `DATABASE_URL`. Staging = caboose (`.env.staging.local`). NIGDY zmian na prod bez osobnej zgody Piotra.

---

## 5. Konwencja testu (każdy plik trzyma ten wzorzec — wzór = `TESTY_M01_CZAT.md`)
§0 kontekst architektoniczny (tabela komponent↔plik↔stan) → setup (dev :3000/:3001, login OWNER DBR77, DevTools Network+Console) → sekcje per-funkcja (kroki+asercje+edge-case'y, warianty happy/negatywne/graniczne/role) → ścieżki cross-module → mapa epików → testy przekrojowe (i18n PL/EN, dark, A11y, zero błędów konsoli) → regresja → format raportu + DoD.
**Zasada dowodu E2E:** PASS = potwierdzenie w Network (endpoint+payload) i/lub DB. **Sam wygląd UI to NIE dowód.** Znaczniki: `[MANUAL]` (drag/audio/OAuth/incognito), `[FLAG]` (flaga/rola), `[DB]` (wiersz/kolumna).

---

## 6. Backlog findingów APLIKACJI (defekty KODU, nie testów) — NIETKNIĘTE
Pełna lista: `_BACKLOG_FINDINGI_APLIKACJI.md`. Najważniejsze:
- **F1 [M13, P1-bezpieczeństwo]** — bramki stanów inicjatyw (`submit-review/approve/reject/start-execution`) NIE egzekwują ról na serwerze (RBAC tylko na `PATCH /:id/status`). Gating tylko po stronie klienta → możliwy bypass. Do empirycznej weryfikacji: czy dedykowane endpointy zwracają 200 dla nieuprawnionej roli.
- **F2 [M16, P2-bezpieczeństwo]** — `getModel(modelId, orgId?)` z opcjonalnym org-scope.
- **F3 [M23, P2]** — Goals/Challenges/Strategy NIE zasilają Teresy (`organization_context_store` nieczytany przez `OrganizationContextService`; kontekst Teresy bierze cele z `organization_context_claims`).
- **F4 [M22]** — martwe przyciski pause/archive w Research.
- **F5 [M19]** — Collaborate STUB nieukryty (DP-5 niewdrożona).
- **F6/F7 [M13]** — sprzeczność źródła `canExecuteGate`; przejścia DONE→TRACKING/APPROVED→SCHEDULED bez dedykowanego endpointu (tylko `PATCH /:id/status`).

---

## 7. CO DALEJ — wznowienie pracy (następny etap = WYKONYWANIE testów)

Stan: testy gotowe, M01–M04 już raz wykonane (`WYNIKI_M01-M04_2026-06-16.md`). Pozostaje wykonać M05–M27 + A1.

**Punkt wejścia dla kolejnego agenta:**
1. Przeczytaj ten handoff + `_INDEX.md`.
2. Wybierz paczkę modułów do wykonania (sugestia: kontynuuj główną ścieżką produktową — **M10 Wywiad → M13 Inicjatywy → M14 Wdrożenie → M15 Rezultaty**, bo to rdzeń biznesowy i one się zazębiają).
3. Wykonuj test krok-po-kroku w działającej aplikacji (preview/headless), z dowodem Network/DB. Wynik zapisuj do `Harvard/Testy manualne/WYNIKI_<Mxx>_<data>.md`.
4. Każdy FAIL klasyfikuj: **defekt aplikacji** vs **nieaktualność testu**. Defekty aplikacji dopisuj do `_BACKLOG_FINDINGI_APLIKACJI.md`.

**Otwarte pozycje do potwierdzenia na żywo** (odnotowane przez agentów):
- M13: czy serwer realnie zwraca 200 dla nieuprawnionej roli na endpointach przejść (potwierdzenie F1).
- M10: server STT na staging (klucz OPENAI/GROQ) — czy transkrypcja głosowa zapisuje się do DB.
- M18: cold-start persistencji wymaga migracji 780+781 na danym środowisku (PRE-CHECK w teście).
- M21: faktyczna liczba PASS testów Meeting do zmierzenia `vitest run` (~32 wg zliczenia).

**Instrukcja wykonania dla agenta** (gotowy wzór był dany dla M01–M04 w tej sesji) — można poprosić o analogiczną dla kolejnej paczki; kluczowe punkty: prod-caution na DB, dowód E2E Network/DB, kolejność sekwencyjna (nie równolegle na tej samej bazie/koncie), raport per paczka.

---

## 8. Mapa plików (szybki skan)
```
Harvard/Testy manualne/
├── _HANDOFF_2026-06-17.md          ← TEN PLIK (punkt wejścia)
├── _INDEX.md                        ← indeks 28 modułów + konwencja
├── _AUDYT_ZGODNOSCI.md             ← raport audytu + wzorce systemowe
├── _BACKLOG_FINDINGI_APLIKACJI.md  ← defekty KODU (F1–F7) do naprawy
├── WYNIKI_M01-M04_2026-06-16.md    ← wyniki 1. wykonania M01–M04
└── TESTY_M01..M27 + TESTY_A1       ← 28 specyfikacji (~1954 scenariuszy)
```
