name: E2E-1 staging matrix

on:
  schedule:
    - cron: '7 5 * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: Staging URL
        required: false
        default: https://staging.consultify.ai

permissions:
  actions: read
  contents: read

concurrency:
  group: e2e-1-staging-matrix
  cancel-in-progress: false

jobs:
  matrix:
    name: ${{ matrix.variant }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      # The same staging account must never switch organizations concurrently.
      max-parallel: 1
      matrix:
        variant:
          - admin-nw-northwind-admin-en-light
          - admin-nw-northwind-admin-en-dark
          - admin-nw-northwind-admin-pl-light
          - admin-nw-northwind-admin-pl-dark
          - owner-dbr77-dbr77-owner-en-light
          - owner-dbr77-dbr77-owner-en-dark
          - owner-dbr77-dbr77-owner-pl-light
          - owner-dbr77-dbr77-owner-pl-dark
    env:
      GH_TOKEN: ${{ github.token }}
      E2E_ADMIN_NW_EMAIL: ${{ secrets.E2E_ADMIN_NW_EMAIL }}
      E2E_ADMIN_NW_PASSWORD: ${{ secrets.E2E_ADMIN_NW_PASSWORD }}
      E2E_OWNER_DBR77_EMAIL: ${{ secrets.E2E_OWNER_DBR77_EMAIL }}
      E2E_OWNER_DBR77_PASSWORD: ${{ secrets.E2E_OWNER_DBR77_PASSWORD }}
      E2E_BASE_URL: ${{ inputs.base_url || 'https://staging.consultify.ai' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium

      - name: Download exact previous variant
        continue-on-error: true
        run: |
          run_id="$(gh run list --workflow e2e-1-matrix.yml --status completed --limit 20 --json databaseId --jq '.[0].databaseId // empty')"
          if [ -n "$run_id" ]; then
            gh run download "$run_id" --name "e2e-1-${{ matrix.variant }}" --dir previous
          fi

      - name: Exercise staging variant
        run: node scripts/e2e-1-matrix/run.mjs --variant "${{ matrix.variant }}" --out "test-results/e2e-1-matrix/${{ matrix.variant }}" --previous previous

      - name: Upload variant evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-1-${{ matrix.variant }}
          path: test-results/e2e-1-matrix/${{ matrix.variant }}/
          retention-days: 30

  aggregate:
    name: Aggregate denominator and delta
    if: always()
    needs: matrix
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: e2e-1-*
          path: test-results/e2e-1-matrix
          merge-multiple: false
      - name: Verify workflow-to-contract wiring
        run: |
          node --test scripts/e2e-1-matrix/contract.test.mjs
          node --test scripts/e2e-1-matrix/classify.test.mjs
      - run: node scripts/e2e-1-matrix/aggregate.mjs test-results/e2e-1-matrix test-results/e2e-1-aggregate
      - if: always()
        run: cat test-results/e2e-1-aggregate/REPORT.md >> "$GITHUB_STEP_SUMMARY"
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-1-matrix-report
          path: test-results/e2e-1-aggregate/
          retention-days: 90
