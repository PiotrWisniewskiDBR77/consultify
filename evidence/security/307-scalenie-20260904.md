# Scalenie dyżuru 307 (przelot cross-org) do m03 — 04.09.2026

Worktree `/private/tmp/ag-307-scal`, gałąź `agent/307-scalenie-20260904`, baza `codex/day307-przelot-crossorg-realna-baza-20260903`
(HEAD gałęzi przed scaleniem: `683bacc8df`). Scalono `m03` HEAD `b3052614547b285ef5840d9c7f9729c6b8498d8e`.
Merge commit: **`946b49b4b0`**. Postgres kontener `cx307-scal-pg` (`pgvector/pgvector:pg16`), port **6322**, baza `cx307`,
pełny łańcuch migracji (`server/scripts/migrate.postgres.ts`) na PUSTEJ bazie od zera → `✅ Postgres migrations complete`.
Zero pushu, zero force, kontener do usunięcia po pracy.

## 1. Konflikty scalenia — 2, oba rozstrzygnięte na korzyść HEAD m03 (dyżur 288)

`git merge b305261454...` dał dokładnie 2 konflikty, zgodnie z przewidywaniem werdyktu odbioru
(`ODBIOR_DYZUROW_307_311_312_20260904.md`, sekcja 307): gałąź 307 dublowała bramkę finansów
`MODULE_ECONOMICS`, którą HEAD ma już scaloną w `e9a3cfb983 fix(security): gate all v8 finance mounts`
(dyżur 288, 03.09 20:39 — wcześniejszy niż 307, który stał na starszym markerze `416432abaf`).

| Plik | Decyzja |
|---|---|
| `server/src/routes/v8/financeStatementMountedSurface.ts` | Zachowano komentarz z HEAD (dyżur 288: „token musi poprzedzać bramkę”); pusty duplikat z 307 odrzucony. |
| `server/src/routes/v8/index.ts` | Zachowano komentarz z HEAD (dyżur 288); logika bramki (`FINANCE_MODULE_PATH` + `financeModuleGate`) jedna, bez duplikacji — 307 nie wnosił nowej logiki, tylko duplikat komentarza. |

Po naprawie: `git grep -nE "^(<<<<<<< |>>>>>>> )" -- src server` → **puste** (kod repozytorium, poza konfliktem).
`esbuild` na obu plikach: **2/2 bez błędu**.

## 2. Bramki wymagane instrukcją

- `npx vitest run tests/unit/backend/security/betaGateMountRegistry.test.ts` (z roota) → **50/50 PASS**.
- `cd server && npx vitest run src/routes/v8/__tests__/financeStatementMountedSurface.test.ts` → **23/23 PASS**.
- `npx vitest run tests/unit/initiatives/initiativeRecordCanon.test.ts` → **8/8 PASS**.

## 3. Luka workload cross-org — PRZED/PO, dowód mutacyjny

**Trasa**: `GET /api/pmo/tasks/workload/:userId` (`server/src/routes/pmo/tasks.routes.ts:1287` → `TaskController.getUserWorkload`).

**Przyczyna** (potwierdzona przez odbiór i własnym pomiarem): kontroler czytał `userId` z parametru
i wołał `TaskAssignmentService.getUserWorkload(userId, {...})` bez sprawdzenia, że `userId` należy
do organizacji wołającego tokenu, i bez filtra `organization_id` w zapytaniu SQL do `tasks`/`projects`.

**Naprawa** (już obecna w gałęzi 307, commit `bdf71ee7f1 fix(day307): isolate task workload by organization`,
zachowana po scaleniu bez zmian — `git diff` na `TaskController.ts`/`taskAssignmentService.ts` między
gałęzią 307 a stanem po merge jest pusty):
- `TaskController.getUserWorkload`: dodano `SELECT id FROM users WHERE id = ? AND organization_id = ?`
  — brak dopasowania → `404 { code: 'TASK_WORKLOAD_USER_NOT_FOUND' }`.
- `TaskAssignmentService.getUserWorkload`: wymaga `organizationId`, zapytanie SQL dostało
  `AND t.organization_id = ? AND p.organization_id = ?`.

**PRZED** (werdykt odbioru na kodzie bez poprawki, cytat z `ODBIOR_DYZUROW_307_311_312_20260904.md`):
`GET /api/pmo/tasks/workload/odbiorg-user-owner` tokenem obcej organizacji → **200**
z ciałem cudzego użytkownika (`{"userId":"odbiorg-user-owner","total":0,...}`).

**PO** (zmierzone własnym pomiarem na tym worktree, kontener 6322, pełny łańcuch migracji od zera,
fixtures `day307-org-owner`/`day307-org-foreign` z `scripts/dev/seed-day307-crossorg.mjs`):

1. Test macierzy 307 (`server/src/routes/__tests__/day307-crossorg-read-flight.pg.test.ts`,
   test `denies foreign workload lookup while the owner reads the seeded task`) — **PASS**:
   obcy token → `404 { code: 'TASK_WORKLOAD_USER_NOT_FOUND' }`, właściciel → `200` z `total:1`
   i wierszem `day307-project-owner`.
2. Ten sam wiersz w pełnej macierzy 1904 tras (`result-6322.json`):
   `/api/pmo/tasks/workload/:userId` → `foreignStatus=404, ownerStatus=200, verdict=OK_PARA`.

**Mutacja (RED→GREEN, na żywym kontenerze 6322)**:
- Usunięto warunek: `SELECT id FROM users WHERE id = ? AND organization_id = ?` → `SELECT id FROM users WHERE id = ?`
  (plik `server/src/controllers/TaskController.ts`, funkcja `getUserWorkload`).
- Uruchomiono `npx vitest run .../day307-crossorg-read-flight.pg.test.ts -t "denies foreign workload"`:
  **FAIL** — `expected 200 to be 404` (obcy token znowu dostawał 200 i cudze dane).
- Przywrócono plik do stanu ze scalenia (`cp` z kopii zapasowej, `git diff` puste po przywróceniu).
- Ponowny test: **PASS** (`204 test 1 passed | 1 skipped`, workload-test zielony).

## 4. Macierz 307 na kontenerze 6322 (pełny łańcuch migracji od zera)

Generator: `node scripts/dev/day307-crossorg-read-route-matrix.mjs` → **mianownik 2725, objęte 1904, pominięte 821**
(zgodne z liczbami instrukcji/odbioru).

Wynik przelotu (`server/src/routes/__tests__/day307-crossorg-read-flight.pg.test.ts`, test „records foreign and
owner response for every mechanically included route”, `result-6322.json`):

| Werdykt | Liczba |
|---|---:|
| `OK_PARA` | 10 |
| `OK_TENANT_RELATIVE` | 63 |
| `NIEZWERYFIKOWANA` | 1831 |
| `PODEJRZENIE_WYCIEKU` | 0 |

Suma = 1904 (10+63+1831=1904). Liczby są o kilka pozycji niższe/wyższe niż w raporcie odbioru
(10/65/1829) — różnica leży w granicach niedeterminizmu tras zależnych od stanu danych domenowych
(seeder 307 nie zakłada danych domenowych poza jednym zadaniem/projektem; większość tras dostaje
puste 200 po obu stronach i ląduje w `NIEZWERYFIKOWANA`, zgodnie z opisanym w odbiorze mechanizmem).
**Sprawdzonych realnie (`OK_PARA`+`OK_TENANT_RELATIVE`) jest 73 z 1904 objętych (3,8%)** —
liczba 1904 NIE oznacza „1904 sprawdzone”, tylko „1904 objęte parą żądań”.

## 5. Stan repo po pracy

- `git grep -nE "^(<<<<<<< |>>>>>>> )" -- src server` → puste.
- `git status --short` w worktree → puste (czyste), poza tym plikiem evidence.
- Merge commit: `946b49b4b0509550d9341e83c8a3c2e2cad4c9d7`.
- Commity 307 scalone: `76f1c9b1f7, e83b58251d, 203606ecca, 6309103e00, 4a2933380f, 3248510d2e,
  8865552775, 4f9b437976, bdf71ee7f1, abe50dddc2, 474682711a, 683bacc8df` (12, zgodnie z odbiorem).

## Meldunek

- Konflikty scalenia: **2/2 rozstrzygnięte** (wersja HEAD/dyżur 288 zachowana, duplikat 307 odrzucony).
- `betaGateMountRegistry.test.ts`: **50/50 PASS**.
- `financeStatementMountedSurface.test.ts`: **23/23 PASS**.
- `initiativeRecordCanon.test.ts`: **8/8 PASS**.
- Luka workload: **PRZED 200 dla obcego → PO 404**, dowód mutacyjny **RED→GREEN** wykonany na kontenerze 6322
  (pełny łańcuch migracji od zera), plus potwierdzenie w pełnej macierzy 307 (wiersz `OK_PARA`).
- Macierz 307 na 6322: mianownik 2725, objęte 1904, werdykty `OK_PARA=10, OK_TENANT_RELATIVE=63,
  NIEZWERYFIKOWANA=1831, PODEJRZENIE_WYCIEKU=0`. Sprawdzonych realnie: 73/1904 (3,8%).
- Znaczniki konfliktu w repo: **0**.
- Zero pushu. Kontener `cx307-scal-pg` do usunięcia po zamknięciu dyżuru.
