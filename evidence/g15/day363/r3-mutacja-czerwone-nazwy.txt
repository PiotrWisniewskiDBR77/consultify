finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/baselines (freezeBaseline) denies a MISSING membership row with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/baselines (freezeBaseline) denies a REVOKED member with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/baselines (freezeBaseline) denies a SUPERADMIN token without an ACTIVE membership row (no role bypass)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/baselines (freezeBaseline) same signed token is denied on the NEXT request after membership is revoked (no per-token caching)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/entries (appendLedgerEntry) denies a MISSING membership row with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/entries (appendLedgerEntry) denies a REVOKED member with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/entries (appendLedgerEntry) denies a SUPERADMIN token without an ACTIVE membership row (no role bypass)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /ledger/entries (appendLedgerEntry) same signed token is denied on the NEXT request after membership is revoked (no per-token caching)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates (createGate) denies a MISSING membership row with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates (createGate) denies a REVOKED member with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates (createGate) denies a SUPERADMIN token without an ACTIVE membership row (no role bypass)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates (createGate) same signed token is denied on the NEXT request after membership is revoked (no per-token caching)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /post-investment-reviews (createPostInvestmentReview) denies a MISSING membership row with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /post-investment-reviews (createPostInvestmentReview) denies a REVOKED member with 403 and writes nothing
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /post-investment-reviews (createPostInvestmentReview) denies a SUPERADMIN token without an ACTIVE membership row (no role bypass)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /post-investment-reviews (createPostInvestmentReview) same signed token is denied on the NEXT request after membership is revoked (no per-token caching)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates/:id/advance (advanceGate) denies a MISSING membership row with 403 and leaves the gate unchanged
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates/:id/advance (advanceGate) denies a REVOKED member with 403 and leaves the gate unchanged
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates/:id/advance (advanceGate) denies a SUPERADMIN token without an ACTIVE membership row (no role bypass)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates/:id/advance (advanceGate) same signed token is denied on the NEXT request after membership is revoked (no per-token caching)
finance-value.routes.ts — requireActiveMembership wall (real Postgres, real auth) membership wall — POST /capture/gates/:id/advance (advanceGate) ACTIVE membership -> 200 and the gate transitions pending -> passed with signed_off_by set
