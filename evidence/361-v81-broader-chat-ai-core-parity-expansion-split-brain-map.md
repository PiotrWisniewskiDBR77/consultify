# V8.1 Evidence - broader `Chat / AI core` parity expansion split-brain map

Date: 2026-03-27
Program: `POST_V81_BACKLOG_DEBT_REDUCTION_PROGRAM`
Lane: broader `Chat / AI core` parity expansion
Status: `active`

## Why this lane is now active

The accepted `broader Mobile redesign` lane has now reduced the shared mobile residual to a bounded accepted state.

What remains next is no longer one more small chat or AI-core continuity fix. It is broader parity expansion across the live chat surface, governed send/turn behavior, and wider AI-core operator/runtime exposure.

## Current split-brain

The broader chat and AI-core product surface still mixes truth across accepted bounded continuity and wider live behavior:

1. `evidence/176-v81-chat-t2-acceptance.md` closed the bounded active-surface chat continuity chain for snapshot entry, handoff readback, and handoff creation
2. `evidence/180-v81-ai-core-t2-acceptance.md` closed the bounded operator-facing AI-core read chain for runtime summary, tool catalog, and tool-policy readback
3. those accepted packets did not claim to close the broader parity surface across chat send/composer behavior, turn-level AI-core exposure, trust/provenance readback, or wider operator/runtime continuity

This creates a visible broader split-brain:

1. the live chat surface now has bounded governed control-strip continuity,
2. the AI-core operator surface now has bounded governed read continuity,
3. but wider chat/AI-core parity still remains outside those accepted slices and is not yet reconciled as one promoted broader lane

## Smallest honest first packet

The first bounded packet is:

`broader Chat / AI core parity expansion split-brain map`

It is the smallest honest packet because it:

1. names the broader parity residual instead of pretending one obvious chat or AI-core bug remains
2. preserves the already accepted bounded `Chat` and `AI core` cuts as done work
3. avoids silently broadening into a whole chat/AI-platform rewrite without explicit promotion
4. prepares the next real bounded packet only after the broader residual is explicit

## Explicitly not this packet

This split-brain map does not activate:

1. a full chat composer/send-path rewrite in one go
2. a whole AI-platform operator/runtime redesign
3. reopening the accepted bounded `Chat` lane
4. reopening the accepted bounded `AI core` lane
