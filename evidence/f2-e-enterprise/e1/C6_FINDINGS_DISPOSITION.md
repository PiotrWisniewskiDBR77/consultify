# E1 disposition of the six inherited C6 findings

Scope: F2-E E1 full organization export. This record names every finding from `C6_INDEPENDENT_REVIEW.md`; it does not claim that unrelated C6 work is complete.

| Finding | E1 disposition | Behavioral evidence | Mutation / negative evidence |
|---|---|---|---|
| C6-R1 credential disclosure | Addressed in E1. All 1,930 physical relations have explicit table and column policy. Credential/session tables are excluded; approved business tables use declared projections. DEC-493 projectors retain Interview/task content and remove person identity. | `classification-final/FREEZE_MANIFEST.json`; `five-families-realpg.log`; classification verifier; privacy unit tests. | Classification mutation suite rejects `access_codes.code_hash`, fake semantic sources, person-identity fields and credential-state projection. |
| C6-R2 AI budget UI mismatch | Addressed in this non-migration E1 remediation. `organization_ai_settings` is the sole organization-wide monthly cost authority. A deterministic organization ledger is created when missing, seeded conservatively from the maximum legacy organization cost budget, and then checked/updated through the same budget service used by the AI pipeline. User/token/request limits remain separately enforced. The new-organization default is USD 50. | `aiBudgetOrgSettingsContract.test.ts` proves 9/10 allow, 10/10 deny, 20 restores allow, and the USD 50 default. `aiSettingsBudgetContract.http.pg.test.ts` mounts the production settings route with a signed JWT and real PostgreSQL: UI-shaped 10/10/freeze persists and reloads, recorded usage reaches USD 10 and blocks, then saved 20/20 allows again. Fresh results are in `NON_MIGRATION_REMEDIATION/targeted-unit.log` and `NON_MIGRATION_REMEDIATION/final-realpg.log`. | The unit contract would fail if enforcement selected a competing legacy organization-wide monthly cost limit, skipped canonical ledger creation, failed to record usage, or ignored the changed setting. The mounted RealPG test guards the request-shape, persistence and enforcement bridge. |
| C6-R3 legal-hold fail-open/race | Addressed for export. Export takes the organization advisory lock before its repeatable-read, read-only transaction and checks policy in that transaction. Policy writers take the same key, so either the hold commits first and export refuses, or the export snapshot commits before the hold writer. Policy read failure becomes `POLICY_READ_FAILED`. | `organizationExportSnapshot.test.ts`; `organizationExportSnapshot.realpg.test.ts`; `organization-export-snapshot.gateway.pg.test.ts`. | Lost lock ACK, begin ACK, rollback and unlock mutations fail closed; controlled writer/export interleavings prove the lock ordering. |
| C6-R4 populated deletion versus immutable receipts | Resolved by owner decision DEC-478 for E1: destructive deletion remains disabled and deterministically returns HTTP 410. E1 does not alter receipts, triggers or retention. | `organizationDeletionApprovedOut.test.ts`; `organization-deletion-approved-out.gateway.pg.test.ts`; `superadmin.routes.ts` mounted response. | Tests assert no database/policy/deletion executor and no success receipt runs behind the 410 response. |
| C6-R5 incomplete/truncated export | Addressed in E1. The contract covers the 1,930 live public/v8 relations including proved descendant ownership paths; archive pages every EXPORT relation without the former 20,000-row cap, uses one pinned snapshot, and records per-table counts, SHA256, `asOf`, excluded/derived/unresolved/skipped sections. | `streaming-realpg.log`; archive unit log; classification guard; five-family API-to-archive test. | Unclassified-table CI mutation is RED; public/v8 collision and tenant-B sentinels are absent from tenant-A output; manifest SHA is recomputed from ZIP entries. |
| C6-R6 legal hold surfaced as HTTP 500 | Addressed for synchronous owner/superadmin export and explicit for asynchronous E1 jobs. Sync Gateway paths map `OrgPoliciesError` to 423/503. A job publishes the stable error code and now writes `organization_export_failed`; it never exposes the internal error text. | `organization-export-snapshot.gateway.pg.test.ts`; `organizationExportJobService.test.ts`; `ui-job-unit.log`. | Wrong resume token returns 404; failed job test proves stable `LEGAL_HOLD`, failed audit outcome, and no internal message in the public view. |

All six inherited C6 findings now have an explicit disposition and behavioral evidence: R1, R2, R3, R5 and R6 are addressed; R4 remains intentionally held at the safe HTTP 410 boundary required by DEC-478. This does not remove the separate full-E1 HOLD for restart-safe, multi-replica durable export jobs, which remains `MIGRATION_REQUIRED` and is outside the C6 finding set.
