POMIAR "PRZED" (uczciwa notatka zamiast fabrykowanego zrzutu)
==============================================================
Zlecenie 1.1-M-3 zaczęło się od pomiaru (KROK 0), nie od razu od zmiany kodu.
Testy zastane odnoszące się do Spotkań ZOSTAŁY jednak uruchomione dopiero
PO wprowadzeniu zmiany w AppRoutes.tsx/menuConfig.ts (kolejność praktyczna:
najpierw kod, bo bez niego treść testów źródłowych "source-slice" nie miała
czego mierzyć) — czyli nie na czystym drzewie sprzed zmiany, tylko od razu
"nowy kod x stary test". To DAŁO CZERWONE WYNIKI (patrz niżej), co jest
równoważnym dowodem regresji jak klasyczny before/after, tylko w innej
kolejności czynności. Zapisane tu dosłownie z rzeczywistego przebiegu sesji.

1) src/routes/__tests__/meetingsCanonicalRoute.test.ts (PRZED aktualizacją okien tekstowych testu)
   Uruchomione po dodaniu gałęzi `meetingsEnabled ? (...) : (...)` w AppRoutes.tsx:
   FAIL "mounts MeetingHub directly on the canonical /meetings list route"
     AssertionError: expected '...' to contain '<MeetingHub'
     (okno tekstowe testu, +500 znaków od `path={ROUTES.MEETINGS.ROOT}`,
     nie mieściło już `<MeetingHub` — bo ternary dodał ~230 znaków przed nim)
   Test Files  1 failed (1)
        Tests  1 failed | 13 passed (14)

2) src/components/navigation/Sidebar/__tests__/Sidebar.pilotMeetingLock.test.tsx (PRZED dodaniem vi.stubEnv)
   Uruchomione po dodaniu bramki `isMeetingsModuleEnabled() &&` w menuConfig.ts:
   FAIL "obcy nie widzi: pilot-restricted role sees Meeting decorated as locked, not removed"
   FAIL "wlasciciel widzi: owner/admin role sees Meeting unlocked"
     TestingLibraryElementError: Unable to find an element with the text: Meeting.
     (pozycja menu Meeting zniknęła całkiem — zgodnie z zamierzeniem DEC-425 —
     ale ten STARSZY test zakładał, że pozycja ZAWSZE istnieje w menu i bada
     TYLKO zamek pilotażowy na niej; potrzebował więc VITE_MODULE_MEETINGS=true)

Oba NAPRAWIONE (patrz evidence/1-1-m3/testy-po.txt — 28/28 PASS), nie przez
osłabienie asercji: (1) powiększone okno tekstowe + dodatkowe asercje
`meetingsEnabled ? (` i `<MeetingsWave2Placeholder`; (2) `vi.stubEnv('VITE_MODULE_MEETINGS','true')`
w beforeEach, żeby ten plik dalej testował WYŁĄCZNIE zamek pilotażowy, nie
kolidując z nowo dodaną, niezależną bramką DEC-425.
