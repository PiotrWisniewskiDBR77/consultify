# Promocja-na-sucho — raport z liczbami (2026-09-06)

Wykonane w worktree `/private/tmp/wt-promocja` (gałąź `mvp/promocja-na-sucho`,
baza `codex/m03-admin-20260824` = `origin/staging`, z bare repo
`/Users/piotrwisniewski/Developer/consultify-recovery-vault-20260820.git`).
Żadna komenda nie pisała do `origin/demo`, `origin/staging` ani do Railway —
jedyne wyjścia na zewnątrz to `railway variables --json` (odczyt),
`railway deployment list --json` (odczyt), `gh run list`/`gh api` (odczyt),
`curl /api/health` (odczyt) i migracja na LOKALNEJ bazie próbnej w kontenerze
`consultify-noc-pg` (utworzona i usunięta w tej samej sesji).

## KROK 1 — merge na sucho `origin/demo` ← `origin/staging`

- `git fetch origin demo staging` → `origin/demo` = `f3237e94230481d2bf4ad0a9c0dc10b1391191c9`,
  `origin/staging` = `dd321379bb1dab4108040fb4e91fb57af68df3a7`
- `git merge-base origin/demo origin/staging` = `f3237e94...` → **`origin/demo` jest
  ścisłym przodkiem `origin/staging`** (0 commitów tylko-w-demo). Skutek: merge
  `demo ← staging` jest z definicji szybkoprzewijalny (fast-forward-able) —
  **zero realnych konfliktów jest logicznie gwarantowane**, i tak też wyszło.
- `git log --oneline origin/demo..origin/staging | wc -l` = **6917 commitów**
- `git diff --name-only origin/demo HEAD` (po `merge --no-ff`) = **17356 plików**
  (uwaga: `git diff` zgłosił `exhaustive rename detection skipped` — przy tej
  skali to normalne, nie wpływa na policzenie ścieżek)
- `git merge --no-ff origin/staging` na gałęzi próbnej `proba/demo-merge`
  (start = `origin/demo`) → **exit 0, `git status --short` PUSTY (zero
  konfliktów, zero nierozstrzygniętych ścieżek)**. Commit scalający:
  `c9830b1f6f`.
- Rozkład 17354 zmienionych plików (z `/tmp/diff_files.txt`, `wc -l`):
  `docs/` 3289 · `src/` 2222 · `server/src/` 1509 · `tests/` 1092 ·
  `scripts/` 292 · `server/migrations/` 218 · `Harvard/` 38 ·
  `public/locales/` 6 · `.github/` 4.

### Migracje

- `git diff --name-status origin/demo HEAD -- server/migrations`:
  **216 dodanych (A), 1 zmieniony (M), 1 usunięty (D)**.
  - M: `server/migrations/README.md` (tekst, nie SQL).
  - D: `server/migrations/never-ran/017_consultant_mode.sql.sql` (podwójne
    rozszerzenie, katalog `never-ran/` — z nazwy nigdy nieuruchamiany).
  - **Zero zmodyfikowanych plików `.sql` z realną migracją** — wszystkie 216
    nowych migracji to pliki dodane, nigdy nie nadpisane historyczne.
  - Nazwy nowych migracji: mieszanka wzorca `YYYYMMDD[a-z]_opis.sql` (najstarsza
    `20260813_audits_method_core.sql`) i `NNN[a]_opis.sql` (najnowsza
    `963_user_ai_settings_chat_suggestions.sql`) — zgodne z konwencją repo,
    żadna nazwa spoza obu wzorców.
- Migracja na sucho: świeża baza `consultify_demo_proba` w kontenerze
  `consultify-noc-pg` (`postgresql://postgres:noc@127.0.0.1:54400/consultify_demo_proba`),
  `DB_TYPE=postgres NODE_ENV=test npx tsx server/scripts/migrate.postgres.ts`
  z gałęzi `proba/demo-merge` (stan po merge) →
  **`✅ Postgres migrations complete`, kod wyjścia 0**.
  - `schema_migrations` po migracji: **904 wiersze** (z 1109 plików `.sql` w
    `server/migrations/` na najwyższym poziomie — reszta to `never-ran/` i
    warianty pomijane przez sam runner, zgodnie z jego własną logiką selekcji).
  - `information_schema.tables` (schema `public`) po migracji: **1806 tabel**.
  - Baza próbna usunięta (`dropdb`) natychmiast po pomiarze.
- Gałąź próbna `proba/demo-merge` usunięta (`git branch -D`) po pomiarze —
  **nic nie zostało wypchnięte na `origin`**.

## KROK 2 — bramka wdrożenia (`.github/workflows/railway-deploy.yml` +
`scripts/validate-deploy-target.sh`)

Patrz `docs/program/demo-pilotaz/F2_F3_PRZEBIEG.md`, sekcja „USTALENIE
KRYTYCZNE”, dla pełnego opisu z komendami. Skrót:

- Push na gałąź `demo` **nie wdraża niczego** (brak triggera `on.push` dla tej
  gałęzi w workflow). Jedyna ścieżka: `workflow_dispatch(environment=demo,
  confirm_demo=yes)`, który checkoutuje **detached HEAD na SHA z tagu
  `staging-deployed`** — gałąź `demo` jest ignorowana przez mechanizm promocji.
- `DEPLOY_TARGET_GUARD_ENFORCE` **nie jest ustawiona nigdzie** (repo-level vars,
  `demo` env vars, `staging` env vars — sprawdzone `gh variable list` / `gh api
  .../environments/{demo,staging}/variables`) → bramka §B (zgodność bazy
  migracji z bazą aplikacji) działa w trybie **ADVISORY globalnie**.
- Środowisko `demo` MA jednak oba sekrety §B ustawione
  (`DEMO_APP_DATABASE_URL`, `DEMO_MIGRATION_DATABASE_URL` — `gh api
  .../environments/demo/secrets`) oraz `DEMO_DB_HOST_FINGERPRINT=trolley` (var)
  → **mimo trybu advisory, sprawdzenie rozjazdu i pin odcisku hosta WYKONUJĄ
  SIĘ w pełni** dla promocji na demo (advisory zwalnia tylko z WYMOGU podania
  danych, nie z reakcji na obserwowany rozjazd).
- Środowisko `staging` NIE MA żadnego z dwóch sekretów §B ani
  `STAGING_DB_HOST_FINGERPRINT` → bramka na stagingu przechodzi z GŁOŚNYMI
  ostrzeżeniami, bez weryfikacji bazy (advisory + brakujące dane).
- **Znalezisko na żywo (nie w zakresie samej bramki, ale bezpośrednio na jej
  drodze):** tag `refs/tags/staging-deployed` = `b852ade6164e...` (2026-09-04),
  **835 commitów za** obecnym `origin/staging` (`dd321379bb...`), który jest już
  live na `staging.consultify.ai` (`curl .../api/health` → `gitSha` zgodny).
  `gh run list --workflow=railway-deploy.yml` (15 ostatnich): ostatni SUKCES
  zadania `Deploy Staging` = 2026-09-04 21:24 (`b852ade616`, to jest ten tag);
  jedyna próba potem (2026-09-05 07:28, `5ffdabe05e`) zakończyła się
  **FAILURE** na kroku „Deploy app to staging” (obraz zbudowany i wypchnięty
  poprawnie, ale deployment Railway dostał status `FAILED` — prawdopodobnie
  `preDeployCommand: node dist/scripts/release-migration-gate.js` z
  `railway.json`). **Wniosek: promocja demo dziś, bez naprawy tagu, wysłałaby
  stary commit sprzed 835 commitów.** Naprawa opisana jako KROK 3 w
  `F2_F3_PRZEBIEG.md`.
- Cofnięcie: Railway dashboard → poprzedni `SUCCESS` → Rollback (30 s) **lub**
  restore-commit do przodu wg `_RUNBOOK_COFANIA.md` warstwa 3. Nigdy `git push
  --force`/`reset` na `demo`.

## KROK 3 — zmienne (staging vs demo, odczyt `railway variables --json`,
usługa `consultify`, projekt `a6d59e88-263d-45f3-96bc-861f66bf467b`)

- Kluczy razem: staging 148, demo 184.
- Metoda kanoniczna (`node scripts/demo/porownaj-flagi.mjs`, wzorzec
  `^(VITE_|ENABLE_|FEATURE_)`, ZGODNA z `evidence/demo-pilotaz/PROBA_NA_SUCHO_20260906.md`
  krok 10 z tego samego dnia — **27 brakujących flag, bez zmian od porannego
  pomiaru**):
  - **27 flag** obecnych na stagingu, brak na demo (lista + wartości w
    `docs/program/demo-pilotaz/F2_ZMIENNE_DEMO.sh`, blok 1).
  - **2 flagi o sprzecznej wartości**: `ENABLE_TEST_GATEWAY` (staging=false,
    demo=true), `ENABLE_V8_SHADOW_MODE` (staging=false, demo=true) — obie
    furtki testowe dziś OTWARTE na witrynie pokazowej.
  - `CSRF_MODE`: staging=`report`, demo=BRAK.
  - `AI_BUDGETS_ENABLED`: BRAK po obu stronach jako zmienna, ale kod
    (`server/src/services/ai/AIPipeline.ts:265`) domyślnie WŁĄCZA budżety, gdy
    zmienna jest nieustawiona (`!== 'false'`) — ustawienie `true` na demo jest
    deklaracją, nie zmianą zachowania.
  - `APP_BUILD_SHA` na demo = `f3237e94230481d2bf4ad0a9c0dc10b1391191c9` —
    zgodny z żywym `gitSha` (nie jest dziś fałszywy), ale ustawiany ręcznie w
    Railway variables; sam workflow i tak nadpisuje tę zmienną przy każdym
    `promote-demo` (`railway variables --set "APP_BUILD_SHA=${SOURCE_SHA}"
    --skip-deploys`), więc usunięcie jest higieną, nie naprawą aktywnej awarii.
  - **UWAGA METODYCZNA**: naiwne porównanie WSZYSTKICH kluczy (`comm`) dało 42
    różnice — błędne, bo łapie 13 zmiennych operacyjnych spoza definicji
    „flaga” (`SMTP_*`, `ALERT_EMAIL_RECIPIENTS`, `DISABLE_RATE_LIMIT`,
    `INBOX_WEBHOOK_SECRET`, `TEST_SUPPORT_KEY`, `UNSPLASH_ACCESS_KEY`,
    `WHATSAPP_FROM`, `DB_MANAGED_SCHEMA`, `DEV`, `API_RATE_LIMIT_MAX`,
    `RATE_LIMIT_ALLOW_PROD_DISABLE`, `ALLOW_BRANDED_DEMO_ORG`,
    `PARTNER_SELF_CONNECT_ENABLED`). Liczba kanoniczna (VITE_/ENABLE_/FEATURE_)
    = **27**, zgodna z zapisem w
    `docs/program/demo-pilotaz/PLAN_DEMO_KLIENCI_I_POKAZY.md` (F2).

## Sprzątanie

- Gałąź próbna `proba/demo-merge`: **usunięta** (`git branch -D`).
- Baza próbna `consultify_demo_proba`: **usunięta** (`dropdb`), zweryfikowane
  `psql -l` po fakcie.
- Nic nie wypchnięte na `origin` z tego worktree.
- Dokumenty tego zlecenia (ten plik + `F2_F3_PRZEBIEG.md` +
  `F2_ZMIENNE_DEMO.sh`) commitowane lokalnie na `mvp/promocja-na-sucho`, BEZ
  push.
