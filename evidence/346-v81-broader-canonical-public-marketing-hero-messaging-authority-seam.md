# V8.1 Evidence - broader canonical `/` public hero messaging authority seam

Date: 2026-03-27
Program: `POST_V81_BACKLOG_DEBT_REDUCTION_PROGRAM`
Lane: broader canonical `/` and public marketing breadth
Packet: `canonical / hero messaging authority`
Status: `landed`

## Seam closed

The first real bounded packet in the broader public-marketing lane now aligns the active canonical `/` hero messaging with the V8 landing SSOT category and analogy.

## What changed

1. `src/components/Landing/EpicHeroSection.tsx` now presents the hero around the canonical V8 messaging pair instead of generic "AI-powered consulting platform" phrasing
2. `public/locales/en/translation.json` now defines the active hero copy around `Consulting Intelligence Platform` plus `Spotify for consulting knowledge`
3. `public/locales/pl/translation.json` now mirrors that same authority for the main Polish public landing locale
4. `tests/components/Landing/EpicHeroSection.messaging-authority.test.tsx` now proves the active landing hero exposes the canonical category, analogy, and structured-workflow differentiator on the live component

## Why this packet matters

Before this packet:

1. `docs/product/LANDING_V8_SSOT.md` already defined the canonical hero messaging
2. the active `/` hero still mixed in generic platform wording and older profit-first copy
3. the broader public-marketing lane had no first code-backed closure after the split-brain map

After this packet:

1. the canonical `/` hero now speaks in the same category and analogy language as the SSOT
2. the broader lane has a first honest landed packet without pretending to solve the entire public redesign in one move
3. the remaining residual stays explicit around broader section-order, supporting-route copy, and visual-system breadth

## Lane state after this packet

The broader canonical `/` and public marketing breadth lane remains active.

The next step is to assess the next smallest honest packet after hero messaging authority, likely around broader section-order authority on canonical `/` or the next highest-value supporting public route copy seam.
