# P5 — raport PRZED/PO (05.09.2026)

Punkt startowy: `origin/staging` = `210ec9dbc3afabd98e1fc81ccb3c8d4c6936a0ba`.
Gałąź robocza: `codex/p5-ladowanie`. Bez push.

## Wynik liczbowy

| Bramka | PRZED | PO | Status / dowód |
| --- | ---: | ---: | --- |
| `stream/partial/:sessionId`, brak partial | HTTP 404 | HTTP 200 + `found:false` | PASS, test kontraktowy 2/2 |
| `map/candidate`, brak kandydata | 2 × HTTP 404 / otwarcie | HTTP 200 + `candidate:null`; 1 współdzielone żądanie in-flight | PASS, test kontraktowy 2/2 |
| `review-snapshots/published`, brak snapshotu | HTTP 404 | HTTP 404 | BLOCKED: krok 8 wymaga osobnego słowa nadzorcy |
| DEMO_STORY / runtime-v1: planning + legacy subresources | 11 błędów 404 zmierzonych w audycie | 0 wywołań w teście origin-gate (gate obejmuje 14 aktualnych odczytów) | KOD GREEN 3/3; commit BLOCKED przez freeze `05_INITIATIVES` |
| Megatrendy — dostępność modelu przy starcie | dynamiczny import mógł trwale ustawić `null`; endpoint 503 | statyczny import, kontrola 5 eksportów, `megatrendsAvailable:true` w teście | KOD GREEN; realny HTTP 200 NOT_PROVEN bez DB/tokena |
| Hook 300 ms / 8 s / 15 s | brak wspólnego kontraktu | 3 fazy + reset/cancel | PASS 3/3 |
| Realizacja Praca/Zasoby | cisza / tekst / pustka 15,5–22 s | cisza <300 ms, animowany szkielet >=300 ms, slow >=8 s, timeout >=15 s | PASS 13/13 wraz z fan-out |
| Płótna pomysłu | pustka 4–6 s | wspólny skeleton przez fetch i hydratację grafu; Process Flow i Whiteboard bez flagowego bespoke fallbacku | test kontraktu 2/2; zrzut EVIDENCE_MISSING |
| Liczniki Notatnika | `0` przez ok. 3–4 s | `—` do pierwszego `onScopeCountsChange` | testy 6/6; zrzut EVIDENCE_MISSING |
| Narzędzia — lazy start | goły spinner 3–10 s | wspólny panel: cisza <300 ms, skeleton 300 ms, slow 8 s, timeout 15 s | PASS 5/5; zrzut EVIDENCE_MISSING |
| Console-clean, 16 modułów | brak trwałego wyniku baseline w repo | 16 tras; 14 czystych, 2 z błędami; 21 błędów konsoli i 15 odpowiedzi 401 | RUN wykonany, gate FAIL; `console-clean-po.json` |
| Ekrany z błędami konsoli — spadek | UNKNOWN | 2/16 | próg >=80% NOT_PROVEN, bo brak porównywalnego przebiegu PRZED; sesja utraciła ważność w trakcie pomiaru |

## Krok 10 — `organizations/current`

Wykonano `N=10` odseparowanych zimnych startów własnego Vite na wolnym porcie 3097, z proxy do
`https://staging.consultify.ai`; nie przejęto obcych procesów na 3000/3100. Każdy przebieg
zakończył się przekierowaniem do logowania przed wywołaniem `organizations/current`, więc
liczba odpowiedzi z tej trasy wyniosła 0 (w tym 404 = 0). To nie dowodzi braku sporadycznego
404: reprodukcja jest `EVIDENCE_MISSING_AUTH`, ponieważ przekazana sesja utraciła ważność po
pierwszej części odbioru. Surowy dowód: `organizations-current-cold-starts.json`. Nie napisano
poprawki na ślepo.

## Megatrendy — diagnoza

Specyfikator `.js` jest poprawny dla `moduleResolution: NodeNext`; plik TypeScript emituje się
do `server/dist/src/models/megatrend.js`, a import przeszedł z `tsx` i z `dist`. Naprawiono
rzeczywisty mechanizm maskujący: dynamiczny import z cichym `catch`, który zamieniał dowolną
awarię inicjalizacji w trwałe `null`. Realny status 200 nadal zależy od istniejącej tabeli,
danych i uwierzytelnienia — test importu nie jest jego substytutem.

## Niezamknięte bramki

1. Krok 8: oczekuje na jawne słowo nadzorcy; bez niego trzeci kontrakt nadal zwraca 404.
2. Krok 9: DEC-397 został rozszerzony przez właściciela na P5; commit otrzymuje marker
   `[ODMROZENIE 05_INITIATIVES DEC-397]` wymagany przez aktualny freeze.
3. Zrzuty 0,4–0,8 s wykonano, lecz trzy zakończyły się na ekranie logowania, a jeden przebieg
   Notatnika zanotował 8 błędów po rotacji sesji. Pliki pozostają dowodem nieudanego pomiaru,
   nie dowodem akceptacji UI. Realne Megatrendy 200, odczyt `.json` Network i >=80%
   console-clean pozostają `NOT_PROVEN` bez ponownie ważnej sesji.

Werdykt: **PARTIAL / NOT_PROVEN**, nie „cel osiągnięty”.

## Integracja z `origin/staging`

- P5 przed merge: `f4ba4b7fd3` (krok 9 z markerem `[ODMROZENIE 05_INITIATIVES DEC-397]`).
- Zmergowany `origin/staging`: `59e282df885161467102ceb0c23a14c8717b2bec`.
- Merge commit: `821bb5b926`; jeden konflikt w `ExecutionResourcesSurface.tsx` rozwiązany przez
  zachowanie obu niezależnych zmian (`useDeferredLoading` z P5 i `capacityUnitLabel` ze stagingu).
- Po merge: 16 plików testowych / 66 testów PASS (hook, fan-out, powierzchnie Realizacji i
  origin-gate Inicjatyw); serwerowy `tsc --build server/tsconfig.build.json` exit 0;
  `check-list-canon.sh` oraz `check-artefakt.sh` PASS.
- Working tree po integracji: czysty; branch jest 11 commitów przed `origin/staging`. Bez push.

Stan celu §10 pozostaje **PARTIAL / NOT_PROVEN**: bramki kodowe i integracyjne są zielone,
ale sesja odbiorowa wygasła podczas pomiarów, dlatego zrzuty UI, Megatrendy HTTP 200,
porównywalny PRZED/PO console-clean ≥80% i reprodukcja handlera `organizations/current`
nie mają kwalifikowanego dowodu zalogowanego runtime. Krok 8 nadal czeka na osobną decyzję #6b.
