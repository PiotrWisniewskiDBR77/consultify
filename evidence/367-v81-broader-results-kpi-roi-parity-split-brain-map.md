# V8.1 Evidence - broader `Results / KPI / ROI` parity split-brain map

Date: 2026-03-27
Program: `POST_V81_BACKLOG_DEBT_REDUCTION_PROGRAM`
Lane: broader `Results / KPI / ROI` parity
Status: `active`

## Why this lane is now active

The broader `Chat / AI core` parity expansion lane has now reduced the smallest honest live-surface and operator readback residuals into a bounded accepted state.

What remains next is no longer one more small results read fix. It is broader parity expansion across visible results write surfaces, ROI mutation continuity, and wider results operator/reporting breadth.

## Current split-brain

The broader results product surface still mixes truth across accepted bounded V8-first reads and wider live behavior:

1. `evidence/134-v81-results-kpi-roi-t2-acceptance.md` closed the bounded read/runtime-truth chain for results entry authority, governed runtime strip continuity, ROI portfolio/detail reads, KPI catalog reads, and KPI drawer detail reads
2. those accepted packets did not claim to close broader KPI creation/reporting writes, ROI assumption/edit flows, or wider operator/report surfaces
3. the repository still shows active broader surfaces such as `KPICreateModal`, `ROIAssumptionEditor`, and wider results module/operator views that sit beyond the accepted bounded read scope

This creates a visible broader split-brain:

1. active results reads are now largely governed and V8-first on the accepted bounded surfaces,
2. but broader KPI/ROI write and operator breadth still remains outside that accepted slice,
3. so broader `Results / KPI / ROI` parity is not yet reconciled as one promoted lane.

## Smallest honest first packet

The first bounded packet is:

`broader Results / KPI / ROI parity split-brain map`

It is the smallest honest packet because it:

1. names the broader results residual instead of pretending one obvious KPI or ROI bug remains
2. preserves the already accepted bounded `Results / KPI / ROI` lane as done work
3. avoids silently broadening into a whole results-platform rewrite without explicit promotion
4. prepares the next real bounded packet only after the broader residual is explicit

## Likely next bounded seam

The strongest next bounded candidate now looks like:

`visible KPI create continuity on a governed V8-first seam`

Why this candidate leads:

1. `KPICreateModal` is an active visible write surface
2. the accepted results lane intentionally stopped at reads/runtime truth, not writes
3. KPI creation is narrower and more honest than trying to absorb KPI reports plus ROI edits plus results operator breadth into one packet

## Explicitly not this packet

This split-brain map does not activate:

1. a full results module/operator redesign in one go
2. all KPI and ROI writes at once
3. reopening the accepted bounded `Results / KPI / ROI` lane
4. a broad finance/results convergence rewrite framed as one more parity fix
