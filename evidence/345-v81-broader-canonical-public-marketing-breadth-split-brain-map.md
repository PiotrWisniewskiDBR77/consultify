# V8.1 Evidence - broader canonical `/` and public marketing breadth split-brain map

Date: 2026-03-27
Program: `POST_V81_BACKLOG_DEBT_REDUCTION_PROGRAM`
Lane: broader canonical `/` and public marketing breadth
Status: `active`

## Why this lane is now active

The accepted `Landing page redesign` and `Landing homepage IA` lanes closed bounded shell, CTA, and top-level authority seams on the public surface.

What remains is no longer a narrow homepage IA fix or one more shared-shell parity cut. It is broader canonical `/` and public marketing breadth across narrative, section hierarchy, and visual-system coherence.

## Current split-brain

The public marketing surface still mixes truth across canonical docs and live pages:

1. `docs/product/LANDING_V8_SSOT.md` defines the canonical landing narrative, section hierarchy, and V8 commercial messaging
2. `src/views/ProductEntryPage.tsx` is the canonical `/` implementation, but its broader copy/section-order/visual treatment remained outside the accepted `Landing homepage IA` lane
3. accepted redesign packets aligned shell and CTA authority across live routes like `src/views/ResourcesPage.tsx`, `src/views/ToolsShowcasePage.tsx`, `src/views/AuditsShowcasePage.tsx`, and `src/views/BecomePartnerView.tsx`
4. those accepted packets did not claim to close the broader public marketing system across canonical `/`, supporting marketing routes, and the SSOT narrative surface

This creates a visible broader split-brain:

1. the public marketing surface now shares bounded shell and CTA contracts,
2. but broader section-order, copy, and visual-system breadth is still not explicitly reconciled as one promoted lane,
3. so the remaining public marketing work is broader redesign-shaped rather than another hidden shell bug.

## Smallest honest first packet

The first bounded packet is:

`broader canonical / and public marketing breadth split-brain map`

It is the smallest honest packet because it:

1. names the broader redesign residual instead of pretending one obvious code tweak remains
2. preserves the already accepted landing shell and homepage IA cuts as done work
3. avoids silently broadening into a full public marketing rewrite without explicit promotion
4. prepares the next real bounded public-marketing packet only after the redesign-shaped residual is explicit

## Explicitly not this packet

This split-brain map does not activate:

1. a full homepage rewrite in one go
2. a whole brand-refresh or asset-production program
3. Anna-specific voice, prompt, multilingual, or analytics work
4. broader mobile redesign or authenticated app-shell work
