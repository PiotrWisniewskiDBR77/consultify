# V8.1 Evidence - broader canonical `/` trust-strip order authority seam

Date: 2026-03-27
Program: `POST_V81_BACKLOG_DEBT_REDUCTION_PROGRAM`
Lane: broader canonical `/` and public marketing breadth
Packet: `canonical / trust-strip order authority`
Status: `landed`

## Seam closed

The next bounded packet in the broader public-marketing lane now aligns the active canonical `/` trust/social-proof placement more closely with the V8 landing section-order contract.

## What changed

1. `src/views/ProductEntryPage.tsx` now places `TrustStrip` after the explanatory/value sections instead of directly under the hero
2. the active landing flow now reads more like hook -> explain -> prove -> trust/credibility -> convert instead of opening with proof badges before the broader product story
3. `tests/components/ProductEntryPage.kb-preview.test.tsx` now proves that `TrustStrip` follows the explanatory landing sections on canonical `/`

## Why this packet matters

Before this packet:

1. `docs/product/LANDING_V8_SSOT.md` placed social proof / trust late in the value-proof funnel
2. the live canonical `/` surfaced `TrustStrip` immediately after hero
3. the broader lane still had an obvious section-order mismatch on the main public surface

After this packet:

1. the live canonical `/` no longer front-loads trust badges ahead of the explanatory sections
2. section-order authority on the active landing page is incrementally closer to the SSOT without pretending the whole narrative redesign is complete
3. the remaining residual is still explicit around missing problem/platform-pattern/value-layer structure and broader public-route coherence

## Lane state after this packet

The broader canonical `/` and public marketing breadth lane remains active.

The next step is to assess the next smallest honest packet after hero messaging authority and trust-strip order authority, likely around a missing canonical `/` problem/platform-pattern narrative seam.
