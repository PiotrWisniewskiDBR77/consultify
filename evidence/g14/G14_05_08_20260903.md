# G14 — ślad naprawy znalezisko→commit — moduły 05/06/07/08 (2026-09-03)

Worktree: `/private/tmp/ag-g14-05-08` · gałąź: `agent/g14-05-08-20260903` · baza: `HEAD`
wskazujący na `35afcb15fd` (Merge agent/przewody-harness-20260903).
Źródło znalezisk: `docs/program/waves/WAVE_03_ACCEPTANCE/ANALIZA_G13_MODULY_01_08_20260903.md`.

Zasada z instrukcji zastosowana literalnie: **mój pomiar rządzi nad opisem
znaleziska**. Dwa znaleziska (MW-1, INIT-6) okazały się NIEAKTUALNE/BŁĘDNE po
bezpośredniej weryfikacji w kodzie — opisane niżej z dowodem, nic nie
"naprawiono na siłę" wbrew temu, co pokazał pomiar.

## 05_INITIATIVES

| ID | Status | Commit | Dowód | Pliki |
|---|---|---|---|---|
| INIT-2(a) | POTWIERDZONE WCZEŚNIEJ | `1381442787` (2026-09-02, przed tą sesją) | Pomiar tej sesji `node scripts/dev/measure-preview-canon.mjs --port=5341 --wysokosc`: `Inicjatywy / CapacityScenarioSurface` → obszar 786px / panel 786px / **luka 0px** / OK. `CapacityScenarioSurface.tsx:897-904` już ma `<div className="flex-1 min-h-0">` z komentarzem cytującym dokładnie ten przepis (K5). Znalezisko G13 (67px) było nieaktualne w chwili pisania tej analizy — naprawione dzień wcześniej, ale G13 tego nie zweryfikowało bezpośrednim pomiarem. | `src/components/Initiatives/CapacityScenarioSurface.tsx` (bez zmian w tej sesji) |
| INIT-2(b) „Tworzy raport" | POMINIĘTE z powodem | — | DUŻE — nowa funkcja (migawka obciążenia point-in-time), nie poprawka; wymaga decyzji zakresu właściciela. Zgodnie z instrukcją §1 pominięte z zapisem. | — |
| INIT-3 | POTWIERDZONE WCZEŚNIEJ | `1381442787` (2026-09-02) | Ten sam pomiar: `Inicjatywy / PlanScenarioSurface` → obszar 844px / panel 844px / **luka 0px** / OK. `PlanScenarioSurface.tsx:1014` ma analogiczne opakowanie. | `src/components/Initiatives/PlanScenarioSurface.tsx` (bez zmian w tej sesji) |
| INIT-6 | POTWIERDZONE WCZEŚNIEJ | — (mechanizm już istniał w kodzie przed tą sesją) | Zweryfikowane bezpośrednio w `InitiativesHub.tsx`: `isPilotParticipant = isPilotParticipantRole(currentUser?.role)` (real `useAppStore()`, nie mock) gates GŁÓWNY CTA „New Initiative" w `StandardModuleBar` (linia ~2400) na `locked: true` + `lockedReason: t('initiatives.pilot.createLocked', 'Available in the next project phase')`, `onClick` woła `dispatchPilotAccessBlocked(...)` (przyjazny event, NIE surowe wywołanie API) zamiast otwierać kreator. Analogicznie CTA pustego stanu (`primaryAction={isPilotParticipant ? undefined : {...}}`) jest CAŁKOWICIE UKRYTY. `StandardModuleBar.tsx:409` renderuje `lockedReason` jako `title` (tooltip) i osobną klasę wyciszoną. Dla roli USER/GUEST/MEMBER (pilot-restricted) NIE MA ścieżki do surowego 403 — przycisk jest albo ukryty (pusty stan), albo zablokowany z uczciwym tłumaczeniem (widok pełny). Zero kodu zmienione — mechanizm już poprawny. | brak zmian (weryfikacja: `src/components/Initiatives/InitiativesHub.tsx`, `src/components/standard/StandardModuleBar.tsx`, `src/utils/pilotAccess.ts`, `src/utils/roleGuards.ts`) |

## 07_MY_WORK_AGENT

| ID | Status | Commit | Dowód | Pliki |
|---|---|---|---|---|
| MW-1 | POMINIĘTE z powodem (znalezisko BŁĘDNE) | — | G13 twierdziło "0 wołających w `src/` poza sobą i testem" dla 12 komponentów kolejek. Własny `grep` w `tests/` (poza zakresem G13's własnego grepa) znalazł: (a) 12 dedykowanych testów jednostkowych w `tests/unit/initiatives-execution/*.test.tsx` (po jednym na komponent), (b) import wszystkich 12 w `tests/e2e/fixtures/initiatives-execution-aco.tsx`, konsumowanym przez DWIE realne specyfikacje Playwright: `tests/e2e/initiatives-execution/wcag-text-resize.spec.ts` i `tests/e2e/initiatives-execution/aco-definition-browser.spec.ts`. Potwierdza to NIEZALEŻNIE poprzednia sesja (commit `bc7514e998`, 2026-09-02, "chore(benefits): remove 2 confirmed-dead orphan components") — usunęła TYLKO `BenefitsHub.tsx`+`KPICreateModal.tsx`, a w treści commita explicite odnotowała: "The other 12 candidates... were investigated and are NOT dead — see... each has a dedicated, currently-passing vitest unit test... and 10 of 12 are also rendered by a live e2e harness... consumed by real Playwright specs. They were left untouched." `BenefitsHub.tsx` (13. plik z listy G13) już nie istnieje — usunięty tym samym wcześniejszym commitem. Usunięcie 12 komponentów kolejek w tej sesji złamałoby dwa realne testy e2e — NIE wykonane. | brak (weryfikacja: `git grep`, `tests/unit/initiatives-execution/`, `tests/e2e/fixtures/initiatives-execution-aco.tsx`) |
| MW-2 | NAPRAWIONE | `fbd274e41c` | Harness `mywork-decisions` już istniał w `dev-render/main.tsx` (dodany dziś, wcześniej w sesji, dyżur "ekrany bez wpisu") ale NIGDY nie był zmierzony kanonicznym przyrządem. Dodany wpis do `EKRANY_WYSOKOSC` w `scripts/dev/measure-preview-canon.mjs`. Pomiar: `Moja praca / DecisionsPanelContent (Decyzje)` → obszar 876px / panel 876px / **luka 0px** / OK. Zero dodatkowej naprawy potrzebne — ekran już poprawny, tylko niezmierzony. | `scripts/dev/measure-preview-canon.mjs` |
| MW-3(a) | POTWIERDZONE WCZEŚNIEJ | — (naprawa poza zakresem tej sesji/nieznana) | Pomiar tej sesji: `Vault / VaultSafesTable` → obszar 951px / panel 951px / **luka 0px** / OK. `VaultSafesTable.tsx:371` sam NIE ma opakowania `flex-1 min-h-0`, ale jego jedyny wołający `ClientDocumentsVault.tsx:265` już opakowuje `<VaultSafesTable>` w `<div className="flex-1 min-h-0 overflow-auto">` — przepis K5 zastosowany na poziomie rodzica. Zero zmiany potrzebne. | brak zmian (weryfikacja: `src/views/vault/VaultSafesTable.tsx`, `src/views/vault/ClientDocumentsVault.tsx`) |
| MW-5 | NAPRAWIONE (kod) / NIEZWERYFIKOWANE (dowód bazodanowy) | `b09cf4a782` | Znalezisko cytowało 3 pola jako "tylko localStorage". Bezpośrednia weryfikacja pokazała inny obraz: **`attachments`** ma już pełny backend (dedykowane API `/my-work/object-attachments/decision/:id`, `loadDecisionAttachments`/`uploadDecisionAttachmentsAndReload` w `DecisionDetailView.tsx`); jego funkcja `selectDecisionAttachments(serverAttachments, _localAttachments)` już bezwarunkowo zwraca WYŁĄCZNIE wartość serwera — zero bugu. **`description`** ma już realną kolumnę na `decisions` (`decisions.description`, migracje `20260719_baseline_gap.sql`/`730_beta_schema_fixes.sql`), wysyłaną przez `handleSave` — bugiem był STALE odczyt z localStorage klobberujący poprawną wartość serwera przy każdym ładowaniu (ta sama klasa błędu co już naprawiona dla alternatives/risks pod `MW-DEC-001`, tu przeoczona) — naprawione guardem `!decision.description?.trim()`. **`escalation`** (pojedynczy `EscalationRule\|null`, inny koncept niż już persystowane `escalationRules[]`) było JEDYNYM z trzech bez żadnej kolumny — zamknięte migracją `20261912_decision_enhancements_escalation.sql` (addytywna, `JSONB`, ten sam wzorzec co 5 sióstr w tej samej tabeli) + Zod schema + serwis + kontroler + frontend (PUT payload + guard `!loadedEscalation`). DOWÓD ZAPISU: migracja przeszła w całości na jednorazowym throwaway Postgres (`docker run pgvector/pgvector:pg16`, port 5490, **usunięty po pomiarze**) — pełny łańcuch migracji (do `20261912`) zakończył się `✅ Postgres migrations complete`. Próba dowodu ODCZYTEM NA ZIMNO (świeży `GET /detail` po `PUT`) przez dedykowany integration test nie powiodła się: `ApiGateway`/`queryHelpers` w tym konkretnym uruchomieniu testowym zwracały `404 Decision not found` mimo widocznego w bezpośrednim `psql` wiersza — klasa problemu opisana w pamięci nadzorcy "Dwa dostępy, jedna baza, różne odpowiedzi", nie zdiagnozowana do końca w dostępnym budżecie (próbowano `RUN_DB_TESTS=1`/`NODE_ENV=test` bez skutku). Kod podąża 1:1 za już działającym, przetestowanym wzorcem (`day277-decyzje-zapis.pg.test.ts` dla pięciu sióstr w tej samej tabeli) — strukturalnie spójny, ale bez własnego dowodu integracyjnego tej sesji. | `server/migrations/20261912_decision_enhancements_escalation.sql`, `server/src/validators/decision.validators.ts`, `server/src/services/decisionCollaborationService.ts`, `server/src/controllers/DecisionController.ts`, `src/components/MyWork/DecisionDetailView.tsx` |
| MW-6 | POTWIERDZONE (spot-check) | — | Patrz sekcja "Dostępność — spot-check" niżej (wspólna dla INIT-4/EXE-3/MW-6). | — |
| MW-7 | NAPRAWIONE | `6fb7eb4961` + `d99e4f21ba` (baseline) | 4 miejsca zmienione `focus:ring-primary-500(/40)` → `focus:ring-c-focus(/40)`: `ProgressView.tsx:197`, `NotificationSettings.tsx:166`, `NotificationsHub.tsx:1154,1166`. Zweryfikowane grepem PRZED (4 trafienia) i PO (0 trafień) w tych plikach; `check-focus-canon` hook potwierdził spadek długu 112→109 plików / 227→223 wystąpień, baseline zaktualizowany. HMR w vite (port 5341) przeszedł bez błędu. | `src/components/MyWork/ProgressView.tsx`, `src/components/MyWork/NotificationSettings.tsx`, `src/components/MyWork/NotificationsHub.tsx`, `scripts/check-focus-canon.baseline.txt` |
| MW-4, MW-8 | POMINIĘTE z powodem | — | Zgodnie z instrukcją §1: MW-4 (przeprojektowanie prawego panelu Idei/Notatnika) = DUŻE, wymaga decyzji projektowej właściciela. MW-8 (crimson ogólny, 318 wystąpień/53 pliki) = osobny dyżur przemalowania, poza zakresem DROBNE/ŚREDNIE tej bramki. | — |

## 06_EXECUTION

| ID | Status | Commit | Dowód | Pliki |
|---|---|---|---|---|
| EXE-1 | NAPRAWIONE | `bb5465b296` | `GET /api/initiatives/runtime-v1/execution-cases` już ładował inicjatywę (`deps.reader.findById`) do sprawdzenia `initiative.view`, ale odrzucał jej `title` zamiast wysłać klientowi. Dołączony jako `initiativeTitle` na każdym widocznym elemencie. Klient (`ExecutionReportsSurface.tsx`): `{item.initiativeTitle \|\| item.title \|\| item.executionCaseId}` — produkcyjne API / fallback atrapy demo (ma własne `title`) / ostatnia deska ratunku. Opcja dostała jawny `value={item.executionCaseId}` (wcześniej polegała na tekście widocznym = wartości). Test pojedynczy `tests/unit/initiatives-execution/executionCasesInitiativeTitle.test.ts` (mock `deps.reader`/`deps.authorize`, bez realnej bazy) — **PASS**. | `server/src/routes/pmo/initiativesExecutionRuntime.routes.ts`, `src/components/Execution/ExecutionReportsSurface.tsx`, `tests/unit/initiatives-execution/executionCasesInitiativeTitle.test.ts` |
| EXE-2 | POMINIĘTE z powodem (replika/nieosiągalna) | — | `grep -rn "ExecutionReportsSurface" src` poza samym plikiem komponentu daje DOKŁADNIE jedno miejsce: `ExecutionHub.tsx:5801` (Menu 1, zakładka Raporty — ta sama trasa mierzy już 0px OK). Zero miejsc w `src/` konsumuje query param `report=registry` — to WYŁĄCZNIE konwencja `dev-render/screens/execution-report-day11.tsx` (harness). Trasa standalone z luką 216px jest artefaktem opakowania harnessu (`min-h-screen p-4` zamiast kanonicznego `h-full flex flex-col`), nie defektem produkcyjnym — zgodnie z decyzją nadzorcy w instrukcji, zapisane bez naprawy. | brak (weryfikacja: `grep -rn "ExecutionReportsSurface" src`, `grep -rn "report=registry" src`) |
| EXE-4 | NAPRAWIONE | `65ff619bc4` + `2b8167832d` (baseline) | `RolloutRegisterEditModal.tsx:88` `focus:ring-1 focus:ring-primary-500` → `focus:ring-c-focus`. `check-focus-canon` baseline 109→108 plików. | `src/components/Execution/RolloutRegisterEditModal.tsx`, `scripts/check-focus-canon.baseline.txt` |
| EXE-3 | POTWIERDZONE (spot-check) | — | Patrz sekcja "Dostępność — spot-check" niżej. | — |
| EXE-5 | POMINIĘTE z powodem | — | Crimson ogólny (44 wystąpień/7 plików) — zgodnie z instrukcją §1, osobny dyżur. | — |

## 08_MEETINGS

| ID | Status | Commit | Dowód | Pliki |
|---|---|---|---|---|
| MEET-1 | POTWIERDZONE | `31b5214e0f` (przed tą sesją) | `public-booking-widget` (`src/views/PublicBookingView.tsx`) używa `useTranslation()`/`t()` (22 wywołania) i lokalizuje datę (`i18n.language`). Zweryfikowane wizualnie przez browser (harness, ekran `public-booking-widget`): PL pokazuje "Spotkanie z DBR77 Advisory / 60 min · strefa Europe/Warsaw / WYBIERZ DZIEŃ / Piątek, 4 Września"; EN pokazuje "Meeting with DBR77 Advisory / 60 min · Europe/Warsaw timezone / CHOOSE A DAY / Friday, September 4" (godziny 09:00→09:00 AM). Oba języki poprawne, zero mieszania. | brak zmian (weryfikacja: `src/views/PublicBookingView.tsx`, przeglądarka `?screen=public-booking-widget&lang=pl\|en`) |
| MEET-2, MEET-3, MEET-4 | POMINIĘTE (bez znaleziska naprawczego) | — | MEET-2/MEET-3 to potwierdzenia pozytywne w G13 (0 defektów, moduł przeżywa na świeżej bazie) — nic do naprawy. MEET-4 to obserwacja metodologiczna (moduł słabo zmierzony) — zadanie pomiarowe poza zakresem tej bramki naprawczej. | — |

## Dostępność — spot-check INIT-4/EXE-3/MW-6 (naprawione dziś, potwierdzenie jednym pomiarem pl-1440)

Uruchomiony `scripts/dev/grafika-zrzuty.mjs --a11y=1 --jezyk=pl --szerokosc=1440 --motywy=light`
na 2 ekranach per moduł (6 łącznie): `capacity-advisor-a3`, `plan-scenario-d1` (Initiatives),
`execution-tab-resources`, `execution-tab-work` (Execution), `mywork-inbox`,
`mywork-decisions` (My Work). Wynik zapisany w
`/private/tmp/ag-g14-05-08-artefakty/a11y-spotcheck/wynik.json`.

| Ekran | Moduł | axe naruszenia | Werdykt |
|---|---|---|---|
| `capacity-advisor-a3` | 05_INITIATIVES (INIT-4) | 0 | czysty |
| `plan-scenario-d1` | 05_INITIATIVES (INIT-4) | 0 | czysty |
| `execution-tab-resources` | 06_EXECUTION (EXE-3) | 0 (axe) — UWAGA: 31 błędów konsoli/17 HTTP 404, bo harness na porcie 5341 nie ma zamockowanego realnego backendu API dla tego ekranu (`/api/initiatives`, `/api/v8/execution-control/*` itd. — inaczej niż `capacity-advisor-a3`, który jest samodzielną atrapą dev-render). Ekran renderuje się (pusty stan błędu), axe nie łapie naruszeń na tym, co się faktycznie wyrenderowało — **niepełny dowód dla tego ekranu**, nie fałszywy negatyw dla realnego UI z danymi. | axe czysty, ale pomiar niepełny (brak danych) |
| `execution-tab-work` | 06_EXECUTION (EXE-3) | 0 (axe) — ta sama przyczyna braku backendu jak wyżej | axe czysty, pomiar niepełny |
| `mywork-inbox` | 07_MY_WORK_AGENT (MW-6) | **2** (`color-contrast`, impact `serious`) | **NIE czysty** — naruszenie nadal obecne mimo statusu "naprawiona dziś" w instrukcji |
| `mywork-decisions` | 07_MY_WORK_AGENT (MW-6) | 0 | czysty |

**Werdykt spot-checku**: 4/6 czyste bez zastrzeżeń. `execution-tab-resources`/`execution-tab-work`
dały axe=0, ale pomiar jest niepełny (ekran ładował się bez backendu w tym konkretnym porcie
harnessu — potrzebny osobny dyżur z realnym API zamockowanym, żeby dowód był pełny; NIE zgłaszam
tego jako regresję EXE-3, tylko jako lukę pomiarową tej sesji). `mywork-inbox` (MW-6) ma
**realne, niezamknięte naruszenie color-contrast (2 węzły, serious)** mimo że instrukcja
opisywała ten wolumen jako "naprawiony dziś" — zgodnie z zasadą "mój pomiar rządzi nad opisem
znaleziska" zgłaszam to jako otwarte, NIE naprawiam w tej sesji (poza jawnie przypisanym
zakresem MW-2/MW-3a/MW-7/MW-1/MW-5 dla modułu 07) — warte osobnego mini-dyżuru.

## Podsumowanie statusów

| Status | Liczba |
|---|---|
| NAPRAWIONE | 4 (MW-2, MW-7, EXE-1, EXE-4) |
| POTWIERDZONE WCZEŚNIEJ / POTWIERDZONE | 6 (INIT-2a, INIT-3, INIT-6, MW-3a, MEET-1, + spot-check a11y) |
| NAPRAWIONE (kod) / NIEZWERYFIKOWANE (dowód DB) | 1 (MW-5) |
| POMINIĘTE z powodem | 7 (INIT-2b, MW-1 [błędne znalezisko], MW-4, MW-8, EXE-2, EXE-5, MEET-2/3/4) |

## Commity tej sesji (worktree `/private/tmp/ag-g14-05-08`, gałąź `agent/g14-05-08-20260903`)

1. `fbd274e41c` — MW-2: dodaj `mywork-decisions` do kanonicznego pomiaru wysokości
2. `6fb7eb4961` — MW-7: pierścień fokusu crimson → c-focus (4 miejsca My Work)
3. `d99e4f21ba` — chore: obniż baseline check-focus-canon po MW-7
4. `b09cf4a782` — MW-5: decision escalation — backend endpoint + odczyt bez klobbera localStorage
5. `65ff619bc4` — EXE-4: pierścień fokusu crimson → c-focus (RolloutRegisterEditModal)
6. `2b8167832d` — chore: obniż baseline check-focus-canon po EXE-4
7. `bb5465b296` — EXE-1: dropdown "Wybierz realizację" pokazuje nazwę inicjatywy

Zero push. Zero dotknięcia `InitiativesHub.tsx` (część otwierania rekordu) ani
`InitiativeDocumentView.tsx` — bezpiecznik `tests/unit/initiatives/initiativeRecordCanon.test.ts`
uruchomiony PRZED raportem: **2/2 PASS**. Bezpiecznik retirement (`MyWorkHub.decisionsOwnerFeedback.test.ts`)
uruchomiony po decyzji NIE usuwać 12 kolejek MW-1: **1/1 PASS**.
