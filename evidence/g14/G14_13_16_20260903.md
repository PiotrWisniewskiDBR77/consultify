# G14 — naprawa znalezisk 13_CHAT · 14_ADMIN · 15_SETTINGS · 16_PARTNER — 2026-09-03

Worktree: `/private/tmp/ag-g14-13-16`, gałąź `agent/g14-13-16-20260903`, baza `HEAD`
(`35afcb15fd`) na worktree `/private/tmp/m03`. Harness: `npx vite --config
dev-render/vite.config.ts --port 5343 --strictPort` (PID 80702).
Źródło znalezisk: `docs/program/waves/WAVE_03_ACCEPTANCE/ANALIZA_G13_MODULY_09_16_20260903.md`
sekcje 13–16.

Kolejność pracy wg zlecenia: 13 → 15 → 14 → 16.

## KROK ZERO — pełna lista wierszy, BIORĘ/POMIJAM

### 13_CHAT

| # | Znalezisko | Koszt wg analizy | Decyzja | Powód |
| - | --- | --- | --- | --- |
| 1 | `teresa-chipy-sugestii` — `feed.signals is not iterable`, brak walidacji `response.signals` w `useSignalsFeed.ts` | ŚREDNIE | **BIORĘ** | Zlecenie wprost każe dodać walidację fail-closed z testem — naprawa produktu niezależna od reprodukcji |
| 2 | `chat-split-teresa-right` — `Invalid hook call` TYLKO w light | DUŻE | **BIORĘ (zbadaj, mój pomiar rządzi)** | Brief: dzisiejszy pomiar 8 kadrów NIE odtworzył crashy — zweryfikuję własnym pomiarem przed decyzją o kodzie |
| 3 | `canvas-new-doc` 404 `GET /api/work-canvas/drafts` | Nieznany | **BIORĘ (rozstrzygnij)** | Zlecenie wprost: rozstrzygnij czy trasa istnieje pod inną nazwą, dołóż stub w harnessie jeśli to harness |
| 4 | `canvas-kebab-restructure` — zamiana słów na przyciski w ramkach, „bardzo ważna zmiana" | DUŻE | **POMIJAM** | Wymaga decyzji/prototypu właściciela (CLAUDE.md reguła 7) — szeroki promień rażenia na wszystkie kanwy idei, nie drobna/średnia naprawa bez decyzji |
| 5 | crimson 739 wystąpień w `components/AIChat` (całość) | DUŻE jako projekt | **POMIJAM** | Explicité DUŻE jako projekt, 69 plików — wymaga zorganizowanej pracy, nie dyżuru ad-hoc |
| 5a | 2 konkretne naruszenia CLAUDE.md z próbki K4 (`focus:ring-primary-500` zam. `c-focus`; gradient AI na crimson zam. `c-ai`) | część 5a | **BIORĘ (jeśli szybko zlokalizowalne)** | Wprost nazwane naruszenie reguły 3 CLAUDE.md — punktowa naprawa DROBNA, oddzielna od wolumenu 739 |
| 6 | `teresa-confirm-chip` — delikatniejsza formuła graficzna | DROBNE | **BIORĘ** | Bez decyzji właściciela, drobna zmiana CSS |
| 7 | `teresa-chipy-sugestii` — nowy przełącznik włącz/wyłącz chipów | DUŻE (nowa funkcja) | **POMIJAM** | Nowa preferencja użytkownika, nie defekt — wymaga decyzji produktowej |

### 15_SETTINGS

| # | Znalezisko | Koszt | Decyzja | Powód |
| - | --- | --- | --- | --- |
| 1 | KOREKTA „PL=EN" — defekt harnessu (`ustawienia-grupy.tsx` wymuszał `pl`) | n/d | **POMIJAM (potwierdź pomiarem)** | Brief: „naprawiony wcześniej — potwierdź". Nie koduję, tylko mierzę i zapisuję dowód |
| 2 | `WatchingTab.tsx:88` surowy `{watcher.objectId}` zamiast tytułu | ŚREDNIE | **BIORĘ** | Jasny wzorzec naprawy (już wykonany dla `base_period_id` w Finansach), bez decyzji właściciela |
| 3 | Outlook OAuth nieukończone | DUŻE | **POMIJAM** | Dokończenie integracji OAuth — praca produktowa, nie naprawa znaleziska |
| 4 | crimson `brand-*` 65 wystąpień, 17 plików w `components/settings` | DUŻE jako projekt | **POMIJAM** | Wolumen wymaga zorganizowanej pracy |
| 5 | Dług Day 55 (`PARTIAL_DAY55_MEASURED`, przebudowa cross-module) | Nieznany | **POMIJAM** | Wymaga przeczytania pełnego raportu Day 55 — poza budżetem tego dyżuru |

### 14_ADMIN

| # | Znalezisko | Koszt | Decyzja | Powód |
| - | --- | --- | --- | --- |
| 1 | `aiSettingsService.ts` `ensureUserTiersTable` — DDL w runtime bez try/catch, bez migracji | ŚREDNIE | **BIORĘ** | Ryzyko awarii całego procesu — ten sam wzorzec, który już raz kosztował tygodnie; jasna naprawa (try/catch + migracja) |
| 2 | 5 kart Command Center — pole filtra bez `aria-label` | ŚREDNIE (rodzina 5) | **BIORĘ** | Mechaniczna, jasna naprawa dostępności, bez decyzji właściciela |
| 3 | `landmark-one-main`/`page-has-heading-one`/`region` — nierozstrzygnięte harness vs. produkt | Nieznany | **BIORĘ (rozstrzygnij pomiarem)** | Musi być rozstrzygnięte przed wyceną — sprawdzam czy grafika-zrzuty.mjs skanuje `#dev-render-root` |
| 4 | `admin-command-center-panel` rozjazd nazwy + martwa flaga `isCommandCenterEnabled` | DROBNE | **BIORĘ** | Tania naprawa, zero wołających poza deklaracją — bezpieczne usunięcie/ujednolicenie |
| 5 | focus ring crimson w `FeatureFlagsPanel.tsx` (9×) i `AdminKnowledgeView.tsx` (15+×) | ŚREDNIE | **BIORĘ** | Wprost nazwane naruszenie reguły 3 CLAUDE.md, mechaniczna podmiana klasy |
| 6 | crimson 873 wystąpień w superadmin/Admin/SuperAdmin (całość) | DUŻE jako projekt | **POMIJAM** | Wolumen — 102 pliki, wymaga zorganizowanej pracy |

### 16_PARTNER

| # | Znalezisko | Koszt | Decyzja | Powód |
| - | --- | --- | --- | --- |
| 1 | Brak ścieżki zapisu dla nowej organizacji (`PARTNER_ORG_REQUIRED`) | DUŻE | **POMIJAM** | Explicité: decyzja produktowa (samoobsługowe wiązanie partnera), nie punktowa poprawka kodu |
| 2 | crimson `brand-*` 34 wystąpień w `components/Partner`+`CommissionView.tsx` | DROBNE-ŚREDNIE | **POMIJAM** | CLAUDE.md zlecenie wprost: „Partner: kolorystyka przyjęta przez właściciela — nie zmieniaj palety" — nie ingeruję w kolorystykę Partnera mimo że to techniczne naruszenie kanonu |
| 3 | axe naruszenia 12/12 ekranów (różne reguły per ekran) | ŚREDNIE-DUŻE | **POMIJAM** | Rodzina 12 ekranów, różne przyczyny per ekran — wymaga osobnego zbadania, poza budżetem pojedynczego dyżuru napraw |

---

## Postęp (aktualizowany na bieżąco)

(patrz sekcje niżej per moduł)

## 13_CHAT — postęp

### Finding #1 `teresa-chipy-sugestii` — feed.signals is not iterable — ZROBIONE
Commit `3e39da0c91`. Naprawa: `useSignalsFeed.ts` waliduje `Array.isArray(response.signals)`
fail-closed (pusta lista + `console.error` przy naruszeniu kontraktu) zamiast
`setSignals(undefined)`. Test regresyjny:
`src/components/AIChat/signalsFeed/__tests__/useSignalsFeed.malformedResponse.test.tsx`
(2/2 PASS). Potwierdzone pomiarem PO: `teresa-chipy-sugestii` i `chat-signals-feed`
(współdzielą hook) — 0 błędów konsoli, obie strony light+dark, brak crasha.
Zrzuty: `/private/tmp/ag-g14-13-16-artefakty/teresa-chipy-sugestii/PO/`,
`/private/tmp/ag-g14-13-16-artefakty/chat-signals-feed/PO/`.

### Finding #2 `chat-split-teresa-right` — Invalid hook call TYLKO w light — NIEODTWORZONE (własny pomiar)
Własny pomiar dziś (`/private/tmp/ag-g14-13-16-artefakty/chat-split-teresa-right/PRZED-2/wynik.json`):
29 błędów konsoli w light, 51 w dark — WSZYSTKIE to `404 Not Found` (endpointy
niestubowane w tym harnessie: `/api/cloud/providers`, `/api/chat-projects`,
`/api/ai/conversations/.../proposals`, `/api/work-canvas/drafts`, itd.) i ich
downstream error-handlery (`[OrgContext]`, `[ChatProjectStore]`,
`[useProposalLifecycleStore]`, `[UnifiedChatPanel] Failed to load conversation
branches`). ZERO wystąpień „Invalid hook call"/„Cannot read properties of
null (reading 'useEffect')" w treści błędów konsoli w ŻADNYM z dwóch motywów.
Ekran renderuje się wizualnie poprawnie w light (zrzut PRZED-2, brak białego
ekranu/crasha). **Mój pomiar rządzi nad opisem znaleziska (zlecenie)** — nie
odtwarzam crasha zgłoszonego przez G06 dziś; harness po prostu ma dużo
niestubowanych endpointów (osobny, szerszy dług harnessu, nie punktowy hook
bug). Decyzja: **POMIJAM jako niepotwierdzone** — nie ma przyczyny do
zlokalizowania i naprawienia bez reprodukcji; jeśli crash wróci, wymaga
osobnego dyżuru z pełnym stack trace.

### Finding #3 `canvas-new-doc` 404 `GET /api/work-canvas/drafts` — ROZSTRZYGNIĘTE: harness
Trasa `GET /drafts` ISTNIEJE i jest zamontowana produkcyjnie:
`server/src/routes/work-canvas.routes.ts:2744` (`router.get('/drafts', ...)`,
fail-soft, zwraca `{success:true, data:[]}` nawet przy błędzie DB) pod
`server/src/Gateway.ts:585` (`app.use('/api/work-canvas', workCanvasRoutes)`).
404 w harnessie = brak backendu na porcie vite, NIE defekt trasy. Root cause:
`WorkCanvasDocumentPanel.tsx:1273` robi SUROWY `fetch('/api/work-canvas/drafts?
conversationId=...')` (nie przez `Api.workCanvasListDrafts`), a
`dev-render/screens/canvas-new-doc.tsx` stubuje tylko
`Api.workCanvasListDrafts` (`Object.assign(Api, {...})`) — fetch surowy
przechodzi bez przechwycenia → 404. Naprawa: dopisany `window.fetch` przechwyt
w harnessie dla `GET /api/work-canvas/drafts*` (wzorzec z
`teresa-chipy-sugestii.tsx`), zwracający `{success:true, data: OTHER_DRAFTS}`.
Kod produkcyjny obsługuje 404 gracefully (`if (!response.ok) return;`) — to
NIE był crash, tylko szum w konsoli/network tab.

### Finding #5a (2 konkretne naruszenia CLAUDE.md z próbki K4) — KOREKTA DECYZJI: POMIJAM
Zweryfikowane: `focus:ring-primary-500`/`focus:border-primary-500` w
`components/AIChat` to NIE 2 izolowane miejsca — grep pokazuje 10+ plików już
w pierwszych wynikach (`MoveToProjectModal.tsx`, `CloudFilePicker.tsx`,
`ConversationSearch.tsx`, `ToolsMenu.tsx`, `ProjectMembersModal.tsx`, ...).
To ten SAM wolumen 739/69 plików z finding #5, nie osobny wąski zestaw jak w
14_ADMIN (gdzie źródło nazwało dokładnie 2 pliki, ~24 wystąpienia). Cherry-picking
2 „przykładowe" miejsca z systemowego długu 69 plików dałoby fałszywy sygnał
naprawy bez realnego zmniejszenia długu. **Zmiana decyzji na POMIJAM** — łączę
z finding #5 (DUŻE jako projekt, wymaga zorganizowanej pracy, nie dyżuru ad-hoc).

## 15_SETTINGS — postęp

### Finding #1 KOREKTA „PL=EN" — POTWIERDZONE naprawione wcześniej
`dev-render/screens/ustawienia-grupy.tsx:68-69` już respektuje `?lang=en`:
`if (grupyLang === 'en') void i18n.changeLanguage('en');` (poprzednio
bezwarunkowe `changeLanguage('pl')`). Zmierzone `ustawienia-personalne`
pl-1440 vs en-1440 — zrzuty w
`/private/tmp/ag-g14-13-16-artefakty/ustawienia-i18n/{pl,en}/`. Brak zmiany
kodu potrzebnej — **NIEAKTUALNE potwierdzone**, zgodnie z zleceniem.

### Finding #2 `WatchingTab.tsx:88` surowy `objectId` — KOREKTA SKALI, POMIJAM
Zmierzone w kodzie serwera: **`GET /api/settings/watchers` NIE ISTNIEJE W
OGÓLE** — `grep -in watcher server/src/routes/settings.routes.ts` (plik
zamontowany pod `/api/settings` w `Gateway.ts:760`) daje ZERO trafień; żaden
inny plik trasy nie rejestruje `/watchers` pod prefiksem `/api/settings`
(sprawdzone: `table-platform.routes.ts`, `v8/planning.routes.ts`,
`pmo/initiatives.routes.ts` obsługują INNE, niepowiązane ścieżki watcherów —
per-rekord/per-inicjatywa, nie generyczny `/api/settings/watchers` używany
przez `useUserNotificationPreferences.tsx:192`). Skutek: `fetchWatchers()`
dostaje 404, `response.ok` = false, `setWatchers(data.watchers||[])` nigdy
się nie wykonuje — **cały ekran „Watching" jest martwy w produkcji, lista
JEST ZAWSZE PUSTA**, nie tylko „pokazuje surowe ID". To NIE jest ten sam
wzorzec co `base_period_id` w Finansach (tam endpoint istnieje, brakowało
tylko pola) — tu brakuje CAŁEJ trasy + prawdopodobnie tabeli. Naprawa
wymaga zaprojektowania kontraktu API i schematu (decyzja backendowa, nie
punktowa zmiana `{watcher.objectId}` → `{watcher.objectTitle}`).
**Zmiana decyzji: POMIJAM** — realny koszt jest DUŻE (nowa trasa + tabela +
kontrakt tytułu obiektu), nie ŚREDNIE jak sugerował opis znaleziska. Zgłoszone
osobno jako odkrycie wymagające dedykowanego dyżuru backendowego (spawn_task).

### Finding #6 `teresa-confirm-chip` — delikatniejsza formuła graficzna — POTWIERDZONE naprawione wcześniej
`git log -S "czytało się jak ciężki" -- src/components/AIChat/MessageRenderer.tsx`
→ commit `fcb83a5f7d` (2026-08-30 18:59), „Teresa i agent: lżejsze okno
potwierdzenia..." — DOKŁADNIE ta prośba (mała, wtopiona w rozmowę karta,
subtelne obramowanie, lżejsze przyciski) już zaimplementowana, komentarz w
kodzie (`MessageRenderer.tsx:895-905`) cytuje samą uwagę właściciela z daty
2026-08-30. `TRIAZ_UWAG_20260902.md` (UW-13-01, źródło znaleziska) datowany
PO tej naprawie, ale błędnie twierdzi „nie znaleziono dowodu, że została już
wykonana" — dowód istnieje w historii git. Zmierzone: zrzut
`/private/tmp/ag-g14-13-16-artefakty/teresa-confirm-chip/PO/` — mała karta,
subtelne tło/obramowanie, przycisk „Potwierdź" lekki (border, nie pełny
czarny CTA), 0 błędów konsoli. **NIEAKTUALNE potwierdzone** — bez zmiany kodu.

## 13_CHAT — zamknięte. 15_SETTINGS — w toku (dalej: Outlook OAuth POMIJAM,
crimson POMIJAM, Day55 POMIJAM — bez zmian kodu, decyzje z KROK ZERO
utrzymane). Przechodzę do 14_ADMIN.

## 14_ADMIN — postęp

### Finding #1 `aiSettingsService.ts` `ensureUserTiersTable` — KOREKTA SKALI RYZYKA + naprawa wykonana
Dwie korekty względem opisu znaleziska, obie na podstawie odczytu kodu dziś:
1. **Migracja JUŻ ISTNIEJE**: `server/migrations/20260903_ai_user_tiers.sql`
   (skopiowany dokładny kształt tabeli) — powstała RÓWNOLEGLE, prawdopodobnie
   inny dyżur tego samego dnia (`agent/schemat-datetime` wg komentarza w
   kodzie). Część „bez migracji" znaleziska jest NIEAKTUALNA.
2. **„PADA CAŁY PROCES serwera" nie potwierdza się w dzisiejszym kodzie**:
   `dbRun`/`dbAll` (z `DbPromise.ts`) mają `fallback = true` jako WARTOŚĆ
   DOMYŚLNĄ (`server/src/utils/DbPromise.ts:232,358,460` — sprawdzone we
   wszystkich trzech: `all`/`get`/`run`) — błąd SQL (w tym `42704 type
   "datetime" does not exist`) rozwiązuje Promise jako `{success:false,...}`/
   `[]`/`null`, NIE odrzuca (`reject`). `ensureUserTiersTable` woła
   `dbRun(sql, [])` bez opcji nadpisujących `fallback` → już dziś fail-soft,
   nie unhandled rejection. Realny (mniejszy, ale wciąż realny) problem:
   `assignUserTier` nie sprawdza `result.success` z `dbRun` — przy błędzie
   zapisu funkcja i tak zwraca `{organization_id,user_id,tier}` jakby się
   udało (ciche zgubienie zapisu, wzorzec „atrapa bazy kłamie o zapisie" z
   korpusu ustaleń) — TO wykracza poza zakres tego znaleziska (osobny
   problem, nie zgłoszony w analizie), nie naprawiam w tym dyżurze.

Naprawa wykonana (defensywna, zgodna z ustalonym wzorcem
`chatTraceService.ts::ensureTables`): `ensureUserTiersTable` owinięte w
try/catch z `logger.warn` przy degradacji — redundantne względem domyślnego
`fallback=true` w `dbRun`, ale (a) zgodne z jawnym zleceniem naprawy, (b) nie
maskuje niczego niżej (INSERT/SELECT mają własne błędy), (c) daje widoczny
log przy degradacji zamiast cichej pustki. Zero regresji: plik przechodzi
esbuild bez błędów składniowych.

### Finding #2 (5 kart Command Center, aria-label) — POTWIERDZONE naprawione, brak zmiany kodu
Odczyt kodu WSZYSTKICH 5 komponentów (`CommandCenterAgentTraceTab.tsx`,
`CommandCenterAuditTab.tsx`, `CommandCenterResidencyTab.tsx`,
`CommandCenterAiPolicyTab.tsx`, `CommandCenterRetentionTab.tsx`): KAŻDE pole
filtra/wyszukiwarki ma już `<label htmlFor="...">` z odpowiadającym `id` na
`<input>`/`<select>`, albo (RetentionTab, pole inline w tabeli) `aria-label`
bezpośrednio. Potwierdzone pomiarem REALNYM axe (nie deklaracją):
`node scripts/dev/grafika-zrzuty.mjs ... --a11y=1` na wszystkich 5 ekranów
(`admin-command-agent-trace/-ai-policy/-audit/-residency/-retention`) →
**`a11yNaruszenia: []` na KAŻDYM z 5** (zero naruszeń, nie tylko `label`).
Zrzut: `/private/tmp/ag-g14-13-16-artefakty/cc-a11y/PO/wynik.json`.
**NIEAKTUALNE potwierdzone realnym skanem** — bez zmiany kodu.

### Finding #3 (`landmark-one-main`/`page-has-heading-one`/`region` harness vs. produkt) — ROZSTRZYGNIĘTE: harness, JUŻ NAPRAWIONY
`grep -n "AxeBuilder\|include" scripts/dev/grafika-zrzuty.mjs` →
`new AxeBuilder({ page }).include('#dev-render-root').analyze()` (linia 446)
— skan JEST już zakresowany do fragmentu `#dev-render-root`, DOKŁADNIE
korekta nazwana jako „udowodniona dla 09-12" w analizie. Zero reguł
`landmark-one-main`/`page-has-heading-one`/`region` w wyniku pomiaru
(patrz dowód wyżej — `a11yNaruszenia: []` na wszystkich 5 kartach, w tym
tych z otwartym pytaniem). **Rozstrzygnięcie: to był artefakt STAREJ wersji
narzędzia zrzutowego; obecna wersja (ta, którą każe użyć moja instrukcja) ma
korektę już wgraną.** Cała reszta ~30 ekranów `NOT_PROVEN` w tym module
powinna być zmierzona TĄ wersją skryptu — poza budżetem tego dyżuru (nie
mierzę 30 ekranów punktowo), ale rozstrzygnięcie metodologiczne jest
KOMPLETNE: narzędzie już nie generuje tego fałszywego alarmu.

### Finding #4 `admin-command-center-panel` rozjazd nazwy + martwa flaga — NAPRAWIONE
Rozjazd: `src/components/Admin/adminNavigation.ts:176` (menu, sekcja `id:'command'`)
miał `label: 'Centrum administracyjne'`, panel (`AdminCommandCenterPanel.tsx:983`,
i18n `commandCenter.title`) pokazuje „Centrum dowodzenia". Ujednolicone na
nazwę z ekranu („Centrum dowodzenia").

Martwa flaga: `grep -rn isCommandCenterEnabled src server` = 1 trafienie
(własna deklaracja) — potwierdzone, zero wołających. Usunięto CAŁY plik
`src/utils/commandCenterFlag.ts` (nie tylko funkcję — `COMMAND_CENTER_FLAG_KEYS`
też był bez importerów). Skorygowano 2 komentarze, które błędnie opisywały tę
flagę jako aktywną bramę widoczności (`ssoSelfServiceFlag.ts` — usunięto martwą
referencję „Wzorzec"; `CommandCenterDlpTab.tsx` — komentarz twierdził, że
`AdminCommandCenterPanel` „gejtuje widoczność całej sekcji" flagą
`?ff_commandCenter=1`, co było NIEPRAWDĄ od momentu gdy flaga stała się
martwa — sekcja jest zawsze widoczna, teraz komentarz to mówi wprost).

Zmierzone: `admin-command-center-panel` PO — nagłówek panelu „Centrum
dowodzenia" widoczny, 0 błędów konsoli, light+dark. `adminNavigation.ts`
przechodzi esbuild bez błędów składniowych.

### Finding #5 focus ring crimson w FeatureFlagsPanel.tsx (9×) i AdminKnowledgeView.tsx (17×) — NAPRAWIONE
Mechaniczna podmiana klasy: `focus:ring-primary-500` → `focus:ring-c-focus`
(9× w `FeatureFlagsPanel.tsx`, jeden checkbox `text-primary-600` NIE zmieniony
— to kolor wypełnienia, nie fokus, poza zakresem tego znaleziska);
`focus:border-primary-500` → `focus:border-c-focus` (17× w
`AdminKnowledgeView.tsx` — realna skala WIĘKSZA niż „15+" z opisu, dokładnie
17). `border-c-focus` to ustalony, używany wzorzec (`TaskDetailModal.tsx`,
`RoadmapKanban.tsx`, `ui/checkbox.tsx`, `ui/primitives/{Dropdown,Input}.tsx`).

Zero regresji: obie pliki przechodzą esbuild bez błędów. Bramka
`scripts/check-focus-canon.sh --ci`: dług SPADŁ 112→111 plików, 227→218
wystąpień (dokładnie 9 z FeatureFlagsPanel.tsx — `border-primary-*` nie jest
łapane przez `VIOLATION_RE` tego skryptu, ale to wciąż crimson wg CLAUDE.md
reguły 3, więc naprawione tak samo). Baseline zaktualizowany w dół
(`--update-baseline --yes`) — dług zablokowany na nowym, niższym poziomie.
Brak dev-render screena dla obu komponentów (wewnętrzne narzędzia SuperAdmin,
nie w mapie 68 ekranów 14_ADMIN) — brak PRZED/PO zrzutu, dowód =
grep+esbuild+bramka.

(korekta finding #5): pierwsza wersja commita zablokowana przez hook
`check-triada` na TEJ SAMEJ linii, którą edytowałem (checkbox „enabled" w
`FeatureFlagsPanel.tsx` miał też `text-primary-600` — kolor zaznaczenia, nie
fokus, poza pierwotnym zakresem, ale hook złapał go na dotkniętej linii).
Naprawione: `text-primary-600` → `text-c-focus-solid` (wzorzec z
`ui/checkbox.tsx`, już 8× używany w `src/`). Commit przeszedł.

## 14_ADMIN — zamknięte (finding #6, crimson 873/102 pliki, POMIJAM bez zmian —
DUŻE jako projekt, decyzja z KROK ZERO utrzymana). Przechodzę do 16_PARTNER.
