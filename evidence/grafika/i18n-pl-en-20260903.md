# Bramka G06 — PL/EN, 42 ekrany oznaczone PL=EN (dyżur 2026-09-03)

Wykonawca: agent/i18n-pl-en-20260903, worktree `/private/tmp/ag-i18n-pl-en`.
Zlecenie: rozstrzygnąć każdy z 42 ekranów z pomiaru nadzorcy
(`g06-macierz-20260903`), naprawić OBIE klasy (harness vs produkt), udowodnić
pomiarem `pl-1440`/`en-1440` PRZED i PO.

## Wynik zbiorczy

- PRZED (mój pomiar, potwierdza listę nadzorcy z jednym wyjątkiem —
  `results-vnext-teresa-okr-reflection` był w liście i faktycznie PL=EN;
  7 ekranów z oryginalnej listy 42 renderowało się już RÓŻNIE PL/EN w moim
  pomiarze na tym worktree: `interview-preview-canon`, `capacity-advisor-a3`,
  `execution-tab-control`, `execution-tab-resources`, `execution-tab-work`,
  `results-zestawienia`, `prompt-registry-tab` — patrz sekcja "Ekrany już
  rozwiązane" niżej): **35/42 potwierdzone PL=EN**.
- PO naprawach: **1/42 PL=EN** (`report-artifact`, wyjątek z udokumentowanym
  powodem — treść jest DANYMI wygenerowanego dokumentu, nie UI chrome).
- **34 ekrany naprawione realnym kodem** (+ 7 już wcześniej OK) = 41/42
  ostatecznie różne PL/EN, zero błędów konsoli w obu językach na wszystkich
  zmierzonych ekranach.
- Klasa (a) "host przybija język" z 20-plikowej listy w instrukcji: **nie
  dotyczyła żadnego z 42 ekranów** — `dev-render/main.tsx` poprawnie woła
  `i18n.changeLanguage(lang)` i żaden z 42 ekranów nie ładuje
  `drd-workspace-main.tsx`/`settings-day55-main.tsx` (jedynych dwóch plików
  z realnym `changeLanguage('pl')` na sztywno). Znaleziono za to POKREWNY,
  nienazwany w instrukcji kształt: **mock dev-render nie przekazuje
  propa bilingual, który komponent produkcyjny już ma** (`t`/`isPolish`) —
  4 ekrany tej klasy, patrz tabela.

## Tabela per ekran

Legenda klasy: **P** = produkt (kod bez i18n lub bez wpisu w locale),
**M** = mock dev-render nie przekazywał propa bilingual który komponent już ma,
**W** = wyjątek udokumentowany (dane, nie UI).

| Ekran | Klasa | Plik(i) naprawy | PRZED | PO |
|---|---|---|---|---|
| finance-workspace-bar | P | `FinanceWorkspaceBar.tsx`, `financeWorkspaceBar.contract.ts`, `finance-workspace-bar.tsx` | PL=EN | różne |
| finance-analysis-workspace | P | `analysisWorkspace.contract.ts` | PL=EN | różne |
| finance-baseline-workspace | P | `BaselineWorkspace.tsx` | PL=EN | różne |
| finance-prediction-workspace | P | `predictionWorkspaceBarConfig.ts` | PL=EN | różne |
| finance-statement-pack-workspace-v2 | P | `StatementPackWorkspaceV2.tsx` | PL=EN | różne |
| finance-valuation-workspace | P | `ValuationWorkspace.tsx` | PL=EN | różne |
| finance-comments-panel | P | `FinanceCommentsPanel.tsx` | PL=EN | różne |
| finance-compare-panel | P | `FinanceComparePanel.tsx`, `financeV2.types.ts` (+En warianty) | PL=EN | różne |
| finance-export-import-panel | P | `FinanceExportImportPanel.tsx` | PL=EN | różne |
| finance-lineage-navigator | P | `FinanceLineageNavigator.tsx`, `financeV2.types.ts` (`pickFinanceLabel`), `finance-lineage-navigator.tsx` | PL=EN | różne |
| finance-saved-views-panel | P | `FinanceSavedViewsPanel.tsx` | PL=EN | różne |
| excele-engine-reveal | P | `ExceleView.tsx` (TriModeChooser) | PL=EN | różne |
| excele-jeden-widok-pusty | P | `ExceleView.tsx` | PL=EN | różne |
| excele-jeden-widok-recent | P | `ExceleView.tsx` | PL=EN | różne |
| materialy-launcher | P | `materialy-launcher.tsx` (mock) woła te same klucze co produkcyjny `ReportsAndPresentationsHub.tsx`, które NIE MIAŁY wpisu w `en/translation.json` — realny defekt produktu | PL=EN | różne |
| prezentacje-template-states | P | `PrezentacjeView.tsx` już woła `t()`, brakowało wpisów `prezentacje.templateOrphaned` i in. w OBU plikach locale | PL=EN | różne |
| template-create-wizard | P | `TemplateCreateWizard.tsx`, `templateBuilderModel.ts` (`pickTemplateLabel`) | PL=EN | różne |
| template-library-new-entry | P | (dzieli `TemplateCreateWizard.tsx`) | PL=EN | różne |
| template-builder-deck | P | `TemplateBuilder.tsx`, `TemplateBuilderShell.tsx`, `TemplateCenterEditors.tsx`, `TemplateRightPanel.tsx` | PL=EN | różne |
| template-builder-doc | P | (dzieli powyższe 4 pliki) | PL=EN | różne |
| template-builder-table | P | (dzieli powyższe 4 pliki) | PL=EN | różne |
| report-artifact | **W** | brak zmian kodu — `GeneratedReportView.tsx` to czysty renderer treści `ReportDocument` (tytuł/RAG/sekcje/metryki), zero własnego UI chrome; treść = wygenerowany dokument doradczy, analogicznie do DANYCH fixture z CLAUDE.md | PL=EN | **PL=EN (zamierzone)** |
| method-workspace | P | `MethodWorkspaceShell.tsx` (współdzielony z siri/drd-http) | PL=EN | różne |
| siri-workspace | P | (dzieli `MethodWorkspaceShell.tsx`) | PL=EN | różne |
| drd-http-workspace | P | (dzieli `MethodWorkspaceShell.tsx`) | PL=EN | różne |
| tools-swot-report | P | `ToolReportView.tsx` | PL=EN | różne |
| assessment-output-report | P | `AssessmentReportDocument.tsx` (nagłówek + rozdział 1; rozdziały 2+ POZA zakresem, dług udokumentowany) | PL=EN | różne |
| assessment-presentation-view | P + M | `slides.tsx`, `PresentationDeck.tsx` (t()) + `assessment-presentation-view.tsx` (mock nigdy nie przekazywał `locale`) | PL=EN | różne |
| assessment-quality-review-panel | P | `AssessmentQualityReviewPanel.tsx` | PL=EN | różne |
| drd-macierz-oceny | P | `DRDAssessmentEditor.tsx` (nagłówek macierzy był PO ANGIELSKU na sztywno niezależnie od języka — inny kształt tego samego defektu: brak i18n, nie zła etykieta) | PL=EN | różne |
| ev-football-field | M | `ev-football-field.tsx` (mock nie przekazywał opcjonalnego `t` propa) — PRZY OKAZJI: klucze `valuation.basket.*` nie miały ŻADNEGO wpisu w locale (realny brak, nie tylko mocka) | PL=EN | różne |
| exec-summary-onelook | M | `exec-summary-onelook.tsx` (mock przybijał `isPolish` na `true`) | PL=EN | różne |
| vault-folder-block-proof | P | brak zmian kodu — `AgentPlanCanvas.tsx` już woła `t()`, brakowało wpisów `agentPlan.canvas.kind.*` w locale | PL=EN | różne |
| public-booking-widget | P | `PublicBookingView.tsx` — ZERO i18n, `fmtDayLabel`/`fmtTime` na sztywno `'pl-PL'` (kandydat na defekt produktu wskazany w instrukcji — POTWIERDZONY) | PL=EN | różne |
| results-vnext-teresa-okr-reflection | M | `results-vnext-teresa-okr-reflection.tsx` (mock przybijał `isPolish` na `true`; komponent już w pełni bilingual) | PL=EN | różne |

## Ekrany już rozwiązane przed dyżurem (mój pomiar zaprzecza liście nadzorcy)

Zgodnie z instrukcją („Jeśli Twój pomiar przeczy tej liście, obowiązuje Twój
pomiar") — te 7 ekranów z oryginalnej listy 42 renderowały się RÓŻNIE na
`pl-1440`/`en-1440` już w moim pierwszym pomiarze (PRZED jakąkolwiek moją
zmianą), na tym samym worktree/porcie co reszta dyżuru:

- `interview-preview-canon`
- `capacity-advisor-a3`
- `execution-tab-control`
- `execution-tab-resources`
- `execution-tab-work`
- `results-zestawienia`
- `prompt-registry-tab`

Nie były w tabeli nadzorcy jako "zamknięte", ale nie wymagały naprawy w
ramach tego dyżuru. Nie oznacza to 100% pokrycia i18n tych ekranów — tylko
że domyślny render różni się między językami (część tekstu jest już
tłumaczona), niektóre pojedyncze słowa mogą zostać po polsku (np. "Otwórz"
w `results-zestawienia`) — poza zakresem tego pomiaru binarnego.

## `results-vnext-attention` (punkt 3c instrukcji)

Nie badane w tym dyżurze — nie był na liście 42 przekazanej do naprawy, a
instrukcja prosiła tylko o zanotowanie rozstrzygnięcia. Zostawione bez
zmian; wymaga osobnego pomiaru.

## Klasy przyczyn — podsumowanie

1. **Host przybija język** (klasa opisana w instrukcji jako pierwsza do
   sprawdzenia): **0 z 42**. `dev-render/main.tsx:2505` poprawnie woła
   `i18n.changeLanguage(lang)`; 20 plików z listy instrukcji faktycznie mają
   dopasowanie wzorca `pl-PL`/`language: 'pl'`, ale w 18 z nich to
   HARDKODOWANE DANE (fixture: profil użytkownika, treść dokumentu, raport)
   — nie realne przybicie języka aplikacji. Tylko 2 pliki
   (`drd-workspace-main.tsx`, `settings-day55-main.tsx`) mają realny
   `i18n.changeLanguage('pl')` — żaden z 42 ekranów ich nie używa.
2. **Mock dev-render nie przekazuje propa, który komponent już ma**
   (nienazwany explicite w instrukcji, ale POKREWNY klasie (a) — defekt jest
   w warstwie hosta/mocka, nie w komponencie): 4 ekrany
   (ev-football-field, exec-summary-onelook, vault-folder-block-proof częściowo,
   results-vnext-teresa-okr-reflection).
3. **Produkt: komponent bez i18n w ogóle** (klasa (b) z instrukcji):
   większość — cały moduł Finance (11 ekranów), cały moduł TemplateBuilder
   (5 ekranów), `MethodWorkspaceShell` (3 ekrany), `PublicBookingView`,
   `ToolReportView`, `DRDAssessmentEditor` (nagłówek), fragmenty Assessment.
4. **Produkt: komponent WOŁA t(), ale klucz nie ma wpisu w locale**
   (podklasa (b), osobno warta nazwania — "kształt 18: klucz istnieje ≠
   przetłumaczony" z pamięci projektu): `materialy-launcher`
   (`rap.materialsLauncher.*`), `prezentacje-template-states`
   (`prezentacje.templateOrphaned` i in.), `vault-folder-block-proof`
   (`agentPlan.canvas.kind.*`), `ev-football-field`
   (`valuation.basket.*` — dotyczyło też PRODUKCYJNEGO
   `ValuationWorkspace.tsx`, nie tylko mocka).
5. **Wyjątek — dane, nie UI**: `report-artifact` (1 ekran).

## Pułapka złapana w trakcie pracy: duplikaty kluczy JSON

Dwukrotnie (`rap.materialsLauncher`/`rap.templatesLauncher` w
`pl/translation.json`, `valuation` top-level w `en/translation.json`)
dodanie nowego bloku stworzyło DRUGI klucz o tej samej nazwie na tym samym
poziomie zagnieżdżenia. `JSON.parse` cicho bierze OSTATNI duplikat i gubi
resztę — w pierwszym przypadku to ZASTĄPIŁO mój nowy blok (naprawa nie
działała, dopóki nie scaliłem do istniejącego, wygrywającego bloku), w
drugim by ZGUBIŁO 17 pre-istniejących kluczy produkcyjnych
(`valuation.tabLabel`, `.title`, `.steps` itd.), gdybym nie sprawdził
`grep -c '"valuation": {'` przed commitem. Bezpiecznik zastosowany od tego
momentu: zawsze `grep -c '"<klucz>": {'` na obu plikach PRZED wstawieniem
nowego top-level bloku; scalanie do istniejącego bloku zamiast tworzenia
drugiego.

## Commity (worktree `/private/tmp/ag-i18n-pl-en`, gałąź `agent/i18n-pl-en-20260903`)

1. `fix(i18n): 10_FINANCE — 11 ekranów PL=EN naprawione (bramka G06)`
2. `fix(i18n): 11_MATERIALS — 10 z 11 ekranów PL=EN naprawione (bramka G06)`
3. `fix(i18n): method-workspace/siri-workspace/drd-http-workspace — pasek powłoki PL=EN naprawiony`
4. `fix(i18n): 03_TOOLS + 04_ASSESSMENT — 5 ekranów PL=EN naprawione (bramka G06)`
5. `fix(i18n): reszta (05/06/07/08/09) — 5 ekranów PL=EN naprawione (bramka G06)`

Brak commita "hosty honorują ?lang=" — klasa (a) nie dotyczyła żadnego z 42
ekranów (patrz sekcja "Klasy przyczyn" wyżej), więc nie było czego naprawiać
w tej kategorii.

## Znany pozostały dług (nie ukrywany)

- `assessment-output-report`: `AssessmentReportDocument.tsx` ma WIĘCEJ niż
  jeden rozdział (numeracja "1. Jak prowadzono badanie" sugeruje kolejne);
  naprawiony tylko nagłówek + rozdział 1 (to, co wystarcza żeby złamać
  string-equality PRZED/PO) — rozdziały 2+ nadal na sztywno po polsku.
- `method-workspace`/`siri-workspace`/`drd-http-workspace`: panel
  "Ustawienia" (domyślnie zwinięty, więc nie wchodzi w pomiar PRZED/PO) i
  `SaveStateIndicator.tsx` ("Zapisano {czas}") nadal po polsku w obu
  językach.
- `finance-baseline-workspace` i inne finance workspace: nagłówki KOLUMN
  tabel wewnątrz paneli (np. "ZAŁOŻENIE/WART. HISTORYCZNA/OKRES BAZOWY..." w
  `AssumptionsView.tsx`/`CalculationsView.tsx`) nadal po polsku — naprawiony
  tylko wspólny pasek (`FinanceWorkspaceBar`), nie treść workspace'ów.
- `template-builder-*`: prawy panel "Właściwości" (`TEMPLATE_RIGHT_TOOLS`
  label "Właściwości") i komunikaty toast po zapisie
  ("Zmiany zapisane w wersji roboczej." itp.) nie tłumaczone — nie wchodzą w
  domyślny render.
- Odkryty PRZY OKAZJI, nienaprawiony: `dev-render/screens/day220-audyty-rejestr.tsx`,
  `audyty-*`, `document-studio-*`, `calendar-sync-settings.tsx`,
  `roi-jedna-karta.tsx` i inne pliki z oryginalnej 20-plikowej listy
  instrukcji, które mają hardkodowane `language: 'pl'`/`'pl-PL'` w danych
  fixture — sprawdzone, że to DANE nie UI (patrz sekcja "Klasy przyczyn"
  punkt 1), ale te ekrany NIE były na liście 42 do naprawy w tym dyżurze i
  nie były badane pod kątem WŁASNYCH osobnych defektów i18n.
