# G06 — naprawa dostępności (axe), moduł 13_CHAT — 2026-09-03

Robotnik: agent naprawczy G06, gałąź `agent/fix-a11y-13-16-20260903`,
worktree `/private/tmp/ag-fix-a11y-13-16` (baza: `origin/demo` przez `/private/tmp/m03`).

## 1. Wynik: kadry z realnym naruszeniem (reguły poza `landmark-one-main`,
   `page-has-heading-one`, `region` — te trzy to szum hosta dev-render)

| Faza | Ekranów | Kadrów (theme×lang) | Kadrów z naruszeniem |
|---|---|---|---|
| PRZED (pl, 1440, light+dark) | 7 | 14 | 10/14 |
| PO (pl, 1440, light+dark) | 7 | 14 | **0/14** |
| PO (en, 1024, light+dark) | 7 | 14 | **0/14** |

## 2. Mapa: reguła → komponent → plik

| Reguła axe | Ekrany (PRZED) | Komponent | Plik | Naprawa |
|---|---|---|---|---|
| `aria-input-field-name` (4/7) | canvas-kebab-restructure, canvas-new-doc, canvas-toolbar-md-history, chat-split-teresa-right | Edytor Tiptap (`role="textbox"` + `contenteditable`) bez `aria-label`/`aria-labelledby`/`title` — wszystkie ekrany Canvas dzielą ten sam edytor | `src/components/AIChat/CanvasEditor/CanvasRichEditor.tsx` | `editorProps.attributes['aria-label']` w konfiguracji `useEditor` (nowe klucze i18n `canvas.editor.ariaLabel`, PL+EN) |
| `aria-prohibited-attr` (1/7) | chat-signals-feed | Kropka „Moje" — `<span aria-label="Moje">` bez roli; `aria-label` niedozwolony na elemencie generic | `src/components/AIChat/signalsFeed/ChatSignalsFeed.tsx` | Dodano `role="img"`, żeby `aria-label` był poprawny |
| `landmark-main-is-top-level` (1/7) | chat-split-teresa-right | `<main>` wewnątrz panelu dokumentu Canvas zagnieżdżony w `<aside aria-label="Canvas work area">` (landmark complementary) w `UnifiedChatPanel` — dwa poziomy landmarków, `<main>` nie może być zagnieżdżony | `src/components/AIChat/WorkCanvasDocumentPanel.tsx` | Zmieniono `<main>`→`<div>` (panel nie jest stroną najwyższego poziomu, tylko treścią w `<aside>`) |

Pozostałe ekrany (teresa-chipy-sugestii, teresa-confirm-chip) były już czyste w PRZED.

## 3. Rozstrzygnięcie hipotez z rejestru (pkt 2 instrukcji)

- **`chat-signals-feed` renderuje się poprawnie, bez crasha** — hipoteza rejestru
  o `feed.signals is not iterable` NIE potwierdziła się w realnym pomiarze. Sprawdzono
  kod: `dev-render/screens/chat-signals-feed.tsx` mockuje `api.get` tak, że zawsze
  zwraca `{ signals: [...], nextCursor, producerEnabled }` — nigdy `undefined` —
  niezależnie od parametru `stan`. Zero błędów konsoli, zero crasha w PRZED i PO.
- **`chat-split-teresa-right` NIE wykazał niedeterministycznego crasha „Invalid hook
  call"** w żadnym z pomiarów (PRZED, PO, ani w powtórzonej weryfikacji diagnostycznej)
  — renderował się poprawnie za każdym razem. Konsola pokazywała 29 komunikatów w
  PRZED (nie sprawdzano szczegółowo treści — status renderowania był „OK" za każdym
  razem, nie błąd krytyczny) — nie warunek blokujący pomiar.
- `color-contrast` na `teresa-chipy-sugestii` — **nie potwierdzone**: ekran miał
  0 naruszeń w PRZED (już czysty).
- Realne naruszenia (`aria-input-field-name`, `aria-prohibited-attr`,
  `landmark-main-is-top-level`) różnią się od podpowiedzi rejestru
  (`aria-input-field-name` się zgadza; `aria-prohibited-attr`+`color-contrast` na
  chat-signals-feed — zmierzono TYLKO `aria-prohibited-attr`, `color-contrast` nie
  potwierdzone na tym ekranie).

## 4. Komendy pomiaru (kanoniczne, `scripts/dev/grafika-zrzuty.mjs`)

```
EKRANY="canvas-kebab-restructure,canvas-new-doc,canvas-toolbar-md-history,chat-signals-feed,chat-split-teresa-right,teresa-chipy-sugestii,teresa-confirm-chip"

# PRZED
node scripts/dev/grafika-zrzuty.mjs --base=http://127.0.0.1:5332 --ekrany=$EKRANY \
  --katalog=13_CHAT-przed --faza=PRZED --jezyk=pl --szerokosc=1440 --motywy=light,dark \
  --rozwin-sekcje=1 --a11y=1 \
  --wyjscie=/private/tmp/ag-fix-a11y-13-16-artefakty/13_CHAT/przed-pl-1440 \
  --wynik-json=/private/tmp/ag-fix-a11y-13-16-artefakty/13_CHAT/przed-pl-1440/wynik.json

# PO (pl, 1440) / PO (en, 1024) — analogicznie z --katalog=13_CHAT-po[-en],
# --jezyk/--szerokosc odpowiednio zmienione
```

Diagnostyka lokalizacji naruszeń (selektor+html): jednorazowy pomocniczy skrypt
Playwright+axe uruchamiany z tego samego serwera dev, **nie jest częścią oficjalnego
pomiaru PRZED/PO** i nie wszedł do repo.

Surowe dane (poza repo): `/private/tmp/ag-fix-a11y-13-16-artefakty/13_CHAT/{przed-pl-1440,po-pl-1440,po-en-1024}/`

## 5. Co nie zostało naprawione i dlaczego

Wszystkie zmierzone realne naruszenia (poza trzema regułami szumu hosta) są zamknięte.

## 6. Pliki produktu zmienione

- `src/components/AIChat/CanvasEditor/CanvasRichEditor.tsx`
- `src/components/AIChat/signalsFeed/ChatSignalsFeed.tsx`
- `src/components/AIChat/WorkCanvasDocumentPanel.tsx`
- `public/locales/pl/translation.json`, `public/locales/en/translation.json`
  (nowy klucz `canvas.editor.ariaLabel`)
