# Dowód: pięć flag domyślnie ON (DEC 03.09 wieczór)

Zadanie: `agent/flagi-on-20260903`, gałąź bazowa `HEAD` z `/private/tmp/m03` (SHA startowe
`58ef0771d7`). Worktree: `/private/tmp/ag-flagi-on`. Vite dev-render: port 5430 (PID 66343,
proces sprzątnięty na końcu).

Decyzje źródłowe:
- A1–A4: `docs/program/DECYZJE_WLASCICIELA_DO_PODJECIA_20260904.md` (tabela u góry, wiersze A1–A4)
- R-11: `docs/program/DECYZJE_WLASCICIELA_P0P1_20260904.md` wiersz 39 (rodzina R-11, `MYW-NBK-CORE-001`)
- Audyt przewodów: `docs/program/waves/WAVE_03_ACCEPTANCE/AUDYT_PRZEWODOW_ODBIORU_20260903.md`

## Tabela zbiorcza

| Flaga | Plik:linia | PRZED | PO | Warstwy override (kolejność, najwyższa wygrywa) | Commit |
| --- | --- | --- | --- | --- | --- |
| A1 kpiRegistry/roiRegistry/okrRegistry/resultsSearch/attentionEntry | `src/components/ResultsVNext/resultsVNextFeatureFlags.ts:222-234` | kpiRegistry: ON poza public prod (od 27.08). roiRegistry/okrRegistry/resultsSearch/attentionEntry: `return false` zawsze | Wszystkie 5: ON poza public prod (`resultsVNextHostAllowsDefaultOn`/`isPublicProductionHost`) | query `?ff_resultsVNext<Flag>=0\|1` → localStorage `ff.results_vnext_*` → env `VITE_RESULTS_VNEXT_*_ENABLED` → owner-review-mode (zawsze ON) → demo-acceptance-profile (ON, poza `resultsLegacyArchive`) → default (nowe: `resultsVNextHostAllowsDefaultOn(hostname)`) | `67318266d1` |
| A2 financeCommentsV1 | `src/hooks/useFinanceCommentsFlag.ts:26` | `defaultValue: false` | `defaultValue: true` | localStorage (`allowLocalOverride`, klucz `consultify_feature_flags`) → remote `/feature-flags/runtime` (tylko zalogowani) → default | `1eeea06938` |
| A2 financeCompareV1 | `src/hooks/useFinanceCompareFlag.ts:29` | `defaultValue: false` | `defaultValue: true` | jw. | `1eeea06938` |
| A2 financeExportImportV1 | `src/hooks/useFinanceExportImportFlag.ts:26` | `defaultValue: false` | `defaultValue: true` | jw. | `1eeea06938` |
| A2 financeLineageNavigatorV1 | `src/hooks/useFinanceLineageNavigatorFlag.ts:28` | `defaultValue: false` | `defaultValue: true` | jw. | `1eeea06938` |
| A2 financeSavedViewsV1 | `src/hooks/useFinanceSavedViewsFlag.ts:24` | `defaultValue: false` | `defaultValue: true` | jw. | `1eeea06938` |
| A2 financeStatementPackWorkspaceV2 | `src/hooks/useFinanceStatementPackWorkspaceV2Flag.ts:33` | `defaultValue: false` | `defaultValue: true` | jw. + `isFinanceOwnerReviewModeEnabled()` (zawsze ON, OR-owany) | `1eeea06938` |
| A3 orgRedesignV1 | `src/utils/orgRedesignFlag.ts:59` (`readEnvFlag()`) | `return parsed === null ? false : parsed` (twardo OFF od 29.08 mimo dokumentacji "default ON od DEC-2026-08-26-78") | `return parsed === null ? true : parsed` | query `?ff_org_redesign_v1=0\|1` → localStorage `ff.orgRedesignV1` → env `VITE_ORG_REDESIGN_V1_ENABLED` → default (nowe: `true`) | `90780b4e52` |
| A4 interviewCreatorShellFlag (INT-1) | `src/utils/interviewCreatorShellFlag.ts:47` (`readEnvFlag()`) | `parseFlag(...) ?? false` | `parseFlag(...) ?? true` | query `?ff_interviewCreatorShell=0\|1` → localStorage `ff.interview_creator_shell` → env `VITE_INTERVIEW_CREATOR_SHELL` → default (nowe: `true`) | `ba0da208a3` |
| R-11 ENABLE_NOTEBOOK_SPEC_A_SHELL (MYW-NBK-CORE-001) | `src/components/MyWork/notebook/notebookSpecAShellFlag.ts:18` | `export const ENABLE_NOTEBOOK_SPEC_A_SHELL = false` | `= true` | query `?ff_notebookSpecAShell=0\|1` → localStorage `ff.ENABLE_NOTEBOOK_SPEC_A_SHELL` → env `VITE_ENABLE_NOTEBOOK_SPEC_A_SHELL` → default (nowe: `true`) | `b31f5495ce` |

Bezpiecznik zbiorczy: `tests/unit/flags/flagiDomyslnieOn.test.ts` (commit `133b51d3c6`, 21/21 PASS,
dowód mutacyjny wykonany ręcznie — cofnięcie `ENABLE_NOTEBOOK_SPEC_A_SHELL` na `false` czerwieni
dokładnie 2/21 testów tego pliku, reszta 19/21 bez zmian).

## Pułapka "flaga OFF w kodzie ≠ flaga wyłączona" — zmierzone warstwy, nie założone

Dla A1 (Wyniki) i A3/A4/R-11 (`readEnvFlag`/`ENABLE_*` wzorce) sprawdziłem KAŻDĄ warstwę osobno,
zgodnie z poleceniem — nie tylko `defaultValue`:

- **A1**: `isResultsVNextFlagEnabled()` ma DWA wczesne `return true` PRZED dotarciem do
  `defaultValue`-equivalentu: `isResultsOwnerReviewModeEnabled()` i
  `isDemoAcceptanceProfileEnabled()` (ten drugi ma jawny wyjątek dla `resultsLegacyArchive` —
  jedynej flagi w pliku, którą demo NIE włącza). Zmierzone: na hoście demo/staging obie te
  ścieżki i tak włączały 4 z 5 flag PRZED naszą zmianą (poza `resultsLegacyArchive`) — nasza
  zmiana dotyczy WYŁĄCZNIE zachowania POZA tym profilem (realna produkcja klienta spoza
  demo/staging, oraz każdy test jednostkowy, który nie ustawia profilu).
- **A3/A4/R-11**: żadna z tych trzech NIE ma odrębnej ścieżki `isXOwnerReviewModeEnabled()` ani
  profilu demo — jedyna warstwa nad `defaultValue` to query→localStorage→env, którą
  zweryfikowałem osobno dla każdej (patrz testy `awaryjny wyłącznik` w
  `flagiDomyslnieOn.test.ts`).
- **A2**: `useFeatureFlags()` (`src/hooks/useFeatureFlags.tsx`) NIE czyta `import.meta.env`
  bezpośrednio dla te flagi (zmierzone przez pełny odczyt pliku, linie 400-644) — jedyne warstwy
  to `localOverrides` (localStorage `consultify_feature_flags`, tylko gdy
  `enableLocalOverrides` i `allowLocalOverride`) i `remoteFlags` (fetch
  `/feature-flags/runtime`, tylko dla zalogowanego użytkownika — anonimowo zwraca `{}` i default
  jest ostateczny). Żadnego ukrytego `return true` przed `defaultValue`.

## Zakres A1: 5 nazwanych domen, NIE "wszystkie flagi pliku"

Plik `resultsVNextFeatureFlags.ts` ma 7 flag. Decyzja A1 nazywa dosłownie "KPI, OKR, ROI,
wyszukiwarka, uwaga" — 5 domen, zgodnie z audytem przewodów (`AUDYT_PRZEWODOW_ODBIORU_20260903.md`
wiersz 398: "14 z 19 ekranów"). `managementReportEntry` (E.1, nawigacyjny link do Management
Reports) i `resultsLegacyArchive` (archiwum, JAWNIE wyjęte spod profilu demo w kodzie z
komentarzem "właściciel nie widział jeszcze tej ścieżki") NIE są objęte tą decyzją i zostają
default OFF. Zaznaczone explicite w kodzie i w teście bezpiecznika.

## Dowód: ekrany harnessu (zrzuty PO, bez parametru wymuszającego flagę)

Katalog zrzutów: `evidence/grafika/flagi-on-20260903/` (ten worktree).

### A1 — `results-vnext-kpi-registry` (bez `&ff=...`)

`a1-results-vnext-kpi-registry-{light,dark}.png` — narzędzie:
`node scripts/dev/grafika-zrzuty.mjs --base=http://127.0.0.1:5430 --ekrany=results-vnext-kpi-registry
--jezyk=pl --motywy=light,dark --szerokosc=1440 --a11y=1`. Widać NOWY rejestr KPI vNext
(pigułki KPI/OKR/ROI, tabela z filtrami statusu, panel podglądu z akcjami Zawieś/Otwórz
pomiary/Archiwizuj) — to była już flaga ON przed tym dyżurem (27.08), więc to **kontrola
poprawności mechanizmu**, nie dowód nowej zmiany.

**Ograniczenie zmierzone, nie przemilczane**: harnessowe ekrany dla NOWO włączonych domen
(`results-vnext-roi-registry`, `results-vnext-okr-registry`, `results-vnext-search-registry`,
`results-vnext-attention`) WSZYSTKIE forsują flagę przez `window.localStorage.setItem(...)`
bezwarunkowo w ciele komponentu (np. `results-vnext-roi-registry.tsx:133`,
`flagOff ? '0' : '1'` — domyślnie `'1'` niezależnie od kodu produkcyjnego), z komentarzem w
pliku wprost tłumaczącym po co (uniknięcie "stale '1' z poprzedniej wizyty"). Nie da się przez
te konkretne ekrany zrobić zrzutu PRZED/PO domyślnej wartości — dokładnie przypadek z
instrukcji "harness wymusza flagę parametrem". Zmierzone przez `grep` kodu ekranu, nie
przypuszczone. Dowód dla tych 4 domen to REALNY test jednostkowy
(`isResultsVNextFlagEnabled('roiRegistry'|'okrRegistry'|'resultsSearch'|'attentionEntry')`
zwraca `true` z zerowymi override'ami — `tests/unit/flags/flagiDomyslnieOn.test.ts`, 21/21 PASS)
oraz mechanizm współdzielony z `kpiRegistry` (identyczna funkcja `isResultsVNextFlagEnabled`,
jedna gałąź kodu dla wszystkich pięciu — zmiana jest strukturalnie identyczna, nie per-domain
hack).

### A2 — `finance-hub` (bez parametrów)

`a2-finance-hub-{light,dark}.png` — Finance Hub listing renderuje się poprawnie (6 sprawozdań,
filtry statusu, panel podglądu). Ekran NIE pokazuje bezpośrednio `StatementPackWorkspaceV2`
(trzeba by otworzyć konkretne sprawozdanie w pełnym widoku, co wymaga dodatkowej nawigacji poza
zakres tego dyżuru) — służy jako **kontrola, że Finance Hub (realny wołacz flagi,
`FinanceHub.tsx:751`) nie posypał się po flipie**. Realny dowód działania flagi:
`StatementPackWorkspaceV2.flag.test.tsx` (2/2 PASS, test "ON (default, DEC 03.09 wieczór A2,
no override): mounts and calls listLines + getIdentity" — bez ŻADNEGO override'u, tylko domyślna
wartość z kodu).

**5 z 6 paneli A2 (Comments/Compare/ExportImport/LineageNavigator/SavedViews) NIE mają żadnego
wołacza produkcyjnego** — każdy plik flagi mówi to wprost we własnym komentarzu ("komponent
SAMODZIELNY", "brak montażu produkcyjnego"), potwierdzone `git grep` po nazwie komponentu: jedyne
odwołania poza plikiem flagi/testami to własny `dev-render/screens/finance-*-panel.tsx`. Flip
`defaultValue` dla tych pięciu nie zmienia NIC w realnym UI — zero widzów poza dev-render.
Zaznaczone też w treści commita `1eeea06938`.

### A3 — `org-identity-operating` BEZ `?ff_org_redesign_v1=1`

Ekran `org-identity-operating` ma w `scripts/dev/g06-macierz-ekrany.json` (`_parametry`) wpis
`"org-identity-operating": "ff_org_redesign_v1=1"` — narzędzie `grafika-zrzuty.mjs` DOKLEJA ten
parametr automatycznie przy zrzucie przez macierz, więc zrzut tym narzędziem NIE mierzy
domyślnej wartości. Zamiast tego nawigacja bezpośrednia przez przeglądarkę (bez parametru):
`http://127.0.0.1:5430/?screen=org-identity-operating&lang=pl&theme=light` (bez `&ff_org_redesign_v1`).
Zaobserwowany, żywy render (opisany, bo Browser-pane nie zapisuje pliku PNG do repo): breadcrumb
"Organizacja › Profil organizacji › Tożsamość i model działania", Menu 2 pigułki
(Tożsamość/Skala/Model dostawy/Rynki i systemy), Menu 3 chipy z licznikami (Wszystkie 16 /
Uzupełnione 11 / Do uzupełnienia 5 / Konflikty 2), karty treści z "pochodzeniem faktu"
(dokument/interview, znacznik "niezgodnych wartości ze źródeł") i sekcją zwijaną "Szczegóły
techniczne" — dokładnie kształt opisany w `orgRedesignFlag.ts` jako `ON` (11 skonsolidowanych
ekranów, jeden wspólny szkielet), NIE stary układ 21 pozycji. To jest bezpośredni dowód runtime
na domyślną wartość ON bez żadnego parametru wymuszającego.

### A4 — `interview-creator-shell`

**Ograniczenie zmierzone**: ten ekran harnessu ZAWSZE zapisuje flagę do `localStorage` w ciele
komponentu (`dev-render/screens/interview-creator-shell.tsx:58-61`,
`scene === 'off' ? 'false' : 'true'` — domyślnie `'true'` niezależnie od kodu produkcyjnego) i
żaden inny ekran dev-render nie montuje `InsightCreatorModal`/`InitiativeWizardModal` bez tego
samego wymuszenia (`git grep` potwierdza: tylko `interview-creator-shell.tsx` i
`unified-create-launcher.tsx`, a ten drugi nie montuje modala wprost). Nie da się przez dev-render
zrobić zrzutu PRZED/PO domyślnej wartości dla tego ekranu. Dowód: test jednostkowy
`isInterviewCreatorShellEnabled()` zwraca `true` z zerowymi override'ami
(`src/utils/__tests__/interviewCreatorShellFlag.test.ts`, 5/5 PASS) — realna funkcja, realna
ścieżka produkcyjna, oba wołacze (`InitiativeWizardModal.tsx:686`, `InsightCreatorModal.tsx:548`)
niezmienione.

### R-11 — `notatnik-centrum-mysli` / `mywork-notebook-rail-speca`

Zgodnie z instrukcją: `notatnik-centrum-mysli` to ekran-ilustracja (mock artefaktu "centrum
myśli"), NIE montuje `NotebookRightRail` wcale — nieprzydatny do tego dowodu (zmierzone
`grep`em, zero trafień na `NotebookRightRail`/`activeTab`/`open` w tym pliku).
`mywork-notebook-rail-speca` jest JEDYNYM ekranem montującym realny `NotebookRightRail`, ale
FORSUJE flagę wprost w ciele komponentu niezależnie od parametru URL
(`window.localStorage.setItem(...)`/`removeItem(...)` na podstawie propa `specA` domyślnie
`true`, zarejestrowanego w `main.tsx:1599` jako `<MyWorkNotebookRailSpecAScreen specA />` —
zawsze wymusza ON, druga rejestracja `mywork-notebook-rail-speca-stary` zawsze wymusza OFF).
Dokładnie przypadek z instrukcji. Dowód: test jednostkowy `isNotebookSpecAShellEnabled()` zwraca
`true` z zerowymi override'ami (`notebookSpecAShellFlag.test.ts`, 3/3 PASS) oraz
`NotebookRightRail.behavior.test.tsx` (13/13 PASS) — test "renders the SPEC-A accordion by
default (DEC 03.09 wieczór R-11, no override)" montuje `NotebookRightRail` BEZ żadnego override'u
i sprawdza klasy `border-l border-c-border-subtle` (marker SPEC-A) na realnym komponencie
produkcyjnym, nie na harnessie.

## Testy — podsumowanie per commit

| Commit | Plik(i) testów | Wynik |
| --- | --- | --- |
| `67318266d1` (A1) | `resultsVNextFeatureFlags.test.ts` | 14/14 PASS |
| `1eeea06938` (A2) | 5× `useFinance*Flag.test.ts` + `StatementPackWorkspaceV2.flag.test.tsx` + `FinanceHub.statementsPreview.canon.test.tsx` (regresja) | 20/20 + 2/2 + 10/10 PASS |
| `90780b4e52` (A3) | `orgRedesignFlag.test.ts` | 5/5 PASS (było 3/5 PASS + 2/5 RED przed zmianą kodu — test już oczekiwał ON, kod kłamał) |
| `ba0da208a3` (A4) | `interviewCreatorShellFlag.test.ts` (5/5 PASS) + `InsightCreatorModal.a11y.test.tsx` (8/12 PASS — 4 RED PRE-EXISTING, zmierzone `git stash` przed/po: te same 4 testy czerwone niezależnie od flipu domyślnej, bo już wcześniej wymuszały override='1' ręcznie) + `InitiativeWizardModal.a11y.test.tsx`/`.projectSelection.test.tsx` (8/8 PASS, regresja) | patrz wyżej |
| `b31f5495ce` (R-11) | `notebookSpecAShellFlag.test.ts` (3/3 PASS) + `NotebookRightRail.behavior.test.tsx` (13/13 PASS, było 12) | patrz wyżej |
| `133b51d3c6` (bezpiecznik) | `tests/unit/flags/flagiDomyslnieOn.test.ts` | 21/21 PASS, dowód mutacyjny wykonany |

## Czego NIE zrobiłem (jawnie)

1. **Nie naprawiłem** 4 pre-istniejących czerwonych testów w `InsightCreatorModal.a11y.test.tsx`
   (a11y kontraktu pola Tytuł w trybie creator-shell — brak `<label>`/`aria-label` na tym polu w
   `WizardModal`). Były czerwone PRZED tym dyżurem (zmierzone `git stash`), nie są skutkiem flipu
   domyślnej. Zleciłem osobne zadanie w tle (`task_b41d5071`) — to jest dokładnie "odbiór
   40-punktową listą po włączeniu", o którym mówi wiersz A4 źródłowej decyzji.
2. **Nie zrobiłem zrzutu** domyślnej wartości A4 (kreator wywiadu) i R-11 (Notatnik) przez
   dev-render — oba jedyne dostępne ekrany harnessu wymuszają flagę bezwarunkowo w kodzie
   komponentu (nie parametrem URL, którego dałoby się pominąć). Dowód zastępczy: testy
   jednostkowe na realnej funkcji rozstrzygającej, zero mocków flagi.
3. **Nie dotknąłem** `managementReportEntry` i `resultsLegacyArchive` (A1) — nie są objęte
   decyzją A1, zostają default OFF, sprawdzone testem.
4. **Nie push'owałem** — praca zostaje w worktree `/private/tmp/ag-flagi-on`, gałąź
   `agent/flagi-on-20260903`, 6 commitów.
5. **Nie uruchamiałem** pełnego `tsc`/`vitest` (zakaz) — tylko pliki testów flag, które
   zmieniałem, plus 3 pliki regresji (FinanceHub canon, InitiativeWizardModal ×2) dla pewności,
   że realny wołacz flagi się nie posypał.
