# i18n runda 3 — rodzina „doradca obciążenia” (dyżur 2026-09-03)

Wykonawca: `agent/i18n-r3-20260903`, worktree `/private/tmp/ag-i18n-r3`, port vite 5412.
Zlecenie: D9 z `docs/program/REJESTR_ZNALEZISK_20260903.md` — harness
`capacity-advisor-a3` miał hardkodowane napisy (nagłówki tabeli, podgląd,
formularz, stany LOADING/ERROR), mimo że PL≠EN w pomiarze binarnym (bo część
tekstu jest już tłumaczona).

## KROK 0 — rodzina

`git grep -ln -iE "capacity.?advisor" -- src dev-render` →
`dev-render/screens/capacity-advisor-a3.tsx` (montuje
`src/components/Initiatives/CapacityScenarioSurface.tsx`) + wzmianki w
`dev-render/main.tsx`/`odbior-grafika.html`.

`scripts/dev/g06-macierz-ekrany.json` → grupa `05_INITIATIVES`:
`capacity-advisor-a3, ev-football-field, exe-002-004-ui-audit,
initiative-record, karta-initiative, plan-scenario-d1`. Jedyny rzeczywisty
"rodzony brat" tej samej rodziny komponentów: `plan-scenario-d1` (montuje
`PlanScenarioSurface.tsx` — analogiczny do `CapacityScenarioSurface.tsx`,
oba są wywoływane obok siebie w `InitiativesHub.tsx`, dzielą wzorzec błędu:
`new Intl.DateTimeFormat('pl-PL', …)` przybite na sztywno). Pozostałe
ekrany grupy (`ev-football-field`, `exe-002-004-ui-audit`,
`initiative-record`, `karta-initiative`) montują inne, niepowiązane
komponenty — nie są rodzeństwem tej rodziny, nie badane w tym dyżurze.

## Metoda pomiaru — mianownik PRZED/PO

`node scripts/dev/grafika-zrzuty.mjs --base=http://127.0.0.1:5412
--ekrany=capacity-advisor-a3,plan-scenario-d1 --jezyk=pl|en --motywy=light
--szerokosc=1440` (klik domyślny w pierwszy wiersz tabeli → otwiera
wbudowany podgląd `StandardPreview`, NIE pełny „workspace" pod
`Otwórz narzędzia obciążenia" — patrz sekcja „Poza zakresem" niżej za
wyjaśnieniem tej granicy). Z pola `tekst` w wynik.json: linie identyczne
między `pl.json` i `en.json` = mianownik defektu, po odjęciu linii, które są
DANYMI (ID-y, daty, nazwy fixture'ów z harnessu takie jak „Cyfrowe
wdrożenie klienta" — CLAUDE.md, wyjątek `report-artifact` z rundy 1),
tytułami roli/stanowiska („Resource Manager", „Controls Engineer" —
potraktowane jak nazwy własne, tak jak w rundzie 1 zostawiono „COO"/„CFO")
i skrótami klawiszowymi jednoliterowymi.

## Wynik zbiorczy

| Ekran | Wspólne linie PL/EN PRZED | Wspólne linie PL/EN PO | Realne defekty naprawione |
|---|---|---|---|
| capacity-advisor-a3 | 48 | 13 | 35 (nagłówki, mapy etykiet, formularz, LOADING/ERROR, ai-hints, tooltip, empty state, rowMenu notes, saturacja) |
| plan-scenario-d1 | 25 | 15 | 1 klasa defektu (`formatDate` przybite na `pl-PL` — 10 linii dat w pomiarze PRZED, 0 w PO) |

13 linii pozostałych w PO dla `capacity-advisor-a3` i 15 dla
`plan-scenario-d1` to WYŁĄCZNIE dane/ID/tytuły ról/skróty klawiszowe —
wypisane w pełni niżej, zero błędów konsoli na żadnym z 4 zrzutów (PL/EN ×
2 ekrany).

### capacity-advisor-a3 — linie wspólne PO (13, wszystkie zakwalifikowane jako dane/nazwy własne)

`2026-W36` (periodId) · `engineering team` (fallback formatowania ID roli
nieobecnej w mapie ról) · `11/12/13 FTE-week` / `7/8/9 FTE-week` (dane
liczbowe + jednostka z fixture'u) · `-4` (dana liczbowa) · `Resource
Manager` ×3 (tytuł roli, jak w rundzie 1 „Controls Engineer"/„COO"/„CFO")
· `engineering-capacity` (constraintId) · `AI` (etykieta wspólnego bloku
`StandardPreview`, nie tego komponentu) · `Plan plan-a3:v4` (prefiks
„Plan" — to samo słowo w obu językach, nietłumaczony celowo, niska
szkodliwość) · `O` (skrót klawiszowy).

### plan-scenario-d1 — linie wspólne PO (15, wszystkie dane)

5× nazwa inicjatywy z fixture'u harnessu (polskie nazwy, analogicznie do
wyjątku `report-artifact` z rundy 1) · 6× `—` (placeholder pustej wartości,
identyczny z założenia) · `AI`, `init-onboarding`, `O` (jak wyżej).

## Co naprawiono (plik → klasa defektu)

### `src/components/Initiatives/CapacityScenarioSurface.tsx`

- 5 map etykiet (`knowledgeLabel`, `confidenceLabel`, `scenarioStateLabel`,
  `rowKindLabel`, `criticalityLabel`) — były `Record<K,string>` ze STAŁĄ
  polską wartością (pokazywały się identycznie w EN); zamienione na
  funkcje wołające `i18n.t()` (moduł poza komponentem — używa globalnej
  instancji `i18n` z `@/i18n`, nie hooka). `scenarioStateLabel` re-używa
  istniejących kluczy `initiatives.planScenario.status.*` (ten sam tekst,
  jeden punkt prawdy zamiast duplikatu).
- 10 nagłówków kolumn tabeli (`Okres / ograniczenie`, `Rola / zespół`,
  `Rodzaj`, `Potrzeby (zakres)`, `Zasoby (zakres)`, `Luka`,
  `Presja (zakres)`, `Pewność`, `Waga`, `Opiekun`) → `t()`.
- LOADING (`Loading Capacity scenarios` — było na sztywno PO ANGIELSKU,
  nie tłumaczyło się na PL) i ERROR (`Capacity register unavailable…` +
  przycisk `Retry`, też sztywny angielski) → `t()` z tłumaczeniem PL.
- Formularz: etykieta `Aktywny wariant obciążenia` (sztywny PL, nie
  tłumaczyła się na EN) → `t()`.
- Podgląd (`StandardPreview`): `details.label`, `details.text`
  (fallback), 4× `properties.label` (Zapotrzebowanie/Dostępność/
  Luka/Właściciel), `ai.hints` (`Challenge assumptions`/`Compare options`
  — sztywny angielski, nie tłumaczył się na PL), `ai.disabledTooltip` →
  `t()`.
- `rowMenu.universalHandlers.archiveNote` i `rowMenu.destructive.note`
  (oba sztywny angielski) → `t()`.
- `empty.title`/`empty.description` tabeli (sztywny PL, nie tłumaczył się
  na EN — nie widoczne w tym pomiarze bo tabela ma dane, naprawione przy
  okazji, koszt marginalny) → `t()`.
- Saturacja (`renderKnowledgeToken` + `constraintRows` useMemo): dwa
  skomponowane napisy z hardkodowanym polskim słowem/sufiksem wewnątrz
  template stringa (`(bazowo X%)`, `UNKNOWN — nie dotyczy ograniczenia`)
  oraz trzeci nienazwany w D9 (`UNKNOWN — brak pełnego zakresu`) →
  `i18n.t()` inline; `constraintRows` useMemo dostał `i18n.language` do
  tablicy zależności (bez tego wartość zostałaby stale po przełączeniu
  języka bez przeładowania strony — teoretyczna luka, bo harness i tak
  przeładowuje stronę przy zmianie języka, ale poprawka jest jednowierszowa
  i zamyka klasę błędu na przyszłość).
- `formatPeriodDate` — `Intl.DateTimeFormat('pl-PL', …)` przybite na
  sztywno (ten sam kształt co defekt `public-booking-widget` z rundy 1) →
  przełącza na `en-US` gdy `i18n.language !== 'pl'`. Nie widoczne w tym
  pomiarze wprost (kolumna `freshness` jest domyślnie ukryta), naprawione
  bo dotyka tej samej funkcji co widoczny defekt w `plan-scenario-d1`.
- Import `i18n from '@/i18n'` dodany (wzorzec z
  `InitiativeSourceLink.tsx`/`InitiativeDocumentView.tsx`).
- **Świadomie NIE naprawiono** (potraktowane jako nazwy własne/tytuły
  ról, nie generyczny angielski słownik — zgodnie z metodą rundy 1):
  `'resource-manager': 'Resource Manager'`, `'controls-engineer':
  'Controls Engineer'` w mapie `actorLabel`. Ryzyko błędnej klasyfikacji:
  jeśli Piotr uzna to za defekt, to 2 dodatkowe klucze, kilka minut pracy.
- **Poza zakresem — DŁUG UDOKUMENTOWANY, nie naprawiony**: panel
  „workspace" pod `Otwórz narzędzia obciążenia" (double-click, sekcja
  `workspaceOpen` w kodzie, linie ok. 1130–1440) — `CapacityOptionsPanel`,
  edytor zobowiązania zasobowego (`commitmentEditorOpen`), `RangeView`,
  `OptionImpact`, przyciski Zapisz szkic/Opublikuj/Zamknij, komunikaty
  `commitmentWrite` (5 stringów). Szacunek: **~65-75 dodatkowych
  hardkodowanych napisów** (głównie polski niezmieniający się na EN, kilka
  angielskich niezmieniających się na PL — te same dwie klasy błędu co
  naprawione wyżej). NIE jest to część pomiaru `grafika-zrzuty.mjs`
  domyślnym klikiem (który otwiera tylko embedded preview, nie pełny
  workspace) — poza „mianownikiem PRZED" tego dyżuru. Wymaga osobnego
  dyżuru z własnym pomiarem (klik + otwarcie workspace w skrypcie albo
  ręczny scenariusz).
- Blok danych demo pod `if (demoMode)` (linie ok. 398–470, `'Atelier
  Capacity Baseline'`, `'Transformation Office'`, `'Data engineering skill
  gap'` itd.) — **nieosiągalny w tym harnessie** (`capacity-advisor-a3.tsx`
  woła `demoMode={false}`), nie dotyczy pomiaru, nie ruszony.

### `src/components/Initiatives/PlanScenarioSurface.tsx`

- `formatDate` — identyczny defekt jak wyżej (`Intl.DateTimeFormat`
  przybite na `'pl-PL'`, 10 dat widocznych w pomiarze PRZED renderowało się
  identycznie PL/EN: `01 wrz 2026` zamiast `Sep 01, 2026`) → przełącza na
  `en-US` po `i18n.language`. Import `i18n from '@/i18n'` dodany.
- **Nie audytowano** reszty pliku (82 KB, ~2200 linii) — poza jednym
  zmierzonym defektem żaden inny hardkodowany napis nie wypłynął w
  pomiarze binarnym tego dyżuru (reszta UI już przechodzi przez `t()` z
  wcześniejszej rundy, widać to po tabeli PRZED: tylko daty i nazwy
  inicjatyw z fixture'u były wspólne). To NIE jest twierdzenie o 100%
  pokryciu i18n tego pliku — tylko że binarny gate PL≠EN dla
  `plan-scenario-d1` jest teraz czysty.

### `public/locales/pl/translation.json` i `public/locales/en/translation.json`

Nowy blok kluczy pod `initiatives.capacityAdvisor.*`:
`loading, unavailable, retry, activeScenario, knowledge.{known,estimated,
unknown,unconfirmed}, confidence.{high,medium,low}, rowKind.{period,
constraint}, criticality.{assessed,toAssess}, columns.{titlePeriod,
roleTeam,kind,demandRange,supplyRange,gap,pressureRange,confidence,weight,
owner,affectedInitiatives,freshness,proposedResponse}, saturationBaseline,
saturationNotApplicable, saturationIncompleteRange, details.{label,text},
properties.{demand,supply,owner}, ai.{hintChallenge,hintCompare,
disabledTooltip}, emptyTitle, emptyDescription, archiveNote,
destructiveNote, resourceOwnerFallback` (44 nowe klucze/liście w każdym
pliku). `scenarioStateLabel` re-użył istniejących
`initiatives.planScenario.status.*` (0 nowych kluczy tam).

Sprawdzone PRZED wstawieniem: `grep -c '"capacityAdvisor": {'` = 1 w obu
plikach zarówno przed jak i po edycji (bezpiecznik z rundy 1 — duplikat
JSON-owy cicho gubi klucze).

## Liczby liści `translation.json`

| Plik | PRZED | PO | Δ |
|---|---|---|---|
| `public/locales/pl/translation.json` | 34245 | 34289 | +44 |
| `public/locales/en/translation.json` | 32256 | 32300 | +44 |

Liczone `python3` rekurencyjnym zliczeniem liści (nie-dict wartości) —
żadna gałąź nie zmalała.

## R3 — rozszerzenie na rodzeństwo (05_INITIATIVES/06_EXECUTION)

Jedynym realnym rodzeństwem znalezionym w KROK 0 jest `plan-scenario-d1`
(już zmierzony i naprawiony wyżej — 1 klasa defektu, koszt DROBNY: jedna
funkcja, jedna linia zmieniona). Nie znaleziono w tym dyżurze innych par
komponentów w `05_INITIATIVES`/`06_EXECUTION` z tym samym wzorcem („dwa
podobne komponenty montowane obok siebie, jeden ma i18n, drugi nie") —
pozostałe ekrany grupy `05_INITIATIVES`
(`ev-football-field, exe-002-004-ui-audit, initiative-record,
karta-initiative`) montują niepowiązane komponenty i były już przedmiotem
osobnych dyżurów (np. `ev-football-field` naprawiony w rundzie 1 —
`evidence/grafika/i18n-pl-en-20260903.md`). `06_EXECUTION` (8 ekranów:
`exec-summary-onelook, execution-report-day11, execution-tab-control,
execution-tab-list, execution-tab-resources, execution-tab-rollout,
execution-tab-summary, execution-tab-work`) nie sprawdzone w tym dyżurze —
3 z nich (`execution-tab-control`, `execution-tab-resources`,
`execution-tab-work`) były już odnotowane jako PL≠EN w rundzie 1 bez
dalszej naprawy; `exec-summary-onelook` naprawiony w rundzie 1. Pozostałe
(`execution-report-day11`, `execution-tab-list`, `execution-tab-rollout`,
`execution-tab-summary`) nigdy nie zmierzone tą metodą — poza zakresem
tego dyżuru (nie są rodzeństwem komponentu doradcy obciążenia, wymagałyby
osobnego KROK 0).

## esbuild / testy

- `npx esbuild src/components/Initiatives/CapacityScenarioSurface.tsx
  --loader:.tsx=tsx --outfile=/dev/null` → OK (56.4kb, 0 błędów)
- `npx esbuild src/components/Initiatives/PlanScenarioSurface.tsx
  --loader:.tsx=tsx --outfile=/dev/null` → OK (75.5kb, 0 błędów)
- `npx esbuild src/components/Initiatives/InitiativesHub.tsx
  --loader:.tsx=tsx --outfile=/dev/null` → OK (88.3kb, 0 błędów; sprawdza
  że oba zmienione pliki nadal importują się poprawnie z hosta)
- Brak istniejących plików testowych dla `CapacityScenarioSurface.tsx`/
  `PlanScenarioSurface.tsx` (`find` po nazwie — pusty wynik), brak
  odwołań do zmienionych napisów w `src/components/Initiatives/__tests__/`
  (`grep -rl` — pusty wynik) → nic do przepuszczenia przez vitest.
- Zero błędów konsoli na 4 zrzutach (PL/EN × capacity-advisor-a3/
  plan-scenario-d1), potwierdzone przez `grafika-zrzuty.mjs`.
- Weryfikacja wzrokiem: zrzuty PO (PL i EN, `capacity-advisor-a3`)
  porównane obok siebie — układ tabeli/podglądu identyczny w obu
  językach (znane przycinanie kolumn tekstem „…" to WCZEŚNIEJSZY,
  udokumentowany w kodzie kompromis 13-kolumnowej tabeli — komentarz
  „97-czternascie-kolumn" — nie regresja tego dyżuru, występuje
  identycznie w obu wersjach PRZED).

## Czego NIE zrobiono i dlaczego

1. Panel „workspace" `CapacityScenarioSurface` (double-click) — ~65-75
   napisów, poza mianownikiem pomiaru domyślnego kliku, wymaga osobnego
   dyżuru z rozszerzonym scenariuszem pomiaru.
2. Blok danych `demoMode` w `CapacityScenarioSurface.tsx` — nieosiągalny w
   tym harnessie (`demoMode={false}`), nie dotyczy.
3. Pełny audyt i18n `PlanScenarioSurface.tsx` (2200 linii) — naprawiono
   wyłącznie zmierzony defekt (`formatDate`), reszta pliku nieaudytowana
   linia po linii (choć pomiar binarny wychodzi czysty).
4. Role/tytuły `Resource Manager`/`Controls Engineer` — pozostawione jako
   nazwy własne (spójne z decyzją rundy 1 dla `COO`/`CFO`), nie
   przetłumaczone; jeśli to błędna klasyfikacja, wymaga 2 kluczy.
5. `06_EXECUTION` (8 ekranów) i pozostałe ekrany `05_INITIATIVES` — nie
   zmierzone w tym dyżurze (nie są rodzeństwem doradcy obciążenia — patrz
   sekcja R3 wyżej), 4 z nich mają już status z rundy 1 (1 naprawiony, 3
   odnotowane PL≠EN bez naprawy).

## Commity (worktree `/private/tmp/ag-i18n-r3`, gałąź `agent/i18n-r3-20260903`)

Patrz `git log --oneline` na tej gałęzi — commit per plik zgodnie z
regułą pracy.
