# Dependencies
node_modules
server/node_modules

# Build artifacts
dist
server/dist
server/dist-temp
**/*.tsbuildinfo
coverage
playwright-report
test-results

# Environment files — NEVER bake secrets into the image.
# Runtime env vars are injected by Railway. Keep `*.example` for docs.
.env
.env.*
!.env.example
!.env.*.example
!.env.*.template
server/.env
server/.env.*
!server/.env.example
!server/.env.*.example

# Database (for Docker builds)
*.db
*.db-journal

# Logs
*.log
logs

# OS files
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.swp
*.swo

# Git
.git
.gitignore

# Blog content (not needed in container)
Blogs

# Documentation (not needed in container)
docs
evidence
!README.md
knowledge/**/*.pdf
knowledge/**/*.xlsx
knowledge/**/*.pptx
knowledge/**/*.docx
knowledge/**/*.zip

# Tests (not needed in production)
tests
**/*.test.ts
**/*.test.tsx
**/*.test.js
**/*.spec.ts
playwright.config.ts
vitest.config.ts

# Local-only directories (never in container)
Softs
App_for_ideas
Consulitinity przegląd
Logo consultinity
Piotr_Tools
Plast-met
_quarantine
_backup
_archived_backups
_analysis
.cursor
.claude
.codex-worktrees
.tmp
screenshots
exports
