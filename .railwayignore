# Keep deploy uploads small (Railway CLI has an effective ~45MB upload ceiling).
# Only exclude folders that are not required to build/run the app in Railway.

# Local data / personal tooling
/Piotr_Tools/
/data/
/_quarantine/
/secure_backups/
/_backup/
/.tmp/
/.claude/
/.codex-worktrees/
/_analysis/

# Blog content (heavy assets, not needed for API)
/Blogs/
Blogs 2.zip
/Logo consultinity/

# Large knowledge/doc corpora (keep uploads small)
# NOTE: We DO deploy the markdown knowledge packs needed for AI re-indexing:
# - knowledge/tool-kb/**
# - knowledge/Pigułki wiedzy */**
#
# Railway ignore does not reliably support negation patterns, so we explicitly
# exclude the heavyweight knowledge subfolders and keep only the packs.
/knowledge/ADMA/
/knowledge/DRD/
/knowledge/Finanse/
/knowledge/KPI/
/knowledge/Prezentacje */
/knowledge/SIRI/
/knowledge/vector-website/
/knowledge/**/*.pdf
/knowledge/**/*.xlsx
/knowledge/**/*.pptx
/knowledge/**/*.docx
/knowledge/**/*.zip
/docs/
/wdrozenia/
/Plast-met/
/Consulitinity przegląd/
/App_for_ideas/
/Softs/

# Tests & artifacts (not required at runtime)
/tests/
/playwright-report/
/test-results/
/coverage/
/screenshots/
/evidence/
/exports/
/odpowiedzi/

# Local uploads / exports (not required for build)
/uploads/
server/exports/
server/logs/
server/backups/
server/*.db
server/*.db-journal
server/*.db-wal
server/*.db-shm
server/*.sqlite
server/*.sqlite-*

# Build outputs
/dist/
/dist-ssr/
server/dist/

# Heavy directories excluded from upload (Railway builds from source)
node_modules/
server/node_modules/
.git/
.codex-worktrees/
/dist/
/server/dist/

# IDE / agent artifacts
.cursor/
agent-transcripts/

# Root-level local debug artifacts
/debug-*.png
/debug-*.txt
/final-*.txt
/test-*.html
/test-*.png
/test-*.txt
/test-*.md
/test-*.mjs
/test-rec-map-*.txt
/validation-output*.txt
