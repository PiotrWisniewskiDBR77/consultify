# Keep deploy uploads small (Railway CLI has an effective ~45MB upload ceiling).
# Only exclude folders that are not required to build/run the app in Railway.

# Local data / personal tooling
/Piotr_Tools/
/data/
/_quarantine/
/.drive-sync-backup/
/secure_backups/
/_backup/
/_local_production/
/.tmp/
/.claude/
/.codex-worktrees/
/_analysis/
/assets/
/test-drafts/

# Blog content (heavy assets, not needed for API)
/Blogs/
*.zip
/Logo consultinity/

# Large knowledge/doc corpora — exclude entirely from uploads.
# Use leading slash to anchor to repo root so src/views/knowledge/ is NOT excluded.
# knowledge/ is gitignored so git-triggered builds also have no knowledge files.
# The Dockerfile knowledge test is now non-fatal (logs a warning, does not exit 1).
/knowledge/
/Harvard/
/knowledge/Pigułki wiedzy /**/*.zip
/docs/
/wdrozenia/
/Plast-met/
/Consulitinity przegląd/
/App_for_ideas/
/Softs/

# Tests & artifacts (not required at runtime)
# Use no-leading-slash so Railway's ignore engine reliably matches
tests/
/playwright-report/
/test-results*/
/test-screenshots/
/coverage/
/screenshots/
/evidence/
/exports/
/odpowiedzi/
/public/videos/
/public/videos/**
/public/screenshots/
/public/screenshots/**
/public/demo/
/public/demo/**

# NIE dodawać tu /public/images/ ani /public/assets/ — to NIE są artefakty
# testowe, tylko żywe assety frontendu (hero landingu, grafiki partnerskie,
# obrazki landing/mode_ai_*). Wykluczone 2026-06-13 → `railway up` nie wysyłał
# ich do build contextu → `vite build` nie miał czego skopiować do dist/ →
# consultify.ai zwracał ASSET_NOT_FOUND na /images/app-hero-chat.png
# (pusty hero) i /assets/landing/*. Weryfikacja po deployu:
#   curl -o /dev/null -w '%{http_code}\n' https://consultify.ai/images/app-hero-chat.png

# SQLite test artifacts (can be large, never needed for Railway builds)
**/*.test.db
**/*.test.db-journal
**/*.test.db-shm
**/*.test.db-wal

# Local uploads / exports (not required for build)
uploads/
/uploads/
/server/exports/
/server/exports/**
/server/logs/
/server/logs/**
/server/uploads/
/server/uploads/**
/server/backups/
/server/backups/**
/server/migrations-archive/
/server/migrations-archive/**
/server/*.db
/server/*.db-journal
/server/*.db-wal
/server/*.db-shm
/server/*.sqlite
/server/*.sqlite-*
/server/consultinity.db
/server/coverage/
/server/coverage/**
/server/test-results/
/server/test-results/**
/server/tests/
/server/tests/**
/server/seed/
/server/seed/**
/server/seeds/
/server/seeds/**
/server/migrations-v2/
/server/migrations-v2/**
/server/src/_backup/
/server/src/_backup/**

# Build outputs
/dist/
/dist-ssr/
server/dist/
.nx/
.vite/

# Heavy directories excluded from upload (Railway builds from source)
node_modules/
server/node_modules/
.git/
.codex-worktrees/
/dist/
/server/dist/

# IDE / agent artifacts
.cursor/
agent-transcripts/

# Root-level local debug artifacts
/debug-*.png
/debug-*.txt
/final-*.txt
/test-*.html
/test-*.png
/test-*.txt
/test-*.md
/test-*.mjs
/test-rec-map-*.txt
/validation-output*.txt
/junit.xml
/e2e-results.xml
/reports/
/reports/**

# Local env files — NEVER upload secrets to Railway build context.
# Railway env vars are injected at runtime via `railway variables`.
# We list concrete filenames (no negation) because Railway's ignore engine
# historically does not reliably support `!` negation patterns.
/.env
/.env.local
/.env.production
/.env.staging.local
/.env.staging.secrets
/server/.env
/server/.env.local
/server/.env.production
/server/.env.staging.local
/server/.env.staging.secrets

# TS incremental build caches (force fresh compile)
**/*.tsbuildinfo
server/dist-temp/
.tmp/
.tmp/**

# VTS credentials (never ship)
vts-wave2-credentials*.csv
