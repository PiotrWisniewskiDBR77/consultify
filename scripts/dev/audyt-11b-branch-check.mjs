import { chromium } from 'playwright';
const AUTH = '/private/tmp/stanowisko-noc/auth-11b.json';
const BASE = 'http://127.0.0.1:3117';
const OUT_DIR = '/private/tmp/wt-11b/evidence/1-1-b';
const browser = await chromium.launch();
const ctx = await browser.newContext({ storageState: AUTH, viewport: { width: 1440, height: 900 } });
const page = await ctx.newPage();
await page.goto(`${BASE}/chat?workPanel=1`, { waitUntil: 'networkidle', timeout: 30000 });
await page.waitForTimeout(2000);
const composer = page.getByPlaceholder(/Zapytaj Teres/i);
await composer.click();
await composer.fill('test8');
await page.locator('[data-testid="chat-send-btn"]').first().click();
await page.waitForTimeout(15000);
const label = await page.locator('button[aria-label^="Current branch"], button[aria-label^="Bieżąca gałąź"]').first().getAttribute('aria-label');
console.log('branch aria-label now:', label);
await page.locator('button[aria-label^="Bieżąca gałąź"], button[aria-label^="Current branch"]').first().click();
await page.waitForTimeout(500);
await page.screenshot({ path: `${OUT_DIR}/26-branch-dropdown.png` });
await browser.close();
