# check-focus-canon.sh — BASELINE dlugu fokusa crimson (ratchet PER PLIK, K-41).
# Format: <liczba>\t<sciezka>. Liczba = wystapienia VIOLATION_RE
# (ring-primary-*/outline-primary-*/ring-offset-primary-*) w CALYM pliku.
#
# Bramka (--ci) przepuszcza plik dopoki jego liczba NIE ROSNIE wzgledem
# WLASNEGO wiersza (nie sumy repo — patrz K-41 w naglowku check-focus-canon.sh).
# Nowy plik z naruszeniem (brak wiersza = baseline 0) = FAIL. Regeneruj
# (--update-baseline) TYLKO gdy dlug SWIADOMIE SPADL — nigdy zeby uciszyc
# nowa regresje.
#
# WYGENEROWANO: 2026-09-03 (scripts/check-focus-canon.sh --update-baseline)
# RAZEM: 0 wystapien w 0 plikach
#
# Posortowane po sciezce (stabilny diff). Dlug maleje = wiersze znikaja / liczby spadaja.
