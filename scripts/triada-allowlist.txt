# triada-allowlist.txt — wyjątki od check-triada.sh (VF0-2, 2026-07-19).
# Przeniesiony razem ze scripts/check-triada.sh z .claude/hooks/ (2026-07-19,
# .claude/ jest gitignored — hook+allowlist żyły tylko lokalnie per-sesja).
# Jeden wzorzec ścieżki na linię (dopasowanie shellowym globem do ścieżki pliku,
# np. */src/components/brand/Logo.tsx albo */src/components/brand/*).
# Linie zaczynające się od # oraz puste są ignorowane.
# Na start PUSTA — żadnych plików nie znaleziono, które legalnie potrzebują
# c-accent/primary-100/200/300 poza kanonem (brand/logo). Dopisz tu ścieżkę
# TYLKO ze świadomą decyzją (np. świadomy hardkod loga marki), nie żeby ominąć hook.

# MW-08 canonical reconciliation (2026-08-03):
# src/components/MyWork/NotebookContent.tsx — PRE-EXISTING crimson
# (bg-c-accent-soft/text-c-accent) on "New page"/"Load more"/template card/
# AI-suggestion hover states — already present on the canonical integration
# base (1421ae29dc) before any MW-08 work touched this file, confirmed via
# `git show 1421ae29dc:...NotebookContent.tsx | grep -c c-accent` = 9.
# check-triada's hit here is a diff-based false positive: the mandatory
# `eslint --fix` formatting pass (the file had ~1350 mechanical prettier
# errors after the mobile-UX cherry-pick) reflowed indentation around these
# pre-existing lines, so git's diff shows them as "changed" even though the
# token content is byte-identical (proved separately: esbuild --minify on the
# pre- and post-fix file produces the same sha256). Not a drive-by fix —
# removing this pre-existing crimson is out of scope for MW-08 reconciliation
# and would be its own task.
src/components/MyWork/NotebookContent.tsx
