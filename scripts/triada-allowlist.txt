# triada-allowlist.txt — wyjątki od check-triada.sh (VF0-2, 2026-07-19).
# Przeniesiony razem ze scripts/check-triada.sh z .claude/hooks/ (2026-07-19,
# .claude/ jest gitignored — hook+allowlist żyły tylko lokalnie per-sesja).
# Jeden wzorzec ścieżki na linię (dopasowanie shellowym globem do ścieżki pliku,
# np. */src/components/brand/Logo.tsx albo */src/components/brand/*).
# Linie zaczynające się od # oraz puste są ignorowane.
# Na start PUSTA — żadnych plików nie znaleziono, które legalnie potrzebują
# c-accent/primary-100/200/300 poza kanonem (brand/logo). Dopisz tu ścieżkę
# TYLKO ze świadomą decyzją (np. świadomy hardkod loga marki), nie żeby ominąć hook.
# Asercja NEGATYWNA pilnująca braku crimson focus-ring (strażnik, nie użycie) — nadzorca 2026-08-26
*/src/components/Organization/redesign/__tests__/OrganizationKnowledgeGraphScreen.test.tsx
src/components/Organization/redesign/__tests__/OrganizationKnowledgeGraphScreen.test.tsx
# Asercja NEGATYWNA pilnująca braku crimson w chipie filtra (1.1-Z4 #2, 06.09.2026)
*/src/components/Conclusions/__tests__/ConclusionsHub.filterChipCanon.test.tsx
src/components/Conclusions/__tests__/ConclusionsHub.filterChipCanon.test.tsx
# W73 K6: właściciel jawnie pozostawił kolor postępu kreatora c-accent bez zmian.
src/components/Onboarding/EnterpriseOnboardingWizard.tsx
# K-20c (DEC-575): ratchet NEGATYWNY pilnujący braku crimson `bg-brand` na
# zaznaczonym checkboxie powiadomień — musi nazwać zakazany token, żeby go
# pilnować (ten sam wzorzec co dwa wyjątki wyżej).
*/src/components/settings/__tests__/UstawieniaPstryczki.k20.test.ts
src/components/settings/__tests__/UstawieniaPstryczki.k20.test.ts
# D-87 (DEC-646): ratchet NEGATYWNY pilnujący, że pierścień ładowania listy
# dokumentów niesie token neutralny `text-c-text-muted`, a NIE crimson
# `text-primary-500` — musi nazwać zakazany token, żeby go pilnować (ten sam
# wzorzec co wyjątki wyżej).
*/src/components/documents/__tests__/DocumentSidePanel.spinnerToken.test.tsx
src/components/documents/__tests__/DocumentSidePanel.spinnerToken.test.tsx
# D-01 (DLUG-PO-MVP, DEC-607): ratchet NEGATYWNY pilnujący, że CTA „Get started"
# w modalu powitalnym onboardingu to neutralny navy `primary`, NIE crimson
# `bg-crimson-600` (#85182F) — musi nazwać zakazany token, żeby go pilnować
# (ten sam wzorzec co wyjątki wyżej).
*/src/components/Onboarding/__tests__/FirstRunOnboarding.test.tsx
src/components/Onboarding/__tests__/FirstRunOnboarding.test.tsx
