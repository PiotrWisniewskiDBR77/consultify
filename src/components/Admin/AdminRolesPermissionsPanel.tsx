import{Shield,Trash2}from'lucide-react';import React,{useEffect,useMemo,useState}from'react';import{createSecurityRole,deleteSecurityRole,getSecurityRoles,type SecurityRole,updateSecurityRole}from'../../services/adminRolesApi';import{ConfirmDialog}from'../MyWork/shared/ConfirmDialog';import{StandardTable,type TableColumn,type TableRow}from'../standard/StandardTable';export const AdminRolesPermissionsPanel:React.FC=()=>{const[data,setData]=useState<SecurityRole[]>([]),[name,setName]=useState(''),[permissions,setPermissions]=useState(''),[edit,setEdit]=useState<SecurityRole|null>(null),[del,setDel]=useState<SecurityRole|null>(null),[ownerOnly,setOwnerOnly]=useState(false),[error,setError]=useState<string|null>(null);const load=()=>getSecurityRoles().then(setData).catch((e:any)=>{if(e?.code==='PROJECT_ROLES_MANAGE_REQUIRED'||String(e?.message).includes('PROJECT_ROLES_MANAGE_REQUIRED'))setOwnerOnly(true);else setError(e.message)});useEffect(()=>{void load()},[]);const save=async()=>{const p=permissions.split(',').map(x=>x.trim()).filter(Boolean);try{setData(edit?await updateSecurityRole(edit.id,name,p):await createSecurityRole(name,p));setName('');setPermissions('');setEdit(null)}catch(e){setError(e instanceof Error?e.message:'Błąd zapisu')}};const cols=useMemo<TableColumn[]>(()=>[{id:'name',label:'Rola'},{id:'permissions',label:'Uprawnienia'},{id:'updated',label:'Aktualizacja'}],[]),rows=useMemo<TableRow[]>(()=>data.map(r=>({id:r.id,name:r.name,permissions:(r.permissions||[]).join(', ')||'Brak',updated:r.updated_at?new Date(r.updated_at).toLocaleString():'—'})),[data]);if(ownerOnly)return <div role="alert" className="rounded-xl border border-c-border p-5">Zarządzanie rolami projektowymi wymaga uprawnienia właściciela organizacji.</div>;return <div className="space-y-4"><h2 className="text-lg font-semibold text-c-text">Role i uprawnienia</h2>{error&&<div role="alert">{error}</div>}<section className="flex flex-wrap gap-2 rounded-xl border border-c-border p-4"><input aria-label="Nazwa roli" value={name} onChange={e=>setName(e.target.value)} className="rounded border border-c-border bg-c-surface p-2" placeholder="Nazwa roli"/><input aria-label="Uprawnienia" value={permissions} onChange={e=>setPermissions(e.target.value)} className="min-w-64 rounded border border-c-border bg-c-surface p-2" placeholder="permissions, po przecinku"/><button disabled={!name.trim()} onClick={()=>void save()} className="rounded bg-c-accent px-3 py-2 text-white">{edit?'Zapisz rolę':'Dodaj rolę'}</button></section><StandardTable columns={cols} data={rows} rowMenu={row=>({primary:[{id:'edit',label:'Edytuj',onClick:()=>{const r=data.find(x=>x.id===row.id);if(r){setEdit(r);setName(r.name);setPermissions((r.permissions||[]).join(', '))}}}],destructive:{label:'Usuń',icon:Trash2,onClick:()=>setDel(data.find(x=>x.id===row.id)||null)}})} empty={{icon:Shield,title:'Brak ról niestandardowych',description:'W tej organizacji nie zdefiniowano ról projektowych.'}} persistKey="admin.roles"/><ConfirmDialog isOpen={!!del} onCancel={()=>setDel(null)} onConfirm={()=>{if(del)void deleteSecurityRole(del.id).then(setData).finally(()=>setDel(null))}} title="Usunąć rolę?" description="Przypisania tej roli przestaną obowiązywać. Odzyskanie wymaga ponownego utworzenia roli." confirmLabel="Usuń rolę" variant="danger"/></div>};
