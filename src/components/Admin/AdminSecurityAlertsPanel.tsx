import{ShieldAlert}from'lucide-react';import React,{useCallback,useEffect,useMemo,useState}from'react';import{getSecurityAlerts,resolveSecurityAlert,type SecurityAlert}from'../../services/adminSecurityAlertsApi';import{StandardTable,type TableColumn,type TableRow}from'../standard/StandardTable';
export const AdminSecurityAlertsPanel:React.FC=()=>{const[data,setData]=useState<SecurityAlert[]>([]),[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{try{setData(await getSecurityAlerts());setError(null)}catch(e){setError(e instanceof Error?e.message:'Błąd alertów')}},[]);useEffect(()=>{void load()},[load]);const cols=useMemo<TableColumn[]>(()=>[{id:'type',label:'Typ'},{id:'severity',label:'Dotkliwość'},{id:'user',label:'Użytkownik'},{id:'ip',label:'IP'},{id:'time',label:'Czas'},{id:'status',label:'Status'}],[]);const rows=useMemo<TableRow[]>(()=>data.map(a=>({id:a.id,type:a.event_type,severity:a.severity,user:a.user_email||'—',ip:a.ip_address||'—',time:new Date(a.created_at).toLocaleString(),status:a.resolved?'Rozwiązany':'Otwarty'})),[data]);return <div className="space-y-4"><h2 className="text-lg font-semibold text-c-text">Alerty bezpieczeństwa</h2>{error&&<div role="alert">{error}</div>}<div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-c-border p-4">Nierozwiązane: {data.filter(a=>!a.resolved).length}</div><div className="rounded-xl border border-c-border p-4">Wysokiego ryzyka: {data.filter(a=>['critical','high'].includes(a.severity)).length}</div></div><StandardTable columns={cols} data={rows} rowMenu={row=>{const a=data.find(x=>x.id===row.id);return a&&!a.resolved?{primary:[{label:'Oznacz jako rozwiązane',icon:ShieldAlert,onClick:()=>void resolveSecurityAlert(a.id).then(setData).catch(e=>setError(e.message))}]}:undefined}} empty={{icon:ShieldAlert,title:'Brak alertów',description:'Nie wykryto zdarzeń bezpieczeństwa.'}} persistKey="admin.securityAlerts"/></div>};
