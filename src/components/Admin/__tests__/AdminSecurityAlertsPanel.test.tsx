import{fireEvent,render,screen}from'@testing-library/react';import React from'react';import{describe,expect,it,vi}from'vitest';import{getSecurityAlerts,resolveSecurityAlert}from'../../../services/adminSecurityAlertsApi';import{AdminSecurityAlertsPanel}from'../AdminSecurityAlertsPanel';vi.mock('../../../services/adminSecurityAlertsApi',()=>({getSecurityAlerts:vi.fn(),resolveSecurityAlert:vi.fn()}));describe('AdminSecurityAlertsPanel',()=>{it('renders and resolves with server readback',async()=>{vi.mocked(getSecurityAlerts).mockResolvedValue([{id:'a1',event_type:'login_failed',severity:'critical',resolved:0,created_at:'2026-08-24T10:00:00Z'}]);vi.mocked(resolveSecurityAlert).mockResolvedValue([]);render(<AdminSecurityAlertsPanel/>);expect(await screen.findByText('login_failed')).toBeInTheDocument();const buttons=screen.getAllByRole('button');fireEvent.click(buttons[buttons.length-1]);fireEvent.click(await screen.findByText('Oznacz jako rozwiązane'));expect(resolveSecurityAlert).toHaveBeenCalledWith('a1')});});
