import{fireEvent,render,screen}from'@testing-library/react';import React from'react';import{describe,expect,it,vi}from'vitest';import{createBreakGlass,getBreakGlass,revokeBreakGlass}from'../../../services/adminBreakGlassApi';import{AdminBreakGlassPanel}from'../AdminBreakGlassPanel';vi.mock('../../../services/adminBreakGlassApi',()=>({getBreakGlass:vi.fn(),createBreakGlass:vi.fn(),revokeBreakGlass:vi.fn()}));describe('AdminBreakGlassPanel',()=>{it('requires typed target, reason and approver before confirmation',async()=>{vi.mocked(getBreakGlass).mockResolvedValue({sessions:[],policy:{breakGlassEnabled:true,breakGlassApprovers:['u2']},approvers:[{id:'u2',email:'approver@example.com'}]});vi.mocked(createBreakGlass).mockResolvedValue({sessions:[],policy:{breakGlassEnabled:true,breakGlassApprovers:['u2']},approvers:[]});render(<AdminBreakGlassPanel/>);const activate=await screen.findByRole('button',{name:'Aktywuj na 1h'});expect(activate).toBeDisabled();fireEvent.change(screen.getByLabelText('Powód break-glass'),{target:{value:'Emergency access needed'}});fireEvent.change(screen.getByLabelText('Zatwierdzający'),{target:{value:'u2'}});fireEvent.change(screen.getByLabelText('Potwierdzenie celu'),{target:{value:'BREAK-GLASS'}});expect(activate).toBeEnabled();fireEvent.click(activate);expect(await screen.findByText(/Zakres: awaryjne uprawnienia/)).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Aktywuj break-glass'}));expect(createBreakGlass).toHaveBeenCalledWith('Emergency access needed','u2')});});
