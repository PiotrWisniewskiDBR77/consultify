import{render,screen}from'@testing-library/react';import React from'react';import{describe,expect,it,vi}from'vitest';import{getLegalHold}from'../../../services/adminLegalHoldApi';import{AdminLegalHoldPanel}from'../AdminLegalHoldPanel';vi.mock('../../../services/adminLegalHoldApi',()=>({getLegalHold:vi.fn()}));describe('AdminLegalHoldPanel',()=>{it('shows factual state and honest missing registry',async()=>{vi.mocked(getLegalHold).mockResolvedValue({legalHoldEnabled:true,blockedOperations:['data_export','organization_deletion'],matterRegistryAvailable:false});render(<AdminLegalHoldPanel/>);expect(await screen.findByText('Wstrzymanie aktywne')).toBeInTheDocument();expect(screen.getByText(/Rejestr spraw nie jest jeszcze prowadzony/)).toBeInTheDocument();expect(screen.queryByRole('button')).not.toBeInTheDocument()})});
