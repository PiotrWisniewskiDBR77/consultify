/**
 * appearance/KeyboardShortcutsSettings
 * Re-exports the canonical KeyboardShortcutsSettings to avoid duplicate implementations.
 * Named export only - no default re-export to avoid bundler issues
 */
export { KeyboardShortcutsSettings } from '../KeyboardShortcutsSettings';
