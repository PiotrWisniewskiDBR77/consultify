import{apiDelete,apiGet}from'./api/baseClient';export interface AdminGuest{user_id:string;email:string;first_name?:string;last_name?:string;granted_at:string;status:string;scope_type:string;project_id?:string|null;expires_at?:string|null}export async function getAdminGuests(){return(await apiGet<{guests:AdminGuest[]}>('/admin/guests')).guests??[]}export async function revokeAdminGuest(id:string){return(await apiDelete<{guests:AdminGuest[]}>(`/admin/guests/${encodeURIComponent(id)}`)).guests??[]}
