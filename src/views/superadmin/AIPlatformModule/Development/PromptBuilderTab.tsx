/**
 * PromptBuilderTab - Development > Prompt Builder
 * Wrapper for AIIntelligenceView (Block Builder + Test Bench)
 */

import React from 'react';

import { AIIntelligenceView } from '../../AIIntelligenceView';

export const PromptBuilderTab: React.FC = () => {
  return <AIIntelligenceView />;
};

export default PromptBuilderTab;
