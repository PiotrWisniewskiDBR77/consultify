# Consulitinity Przeglad

This tree is preserved as tracked audit evidence and review history.

## Important

- It is **not** a product or UI source of truth.
- Use `docs/` for canonical documentation.
- Use `docs/cleanup/PARALLEL_DOC_TREES_CLASSIFICATION.md` for the current authority model.

## Working Interpretation

- keep audit outputs for traceability
- do not derive new canonical product rules from this tree alone
- when an audit conclusion becomes durable doctrine, restate it in `docs/`
