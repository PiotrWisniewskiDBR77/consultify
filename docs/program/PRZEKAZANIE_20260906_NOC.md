# PRZEKAZANIE — 06.09.2026 noc (sesja Fable #24 → #25)

Właściciel ~21:50: „zostało 8 % kontekstu, przerzućmy to do następnego agenta; całą nocną zmianę robi nowy agent”.
Właściciel ŚPI (po 17 h pracy). Rano chce JEDNO zbiorcze przejście odbiorcze. Ten plik = jedyny punkt wejścia.

## 0. Pierwsze 5 minut
1. Przeczytaj do końca; potem rejestr `docs/program/PROGRAM_NAPRAWCZY_20260905/01_INDEKS_I_HARMONOGRAM.md`
   (nowe wiersze WSTAWIA SIĘ NAD kotwicą `| 1.5 P8 (wklejka 2) |`; czas TYLKO z `date '+%H:%M'`; wiersze pisz skryptem
   Pythona z nagłówkiem `# -*- coding: utf-8 -*-` uruchamianym z `PYTHONIOENCODING=utf-8` — heredoc z polskimi znakami
   i `$(...)` w treści bywał odrzucany; w f-stringach escapuj `{{ }}`).
2. Arkusz właściciela: `docs/program/plan-pojemniki/stan.json` → `node scripts/dev/plan-pojemniki/generuj.mjs` →
   kopia `PANEL.html` do scratchpadu jako `trzy-pojemniki.html` → Artifact z `url=https://claude.ai/code/artifact/2a86e4bf-46b5-4056-a472-264dc4a26da6`
   (ZAWSZE ten url; werdykty modułów tylko `brak|tak|nie`; przed pierwszą publikacją narzędzie każe przeczytać żywą wersję — `action: read` z tym url).
3. Katalog pracy `/private/tmp/m03` (gałąź `codex/m03-admin-20260824`), bare `/Users/piotrwisniewski/Developer/consultify-recovery-vault-20260820.git`.
   Stanowisko: API 4100, vite 3090 (zrestartowane 21:36 na HEAD; po każdym scaleniu serwera: `bash scripts/dev/stanowisko-lokalne/stop.sh; bash scripts/dev/stanowisko-lokalne/start.sh`),
   PG 54400 (org DBR77, statusy inicjatyw JUŻ na słowniku 7), sesja `/private/tmp/stanowisko-noc/auth.json` (odświeżona 19:50; odświeżenie: `node scripts/dev/stanowisko-lokalne/zaloguj-api.mjs`).
4. Dysk: 89 GB wolne (o 20:1x było 2 GB — `df -h /private/tmp` przed każdym `worktree add`; NIE ruszaj `~/Developer/FizzUp*` — aplikacja właściciela).
5. Pamięć: `przekazanie-sesja-fable-25.md` (ZACZNIJ TU), `zasady-pracy-nadzorcy`, `zasada-minimum-fable`, `evil-merge-po-hooku`, `dwie-sesje-nadzorcze-na-jednym-m03`, `zadania-do-akceptacji-zrozumiale`.

## 1. Stan na ~21:55
- **Staging:** health `62bc593e55` (paczka 13b). **Paczka 14 (`d5704fb67e`, push 21:37) — deployment `ac7da0dd-8c67-4b09-acb7-fd3463a4e525` FAILED** (run 34055453200). Build backendu przeszedł (etapy api 20/25 w logu), więc padło na **pre-deploy (bramka migracji `release-migration-gate`: pierwsza migracja danych P12 `20262103_p12_initiative_status_slownik` na bazie właściciela) albo na healthchecku**. PIERWSZE ZADANIE NOCY: `railway logs --build -e staging -s consultify -n 600 ac7da0dd-8c67-4b09-acb7-fd3463a4e525 | grep -iv 'Config as Code'` (koniec logu) i `railway logs --deployment -e staging -s consultify -n 200 <id>`; podejrzenia: (a) CHECK `initiatives_status_check` na stagingu ma inną definicję niż lokalnie (P12 §3.12: dwie migracje pod tą samą nazwą — `UPPER(status)` vs literalna lista), (b) wiersze o statusie spoza mapowania, (c) `ie_aggregate_state` na stagingu ≠ 0 (lokalnie 0 — P12 kazał policzyć osobno). Staging nadal serwuje 13b — właściciel nic nie traci. Nie naprawiaj migracji „na ślepo”: dry-run `migrate.postgres.ts --dry-run` przeciw `DATABASE_PUBLIC_URL` stagingu (odczyt) i pomiar `SELECT status, count(*) FROM initiatives` tam.
- **Właściciel odebrał (Tak):** Czat, Moja Praca, Organizacja, Panel admin, Ustawienia, Wyniki, Materiały, Partnerzy, Kokpit Realizacji (karta 5), Audyty-Wnioski (karta 6). Zostaje: Realizacja jako moduł + galeria kart N.
- **Dziś scalone (w paczce 14):** R4b, P11 r2, P10 r2, P12 + P12-int + P12-int-b, Z5 (cast flag w 124 plikach, DEC-431), kontrakty kart N (SSOT `docs/ssot/KARTA_N_KONTRAKT.md` K1–K30 + partie B0–B8 w `P10/*.md`, inwentarz 72 typów / 140 egz. `P10/INWENTARZ_KART_N_PELNY.md`), plan Realizacji `1_12_REALIZACJA_PLAN.md`.
- **Decyzje właściciela dziś:** DEC-397b…DEC-443 (rejestr, każda z cytatem). Do wykonania: DEC-432 kontrakt = jedyne źródło sekcji; DEC-433 „Pracuj z AI” wszędzie (WYJĄTEK: Notatnik zostaje ze swoimi 5 AI); DEC-434 Teresa tylko Menu 1 (także osadzone czaty Document Studio/Exceli); DEC-435/436 Wyniki; DEC-437 dane pokazowe (odchylenie, karta działania, spotkanie); DEC-438 Ocena (import PDF ZOSTAJE w MVP, prezentacja osobna karta, CMMI/Lean „Planowane”); DEC-439 Narzędzia (katalog serwerowy treści; NIE odmrażać drugiego narzędzia); DEC-440 Finanse (sprawozdanie + analiza do rejestru; stary model wygaszony); DEC-441 runtime docelowo; DEC-442 warsztat pomysłu; DEC-443 wzorzec wywiadu z powłoką; 4 ekrany bez tożsamości ZOSTAJĄ w kanonie; plan agenta zamrożony; puste sekcje ukrywane. E-mail Iriny do pilotażu: wiersz rejestru 21:3x.

## 2. W TOKU (agenty poprzedniej sesji — ich meldunki NIE dotrą do Ciebie; odbieraj po commitach w gałęziach)
| worktree | gałąź | co | jak odebrać |
|---|---|---|---|
| `/private/tmp/wt-sceptyk-odbioru-0609` | `mvp/sceptyk-odbioru-0609` | Opus: werdykt DONE/PARTIAL/NOT dla ~45 wytycznych właściciela na HEAD, `SCEPTYK_ODBIORU_20260906.md` + zrzuty | commit `docs(program): sceptyk odbioru…` → merge --no-ff (docs), worktree remove; §4 = lista do pokazania rano, §5 = czego NIE mówić „gotowe” |
| `/private/tmp/wt-p12intc-zapisy-slownik7` | `mvp/p12intc-zapisy-slownik7` | Sonnet: ~20 literałów starego słownika w ścieżkach ZAPISU `initiativeTransitionService.ts`, i18n statusDescription, reguła on_hold | merge ze znacznikami DEC-424, potem pełny tsc serwera = 0 PRZED pushem |
| Codex P13-A/B/C | `codex/p13a-karty-n`, `codex/p13b-karty-n`, `codex/p13c-karty-n` (worktree `/private/tmp/codex-p13{a,b,c}-…`) | wklejki 10/11/12 wydane właścicielowi ~21:45 (Faza B kart N: A = Moja Praca/Wywiad/Inicjatywy, B = Ocena/Audyty/Narzędzia, C = Wyniki/Materiały/Finanse/Realizacja); znacznik `29992c920b` jest na github-backup | rano: `P13/{A,B,C}_98_RAPORT.md` → merge --no-ff ze znacznikami → tsc serwera → paczka 15 |
Sprawdzanie: `git -C <bare> log --oneline codex/m03-admin-20260824..<gałąź>` i `git -C <worktree> status --short` (drzewo czyste + raport = skończone). Worktree'y Codexa P10/P11/P12 (`/private/tmp/codex-p1{0,1,2}-…`) są scalone i czyste — można usunąć (`git worktree remove --force`), jeśli właściciel nie ma w nich otwartych sesji.

## 3. Kolejka nocy (w tej kolejności)
1. Paczka 14: diagnoza FAILED (§1) → naprawa → push jako paczka 14b (procedura §4 pkt 5–6) → wiersz w rejestrze + stan.json/panel.
2. Odbiór sceptyka i P12-int-c → w paczce 14b lub 15.
3. **R3 (Opus, L):** kamienie milowe jako obiekt (data planowana zamrożona / aktualna / fakt), baseline i re-baseline PRZEZ DECYZJĘ z zatwierdzającym (od drugiego przesunięcia), kolumna „Odchylenie (dni)” z baseline; plan `1_12_REALIZACJA_PLAN.md` §C4 R3; nie dotykaj plików sesji Codexa P13.
4. **Dane pokazowe (Sonnet, DEC-437):** 1 odchylenie KPI → karta działania, 1 spotkanie — przez kontrakt produktu na 54400, wpis w `scripts/dev/stanowisko-lokalne/README.md` §Dane (robotnik NIE pisze w `/private/tmp/stanowisko-noc`).
5. **Z6 (Opus):** `20260412_seed_business_templates.sql` pending na stanowisku wywraca zimny start serwera (CHECK `v8_artifact_origin_links_origin_runtime_check` vs 44 wierszy document_template; tp_migration_history 731/740; na stagingu 656 wpisów TP, plik NIE zaaplikowany) — migracja korygująca addytywna (rozszerzyć CHECK), nie edycja zastanej; co to znaczy dla świeżej bazy demo.
6. **Dane stagingu (Sonnet, read-only + propozycja):** 4× powtórzone zadania w Pracy (są tylko na stagingu; SQL po `DATABASE_PUBLIC_URL` z `railway variables --environment staging --service consultify --json`, połączenie BEZ SSL), właściciele RAID spoza organizacji (16/16), angielskie tytuły decyzji/RAID (dane). Kasowanie TYLKO skryptem z dry-run + CSV + manifest (wzorzec `server/scripts/usun-rekordy-aco.ts`, org DBR77 `a3e05d4a-5397-419d-b486-8e44366c0063`).
7. Rano: odbiór P13-A/B/C, paczka 15, galeria kart N (jeden obraz na kartę z rozwiniętym „Pracuj z AI”), zbiorcza karta odbioru dla właściciela (tylko DONE ze sceptyka), arkusz, ten plik → nowa wersja.
Poza zakresem nocy: 1.9 re-audyt, 1.10 demo (po 1.9), S1.11 zamrożenie tagami, S1.12 przekazanie pojemnik 2, Fala 2.

## 4. Procedura odbioru (obowiązkowa)
1. Zrzut z `evidence/` robotnika obejrzeć (Read); `.png.json`: `url` ≠ `/login`, `bledyKonsoli` = 0. Zrzut z harnessu dev-render ≠ produkt.
2. `git -C /private/tmp/m03 merge --no-ff <gałąź> -m "… [ODMROZENIE <MODUL> DEC-<n>] …"` — nazwy TYLKO z `docs/program/MVP_FINAL_ZAMROZONE.json` (+ `00_SHARED`, `16_GLOBAL_STANDARDS` gdy hook zażąda). Merge stanął bez `UU` = hook → tylko ponowny `git commit -m` z pełnymi znacznikami; NIGDY „sprzątanie” drzewa.
3. Drzewo: BASE = merge-base sha-m03-sprzed-gałęzi i tipu; `git diff HEAD <tip> -- <pliki gałęzi bez translation.json/dev-render>` = 0; pliki usuwane: `git ls-files` = 0.
4. `git -C <bare> worktree remove --force <worktree>`; wiersz w rejestrze; stan.json gdy zmienia stan pozycji.
5. Przed KAŻDYM pushem: `git ls-remote origin staging` + `gh run list --workflow=railway-deploy.yml --limit 1` (nic nie buduje?), `cd server && NODE_OPTIONS=--max-old-space-size=3072 ../node_modules/.bin/tsc -p tsconfig.build.json --noEmit` = 0 (Docker robi to samo; esbuild robotników nie sprawdza typów — dziś 2 wywrotki), łańcuch migracji na 54400 (`DB_TYPE=postgres NODE_ENV=test RUN_DB_TESTS=1 DOTENV_DISABLED=1 DATABASE_URL=postgres://postgres:noc@127.0.0.1:54400/consultify_noc npx tsx server/scripts/migrate.postgres.ts`), nazwy migracji datowane ≥ `20262105` (`migrationOrdering.ts` sortuje po YYYYMMDD).
6. Push: `git push origin HEAD:staging` + `gh workflow run railway-deploy.yml --ref staging -f environment=staging`; dowód = health gitSha; sonda w tle (pętla curl co 15 s, przerwanie gdy run = failure). Po scaleniu Codexa: `git push github-backup codex/m03-admin-20260824:codex/m03-admin-20260824` (Codex sprawdza znacznik na github-backup).

## 5. Zlecanie robotników (skrót briefu wspólnego)
Worktree: `git -C <bare> worktree add -b mvp/<id> /private/tmp/wt-<id> codex/m03-admin-20260824; printf '[core]\n\tbare = false\n' > <bare>/worktrees/wt-<id>/config.worktree; ln -s /private/tmp/m03/node_modules /private/tmp/wt-<id>/node_modules; git -C /private/tmp/wt-<id> config core.hooksPath .husky` (~1 min; >3 naraz → `run_in_background`).
Zakazy w każdym zleceniu: sparse-checkout, git stash, worktree remove/prune, --no-verify, pkill, push, flagi env, rm -rf poza worktree, dotykanie m03/stanowisko-noc (poza odczytem auth.json/server.env), pełne tsc/vitest (WYJĄTEK: robotnik zmieniający SERWER musi na końcu uruchomić pełny tsc serwera), migracje na 54400 (własna baza testowa), staging/demo/prod, rekordy testowe bez sprzątania, sub-agenci. Pliki innych robotników wypisz jako zakazane. Zrzuty: własny vite 3101–3199 z `VITE_DOTENV_DISABLED=1 VITE_API_TARGET=http://127.0.0.1:4100`; serwer 4100 serwuje kod m03 z chwili restartu — robotnik zmieniający serwer podnosi własny 41xx (`start.sh` jako wzór; zimny start na 54400 wymaga `DB_MANAGED_SCHEMA=off`). Dowód DEC-400: testy zastane przed/po per plik, nowe 0, mutacja celująca w zabezpieczenie → RED, zrzut 1440 light z realnej trasy, commit-per-krok ze znacznikiem. Meldunek: POMIAR → PRZYCZYNA → NAPRAWA → DOWÓD → SHA + ZNALEZISKA + STOP-y. Zlecenie ma kazać ZMIERZYĆ premisę (rejestr starzeje się w godziny — dziś 2 STOP-y Z1).

## 6. Komunikacja z właścicielem (rano)
Krótko, po polsku; jeden obraz + Tak/Nie + jedno zdanie; decyzje w trybie pytanie–odpowiedź z rekomendacją jako pierwszą opcją (AskUserQuestion po 4); nie pokazuj niczego, czego sam nie obejrzałeś; wklejki dla Codexa w bloku kodu (właściciel = listonosz); uczciwość ponad optymizm (SPROSTOWANIE w rejestrze, gdy wpis okazał się fałszywy — dziś 1×: paczka 13 „na żywo”, a build padł).
