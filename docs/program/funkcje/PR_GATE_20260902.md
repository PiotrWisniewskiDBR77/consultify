# PR Gate — doprowadzenie `lint-typecheck` do zieleni (02.09.2026)

Zadanie: naprawić zadanie `🔍 Lint & Type Check` (job `lint-typecheck` w
`.github/workflows/test-suite.yml`) bramki `✅ PR Gate`, bez masowego
`eslint --fix`. Poprzedni wykonawca padł na limicie API po pierwszym
pomiarze — ta sesja zaczęła od zera.

## SHA i gałąź

- Baza: `github-backup/kandydat/staging-20260902b` @ `56913a0b3b`
  (najświeższa linia kandydata na stagingu; **nie** `staging-20260902`
  bez „b" — ta jest 18 commitów za tyłem).
- Gałąź robocza: `fix/pr-gate-typy-lint-20260902`, wypchnięta na
  `github-backup`.
- Tip po naprawie: **`30589e9f2f`** (21 commitów, per plik/regułę).
- Worktree: `/private/tmp/prgate-0902` (padł na pułapkę „bare" opisaną w
  zleceniu — naprawiona przez `config.worktree`).

## Rozbieżność wobec liczb podanych w zleceniu

Pomiar sprzed tej sesji dotyczył innej (starszej) bazy. Na `56913a0b3b`
zmierzyłem sam:

| Pomiar | Podane w zleceniu | Zmierzone na 56913a0b3b | Różnica |
|---|---|---|---|
| `type-check` (front) | 26 błędów / 10 plików | **27 błędów / 11 plików** | +1 błąd, +1 plik (`DocumentStudioTemplateArchitectView.formattingSchema.test.tsx`, `category:'management'` nie w `TemplateCategory`) |
| `type-check:server` | 0 błędów | **1 błąd** (`deckImageSafetyGates.ts`, `tesseract.recognize` niewidoczny przez wildcard `declare module '*.js'`) | +1 |
| `lint` — nienaprawialne automatem | 14 (w tym 3× `react-hooks/rules-of-hooks`, wzmianka o 18× `prefer-const`) | **14, ale inny skład**: `react-hooks/rules-of-hooks` to **4** wystąpienia, nie 3 (dodatkowo `PartnerProgramConfig.tsx:281`). `prefer-const` (18×) jest w rzeczywistości **automatycznie naprawialne** — nie należy do puli ręcznej. | skład różny, liczba total ta sama przez zbieg okoliczności |

Przyczyna: baza kandydata przesunęła się o 18 commitów między pomiarem
zlecenia a startem tej sesji (typowy dryf — patrz `Środowisko testowe
kłamie w obie strony` w pamięci nadzorcy).

## Co naprawiono

### TypeScript — 28 błędów (27 front + 1 server), 13 plików, PRAWIDŁOWO (bez `any`/`@ts-ignore`)

| Plik | Błąd | Naprawa (jedno zdanie) |
|---|---|---|
| `src/store/slices/authSlice.ts` | TS2741 — brak `chatSuggestionsEnabled` w resecie `aiConfig` | Dodano pole, wartość zgodna z domyślną w `chatSlice.ts` (`true`, D-104). |
| `src/views/partner/sections/EarningsSection.tsx` | TS2367 — porównanie z `'certified'`, którego `lifecyclePhase` nigdy nie przyjmuje | Usunięto martwą gałąź (zweryfikowane po stronie serwera: `PartnerLifecyclePhase` nie ma `'certified'`). |
| `src/components/shared/NModeLayout/AIConsultantPanel.tsx` | TS2345 — `t(key, 'string default')` niezgodne z lokalnym `TFunc` | Wywołanie zmienione na formę `{ defaultValue: 'AI Consultant' }` (próbowałem najpierw poszerzyć `TFunc` — zepsuło to przypisanie prawdziwego `TFunction` z i18next; cofnięte). |
| `server/src/types/custom.d.ts` | TS2339 (w `deckImageSafetyGates.ts`) — `tesseract.recognize` niewidoczny | Dodano precyzyjną deklarację `declare module 'tesseract.js'` (TS wybiera dokładne dopasowanie przed wildcardem `'*.js'`). **Uwaga**: pakiet `tesseract.js` nie jest zainstalowany (brak w `package.json`/`node_modules`) — to osobny problem runtime, zgłoszony jako zadanie w tle (nie naprawiony tutaj, bo to wykracza poza naprawę typów). |
| `src/components/MyWork/IdeaMapWorkspace.tsx` | TS2741 — brak `onClose` w `IdeaAISuggestionsPanel` (embedded) | Dodano `onClose={() => handlePanelChange(null)}`, tak jak każdy sąsiedni panel w tym bloku. |
| `src/components/DocumentStudio/__tests__/...formattingSchema.test.tsx` | TS2322 — `category:'management'` spoza `TemplateCategory` | Zmienione na `'report'` (zgodne z `documentType: 'steering_committee_report'`). Mock w `dev-render`, na który się powołuje komentarz testu, ma tę samą wadę — zgłoszony osobno. |
| `src/components/Initiatives/__tests__/InitiativesHub.smoke.test.tsx` | TS2493×2 + TS2352 — `vi.fn(async () => …)` bez parametrów dawało `calls[0]` typu `[]` | Dodano typy parametrów `(url: string, init?: RequestInit)`, wzorem `DeckBuilder.restoreNoWrite.test.tsx`. |
| `src/components/Initiatives/sections/CommentsSection.tsx` | TS2322×3 — handlery zwracały `void`, kontrakt wymaga `Promise<MutationResult>` | `onAddComment` owinięty (`handleAddComment` context bez zmian — używany też bezpośrednio gdzie indziej); `onDeleteComment`/`onLikeComment` (lokalne, best-effort) teraz zwracają `{ok:true}`. |
| `src/components/Initiatives/sections/LinkedItemsSection.tsx` | TS2322×2 — jw. | `onAdd`/`onRemove` zwracają `MutationResult`; **usunięto własne toasty** (dublowałyby toasty `SharedLinkedItemsSection`, ten sam wzorzec co `TaskDetailView.handleAddLinkedItem`). |
| `src/components/assessment/AssessmentHub.tsx` | TS2345×2 — `unknownPresentation<T>` bez inferencji dawało `PresentationState<unknown>` | Jawne `unknownPresentation<number>(...)`. |
| `src/components/shared/NModeSections/AttachmentsLinksCanvas.tsx` | (wsparcie dla TaskDetailView) | Prop typy poszerzone na `Promise<void \| MutationResult>` — `DecisionDetailView` faktycznie zwraca `void`, `TaskDetailView` `MutationResult`; komponent i tak nigdy nie czyta wyniku. |
| `src/components/MyWork/TaskDetailView.tsx` | TS2345 (taskId null) + TS2322×4 (Attachments/LinkedItems) + TS2322 (onGenerateAIComment) | Strażnik `if(!taskId)` przed `generateAIComment`; reszta rozwiązana przez powyższe dwie naprawy + lokalny wrapper na `generateAIComment` (zostaje `void`-owy, bo jest też `onAIEnhance` gdzie indziej). |
| `src/components/MyWork/DecisionDetailView.tsx` | TS2322 (category) + TS7006×4 (implicit any) + TS2322 (onGenerateAIComment) | `mapDecisionServerRisk.category` zwalidowana przeciw realnej unii (fallback `'business'`, nie ślepy cast); `(a: Alternative)`/`(r: RiskItem)` jawnie; ten sam wrapper co TaskDetailView. |

### Lint — 14 błędów nienaprawialnych automatem, 8 commitów per reguła

| Reguła | Plik(i) | Naprawa |
|---|---|---|
| `react-hooks/rules-of-hooks` (4×) | `ExecutionWorkSurface.tsx` — **realny błąd**; `AgentProcessTemplatesPanel.tsx`, `PartnerProgramConfig.tsx`×2 — fałszywe alarmy | `ExecutionWorkSurface.tsx:617`: `useEffect` stał PO wczesnym `if (state==='ERROR') return` — efekt (rejestracja filtra Menu 2) nie odpalał się na renderze błędu, odpalał na pozostałych — niespójna liczba hooków między renderami tej samej instancji (React w końcu rzuciłby „Rendered more hooks…"). Przeniesiony przed `return`. Pozostałe dwie: funkcje `useForTransformation`/`useLegacyPartnerOperatorReview` nie są hookami (zwykły async call, sprawdzenie env), tylko nazwa pasowała do heurystyki `use[A-Z]` — przemianowane. |
| `@typescript-eslint/no-namespace` | `requireAudit.middleware.ts` | `declare global { namespace Express {...} }` to jedyny sposób augmentacji Express w zwykłym `.ts` (analogiczny wzorzec w `.d.ts` jest zwolniony przez `eslint.config.js`) — scoped `eslint-disable` z uzasadnieniem. |
| `no-fallthrough` | `meeting.routes.ts` | Zamierzone grupowanie case'ów nie miało komentarza pasującego do `/falls?\s?through/i` — dodana linia `// falls through`. Zero zmiany zachowania. |
| `no-unsafe-finally` | `steadyAuthenticatedLoad.ts` | **Nie kosmetyka** — `throw error` w wewnętrznym catch siedział wewnątrz ZEWNĘTRZNEGO `finally`; gdyby zewnętrzny `try` już rzucił (prawdziwy błąd testu wydajności) i sprzątanie też by padło, ten `throw` PODMIENIAŁBY oryginalny błąd — `main().catch()` zgłosiłby awarię sprzątania zamiast realnego wyniku testu. Naprawione: sprzątanie loguje błąd (`console.error` + pole `cleanupError` w checkpoincie), nigdy nie rzuca z `finally`. |
| `no-irregular-whitespace` (2×) | `numberNotation.ts` | Dosłowne znaki Unicode (NBSP, wąska NBSP, cienka spacja, spacja cyfrowa, arabski separator tysięcy) wpisane wprost w regex — zamienione na `\uXXXX`. Identyczne zachowanie, zweryfikowane 18/18 testów `numberNotation.test.ts`. |
| `no-useless-escape` (2×) | `managementReportsService.ts`, `resultsWriterObservationMigration14.realdb.test.ts` | `\[` w klasie znaków i `\/` w zwykłym stringu JS — oba zbędne, usunięte. |
| `no-unused-expressions` (2×) | `transformationMobilizationOwnerAdapterService.ts`, `MethodNavigator.tsx` | Ternary jako samodzielna instrukcja (tylko efekt uboczny) — przepisane na `if/else`, identyczna logika gałęzi. |
| `@typescript-eslint/no-this-alias` | `VoiceAnswerChannel.transcript.test.tsx` | Nie klasyczny `var self = this` — to samorejestracja skonstruowanej instancji fejkowego `MediaRecorder` do przechwycenia przez test (nie ma innego uchwytu). Scoped `eslint-disable` z uzasadnieniem. |

## PR Gate — tabela zadań

| Zadanie | Komenda | Lokalnie? | Wynik | Czas |
|---|---|---|---|---|
| 🔍 Lint & Type Check → ESLint | `npm run lint` | tak | **CZERWONE** — 51 512 błędów `prettier/prettier`/`simple-import-sort` (0 nienaprawialnych — patrz niżej) | ~20 s (single-thread) |
| 🔍 Lint & Type Check → tsc (front) | `npm run type-check` | tak | ✅ PASS — 0 błędów (było 27) | ~3 min |
| 🔍 Lint & Type Check → tsc (server) | `npm run type-check:server` | tak | ✅ PASS — 0 błędów (było 1) | ~15 s |
| 🔍 Lint & Type Check → check:colors | `npm run check:colors` | tak | ❌ FAIL — **na czystej bazie też** (zweryfikowane `git stash`/checkout), nie moja regresja, poza zakresem tego zadania | ~5 s |
| 🔍 Lint & Type Check → check:a11y-jsx | `npm run check:a11y-jsx` | tak | ❌ FAIL — **na czystej bazie też**, jw. | ~5 s |
| 🔍 Lint & Type Check → check:a11y-focus | `npm run check:a11y-focus -- --base HEAD~1` | tak | ✅ PASS | ~3 s |
| 🔍 Lint & Type Check → check:triada:all | `npm run check:triada:all` | tak | ✅ PASS (dług spadł 3414→3147) | ~30 s |
| 🔍 Lint & Type Check → check:list-canon | `npm run check:list-canon` | tak | ✅ PASS | ~10 s |
| 🔍 Lint & Type Check → check:artefakt | `npm run check:artefakt` | tak | ✅ PASS | ~5 s |
| 🔍 Lint & Type Check → verify:canonical-16 | `npm run verify:canonical-16` | tak | ✅ PASS (`ok: true`) | ~3 s |
| 🧪 Unit Tests | `vitest run tests/unit` (CI dzieli na 4 shardy) | tak | ⚠️ 29 plików / 72 testy czerwone — **lepiej niż baza** (30/73 na `56913a0b3b`, zweryfikowane pełnym przebiegiem obu wersji), nie regresja | ~7.5 min |
| 🎨 Component Tests | `vitest run tests/components` (CI dzieli na 8 shardów) | tak | ⚠️ 76 plików / 191 testów czerwone — **identyczne na czystej bazie** dla 10 plików dotykających moich zmienionych źródeł (zweryfikowane per-plik na obu SHA), nie regresja | ~9.7 min |
| 🎨 Component Tests → My Work co-located | `vitest run src/components/MyWork` | nie uruchomiono osobno (część już pokryta przez `tests/unit`/`tests/components`) | — | — |
| 🏗️ Initiatives Unit Gate | `npm run test:initiatives` | nie uruchomiono (poza „unit/component") | — | — |
| ✅ Acceptance Tests | `npm run test:acceptance` (Postgres) | **nie** — wymaga bazy | — | — |
| 🔗 Integration Tests | `vitest run tests/integration` (Postgres, 3 shardy) | **nie** — wymaga bazy | — | — |
| 🧬 Colocated Tests | `vitest run server/src/**/__tests__ + src/**/__tests__` (Postgres) | **nie** — wymaga bazy | — | — |
| 🔒 Security Integrity Gate | (skrypt bezpieczeństwa) | nie uruchomiono (poza zakresem tego zadania) | — | — |
| 🌐 E2E Tests / 🧠 E2E M06 | Playwright | **nie** — e2e, jawnie wykluczone przez zlecenie | — | — |
| ✅ Readiness Smoke | Playwright | **nie** — e2e | — | — |
| 📈 Patch Coverage Gate | coverage diff | nie uruchomiono | — | — |
| 🛡️ Test Quality / 🚫 Skip-Scan / 🎯 Levels Coverage | skrypty pomocnicze | nie uruchomiono (poza zakresem) | — | — |

Bezpieczniki zastosowane: `--retry=0` wszędzie, weryfikacja że `vitest.config.ts:210`
przypina `DB_TYPE=sqlite` domyślnie (zgodne z env joba `unit-tests`/
`component-tests` w CI — one też nie ustawiają `DB_TYPE`), zero „No test
files found" potraktowanych jako PASS.

## Co zostaje czerwone i dlaczego

1. **`npm run lint` (ESLint całość)** — pozostanie czerwone bez masowego
   `--fix`. 51 512 błędów to WYŁĄCZNIE `prettier/prettier` (50 308) i
   `simple-import-sort/{imports,exports}` (1 184 + 20) — formatowanie, nie
   logika. `--quiet` ukrywa tylko OSTRZEŻENIA, nie błędy, więc nawet zero
   ostrzeżeń nie daje zielonego `npm run lint`. To zadanie miało jawny
   zakaz masowego `--fix` — bramka `lint-typecheck` nie może dziś przejść
   w całości bez decyzji właściciela: albo świadomy, jednorazowy
   `--fix` (ryzyko: ~2146 plików na tym tipie, patrz niżej), albo
   przepisanie `npm run lint` na bramkę ratchet (jak `check-triada`/
   `check-list-canon`) zamiast twardego blokera na całe repo.
2. **`check:colors` i `check:a11y-jsx`** — czerwone, ale **potwierdzone
   jako stan bazy** (`git stash`/checkout na `56913a0b3b` daje identyczną
   listę naruszeń). Nie moja regresja, poza mandatem tego zadania (26 TS +
   14 lint-do-ręki). ~40 i ~17 plików z długiem odpowiednio.
3. **Unit/Component testy** — istniejący dług (30/73 i tożsamy zestaw na
   bazie), moja gałąź go NIE pogłębia (unit: lepiej o 1 plik/1 test;
   component: identycznie na weryfikowanej próbce). Pełnego 1:1 porównania
   component (76 plików) na obu SHA nie zrobiłem — zbyt kosztowne czasowo
   (~10 min × 2); zweryfikowałem selektywnie 10 plików, które faktycznie
   importują moje zmienione źródła (`IdeaMapWorkspace`, `DecisionDetailView`,
   `TaskDetailView`, `AssessmentHub`, `EarningsSection`,
   `DocumentStudioTemplateArchitectView`) — wszystkie identyczne na obu SHA.
4. **Acceptance/Integration/Colocated/E2E/Security** — nie uruchomione,
   wymagają Postgres i/lub sieci zewnętrznej; jawnie wykluczone przez
   zlecenie.

## Dry-run `eslint --fix` na tym tipie (30589e9f2f)

`npx eslint . --fix-dry-run -f json` → **2146 plików** zmieniłby
`eslint --fix` (licząc pliki z rzeczywistym `.output`, nie tylko
zgłoszeniem). Nie uruchomiony — masowy `--fix` był jawnie poza zakresem.

## `check-list-canon` i `vite build` (krok 5 zlecenia)

- `bash scripts/check-list-canon.sh` → **EXIT 0** ✅
- `npx vite build` → **EXIT 0** ✅ (2 min 36 s; ostrzeżenie o chunkach >500 kB,
  pre-istniejące, nie z tej sesji)
- Push: `github-backup/fix/pr-gate-typy-lint-20260902` @ `30589e9f2f` ✅

## Czego nie zmierzyłem

- Pełnego 1:1 porównania **wszystkich** 76 czerwonych plików component-tests
  między moją gałęzią a bazą (zmierzyłem selektywnie 10, patrz wyżej).
- `test:initiatives`, `test:acceptance`, `integration-tests`,
  `colocated-tests`, `security-integrity`, `patch-coverage`,
  `test-quality-check`, `skip-scan-gate`, `levels-coverage-gates` — część
  wymaga Postgres/sieci (jawnie wykluczone), część pominięta z braku czasu
  w budżecie tej sesji.
- Realnego uruchomienia joba `lint-typecheck` na GitHub Actions (tylko
  lokalna symulacja komend) — `npm run lint` w CI **da inny exit code
  niż lokalnie tylko jeśli** ktoś włączy tam `--quiet` inaczej niż tu; nie
  zweryfikowałem literalnie na runnerze.
