---
doc_id: funkcje-odbior-205-206
status: canonical
truth_type: acceptance
established: 2026-08-31
---

# ODBIÓR 205 + 206 · OBA NIE SCALAĆ (FIX-y wydane) — moduł 17, pierwsza para

## 205 pętla mądrości — SCALONE PO FIX-205 (`7936b45e7a`+`88ad7de358`+`82d7e7318e`)

★ Ostatnia mila DOMKNIĘTA: `buildOrganizationSection` renderuje
`org.notes.manualContext`, dowód NA WYRENDEROWANYM PROMPCIE („### Notatki
organizacji…" z magicznymi stringami), mutacja revertu → czerwony. Koszt R1
zamknięty: supersede po (org, section) zamiast append (20 PUT → 1 aktywny claim,
19 superseded) + `rebuildSnapshot:false` (snapshot jest write-only — nikt go nie
czyta, zmierzone w migracji). Zero plików w src/ — moduł 01 nietknięty.

### Pierwotny werdykt: R1 **C** · R2 **C+** · R3 **B+** · R4 **A−**
Zapis-obok działa technicznie (mutacje 3/3 czerwone, moduł 01 CLOSED_FINAL
nietknięty — 0 plików w src/), ale **★ treść NIE DOCIERA DO PROMPTU**: audytor
zmierzył sondą `{inResolved:true, inOrgLayer:true, inPrompt:FALSE}` —
`AIPipeline.buildOrganizationSection:1754` nie czyta `org.notes` (zero trafień
`.notes` w pliku), a furtka `aiContextBuilder:674` ma whitelistę source_type,
która odrzuca oba nowe typy. „Claim istnieje ≠ prompt go niesie" — ósmy kształt
o poziom wyżej. **Drugi, nieujawniony koszt:** autosave co 1,5 s × pełny
rebuildSnapshot + append-only claims (zmierzone: 20 zapisów → 60 claimów)
zalewałby też kolejkę governance (pending, limit 200/500).
FIX-205: (1) render `notes.manualContext` w prompcie ALBO whitelist +
asercja NA WYRENDEROWANYM PROMPCIE; (2) upsert zamiast append +
`rebuildSnapshot:false` w zapisie-obok.

## 206 tool-loop — mechanizm **4/5** · bezpieczeństwo **1/5** · dowody **1/5**
Rzemiosło dobre (własna rodzina `readTools`, 11/11 wycenionych, flaga OFF
potwierdzona, licencja czysta, raport uczciwie PARTIAL). **Sprzężenie z 210
OBALONE pomiarem** — w pętli biegnie szczelny handler z `userId` (dowód: cudzy
dokument → `results: []`, czułość sprawdzona na właścicielu).

### ★★ BLOKER: P0 CROSS-ORG wprowadzony przez ten dyżur
`executeGetInitiativeStatus` (toolDefinitions:1247-1266) filtruje TYLKO
`project_id`, bez `organization_id`; 206 karmi go `projectId` **z ciała żądania**
(`ai.routes.ts:4892`). Zmierzone: org-A + cudzy projekt → nazwa, status, ROI
inicjatywy org-B. Dziś martwe (jedyny wołacz plannera nie podaje projectId) —
**scalenie by to ożywiło**. To samo `executeGetAssessment:1137`.
Plus: tryb prywatny nie dociera do pętli (chatPolicyGateway realnie zmienia
zakresy); **testy TAUTOLOGICZNE** (mutacje „readToolDefs=[]" i „flaga zawsze ON"
→ 6/6 PASS); fantom panelu „Deep Research" przy każdej turze (slot
researchProgress, wyrenderowany przez audytora); timeout raportowany jako
`completed`; R3 bez ani jednego przebiegu modelu (0/2 budżetu).
FIX-206: (1) org-scope + walidacja członkostwa ALBO odcięcie projectId z body,
z testem cross-org; (2) privateMode do pętli; (3) testy BEHAWIORALNE (mutacja
readToolDefs=[] MUSI być czerwona) + izolacja przez pętlę; (4) własny slot SSE
zamiast researchProgress; (5) timeout ≠ completed; (6) R3 z realnym modelem.
