---
doc_id: dlug-c-d-20260901
status: working
truth_type: worklist
established: 2026-09-01
od: nadzorca toru grafiki (sesja 01.09, popołudnie)
zrodlo: status.json (313 ekranów) — wszystkie oceny C i D
---

# Dług C/D — lista robocza pogrupowana PO PRZYCZYNIE

**Po co ten dokument.** Właściciel przeklikał 260 kart i przyjął 255. To, czego NIE widział,
to 27 ekranów z oceną C („nie pokazujemy") i 33 z oceną D („odłożone") — razem **60 pozycji**.
Ten dokument porządkuje je nie po ekranie, tylko po **przyczynie**, bo w tym repozytorium
naprawa per wywołanie odrasta (pamięć nadzorcy `naprawa-per-wywolanie-odrasta`).

**Zmierzone, nie przepisane.** Liczby ekranów pochodzą z `status.json` odczytanego
2026-09-01. Liczby zasięgu (kolumna „realny zasięg") pochodzą z greppów uruchomionych
w tej samej godzinie — podane niżej przy każdej rodzinie.

---

## ★ PIERWSZE ZNALEZISKO: to nie jest dług rozproszony po produkcie

**Moduł 12 (Administracja) daje 25 z 60 pozycji — 42% całego długu C/D**, przy czym
Administracja to jeden z osiemnastu modułów. Reszta rozkłada się cienko po całości.

Wniosek dla planowania: nie ma sensu „obchodzić 60 ekranów". Sensowne są dwie osobne
roboty — (1) **jedno przejście przez Administrację kanonem**, (2) kilka wąskich rodzin
w pozostałych modułach.

---

## RODZINY PRZYCZYN

### R1 · Zbudowane, ale niepodłączone — brakuje jednego przewodu (5 ekranów, wszystkie C)

Komponent istnieje, silnik działa, testy są. Nie ma **wołacza** — użytkownik tego nie widzi.
To nie defekt grafiki: właściciel oceniałby funkcję, której w produkcie nie ma.

| Ekran | Czego brakuje | Koszt |
| --- | --- | --- |
| `tools-swot-live` | gniazda/zakładki w sesji narzędzia Dynamiczny SWOT | 1 przewód |
| `finance-value-panels` | zakładki w `FinanceHub` + przekazania fetcherów (7 paneli, backend już je zna z nazwy) | 1 przewód |
| `siri-tier` | widok w produkcie **i** wejście do niego (wg `ASSESSMENT_KB_SIRI.md` §4 osobny ekran) | 2 przewody |
| `deck-quality-badge` | przepiąć harness z martwego kreatora na żywy `DeckBuilder` — funkcja JEST w produkcie | 1 przewód (w harnessie) |
| `agent-plan-view` | **nic** — funkcja żyje przez Agent Hub, to osierocony wrapper | wyrejestrować |

**Decyzja nadzorcy:** cztery pierwsze to tor funkcji (mechanika), nie grafiki. `agent-plan-view`
zdejmujemy z rejestru. Nie pokazujemy właścicielowi żadnego z nich, dopóki nie ma wołacza.

---

### R2 · Brak lokalizacji — ekran mówi po angielsku (16 ekranów: 10 C + 6 D)

Nie pojedynczy napis — **brak lokalizacji komponentu albo pusty słownik**. Trzy podtypy,
bo naprawia się je inaczej:

**R2a — komponent bez ani jednego `t()`** (retrofit architektury):
`vault-scope-selector` · `report-builder-block-types` · `report-builder-templates` ·
`aios-home` · `aios-actions` · `aios-research` · `admin-billing-payment-methods` ·
`admin-security-api-access` · `admin-ai-policy-autonomy`

**R2b — komponent gotowy, słownik pusty** (tania naprawa, dopisać klucze):
`tool-outputs-panel` (~15 kluczy `toolOutputs.*` nie istnieje w `pl` ANI w `en`)

**R2c — surowe wartości z serwera pokazane wprost w interfejsie** (naprawa po stronie mapowania):
`admin-security-security-alerts` (enumy `impossible_travel`, `critical`) ·
`admin-audit-events` (nazwy zdarzeń `member removed` **plus surowe ładunki JSON w UI**) ·
`execution-tab-people_change` (`ITEMS`/`CRITICAL`/`BLOCKED`) ·
`results-vnext-kpi-tool` (H1 ekranu to surowy kod `OEE-LINIA-PAKOWANIA`) ·
`admin-ai-ai-operations` (surowe id wplecione w polskie zdanie)

**R2d — treść generowana po angielsku** (to nie interfejs, to wyjście silnika):
`insight-artifact` (streszczenie/ustalenia/ryzyka) · `assessment-matryca` (centrum
`MaturityMatrix.tsx`)

> **★ UWAGA — decyzja właściciela z 31.08 obowiązuje tutaj.** Angielski jest językiem
> WIODĄCYM metodyki: angielskie nazwy poziomów, osi i technologii **nie są defektem**.
> Polski obowiązuje w INTERFEJSIE. Zanim ktokolwiek „naprawi" `assessment-matryca`,
> musi przesiać jej treść przez to kryterium — inaczej powtórzy błąd Z-34 (robotnik
> rozciągnął regułę i zamienił działający polski na angielski).

**Zmierzony zasięg (2026-09-01):** w `src/components/settings` + `src/components/admin` +
`src/views/admin` **10 plików z 254 nie ma ani jednego `t()`**. To **dolna granica** —
obecność `t()` w pliku nie oznacza, że wszystkie napisy są przetłumaczone (znany kształt:
„klucz istnieje ≠ przetłumaczony"). Zlecony osobny pomiar per ekran.

---

### R3 · Crimson jako dekoracja (8 ekranów: 3 C + 5 D)

Pułapka nr 1 z `CLAUDE.md`: `primary-*` każdy numer = crimson #85182F. Czerwień wyłącznie
dla semantyki krytycznej.

Ekrany z kart: `admin-billing-plan-limits` (przycisk `variant="brand"`) ·
`admin-security-security-policy` · `aios-artifacts` · `admin-billing-overview` ·
`admin-security-api-access` (odznaka na KAŻDYM kluczu) · `admin-security-risk-summary` ·
`admin-ai-models-providers` (odznaka PREMIUM) · `admin-ai-ai-limits-budgets` (suwaki)

**Zmierzony realny zasięg:** **95 wystąpień w 17 plikach** w tych samych trzech katalogach —
czyli **dwukrotnie więcej plików niż ekranów zgłoszonych**. Najgęstsze:
`OrgAISettingsView.tsx` (14) · `FeaturesPrivacyTab.tsx` (13) · `PolicyGovernanceTab.tsx` (11) ·
`AuditComplianceTab.tsx` (11) · `CustomComplianceTemplateEditor.tsx` (10).

Nie każde z 95 jest defektem — część może być legalną semantyką krytyczną. Wymaga
przesiania, nie ślepej zamiany. **To jest robota na jedno przejście, nie na osiem łatek.**

---

### R4 · Poza kanonem tabel — własna tabela zamiast StandardTable (8 ekranów: 3 C + 5 D)

Złamanie reguły nadrzędnej 9 z `CLAUDE.md` (krach 07-12, „tabelki jak dla trzylatka").

`admin-audit-events` · `admin-billing-invoices` · `admin-billing-budgets-alerts` ·
`admin-security-scim-lifecycle` (listy jako surowe wiersze tekstu) ·
`admin-security-api-access` (bespoke karty) · `admin-ai-models-providers`
(**surowe `<table>` z fałszywym self-exempt**, `ModelsProvidersTab.tsx:649`) ·
`admin-security-risk-summary` (kolejka jako surowe karty) ·
`admin-security-security-policy` (własny pill-nav, wzorzec ADM-OWN-001)

**Zmierzony zasięg:** `FilterableTable` w **6 plikach**, `StandardTable` w **25**. Czyli
kanon w Administracji jest w większości trzymany — te 6 to wyraźna, policzalna dziura.
Bezpiecznik `scripts/check-list-canon.sh` istnieje; do sprawdzenia, czemu tych 6 nie łapie.

---

### R5 · Tekst niewidoczny w jednym motywie (4 ekrany: 1 C + 3 D)

Najgroźniejsza rodzina, bo **defekty siedzą w komponentach współdzielonych** — jedna
poprawka zdejmuje wiele ekranów, ale jedno przeoczenie odrasta wszędzie.

| Ekran | Plik przyczyny | Który motyw psuje |
| --- | --- | --- |
| `ustawienia-bezpieczenstwo` | lista „Ostatnia aktywność" | **jasny** — biały tekst na jasnym |
| `admin-ai-ai-limits-budgets` | `SettingsCard.tsx:74` (`text-navy-900` bez `dark:`) | ciemny |
| `admin-ai-data-privacy` | `SettingsToggle.tsx:56` — **komponent współdzielony** | ciemny |
| `admin-ai-ai-audit` | `AuditLogViewer.tsx:174,322` (`text-white` bez light) | **jasny** |

**Zmierzony zasięg:** **221 miejsc** w `settings`+`admin` używa `text-navy-900` lub
`text-white` **bez wariantu drugiego motywu**. Ta liczba jest SUROWA — część z nich to
tekst na kolorowym tle, gdzie jest poprawna. Wymaga przesiania z pomiarem kontrastu,
nie zamiany hurtem. Zlecone.

---

### R6 · Format amerykański zamiast polskiego (2 ekrany)

`ustawienia-bezpieczenstwo` (`8/31/2026, 9:18:49 AM` zamiast DD.MM.RRRR) ·
`admin-billing-invoices` (kwoty bez `PLN`)

Rodzina znana i już raz mierzona: „kilka dat US → 29 wystąpień w 22 plikach" (Z-23).
**Przed naprawą przemieść całość tym samym wzorcem.**

---

### R7 · Realny błąd danych, nie wygląd (5 ekranów: 4 C + 1 D) → TOR FUNKCJI

Te ekrany wyglądają źle, bo **pokazują złe dane albo nic**. Grafika ich nie naprawi.

| Ekran | Przyczyna zmierzona w kodzie |
| --- | --- |
| `admin-audit-compliance-evidence` | front czyta `action/actor/risk`, backend zwraca `action_type/admin_id/risk_level` → kolumny puste |
| `inicjatywy-lista` | `INITIATIVE_DATA_CONTRACT_ERROR` — wyścig `localStorage`, pierwszy fetch przed debounce'owanym (300 ms) zapisem |
| `ustawienia-billing` | `SettingsView.tsx:468-472` zwraca `null` → klik „Subskrypcja i płatności" pokazuje **Profil**, pozycja menu nawet się nie podświetla |
| `report-builder-templates` | kolumna SEKCJE zawsze `0` bez wyjaśnienia |
| `admin-security-risk-summary` | (powiązane) „Ryzyka wymagające przeglądu" zawsze `0` — błąd odczytu pola |

> **★ `inicjatywy-lista` to ten sam defekt co zgłoszenie P0 #1** w
> `ZGLOSZENIA_DO_TORU_FUNKCJI.md` (okno 300 ms bez ochrony trybu demo). Tam opisany jako
> hipoteza z dowodem kodowym; **tutaj mamy jego objaw widoczny na ekranie**. To podnosi
> wagę zgłoszenia, ale NIE czyni z niego faktu potwierdzonego — nadal brak pomiaru
> mutacyjnego na żywym backendzie.

---

### R8 · To przyrząd, nie produkt — wyrejestrować z odbioru (7 ekranów: 2 C + 5 D)

`idea-table-production` (harness porównawczy szerokości) · `preview-4-zakladki` (ekran sam
siebie opisuje w komentarzu jako przyrząd; **miał ocenę A do 01.09**) ·
`finance-id-bridge` · `finance-focus-mode` (narzędzia inżynierskie) ·
`results-vnext-registry-shell` (fixtura mechaniki powłoki) ·
`prawy-pas-notatnik-system` (id bez klucza w rejestrze — oddaje listę awaryjną) ·
`document-studio-m1-share-primary` (zdublowany ekran testowy)

To jest ta sama rodzina, którą opisuje `AUDYT_PRZYRZADU_20260901.md`. **Nie naprawiamy —
zdejmujemy z odbioru.** Robota: jedno przejście rejestru, nie siedem.

---

### R9 · Funkcja nie istnieje w produkcie — decyzja produktowa (7 ekranów, wszystkie D)

`initiatives-portfolio-analysis` (komponent usunięty jako martwy kod, 12,8 tys. linii) ·
`execution-export-prezentacja` · `execution-change-signals` (fantom bez implementacji) ·
`results-three-pairs` (stary hub Wyników wycofany 24.08) · `gen-excel-templates-tab`
(właściciel zdjął wejście 30.08, **kodu nie ruszamy** — komponent to żywy koniec kreatora) ·
`org-redesign-v1-...` i `settings-full-module-...` (**wiersze-duchy**: CLOSED_FINAL bez
ani jednego zrzutu w tym drzewie, zastąpione 30 realnymi ekranami)

**Nic do zrobienia w grafice.** Jedyna otwarta sprawa: `initiatives-portfolio-analysis`
wymaga rozstrzygnięcia właściciela — czy funkcja ma wrócić.

---

### R10 · Alias / duplikat pozycji menu (6 ekranów, wszystkie D)

Ta sama treść pod kilkoma pozycjami menu: `admin-billing-usage-costs` (pixel-identyczny
z Przeglądem) · `admin-billing-billing-details` · `admin-audit-high-risk-changes` ·
`admin-audit-retention-export` · `admin-health-diagnostics` · `admin-security-sso`
(**bitowo identyczny** z Polityką bezpieczeństwa)

Cztery są opisane jako „alias zamierzony". **Dwa nie są i wymagają rozstrzygnięcia:**
`admin-billing-usage-costs` (osobny slot menu dla identycznej treści — nazwane w karcie
jako „mylący") i `admin-security-sso` (karta wprost mówi „do potwierdzenia z właścicielem").

---

### R11 · Zamierzony placeholder / fail-closed (2 ekrany, D)

`admin-health-service-status` (`CAN_ACCESS_PLATFORM_OPERATIONS=false` na sztywno, nigdy
nie woła API) · `admin-health-platform-operations`

**Bez działań** — ale patrz pamięć „zamknięte przez wygaszenie": fail-closed bywa zielony
dlatego, że kontekst nie dociera do nikogo. Jeśli te ekrany mają kiedyś działać, potrzebna
para dowodów „obcy nie widzi" + „właściciel widzi".

---

## PODSUMOWANIE — co z tego wynika dla pracy

| Rodzina | Ekranów | Kto to robi | Gotowe do zlecenia |
| --- | --- | --- | --- |
| R2 lokalizacja | 16 | grafika | po pomiarze zasięgu |
| R3 crimson | 8 (17 plików) | grafika | **TAK** |
| R4 kanon tabel | 8 | grafika | **TAK** |
| R5 kontrast motywu | 4 (221 miejsc do przesiania) | grafika | po pomiarze |
| R6 format PL | 2 | grafika | po przemieceniu |
| R8 wyrejestrować przyrządy | 7 | grafika, jedno przejście | **TAK** |
| R1 brak wołacza | 5 | tor funkcji | — |
| R7 błędy danych | 5 | tor funkcji | — |
| R9 nie istnieje | 7 | 1 pytanie do właściciela | — |
| R10 aliasy | 6 | 2 pytania do właściciela | — |
| R11 fail-closed | 2 | bez działań | — |

**Grafika ma realnie 45 ekranów w 6 rodzinach.** Pozostałe 15 to tor funkcji albo decyzje.

**Trzy pytania do właściciela** (pojedynczo, z rekomendacją — nie hurtem):
1. `initiatives-portfolio-analysis` — funkcja usunięta jako martwy kod. Wraca czy nie?
   *Rekomendacja: nie wraca; analiza portfela żyje w rejestrze inicjatyw.*
2. `admin-billing-usage-costs` — osobna pozycja menu pokazująca to samo co Przegląd.
   *Rekomendacja: zdjąć pozycję, treść zostaje.*
3. `admin-security-sso` — pozycja SSO pokazuje bitowo to samo co Polityka bezpieczeństwa.
   *Rekomendacja: to defekt, nie alias — SSO ma mieć własną treść; zgłosić do toru funkcji.*

---

## ★ POMIARY ZASIĘGU — wyniki (2026-09-01, popołudnie)

Trzy pomiary zlecone równolegle, każdy read-only. Raporty:
`POMIAR_R3_CRIMSON.md` · `POMIAR_R5_KONTRAST.md` · `POMIAR_R2_JEZYK.md`.

**Wszystkie trzy podważyły liczbę podaną przez nadzorcę.** Kontekst i wnioski: `DZIENNIK_GRAFIKA.md`
Z-38. Poniżej tylko wyniki.

### R3 crimson — 95 wystąpień, 17 plików, **91 do naprawy, 0 uzasadnionych**

**Przyczyna ustalona i to nie jest niechlujstwo per ekran.** `tailwind.config.js:209`:
`primary: { DEFAULT: '#85182F', // was #7C3AED }` — z komentarzem mówiącym wprost, że podmiana
renderuje brand „app-wide with ZERO call-site edits". **Fiolet przemalowano na crimson globalnie;
żadne z 95 miejsc nie zostało wybrane jako czerwień.** Wszystkie realne stany krytyczne w tych
plikach używają już `danger-*` — stąd B=0.

- Rozkład: `src/components/settings` **0** · `src/components/admin` **69** · `src/views/admin` **26**.
  (Nazwa „Ustawienia" w moim zleceniu nie odpowiadała rozkładowi — dług jest wyłącznie w Administracji.)
- **91 to 8 powtarzalnych wzorców, nie 91 decyzji:** ozdobna ikona nagłówka ×27 · stan aktywny ×20 ·
  obwódka fokusu ×19 · kolor kategorii ×14 (w tym odznaka PREMIUM) · poświata pod przyciskiem ×5 ·
  link ×6 · **odwrócona semantyka ×4** · `variant="brand"` na zwykłym CTA ×1.
- **Naprawa wzorcami, nie plikami** — jeden przebieg = jeden wzorzec przez wszystkie 17 plików,
  jeden zrzut kontrolny per wzorzec. Plik-po-pliku zostawi rozjazdy między sąsiednimi ekranami.

**Cztery linie z odwróconą semantyką — zweryfikowane przez nadzorcę odczytem kodu, naprawa pierwsza:**
`ApiKeysManagementView.tsx:334,341` — klucz API **aktywny** = crimson, **wygasły/odwołany** = `danger`;
dwa stany, dwa odcienie czerwieni, użytkownik ich nie odróżni.
`AdminLLMView.tsx:620,625` — ikona `AlertTriangle` świeci crimsonem, **gdy `error_rate ≤ 5%`**,
czyli gdy jest DOBRZE; przy realnym przekroczeniu progu przechodzi na `danger`. Zdrowy stan
wygląda na alarm.

**Sprzeczność w naszych własnych źródłach — ROZSTRZYGNIĘTA przez nadzorcę.**
`tailwind.config.js:150` (komentarz): „c-accent (crimson/brand): TYLKO brand/CTA/**selected**".
`CLAUDE.md` pkt 3 + `Button.tsx` §5.1: CTA i stany aktywne = **neutralne**, fokus = `c-focus`.
**Wygrywa `CLAUDE.md` — jest prawem nadrzędnym i wynika z krachu 07-12.** Komentarz w configu jest
nieaktualny i zostanie poprawiony razem z naprawą (dotyczy ~20 z 91 wystąpień). Bez tego następny
wykonawca naprawi zgodny ekran, żeby dopasować go do martwego opisu — kształt Z-36.

**Do decyzji właściciela (nie nadzorcy) — skala poziomów polityki AI**, 4 linie
(`PolicyGovernanceTab.tsx:70,71`, `OrgAISettingsView.tsx:186,187`): ADVISORY=szary,
ASSISTED=niebieski, PROACTIVE=**crimson**, AUTOPILOT=**zielony**. Najwyższa autonomia jest zielona,
środkowa czerwona — skala jest niespójna jako całość, punktowa podmiana crimsona jej nie naprawi.

**Granica pomiaru (podana przez robotnika sama z siebie):** objęto WYŁĄCZNIE `primary-[0-9]`
i `variant="brand"`. Pominięto `crimson-*`, `c-accent`, `#85182F`, `bg-primary` bez numeru oraz
pliki `.ts`/`.css`. **95 to dolna granica; górnej nie znamy.** Do domierzenia.

### R5 kontrast motywów — moja liczba była **zaniżona**, nie zawyżona

Ta sama komenda bez mojego filtru: **449** linii (mój filtr `grep -v 'dark:'` dawał 221 —
odrzucał całe linie, w których `dark:` dotyczyło innej właściwości). Po odjęciu trafień już
poprawnie sparowanych: **351** bazowych wystąpień.

- **Rdzeń** (`settings`+`admin`+`AISettings`, 360 wystąpień): **A=65 defektów** · B=159 poprawnych
  (tekst na tle o stałym kolorze) · C=6 nierozstrzygniętych · 130 już poprawnych.
  Z 65 defektów **28 otwarto i przeczytano pojedynczo**, 37 zaklasyfikowano przez analogię do
  potwierdzonego wzorca — oznaczone w raporcie jako nieotwarte.
- **Reszta `src/`**: 3076 wystąpień, **wyłącznie heureza automatyczna, żadnej linii nie otwarto.**
  Ta liczba nie jest listą defektów i nie wolno jej tak cytować.

**Prawdziwa przyczyna jest czwarta i szersza niż cztery znane ekrany.** `SettingsSection.tsx`
(kontener używany przez **32 pliki**) maluje tło `bg-c-surface-raised`, które w `src/index.css`
realnie zmienia się z motywem: `#f8fafc` (jasny) → `#15213b` (ciemny) — zweryfikowane przez
nadzorcę w liniach 50 i 272. Każdy `text-white` renderowany w jego wnętrzu **znika w motywie
jasnym**; potwierdzono dla 8 plików.

**Kolejność naprawy — wg współdzielności, nie liczby wystąpień** (jedna poprawka zdejmuje wiele ekranów):
`SettingsSection.tsx` (32 konsumentów) → `SettingsToggle.tsx` (18) → `SettingsCard.tsx` (5) →
`AuditLogViewer.tsx` (3).

> Dwa z czterech pierwotnie zgłoszonych defektów zweryfikowane przez nadzorcę w kodzie:
> `SettingsCard.tsx:74` — `<h3 className="font-semibold text-navy-900">` bez wariantu ciemnego.
> `SettingsToggle.tsx:56` — `text-navy-900` na etykiecie przełącznika, również bez wariantu.
> Oba w `src/components/AISettings/`, **nie** w `settings/` ani `admin/`.

### R2 język — trzy różne przyczyny, jedna moja informacja była nieaktualna

- **R2a (brak `t()` w ogóle):** 373 kandydatów mechanicznych → **80 pewnie potwierdzonych**
  (angielski literał w JSX, konkretna linia). Metoda **niedoszacowuje**: `AIRoleBadge.tsx`
  renderuje angielskie `'Operator'` przez zmienną i nie trafił na żadną listę. Realna liczba
  leży między **80 a ~350** i bez ręcznego przeglądu 273 plików nie da się jej podać.
  Osobno: `t(` jako wzorzec fałszywie łapie `setTimeout(`, `reject(` — przez co
  `ActionCenter.tsx` i `ResearchSessionsDock.tsx` (ekrany `aios-*`) **wypadły z listy mimo
  że są defektem**.
- **R2b (klucze brakujące w słowniku): mój przykład był nieaktualny.** Dla `tool-outputs-panel`,
  `report-builder-block-types`, `report-builder-templates` — **0 brakujących kluczy w `pl` i `en`**.
  Robotnik sprawdził `git log`: defekt „~15 kluczy `toolOutputs.*`" został naprawiony wcześniejszym
  commitem na tej gałęzi. Podałem go w zleceniu jako potwierdzony fakt, przepisując z karty ekranu.
  **Karty w `status.json` starzeją się szybciej niż kod.** Trzy ekrany `aios-*` to nie R2b, tylko R2a.
- **R2c (klucz istnieje, ale trzyma angielskie słowo):** **780 kluczy** ma identyczną wartość w `pl`
  i `en`. Z próbki 30 ocenionej regułą właściciela — większość **dozwolona** (nazwy metodyk,
  technologii, marek, słowa uniwersalne jak „Status"), ale są jednoznaczne defekty:
  `myWorkTable.inlineAIFill.aiFillButton = "AI Fill ({{count}})"`,
  `settings.notifications.sounds.sound_bell = "Bell"`.
- **★ Defekt odwrotny, o który nikt nie prosił:** min. **40 kluczy, gdzie plik ANGIELSKI zawiera
  POLSKI tekst** — np. `reports.toast.reportGenerated` = `'Raport wygenerowany pomyślnie'` w obu
  plikach. Skupione w `reports.toast.*` / `reports.export.*`. Klient anglojęzyczny dostaje polski
  komunikat. **Najtańsza naprawa z całej rodziny R2** i jedyna, która psuje produkt w drugą stronę.

**Kolejność naprawy R2:** (1) defekt odwrotny EN=PL, ~40 kluczy — najtańsze i najpilniejsze;
(2) 80 potwierdzonych R2a; (3) jednoznaczne R2c; (4) graniczne R2c — **wyłącznie za akceptem
właściciela**, bo tu przebiega granica decyzji z 31.08 o angielskim jako języku metodyki;
(5) pula 273 nierozstrzygniętych — najdroższe, wymaga ręcznego przeglądu.

### ★ Nowa pułapka pomiarowa — `admin` i `Admin` to ten sam katalog

`ls -di src/components/admin src/components/Admin` → **identyczny inode `462360537`**. macOS
jest nieczuły na wielkość liter; w repozytorium występują importy pod obiema pisowniami. **Każdy
pomiar wymieniający obie nazwy liczy te same pliki dwukrotnie** — i robi to niewidocznie, bo
ścieżki w wyniku się różnią. Kontekst i konsekwencja wdrożeniowa: `DZIENNIK_GRAFIKA.md` Z-39.
