# F2-3 PMO E3 — freeze R2

**Werdykt:** poprawki po niezależnym review są kompletne; kandydat realizuje tworzalne projekty, role i alokacje oraz pięć bramek PMBOK-lite, pozostaje za domyślnie wyłączoną flagą `VITE_PMO_PROJECTS` i czeka na ponowny niezależny odbiór.

## Tożsamość kandydata

- baza linii: `f9239fe307882d5a54c2251b5f631b4a173e702e` (`origin/integracja/20260911`);
- poprzedni niezależny review: `5a114bf0eb` (`REQUEST_CHANGES`, zrebasowany odpowiednik `11fd50ff1b`);
- implementacja po poprawkach: `4be756f1a8de787203a111766a22f53e574de588`;
- zakres diffu implementacji: `f9239fe307882d5a54c2251b5f631b4a173e702e..4be756f1a8de787203a111766a22f53e574de588`;
- finalny manifest jest wyłącznie potomkiem dokumentacyjnym implementacji; jego dokładny SHA wskazuje niezmienny ref backup zgłoszony CTO.

## Zakres zachowania

- `/projects` przekierowuje do `/my-work` przy fladze OFF i montuje kanoniczny ekran projektów przy ON.
- Użytkownik tworzy projekt w UI, otwiera go w `StandardPreview`, przypisuje role DEC-488 i ustawia alokację per osoba zgodnie z DEC-480.
- Widok pokazuje odpowiedzialności, plan komunikacji i wejścia do akceptacji wyprowadzone z ról projektu.
- `StandardTable` pokazuje pięć przejść: Context → Assessment → Initiatives → Roadmap → Execution → Stabilization.
- Zatwierdzenie wymaga roli Sponsor, Leader, Steering Committee albo PMO. Wildcard aplikacyjny nie omija roli projektowej.
- Odczyt obcego projektu kończy się 404. Zapis receipt i zmiana fazy są atomowe na jednym połączeniu PostgreSQL.
- Nie powstała druga implementacja H1b ani martwy fallback `RESOURCE_RESPONSIBILITY`. Pakiet nie używa nieistniejących `review_requested_at` ani `approved_at` dla propozycji przejść.

## Poprawki po review

- CLOSURE_GATE interpretuje `decisions.required` jako TEXT bez porównania liczbowego.
- KPI są liczone z kanonicznych `project_kpis` oraz `initiative_kpis` powiązanych z `kpi_measurements` przez inicjatywę projektu; nie ma odwołania do `kpi_results`.
- Błąd zapytania kryterium kończy ocenę diagnostycznym `STAGE_GATE_CRITERION_QUERY_FAILED`, zamiast udawać zwykłe `NOT_READY`.
- RealPG przechodzi wszystkie pięć bramek, sprawdzając stan negatywny przed każdą oraz trwały receipt i finalną fazę `Stabilization`.
- Minimalny budżet UI wynosi `0.01`, zgodnie z kontraktem Zod `.positive()`; test odrzuca zero.
- Statusy inicjatyw w dowodzie to kanoniczne `APPROVED` i `CLOSED`; nie dodano prywatnych stanów ani martwych literałów przejść.

## Migracja 20262190

`server/migrations/20262190_f2_3_pmo_stage_gates.sql` pozostaje pojedynczą zmianą addytywną: `CREATE TABLE IF NOT EXISTS stage_gates`, pięć wartości `gate_type`, FK `projects(id) ON DELETE CASCADE` i dwa indeksy. Nie zawiera danych, backfillu, `DROP` ani `ALTER`; nie koliduje z pulą F2-E od 20262200. Dowód migracyjny obejmuje strict empty PostgreSQL 2×, kopię schematu linii 2×, idempotencję, rollback operatorski i niezmieniony release gate.

## Walidacja R2

| Bramka | Wynik |
|---|---|
| server TypeScript | PASS, 0 błędów |
| esbuild per zmieniony plik frontendowy | PASS, 8/8 |
| focused unit/contract po rebase | PASS, 16/16 |
| wszystkie pięć bramek RealPG | PASS, 1/1; pięć negatywów i pięć trwałych przejść |
| ApiGateway + JWT + RealPG | PASS, 3/3: obca organizacja 404, rola bez zgody 403, Sponsor 200 + SQL readback |
| rollback atomowej transakcji | PASS, 1/1 |
| `git diff --check origin/integracja/20260911...HEAD` | PASS |

Finalny dowód zachowania w przeglądarce pokazuje tę samą tabelę pięciu bramek w jasnym i ciemnym motywie:

- `evidence/f2-3-pmo/e3/01_stage_gates_dark.png` — SHA-256 `307c225cf3689862eb636d174328a1824bb62d3ebc278ae6d60ac43641863b0f`;
- `evidence/f2-3-pmo/e3/02_stage_gates_light.png` — SHA-256 `ebd0d4b2fcb0f407549563da6284e08e9eb50234666fadf2fcdd231f6b68ba7c`.

Katalog `evidence/f2-3-pmo` ma 1,040,887 B w 31 plikach łącznie z manifestem, czyli pozostaje poniżej limitu 2 MiB. Usunięto redundantne stare logi, buildy oraz zrzuty stanów pośrednich; zachowano finalne dowody zachowania.

## Granice

- Flaga chroni powierzchnię UI; istniejąca rodzina backendowa `/api/stage-gates` pozostaje wspólna dla wcześniejszych wywołań.
- E3 nie implementuje propozycji przejść H1b, polityk akceptacji E4 ani filtrów projektowych fali F.
- Kandydat nie jest scalony ani wdrożony. Decyzję podejmuje CTO po świeżym niezależnym przeglądzie.
