# P2 Plan — E1 delivery note

**Verdict: E1 is ready for independent review; no migration is required.**

Branch: `codex/plan-inicjatyw-20260914`

Base: `13070169a439985e34db2d8daa9807cb26870599` (`origin/integracja/20260911`)

Decision marker: `[ODMROZENIE 05_INITIATIVES DEC-497]`

## Delivered

E1 adds a feature-gated AI analysis path to the existing Plan analysis command. The service sends the exact versioned plan horizon and tenant-scoped initiative snapshot to the configured premium LLM, validates the returned dependency graph, separates absolute and conditional paths, and rejects ungrounded or cyclic output. A successful result is stored in the existing versioned `plan_analysis_proposal` aggregate with its model, analysis timestamp, scenario version, observations, and critical paths.

The runtime reads source fields and existing edges from PostgreSQL. It refuses incomplete plan context. Provider failure returns `AI_UNAVAILABLE`; invalid model output returns `AI_RESPONSE_INVALID`. There is no fallback that presents generated or solver data as AI analysis.

`VITE_INITIATIVES_PLAN_ANALYSIS=false` is documented and production behavior remains fail-closed unless the value is exactly `true`. The request schema defaults to the existing `SOLVER` path, so the OFF line retains existing behavior.

## Canonical UI decision

The existing `src/components/Initiatives/gantt/InitiativeGantt.tsx` was measured before implementation. It already accepts dependency connectors and critical-path IDs over normalized schedule items. E2/E3 should adapt accepted proposals into that component; E1 creates no competing Gantt.

## Evidence

Evidence is under `evidence/f2-p2-plan/e1/`:

- `focused-tests.log`: 3 focused files, 9 tests, all passed against the package PostgreSQL instance;
- `realpg-environment.log`: PostgreSQL identity, migration count, and canonical tables;
- `server-tsc.log`, `service-esbuild.log`, `route-esbuild.log`: static gates passed;
- `GANTT_MEASUREMENT.md`: measured reuse contract;
- `REAL_PROVIDER_PROBE.md`: successful external-provider response is honestly `NOT_PROVEN` because this environment has no configured premium provider.

## Review boundary

This author does not accept the package. E1 stops at the freeze manifest for independent review. E2/E3 remain unopened until CTO approval.
