# Odbiór CTO 05.09 — Partner (16) · Logowanie/Rejestracja (19)

Inspektor: agent CTO-odbiorca-i-naprawiacz. Worktree napraw: `/private/tmp/ag-cto-partner`
(gałąź `agent/cto-pass-partner`, bazowany na HEAD m03 `e9b7f9e842`), własny vite `:3035`
wskazujący na `https://staging.consultify.ai` (ten sam backend/dane co realna aplikacja
`:3000`) dla ekranów publicznych i stanu "niepodłączony". Dla stanu "partner podłączony"
(25 sekcji portalu) użyto istniejącego dev-render harnessu `dev-render/screens/partner-portal.tsx`
(+ `partner-settlements-view.tsx`) na osobnym vite `:3038` — mocuje REALNY komponent
`<PartnerPortalViewNew />` z danymi demo „Zenit Consulting Sp. z o.o.", zgodnie z CLAUDE.md #7
(żaden ekran modułu 16 nie miał dotąd wpisu w rejestrze grafiki — `katalogi: []` w
`scripts/mvp-final/moduly.mjs`, więc to pierwszy przebieg, nie porównanie z zatwierdzonym
obrazem).

## Zakres i metoda

Konto właściciela na stagingu NIE jest podłączonym partnerem (poprawne zachowanie —
`/partner/*` gate to wyłącznie `requireAuth`, patrz komentarz bezpieczeństwa w
`AppRoutes.tsx:3644`), więc:
1. Ekrany publiczne/niepodłączone (`/partner`, `/partner/pricing` → `/become-partner`,
   `/become-partner/apply`) — zweryfikowane na ŻYWO z `:3035`.
2. 12 wariantów portalu partnera (Start ×3, Pulpit, Polecenia ×2, Klienci/Organizacje ×2,
   Prowizje, Akademia, Zasoby, Profil) — zweryfikowane przez dev-render `:3038` (realny
   komponent, mock danych — jedyny bezpieczny sposób zobaczyć te ekrany bez podłączania
   prawdziwego konta partnerskiego właściciela).
3. Logowanie/Rejestracja/Reset hasła — świeży kontekst Playwright BEZ `ODBIOR_AUTH_STATE`,
   `:3035`, zgodnie z zasadą „nie loguj się, żeby zobaczyć ekran logowania".

## Liczby

| Pakiet | Ekranów sprawdzonych | OK | Naprawione | Poza zakresem (zgłoszone) |
|---|---|---|---|---|
| 16 Partner — publiczne/niepodłączone | 3 | 3 | 1 (`/become-partner/apply`) | 0 |
| 16 Partner — portal podłączony (dev-render) | 13 | 11 | 3 (dashboard, referral-tools, resources×4, earnings) | 2 (patrz niżej) |
| 19 Logowanie/Rejestracja/Reset | 5 | 5 | 0 | 0 |
| **Razem** | **21** | **19** | **4 pliki kodu + 1 plik i18n** | **2** |

(„Naprawione" liczy PLIKI/ekrany dotknięte, nie pojedyncze stringi — patrz commit
`1f2f031911` w worktree za pełną listę).

## Partner — ekrany publiczne/niepodłączone

| Ekran | Trasa | Werdykt PRZED | Naprawa | Werdykt PO |
|---|---|---|---|---|
| Portal Partnerski, stan niepodłączony | `/partner` | ZGODNY | — | ZGODNY — polski, karta „Profil partnera nie jest jeszcze podłączony", boczne menu poprawnie wyszarzone |
| Strona programu partnerskiego (marketing) | `/become-partner` (cel przekierowania `/partner/pricing`) | ZGODNY | — | ZGODNY — 6 ścieżek partnerskich, 5 modeli współpracy, FAQ, zero błędów konsoli, zero angielskich etykiet stałych |
| Formularz zgłoszeniowy partnera | `/become-partner/apply` | **DEFEKT ×2** | Naprawiono | ZGODNY |

### Defekt 1 — hero renderował się z białym tekstem na białym tle
`src/views/PartnerApplicationView.tsx:77`. Ciemne tło hero (gradient granatowy) miało
`-z-10` na dziecku, a rodzic `<section>` miał tylko `relative` (bez `z-10`) — bez własnego
kontekstu stackingu ujemny z-index dziecka spychał je POD białe tło strony (`<body>`),
więc cały nagłówek + 3-punktowa lista + tytuł formularza (białe/jasne kolory tekstu na
zakładanym ciemnym tle) był praktycznie nieczytelny. Wzorzec poprawny w tym samym repo:
`EpicHeroSection.tsx:130` (`relative z-10` na sekcji). Naprawa: dodano `z-10` do klasy
sekcji. Dowód: `zrzuty/become-partner-apply-po.png` — ciemne tło widoczne, tekst czytelny.

### Defekt 2 — cały formularz zgłoszeniowy po angielsku
Ten sam plik: badge/tytuł/podtytuł strony ładnie po polsku przez `t()`, ale CAŁY blok
formularza (`Public lead form`, `Tell us how you want to grow…`, etykiety `Full name` /
`Business email` / `Company` / `Website` / `Country` / `Your role` / `Team size` /
`Primary focus area` / `Why do you want to join?`, placeholder, 3-punktowa lista korzyści,
komunikat błędu) był na sztywno po angielsku. Naprawa: owinięto w `t()` z nowymi kluczami
`partner.apply.*` w `public/locales/pl/translation.json`. Dowód: `become-partner-apply-po.png`.

## Partner — portal podłączony (13 ekranów, dev-render)

| Ekran (wariant) | Werdykt PRZED | Naprawa | Werdykt PO |
|---|---|---|---|
| Start — niepodłączony | ZGODNY | — | ZGODNY |
| Start — aktywny (saldo + następny krok) | ZGODNY | — | ZGODNY |
| Start — błąd połączenia | ⚠ patrz uwaga | — | bez zmian kodu |
| Pulpit (Dashboard) | **DEFEKT** | Naprawiono | ZGODNY |
| Polecenia / Moje linki i kody (wypełnione) | **DEFEKT** | Naprawiono | ZGODNY |
| Polecenia / Moje linki i kody (puste) | ⚠ fixture harnessu | — | patrz uwaga |
| Klienci / Organizacje (wypełnione) | ZGODNY (drobna uwaga nagłówka) | — | ZGODNY |
| Klienci / Organizacje (puste) | ⚠ fixture harnessu | — | patrz uwaga |
| Prowizje | **DEFEKT** | Naprawiono | ZGODNY |
| Akademia / Ścieżka nauki | ZGODNY | — | ZGODNY |
| Zasoby / Dokumentacja | **DEFEKT** | Naprawiono | ZGODNY |
| Zasoby / Materiały marketingowe | **DEFEKT** (ten sam plik) | Naprawiono | ZGODNY |
| Zasoby / Studia przypadków | **DEFEKT** (ten sam plik) | Naprawiono | ZGODNY |
| Zasoby / Szablony PMO | **DEFEKT** (ten sam plik) | Naprawiono | ZGODNY |
| Profil / Dane firmy | ZGODNY | — | ZGODNY |

### Defekt 3 — Pulpit i Polecenia: opisy kafli KPI po angielsku + zły format waluty
`src/components/Partner/PartnerRuntimeSummaryStrip.tsx` (Pulpit) i
`src/views/partner/sections/ReferralToolsSection.tsx` (Polecenia): drugorzędny opis pod
każdą liczbą KPI był budowany szablonem string bez `t()` — „412 unique", „66 signups",
„41 trials", „3 sources", „2 tracked days", „earn lifecycle". Dodatkowo kwota „Gotowe do
wypłaty" używała `.toLocaleString()` bez locale i stawiała kod waluty PRZED liczbą
(„PLN 10,800" — amerykański format przecinka) zamiast konwencji reszty apki
(„10 800,00 zł" / liczba + waluta PO, `Intl.NumberFormat('pl-PL', …)`, wzorzec z
`PartnerCanonicalRuntimePanel.tsx:130`). Naprawiono oba pliki + dodano etykietę fazy
cyklu życia partnera przez tę samą mapę `lifecycle.*` co kanoniczny panel ([]„earn" →
„Rozliczenia", zamiast surowego angielskiego słowa). Dowód: `dr-dashboard-po.png`,
`dr-referral-tools-po.png` — teraz „412 unikalnych", „66 rejestracji", „41 okresów
próbnych", „3 źródeł", „2 śledzonych dni", „etap: Rozliczenia", „10 800,00 PLN".

### Defekt 4 — Prowizje: kolumna „Typ" pokazywała surowy enum po angielsku
`src/views/partner/sections/EarningsSection.tsx:878` renderowała `tx.transactionType`
przez `.toLowerCase().replace('_',' ')` + CSS `capitalize` — bez tłumaczenia: „Subscription
Renewal", „New contract" w tabeli złożonej wyłącznie z polskich etykiet (klient, status,
data). Naprawiono: mapa `TRANSACTION_TYPE_LABEL_FALLBACK` + klucze
`partner.earnings.transactionType.*` w pl.json, zgodne z kanonicznym enumem
`server/src/services/partnerCommissionService.ts` (`INITIAL/SUBSCRIPTION/RENEWAL/UPSELL/
ONE_TIME/REFUND/BONUS`) plus warianty z danych demo (`SUBSCRIPTION_RENEWAL`,
`NEW_CONTRACT`). Dowód: `dr-earnings-po.png` — „Odnowienie subskrypcji", „Nowy kontrakt".

### Defekt 5 — Zasoby: nagłówki 4 podstron po angielsku mimo polskiego menu
`PartnerPortalView.tsx:2353` (`ResourcesSection`) miał osobną mapę `titles` na sztywno po
angielsku („Documentation", „Marketing Materials", „Case Studies", „PMO Templates"), mimo
że dokładnie te same etykiety w bocznym menu (`partner.sidebar.documentation` itd.) były
już poprawnie po polsku. Naprawiono: mapa `titles` reużywa tych samych kluczy `t()`.
Dowód: `dr-resources-documentation-po.png`, `-marketing-po.png`, `-casestudies-po.png`,
`-templates-po.png` — nagłówek karty = etykieta menu, jak w obrazie kanonu.

### Poboczna naprawa (bez ekranu w dev-render): `ClientAccessView.tsx`
Ta sama rodzina błędu (angielskie fallbacki gdy brakuje danych: „Organization", „Team
Member", „Member", „partner access") w funkcjach normalizujących `normalizeClient` /
`normalizeEmployee` — poza kontekstem komponentu, więc naprawiono przez `i18n.t()`
(wzorzec z `services/api.ts`, `hooks/useAIStream.ts`), nie hookiem. Zweryfikowane tylko
przez `esbuild` — ekran wymaga realnego rekordu partnera z brakującą nazwą klienta, czego
harness nie odtwarza; ryzyko regresji niskie (zmiana 1:1 zastępuje literal domyślną
wartością z `t()` o identycznym angielskim fallbacku).

### Uwaga — Start, błąd połączenia (bez naprawy)
Wariant `partner-start-error` renderuje się IDENTYCZNIE jak `partner-start-active` (te
same kwoty/saldo) zamiast dedykowanego stanu błędu. Nie potwierdziłem, czy to fixture
dev-render nie ustawia właściwego stanu API, czy komponent faktycznie nie ma osobnego
renderu dla `connectionState==='error'` poza kartą „Start" (widziałem tylko sam ten
ekran, nie **kod** ścieżki błędu) — zostawiam jako pytanie otwarte, NIE fix na ślepo.

### Uwaga — warianty „puste" w dev-render nie są puste (fixture, nie produkt)
`partner-referral-tools-empty` i `partner-organizations-empty` renderują te same
wypełnione dane demo co warianty `-filled` (3 wiersze kampanii / 3 organizacje zamiast
0). To błąd danych fixture w `dev-render/screens/partner-portal.tsx` (wariant nie
rozgałęzia stubu fetch), NIE błąd produktu — realny pusty stan (`noCampaigns`,
`noReferredCustomers` — klucze i18n już istnieją i są po polsku) nie został tu
zweryfikowany na żywo. Nie naprawiłem (dev-only tooling, poza bezpiecznym zakresem tej
tury; grozi zepsuciem czegoś, czego nie zdążę ponownie zweryfikować).

## Poza zakresem modułu Partner (zgłoszone, NIE naprawione)

1. **`src/views/superadmin/revenue/PartnerSettlementsView.tsx`** (moduł SuperAdmin, ekran
   „Rozliczenia z partnerami") ma DOKŁADNIE ten sam błąd co Defekt 4 — kolumna „Typ"
   pokazuje `Subscription` / `One Time` po angielsku bez tłumaczenia. Trafiłem na ten
   ekran przez dev-render (był omyłkowo w tej samej liście wariantów partnera), ale to
   nie jest trasa `/partner/*` — należy do odbioru SuperAdmin, nie tego pakietu.
2. Drobna: nagłówek kolumny „UŻYTKOWNICY" w tabeli Organizacje ucina się do
   „UŻYTKOWN…" przy domyślnej szerokości — komponent współdzielony (`FilterableTable`),
   niepewne czy to defekt Partnera czy ogólny (nie badałem innych modułów w tej turze).

## Logowanie / Rejestracja / Reset hasła (19) — niezależna weryfikacja

Wszystkie 5 ekranów sprawdzone świeżym kontekstem Playwright BEZ sesji — potwierdzają
werdykt poprzedniej rundy (`evidence/odbior-zywo-20260905/19-logowanie/RAPORT.md`,
5/5 ZGODNY): logowanie, rejestracja, „Zapomniałeś hasła", ustawianie nowego hasła
(z `?token=`) i błąd „Nieprawidłowy lub brakujący token resetu" (bez `?token=`) — wszystkie
po polsku, CTA neutralne (białe), linki niebieskie, czerwień tylko na realnym komunikacie
błędu (semantyka krytyczna, nie CTA). Zero błędów konsoli na wszystkich 5. Brak naprawy
potrzebnej.

## Weryfikacja techniczna

`esbuild` per plik na wszystkich 6 dotkniętych plikach kodu + walidacja JSON
`translation.json` — OK. `npx vitest run` na jedynym istniejącym teście dotykającym
zmienionych plików (`PartnerPortalView.organizationsColumnWidth.day224.test.tsx`) — 1/1
PASS. `scripts/check-list-canon.sh` — dług NIE rośnie (spadek o 3 na pełnym skanie).
`scripts/check-artefakt.sh` — dług NIE rośnie (crimson w powłoce artefaktów: 8, baseline
9). Commit w worktree: `1f2f031911` na gałęzi `agent/cto-pass-partner`
(`/private/tmp/ag-cto-partner`), NIE scalone do `demo`/`Londyn` — czeka na przegląd.

## Zdanie dla właściciela (Partner)

Publiczna strona programu partnerskiego i formularz zgłoszeniowy są teraz w całości po
polsku i czytelne (naprawiłem ciemne tło, które renderowało się jako białe na białym);
sam portal partnera dla podłączonego partnera wygląda dobrze i zgodnie z resztą apki po
naprawie pięciu miejsc, gdzie liczby i etykiety wyświetlały się po angielsku albo w
amerykańskim formacie kwoty — nie widziałem tego wcześniej na żywo, bo żaden ekran tego
modułu nie miał jeszcze Twojej akceptacji na obrazku, więc to pierwszy niezależny
przegląd, nie porównanie z Twoim „tak".

## Zdanie dla właściciela (Logowanie/Rejestracja)

Ekrany logowania, rejestracji i resetu hasła są czyste i zgodne z poprzednim odbiorem —
nic nowego do naprawy, wszystko po polsku, kolory poprawne.
