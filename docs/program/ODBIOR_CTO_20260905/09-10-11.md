# Odbiór CTO 05.09 — Finanse (09) · Materiały (10) · Audyty (11)

Inspektor: agent CTO-odbiorca-i-naprawiacz. Worktree napraw: `/private/tmp/ag-cto-fma`
(gałąź `agent/cto-pass-fma`, HEAD `78f9e596ec`), własny vite `:3033` wskazujący na
`https://staging.consultify.ai` (ten sam backend/dane co realna aplikacja `:3000`).
Wykluczone z zakresu: pliki importera pakietu sprawozdań („statement-pack") w Finansach —
naprawia je osobny agent.

## Metoda

Ten pakiet trzech modułów miał już za sobą **7 rund** niezależnego odbioru na żywo
(`evidence/odbior-zywo-20260905/{09-finanse,10-materialy,11-audyty}/wyniki.json` +
`RAPORT.md`), z realnymi naprawami potwierdzonymi między rundami (redeploy stagingu
b852ade6→770f9e4991 w trakcie sesji, kilka napraw frontendowych i jedna zdalna flaga).
Zamiast powtarzać od zera 53 zrzuty, przyjąłem tamten materiał jako punkt startowy,
zweryfikowałem NAJNOWSZY werdykt każdego ekranu (po numerze rundy) i:
1. dla wszystkiego, co już ZGODNE/NOWY_WZORZEC/DANE(kompozycja OK) — potwierdziłem bez
   ponownego zrzucania (zero ryzyka regresji, oszczędność wobec identycznych danych);
2. dla pozostałych realnych różnic — przeczytałem wskazany kod, oceniłem czy przyczyna
   jest frontendowa i bezpieczna do naprawy w tej turze, i albo naprawiłem +
   zweryfikowałem własnym zrzutem z `:3033`, albo zostawiłem jako DEFEKT ze specyfikacją.

## Liczby

| Moduł | Ekranów | OK (zgodne/nowy wzorzec/dane-only) | Naprawione w tej turze | Pozostałe DEFEKT |
|---|---|---|---|---|
| 09 Finanse (bez importera sprawozdań) | 13 | 9 | 0 (patrz niżej — zbadane, brak bezpiecznej naprawy frontendowej) | 4 |
| 10 Materiały | 36 | 34 | 1 (`report-builder-library-template` — i18n) | 1 (+1 do decyzji właściciela) |
| 11 Audyty | 4 | 4 | 0 | 0 |
| **Razem** | **53** | **47** | **1** | **5** (+1 decyzja) |

## 09 Finanse — 13 ekranów

### OK (9) — bez zmian, potwierdzone na najnowszym zrzucie/opisie rundy
`finance-hub`, `finance-model-workspace`, `finance-comments-panel` (runda 5),
`finance-lineage-navigator` (runda 2), `finance-workspace-bar` (runda 5),
`finance-saved-views-panel` (runda 2), `finance-export-import-panel` (runda 2),
`finance-analysis-workspace` (runda 3 — kompozycja V3 zgodna, brak KPI to dane),
`finance-statement-pack-workspace-v2` (runda 3 — WYKLUCZONE z mojego zakresu:
importer sprawozdań, inny agent; kompozycyjnie już zgodne, różnica czysto danowa).

### DEFEKT — pozostałe po zbadaniu (4), wszystkie wymagają pracy backendowej

1. **`finance-compare-panel`** — panel porównania nie renderuje się, bo
   `FinanceWorkspaceUtilities.tsx:57-60` montuje go tylko dla artefaktu z drugą
   wersją biznesową; żaden z 3 kanonicznych artefaktów wyceny w organizacji
   stagingu takiej drugiej wersji nie ma. Naprawa wymaga NOWEGO endpointu
   backendowego tworzącego powiązanie Baseline/Scenario — nie frontend.
2. **`finance-prediction-workspace`** — sporadyczny (~6% prób, zmierzone 1/16
   świeżych otwarć na gitSha 770f9e4991) pusty biały ekran zamiast widoku
   predykcji. Zbadałem `src/components/Economics/FinanceHub.tsx` (branch
   `fullView` ok. linii 4019-4023, ten sam kod co znany incydent FIX-4 z
   2026-08-25) — hipoteza wskazuje na wyścig między routingiem do `fullView`
   a `lazy()`-owanym `FinanceV3PredictionWorkspace`/`FinanceWorkspaceUtilities`,
   ale bez żywego DevTools Profilera (poza zakresem tej tury — brak
   deterministycznej reprodukcji w headless) nie da się bezpiecznie wskazać i
   naprawić dokładnej linii bez ryzyka wprowadzenia nowej regresji w kodzie
   renderowanym dla WSZYSTKICH warsztatów finansowych. Zostawione jako DEFEKT
   ze specyfikacją do zbadania pod realnym profilerem.
3. **`finance-valuation-workspace`** — zakładka „Wyniki" zablokowana dla
   WSZYSTKICH rekordów wyceny w organizacji komunikatem wprost z kodu:
   „Ten wariant nie ma dziś zapisanego powiązania (lineage edge) z żadną
   wersją Baseline/Scenario" (`NO_VALUATION_SOURCE_EDGE`). Wymaga nowego
   endpointu backendowego tworzącego to powiązanie — poza zakresem frontendu.
4. **`finance-baseline-workspace`** (7 rund, wciąż otwarty) — flaga
   `financeBaselineWorkspaceV1` jest już włączona i szkielet V3 się renderuje,
   ale KAŻDY sposób utworzenia nowego modelu bazowego (i „Oprzyj na
   sprawozdaniu", i „Rozpocznij od zera") kończy się `409
   BASELINE_CONTEXT_NOT_CONFIGURED`, bo `CreateModelModal.tsx` (funkcja
   `handleCreate`) tworzy tylko starszy wiersz `financial_models` i NIGDY nie
   woła `PUT /api/v8/finance-v2/baseline/:businessVersionId/context`
   (`configureBaselineWorkspaceContext`,
   `server/src/services/finance/canonical/baselineContextService.ts`).
   Sprawdziłem: dopisanie tego wywołania z frontendu jest TECHNICZNIE możliwe
   (endpoint już istnieje), ale wymaga poprawnego doboru `entityId`,
   `openingBalanceSheetPeriodId`, `forecastPeriodIds` i `assumptionRowOrder` —
   to logika domenowa finansów (dopasowanie okresów sprawozdania/prognozy),
   nie mechaniczna zmiana UI. Ryzyko wysłania złego payloadu i zepsucia
   raz utworzonego modelu bazowego jest wyższe niż wartość naprawy „na ślepo"
   w tej turze — zostawiam jako DEFEKT z gotową specyfikacją dla dedykowanej
   sesji finansowej (patrz `evidence/odbior-zywo-20260905/09-finanse/wyniki.json`,
   wpis `finance-baseline-workspace`, pole `opis`, runda 7 — zawiera dokładne
   miejsca w kodzie i proponowany kształt wywołania).

## 10 Materiały — 36 ekranów

### OK (34)
27 ekranów ZGODNYCH bez zastrzeżeń + 5 oznaczonych `NOWY_WZORZEC` — to NIE są
defekty: to ekrany, gdzie produkt już poprawnie wdrożył PÓŹNIEJSZĄ decyzję
właściciela (pasek Excela ze słów na ikony; wyśrodkowane karty błędu Document
Studio), a jedynie zatwierdzony obraz referencyjny z 30.08 jest przeterminowany
(`sheet-artifact`, `excele-prawy-panel-standard`, `excele-edytowalna-siatka`,
`document-studio-resume-error`, `document-studio-template-resolve-error`) —
do zrobienia: podmienić obraz referencyjny na świeży zrzut, nie zmieniać kodu.
Plus `report-builder-library-template` (naprawione niżej) i
`document-studio-blocks-i18n` (`BRAK_W_APLIKACJI` — potwierdzone w kodzie, że
zatwierdzony obraz to wyłącznie kompozycja z dev-render harnessu, trzy bloki w
aplikacji zawsze żyją osobno; nie jest to defekt, tylko nieistniejący w
produkcie ekran-referencja).

### NAPRAWIONE w tej turze (1)

**`report-builder-library-template`** — kreator „Nowy raport z wzorca" pokazywał
angielskie nazwy/opisy sześciu kanonicznych wzorców systemowych źródła
ASSESSMENT, bo żaden z nich nie ma w bazie polskiej wersji tekstu
(`report_builder_templates.name/description`, zweryfikowane
`GET /api/report-builder/templates/:id/details` dla wszystkich sześciu:
`tpl-assessment-default`, `tpl-assessment-summary-v2`, `tpl-drd-board-report-v2`,
`tpl-drd-full-diagnostic-v2`, `tpl-final-transformation-report`,
`tpl-strategic-review-exec`). Naprawa: warstwa wyświetlania
`LIBRARY_TEMPLATE_PL_COPY` w `src/views/ReportBuilderView.tsx` — gdy interfejs
jest po polsku, karta szablonu pokazuje polskie tłumaczenie; nieznane
(organizacyjne) wzorce nadal pokazują to, co zwróci serwer. Zweryfikowane
świeżym zrzutem z `:3033`
(`evidence/odbior-cto-20260905/materialy/report-builder-library-template-po.png`):
karta pokazuje teraz „Raport dojrzałości cyfrowej DRD — pełna diagnoza" +
polski opis. Pozostała różnica na tym ekranie jest czysto danowa (rozwijana
lista ocen jest pusta, bo żaden assessment DBR77 nie ma statusu
„Zatwierdzony") — nie do naprawy frontendem. Commit:
`78f9e596ec` w gałęzi `agent/cto-pass-fma`.

### Do decyzji właściciela (1) — NIE naprawiane w tej turze

**`document-studio-ai-teresa`** — realny panel „Z AI" w Document Studio to dziś
brief + przycisk do globalnej Teresy, NIE czat Teresy osadzony w panelu.
Zatwierdzony obraz referencyjny pokazuje osadzony czat. Notatka z poprzedniej
rundy przywołuje `KANON_Z_ODBIOROW.md:317` z decyzją właściciela z 01.09 „JEDNA
TERESA W SWOIM OKNIE" (dwa odrzucenia osadzonego czatu). **To stoi w
sprzeczności** z zapisem w moim zleceniu („Document Studio is the owner's
confirmed exception: Teresa embedded in its right panel"). Nie rozstrzygam tej
sprzeczności sam — zmiana architektury Teresy (osobne okno vs panel) to decyzja
produktowa, nie defekt UI do cichej naprawy. **Do wyjaśnienia z właścicielem:
która decyzja jest aktualna.**

## 11 Audyty — 4 ekrany

Wszystkie 4 (`audyty-warsztat-kryterium`, `audyty-piec-powierzchni`,
`audyty-raport-dokument`, `audyty-drd-report`) są dziś **ZGODNE** (rundy 2-5) —
moduł przeszedł od stanu „wszystko puste" (runda 1) do pełnej zgodności po
zaseedowaniu pakietu audytowego i włączeniu flagi `ff_auditsReportChain`.
Brak pracy do wykonania w tej turze.

## Weryfikacja naprawy

- `npx esbuild src/views/ReportBuilderView.tsx --bundle` — kompiluje się bez błędów.
- `bash scripts/check-list-canon.sh` — OK, dług nie rośnie (spadek o 3).
- `bash scripts/check-artefakt.sh` — OK, dług nie rośnie.
- Świeży zrzut z własnego vite `:3033` (ta sama trasa, ten sam rekord co w
  rundzie 7) potwierdza polską nazwę i opis szablonu.
- Brak testu jednostkowego dla `ReportBuilderView.tsx` w repo (sprawdzone
  `find` po nazwie) — weryfikacja oparta o kompilację + zrzut na żywo.

## Pozostały dług (do przekazania)

1. `finance-compare-panel` — potrzebny endpoint tworzący drugą wersję biznesową (backend).
2. `finance-prediction-workspace` — sporadyczny pusty render (~6%), potrzebna sesja z DevTools Profilerem.
3. `finance-valuation-workspace` — potrzebny endpoint tworzący lineage edge Baseline/Scenario→Valuation (backend).
4. `finance-baseline-workspace` — `CreateModelModal.handleCreate` nigdy nie woła `configureBaselineWorkspaceContext`; specyfikacja gotowa w `evidence/odbior-zywo-20260905/09-finanse/wyniki.json` (runda 7).
5. Pięć obrazów referencyjnych w Materiałach jest przeterminowanych (`sheet-artifact`, `excele-prawy-panel-standard`, `excele-edytowalna-siatka`, `document-studio-resume-error`, `document-studio-template-resolve-error`) — do podmiany na świeże zrzuty, nie do naprawy kodu.
6. `document-studio-ai-teresa` — sprzeczna decyzja o architekturze Teresy (osadzona vs własne okno) — do rozstrzygnięcia z właścicielem.
