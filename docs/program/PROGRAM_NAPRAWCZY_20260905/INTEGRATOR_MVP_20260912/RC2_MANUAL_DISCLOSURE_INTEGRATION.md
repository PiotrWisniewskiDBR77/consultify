# RC2 canonical current-content and export disclosure integration

Base6038c2ae78db0b2acde777ed749eab94a8ad6323. Seven canonical files reconciled from cf0848→459069; existing modified files first verified byte-identical to prerequisite cf0848. No wholesale held-base merge. Root transaction cleanup, authorization, Interview privacy, deletion and legal-hold behavior retained. RC1 remains immutable; no deployment.

Bounded acceptance: MANUAL_HUB current proposal/initiative content requires verified same-tenant project, source provenance, sole self-reference and matching registration. Unsupported/private/historical/typed children retain unresolved lineage. Independent author-source13unit+1Gateway ACCEPT; root configured combined Gateway1/1PASS with actual project/Submit/Register/Amend/read/JSON/CSV and storage readback. Initial root invocation failed test-only flag guard, then correctly configured run passed; not a product failure/waived test. Flags not changed in product defaults.

Existing Superadmin JSON action preserves original Blob and shows complete/partial/unknown in role=status. Wrong tenant/malformed JSON rejects download, structured envelope/date/counts validated before COMPLETE. Global in-flight guard prevents concurrent second row export. CSV keeps table,row_index,data_json and business records/counts, adds reserved __consultify_export_manifest_v1 metadata row. Consumers treating every row as business must explicitly skip this metadata identity. Identity collision rejects rather than overwriting data. Personal GDPR unchanged; organization self-service UI is still required.

Evidence: root mounted22/22PASS (8previous+14export), independent22/22; CSV11/11 independent/root, with Python standard-library csv parser (Python3 required for this test); combined backend30/30 across canonical13/export11/Interview6. Root actual Gateway1/1. No double-counting CSV11 as additional to backend30. Duplicate guard mutant1FAIL21PASS→restored22; CSVmanifest omission1FAIL10PASS→restored11; prior generic-success mutation4FAIL10PASS→restored14 (older denominator). React act warnings retained. Source-reviewed format-only changes follow independent acceptance, no intended behavior delta.

Servertsc0; frontend tsc exit2 with192vs192 equal file/code counts and no additions (not clean TypeScript). Initial build51456 terminated134 at default4GiB Vite heap (FATAL log retained). Retry34350 NODE_OPTIONS=--max-old-space-size=8192 npm run build exited0 with unchanged product source; index SHA256 9e7d7735cb704d1a82910012cf8df3578e1d5557ba752ebbae25b21c6f6e4d07. Chunk warnings retained; not a cold-start performance result. Full built-browser actual download, self-service admin entry, complete business export, deletion and full E4/MVP remain OPEN. Receipt/manifest/source hashes remain in external integrator handoff.
