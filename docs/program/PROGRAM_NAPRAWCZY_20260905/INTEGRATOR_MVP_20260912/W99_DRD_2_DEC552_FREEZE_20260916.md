# DRD-2 · DEC-552 — freeze 2026-09-16

**Werdykt: READY FOR CTO REVIEW.** Paczka bazuje na `9cf10528e3`, jest osłonięta flagą `VITE_DRD_INTERVIEW_V2` domyślnie OFF i nie zawiera migracji ani zapisów stagingowych.

## Zachowanie

- Kanoniczny `MethodWorkspaceShell` dostał opcjonalny slot treści Interview; OFF zachowuje dotychczasowy `InterviewFocusPanel` bez zmiany.
- DRD ON pokazuje trzy kolumny: istniejący `MethodNavigator`, listę poziomów bieżącego obszaru oraz jedno okno poziomu z trzema pytaniami pomocniczymi, jednym polem odpowiedzi i paskiem dowodów.
- Yes zapisuje `ANSWER_CONFIRMED/confirmed` i otwiera N+1. No zapisuje `ANSWER_CONFIRMED/no`, zachowuje current=N-1, wygasza wyższe poziomy i przechodzi do następnego obszaru. No jest neutralne kolorystycznie.
- I need help pobiera roster sesji, wybiera rolę `evidence_owner` (fallback: właściciel sesji), tworzy idempotentne zadanie `/tasks`, zapisuje oznaczone `ANSWER_CONFIRMED/dont_know` i dodaje blocker freeze aż późniejsza decyzja Yes/No zastąpi Help.
- Klik komórki macierzy ustawia dokładny unit/level i przełącza na to samo okno Interview. Mechanika progresji `drdAdapter.resolveOpenLevels` i DEC-544–548 nie została przepisana.
- Kopia zaakceptowanej makiety działa jako dev-render produktu: `dev-render/screens/u19-drd-trzy-kolumny.tsx`.

## Dowody

- Delta + importerzy: 5 plików testowych, **25/25 PASS**, `--retry=0`, jeden worker.
- Pełny frontend TypeScript bez limitu: baza **169**, kandydat **169**, zero diagnostyk w zmienionych ścieżkach.
- Pełny server TypeScript bez limitu: **0**.
- `check:flagi:dockerfile`: PASS, `analyzedFlags=194`, `dockerArgs=205`, `wyjatki=13`, `brakujace=0`.
- Esbuild zmienionych wejść i dev-render: PASS. `diff --check`: PASS.
- Render readback: HTTP 200, błędy konsoli 0, page errors 0, overflow poziomy 0, stary `InterviewFocusPanel` 0, kolumna poziomów 1, workspace poziomu 1, textarea 1.
- Obejrzane zrzuty Northwind EN 1440×900:
  - `evidence/drd-2-dec552/northwind-en-light-1440x900.png`
  - `evidence/drd-2-dec552/northwind-en-dark-1440x900.png`
  - parametry i pomiar: `evidence/drd-2-dec552/render-readback.json`

## Granice

Nie uruchamiano flagi ani zapisu na stagingu. Nie wykonano deployu, migracji, zmian Railway ani pushu na gałęzie chronione. Lokalny test interakcji potwierdza wybór adresata i reguły projekcji; realne utworzenie zadania pozostaje do kliknięcia po integracji i włączeniu flagi przez CTO.
