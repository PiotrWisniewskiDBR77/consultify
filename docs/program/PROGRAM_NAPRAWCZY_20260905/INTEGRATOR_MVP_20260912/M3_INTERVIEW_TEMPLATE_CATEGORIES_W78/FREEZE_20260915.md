# M3 — Interview template categories — DEC-519 freeze

**READY FOR CTO REVIEW.** Product commit `01ab0287d1` implements DEC-519 on exact base `dcbd6c052a15f6ef65a6ec698bb0cbda4a7902fe`; CTO owns acceptance and independent review.

## Delivered behavior

- `interview_library_templates.format` is additive and nullable. The subject remains in `category`; format values are `pulse`, `standard`, or `deep_dive`.
- The 18 global V6 templates migrate to eight subject categories: strategy 4, operations 4, digital 2, data 1, finance 2, cost 1, commercial 2, people 2.
- Create, update, clone, publish snapshot, and API readback preserve `format` independently from `category`.
- Template Library and template preview show the localized subject category and separate localized format in EN/PL.
- Missing manager evidence in `AI SCORE` and `ESCALATION` renders as `—` with an EN/PL explanation of when or why evidence is absent.

## PostgreSQL evidence

Disposable local database: PostgreSQL 16 + pgvector, `cx-d-m3-pg`, port 6454. No staging/demo database was used.

- Fresh strict runner on an empty database: 920 migrations, exit 0. The strict runner excludes seed files by policy, so DEC-519 permits an exact zero-row schema-only no-op while rejecting every partial population from 1 through 17.
- After applying the canonical V6 seed: 18 global system templates; before migration `Pulse=2`, `Standard=15`, `Deep Dive=1`.
- First direct migration pass: `UPDATE 18` formats, `UPDATE 18` categories.
- Readback: `commercial=2, cost=1, data=1, digital=2, finance=2, operations=4, people=2, strategy=4`; formats `deep_dive=1, pulse=2, standard=15`.
- `Other category` denominator: 0 of 18 migrated rows. Every stored category has a localized UI label.
- Second direct migration pass: `UPDATE 0` formats, `UPDATE 0` categories.
- Additive rollback: `UPDATE 18`; categories return to `Deep Dive=1, Pulse=2, Standard=15`; `format` remains `text`, nullable, and 18 migrated values become NULL. Rollback contains no `DROP`.
- Reapply after rollback: `UPDATE 18` formats and `UPDATE 18` categories.

## Verification

- Targeted tests: 4 files, 17/17 passed with `--retry=0`.
- Per-file esbuild: InterviewController, publication service, InterviewHub, TemplateBuilder and InterviewTemplatePreview passed.
- TypeScript 5.8.3 using the shared `/Users/piotrwisniewski/Developer/Consultify/node_modules` symlink.
- Server TSC in the same shared toolchain: clean `dcbd6c052a` line 27 errors, M3 candidate 27 errors, identical paths; delta 0. CTO's earlier 0-error result is retained only as a historical reference because its worktree had a local `node_modules` directory instead of the intended root symlink.
- Front TSC in the same shared toolchain: clean line and M3 candidate both exceeded the required 120-second cap. Both counts are therefore `NOT_PROVEN`; CTO's earlier 177 count is retained only as a historical reference.
- UI screenshots: `NOT_PROVEN` in this package. The rendered component and localization tests pass, but no screenshot is claimed as acceptance evidence.

## Review boundary

Review `01ab0287d1` plus this freeze commit against `dcbd6c052a`. The migration affects only global V6 system templates and rejects partial V6 populations. Organization and private templates are unchanged. No deploy, staging write, Railway change, or protected checkout mutation occurred.
