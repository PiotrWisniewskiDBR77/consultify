# C6 export prerequisites on real RC1/RC2

Read-only exact comparison: RC1 `c4c67a677effb4c181c81cd8f1d28649f1073579` versus checkpoint `47e024f00b81a75884ca21fe27d96ec7133beec0`, via git show/diff. **47-only hunks are insufficient.** Previous NO NEW SOURCE BLOCKER applies to 47 on held base0025, not to an executable root integration. No code/runtime changes in this review.

## Minimal closed dependency list

1. Four new production files from exact47: organizationExportSnapshot.ts, organizationExportContract.ts, organizationExportMvpContract.ts, organizationExportService.ts. These provide snapshot ownership, explicit schema contract and qualified export engine. No dynamic privacy WIP from author's current filesystem.
2. organizationLifecycleService.ts: add the ENTIRE securityManifest type (absent in RC1), replace RC1's old exportOrganizationData implementation/MAX_ROWS_PER_TABLE with the new service re-export. Keep current assertNotReservedOrganizationId, CSV conversion and every deletion/discovery function unchanged. CSV already exists in RC1. New engine imports reserved-ID guard/result type from this module; do not remove them. Removed old 20k cap is a real behavior change; complete:false/unresolved is the honest completeness boundary, not full replacement business coverage.
3. OrgPoliciesService.ts: add PoolClient/acquirePgClient/uuid imports; extend OrgPoliciesError with POLICY_READ_FAILED and statusCode423/503; add lockOrganizationPolicy using pg_advisory_xact_lock(hashtextextended(org,0)); add requireNoLegalHoldInTransaction with default FOR SHARE and export-only lockRows:false; expose new helpers as needed. Most importantly replace canonical upsertOrgPolicy's queryHelpers read/write with a single pinned transaction: BEGIN→same advisory lock→SELECT FOR UPDATE→INSERT/UPDATE RETURNING→COMMIT, rollback/release. Without this writer prerequisite the export lock does not serialize policy changes. RC1 source search identifies this service as the runtime INSERT/UPDATE writer; superadmin policy route already delegates to it.
4. ownership.routes.ts: RC1 has no self-service export route or administrator helper. Add ONLY required imports (acquirePgClient, exportOrganizationData, organizationExportToCsv, OrgPoliciesError, withOrganizationExportSnapshot), persisted ACTIVE OWNER/ADMIN tenant helper, and GET /:orgId/export with wrapper/error mapping. Preserve existing verifyToken/apiAuthRateLimiter. Do NOT bring createHash/randomUUID, deleteOrganizationDataInTransaction, DELETE /:orgId, confirmation flow or deletion receipt writer. Prefer persisted membership role as authority; exact47 helper has JWT fallback when membership.role is empty, so explicitly test missing/invalid persisted roles and avoid granting authority from stale JWT.
5. superadmin.routes.ts: retain existing requireNoLegalHold import and deletion route unchanged. Add only OrgPoliciesError + snapshot wrapper imports, wrap existing export callback, map policy errors before attachment, remove export-only outer release (wrapper owns client). Do not replace imports wholesale: exact47 removed requireNoLegalHold only because its held deletion path changed. Root deletion still needs it.
6. Four exact47 test files: contract9, snapshot unit9, realPG7, Gateway5. Existing assertion helper, authentication middleware, ProjectController and projectOwnerMembershipService have no RC1→47 diff and need no transplant. Existing Gateway.ts:987–988 already mounts ownership at /api/organizations; no new route mount required. PostgresDatabase.ts differs because held base has application-transaction orchestration; acquirePgClient is already present in RC1 and export uses explicit pinned clients, so DO NOT transplant that unrelated database diff.

## Explicit exclusions and behavior boundaries

Global hasLegalHold fail-closed change in47 is NOT required by export's transactional reader and would change inherited deletion/error behavior. Keep RC1 global helper for this narrow integration; track its legacy fail-open separately. Do not import self-service deletion endpoint, deletion receipt migration, held superadmin deletion changes, AI budgets, application transaction adapter or unrelated98 report completion claims. Existing missing org_policies-table treatment (return without hold) is retained by the exact prerequisite, not a newly established policy decision. Schema/migration availability must be verified read-only before runtime; no receipt table is necessary for export.

The canonical policy writer conversion is an essential SHARED prerequisite, not an export-local cosmetic change. It changes its DB execution contract to pinned PostgreSQL and must be named in integration report and tests. Existing superadmin PUT policy endpoint must remain authorized and preserve retention/residency/hold patch semantics.

## Required merged-source acceptance

- Compile/typecheck imports and run exact9+9+7+5 on merged root source, preserving names, zero skipped and local DB identity guards. Final source hashes must differ honestly from author47 where root integration needs reconciliation.
- Real canonical policy writer absent INSERT and existing UPDATE must block export until commit, then HTTP423/no attachment for both relevant routes; export-first must block the writer. No fixture-only raw SQL stand-in for canonical upsert.
- Test policy writer partial patches: existing omitted values unchanged; explicit false/null persisted and read back; transaction errors rollback. Exercise existing authenticated superadmin policy route because its writer implementation changes on root.
- Newly introduced self-service endpoint: valid ACTIVE OWNER/ADMIN accepted, same-tenant MEMBER denied, inactive/missing membership denied, stale JWT ADMIN with persisted MEMBER denied, foreign org denied, unauthenticated denied. Empty persisted role must not gain authority through JWT fallback. Existing final5 only proves tenant denial and valid ADMIN, so this auth prerequisite denominator is additional and necessary on root.
- Preserve real project POST201/GET200→JSON+CSV→SQL with A/B and owned membership, truthful incomplete manifest. Unit schema/type/parent/security checks stay distinct from real namespace/business proof.
- Regression check that existing root deletion source/receipts/C4 cleanup remain byte-identical except unavoidable unrelated pre-existing changes; no destructive delete runtime needed for this export-only packet. Source review of retention/residency route and one authorized policy read/write are necessary; no staging/live.

Conclusion: integrate a named prerequisite-plus-checkpoint export packet, not a cherry-pick47 or whole0025. Full business/privacy export remains open. No claim that source-only review qualifies new root endpoint/auth or shared writer runtime.
