# W100 — STAGE-1 / DEC-539 freeze

**READY FOR CTO REVIEW.** STAGE-1 persists the twelve-stage initiative lifecycle as product truth while retaining the seven-code `status` as a compatibility projection.

## Delivery

- Adds the additive migration `20262260_initiatives_lifecycle_stage.sql` with constrained `lifecycle_stage` and provenance in `lifecycle_stage_source`.
- Backfills from `ie_aggregate_state.payload_json.lifecycleState`; rows without a precise aggregate use the first approved stage in the 7→12 mapping and are marked `mapped`.
- Installs a compatibility trigger so an explicit stage derives the seven-code status and remaining status-only writers derive a deterministic stage. `REJECTED` remains a disposition and preserves the last lifecycle stage.
- Updates the central creation and transition writers to write both projections explicitly.
- Moves the Initiatives list, twelve-option Status filter, initiative card pill, Execution Bank, report read/write path, Teresa grounding and card exports to the persisted stage, with the old status retained only as compatibility data.
- Keeps rollback additive: clear both new values; do not drop columns.

## Evidence

- RealPG strict-schema readback: 1,811 public tables, 12 initiatives, 12 mapped, 0 invalid pairs, second migration pass 0/0 updates.
- Targeted tests: 46/46 passed across lifecycle, register, list/menu, Teresa grounding and Execution Bank families.
- Frontend TSC: 0. Server TSC: 0. Per-file esbuild and `git diff --check`: pass.
- Browser: `http://127.0.0.1:4214/?screen=inicjatywy-lista&lang=en&theme=light`.
- Screenshot: `evidence/stage-1-dec539/initiatives-12-stage-dropdown-en-light-1440x900.png` (476 KB).
- Detailed readback: `evidence/stage-1-dec539/READBACK.md`.

## Scope boundary

This package does not remove the seven-code column, redesign governance/RACI, or implement STAGE-2/3. It made no staging write, deployment, Railway change, or protected-branch push.
