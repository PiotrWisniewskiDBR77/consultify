# STAGE-1 DEC-539 — freeze on current line

Status: **READY FOR CTO REVIEW**.

## Identity

- Base: `be57c5dae677844a224d78d874d3ab52444a882d`.
- Branch: `codex/stage-1-dec539-final-20260917`.
- Decision: DEC-539; module markers required at commit: `05_INITIATIVES`, `06_EXECUTION`, `07_MY_WORK_AGENT`, `13_CHAT`, `WSPOLNE`.
- Migration: additive `20262260_initiatives_lifecycle_stage.sql`; no migration with this id exists on the base or in the read-only staging ledger snapshot from 16 September.

## Frozen behavior

1. `initiatives.lifecycle_stage` stores one of the twelve DEC-490 stages; `initiatives.status` remains the seven-code compatibility projection.
2. Precise stage wins over a lossy status write. Writing the same seven-code status cannot degrade `SCHEDULED` to `APPROVED_BACKLOG`.
3. Backfill disables the sync trigger, preserves all `status` counts and records `lifecycle_stage_source` explicitly.
4. Runtime mapping has one canonical source in `server/src/constants/initiativeLifecycleStages.ts`; the browser adapter imports it.
5. `PENDING_APPROVAL -> READY_FOR_DECISION` and `CLOSED -> CLOSED`; `REJECTED` stays terminal and visible.
6. UI presentation is behind `VITE_INITIATIVES_STAGES_12`, strict true and default OFF. OFF preserves the former chip, Next gate, presets and Plan behavior; ON reads `lifecycleStage`.
7. Auto-start requires the compatibility status `APPROVED` and precise stage `SCHEDULED`; report generation guards an unknown lifecycle target.

## Acceptance evidence

- Fresh/replay migrations: **926 / 0**.
- RealPG critical family: **15/15 PASS**.
- Focused unit/UI family: **60 PASS, 2 baseline-identical P11 failures**; no STAGE-1 regression.
- Server TSC base/candidate: **0=0**.
- Frontend exact-lock TSC base/candidate: **152 -> 150**.
- Independent static review first returned HOLD for OFF leakage, duplicate runtime maps, stale evidence and seven new casts. The fixes remove all four findings; final independent re-review: **ACCEPT, P0=0, P1=0, P2=0** (24/24 reviewer-selected tests PASS).

Full current evidence: `evidence/stage-1-dec539/READBACK.md`.
