# W105 — STAGE-1 / DEC-539 fix freeze

**READY FOR CTO REVIEW.** The Wpis 105 code, migration and RealPG blockers are closed; the browser evidence matrix is explicitly partial and does not claim the missing captures.

## Delivery

- Guards the report writer against unknown lifecycle targets.
- Preserves a precise lifecycle stage when an old status writer repeats the same seven-code group.
- Synchronizes precise aggregate lifecycle writes into `initiatives.lifecycle_stage` and records aggregate provenance.
- Makes migration backfill status-neutral and measured before/after; reruns are no-op.
- Aligns `PENDING_APPROVAL -> READY_FOR_DECISION` and `CLOSED -> CLOSED`, with bidirectional parity tests.
- Reads auto-start candidates from the persisted stage column.
- Gates every twelve-stage UI change behind default-OFF `VITE_INITIATIVES_STAGES_12`, including Docker ARG/ENV propagation.
- Adds typed lifecycle fields to `PortfolioInitiative`, removing local casts from the touched projection path.

## Evidence

- Base: `9981c41c2d`; branch: `codex/a-stage-1-dec539-fix-20260916`.
- RealPG: **15/15** passed across H1c, H1d and two-store reconciliation.
- Unit/importer family: **46/46** passed across 10 files.
- Full TSC: frontend **169 base -> 167 candidate**; server **0**.
- Fresh migration and no-op rerun: status counts unchanged, second pass **0/0** updates.
- Docker flag guard: **0 missing**. Per-file esbuild and `git diff --check`: pass.
- Saved browser evidence: `evidence/stage-1-dec539/initiatives-12-stage-dropdown-en-light-1440x900.png`.
- Detailed receipt and the explicit missing browser captures: `evidence/stage-1-dec539/READBACK.md`.

## Scope boundary

This package does not remove the seven-code column, redesign governance/RACI, or implement STAGE-2/3. It made no staging write, deployment, Railway change, or protected-branch push.
