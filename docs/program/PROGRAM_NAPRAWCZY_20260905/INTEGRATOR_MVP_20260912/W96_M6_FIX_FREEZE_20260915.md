# W96 M6 fix — READY FOR CTO REVIEW

**Werdykt:** M6 jest ponownie zamrożone jako **READY FOR CTO REVIEW** na bazie
`9cf10528e3`; P0 TypeScript i konflikt testu Banku z F12 są zamknięte.

## Zakres jednej paczki

Paczka odtwarza na aktualnej linii pełny nadzbiór M6 z W81: jawny wybór
Process Flow, receipty konwersji, odsunięcie etykiet pasów od palety, krótki
nagłówek wariancji Banku, błędy raportu wewnątrz powłoki, raporty PM EN-first,
projekcję języka DRD oraz dostępność Menu 2 przy 1280 px.

Poprawki odbiorowe W96:

- trzy fikstury `TabConfig` w `ModuleNavBar.responsive1280.test.tsx` mają jawne
  `icon: null`, zgodnie z wymaganym kontraktem `TabConfig.icon`;
- `ExecutionBankViews.columnWidths.test.tsx` zachowuje decyzję F12: kolumny
  `health` i `blockerCount` są domyślnie schowane; test mierzy widoczne kolumny,
  kanoniczną podłogę statusu i nadal wymaga `Δ days`.

## Dowody

- mianownik M6: **8/8 plików, 40/40 testów PASS**, `--retry=0`, jeden worker;
- pełny frontend TypeScript bez limitu czasu, to samo środowisko i TypeScript:
  linia **169**, kandydat **169**, RC 2 / RC 2; identyczne pierwsze trzy
  diagnostyki (`CanvasRichEditor.tsx:334`, `CanvasRichEditor.tsx:408`,
  `UnifiedChatPanel.tsx:2071`);
- pełny server TypeScript: linia **0**, kandydat **0**, RC 0 / RC 0;
- `check:flagi:dockerfile`: PASS, `193/204/13/0`;
- `check:artefakt`: PASS, `8/0/117`; `check:list-canon`: PASS, `349`;
  `check:jezyk:ci`: PASS; nowych `as any`: **0**; `diff --check`: czysty;
- produkcyjny build: **PASS**, 10 953 moduły, 37.82 s.

Pierwszy pomiar linii wskazał 170 wyłącznie dlatego, że sparse checkout nie
zawierał śledzonego `shared/contracts/federatedActionManifest.ts`. Po dodaniu
`/shared/` do sparse checkout oba pełne przebiegi wykonano na kompletnym drzewie;
wynik kanoniczny to 169/169.

## Granice

Brak migracji, zapisów stagingowych, rekordów testowych, deployu i zmian Railway.
Paczka nie dotyka zakresów OPEN-1 ani pozostałej kolejki W96. Werdykt ACCEPT
należy do CTO.
