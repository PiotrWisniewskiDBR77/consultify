# Combined RC2 export source review

Frozen19-path manifest RC2_EXPORT_COMBINED_SOURCE_MANIFEST.json over HEAD5e7db466f5741839f7186cd9cab33fa6fad8fa2b: independent19/19 current-byte matches. Read full C6_INTERVIEW_INTEGRATION_PACKET.md and privacy/contract/wiring source. No DB or runtime run during root pipeline. **NO NEW SOURCE BLOCKER within prerequisite + snapshot/projects + five Interview relations.** Final combined runtime/HTTP policy acceptance still pending.

Both self-service and superadmin export use trusted authenticated req.user.id as actorId; no request-body/query actor override. Self-service strict persisted ACTIVE OWNER/ADMIN and tenant predicate retained. Superadmin retains verifyToken then requireSuperAdmin plus audit. Administrative role is not passed to anonymity projection and cannot disable its wall. Missing actor cannot claim respondent. Broader auth timing beyond existing route guard is not newly proven here.

organizationExportService.ts:309–348 builds privacy parent map only from the validated public.interview_sessions contract and tenant-scoped rows read under the pinned snapshot. Notes/questions/evidence with missing, drifted or cross-org parent are removed and unresolved is recorded. Assignment/session use their own persisted anonymity/assignee or owner. Final counts are recalculated after removal. Failed privacy processing cannot return the initially collected raw rows because no response is sent until the whole export finishes.

organizationExportInterviewPrivacy.ts:23–61 removes recommendations, per-question and weak-answer feedback and rubric justification; malformed serialized snapshots become null; only enumerated summary/score structure survives. Other wall projections clear answer/payload/context/transcript, material pointers, summaries, notes and stored assignment free-text review fields. Respondent exception compares authenticated actor to persisted respondent ID. Metadata retained by the accepted D18 safe-reader contract is not a promise of broader anonymity against arbitrary free text in every field. Derived Knowledge remains unclassified. No provider or storage writes occur in projection.

New explicit five relations plus existing root/projects/project_members give8 classified EXPORT,4 security exclusions and1911 originally unresolved; actual drift can increase omissions. No assignment-members logical expansion, no arbitrary user FK traversal, no v8 business proof. Schema/type/PK/FK drift checks and owner-parent contract checks remain present. Canonical policy discard fix remains in source; global hasLegalHold/deletion not overwritten.

Author privacy6unit/real1 and9auth evidence distinct from root combined pipeline. Auth baseline9PASS→fixed9PASS is coverage/hardening, not an observed bypass. Historical schema mutation is not a production exploit. Prior independent policy cleanup3PASS remains service-level proof on the prerequisite; final19-file manifest keeps that fix.

## Prepared remaining HTTP policy test

Use fresh synthetic target organization and separate actual persisted SUPERADMIN and ADMIN users with signed local JWT via actual Gateway on free4216; no existing fixture resets. Require local6457/cx6_export_contract guard. Seed no policy so authorized PUT must INSERT, GET must return it; subsequent partial PUT updates hold alone while preserving retention/residency. Then PUT residencyRegion:null/legalHoldEnabled:false persists null/0; omitted retention stays30. Existing route maps retentionDays:null to undefined, so explicitly assert retained30 and report inherited limitation (service null-clear is separately proven, not HTTP). GET/PUT as ADMIN and unauthenticated must deny, full SQL row before/after unchanged. A stale SUPERADMIN JWT for persisted ADMIN can also verify actual middleware authority. Keep created rows for audit; close server/pools. Wait for root to release DB before execution.

The missing HTTP policy route coverage is required by the new shared writer prerequisite, not a request to redesign the existing route or introduce new semantics. No product changes authorized by this review.

## First independent HTTP attempt — fixture prerequisite missing

RC2_POLICY_HTTP_INDEPENDENT.json/log:3 executed,1PASS/2FAIL. Both authorized write cases returned500 because audit_events relation is absent in disposable cx6_export_contract and mandatory requireAudit failed. This is an explicit test-environment dependency, not qualified product RED or full acceptance. No audit bypass or invented schema applied. ADMIN/stale SUPERADMIN GET+PUT each403 and anonymous401, SQL unchanged; organization_members role CHECK before/after identical. Server and pools closed. Fresh attempt after faithful schema-only audit_events restoration is required; original attempt must remain preserved.
