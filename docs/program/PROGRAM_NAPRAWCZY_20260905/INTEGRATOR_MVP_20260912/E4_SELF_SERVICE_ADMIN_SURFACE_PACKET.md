# E4 self-service organization export — existing Admin surface

Read-only preparation on RC2 codex-w17-deck-autosave-20260912 (base6038c2ae; no source/DB/API/browser changes). Recommendation: **existing /admin/audit/retention-export, within the already mounted AdminAuditLogPanel**, with a bounded organization-export control scoped to that child route. Reuse the existing panel/layout; do not add a screen or duplicate its instance.

## Authority and source basis

Read full owner_feedback/14_ADMIN/FINAL_IMPLEMENTATION_SPEC.md: Admin owns organization-wide controlled operations; Audit Log includes Retention & Export; server tenant/role enforcement mandatory; Command Center aggregates and links rather than duplicates editors. Read full owner_feedback/13_SETTINGS/FINAL_IMPLEMENTATION_SPEC.md: Settings personal export/preferences; tenant policy belongs Admin; use existing shell, announced async statuses and no false READY. Module14/15 acceptance records preserve owner freeze/history and explicitly separate backup/staging/technical replay; E4's later CODEX6 instruction78–84 mandates administrator self-service export, not personal GDPR. New work must carry appropriate14_ADMIN/WSPOLNE mandate marker under root coordination, not reopen15_SETTINGS wholesale.

Actual navigation adminNavigation.ts:229 has Retention & Export child. AdminSettingsModule.tsx:377–384 explicitly treats retention-export/high-risk-changes as aliases of existing audit panel; case audit:490–497 mounts AdminAuditLogPanel. Existing test AdminSettingsModule.test.tsx:404 proves route wiring only with mocked panel, not actual file behavior. ProtectedRoute.tsx:25–27 gives OWNER and ADMIN tenant-admin access;86–91 explicitly prevents SUPERADMIN inheriting customer Admin (redirects platform scope). Backend self-service requires current tenant ID and persisted ACTIVE OWNER/ADMIN, not Auditor/Security Admin merely because expert specification lists future roles.

OrganizationSettings.tsx under components/Settings is not the canonical current tenant administration destination. Current SettingsView organization/defaults paths use ownership/read-only handoffs; accepted15_SETTINGS explicitly moved organization-owned billing/defaults/Admin responsibility. Do not revive that large legacy component or replace personal DataControlsSettings export.

## Minimal implementation closure

1. AdminSettingsModule: on the existing audit fallback pass currentUser.organizationId and currentUser role (or a narrow allowed predicate) plus whether resolvedLocation.screen==='retention-export' to the SAME AdminAuditLogPanel. Other audit routes retain existing behavior. Avoid reading arbitrary organizationId from URL/query or adding a tenant chooser. Parent already uses currentUser.organizationId for AdminOrganizationDefaultsPanel:500.
2. AdminAuditLogPanel: retain existing audit CSV action (handleExport:74; Api.exportTenantAdminAuditLogs → GET/api/admin/audit-logs/export; file admin-audit-YYYY-MM-DD.csv) and retention editor. Add a clearly labeled “Export organization data (JSON)” control in existing bounded Retention & Export area only, using existing button/status anatomy. Explicitly distinguish its organization scope from “Export audit log CSV”; no duplicate panel. Default props keep other callers compatible. Render no org-export action when missing ID or role unsupported; server remains authority.
3. API client: add a separate narrowly named self-service helper, e.g. exportOwnOrganizationData(orgId,format='json'), GET/api/organizations/${encodeURIComponent(orgId)}/export?format=json with existing getHeaders; Blob response and structured safe error status/code. Do not repurpose superadmin-only Api.exportOrganizationData or GDPR methods. No backend endpoint changes necessary.
4. Reuse organizationExportDisclosure(original parsedBlob, capturedOrganizationId). Same COMPLETE/PARTIAL/UNKNOWN semantics, save original Blob, filename scoped to org, persistent role=status/error, no success until successful accepted response. JSON first is the minimum self-service path; CSV backend already exists but no need to invent selector for acceptance. Explicitly call JSON a scoped/possibly partial export, not full backup.
5. Use a synchronous in-flight guard and cleanup in finally; deny duplicate request, retry after error. Capture tenant/actor identity and suppress stale completion after organization/account switch/unmount; never download previous tenant result into changed context. No role/flag bypass to make screen render. Existing audit loading failure must not silently imply organization export is complete; if panel currently early-returns on auditload error, document and test whether export remains accessible, without changing unrelated audit semantics by accident.

## Exact tests and future built proof

- src/views/admin/__tests__/AdminSettingsModule.test.tsx: retention-export mounts once with correct active org/scope props; events/high-risk routes do not gain unrelated org-export controls; existing route test mock currently proves only panel selection.
- tests/unit/components/Admin/AdminAuditLogPanel.test.tsx: add actual mounted button→self-service helper cases for owner/admin, missing ID/member hidden/denied behavior, partial/unknown/complete originalBlob download/status, nonOK423/403no file/retry, deferred connected double trigger and actor/org switch. Keep existing authoritative audit-load failure/nonempty-count tests; audit CSV still calls its own API.
- API helper contract: correct encoded tenant path/auth, JSON query, nonOK no blob; no calls to /superadmin or GDPR. Test real responses, not regex source checks.
- Existing real organization-export-authority.gateway.pg.test.ts and snapshot/privacy/canonical suites already cover backend; reuse merged-source proof, do not call it browser proof.
- Built RC2: root-issued ports/backend/DB only, exactdist/hash and no/srcimports. Real OWNER/ADMIN login through program shell→Admin→Audit→Retention & Export; actual download file/filename/MIME/hash; parsed manifest complete:false and canonical own content, foreign sentinel absent, D18 wall; screenshot light/dark1440 and observable status. MEMBER denied direct route/API, switched tenant stale completion suppressed; legal-hold no download. Verify existing audit CSV still independent. No delete, schema or live actions in this browser run.

This delivers the missing self-service export entry without shrinking E4 to SuperAdmin. Full E4 deletion and entire business export denominator remain separately OPEN even after this button/download is proven; current partial data scope must remain visible to the administrator.
