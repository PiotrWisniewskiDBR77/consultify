# DRD-2b · Wpis 103 · DEC-552 — freeze 2026-09-16

**Werdykt: READY FOR CTO REVIEW.** Implementacja `a076ad5181` bazuje na `85541745e8`, zachowuje `VITE_DRD_INTERVIEW_V2` domyślnie OFF, nie zawiera migracji i nie wykonuje zapisów na stagingu.

## Domknięte zachowanie

- `DrdLevelInterviewWorkspace.save()` obsługuje odrzucenie zapisu i pokazuje czytelny komunikat EN/PL; wpisana odpowiedź pozostaje w polu.
- `I need help` zapisuje zdarzenie odpowiedzi przed pobraniem roli i `POST /tasks`. Jeżeli zadanie nie powstanie, decyzja poziomu pozostaje zapisana, a UI mówi wprost, że zawiódł tylko krok zadania.
- Development i18n ostrzega o surowym brakującym kluczu. Test skanuje realną ścieżkę DRD Interview oraz harness i wymaga zera brakujących kluczy EN.
- Harness i realna powłoka używają `discoveryToolsMain.toolContextPanel.workWithAi`; zrzuty pokazują `Work with AI`, nie surowy klucz.
- Przy fladze ON nawigator pokazuje `current/max`; Sales Processes ma `1 / 7`. Przy OFF model nie dostaje `maxLevel`, więc zachowuje dotychczasowe `current/target`.

## Dowody

- Focused: **13/13 PASS**, `--retry=0`.
- Szerszy importer runtime: **20/20 PASS**. Zastany test nagłówka zależny od języka: **3 FAIL / 4 PASS** identycznie na bazie i kandydacie, delta regresji 0.
- Pełny TypeScript bez limitu: frontend **0 → 0**, server **0 → 0**.
- Flagi: `194 / 205 / 13 / 0`; kanon artefaktu `8 / 0 / 117`; kanon list `349`, bez delty.
- Esbuild ESM: 5/5 wejść PASS; `git diff --check` PASS.
- Obejrzane zrzuty realnego `MethodWorkspaceShell`, Northwind, EN 1440×900:
  - `evidence/drd-2b-w103/drd-2b-en-light-1440x900.png` — SHA-256 `dd560d1986ee7ef61bf53b83962249a5bc57d4428136284b317193136e15c01d`;
  - `evidence/drd-2b-w103/drd-2b-en-dark-1440x900.png` — SHA-256 `c866fc710d8f29098f897f1ae360fd609e7c1311062ba31ec8b31bb88990fbab`.
- Oba kadry wygenerowano z `uwagi=0`; nie zawierają kontrolek harnessu „← Lista" ani „Uwagi".

## Review i granice

Niezależny review wykrył P1: pierwsza wersja przekazywała `maxLevel` bezwarunkowo i zmieniała OFF z `1/5` na `1/7`. Poprawka bramkuje pole opcją V2, a test dowodzi obu wariantów. Po poprawce review: PASS.

Nie wykonano deployu, migracji, zmian Railway ani pushu na gałęzie chronione. Zrzuty powstały w lokalnym dev-render; nie są dowodem działania API na stagingu.
