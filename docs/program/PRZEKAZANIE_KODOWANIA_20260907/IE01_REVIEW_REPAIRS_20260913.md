# IE01 — three review repairs

Base a1e0ab0783b8de48e0e89b2da25b06f01e1ffc55. No deployment and no complete-module acceptance.

1. Reject configured profiles disabling named review for any of the eight mandatory Definition cards. The key list derives from existing readiness requirements. Extra cards may still disable review; unprofiled legacy records are unchanged. Existing embedded invalid profiles require inventory and governed correction; this does not rewrite them.
2. Reset record-local card navigation/finding state before effects serialize the previous record's card into a new record URL. Preserve the actor/initiative-keyed draft store.
3. Derive the preparation lens consistently from search parameters, including router history navigation.

Evidence: schema/native RED→GREEN; independent domain19/19 and actual Gateway16/16, including all eight invalid profile preview/apply409 with unchanged aggregate, empty selections and receipts. Initial Gateway0/8 after validation is retained: the old fixture encoded invalid profiles and required explicit correction. Navigation same four names RED2PASS2FAIL→GREEN4PASS. Server tsc exit0. Independent navigation review is pending; final receipt is recorded separately.

No baseline or freeze gate was bypassed. Full 26-card behavior, supported profiles, typed waiver, full lifecycle and operational release gates remain open.
