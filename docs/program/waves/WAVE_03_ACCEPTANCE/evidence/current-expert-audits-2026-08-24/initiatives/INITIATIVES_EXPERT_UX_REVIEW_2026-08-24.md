# Initiatives — independent expert UX review — 2026-08-24

## Verdict

`NO-GO_FOR_OWNER_ACCEPTANCE / REMEDIATION_REQUIRED`

This is an independent UX and visual-system review, not owner acceptance. No code, data, lifecycle state or acceptance gate was changed.

Reviewed source checkpoint: `2cf780d62f9a421ec4b372e8168b247435c464ec`.

The most recent retained browser images for Initiatives show runtime marker `LOCAL @cc8848eb7d33`; therefore they are useful visual evidence but do **not** prove the current source checkpoint. Current-HEAD browser behavior is `NOT_VERIFIED` until an exact-SHA replay is captured.

## Evidence and standards used

- module gate and owner register: `modules/05_INITIATIVES/MODULE_ACCEPTANCE.md`;
- application UI canon: `docs/ui-standards/TRIADA_KANON.md` and `docs/program/CROSS_CUTTING_STANDARDS.md`;
- retained browser evidence: `docs/program/evidence/current-browser-replay/2026-08-23/initiatives/01-initiative-register.png` and `02-initiative-showcase-card.png`;
- current source: `InitiativesHub.tsx`, `CanonicalInitiativeRegister.tsx`, `InitiativePreviewV3.tsx`, `PlanScenarioSurface.tsx`, `CapacityScenarioSurface.tsx`, `Wizard/InitiativeWizardModal.tsx`.

The canonical product direction used here is the owner-approved three-destination IA: `Initiatives / Plan / Capacity`. Candidate and Portfolio semantics may remain in the model but must not reappear as duplicate top-level destinations.

## Atomic findings

| ID | Evidence | Required standard | Impact | Priority | Testable correction |
|---|---|---|---|---:|---|
| `INI-UX-001` | Retained images carry `LOCAL @cc8848eb7d33`, while reviewed HEAD is `2cf780d62f9a...`. `MODULE_ACCEPTANCE` also leaves PL/EN, tablet, themes, a11y and console/HTTP pending. | Every visual claim must be bound to the exact candidate SHA and route/state. | A visually improved or regressed current build can be mistaken for the retained one; acceptance would be unauditable. | P0 | Start one exact-HEAD runtime and capture signed screenshots for list, row preview, kebab open, wizard, Plan register/workbench and Capacity register/workbench. Record URL, viewport, locale, theme and visible SHA. Assert screenshot SHA equals `git rev-parse HEAD`. |
| `INI-UX-002` | Current source correctly exposes only `Inicjatywy / Plan / Obciążenie` (`InitiativesHub.tsx:658-686`). Retained list screenshot shows those destinations, but Menu 3 contains a generic `List` chip plus an opened-document tab. | Menu 2 owns module destinations and view modes; Menu 3 is contextual and must not duplicate the current view. | The unexplained `List` control weakens hierarchy and makes the three-level model ambiguous. | P2 | On list entry, verify Menu 3 contains lifecycle/status chips or opened-document tabs only; remove/rename any generic view duplicate. Browser assertion: exactly one selected module destination and no duplicate “List/Table” control across Menu 2 and Menu 3. |
| `INI-UX-003` | Canonical register uses `StandardTable` but passes no selection contract (`CanonicalInitiativeRegister.tsx:325-357`). Separately, Hub defines a bulk bar driven by `selectedIds` (`InitiativesHub.tsx:2149-2264`). The retained canonical screenshot shows no selection checkbox. | Canon item 13: table begins with selection checkboxes; selection must activate the bulk Menu 3 contract. | Bulk actions can exist in code while being unreachable from the accepted register. | P1 | Connect the canonical register to the same selection state. Test: Space toggles a focused row checkbox; selecting 1 and 2 rows shows correct `N selected`; Clear restores lifecycle chips; reload does not leave ghost selection. |
| `INI-UX-004` | Canonical preview provides meta, details and relations, then a custom footer with only `Otwórz` and `Kopiuj link` (`CanonicalInitiativeRegister.tsx:231-323`). The richer V3 body/footer with AI and finance shortcuts exists elsewhere (`InitiativesHub.tsx:1817-1914`, `InitiativePreviewV3.tsx:495+`), creating two competing preview contracts. | One six-block preview: header, meta, details, AI, relations, actions; same component hierarchy in every list. | Users see different initiative previews depending on route/data path; AI, relations and action hierarchy disappear in the canonical register. | P1 | Select one canonical preview implementation and delete the parallel composition from the accepted journey. Browser test checks six ordered landmarks, exactly one Open CTA, Relations behavior, AI enabled/disabled explanation, and identical structure for every lifecycle state. |
| `INI-UX-005` | Retained preview reaches the bottom of the viewport, but only one dark desktop state exists. No exact proof covers resize, long content, tablet or keyboard close. | Preview width `clamp(340px,28%,480px)`, full available height from Menu 3 to viewport bottom, independent scroll, Esc closes one layer, visible focus. | At smaller widths or with long content it may obscure the table, create nested page scroll or lose controls below the fold. | P1 | Add viewport tests at 1440×900 and 1024×768 with long content. Assert preview top/bottom bounds, one internal vertical scroller, no body horizontal overflow, focus enters header, and Esc closes preview without closing the page/document. |
| `INI-UX-006` | Canonical row menu declares only primary `Otwórz`, preview handler and an archive note (`CanonicalInitiativeRegister.tsx:344-355`). There is no retained open-kebab screenshot for current Initiatives. Owner finding `INI-OWN-009` explicitly says dropdown conformity remains pending. | Standard five-block/three-zone dropdown: primary navigation, workflow, data/relationships, universal Preview/Edit/Archive, destructive last; unavailable actions visible with reason when applicable. | The action model is incomplete or impossible to review; status/lifecycle operations can be hidden or placed inconsistently. | P1 | Capture the open kebab for Draft, Active and Closed rows. Contract-test order, separators, icons, disabled reasons, permission/lifecycle guards, Enter activation, arrow-key movement, focus return and one-layer Esc. No owner closure until the visible menu matches the action registry. |
| `INI-UX-007` | `New initiative` launches the five-step `InitiativeWizardModal` (`InitiativesHub.tsx:2377-2426`; wizard step order at `InitiativeWizardModal.tsx:2425+`). `MODULE_ACCEPTANCE` records 129 focused passes but 7 legacy New-Initiative a11y failures and owner retest pending. Owner flow requires a short premise, AI-generated draft, human review/edit and explicit save. | One comprehensible creation path with clear source/premise, AI progress/error, editable proposal, duplicate handling, governance and final receipt; accessible dialog semantics and focus trap. | A large source-anchored wizard may be powerful but is not yet proven usable for ad-hoc creation, and existing a11y failures block acceptance. | P1 | Reconcile the two creation intents in one wizard: `premise or source` → `AI draft` → `review/edit` → `governance` → `created draft receipt`. Run the focused suite with 0 unexplained failures; browser-test initial focus, labelled dialog, step names, Back/Next, error recovery, close confirmation and focus restoration to New initiative. |
| `INI-UX-008` | Plan has a register and versioned workbench, but editing exposes `Scenario ID`, `Portfolio Scenario ID`, `Window unit`, `Timezone` and raw `Ordered periods JSON` (`PlanScenarioSurface.tsx:460-490,764-803`). Sequencing uses Up/Down buttons and tables (`811+`); preview AI is explicitly disabled (`622-626`). There is no weekly editable Gantt or AI Analyze action. | Owner contract `INI-OWN-007`: new analysis, include/exclude and status filters, weekly timeline/Gantt, AI sequencing suggestion, manual movement, save/version/author. | The surface exposes implementation data instead of a decision-making tool and cannot perform the core what-if planning job. | P0 | Replace raw JSON/IDs in the primary UX with labelled scenario metadata and a weekly Gantt. Each initiative has include toggle, lifecycle badge, draggable/resizable bar and dependency indication. `Analyze AI` proposes (never silently applies) changes with rationale. Save creates/revises a named scenario. E2E proves create → filter → include → analyze → adjust → save → cold reopen exact version. |
| `INI-UX-009` | Capacity loads a versioned scenario and exposes demand/supply ranges, constraints and commitments, but the workbench contains technical identifiers (`commitmentId`, `assignmentId`, `resourceManagerId`, `version`) as direct form fields (`CapacityScenarioSurface.tsx:771-812`). Preview AI is disabled (`553-557`); no visible `New analysis` flow tied to selecting a saved Plan exists. | Owner contract `INI-OWN-008`: choose a saved Plan, create multiple analyses, show person/team saturation and conflicts over time, request AI recommendations/reallocation, save/version/report. | The screen is backend-shaped, difficult for managers and incomplete for the core load-analysis decision. | P0 | Add `New capacity analysis`: select plan/version and horizon, confirm capacity assumptions, generate per-person/team heatmap and conflict list, then request proposals. Keep technical IDs read-only in Information/Advanced. E2E proves two analyses can coexist for one plan, proposal is reviewable, save/cold reopen preserves version and source-plan lineage. |
| `INI-UX-010` | Plan and Capacity mix Polish UI with English labels/messages (`Loading Plan Scenario register`, `Scenario ID`, `Save draft`, `Publish Plan Scenario`, `Capacity Scenario Workbench`; cited source ranges above). No retained EN/PL parity evidence exists. | Entire visible surface must switch coherently between PL and EN; no raw technical labels in the ordinary workflow. | Mixed language makes an already complex planning workflow look unfinished and reduces comprehension. | P1 | Move every visible string to locale resources and define business-facing PL/EN copy. Automated test rejects hard-coded visible English in PL; browser capture demonstrates both locales for list, empty, error and workbench states. |
| `INI-UX-011` | Plan’s 14-column register and Capacity’s 12-column register depend on horizontal scrolling; no retained tablet evidence exists. Controls use wrapping but workbench density remains unproven. | Desktop/tablet responsiveness with no clipped primary action, readable first column, stable preview/table split and keyboard-operable horizontal overflow. | At tablet width the user can lose row identity, next action or save/analyze controls while scrolling. | P1 | At 1024×768 keep first column and row menu sticky, move low-priority columns behind column settings, preserve CTA and scenario selector. Test 200% zoom, keyboard scrolling and no overlapping preview/workbench. |
| `INI-UX-012` | Standard tables and many buttons expose labels, but current evidence does not cover Tab order, focus visibility, semantic status announcements, color independence or modal trapping. The module gate retains a11y as open. | WCAG-oriented keyboard and non-color semantics: visible focus, labelled controls, status not color-only, dialogs trap/restore focus, errors announced. | Keyboard and assistive-technology users cannot be certified on this build. | P1 | Run axe on all seven target surfaces and manual keyboard replay. Zero unwaived serious/critical violations; every lifecycle chip has text, async load/save uses status/alert, all icon buttons have accessible names, focus order follows visual order, and contrast passes in both themes. |

## Surface assessment

| Surface | Current assessment | Acceptance blocker |
|---|---|---|
| Three-level menu | Direction is correct: three Menu 2 destinations and contextual Menu 3 infrastructure exist. | Exact-HEAD visual proof and removal/clarification of duplicate generic view control. |
| Initiative table/status | Lifecycle projection and status columns are materially stronger than the historical screen. | Canonical checkbox/bulk reachability and full state/locale proof. |
| Full-height preview | Retained dark desktop image visually reaches the available height. | Current canonical register uses an incomplete/parallel preview composition; tablet, long-content and keyboard proof absent. |
| Dropdown | Standard row-menu adapter is used. | Visible action taxonomy, ordering, disabled reasons and keyboard operation not proven. |
| AI creator | Substantial five-step implementation exists. | Ad-hoc premise-first UX reconciliation, seven recorded test residuals and owner retest. |
| Plan | Versioned backend/workbench scaffolding exists. | Core manager UX—weekly Gantt, AI proposal, what-if interaction—is missing; raw JSON is exposed. |
| Capacity | Versioned scenario, ranges, constraints and governed commitment scaffolding exist. | Plan-selected analysis creator, saturation visualization and AI proposal flow are missing; technical IDs dominate. |
| Responsive/a11y | Some responsive classes and ARIA labels exist in source. | No complete current-HEAD matrix; cannot be accepted. |

## Required verification gate

The following is the minimum evidence packet for re-review:

1. exact-SHA manifest and clean authenticated load;
2. desktop dark/light and tablet screenshots for the list and both analysis surfaces;
3. PL and EN screenshots with no mixed-language copy;
4. Initiative table with status filters, selection and bulk Menu 3;
5. full-height preview with six blocks and long-content scroll;
6. open kebab for at least three lifecycle states;
7. New Initiative premise/source → AI draft → edit → save → cold reopen;
8. Plan create → include/exclude → AI proposal → weekly Gantt adjustment → versioned cold reopen;
9. Capacity select Plan → create two analyses → inspect saturation/conflict → proposal → versioned cold reopen;
10. keyboard/axe/console/HTTP results with literal failures retained;
11. independent owner replay after technical closure.

Until that packet exists, the correct state is `TECHNICAL_WORK_IN_PROGRESS / OWNER_RETEST_PENDING`, never `OWNER_ACCEPTED`.
