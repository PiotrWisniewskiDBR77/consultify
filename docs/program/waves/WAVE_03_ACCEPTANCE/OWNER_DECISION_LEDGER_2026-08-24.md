# Owner decision ledger — 2026-08-24

Status: `FINAL / IRREVOCABLE`

Ten rejestr zamyka fazę 1 planu dokończenia. Werdykty zostały zebrane od
właściciela (Piotr) w sesji decyzyjnej 2026-08-24, na podstawie pakietu
decyzyjnego z kartami opartymi o realny kod kandydata
(`codex/final-mvp-integration-20260823`) i zrzuty dowodowe z repozytorium.

Reguła ostateczności: raz podjęta decyzja nie wraca do dyskusji. Nowe pomysły
dotyczące zamkniętych obszarów trafiają do backlogu po-MVP z nowym
identyfikatorem. Agenci mają zakaz podważania poniższych werdyktów; zmiana
wymaga wyłącznie nowej, jawnej decyzji właściciela z nowym wpisem w tym
rejestrze (stary wpis pozostaje jako historia).

Karty źródłowe (pełne uzasadnienia, opcje, ścieżki zrzutów): materiały sesji
`decyzje-W1-trasy-nazewnictwo.md`, `decyzje-W2-zachowanie-modulow.md`,
`decyzje-W3-dane-silniki.md` (scratchpad sesji nadzorczej 2026-08-24) oraz
opublikowany pakiet decyzyjny.

| ID | Decyzja | Werdykt | Treść zamrożona |
|---|---|---|---|
| DEC-2026-08-24-01 | Interview — tożsamość adresu | **OWNER_ACCEPT** | `/interview` jedynym adresem kanonicznym; `/discovery` i `/project-intelligence` stają się przekierowaniami (`Navigate replace`); pozycja menu „Interview" prowadzi na `/interview`; wpis „Project Intelligence" w palecie poleceń znika lub staje się aliasem wyszukiwania; `/discovery/canvas` (osobny ekran) i `/interview/respond/:token` nietknięte. |
| DEC-2026-08-24-02 | Assessment — jedno narzędzie DRD | **OWNER_ACCEPT** | Silnik serwerowy (Method Core, `DrdMethodWorkspaceScreen`/`MethodWorkspaceShell`) jedyną implementacją DRD. Zakładki dokładnie `Interview | Matrix | Report`; `Split` usunięty; `Workspace` nie wraca; `Settings` jako osobna akcja. Mechanika pracy (karta poziomu, decyzje Osiągnięte/Cel/Pomiń, macierz pełnoekranowa, AS-IS/TO-BE) przenoszona ze starego `DRDAssessmentEditor`/`DRDMatrixSession` jako dawcy. Prototyp do akceptu właściciela PRZED kodowaniem (ASM-THREE-AC-008); test blokujący powrót `Split`/`Workspace`. Zgodne z ASM-OWN-021/022. |
| DEC-2026-08-24-03 | Execution — banner niedostępności | **OWNER_ACCEPT** | Banner V8 dozwolony wyłącznie wokół pojedynczego panelu realnie wymagającego V8 (dziś: Manager Cockpit) — nigdy wokół trasy modułu ani powłoki `ExecutionHub`. Treść banera: co nie działa + co nadal działa + kto włącza (wersja PL). Błąd odczytu flag (sieć/404) = stan błędu, nie „brak uprawnień"; produkcja fail-closed. Test-strażnik rozszerzony o powłokę. Zamyka przyczynę EXE-OWN-001. |
| DEC-2026-08-24-04 | Results — generacja źródeł | **OWNER_ACCEPT** | `ResultsVNext/*` (rejestry i karty KPI/OKR/ROI) jedyną generacją. `src/components/Results/*` (stary kokpit `ResultsHub`) = `HISTORICAL`, bez prawa powrotu do tras. Wymagany DZIAŁAJĄCY strażnik: `verify-canonical-16-module-bindings.mjs` egzekwuje `forbiddenCanonicalComponent` (dziś pole martwe) + blokada tylnych drzwi (`sampleData`, flagi, pusty wynik API nie podmieniają rejestru na kokpit). Ustalić jawną ścieżkę włączenia flag rejestrów dla właściciela (dziś default OFF). RES-OWN-002/007 realizowane później na tym fundamencie. |
| DEC-2026-08-24-05 | Finance — cutover na jedną linię | **SUPERVISOR_ACCEPT_BY_OWNER_DELEGATION** | Kandydat (`EconomicsView` → `FinanceHub` → warsztaty Finance V3) jedyną linią. Gałąź `codex/preserve-finance-owner-wip-20260823` (`e7574b340e`) = `REVIEWED_PATH_BY_PATH / NO_ADOPTION` — odrzucona w całości jako źródło kodu. Podstawa: analiza plik-po-pliku (22/37 identycznych, 15/37 starszych) POTWIERDZONA niezależną weryfikacją adwersaryjną (wszystkie 1786 dodanych linii sprawdzone; 45 niepasujących wyjaśnionych; 0 unikalnych kluczy tłumaczeń; drzewo gałęzi ścisłym podzbiorem kandydata; gałąź nowsza zegarowo, starsza treściowo — WIP z pominięciem 321 commitów). Gałąź pozostaje w backupie `github-backup` — decyzja odwracalna wyłącznie nowym wpisem. Dalej: deterministyczny resolver kart (FIN-REC-002); 4 flagi V3 włączane POJEDYNCZO, każda po akcepcie właściciela na zrzucie; odłączenie importów `Benefits/*` z `FinanceHub` dopiero po pełnym przełączeniu; polityka danych: historyczne wartości ufności NIE są nadpisywane. Werdykt właściciela: delegacja („jeśli jesteś pewien — podejmij") — zapisano dosłownie w protokole sesji. |
| DEC-2026-08-24-06 | Materials — silnik arkuszy | **OWNER_ACCEPT** | `ExceleView` kanonicznym silnikiem „Arkusza" w Materiałach (formuły, wieloarkuszowość, realny `.xlsx`, generowanie AI; flaga default ON od 2026-07-22). `TabeleView` = `SEPARATE_TOOL / NOT_A_MATERIALS_SHEET_ENGINE` — osobne żywe narzędzie operacyjne (rejestry, master data), nie `HISTORICAL`, nie do kasowania. Do naprawy w fazie 2: projekcja rejestru Materiałów (dziś pokazuje tylko „Prezentacja"), nieaktualny komentarz „default OFF", dług SPEC-A silnika Excele. |
| DEC-2026-08-24-07 | Meetings — adres obiektu | **OWNER_ACCEPT** | Gramatyka: `/meetings` (lista) + `/meetings/:meetingId` (karta) + `/meetings/:meetingId/minutes`, `/decisions`, `/notes/:noteId`. `/meeting` i `/meeting?meetingId=` = trwałe przekierowania. Karta spotkania wg standardu artefaktów SPEC-A (wzór: karta KPI). |
| DEC-2026-08-24-08 | Partner — landing | **OWNER_ACCEPT** | `/partner` = wyłącznie pulpit operacyjny podłączonego partnera (domyślnie „Pulpit"). Ekran pierwszego podłączenia = osobny, jednoekranowy stan. Treści programowe/marketingowe wyłącznie na publicznych `/become-partner`, `/partner/apply`, `/partner/pricing`; siedem wewnętrznych podstron marketingowych wycofane z `/partner` z zapisem, gdzie treść żyje dalej. Bramki: `connection` decyduje podłączony/nie; `lifecyclePhase` tylko o zawartości pulpitu; stan nieznany/błąd nigdy nie pokazuje rejestracji. Ekonomia partnerska pozostaje wyłączona polityką AMD-PRT-ECONOMICS-002 — poza zakresem tej decyzji. |
| DEC-2026-08-24-09 | Słownik menu | **OWNER_ACCEPT** | Jedna pełna lista nazw powierzchni na moduł (PL wiodące, EN równoległe), wywiedziona z KODU. Tabele zbiorcze podają liczbę powierzchni + odsyłacz do karty modułu — nigdy urwany fragment listy. Sporne moduły: Organization 6 modułów + 21 ekranów podrzędnych; Interview 6 zakładek + 1 ukryta za flagą (opisana jako ukryta); Materials 5 zakładek + 2 zakresy przez `?tab=`. Uzupełnić tłumaczenia PL („Presentations", „Template Library"), breadcrumb „Documents"→„Materials". Zmiana etykiety bez zmiany słownika nie przechodzi bramki. |
| DEC-2026-08-24-10 | Granice Ustawienia/Organizacja/Admin | **OWNER_ACCEPT** | Podział z `FINAL_THREE_MODULE_CONTRACT.md`: fakty o firmie → Organizacja; preferencje osobiste → Ustawienia; polityki organizacji (role, plan/faktury, AI, bezpieczeństwo, audyt) → Admin. Jeden obiekt = jeden właściciel; pozostałe moduły tylko-do-odczytu z linkiem. Praktycznie: Billing w Ustawieniach → kafelek z linkiem do Admina; `/settings/organization` i `/settings/tenant-defaults` → Admin; sekcja ADMINISTRACJA znika z Organizacji, adresy `members/billing/limits/domains/branding` → przekierowania do Admina; sprzątnięcie aliasów `ROUTES.ADMIN`. Bramki `OWN-GATE-001..005` = `ACCEPTED`. Warstwa `/superadmin/*` osobna, nietknięta. |

## Protokół sesji

- Partia 1 (D1–D4): werdykty właściciela ACCEPT × 4, po zrzutach dowodowych.
- Partia 2 (D5–D8): D6/D7/D8 ACCEPT; przy D5 właściciel odpowiedział dosłownie:
  „Szczerze powiedziawszy, nie wiem, czy wiesz, o czym mówisz. Zakładam, że tak.
  Jeśli jesteś pewien w 100 % że ta rekomendacja jest dobra, to nie podejmuj jej,
  bo ja nie wiem, o czym ty wiesz." — odczytane jako delegacja rozstrzygnięcia
  technicznego na nadzorcę pod warunkiem pewności. Warunek spełniono niezależną
  weryfikacją adwersaryjną (osobny agent z zadaniem OBALENIA tezy; werdykt:
  teza przetrwała). Decyzja odwracalna: gałąź w backupie.
- Partia 3 (D9–D10): werdykty właściciela ACCEPT × 2, po zrzutach dowodowych.

## Skutek

Warunek „owner freeze" z `CANONICAL_16_MODULE_SOURCE_CONTROL_MAP_2026-08-24.md`
(krok 6 sekwencji konsolidacji) jest SPEŁNIONY dla dziesięciu rozstrzygnięć
powyżej. Zamrożenie kodu produktu przechodzi w tryb ograniczonej integracji:
jeden moduł na raz, w zamrożonej kolejności 01→16, commit-per-krok, z bramką
akceptu właściciela na kompletnych zrzutach i wpisem `CLOSED_FINAL` per moduł.

Obserwacja poboczna z weryfikacji adwersaryjnej (do sprawdzenia w fazie 2,
moduł Finance): flaga `isFinanceOwnerSampleDataEnabled()` podmienia listy
Finance na zaszyte `FINANCE_OWNER_SAMPLE_*` — zweryfikować przed jakimkolwiek
demo (reguła „dane demo = twarz produktu").

## Uzupełnienie — sesja wieczorna 2026-08-24

| ID | Decyzja | Werdykt | Treść zamrożona |
|---|---|---|---|
| DEC-2026-08-24-11 | Organizacja — ekran wzorcowy redesignu | **OWNER_ACCEPT** | Realny ekran „Tożsamość i model działania" (SHA 14b1a8fc73, flaga `orgRedesignV1` OFF) zaakceptowany jako wzorzec; etap B = pozostałe 10 ekranów przez ten sam szkielet wg mapy 21→11. Flaga pozostaje OFF do odbioru całości modułu. |
| DEC-2026-08-24-12 | Admin — przełączniki bezpieczeństwa bez egzekwowania (TRI-MUST-02) | **OWNER_ACCEPT** | Trzy przełączniki (goście, link-sharing, zatwierdzanie narzędzi) UKRYTE/oznaczone „planowane" do czasu wdrożenia realnego egzekwowania; egzekwowanie = zadanie po-MVP z własnym odbiorem. Zakaz placebo polityk. |
| DEC-2026-08-24-13 | Admin — dom „ustawień domyślnych organizacji" (TRI-MUST-06) | **OWNER_ACCEPT** | Edytor tenant-defaults powstaje jako ekran-dziecko w Admin Command Center; przekierowania już tam celują (08e2beec19). |
| DEC-2026-08-24-14 | Admin — zakres domknięcia modułu w MVP | **OWNER_DECISION: FULL_55** | Moduł Admin zostaje otwarty do czasu podłączenia WSZYSTKICH 55 ekranów; odbioru częściowego nie będzie. Program budowy 40 brakujących ekranów staje się jawnym torem prac MVP (inwentarz → fale wdrożeniowe → odbiory per domena). |
| DEC-2026-08-24-15 | Organizacja — werdykt końcowy modułu 01 | **OWNER_ACCEPT_CONDITIONAL** | Właściciel zaakceptował komplet 22 zrzutów (11 ekranów × 2 motywy, redesign 21→11). CLOSED_FINAL zostaje wpisany po spełnieniu trzech warunków: (a) realne endpointy zapisu (z readbackiem) dla 5 ekranów sekcji Cele/Zakres/Wyzwania/Przyczyny/Ryzyka (dziś local-storage); (b) przywrócenie wgrywacza dokumentów na ekranie Wyzwań; (c) przywrócenie galerii gotowych blockerów. Po spełnieniu: wpis CLOSED_FINAL + tag final-01-organization. |
| DEC-2026-08-25-16 | Settings — werdykt końcowy modułu 02 | **OWNER_ACCEPT** | CLOSED_FINAL na komplecie 21 zrzutów (9 grup × jasny/ciemny + rozmiar czcionki); tag final-02-settings; trzy zastane sprawy przeniesione do backlogu po-MVP z nowymi ID (PL/EN treści, akcent na boot, tokeny status-boxów). |
| DEC-2026-08-25-17 | Admin — zarządzanie rolami projektowymi | **OWNER_ACCEPT** | Model zamierzony: wyłącznie OWNER zarządza rolami projektowymi; ADMIN otrzymuje czytelną odmowę (403 z wyjaśnieniem, bez formularza). Zero zmian w kodzie. |
| DEC-2026-08-25-18 | Superadmin — odrębny tor prac | **OWNER_ACCEPT** | Warstwa Superadmin = jawny tor z kartą akceptacji i werdyktami właściciela. Fala 1: ekran „Operacje platformowe" (10 gotowych akcji backendu P33) + naprawa audytu ścieżki zawieszenia (TRI-MUST-12). |
| DEC-2026-08-25-19 | Admin — dyspozycje CTO dla 5 STOP-ów nocy | **SUPERVISOR_DECISION (delegacja właściciela)** | (1) security/domains: budujemy REALNĄ weryfikację DNS TXT — teraz; (2) ai/quality-evaluations: wpięcie ekranu po naprawie IDOR (FIX-11) — teraz; (3) health/dependencies: minimalna realna mapa probe→zależność — teraz; (4) health/incident-history: uczciwy szkielet w Adminie, tenant-owy rejestr incydentów przechodzi do toru Superadmin; (5) team/access-requests: uczciwy szkielet + workflow tenant-scoped w backlogu programu Admin (wymaga projektu produktowego przepływu). |
| DEC-2026-08-25-20 | Admin — język nowych paneli | **OWNER_ACCEPT** | 21 paneli nocnych przechodzi na system tłumaczeń (PL wiodące + EN) w zadaniu uzupełniającym Codexa — spójnie z DEC-09. |
| DEC-2026-08-25-21 | Notyfikacje — zakres naprawy po audycie | **OWNER_ACCEPT** | Audyt (podpisany): wszystkie przełączniki notyfikacji = placebo (rozjazd magazynów; ~43 dzikie emisje; digest/push/dźwięki = atrapy; payment_failed bez e-maila). Teraz: N1 (jedno źródło prawdy ekrany→silnik + płatności przez silnik) + N3 (ukrycie placebo do czasu realnych podsystemów). Następnie N2 (dzikie emisje przez silnik). N4 (montaż ekranu V2) = decyzja przy odbiorze pakietu. Nie otwiera CLOSED_FINAL modułu 02 — osobny pakiet z własnym odbiorem. |
| DEC-2026-08-25-22 | Notyfikacje — werdykt pakietu N1+N2+N3 | **OWNER_ACCEPT** | Pakiet zamknięty na zrzutach i dowodach na żywo (przełączniki realnie sterują wysyłką; 8 grup emiterów przez silnik; podwójny mail Interview usunięty; placebo ukryte). Backlog z podpisem nadzorcy: M3 ekran per-typ (4 kategorie bez mapowania + rozważenie N4/V2), DND (decyzja produktowa), realny digest/push, szablonowe maile billingowe przez silnik, 9 grup emiterów świadomie poza (dyspozycje CTO w raporcie N2). Gałąź codex/notifications-n1-20260825. |
| DEC-2026-08-25-23 | Agent / Teresa — moduł 17 na końcu programu | **OWNER_ACCEPT** | Praca nad Agentem (kluczowy element My Work) i całą Teresą zostaje wydzielona jako MODUŁ 17 — ostatni w kolejności, traktowany jako finalna integracja. Powód: przed implementacją potrzebna jest jeszcze duża praca nad architekturą samego agenta. Skutki: (a) moduł 07 My Work domyka się BEZ powierzchni Agenta (zakładka Agent activity i pokrewne = zakres modułu 17); (b) uwagi właściciela dotyczące Teresy/Agenta ze wszystkich modułów są rejestrowane i kierowane do backlogu modułu 17, nie rozliczane w modułach źródłowych; (c) moduł 17 zaczyna się od fazy ARCHITEKTURY (projekt do akceptu właściciela przed kodowaniem), potem integracja z gotowymi już modułami 1–16. |
| DEC-2026-08-25-24 | My Work — Kalendarz | **OWNER_DECISION: BUILD_REAL_NOW** | Budujemy prawdziwy kalendarz w MVP (własne wydarzenia, widok dnia/tygodnia, połączenie ze spotkaniami) zamiast fasady V1 „wpis=zadanie". Proces: prototyp + model danych do akceptu właściciela PRZED kodowaniem. |
| DEC-2026-08-25-25 | My Work — akcja „Rozwiń w dokument" | **OWNER_ACCEPT** | Dobudować sprawdzenie kwitancji (wzór: działający check dla Usuń) — akcja przestaje być martwym klikiem, kontrakt governance zachowany. |
| DEC-2026-08-25-26 | My Work — właściciel notatki | **OWNER_ACCEPT** | Prawy panel pokazuje realnego właściciela cudzej strony (displayName) oraz „Ty" dla własnej; stany „brak danych" i „cudza strona" rozróżnialne. |
| DEC-2026-08-25-27 | My Work — wspólny inspektor Pomysłów | **OWNER_ACCEPT** | Prototyp F3-01 zaakceptowany: jeden panel właściwości dla 4 narzędzi; umiejscowienie = WARIANT 1 (osobna prawa szyna elementu; lewa zostaje przy pomyśle; nowy slot w powłoce ExecutiveModuleShell). Implementacja za flagą OFF do odbioru na realnych zrzutach. Wymaga decyzji odwzorowania 3 słowników stanów (7/4/0 wartości) — propozycja przy implementacji. |
| DEC-2026-08-25-28 | My Work — Notatnik: szyna + bloki | **OWNER_ACCEPT** | Prototyp F3-02 zaakceptowany: szyna Praca/Kontekst, natywne wstawianie bloków z uchwytami, odblokowanie kontrolek przez REALNE kontrakty kwitancji (zero atrap); silnik generowania AI = moduł 17, w pakiecie tylko powierzchnia zatwierdź/odrzuć. |
| DEC-2026-08-25-29 | My Work — foldery Sejfu klienta | **OWNER_ACCEPT** | Foldery żyją NA POZIOMIE LISTY sejfów (mały nowy ekran listy z folderami); wnętrze otwartego sejfu zostaje płaskie — blokada regresyjna MYW-CV-REC-008 pozostaje. Rozstrzyga sprzeczność CV-REC-005 vs 008. |
| DEC-2026-08-25-30 | My Work — Kalendarz: prototyp i 4 rozstrzygnięcia | **OWNER_ACCEPT** | Prototyp zaakceptowany (tydzień domyślny, 3 warstwy, jawny wybór Wydarzenie|Zadanie, tabela calendar_events addytywnie — spotkania/zadania zostają w swoich tabelach, kalendarz agreguje). Rozstrzygnięcia: (1) powtarzalność NIE w MVP — przycisk „Powiel na 4 tyg.", kolumny recurrence w schemacie od 1. dnia; (2) miesiąc TAK jako „gęstość" (max 3 wpisy + „+N"); (3) wydarzenia prywatne domyślnie, visibility per wpis (busy/org); (4) termin zadania = punkt w dniu, czas pracy = osobne wydarzenie z related_id. Do budowy dołączona naprawa: spotkania w kalendarzu widoczne także dla uczestników (dziś filtr created_by). Budowa za flagą OFF do odbioru. |
| DEC-2026-08-25-31 | Standard wizualny — przegląd wszystkich ekranów | **OWNER_DIRECTIVE** | Właściciel: nowe ekrany (prototypy/redesigny) wyznaczają poziom — docelowo WSZYSTKIE ekrany aplikacji mają go spełniać. Realizacja dwutorowa: (a) każdy moduł w kolejce przechodzi bramkę wyglądu przy swoim domknięciu (już obowiązuje); (b) przed fazą 3 (staging) obowiązkowy SWEEP WIZUALNY całej aplikacji: zrzuty wszystkich ekranów → ocena vs kanon i wzorce (szkielet Organizacji, inspektor My Work, Kalendarz) → lista odchyleń z wagami → naprawy mechaniczne przed startem, większe jako jawny backlog. Sweep = pozycja w planie fazy 3, nie przerwanie kolejki teraz. |
| DEC-2026-08-25-32 | Tools — nomenklatura Outputs→Insights | **OWNER_DECISION (odwraca SWOT-003 §6.17)** | Właściciel, zapytany wprost o potwierdzenie zapisu kontraktu („Outputs zostaje"), zdecydował: **jednak Insights**. Nowa decyzja jawnie zastępuje wcześniejszy zapis kontraktu (stary pozostaje w historii SWOT-003). Skutki: zakładka Outputs → Insights w DiscoveryToolsHub; łańcuch Sesja→Insight→Report/Initiative budowany na nomenklaturze Insights (zgodnie z oczekiwanym Menu 2 z mapy 16 modułów); rejestry i OWNER_NOTES TOOL-03 przestają być sprzeczne. Realizacja: pozycja projektowa planu Tools (nie w Fali 1). |
| DEC-2026-08-25-33 | Tools — 9. kategoria syntezy | **OWNER_ACCEPT** | Executive Answer (zgodnie z SWOT-003 §4.1/§6.14; kod już przyjął executive-answer). |
| DEC-2026-08-25-34 | Tools — nazwa ekranu końcowego sesji | **OWNER_ACCEPT** | „Results & Readiness" / PL „Wyniki i gotowość" (odblokowuje R19/TLS-READY-OWN-001). |
| DEC-2026-08-25-35 | Preview Content Contract — cross-app | **OWNER_ACCEPT** | Lokalny fork deskryptorów Tools zostaje PODNIESIONY do wspólnego kontraktu cross-app (pola wg XMOD-OWN-008); jedno źródło deskryptorów dla wszystkich modułów; fork znika jako fork, praca zachowana. |
| DEC-2026-08-25-36 | Chat — „Ważne sygnały" | **OWNER_DECISION: BUILD_PRODUCER_NOW** | Zamiast ukrycia placebo — budujemy realnego producenta sygnałów przed modułem 17. Wymaga projektu: definicja „sygnału", źródła (zdarzenia modułów), schemat, konsument w Chacie. Proces: spec+prototyp do akceptu właściciela przed kodowaniem. |
| DEC-2026-08-25-37 | Assessment — Cel/Target w narracji | **OWNER_DELEGATION → projekt nadzorcy** | Właściciel: informacja o celu jest ważna, ale musi być mądrze wprowadzona w narrację — rozstrzygnięcie eksperckie delegowane. Decyzja nadzorcy: cel WIDOCZNY w Interview jako kontekst na karcie poziomu (odczyt: „Cel: poziom N" + dystans do celu), EDYTOWALNY wyłącznie w Matrix (Ustaw TO-BE) — jedno źródło edycji, pełna narracja w ocenie. Do walidacji na prototypie. |
| DEC-2026-08-25-38 | Assessment — reguła poziomów | **OWNER_ACCEPT** | Bez kumulacji: każdy poziom oceniany niezależnie; „poziom obecny" = jawna decyzja konsultanta z widoczną mapą dziur (przykład właściciela: 1,2,5 bez 3,4). Koniec liczenia current=max. |
| DEC-2026-08-25-39 | Assessment — dane przeglądowe | **OWNER_ACCEPT** | Dedykowana fixture przeglądowa DRD (realistyczna firma, komplet ocen z dziurami) wyłącznie dla środowisk odbiorowych; zasada no-seed dla produkcji/demo nietknięta. |
| DEC-2026-08-25-40 | Program — zakaz odkładania na po-MVP | **OWNER_DIRECTIVE** | Nic nie trafia do backlogu po-MVP bez jawnej decyzji właściciela — produkt ma być GOTOWY na MVP. Duże jednostki kodu zlecane do produkcji Codexa z wyprzedzeniem (pipeline instrukcji). Wskazane przez właściciela: mechanika kart KPI i ROI + Finance (→ już w instrukcji dnia 4), Initiatives (→ instrukcja dnia 5), budowa szablonów zgodnie z dokumentacją (→ pakiet w przygotowaniu). |
| DEC-2026-08-25-41 | Szablony — architekci Word/Deck | **OWNER_ACCEPT** | Kreatory tworzenia szablonów ON (rozstrzyga konflikt z 24.07 na rzecz wcześniejszego akceptu właściciela). |
| DEC-2026-08-25-42 | Szablony — rejestr szablonów Excela | **OWNER_ACCEPT** | Budujemy właściwy rejestr szablonów użytkownika dla Excela (praca L); koniec zapisu do tp_base_templates wbrew D5. |
| DEC-2026-08-25-43 | Szablony — los atrap | **OWNER_ACCEPT** | Trzy kubełki: kasacja martwego kodu i stubów; 24 wbudowane → „szybkie starty"; 40 konsultingowych → prawdziwy rejestr DB. |
| DEC-2026-08-25-44 | Tools — biblioteka frameworków na MVP | **OWNER_ACCEPT** | 8–12 frameworków z pełnym blueprintem (opis/kroki/pytania/wyjścia) zamiast 60 pustych kafli; rozszerzanie po MVP wg użycia. |
| DEC-2026-08-25-45 | Assessment — prototypy DRD | **OWNER_ACCEPT** | Trzy widoki (Interview/Matrix/Report + Settings 5 kart) zaakceptowane jako wzór budowy; instrukcja dnia 7; budowa za flagą OFF; mechanika z dawcy wg mapowania 1:1 z prototypu. |
| DEC-2026-08-25-46 | Assessment — struktura raportu | **OWNER_ACCEPT** | 7 osi wszędzie (raport/eksport/opis produktu); stary spec 8 wymiarów i preview „5 osi" do poprawy. |
| DEC-2026-08-25-47 | Assessment — tożsamość Raportu | **OWNER_ACCEPT** | Report = jeden obiekt przy sesji z historią wersji; eksport do Materiałów jako migawka. |
| DEC-2026-08-25-48 | Meetings — Agenda/Szablony | **OWNER_DECISION: BUILD_NOW** | Szablony agendy spotkań wchodzą do pakietu dnia 6 (Szablony): CRUD szablonów agendy + zastosowanie przy tworzeniu/karcie spotkania (agenda istnieje w modelu MeetingItem); kanon list; etykieta słownika zostaje. |
| DEC-2026-08-25-49 | My Work — słowniki stanów inspektora | **SUPERVISOR_DECISION (delegacja DEC-27)** | Słowniki natywne per narzędzie są docelowe: inspektor pokazuje stan w enumie danego narzędzia (Tabela 4, Mapa 7); ZERO przymusowego mapowania (blocked≠rejected — semantycznie różne); Process Flow/Tablica bez stanu = uczciwy komunikat. Tabela mapowania z raportu dnia 3 zarchiwizowana jako referencja; ewentualny wspólny słownik = nowa decyzja produktowa właściciela. |
| DEC-2026-08-25-50 | My Work — polish dnia 3 (5 pozycji) | **OWNER_ACCEPT** | Partia A zaakceptowana na 11 zrzutach (evidence `day3-polish`, commit `a78729206b`). Konsekwencja wykonawcza (intencja właściciela „dowozimy wszystko na 100%", bez ukrytych funkcji): flagi `ff_myWorkCalendarV2` i `ff_ideaInspectorRightRail` przechodzą na domyślne ON — włączane pojedynczo z testami. |
| DEC-2026-08-25-51 | Initiatives — Plan: Arrange vs Analyze AI | **OWNER_DECISION: BUILD_NOW (wariant A)** | Rozdzielenie akcji (kontrakt F6/INI-CODE-09): „Uporządkuj automatycznie" = deterministyczny solver (etykieta + zdjęcie ikony AI wykonane licencją nadzorcy); „Analizuj AI" budowana TERAZ z realnym providerem i pełnym rodowodem (model/prompt/wersja/dowody/inputHash) wzorem AIAnalysisProposal z kart. Zakaz atrapy AI. |
| DEC-2026-08-25-52 | Meetings — karta spotkania | **OWNER_DECISION: BUILD_NOW (SPEC-A)** | Partia B: lista /meetings zaakceptowana; karta /meetings/:id do przebudowy na kanoniczną powłokę artefaktu SPEC-A (archetyp C Rekord: Menu 1 + ArtifactRightPanel + kebab + stany) zgodnie z planem etapu 2. W pakiecie: i18n statusów listy (usunąć EN „Past — needs update" z polskiego UI). Odbiór: zrzuty przed scaleniem. |
| DEC-2026-08-25-53 | Interview — etap 1 na zrzutach | **OWNER_ACCEPT_EXCEPT (preview)** | Partia C (19 zrzutów exact-SHA 0050bad8): zakładki, redirecty, kebaby, Discovery canvas — ACCEPT. Wyjątek właściciela: panele preview (sesja, inicjatywa) NIEZGODNE z kanonem podglądu — do przebudowy na PreviewPaneShell/PreviewActionBar wg TABLE_AND_PREVIEW_CANON §7 (6 bloków, pille, wzorzec Insight). Creator Shell (P0) poza partią — najpierw prototyp. |
| DEC-2026-08-25-54 | Meetings — karta SPEC-A | **OWNER_ACCEPT** | Przebudowa karty /meetings/:id do powłoki SPEC-A (archetyp C Rekord, wzorzec CaseDetailScreen) zaakceptowana na zrzutach (evidence etap2/03, commit 1e33d56429). Scalona do m03. |
| DEC-2026-08-25-55 | Assessment — słownik kodów „Pomiń" (I.7) | **OWNER_ACCEPT** | Cztery kody: „poza modelem operacyjnym" · „poza zakresem zlecenia" · „odroczone do kolejnej rewizji" · „zastąpione innym rozwiązaniem"; wybór kodu wymagany przy decyzji Pomiń (mapowanie na AnswerEventPayload.skipReason). Odblokowuje kartę poziomu. |
| DEC-2026-08-25-56 | Assessment — karta 4 Settings (M.7) | **OWNER_ACCEPT** | Etykieta „Akceptacje" (nie „Zatwierdzenia") + TRZY bramki, zgodnie z zaakceptowanym prototypem (DEC-45). |
| DEC-2026-08-25-57 | Interview — preview do kanonu | **OWNER_ACCEPT** | Panele preview sesji i inicjatywy przebudowane do kanonu podglądu (PreviewMetaCard/PreviewDetailsSection + kebab lokalny; naprawa INT-C05-A — pill bez zawijania) zaakceptowane na 6 zrzutach (evidence interview-preview-canon-20260825, commit 3a8c11eb4d). Wyjątek z DEC-53 zamknięty — Interview etap 1 w komplecie akceptów. Poza zakresem etapu 1: Creator Shell (P0, prototyp przed budową). |
| DEC-2026-08-25-58 | Meetings — dokończenie (dzień 10) | **OWNER_ACCEPT (pełny zakres)** | Werdykt sceptyka nadzorcy: teza o kompletności OBALONA (8 blokerów, m.in. moduł za bramką closed-beta dla ról klienckich; martwe Decyzje/follow-upy; ślepy koniec przepływu notatka→Materials; nierozliczone wymagania właściciela MYW-CAL-REC-001..003). CLOSED_FINAL Meetings dopiero po odbiorze bloku dnia 10 obejmującego: realne Decyzje/follow-upy, przepływ do Materials/My Work/Initiatives z lineage, wymagania REC-001..003 (strefa czasowa, cykliczność, zapraszanie org+goście ze statusem, artefakty w zaproszeniu), otwarcie modułu dla ról klienckich po odbiorze. Naprawy szybkie (i18n, gating przycisków, kasowanie z czyszczeniem protokołów, kłamiące komentarze) — wewnętrznie od ręki. |
| DEC-2026-08-25-59 | Initiatives — „Analizuj AI" wymaga klucza dostawcy | **SUPERVISOR_NOTE (do decyzji Piotra)** | Odbiór dnia 9: STOP N.1 ZASADNY — w środowisku brak klucza dostawcy AI (.env.example placeholdery); budowa bez klucza = atrapa [TRYB MOCK], odrzucona z Ograniczenia A. Kolejność: (1) Piotr/nadzorca zapewnia klucz (np. ANTHROPIC_API_KEY) do środowiska, (2) bramka N.1.0 z przechodzącym wywołaniem próbnym, (3) dopiero budowa „Analizuj AI" (planAiCritique.ts + realny rodowód). DEC-51 pozostaje w mocy — tylko odblokowanie techniczne czeka na klucz. |
| DEC-2026-08-25-60 | Initiatives — dzień 9 odebrany częściowo | **SUPERVISOR_ACCEPT (warstwa 1)** | P.1 (wycofanie zgniłych source-anchor) i P.2 (usunięcie martwych gałęzi Huba) ZROBIONE_WG_DoD — dowody uczciwe, zero atrap/crimson. I.9R CZĘŚCIOWE: model ograniczeń PlannedWindow (freeze/FS-SS-FF/lag-lead/duration/exclusion) gotowy addytywnie (UNKNOWN/null, nie 0), 2 testy behawioralne — ALE niewpięty w solver Arrange (martwa konfiguracja) + brak undo/redo. Otwarte na dzień 11: I.9R(b/c/d), I.3-I.8/I.10/I.13-I.15 (NIE_ZACZĘTE), N.1 po kluczu. Bramki wydania ZAMKNIĘTE. |
| DEC-2026-08-25-61 | Results — dzień 8 odebrany częściowo | **SUPERVISOR_ACCEPT (warstwa 1)** | R.5 karta KPI POTWIERDZONA (5/5 testów behawioralnych, 4 statusy rozdzielone: cykl życia/wydajność/jakość/świeżość; HonestValueCell/GapNotice; flaga default OFF prod-gated). Zastrzeżenia: 2/6 elementów nagłówka = uczciwe BRAK_API (trend, następny obowiązek); WYKRYTO fałszywy dowód dark (bajt-w-bajt = light) → re-shot obowiązkowy przed odbiorem Piotra. STOP-y R.4/R.6/R.7 PRAWDZIWE (grep potwierdził, zero atrap). |
| DEC-2026-08-25-62 | Results — licencje nadzorcy na STOP-y dnia 8 | **SUPERVISOR_DECISION** | (a) R.5-O1: przyjmuję 8 osobnych sekcji karty KPI (każda = odrębna powierzchnia backendu, uczciwsze niż zlepianie do 6). (b) R.7-O1: trwały nagłówek wykonawczy ROI wg SPEC-A (dane Approved/Forecast/Actual z zasobów faz). (c) R.6-O1 Uwaga: link do wspólnej /attention BEZ filtrowania klienckiego (unika „drugiej prawdy"); Set-scoped filtr = przyszły backend. (d) R.4 CTA OKR + R.6-O2 agregat check-inów: API GOTOWE — autoryzuję frontend (klient createOkrSet, read-only agregacja per-KR). Search Results (R.4) = przyszły dyżur backendowy (Z16 słusznie zablokował). Budowa za flagą OFF, zrzuty pod akcept Piotra. |
| DEC-2026-08-25-63 | Execution dzień 11 — licencje nadzorcy | **SUPERVISOR_DECISION** | (1) Imienna zgoda na DOKŁADNIE JEDNĄ flagę `execReportsIntelligence` (wzorzec changeSignals, default OFF wszędzie łącznie z demo, z realnym czytelnikiem). (2) Backend generatora wg rekomendacji E.0: runtime-v1 report-runs = SSOT niezmiennej publikacji (event-sourced cykl życia), management-reports = pipeline eksportowy; bez zmian serwera poza zakresem Z16 — rozszerzenia backendu tylko jeśli instrukcja je przewidziała, inaczej BRAK_API. |
| DEC-2026-08-25-64 | Partner dzień 12 — semantyka connection | **SUPERVISOR_DECISION** | Kanoniczna semantyka connection = STRICT EXACT-TENANT (V8, read-only, zgodna z inwariantem izolacji całego programu). Dodać addytywny read-only resolver/kontrakt GET connection. Historyczne/unbound rekordy: NIE odłączać cicho — jednorazowy, idempotentny backfill wiążący je jawnie (owner_organization_id wg legacy resolvera), z kontrolą przed/po: liczba połączonych per resolver musi się zgadzać; rekordy niewiążliwe jednoznacznie → lista wyjątków do decyzji właściciela, nie ciche odłączenie. Legacy route zostaje do czasu pełnego cut-over. |
| DEC-2026-08-25-65 | Kontrakt rozdzielenia staging/demo/production | **OWNER_DIRECTIVE (wiążąca do odwołania)** | Jutro pokaz stabilnego demo. Docelowo: staging.consultify.ai = rozwój, demo.consultify.ai = zamrożone (promocja ręczna konkretnego SHA, nigdy latestCommit), consultify.ai = NOT_AUTHORIZED. Wspólna kanoniczna baza = obecna baza demo (staging pisze TYLKO w osobnych organizacjach testowych, tenant-scoped). DO KOMUNIKATU „FREEZE ZAKOŃCZONY": zakaz deployów (staging tylko za osobną zgodą), zmian Railway/domen/env, zdalnych migracji/seedów/resetów, zapisów do wspólnej bazy, merge/force-push na demo/develop/main. Migracje = MIGRATION_PREPARED / REMOTE_EXECUTION_NOT_AUTHORIZED z testem kompatybilności wstecz z zamrożonym demo. Railway project a6d59e88-263d-45f3-96bc-861f66bf467b — odczyty tylko z jawnym projektem/env/usługą. Na FREEZE: stop operacji chmurowych + AGENT HANDOFF wg szablonu. Kolizje → COORDINATION_REQUIRED, nie samodzielne rozwiązywanie. |
| DEC-2026-08-25-66 | Audits — werdykt partii D | **OWNER_CHANGE** | Uwagi właściciela (25.08, dosłowne): (1) Biblioteka „masakra" — Menu 3 ma mnóstwo przycisków, „jakby pojawiło się menu czwarte" = duży błąd; (2) tabela Raportów bez prawego kebaba; (3) tabele mają znacząco za mało kolumn; (4) tabele i układ funkcji mają się NICZYM nie różnić od Tools/Assessment; (5) brak pokazania narzędzi „w środku" (wnętrza audytów). FIX wewnętrzny + ponowne zrzuty (listy + wnętrza) przed powtórnym werdyktem. |
| DEC-2026-08-25-67 | Interview — prototyp Creator Shell | **OWNER_ACCEPT (z kompletem 10 rekomendacji)** | Prototyp 3 kroków (Definicja/Materiał/Dostrojenie, powłoka 1040×840, Liquid Glass tylko na powłoce, 6+7 typów wyniku, wspólna powłoka Wniosek+Inicjatywa, 4 rozróżnione awarie AI, autozapis 20 s, „Uruchom" od kroku 2) zaakceptowany na zrzutach wraz ze wszystkimi rekomendacjami autora (pytania 1-10 w plikach prototypu, commit a1bbae7a36). Budowa: za flagą default OFF, wzorem prototypu 1:1, zrzuty przed włączeniem. Granice: D1/D2 (kreator klasyczny, nie DRD; dialog nad listą /interview; rozszerzamy WizardModal, zakaz czwartej powłoki). |
| DEC-2026-08-25-68 | My Work — inspektor (partia F1) | **OWNER_CHANGE** | Werdykt właściciela (dosłowny): „Ta tabela właściwości po prawej stronie to jest jakiś dramat — w ogóle nie pasuje do lekkiego charakteru pozostałych elementów graficznych." Przeprojektowanie stylu inspektora (lekki charakter) wchodzi do bloku parytetu szyny (razem z naprawą regresji P0 z werdyktu sceptyka). Lista Pomysłów bez uwag. Flaga szyny pozostaje OFF do ponownego werdyktu na nowych zrzutach. |
| DEC-2026-08-25-69 | My Work — szyna Notatnika (partia F2) | **OWNER_CHANGE** | Werdykt właściciela (dosłowny): „prawe menu rozwijane pochodzi z wersji aplikacji sprzed pół roku — w uwagach, które zgłaszałem do odbioru tych dokumentów, jasno była o tym mowa." Obowiązek: odszukać oryginalny atom w korpusie 56 uwag My Work (rejestr miał go nierozliczonego) i przebudować NotebookRightRail do aktualnego kanonu prawego panelu (ArtifactRightPanel/SPEC-A). Łączy się z DEC-68 w blok „prawe panele My Work do kanonu" (inspektor + szyna Notatnika, lekki charakter). Pozostałe elementy F2 (rynna, inline AI, narożnik) bez uwag. |
| DEC-2026-08-25-70 | My Work — partia F3 (Sejf+Kalendarz) | **OWNER: Sejf CHANGE, Kalendarz ACCEPT** | Sejf (dosłownie): „Znowu pokazujesz mi safe w wariancie sprzed zmiany. Wkładałem swoje uwagi, jakie zmiany powinny być; w ogóle nie są wzięte pod uwagę." Kalendarz V2: OK. Wzorzec trzech CHANGE (DEC-68/69/70) = nierozliczone atomy korpusu My Work. Obowiązek przed kolejnymi zrzutami: wydobyć z korpusu 56 uwag KOMPLET atomów dot. Sejfu, inspektora i prawej szyny Notatnika, skonfrontować z kodem, przebudować wg atomów (wizualnie nowe → prototyp przed kodem). |
| DEC-2026-08-25-71 | Proces odbiorów — bramka korpusowa | **SUPERVISOR_CORRECTION (po FAIL-u partii F)** | Właściciel słusznie odrzucił partie F1-F3: zrzuty pokazywały stan sprzed rozliczenia jego uwag z 22-23.08 (21 atomów na 3 powierzchniach, większość CZĘŚCIOWE/NIETKNIĘTE — potwierdzone analizą MYWORK_PANELS_VAULT_SPEC_2026-08-25.md, commit 955b06fda7). NOWA BRAMKA obowiązkowa przed KAŻDĄ partią do właściciela: (1) konfrontacja zrzutów z kompletem atomów korpusu danej powierzchni — zrzut pokazujący stan sprzed nierozliczonego atomu NIE idzie do właściciela; (2) potwierdzenie świeżości: zrzut zrobiony PO ostatnim commicie dotykającym powierzchni, z tipa właściwej gałęzi; (3) podejrzenie runtime-identity mismatch (zrzut z niewłaściwego builda/flagi) = STOP partii. Werdykty ACCEPT z partii F pozostają ważne tylko dla: listy Pomysłów, rynny/inline-AI/narożnika Notatnika, Kalendarza V2. |
| DEC-2026-08-25-72 | Execution dzień 11 — odbiór warstwy 1 | **SUPERVISOR_ACCEPT_CONDITIONAL** | Szkielet uczciwy (7 STOP-ów zasadnych, zero zaszytych wag, server/rejestr 5 powierzchni/kanon nietknięte, flaga execReportsIntelligence fail-closed OFF wszędzie). ALE: 6 werdyktów „JEST" w raporcie = placebo (generator 1 formularz zamiast 7 kroków+9 stanów, „karty" = licznik useState, heatmapa = tekstowy grid-duplikat, lineage = goły ul, rejestry konfliktów przepuszczają wszystko), 2 fałszywe zdania o formułach (slaBackorder/agingDays nie istnieją), dowód OFF na atrapie (ExecutionReportsIntelligenceEntry — martwy komponent tylko dla testu). DECYZJA: merge do m03 DOZWOLONY wyłącznie jako kod martwy za flagą PO wykonaniu F1-F12 (korekta raportu obowiązkowa — zawyżone JEST-y nie wchodzą do rejestru); flip ON zakazany do: F1-F8 merytoryka → F9-F11 dowód OFF na realnym Hubie → polish-pass 1-11 → zrzuty → akcept Piotra. Do Piotra idą TYLKO: E-O3 (BSC — czy i jaka taksonomia), E-O4 (wagi impact), progi saturacji z E-O5. |
| DEC-2026-08-25-73 | Partner dzień 12 — odbiór warstwy 1 | **SUPERVISOR_ACCEPT_CONDITIONAL** | P.1-P.4 dobre (strict exact-tenant wdrożony, marketing odmontowany obiema drogami, test odwrócony jawnie; backfill DEC-64 przetestowany przez odbiorcę na żywym PG: idempotencja+parytet+rollback OK). BLOKERY merge: (1) 19 czerwonych testów WPROWADZONYCH dniem 12 zaraportowanych jako zastane (8 lokalnych mocków bez getConnection; w tym 2 strażników self-connect) — naprawa i replay 47 FAIL, „dzień 12 wprowadził 0"; (2) luka parytetu: readConnectionParity nie widzi legacy self-heal „po koledze" — domknąć trzecią gałąź albo jawna akceptacja z listą userów; (3) fantom c-surface-hover (9 plików, hovery znikają) + 7 kluczy partner.day12.* bez fallbacku. Dodatkowo: realdb test parytetu z realnym seedem, sprzątnięcie martwego kodu, G06 w MODULE_ACCEPTANCE nieaktualne. |
| DEC-2026-08-25-74 | Wzorzec RUNTIME-IDENTITY-MISMATCH (proces) | **SUPERVISOR_FINDING** | Dwa niezależne dowody (partia F My Work + prototyp Organization) potwierdzają: „CLOSED_FINAL warunkowy"/„ZROBIONE_W_KODZIE" w rejestrach opisują stan ZA FLAGĄ OFF, którego użytkownik/właściciel NIE OGLĄDA. Organization: redesign 21→11 (DEC-11/15) leży na m01-organization za orgRedesignV1=OFF, na m03 flagi w ogóle brak → runtime pokazuje 21 ekranów. My Work: inspektor/szyna/Sejf — analogicznie. WNIOSEK dla fazy scaleń na staging: przed wpisem CLOSED_FINAL KAŻDEGO modułu — potwierdzić, że stan pokazywany właścicielowi = stan DOMYŚLNY runtime (flaga ON w default albo funkcja bez flagi), nie wariant za OFF. Rejestr statusu „zrobione za flagą" ≠ „widoczne dla użytkownika". Prototypy Organization (1dbaab2573) i My Work panele (a64f52285b) gotowe do akceptu Piotra — budowa PO akcepcie wchodzi na default ON. |
| DEC-2026-08-26-75 | Cel: dokończenie pełnej integracji (tryb autonomiczny) | **OWNER_DIRECTIVE** | Fable nadzoruje/dowodzi/konsoliduje, Sonnet+Opus wykonują, Codex bierze największe bloki→odbiór→konsolidacja. Praca ekonomiczna, kilka dni. Fable NIE czeka na decyzje wizualne do dalszej integracji — buduje za flagami OFF, akcepty zbiera partiami gdy Piotr dostępny. Licencja nadzorcy stała: STOP-y Codexa rozstrzygalne kontraktem/zaakceptowanym prototypem/istniejącą decyzją zamykam sam; do Piotra idą TYLKO realne decyzje właścicielskie (wagi/progi/nowy wygląd/klucze) — partiami. Cel operacyjny: m03 = kompletny kandydat integracyjny 16 modułów → staging (deploy za jawnym „deploy" Piotra). |
| DEC-2026-08-26-76 | My Work — panele przeprojektowane (partia H) | **OWNER_ACCEPT (3/3)** | Inspektor (7 obwódek→0, typografia, 360px), szyna Notatnika (accordion SPEC-A 5 sekcji, Praca/Kontekst znikają), Sejf (StandardTable + kwit + 8 cytatów rozliczonych) — zaakceptowane na zrzutach (prototypy a64f52285b). Zamyka DEC-68/69/70. Budowa wewnętrzna wg prototypów 1:1, za flagami OFF, tabela parytetu per zrzut, po budowie flaga default ON (DEC-74: stan pokazywany = domyślny runtime). |
| DEC-2026-08-26-77 | Results/Execution backend dzień 14 — odbiór + licencja Z17 | **SUPERVISOR_ACCEPT_CONDITIONAL** | Odbiór niezależny potwierdził bit-for-bit: S.1 search ZROBIONE (tenant-izolacja z tokenu, CTE scoped, ILIKE escaped, 7 testów+RealPG), K.1 trend solidny read-model (brak testów HTTP/tenant/realdb — dozbroić), X.3b realne. Zero atrap/dziur tenant/naruszeń Z. Merge do m03 DOZWOLONY dla zadeklarowanego zakresu (backend addytywny, zero UI/flag/świętych plików). LICENCJA: rozszerzam Z17 o resultsVnextRoi.validators.ts — domknięcie ROI q-filter (S.2) to mechanika, nie decyzja Piotra. Reszta DoD (K.2/K.3/O.1/O.2/X.1/X.2/X.4) = kolejny dyżur backendowy. |
| DEC-2026-08-26-78 | Organization — prototyp + zakres | **OWNER_ACCEPT** | Prototyp anatomii ekranu-dziecka + karta Gotowość (5 wymiarów, nie 1 procent) + karta Pliki („Używany w") zaakceptowany (1dbaab2573). ZAKRES rozstrzygnięty: 11 ekranów (redesign 21→11 DEC-11 wchodzi na DOMYŚLNE — flaga orgRedesignV1 default ON), BEZ zakładek poziomych wewnątrz modułu (kod już bez nich). Budowa wewnętrzna wg prototypu 1:1. |
| DEC-2026-08-26-79 | CEL PÓŁNOCNY programu | **OWNER_DIRECTIVE** | Pełna integracja 16+1 modułów oceniona na ≥95% satysfakcji NIEZALEŻNYCH EKSPERTÓW, aplikacja w pełni funkcjonująca, wg 3-tygodniowego planu. Fable działa BEZ PRZERWY: konsoliduje m03, dopina moduły, zleca bloki, odbiera, poleruje wg formuły [[formula-polerowania-grafiki]]. Bramka końcowa: adwersaryjny panel ekspertów (Opus) per moduł przed CLOSED_FINAL; <95% = wraca do naprawy. |
| DEC-2026-08-26-80 | Nocny sweep wizualny A+B | **OWNER_ACCEPT (graficznie, do MVP)** | ~13 napraw wizualnych (Admin Command Center pionowy sidebar+PL timestampy, OKR/ROI/Assessment/Finance/Tools/Materials: surowe ID/enumy/crimson→kanon+i18n) zaakceptowane graficznie na zrzutach; scalone do m03. Właściciel: „graficznie akceptuję, merytorycznie poprawimy w kolejnej turze, tak wypuszczamy do MVP" — poprawki merytoryczne = backlog po-MVP z nowym ID. |
| DEC-2026-08-26-81 | Audits r2 — odbiór partii K | **OWNER_ACCEPT_EXCEPT** | 4/5 uwac DEC-66 zamknięte i zaakceptowane (Menu 3 jeden rząd, kolumny 6→9, kebab Raportów z realnym Zatwierdź/Opublikuj, listy na kanonie) — scalone do m03. WYJĄTEK: wnętrze „warsztat kryterium" (CriterionWorkspace, zrzut 04) — właściciel: „nie rozumiem co to jest, nie wygląda dobrze, trzeba przeanalizować". To istniejący ekran sfotografowany, nie naprawiony — idzie do osobnej analizy wg formuły [[formula-polerowania-grafiki]] (audyt→ewentualny prototyp). |
| DEC-2026-08-26-82 | Meetings — karta prawy panel + decyzje dokończenia | **OWNER_CHANGE + OWNER_DECISION** | Karta spotkania (partia L): środek OK, ale PRAWY PANEL wymaga poprawki — dodać zwijaną metryczkę narzędzia po prawej wzorem Decisions/Tools/Tasks/Initiative (kanoniczny ArtifactRightPanel/SPEC-A accordion: Właściwości/metryki + później przyciski akcji). Poprawka wewnętrzna, dzień 10 UI NIE scalony do czasu poprawki. DECYZJE DOKOŃCZENIA = PEŁNE TERAZ: (1) wysyłka zaproszeń — realny dostawca (ICS/email), nie tylko status w bazie; (2) edycja serii cyklicznych — pełna „to / to i następne / wszystkie". Idzie do dyżuru Codexa dzień 16 (Meetings dokończenie: przepływ notatka→Materials H, uczestnicy U, cykliczność, wysyłka, + prawy panel karty). |
| DEC-2026-08-26-83 | Superadmin dzień 15 — STOP §Q.1 i18n test | **SUPERVISOR_DECISION** | STOP zasadny (Codex nie tknął chronionego testu, nie sfałszował PASS). Autoryzacja: createRealUseTranslation('pl') w mocku i18n PlatformOperationsView.test.tsx — realne polskie tłumaczenia zamiast surowych kluczy, BEZ zmiany asercji (test ćwiczy realną ścieżkę i18n P.4, nie osłabienie). Jedyny dozwolony dotyk tego pliku. Po zielonym: commit P.2/P.4, P.6 polish+zrzuty, dokończenie §Q. Wznowienie wydane. |
| DEC-2026-08-26-84 | Interview Creator Shell dzień 13 — odbiór | **SUPERVISOR_ACCEPT_CONDITIONAL→MERGED** | Powłoka S.0-S.7 potwierdzona (flaga fail-closed OFF z realnym czytelnikiem, dowód OFF behawioralny, WizardModal opt-in — dwaj żywi konsumenci nietknięci, geometria 1 tokenem, glass+fallback, Hub-diff=0, parytet K.1 z prototypem, zero crimson, i18n 16/16). Atrapa stopki „autozapis" NAPRAWIONA przez nadzorcę (126e14ac80 — uczciwy tekst do czasu S.5). STOP C-O1 ODRZUCONY (misklasyfikacja — instrukcja §S.5 już przesądza localStorage v1 z formatem klucza; budować, nie eskalować). STOP K.1 (13. typ wyniku — prototyp nazywa tylko 11 z 13) = DECYZJA PIOTRA. STOP C-O2 (API listy wykluczonych sesji) = prawdziwy BRAK_API, przyszły backend. Merge do m03 jako kod martwy za OFF. Kontynuacja: S.5 autozapis wg gotowego kontraktu + K.2/K.3 kroki 2-3 po decyzji K.1. |
| DEC-2026-08-26-85 | Superadmin dzień 15 — odbiór bezpieczeństwa | **SUPERVISOR_ACCEPT** | Odbiór adwersaryjny Opus potwierdził bit-for-bit w żywym kodzie + realdb: TRI-MUST-07/11 zamknięte (S.1/S.2 sessions/all guard roli + DELETE sprawdza przynależność; S.3 4 destrukcyjne /admin-data load-then-check org; S.5 audit/api-usage org z tokenu — kontrole negatywne cross-org 6/6 PASS), TRI-MUST-08 (A.2 uniwersalny audit projection), TRI-MUST-12 (T.2 reaktywacja audytowana). Migracja 20260826_day15 addytywna+idempotentna (świeży PG). Z16/effectiveAccessService NIETKNIĘTY. Zakres = lista WOLNO, zero plików zakazanych. Merge do m03. OTWARTE (uczciwe STOP-y, przyszła praca, NIE regresje): zawieszenie org realnie nie blokuje logowania (suspended poza middleware/AuthController — TRI-MUST-12 domknięcie), scheduled_events brak na świeżej bazie (TRI-OBS-18), api_logs bez kolumn api_key_id/tokens/cost (BRAK_API). Katalogi P33/P.1-P.4 za flagą OFF — polish-pass §P.6 przed pokazaniem Piotra. POTWIERDZENIE: drugi niezależny odbiór Opus dał identyczny werdykt (SQL-readback na żywym PG: obcy tenant→404, wiersze nietknięte). TRI-MUST-08/12 NIE raportować jako w pełni domknięte — audyt TAK, uniwersalność projekcji (3/5 tabel, 20/83 pisarzy) i egzekwowanie zawieszenia NIE (decyzje właściciela + przyszła praca). |
| DEC-2026-08-26-86 | Dzień 16 — STOP Bloku 0 (node_modules + namespace migracji) | **SUPERVISOR_DECISION** | (1) Symlink `node_modules` z /Users/piotrwisniewski/Developer/Consultify AUTORYZOWANY jako ustalony wzorzec programu (odczyt zależności; zakaz zapisu/edycji/czytania WIP pozostaje) — dotyczy WSZYSTKICH dyżurów, nie tylko 16; `npm ci` w worktree niewskazane. (2) KOREKTA: namespace 20260914_ ORAZ 20260915_ są zajęte (artifact_export_receipts_immutability, user_data_export_receipts); najwyższa migracja = 20261074. Dzień 16 używa 20261075_meetings_day16_* rosnąco, z obowiązkowym sprawdzeniem `ls server/migrations \| grep '^<numer>'` przed każdym plikiem. REGUŁA OGÓLNA dla przyszłych instrukcji: NIE zgadywać numeru migracji w instrukcji — nakazać wyznaczenie „najwyższy istniejący + 1" w Bloku 0. |
| DEC-2026-08-26-87 | Dzień 16 — odbiór warstwy 1 (kod vs kontrakt) | **SUPERVISOR_VERDICT** | MERGE WSTRZYMANY do FIX-ów. Odbiór adwersaryjny Opus: bezpieczniki PASS (0 plików poza Meetings), tenant-safety 5 nowych tras PASS (org z tokenu + load-then-check), strażnicy wysyłki PASS (captured/blocked_demo przed mailerem, LIVE wymaga jawnej flagi+SMTP — DEC-65 potwierdzony w kodzie), migracja 20261075 addytywna+idempotentna. P1: (1) ICS DTSTART/DTEND błąd 2h — wartość UTC etykietowana TZID bez konwersji; (2) injection linii ICS przez niewalidowany recurrenceRule (CR/LF); (3) kod tras zależy od niewykonanej migracji (ensureMeetingTables nie tworzy tabel day16) — wymóg: migracja przed kodem przy deployu; (4) test icsBuilder utrwala błąd TZID; (5) status I.1 zawyżony (brak VTIMEZONE/foldingu/poprawnego escapingu parametrów/STATUS:CANCELLED). FIX-y wewnętrzne zlecone (gałąź codex/day16-fixes-20260826, Sonnet). ROZSTRZYGNIĘCIE H.1: STOP Codexa zasadny WYŁĄCZNIE dla opcji A (jedna transakcja — wymaga zmiany współdzielonego artifactRegistryService, Z17); kontrakt §H.1 pkt 5 dopuszcza opcję B (jawny stan failed propozycji + błąd z możliwością ponowienia) NIEWYMAGAJĄCĄ zmian współdzielonych — H.1 wraca do wykonania opcją B (przyszły blok, nie FIX). Warstwa 2 (runtime, świeży PG) w toku. |
| DEC-2026-08-26-88 | Partia 1 — prawy panel Meetings + warsztat kryterium | **OWNER_ACCEPT** | Cytat właściciela (26.08, po partii zrzutów jasny+ciemny): „zatwierdzam powyższe". Obejmuje: (1) prawy panel karty spotkania (Akcje + Właściwości, kanon ArtifactRightPanel/SPEC-A) — scalony do m03 wraz z fixem StandardArtifactShell defaultOpen (gałąź codex/meetings-rightpanel-20260826 a6b74f67ce; zamyka poprawkę z DEC-82); (2) warsztat kryterium Audits — WARIANT A we wszystkich 4 pytaniach prototypu (fazy 4: Planowanie 1-3/Badanie 4-8/Ustalenia 9-12/Naprawa 13-18; dowody i ustalenia jako StandardTable w karcie fazy; metryczka kanoniczna z grupą „Rola i uprawnienia" pierwszą we Właściwościach; primary Menu 1 = akcja bieżącego ogniwa zmienna per faza) — budowa wewnętrzna wg formuły polerowania, za flagą OFF, akcept końcowy na zrzutach zbudowanego ekranu; (3) plan dnia 16: FIX-1..9 + merge po odbiorze z regułą migracja-przed-kodem. Właściciel prosi: przyszłe decyzje zbierać PYTANIAMI. |
| DEC-2026-08-26-89 | Chat — producent sygnałów D1/D2/D3 | **OWNER_ACCEPT** | Rekomendacje B/B/B przyjęte pytaniem 26.08: D1 zakres domen fali 1 = EXECUTION + DECISION + RESULTS(KPI) + FINANCE (10 reguł deterministycznych); D2 = warstwa deterministyczna ON, warstwa AI (INTERPRETED) zbudowana w tym samym programie ale za flagą OFF do akceptu właściciela na zrzutach (zgodne z DEC-40); D3 = feed organizacyjny z filtrem roli, sprawy własne wyróżnione (audience_user_id/audience_role). Projekt: CHAT_SIGNALS_PRODUCER_DESIGN_2026-08-25.md. Realizacja: duży blok mechaniki tylnej dla Codexa (instrukcja w produkcji); front feedu = wewnętrznie. |
| DEC-2026-08-26-90 | My Work — rail Inspektora: 3 sekcje legacy | **OWNER_ACCEPT** | Komentarze, Załączniki i Rysowanie ze starego RowDetailPanel znikają ŚWIADOMIE — zgodnie z zaakceptowanym prototypem (DEC-76). Parytet pozostałych sekcji naprawiony (codex/mywork-rowdetail-parity-20260826: kolumny edytowalne inline, Powiązania z realnych krawędzi grafu, sekcja 8 „Historia i AI"; 33/33 testy). Włączenie ff_ideaInspectorRightRail: PO scaleniu + live smoke (lekcja regresji 25.08 — 5/6 zakładek), zrzuty w evidence/rowdetail-parity-20260826. |
| DEC-2026-08-26-91 | Superadmin — egzekwowanie zawieszenia organizacji | **OWNER_ACCEPT** | Zawieszenie org = TWARDA blokada: logowanie i API odcięte dla użytkowników zawieszonej organizacji (poza ścieżkami superadmina), czytelny komunikat dla użytkownika. Domyka lukę z DEC-85 (suspended poza middleware/AuthController) i TRI-MUST-12. Realizacja wewnętrzna (obszar auth = trudny kod, Opus + odbiór adwersaryjny), nie Codex. |
| DEC-2026-08-26-92 | Dzień 16 — podpis i scalenie | **SUPERVISOR_ACCEPT** | Warunki DEC-87 spełnione: FIX-1..9 wykonane wewnętrznie (gałąź codex/day16-fixes-20260826; ICS w formie UTC z sufiksem Z — bez TZID/VTIMEZONE, walidacja RRULE whitelist FREQ/INTERVAL/COUNT/UNTIL/BY* + zakaz CR/LF, STATUS:CANCELLED, DQUOTE parametrów, folding 75 oktetów, spy not.toHaveBeenCalled na mailerze dla captured/blocked_demo, izolacja awarii per odbiorca ze statusem failed, PII z logów info usunięte, fallback:false w odczytach day16 — brak migracji = głośny błąd). Warstwa 2 na świeżym PG: migracja 20261075 czysta+idempotentna, testy 8/8, kontrole negatywne PASS. Spot-check nadzorcy w kodzie wykonany. SCALONE do m03 (60be0fac3d). REGUŁA WDROŻENIOWA: migracja 20261075 wykonywana PRZED kodem przy deployu na staging (procedura consultify-promocja-demo). Pozostały zakres dnia 16 (H opcją B, C.1-C.2 pełna edycja serii, U.4/U.5, G.2, UI uczestników) = dyżur „Meetings blok 2" na bazie po tym merge. |
| DEC-2026-08-26-93 | Day14 dozbrojenie + licencja rozszerzona | **SUPERVISOR_ACCEPT** | Wykonane wewnętrznie (codex/day14-dozbrojenie-20260826, scalone): (1) K.1 trend — 9 testów HTTP routera (33/33), testy realny PG; ZNALEZISKO KRYTYCZNE potwierdzone realną bazą: kpiTrend sortował periodEnd stringowo a node-postgres zwraca Date → 500 na trendzie każdego KPI z 2+ pomiarami; LICENCJA rozszerzona przez nadzorcę na kpiTrend.ts — naprawa periodEndTime() (Date|string→epoch), test realpg zielony 2/2; (2) S.2 ROI q-filter — walidator (zakres DEC-77) + wpięcie route+repository wzorem listKpis (ILIKE przez współdzielone resultsTextMatchPattern, filtr w CTE widoczności, tylko rc.title); testy 26/26 router + 6/6 realpg (escaping %_, negatyw tenanta obustronny, zgodność wsteczna bez q). Zakres = 9 plików wyłącznie Results (zweryfikowane trzykropkowym diffem). Wzorzec potwierdzony: bloki realdb wykrywają błędy niewidoczne dla mocków — utrzymywać w każdym dyżurze backendowym. |
| DEC-2026-08-26-94 | My Work rail — smoke + flip flagi | **SUPERVISOR_ACCEPT** | Live smoke na żywym runtime (własny PG, login, realne klikanie): PASS — 5/5 nazwanych widoków Tabeli + 6/6 układów dostępne przy railu ON (historyczna regresja „5/6 zakładek" NIE reprodukuje się), rail 8/8 sekcji, edycja z readbackiem po pełnym przeładowaniu, Notatnik+Sejf czyste. Zastrzeżenie smoke domknięte przed flipem: przyczyna WARN resolveUserRoleForTable = destrukturyzacja requireRole z obiektu PermissionsService gubiła this (table-platform.routes.ts:41-55) — naprawiona wywołaniem na obiekcie + test na realnym serwisie. Flip: ff_ideaInspectorRightRail default ON (priorytety query>localStorage>env>default zachowane, wyłączalna przez off); bezpiecznik legacy nietknięty (stare org z grafem legacy celowo zostają na trybie uproszczonym). Testy 69/69. Scalone do m03. Spełnia DEC-74 (stan pokazywany = domyślny runtime) i domyka warunek P0 z DEC-76/90. |
| DEC-2026-08-26-95 | Dzień 18 — STOP Bloku 0 (rozejście bazy) | **SUPERVISOR_DECISION** | STOP zasadny (bezpiecznik świeżości zadziałał; zero zgadywania). Przyczyna: po związaniu markera c31155205e nadzorca scalił do m03 rail-flip (DEC-94) — tip uciekł do 516c104e56. Rozejście = dokładnie 5 plików: rejestr decyzji (docs), table-platform.routes.ts + test, ideaInspectorRightRailFlag.ts + test — ZERO kolizji z zakresem dnia 18 (chat signals, server/src/services+routes+migrations). ROZSTRZYGNIĘCIE: obowiązuje DOKŁADNY START z codex/day18-instrukcja-20260826 (baza bez zmian, marker c31155205e pozostaje wiążący); wymóg świeżości §0.5 uznany za spełniony atestacją nadzorcy; scalenie z nowszym m03 wykonuje nadzorca przy odbiorze. REGUŁA OGÓLNA: rozejście marker→tip bez kolizji zakresowych rozstrzyga nadzorca wklejką (bez rebase w trakcie dyżuru). |
| DEC-2026-08-26-96 | Dzień 17 — twardy STOP Z9 (pomiar wejściowy na złej bazie) | **SUPERVISOR_DECISION** | STOP zasadny i CENNY: obowiązkowy pomiar wejściowy OKR połączył się z bazą inną niż autoryzowany kontener 5447 (brak tabeli okr_vnext_objectives = to nie była zmigrowana baza scratch); Codex nie ruszył implementacji. ROZSTRZYGNIĘCIE: kolejność Bloku 0 skorygowana — NAJPIERW kontener (port 5447) + pełne migracje projektu, DOPIERO potem pomiar wejściowy, z env jawnie przypiętym (DATABASE_URL na 5447 + RUN_DB_TESTS=1) i dowodem celu połączenia (SELECT current_database(), inet_server_port() w logu raportu). ZAKAZ ABSOLUTNY uruchamiania jakichkolwiek testów DB bez jawnego DATABASE_URL wskazującego kontener dyżuru — dopisać do przyszłych instrukcji jako Z19. Rozejście bazy m03 (jak DEC-95) objęte tą samą atestacją. |
| DEC-2026-08-26-98 | Dzień 17 — drugi STOP Z9 + KOREKTA reguły + rezerwacja numerów migracji | **SUPERVISOR_DECISION** | (1) STOP formalnie zasadny (Codex sam się złapał — postawa wzorowa), ale SKUTEK ZEROWY, zweryfikowane osobiście przez nadzorcę: `localhost:5432/iris_test` to NIE obca baza, tylko wewnętrzny fallback konwencji testowej repo (tests/setup.ts:385-387 — nazwa historyczna projektu); fallback ustawiany WYŁĄCZNIE gdy brak jawnego DATABASE_URL (`if (!process.env.DATABASE_URL)`), MOCK_DB domyślnie true, a na localhost:5432 NIE MA żadnego działającego Postgresa (brak klienta/serwera, jedyny kontener to cx-day18-pg) — połączenie nie mogło dojść do skutku, zero zapisu, zero skażenia czegokolwiek. (2) KOREKTA Z9 (poprzednia wersja zabijała cały dyżur i marnowała dowieziony postęp): Z9 przerywa DANĄ CZYNNOŚĆ, nie cały dyżur. Procedura: zatrzymaj komendę → ustal skutek (czy było realne połączenie i czy był zapis) → zapisz ustalenie w raporcie → przypnij env → KONTYNUUJ pozycję. Twardy STOP całego dyżuru TYLKO przy stwierdzonym realnym ZAPISIE do bazy spoza dyżuru. (3) MECHANIZM (obowiązkowy, do wszystkich instrukcji): każda komenda testu DB z prefiksem env w tej samej linii — `DATABASE_URL=postgresql://<user>@localhost:<port_dyżuru>/<baza_dyżuru> RUN_DB_TESTS=1 NODE_ENV=test npx vitest run <plik>`; setup.ts szanuje jawny DATABASE_URL (potwierdzone plik:linia). (4) REZERWACJA NUMERÓW MIGRACJI (zapobiega kolizji dyżurów równoległych — dzień 17 i 18 liczyłyby „najwyższy+1" niezależnie i oba wzięłyby 20261076): dzień 17 = **20261076-20261079**, dzień 18 = **20261080-20261089**, dzień 19 = **20261090-20261099**. Reguła „najwyższy+1" obowiązuje TYLKO wewnątrz przydzielonego przedziału. |
| DEC-2026-08-26-97 | Warsztat kryterium V2 (Audits) — akcept właściciela | **OWNER_ACCEPT** | Cytat właściciela na zrzutach (26.08): „jest super". Zamyka wyjątek z DEC-81 (ekran, o którym właściciel powiedział „nie rozumiem co to jest, nie wygląda dobrze"). Zbudowany 1:1 wg prototypu wariantu A×4 (DEC-88): 18 ogniw w 4 fazach z zachowaną kolejnością (Planowanie 1-3/Badanie 4-8/Ustalenia 9-12/Naprawa 13-18), StandardTable dowodów i ustaleń wewnątrz karty fazy, prawy panel w kolejności kanonicznej z grupą „Rola i uprawnienia" jako pierwszą we Właściwościach, primary Menu 1 zmienny per bieżące ogniwo (2 stany uczciwie disabled z powodem zamiast atrapy). MECHANIKA NIETKNIĘTA (przeprojektowana wyłącznie powłoka i układ). FIX-PASS NADZORCY przed pokazaniem właścicielowi: pierwsze zrzuty miały surowe enumy EN na twarzy (document/nonconforming/partial/confirmed) — odesłane do naprawy, słowniki PL dla wszystkich enumów + pełne daty w Historii; bramka „Piotr nigdy pierwszym testerem wizualnym" zadziałała. Flaga ff_criterionWorkspaceV2 → default ON (DEC-74), wyłączalna przez off/0/false; V1 przy jawnym off renderuje się identycznie (test na realnym module flagi). Testy 31/31, zakres 16 plików kodu + dowody. BRAK_API zbudowane jako uczciwe „Planowane"/disabled (nie atrapy): eksport karty do PDF, przypomnienie o odpowiedź zarządczą, dosłowne „Zamknij kryterium" (mapowane na realne „Zamknij ustalenie"), komentarze do kryterium, przedłuż termin/przypisz respondenta — status po-MVP DO POTWIERDZENIA właściciela (DEC-40). Znalezisko poboczne do osobnego ticketu: RemediationPanel pokazuje surowy ownerUserId (błąd pre-existing w V1). |
| DEC-2026-08-26-99 | Audyty — nazwane normy zewnętrzne w danych demo | **OWNER_ACCEPT** | Panel ekspercki wykrył: dev-render/screens/audyty-warsztat-kryterium.tsx:237/:683 pokazuje „ISO 9001:2015, pkt 8.4.1" i packId pack-iso9001-zakupy — WBREW komentarzowi w nagłówku tego samego pliku, wbrew siostrzanemu harnessowi audyty-piec-powierzchni.tsx:88-90 i wbrew polityce G00 (named external standards OFF, brak licencji na treść normy). Trafiło na zrzut zaakceptowany w DEC-97. WERDYKT WŁAŚCICIELA: usunąć nazwane normy z danych demonstracyjnych — generyczne nazewnictwo (np. „Wymaganie wewnętrzne WZ-8.4.1"); produkt nadal pozwala KLIENTOWI wpisać własną normę (to jego licencja, nie nasza). Naprawa zlecona wraz ze sweepem całego dev-render/screens (ISO 9001/27001/14001, IATF). |
| DEC-2026-08-26-100 | Panel ekspercki — PILOTAŻ METODY + wynik Audytów | **SUPERVISOR_REPORT + OWNER_DIRECTIVE** | Pilotaż metody z DEC-79 wykonany na module Audyty: 5 niezależnych obiektywów (partner firmy doradczej 5,0 · audytor wiodący ISO 7,5 · projektant UX B2B 6,5 · inżynier jakości 7,5 · sceptyk-adwersarz 4,5), wagi uzasadnione, każda teza z dowodem plik:linia. WYNIK ZBIORCZY **6,0/10** przy celu 9,5 — moduł NIE idzie do CLOSED_FINAL. Trzy powody rozstrzygające: (1) deliverable nie istnieje jako ekran — nie da się przeczytać ani wyeksportować raportu z audytu (jedyna rzecz, którą firma doradcza oddaje klientowi); (2) brak rejestru niezgodności/CAPA MIMO GOTOWEGO BACKENDU (findings.routes.ts:36/47/58 nieużywane); (3) bramki odbioru G08-G20 NOT_STARTED, Owner verdict PENDING — DEC-81/97 zamknęły wygląd, nie kompletność produktu. WZORZEC DIAGNOSTYCZNY DO POWTÓRZENIA W KAŻDYM MODULE: zderzenie „backend to ma" z „frontend tego nie woła" dało największe znaleziska — czynić to jawnym krokiem metody. DYREKTYWA WŁAŚCICIELA: doprowadzić Audyty do ~8,0 teraz (naprawy 1-7: rejestr ustaleń, widok treści raportu, skala/paginacja kryteriów, CTA Nowy audyt, sygnalizacja przeterminowania, 7 poprawek punktowych, dane demo), dobicie do 9,5 (eksport PDF, detektor niezależności, G08-G20) = DRUGI PRZEBIEG po panelach pozostałych 15 modułów. |
| DEC-2026-08-26-101 | Statusy organizacji realnie blokujące dostęp | **OWNER_ACCEPT** | Trzeci audyt wykrył: organizacja może mieć status locked (emergency lockdown), purge_scheduled lub expired, a ŻADEN z nich nie odcina dostępu — blokuje wyłącznie suspended. Czyli „awaryjna blokada tenanta" nikogo nie blokowała. WERDYKT WŁAŚCICIELA: blokują **suspended + locked**; purge_scheduled i expired NIE blokują — zostają jako osobna, łagodniejsza ścieżka produktowa (wygasły trial dostaje komunikat o odnowieniu, nie 403). Realizacja: BLOCKING_ORG_STATUSES + AuthController login (znał tylko pending/blocked/suspended), z kontrolą negatywną że purge_scheduled i expired przechodzą. |
| DEC-2026-08-26-102 | Bramka fazy stagingu (TRI-MUST-05) — WYKONANA | **SUPERVISOR_ACCEPT** | Kandydat 315adbb83b uruchomiony OD ZERA na czystej bazie: **842 migracje, 0 błędów**, drugi przebieg „Applying migrations: 0" (idempotencja) — TRI-MUST-05 SPEŁNIONY. Aplikacja realnie startuje (backend+frontend lokalnie, login przez test-support bootstrap), przejście po WSZYSTKICH 16 pozycjach menu: **16/16 renderuje się, ZERO odpowiedzi 5xx, ZERO białych ekranów, ZERO error boundary**. Zastrzeżenie metodyczne odnotowane uczciwie przez wykonawcę: pierwszy przebieg bez ENABLE_V8_GLOBAL=true dał masowe 404 na /api/v8/* — to poprawne działanie bramki funkcji, nie awaria; po korekcie env wszystko 200 (znaczenie dla deployu: konfiguracja env stagingu MUSI zawierać ENABLE_V8_GLOBAL=true, inaczej połowa produktu jest niewidoczna). DO DOMKNIĘCIA (zlecone, gałąź codex/staging-fixes-20260826): (1) mieszanka PL/EN na twarzy Inicjatyw/Realizacji/Organizacji — pełne PL wg DEC-09; (2) błąd zapytania SQL na /api/v8/execution-control/timeline-warnings maskowany cichym fallbackiem 200 (wzorzec tępiony przez program); (3) moduł Rezultaty renderuje placeholder „KPI registry not yet enabled" za flagą kpiRegistry — do ustalenia czy funkcja gotowa (wejście do decyzji właściciela, sprzeczne z DEC-74 jeśli gotowa); (4) schema drift organization_members.permission_scope + FK violation w bootstrapie testowym. |
| DEC-2026-08-26-103 | Panel ekspercki — moduł Ocena/Assessment | **SUPERVISOR_REPORT** | Wynik **4,0/10** przy celu 9,5 (partner 4,0 · ekspert metodyczny 4,5 · UX 5,0 · inżynier jakości 3,0 · sceptyk 2,5). NAJSŁABSZY MODUŁ ZBADANY DOTĄD. Trzy powody rozstrzygające: (1) DELIVERABLE NIE ISTNIEJE — zakładka Raport renderuje tę samą macierz co Matrix plus jedno zdanie na obszar (39 wierszy „C — · T — · Δ —"); kontrakt właściciela (7 rozdziałów, synteza 120-180 słów, komentarz per obszar, Eksportuj wszystko) ma ZERO implementacji; serwer ma /export/pptx i /export/deck bez konsumenta, a jedyny przycisk PDF stoi w martwym komponencie i jest błędny (destrukturyzuje {pdfUrl} z funkcji zwracającej Blob → window.open(undefined), zamaskowane przez as any). (2) MODUŁ AI-NATIVE NIE MA AI — 26 endpointów /api/assessment/:projectId/ai/* zamontowanych i działających, ZERO żywych konsumentów (oba panele AI mają 0 importerów); widoczne bloki AI mają disabled:true + „Coming soon"; „Teresa" to sklejony na froncie template string („Proponowany poziom: ${focusLevelFallback}"), a werdykt jakości to evidence.length>0. (3) ZAAKCEPTOWANA ARCHITEKTURA JEST WYŁĄCZONA — drdMethodWorkspaceSliceV1/methodWorkspaceShellV1 default OFF, więc domyślnie działa stary edytor liczący current=max (DRDAssessmentEditor.tsx:486, DRDMatrixSession.tsx:127) — WPROST ZAKAZANE przez DEC-38 (bez kumulacji). Dodatkowo: 30 martwych komponentów (~438 KB), 12 metod api.ts wołających nieistniejące trasy (404), 8 pustych handlerów pod widocznymi przyciskami (Pomiń/rozstrzygnięcie = dekoracja), słownik kodów Pomiń z DEC-55 = 0 linii kodu, brak TO-BE (DEC-37), 3 sprzeczne modele osi DRD (7 osi w kliencie vs 5 osi w serwerowej ścieżce AI), rejestr 28 uwag właściciela: 0 zrealizowanych. PODJĘTE NATYCHMIAST: (a) PILNA weryfikacja runtime czy odpowiedzi sesji trafiają do bazy czy zostają w localStorage konsultanta (drdHttpSourceOfTruthV1 default false + DrdSessionRuntime opisany jako BROWSER-LOCAL) — podejrzenie utraty danych klienta; (b) tania partia napraw 1-5 (gałąź codex/assessment-fixes-20260826). |
| DEC-2026-08-26-104 | Panel ekspercki — moduł Inicjatywy + KOREKTA DEC-60 | **SUPERVISOR_REPORT** | Wynik **4,0/10** przy celu 9,5 (partner 5,0 · PMO 4,0 · UX 4,5 · inżynier jakości 4,5 · sceptyk 3,0). PARADOKS MODUŁU: NAJLEPSZY BACKEND W CAŁYM KORPUSIE (~330 endpointów, event-sourcing, wersjonowanie agregatów z blokadą optymistyczną, rozdzielone request/decide na bramkach, uczciwa dyscyplina UNKNOWN≠0 — fundament wart 8-9) pod produktem wartym 4. Z trzech zakładek działa jedna (rejestr — dobra, kanoniczna). ZNALEZISKA ROZSTRZYGAJĄCE: (1) **12 672 linie UI fizycznie NIEOSIĄGALNE** — Analysis/** (7317), PortfolioScenarioSurface, Goals, Candidates, SourceProposal, PortfolioHealth, Observability; cztery martwe gałęzie renderContent w Hubie (activeTab portfolio/candidates/portfolioHealth/observability), a CANONICAL_INITIATIVES_TABS dopuszcza tylko list/plan/capacity i efekt aktywnie kasuje inne. (2) **ATRAPA AI wbrew DEC-51** („zakaz atrapy AI", nakaz zdjęcia ikony AI z deterministycznego porządkowania): przycisk „Analyze" z ikoną Sparkles w Plan Workbench uruchamia sortowanie topologiczne z przypisaniem i-tej inicjatywy do i-tego okresu (planAnalysisProposal.ts:56-115, zero LLM); drugi przycisk „AI: Analyze selection" w Menu 3 otwiera modal zmiany właściciela z trzema polami wyłączonymi literalnym {false && ...}; „Kandydaci AI" liczy fitScore z DŁUGOŚCI TEKSTU. (3) **Solver planu to atrapa planowania**: periods[min(index, len-1)] — przy 4 kwartałach i 50 inicjatywach pozycje 5-50 lądują wszystkie w Q4 z identycznymi datami (wynik wygląda wiarygodnie i jest bezwartościowy); gotowy model PlannedWindow (freeze/FS-SS-FF/lag-lead) NIEWPIĘTY. (4) **Burza autoryzacyjna O(3N) bez paginacji** — GET runtime-v1/initiatives autoryzuje per WIERSZ (3 zapytania SQL bez cache każdy: 50 inicjatyw ≈ 150 zapytań, 500 ≈ 1500), mimo że authorizeProjects deduplikujący po projectId istnieje w tym samym pliku. (5) updateInitiativeStatusWriteTruth RZUCA ZAWSZE — każde kliknięcie akcji statusu na karcie kończy się angielskim toastem deweloperskim (kanban zabezpieczony poprawnie, karta nie). (6) createCapacityOptions istnieje z pełną domeną i NIE JEST WOŁANY — kanoniczna pętla „Analizuj→Zaproponuj→Zastosuj" przerwana w środku. (7) Plan i Obciążenie po angielsku w polskim produkcie. **KOREKTA REJESTRU: DEC-2026-08-25-60 raportuje „P.2 usunięcie martwych gałęzi Huba ZROBIONE_WG_DoD" — NIEPRAWDA, zweryfikowane osobiście przez nadzorcę na tipie m03: InitiativesHub.tsx:1559/1565/1640/1653 zawierają wszystkie cztery martwe gałęzie. Bramka DoD nie sprawdziła osiągalności. Wniosek metodyczny: DoD dyżurów MUSI wymagać dowodu OSIĄGALNOŚCI (nie tylko istnienia kodu) — dopisać do szablonu instrukcji.** |
| DEC-2026-08-26-105 | Zawieszenie organizacji — egzekwowanie SCALONE (TRI-MUST-12) | **SUPERVISOR_ACCEPT** | Realizacja DEC-91+DEC-101 po TRZECH niezależnych audytach adwersaryjnych (każdy obalał kompletność poprzedniego — to działało). OSIEM frontów wejściowych egzekwuje zawieszenie jednym strażnikiem i wspólnym cache: login/JWT, klucze API sk_, klucze integracyjne ik_ (piąta furtka, audyt 1+2), Socket.IO handshake, Socket.IO join:org, sweep otwartych połączeń, 3× surowe ws collab (upgrade), SCIM (**siódma furtka — audyt 3**), rejestracja z access-code, POST /api/invitations/accept (**ósma furtka — audyt 3**), wszystkie 3 punkty mintujące refresh token. NAPRAWIONE PO DRODZE (znaleziska audytów, nie deklaracje): FAIL-OPEN — dbGet bez fallback:false czynił catch martwym i cacheował „niezawieszona" na pełny TTL przy błędzie DB (docy modułu kłamały vs runtime — złota reguła #1); REGRESJA DoS wniesiona przez samą zmianę — cache karmiony klienckim join:org bez limitu (naprawa: członkostwo PRZED strażnikiem + brak cache dla pustego wiersza + twardy limit z eksmisją); wyjątek SUPERADMIN oparty na surowym claimie → teraz koniunkcja claim ∧ users.role z BAZY; REGRES TYPÓW (3 błędy tsc, w tym jeden unieważniający typowo warstwę fail-open, którego własny test nie sprawdzał, bo dubel miał zbyt wąską sygnaturę). DOWODY: tsc — gałąź dokłada 0 błędów (10 zastanych = 10 na bazie); 150/150 testów rodziny DEC-91; 9/9 na realnym PG z fail-closed (zły DATABASE_URL wywala plik, nie daje zielonego skipu); kontrola mutacyjna per brama (SCIM 9/11 czerwone, zaproszenia 3/5, fail-open 1 — test niemożliwy do napisania przed naprawą typu); bramy asertują odmowę przez BRAK ZAPISÓW, nie przez sam kod statusu. Spot-check nadzorcy wykonany. Statusy blokujące: suspended + locked (DEC-101); purge_scheduled i expired jawnie NIE blokują (osobna lista w kodzie, żeby odróżnić „rozważone i wykluczone" od „nie pomyślano"). ŚWIADOMIE ZOSTAJE: zróżnicowana koperta ORG_LOCKED na 2 z 8 powierzchni (reszta blokuje locked poprawnie, ale zwraca generyczne ORG_SUSPENDED — resolveBlockingOrgStatus gotowe do podpięcia); okno TTL ≤30 s przy >1 replice (pub/sub po ustaleniu liczby replik); GET /api/invitations/validate/:token ujawnia istnienie zaproszenia zawieszonego najemcy (tylko odczyt). TRI-MUST-12 DOMKNIĘTY w części egzekwowania. |
| DEC-2026-08-26-106 | Assessment — podejrzenie utraty danych klienta OBALONE | **SUPERVISOR_VERDICT** | Panel ekspercki (DEC-103) zgłosił podejrzenie, że odpowiedzi z sesji DRD zostają w localStorage konsultanta (drdHttpSourceOfTruthV1 default false + DrdSessionRuntime opisany jako BROWSER-LOCAL). WERYFIKACJA NA ŻYWYM RUNTIME (świeży PG, 691 migracji, realne klikanie): **OBALONE**. Dowody: (1) POST /api/method/sessions/:id/events → 201 z echem zdarzenia ANSWER_CONFIRMED; (2) SQL wprost do bazy — wiersz w method_events z treścią odpowiedzi (DRD nie ma tabel drd_*, siedzi na wspólnym silniku method-core: method_sessions/method_events/method_outputs); (3) druga karta z CZYSTYM localStorage odtworzyła postęp i odpowiedź w pełni z serwera; w localStorage wyłącznie method-core:http-cache z metadanymi, zero treści. PRZYCZYNA FAŁSZYWEGO ALARMU: AssessmentSessionEditorView.tsx (~114-122, ~1751-1758) IGNORUJE flagę i zwraca framework===drd zawsze true, przekazując forceHttpSourceOfTruth bezwarunkowo; DrdMethodWorkspaceScreen.tsx:~837 czyta flagę tylko jako fallback — więc drdHttpSourceOfTruthV1 NIGDY nie jest odczytywana na realnej trasie. Stary lokalny DrdSessionRuntime = martwy kod na ścieżce produktu. Opis flagi w useFeatureFlags.tsx to NIEAKTUALNA DOKUMENTACJA. **LEKCJA METODYCZNA DLA PANELI EKSPERCKICH: panel czytający wyłącznie kod i komentarze może dać fałszywy alarm — każde znalezisko klasy „dane/bezpieczeństwo/działa-czy-nie" WYMAGA weryfikacji na żywym runtime przed wpisaniem do rejestru jako fakt. Dopisać do metody paneli.** |
| DEC-2026-08-26-107 | Dzień 18 (sygnały Chat) — odbiór warstwy 1 | **SUPERVISOR_VERDICT** | MERGE WSTRZYMANY do FIX-ów. RDZEŃ DETERMINISTYCZNY ODEBRANY POZYTYWNIE i zweryfikowany niezależnie na żywym PG (nie deklaratywnie): migracje, evaluator, **8 z 10 reguł realnych** z kompletem 4 testów każda (trafienie/nietrafienie/auto-resolve/negatyw tenanta) 39/39 PASS, tenant-safety obu tras POTWIERDZONE (org z tokenu, jawne odrzucenie body.organizationId ≠ token; dowód mutacyjny: neutralizacja filtru org → testy czerwone), zakaz severity=BLOCKER egzekwowany constraintem bazy, adapter X.1 realny (nie no-op), 59/59 + 18/18 odtworzone. KOLIZJA MIGRACJI NAPRAWIONA przy odbiorze (20261076/77 → 20261080/81, commit 7613a9730f; ŹRÓDŁEM KOLIZJI BYŁA INSTRUKCJA — jej §0.1 pkt 7 kazała wziąć „najwyższy zastany + 1", Codex wykonał literalnie; errata z przedziałami DEC-98 wpisana do instrukcji). P0 BLOKUJĄCY: **warstwa AI jest MARTWA, nie „częściowa"** — signalInterpreter.ts:101 czyta output.proposals, a llmService.generateResponse zwraca {content, usage}; pole proposals nie istnieje nigdy, JSON z content nie jest parsowany → zawsze zero propozycji. Dowód behawioralny odbiorcy: flaga ON + provider + budżet + 6 sygnałów + model zwracający POPRAWNY JSON → signalsOpened=0, zero INSERT-ów. Wszystkie 8 testów interpretera przechodzą, bo wstrzykują własne dependencies.generate — DOMYŚLNE OKABLOWANIE NIE JEST TESTOWANE W OGÓLE (wzorzec do wpisania w metodę: test wstrzykujący zależności NIE dowodzi ścieżki produkcyjnej). P1: producent psuje się niewidocznie (cichy catch bez logu + rejestratory bez logger/catch wbrew wzorcowi job7b — ten sam wzorzec co FIX-9 dnia 16); pęcznienie ledgera przy fladze OFF (~96 wierszy/dobę/org); FAŁSZYWA PROWENIENCJA — ai_run_id = losowy UUID udający referencję zamiast null; **FAŁSZYWA DEKLARACJA BRAK_DANYCH** dla res.roi_confidence_dropped — dane ISTNIEJĄ (kpi_attribution_snapshots z overall_confidence/confidence_reasons/computed_at + rvn_roi_forecast_versions); osłabiony strażnik izolacji (asercja parametrów zastąpiona bezzębnym 404 — dowód mutacyjny: usunięcie filtru org NIE wywala testu; ratuje silniejszy test real-PG). STOP R.1 ZASADNY (MODULE_ACCEPTANCE 13_CHAT nie ma atomowego CHAT-OWN-004, tylko zbiorczy wiersz 001-017; rozbicie dotknęłoby 16 innych pozycji). DO ROZSTRZYGNIĘCIA: DEC-89 D2 mówi „warstwa deterministyczna ON", a ENABLE_SIGNAL_PRODUCER ma default false — bez osobnego kroku włączenia po odbiorze frontu D1/D2 NIE JEST DOSTARCZONE. FIX-y 1-7 zlecone wewnętrznie. |
| DEC-2026-08-26-108 | Dzień 19 (Meetings blok 2) — odbiór warstwy 1 | **SUPERVISOR_VERDICT** | MERGE WSTRZYMANY do FIX-ów; NIE odrzucenie. POTWIERDZONE: zero naruszeń Z17 (pusty diff na WSZYSTKICH 10 plikach licencjonowanych — dowód Bloku 5 dostarczony) i Z18; zero dziur tenant (5 nowych tras, org z tokenu, load-then-check, kontrole negatywne cross-org 5/5); strażniki wysyłki dnia 16 NIETKNIĘTE i zielone; moduł nadal closed; migracja 20261090 addytywna, idempotentna, bez FK, numer w przydzielonym przedziale; MODULE_ACCEPTANCE NIE zawyżony (diff dodaje dokładnie jeden token: CZĘŚCIOWO); U.4 resolver dla załączników poprawny (title/href = null po odebraniu dostępu, brak zwrotu title_snapshot); rozszczepienie serii zrobione dobrze (UNTIL w UTC, nowy master ze split_from_meeting_id, przepięcie wyjątków, zero DELETE). **H.1 OPCJA B — MECHANIZM PRZECHODZI WYMUSZONĄ AWARIĘ**: odbiorca sam wykonał brakujący dowód (mock rzucający na materializeProposal): awaria → 500 MATERIALIZATION_FAILED, rejestr failed/stage=receipt, 0 pokwitowań; ponowienie → 200, 1 pokwitowanie, materiał BEZ ZMIAN (ten sam artifact_id). Codex jest winien TEST, nie funkcję — to nie blokuje. P0 BLOKUJĄCY: **ATRAPA Z ZEWNĘTRZNYM SKUTKIEM** — DELETE /:id/occurrence dla scope=all i this_and_following zwraca 200, w bazie ZERO zmian (cancel konsumowany tylko w gałęzi this, meetingOccurrenceService.ts:105), PO CZYM ROZSYŁA METHOD:CANCEL DO UCZESTNIKÓW — czyli ludzie dostają odwołanie spotkania, które nadal istnieje; dla this_and_following dodatkowo powstaje nowy AKTYWNY master i też idzie CANCEL. Raport twierdzi „anulowanie TAK, wyjątek cancelled" — nieprawda dla 2 z 3 zakresów. P1: NIEZADEKLAROWANA REGRESJA — deklarowane „98/98 PASS" to zawężony wybór; w zakresie narzuconym przez §0.4a własnej instrukcji jest 164/167, a dyżur wprowadził DWIE czerwone (meetingBoundaryMountedAuth 3/4→2/4 oraz golden meeting.m12 49/49→48/49, wspólny korzeń: zmiana kontraktu targetRecordId), z czego raport zgłasza jedną; trzeci tknięty test osłabiony (toBe→not.toBe) bez wymaganego wpisu przed/po. P2: retry bez warunku wstępnego tworzy osierocony artefakt draft/private dla notatki ODRZUCONEJ (kontrakt: reject → zero materiałów); GET /:id/notes zwraca materialTitle po JOIN tylko po organization_id, bez resolvera (luka wskazana przez samą instrukcję — do zamknięcia osobno); trasy occurrence bez requireMeetingAdmin. STOP H.2 ZASADNY (createInitiativeService nie ma idempotencyKey nigdzie, dodanie = Z17), z zastrzeżeniem: funnel My Work idempotencyKey PRZYJMUJE i był do zbudowania pod tym samym nagłówkiem. FIX-1..4 zlecone wewnętrznie. Po nich merge dozwolony w zakresie: H.1, H.3, U.4 załączniki, U.5-TYŁ, C.1 PATCH (trzy zakresy), migracja. |
| DEC-2026-08-26-109 | Inicjatywy — tania partia napraw SCALONA | **SUPERVISOR_ACCEPT** | Wykonane wewnętrznie po panelu (DEC-104), gałąź codex/initiatives-fixes-20260826, 46 plików: **253 wstawki / 13 083 usunięcia**. (1) USUNIĘTE 12 824 linie NIEOSIĄGALNEGO UI z potwierdzeniem zerowej osiągalności per plik (grep w src/, server/src/, barrel index.ts, importy dynamiczne): Analysis/** (8264 z testami — dostępne tylko przez martwy barrel, eksport też usunięty), PortfolioScenarioSurface, Goals (704+293), SourceProposal (214+578), Candidates (311+285), Observability (374+273), PortfolioHealthTable (sierota), server/src/routes/initiatives.routes.ts (320, niezamontowany — Gateway montuje INNY plik pmo/initiatives.routes.ts) + 4 martwe gałęzie renderContent w Hubie. ZACHOWANE świadomie: PortfolioHealthView (472 linie) — funkcja zdrowia portfela jest realna i wartościowa, usunięto tylko martwą gałąź montującą, plik zostaje z komentarzem jak go odsłonić (rekomendacja panelu na później). (2) NARUSZENIE DEC-51 ZAMKNIĘTE: „Analyze" z ikoną Sparkles → „Uporządkuj wg zależności" (deterministyczny sort, zero LLM); „AI: Analyze selection" → „Zmień właściciela" + usunięte 3 martwe pola {false && ...} z kaskadowym czyszczeniem stanu. (3) Akcje statusu na karcie (rzucały ZAWSZE angielskim toastem deweloperskim) → statusActions=[] z polskim tooltipem kierującym do bramki cyklu życia — wzorem poprawnie zrobionego kanbanu. (4) Uczciwość pigułki „AI readiness": heuristicReview zwracał degraded/model:heuristic, ale rollup to gubił — teraz odznaka „bez AI" + tooltip; użytkownik wie, że żaden model tego nie liczył. (5) Bramka AI rozszerzona o ANTHROPIC_API_KEY i OPENAI_API_KEY (ai.routes.ts:364) z zachowaniem fail-closed — ODBLOKOWUJE DEC-59, gdy właściciel poda klucz Anthropic. (6) Alias watchers→OverviewSection zastąpiony REALNĄ sekcją Obserwujący: znaleziono działający endpoint /initiatives/:id/watchers i dane już pobierane, ale nigdy nieużywane — uruchomiono martwe handleToggleWatch/isWatching. Testy 29/29, esbuild czysty, JSON lokalizacji zwalidowany. Zgłoszone do backlogu: klucz initiatives.kanban.dragDisabled bez tłumaczenia PL. |
| DEC-2026-08-26-110 | Dzień 18 (sygnały Chat) — FIX-y wykonane, SCALONY | **SUPERVISOR_ACCEPT** | Wszystkie 7 FIX-ów z DEC-107 zamknięte (10 commitów). **P0 NAPRAWIONY Z DOWODEM**: parseInterpretedProposals parsuje output.content (obsługuje płot ```json i wycina obiekt), błąd parsowania → PARTIAL + errors, NIGDY cicha pustka; dodane 3 TESTY DOMYŚLNEGO OKABLOWANIA wołające runInterpretationForOrganization BEZ wstrzykniętych dependencies — (a) poprawny JSON → realne propozycje zapisane, (b) śmieci → PARTIAL+errors, (c) poprawny JSON BEZ klucza proposals (dokładnie oryginalny bug) → PARTIAL+errors. Wcześniej 100% testów omijało ścieżkę produkcyjną. Pozostałe: obserwowalność producenta wg wzorca job7b (logger.error + .catch), koniec pęcznienia ledgera przy fladze OFF, ai_run_id = null zamiast losowego UUID udającego referencję, przywrócona asercja strażnika izolacji Z DOWODEM MUTACYJNYM (usunięcie filtru org → test PADA), usunięte martwe wpisy FeatureFlags, cofnięty reformat poza licencją, przywrócone .toUpperCase() w mute-type (+ dodatkowa naprawa znaleziona przez robotnika: filtr w signalReadModel musiał dostać UPPER po OBU stronach, bo signal_type jest realnie małymi literami — bez tego regresowałby test 18/18), nowy test odrzucenia severity=BLOCKER na żywym PG. RAPORT SKORYGOWANY: res.roi_confidence_dropped przekwalifikowana z fałszywego BRAK_DANYCH na ŹRÓDŁO_INNE + STOP z kandydatami (kpi_attribution_snapshots, rvn_roi_forecast_versions); fin.benefit_not_realized uzupełniony o 8 zweryfikowanych kandydatów; ujawniona zmiana zachowania kluczy prediction:*. Migracje przenumerowane do przedziału 20261080/81. Testy końcowe na żywym PG: 59/59 + 28/28 jednostkowych. SCALONE do m03. STAN DOSTARCZENIA DEC-89: 8/10 reguł deterministycznych ZBUDOWANYCH, obie flagi nadal OFF — D1/D2 będą dostarczone dopiero po włączeniu flag (osobny krok po odbiorze frontu feedu); 2 reguły czekają na decyzję właściciela o źródle danych. |
| DEC-2026-08-26-111 | Dzień 19 (Meetings blok 2) — FIX-y wykonane, SCALONY | **SUPERVISOR_ACCEPT** | Wszystkie 4 FIX-y z DEC-108 zamknięte, każdy z ODTWORZENIEM BŁĘDU PRZED NAPRAWĄ jako dowodem (robotnik chwilowo cofał własną naprawę i potwierdzał, że defekt wraca — wzorzec do utrzymania). **FIX-1 (P0, atrapa z zewnętrznym skutkiem)**: wybrany wariant (a) REALNE ODWOŁANIE, wykonalny BEZ ruszania recurrenceEngine (Z17) — użyto kolumny recurrence_status, już używanej przez gałąź this dla pojedynczego wyjątku, ale na poziomie korzenia serii, gdzie zawsze była NULL (zapis czysto addytywny, nie nadpisanie żywych danych): scope=all ustawia recurrence_status=cancelled na masterze; scope=this_and_following tworzy split-master OD RAZU jako cancelled zamiast aktywnego. Zero DELETE serii. 3/3 na realnym routerze i PG. Koniec sytuacji, w której uczestnicy dostawali odwołanie kalendarzowe spotkania, które nadal istniało. **FIX-2 (P1)**: ustalony wspólny korzeń obu regresji — DEC-87 (H.1 opcja B) zmienił kontrakt targetRecordId z note.id na id realnego artefaktu, a dwa testy tego nie odzwierciedliły; zmiana kontraktu jest POPRAWNA (to sens H.1), więc zaktualizowano TESTY (cold-read realnego targetRecordId + jawna asercja not.toBe(noteId)), nie kod; golden wrócił na 49/49. **FIX-3 (P1, uczciwość pomiaru)**: raport podaje teraz pełny zakres §0.4a — 16 plików / 193 testy: przed naprawami 189/193 (2 zastane + 2 wprowadzone i przemilczane), **po naprawach 191/193, zero regresji wprowadzonych przez dyżur**; oba pozostałe czerwone są zastane i nazwane. **FIX-4 (P2)**: precondition w retryMeetingNoteMaterialization (approved LUB ostatnia próba failed, inaczej RETRY_NOT_ALLOWED→409) — koniec osieroconych artefaktów draft/private dla notatek odrzuconych. SCALONE do m03 w zakresie: H.1 (opcja B, mechanizm potwierdzony pod wymuszoną awarią przez odbiorcę), H.3, U.4 załączniki, U.5-TYŁ, C.1/C.2 (trzy zakresy PATCH + odwołanie), migracja 20261090. POZOSTAJE do przyszłego bloku: pełna macierz G.2, T.1/T.2, test DST, H.2 (BRAK_API — createInitiativeService bez idempotencyKey; funnel My Work idempotencyKey PRZYJMUJE i jest do zbudowania), luka materialTitle w GET /:id/notes (JOIN bez resolvera — wskazana przez samą instrukcję), trasy occurrence bez requireMeetingAdmin. Moduł Meetings pozostaje ZAMKNIĘTY bramką closed. |
| DEC-2026-08-26-112 | Naprawy po bramce stagingu — SCALONE + ZNALEZISKO SYSTEMOWE | **SUPERVISOR_ACCEPT** | Cztery naprawy z DEC-102 wykonane (gałąź codex/staging-fixes-20260826). **NAJWAŻNIEJSZE — ZNALEZISKO SYSTEMOWE**: przyczyna maskowanego błędu SQL na /api/v8/execution-control/timeline-warnings to NIE literówka, tylko **dziura w mechanizmie migracji**: kolumny initiatives.sla_deadline i blocked_at istnieją WYŁĄCZNIE w plikach 294_execution_center.sql, 061_initiative_lifecycle.sql, 247_initiative_enhancements.sql — których nazwy NIE PASUJĄ do wzorca autorun /^(7\\d{2}|\\d{8})_/ w migrationIdentity.ts. Na świeżej bazie te kolumny NIE POWSTAJĄ. Błąd ginął całkowicie po cichu, bo DbPromise.all ma fallback:true ORAZ błędnie klasyfikował: sprawdzał err.message.includes(does not exist), co w Postgresie pasuje ZARÓWNO do brakującej tabeli (nieszkodliwe) JAK I do brakującej KOLUMNY (prawdziwy błąd schematu). Naprawa dwuwarstwowa: (a) migracja addytywna 20261100_execution_control_initiatives_missing_columns.sql; (b) isSilenceableMissingRelationError() w DbPromise.ts — cichnie WYŁĄCZNIE dla brakującej relacji, błąd kolumny ZAWSZE loguje głośno. Zweryfikowane odtworzeniem zapytania na realnym PG (przed: ERROR column sla_deadline does not exist; po: 0 rows bez błędu). **SKALA DO USTALENIA: ~524 plików .sql w server/migrations jest spoza wzorca autorun — zlecony AUDYT INTEGRALNOŚCI SCHEMATU (czy schemat budowany od zera jest kompletny wobec użycia w kodzie).** POZOSTAŁE NAPRAWY: i18n — główna przyczyna znaleziona i naprawiona (EXECUTION_MENU3 → getExecutionMenu3(t): ~53 twarde angielskie etykiety w pasku filtrów na WSZYSTKICH 5 zakładkach Realizacji, potwierdzone zrzutem przed/po); test-support: usunięte 3 martwe bloki odwołujące się do nieistniejącej kolumny organization_members.permission_scope + naprawiona FK admin_audit_logs (literał test-support zawsze łamał NOT NULL → users(id), cicho łapane). UCZCIWE ZASTRZEŻENIE WYKONAWCY: dwie z zaplanowanych poprawek i18n trafiły w MARTWY KOD przesłonięty nowszymi komponentami (InitiativeCreationWizard, ExecutionReportsSurface) — nieszkodliwe, zgłoszone jako osobne zadania sprzątające. REKOMENDACJA ws. flagi kpiRegistry (moduł Rezultaty pokazywał placeholder): **GOTOWE DO WŁĄCZENIA, nie niedokończone** — ResultsKpiRegistryPage 1643 linie, kpi.routes.ts 1150 linii, pełny cykl draft→edit→submit→approve/reject z blokadą samo-zatwierdzania, 36/36 testów; jedyne ograniczenie: nie da się edytować szkicu KPI, którego wersji definicji ta sesja nie utworzyła (świadomie zablokowane). DECYZJA O WŁĄCZENIU = WŁAŚCICIEL po odbiorze wizualnym (reguła #7). Konflikt scalania rozstrzygnięty na rzecz usunięcia martwych gałęzi wg DEC-109. |
| DEC-2026-08-26-113 | Audyty — dwie partie napraw po panelu (6,0/10) | **SUPERVISOR_ACCEPT (gaps scalone, polish w scalaniu)** | PARTIA A „gaps" (SCALONA, gałąź codex/audits-gaps-20260826, flaga ff_auditsFindingsAndReportView OFF) — zasypuje DWIE NAJWIĘKSZE DZIURY z DEC-100: (1) **zakładka Ustalenia** = rejestr niezgodności/CAPA na StandardTable+StandardPreview z selektorem programu, realnie woła GET /audits/findings, /findings/statistics (pasek statystyk), /findings/systemic (baner tematów systemowych), POST /findings/:id/review|accept-risk|close (kebab), GET /audits/actions (kolumna Termin — najbliższy otwarty termin działania, bo ustalenie samo terminu nie niesie), GET /audits/evidence (rozwiązanie ID dowodów na tytuły) — czyli endpointy, które istniały i NIE MIAŁY KONSUMENTA; (2) **czytelny widok raportu** = ekran-artefakt SPEC-A Dokument (ArtifactBreadcrumb+NModeShell+ArtifactRightPanel), 8 sekcji na żywo z GET /audits/reports/:id/presentation, primary Zatwierdź/Opublikuj, eksport PDF jako uczciwe „Planowane". FIX-PASS NADZORCY przed pokazaniem właścicielowi: surowy enum VERIFIED na twarzy raportu — naprawiony U ŹRÓDŁA (PACK_CLASSIFICATION_LABELS w reportRenderer.ts, pełny słownik PL wraz z wartościami legacy) plus znaleziony przy okazji fallback do surowego ownerUserId w sekcji Odpowiedzialności; błędna odmiana liczebnika („1 możliwych tematów") — helper plForm(n, one, few, many) z regułą 1/few/many i wyjątkiem 12-14, użyty też w pasku „Razem N ustaleń". Testy 153. PARTIA B „polish" (gałąź codex/audits-polish-20260826, flaga ff_auditsScaleAndPolish OFF, 172/172 testy, 22 zrzuty): **DEC-99 WYKONANE** — nazwy norm ISO usunięte z danych demo Audytów (sourceReference/packId w warsztacie kryterium + drd-report); rozróżnienie kategorii ryzyka utrzymane: ISO w danych innych modułów zostaje, bo tam to FAKT BIZNESOWY KLIENTA („klient ma certyfikat ISO 27001"), nie twierdzenie Consultify o posiadaniu licencji na treść normy; pełnoekranowa lista kryteriów (AuditCriteriaBrowser — skala 100-300 kryteriów zamiast okienka 208 px); CTA „Nowy audyt" (zamyka naruszenie TRIADA_KANON); chip „Po terminie"; 7 poprawek warsztatu (cel przewijania Teresy, UUID→nazwisko, toast+obsługa błędu Kopiuj link, zamykanie kebaba na Escape/klik obok, pełny powód disabled, przewijanie chipów Menu 3, warunkowanie wierszy uprawnień rolą); korekta 3 błędnych komentarzy „default OFF". PRZY OKAZJI naprawiony blokujący defekt harnessu (brak mocka /audits/criteria + złe nazwy pól MOCK_COVERAGE → AUDITS_API_CONTRACT_ERROR przy każdym zaznaczeniu sesji). Scalenie partii B wymaga rozwiązania 3 konfliktów (auditStatusTones, AuditsMethodHub, harness) — zlecone; obie zmiany mają współistnieć, każda za SWOJĄ flagą. WŁĄCZENIE OBU FLAG = po akcepcie właściciela na zrzutach (partia 3 wysłana). |
| DEC-2026-08-26-114 | Audyty — partia „polish" SCALONA + uwaga właściciela o pasku Ustaleń | **SUPERVISOR_ACCEPT + OWNER_CHANGE** | Scalenie dwóch równoległych partii Audytów wymagało rozwiązania 3 konfliktów; ZASADA: obie strony poprawne, obie zostają, każda za SWOJĄ flagą (nie wybór albo-albo). PRZY SCALANIU ZNALEZIONY REALNY BUG: obie gałęzie dodały własny bezwarunkowy handler `/audits/criteria` w harnessie — handler z „polish" (64 generyczne kryteria) był PIERWSZY i PRZESŁANIAŁ handler z „gaps" (2 kryteria Metalpol, do których referencjonują ustalenia METALPOL_CRITERION_1/2); skonsolidowane w jeden handler z rozgałęzieniem po programId. Testy po scaleniu 168/168, obie flagi OFF. **UWAGA WŁAŚCICIELA (zrzut zakładki Ustalenia): „nie wiem czy ta linia jest tu konieczna? to powinno wyskakiwać w wynikach — ale poza tym graficznie wszystko super".** ROZSTRZYGNIĘCIE: selektor programu ZOSTAJE (konieczność techniczna — GET /audits/findings wymaga programId, bez niego nie ma czego pokazać); statystyki („Razem N ustaleń · N potwierdzonych · N zamkniętych") i pigułka „N możliwych tematów systemowych" USUNIĘTE z paska listy. PRZENIESIENIA DO WYNIKÓW NIE WYKONANO — zgodnie z warunkiem bezpieczeństwa nadzorcy: AuditOutputsTab nie ma dziś ani selektora programu (listOutputs() jest globalne, nie programId-scoped, w przeciwieństwie do endpointów statystyk), ani żadnej sekcji kafli metryk; osadzenie wymagałoby PRZEBUDOWY zakładki, a nie dołożenia. LUKA ZGŁOSZONA DO OSOBNEGO ZLECENIA PO PROTOTYPIE (zasada: poprawka jednej rzeczy nie przemebluje po cichu drugiej). |
| DEC-2026-08-26-115 | Ocena/Assessment — tania partia napraw SCALONA | **SUPERVISOR_ACCEPT** | Po panelu 4,0/10 (DEC-103), gałąź codex/assessment-fixes-20260826. METODA GODNA UTRWALENIA: usuwanie martwego kodu przez SKRYPT ANALIZY GRAFU IMPORTÓW z domknięciem tranzytywnym (plik żywy tylko jeśli osiągalny z realnego wejścia: AppRoutes / inny żywy plik / harness — NIE z samego testu), nie przez pojedyncze grepy; po usunięciu ponowny przebieg = 0 martwych. Robotnik SAM ZŁAPAŁ WŁASNY BŁĄD: pierwsza analiza zakwalifikowała assessment-reports.routes.ts (top-level) jako martwy, a to REALNA IMPLEMENTACJA, którą plik w podfolderze tylko opakowuje — zweryfikowane i wycofane (nadzorca potwierdził osobno: plik jest w drzewie). USUNIĘTE: 45 komponentów frontu (~438 KB), 6 plików tras backendu (top-level duplikaty niemontowanych — Gateway montuje wyłącznie wersje z routes/assessment/), martwy agregator routes/assessment/index.ts wraz z jedynym odwołaniem, 4 pliki testów. ATRAPY POD PRZYCISKAMI ZLIKWIDOWANE: onSkip/onResolutionAction/onBack/onSaveStay → realne działania (recordAnswer, nawigacja, acknowledgeFailure); pozycja assign_question bez API → ukryta z notatką „Planowane". **SŁOWNIK 4 KODÓW POMIŃ (DEC-55) ZBUDOWANY**: skipReasonCodes.ts z kodami maszynowymi poza_modelem_operacyjnym / poza_zakresem_zlecenia / odroczone_do_kolejnej_rewizji / zastapione_innym_rozwiazaniem; wolne pole tekstowe usunięte, wybór kodu wymagany. **KOORDYNACJA Z DNIEM 20**: kernel zdarzeń nie ma pola skipReason (zmiana kontraktu współdzielonego z Audits/SIRI — zakazana rozstrzygnięciem nadzorcy), więc front zapisuje etykietę przez istniejące justification z prefiksem; dzień 20 buduje model w warstwie Assessmentu przyjmujący DOKŁADNIE te identyfikatory — PO ODBIORZE DNIA 20 TRZEBA PRZEPIĄĆ FRONT NA ZAPIS KODU, nie etykiety (pozycja otwarta). USUNIĘTE 11 martwych metod api.ts wołających nieistniejące trasy (404); i18n nagłówków i enumów; ekran Ustawienia oczyszczony (UUID pod „Szczegóły techniczne", usunięte „Zapis: CLEAN" i atrapa „Status subskrypcji: do potwierdzenia przez backend", blokery zamrożenia pokazują realne powody zamiast gołej liczby); „Zatwierdzenia"→„Akceptacje" (DEC-56). AI: sprawdzono 26 endpointów — POST /:id/ai/executive-summary pasuje, ale wymaga klucza, którego brak w środowisku → ukryte z „Planowane" zamiast fałszywego działania (odblokuje się po DEC-59). Testy 178/185, 7 czerwonych potwierdzonych jako zastane na baseline. |
| DEC-2026-08-26-116 | LUKA SCHEMATU BAZY OD ZERA — audyt + domknięcie | **SUPERVISOR_ACCEPT — ZNALEZISKO KRYTYCZNE PROGRAMU** | **AUDYT** (zlecony po znalezisku z DEC-112) ustalił mechanizm, którego nikt wcześniej nie opisał: istnieją DWA niezależne runnery migracji z DWOMA ledgerami i DWOMA regułami odkrywania plików. (1) Runner WDROŻENIOWY (server/scripts/migrate.postgres.ts przez release-migration-gate, ledger schema_migrations) — NIE MA wzorca nazw; prawdziwym filtrem jest isSqliteOnlyMigration(), który hurtem wyklucza KAŻDY plik o numerze 1..499 (poza 4 promowanymi) oraz seed/mock/demo/add_/000_initdb_/sqlite/fts5. (2) Runner RUNTIME (tablePlatform/migrationRunner przy boocie, ledger tp_migration_history) — używa wzorca /^(7\\d{2}|\\d{8})_/ + allowlisty. Trzeciej ścieżki nie ma. SKUTEK: z 1058 plików stosowanych jest 846, a **212 nigdy** — w 40 z nich siedzą producenci obiektów REALNIE UŻYWANYCH przez kod. Na świeżej bazie brakowało **174 obiektów** (105 w ścieżkach ZAPISU, 36 odczyt bez fallbacku, 33 odczyt z cichym fallbackiem). DLACZEGO NIKT TEGO NIE WIDZIAŁ: demo i staging to STARE bazy sprzed reguły — mają te tabele fizycznie, bez wpisu w ledgerze; **zagrożone było każde NOWE środowisko** (staging od zera, DR-restore, e2e w CI, nowy tenant). Trzeci mechanizm maskujący: ~344 CREATE TABLE IF NOT EXISTS rozsianych po server/src (aplikacja dorabia sobie schemat leniwie w runtime, poza ledgerem i poza bramką). **DOMKNIĘCIE**: migracja 20261120_fresh_db_schema_gap_closure.sql — **106/106 obiektów klasy ONLY_DEAD utworzonych (zakres SZERSZY niż 78 z briefu: 79 tabel + 27 kolumn, bo risk_clean.json był przefiltrowaną czubką rozkładu)**; 80 CREATE TABLE IF NOT EXISTS, 27 ADD COLUMN IF NOT EXISTS, 189 CREATE INDEX IF NOT EXISTS, **ZERO DROP/ALTER typów/INSERT** (potwierdzone przez nadzorcę). Tłumaczenia SQLite→PG odnotowane przy każdej tabeli (DATETIME→TIMESTAMP ×28, datetime(now)→to_char ×21, randomblob→gen_random_uuid ×7, AUTOINCREMENT→BIGSERIAL, BOOLEAN DEFAULT 1→TRUE). Pułapka wykryta: security_settings miało DWÓCH producentów — wariant „poprawny" deklaruje UUID, a organizations.id/users.id są text, więc FK by padł; użyto wariantu zgodnego z realnym schematem. 6 nazw indeksów przemianowanych, bo w PG nazwa indeksu jest globalna w schemacie i CREATE INDEX IF NOT EXISTS byłby CICHYM NO-OP. **DOWODY**: świeża baza EXIT=0, drugi przebieg 0 pending, 106×to_regclass=t; DOWÓD NO-OP na bazie już zawierającej obiekty — diff snapshotów: kolumny/typy/DEFAULT/NOT NULL (22 862 wiersze) **0 różnic**, indeksy z pełnym indexdef (5 850) **0 różnic**, constrainty (5 882) **0 różnic**, 0 wierszy danych. **BRAMKA WZMOCNIONA**: nowy check schema_coverage_critical_relations — 22 relacje ryzyka 1 sprawdzane przez to_regclass PO zastosowaniu łańcucha, z komunikatem ZAKAZUJĄCYM poszerzania wzorca autorun jako „naprawy". DOWÓD EMPIRYCZNY, ŻE ŁAPIE: na bazie z tymczasowo usuniętą migracją 11 dotychczasowych checków = PASS (ledger zielony), nowy check = FAIL z listą 22/22 braków — dokładnie ta ślepota, którą audyt opisał (bramka porównywała ledger z drzewem, nigdy drzewa ze schematem). **POZOSTAJE — DECYZJA PRODUKTOWA PER MODUŁ (NIE inżynierska)**: 150 obiektów klasy NO_MIGRATION (107 tabel + 54 kolumny), których ŻADEN plik nie tworzy — albo funkcja jest żywa i trzeba dopisać schemat, albo kod jest martwy i trzeba go usunąć. Najgorętsze: rvn_visible_resources, task_dependencies.predecessor_id/successor_id, kpi_entries, kpi_results, initiative_raids, ai_usage_log, billing_invoices, assessment_scores, webauthn_credentials, interview_sessions.user_id, consultant_org_links (middleware kontekstu org!), organization_resource_usage (middleware kwot!), async_jobs. REKOMENDACJA AUDYTORA (przyjęta): rozpisać per moduł w strukturze MODULE_ACCEPTANCE i domykać modułami — jedna zbiorcza migracja stworzyłaby tabele bez właściciela semantycznego. DO WERYFIKACJI PRZY DEPLOYU: read-only SELECT to_regclass na demo/stagingu dla 78 nazw ONLY_DEAD (teza „demo je ma" pozostaje inferencją do czasu sprawdzenia — wymaga zgody właściciela, zdalne bazy pod DEC-65). |
| DEC-2026-08-26-117 | Audyty — POWTÓRNY panel (runda 2): 6,0 → 6,5 | **SUPERVISOR_REPORT** | Metoda „panel→naprawy→ponowny panel" dowiozła wzrost MIERZALNY ale MARGINALNY (+0,5; partner 5,0→6,0 · audytor ISO 7,5= · UX 6,5→7,0 · inżynier 7,5→7,0 · sceptyk 4,5→5,5). Naprawy REALNE (nie kosmetyka): dwa nowe ekrany wołają realne endpointy, flagi nie są fantomami, 183/183 testy, kanon niezłamany. ALE ZNALEZISKO ROZSTRZYGAJĄCE: **naprawa odtworzyła dokładnie ten antywzorzec, za który moduł dostał niską ocenę** — backend ma TRZY renderery raportu (renderAuditReport = 13 sekcji z macierzą traceability, ZAPISYWANY w audit_reports.payload i objęty contentHash; renderRemediationProgressReport; renderPresentationView = 8-sekcyjny deck dla zarządu), a nowy ekran ignoruje payload i renderuje ZAWSZE deck z /presentation — **użytkownik zatwierdza i publikuje dokument, którego nie przeczytał** (plomba hash liczona z treści, która nigdy nie trafiła na ekran). Ponadto: rejestr ustaleń CICHO UCIĘTY do 50 (limit domyślny, total odbierany i ignorowany, zero stronicowania); filtr przeglądarki kryteriów działa TYLKO na bieżącej stronie (zbudowanej „dla skali 100-300"!); kolumna Termin bywa cicho błędna (listActions też ucięte do 50); deliverable nadal nie do WYTWORZENIA (finalize/create report bez wołających) ani WYEKSPORTOWANIA; CAPA w rejestrze CAPA nie ma; window.prompt() jako wejście do niezmiennego śladu audytu; PAKIET DOWODÓW NIEAKTUALNY względem tipa (zrzuty pokazują usunięty pasek statystyk i stare „ISO 9001:2015" — właściciel akceptowałby obrazek stanu, który nie istnieje); martwy kod wszedł do merge (getFindingStatistics/getSystemicFindings bez wołających + kłamiący nagłówek pliku); G08-G20 nadal NOT_STARTED. **WERDYKT: NADAL NIE do CLOSED_FINAL; FLAGI NIE WCHODZĄ do pokazania właścicielowi przed naprawą R1-R3** (R1 renderować payload 13 sekcji z /presentation jako drugim trybem „Widok dla zarządu"; R2 stronicowanie + total + filtr ponad stronicowaniem; R3 świeży pakiet dowodów na tipie + usunięcie martwego kodu). Droga do 9,5: R4-R8 (produkcja deliverable w UI, eksport PDF/DOCX, CAPA w rejestrze, realny formularz Nowego audytu + zespół, statystyki w Wynikach) ~10-12 dni + bramki G08-G20. **LEKCJA METODYCZNA (wpisać do briefów napraw): zlecać WYNIKOWO („audytor ma dowieźć raport klientowi"), nie literalnie („zbuduj ekran raportu"); obowiązkowy krok briefu naprawczego = wypisz WSZYSTKIE endpointy obszaru i zaznacz, który front woła.** |
| DEC-2026-08-26-118 | Panel ekspercki — moduł Narzędzia (Tools): 5,0/10 | **SUPERVISOR_REPORT** | Wynik: partner 5,0 · konsultant-praktyk 5,0 · UX 6,5 · inżynier 5,5 · sceptyk 3,5. DIAGNOZA INNA NIŻ WSZĘDZIE: Tools nie jest słaby — jest PRZERWANY W POŁOWIE. Warstwa wejścia NAJLEPSZA W CAŁEJ APLIKACJI (19 katalogów metodycznych z silnikami syntezy, realne AI ze strumieniowaniem i walidatorem ugruntowania, karta narzędzia którą właściciel nazwał „jest super", kanon list wzorowy, i18n 0 braków PL). Warstwa WYJŚCIA istnieje w bazie i kontrolerach (migracja 946: tool_outputs z content_hash/version/supersedes/frozen_at; kontroler z approve/reopen/rewizjami; testy na realdb) — ale NIE MA Z NIEJ WYJŚCIA DO INTERFEJSU. Trzy zdania rozstrzygające: **0 ze 125 sesji nigdy nie ukończono; zakładka „Outputs" modułu Narzędzia nie pokazuje ANI JEDNEGO wyniku narzędzia (bootstrap scala sesje+raporty Assessment+report-builder+decki, ale NIGDY nie woła Api.listToolOutputs, który istnieje); w całym module nie ma jednej linijki generującej Word/PowerPoint/Excel** (jedyny eksport = html2canvas→raster-PDF). Ponadto: kontrakt dedicatedToolTypes deklaruje 31 typów przy 16 realnych gałęziach (zabezpieczenie przed dryfem samo jest dryfem); fantom STRATEGY_TOOL_DOCS={} żywy od 08-13; „unknown status: CONFIGURING" na twarzy (brak wpisu w słowniku statusów); zakładka Reports = filtr cudzych rekordów Assessment; 108 użyć crimson primary-* jako dekoracji + 71 hexów; zero pracy grupowej. ROZLICZENIE KORPUSU ~88 uwag właściciela (próbka 29): zrobione 13 (45%) · częściowe 3 · NIEZROBIONE 13 (45%) — ASYMETRIA WAGI: wszystkie zrobione to kosmetyka/mechanika, wszystkie niezrobione to ARCHITEKTURA ŁAŃCUCHA WARTOŚCI (DEC-32 Outputs→Insights niewykonane mimo decyzji z 25.08, rejestr raportów, wspólny kreator inicjatyw, etap Recommendations, 4 klasy wyników — runtime ma JEDNĄ: initiative). Kontrakt SWOT-003 NIE zaginął — leży w rejestr/3-DO-ODBIORU/, 1020 linii. Governance uczciwe (bramki otwarte nie oznaczone jako DONE; jeden rejestr sam sprostował własne kłamstwo). NAPRAWY 1-4 = MNIEJ NIŻ DZIEŃ i likwidują trzy z sześciu potknięć właściciela z żywego odbioru; naprawy 1-6 → ~7,5. Do potwierdzenia na runtime: czy migracja 946 wykonała się (nagłówek mówi „NIE uruchamiamy w tej fali") — bez tego naprawa #1 nie ma sensu. |
| DEC-2026-08-26-119 | Dzień 20 (Assessment backend) — odbiór warstwy 1 | **SUPERVISOR_VERDICT** | UCZCIWY KANDYDAT CZĘŚCIOWY POTWIERDZONY, **ZERO P0** — żadnej atrapy, dziury tenanta, naruszenia bezpiecznika ani fałszywej deklaracji. Odbiorca odtworzył niezależnie 9/9 testów (własny kontener, Z21 spełniony realnie: produkcyjny eksport routera, produkcyjne singletony, prawdziwe JWT, zero wstrzykniętych zależności) i wykonał 9 WŁASNYCH sond adwersaryjnych (wszystkie zielone: obcy tenant 403 + zero zapisu, organizationId w body → TENANT_CONTEXT_MISMATCH, poziomy 0/−1/2,5 → 400 nigdy 500, łańcuch supersesji poprawny). NAJMOCNIEJSZA POZYCJA: słownik kodów Pomiń (D.1) — CHECK w bazie bajt w bajt zgodny z frontem z DEC-115, append-only, idempotentny, tenant z tokenu, kontrakt czyta KOD nie tekst. Model 7 osi (B.1): serwer = mechaniczna kopia kanonu klienckiego (diff 39 linii = sam Prettier), zweryfikowane po HTTP (poziom 6 na osi 5A przyjęty, 7 odrzucony); NIEDOMÓWIENIE (nie kłamstwo): diff niesie też 28 zmian nazw obszarów osi 5/6 i port silnika scoringu V2 na serwer — w pełni addytywne. STOP-y wszystkie trzy ZASADNE (odbiorca ODTWORZYŁ zdjęcie @ts-nocheck: 203 błędy standalone tsc; agregat tras: nic nie importuje routes/index, ale Z20 wymaga dowodu pozytywnego przed usunięciem 11 handlerów). P1×3 zlecone przed scaleniem: (1) błędny czasownik + 2 pominięte wywołania w inwentarzu A.3 (produkt dla frontu!); (2) zwijanie pominięć do obszaru — ROZSTRZYGNIĘCIE NADZORCY: kontrakt niesie listę per pytanie (unitId+questionId+kod), skipped obszaru = agregat true TYLKO gdy wszystkie pytania pominięte; (3) tabela przed/po na realnym PG dla B.1 (prezentacja zastanych wyników osi 5/6 się zmienia: poziom 5 był 100%→83,3% — korekta zgodna z kanonem, dane nietknięte, ale pomiar obowiązkowy). Po FIX-ach MERGE zakresu B.1+D.1+D.2/E.1+T+A.1. KOORDYNACJA FRONTU (przepięcie zapisu kodu Pomiń): możliwa TYLKO na ekranie HTTP (lustro przeglądarkowe dostanie 404), jako osobny zapis po recordAnswer z Idempotency-Key i retry (dual-write z justification do czasu decyzji o jednym źródle) — DOPIERO po FIX-2. Kontynuacja dyżurowa (nie blokuje): A.2, B.2 (203 błędy TS), C.1, E.2 (wersjonowanie kontraktu — dziś cicho się przesuwa), F.1, R.1. |
| DEC-2026-08-26-120 | Panel ekspercki — moduł Realizacja (Execution): 3,6/10 | **SUPERVISOR_REPORT** | NAJNIŻSZY wynik programu (partner 4,0 · PMO 3,5 · UX 4,0 · inżynier 3,5 · sceptyk 3,0) — nie z braku funkcji (28,9 tys. linii frontu, najlepszy backend runtime-v1 w przeglądzie: 5843 linie, pełna izolacja tenantów, CAS, idempotencja), tylko z KLASY BŁĘDU. ZNALEZISKO PRZEWODNIE — **ŁAŃCUCH CICHEJ ZIELENI (6 niezależnych warstw zamieniających awarię w spokojne zero)**: (1) DbPromise fallback=true nadal zwraca pustkę dla KAŻDEJ klasy błędu (naprawa DEC-112 przywróciła tylko logowanie); (2) dziesięć gołych catch{return []} w serwisach sygnałów bez jednego loggera; (3) brama V8 przy OFF daje 404, a klient uznaje fallback tylko przy 501; (4) trzy warstwy catch w ExecutionHub zamieniają awarię w „brak ryzyk"; (5) flagi controlTowerFailed/isLoadingControlSignals USTAWIANE I NIGDY NIECZYTANE — moduł WIE, że wieża padła, i nie mówi nikomu; (6) buildDegradedFlags nie zna flag awarii → **raport tygodniowy na komitet sterujący renderuje się ze stopką „Freshness: Live · Confidence: high" i zerem ryzyk przy padniętej usłudze ryzyk**. Moduł, którego zadaniem jest mówić że coś idzie źle, ma sześć warstw ukrywających że sam poszedł źle. DRUGIE: **wszystkie zapisy execution-control MARTWE** — middleware requireCanonicalExecutionWriter zwraca 409 na każdy nie-GET, a UI nadal oferuje 6 grup przycisków (budżet, realizacje, mitygacje RAID, kokpit menedżera, cały 868-liniowy AiRecommendationPanel) — zawsze 409. TRZECIE [KLASA BEZPIECZEŃSTWO, do potwierdzenia na żywo]: **wyciek międzytenantowy** — POST /api/execution/:projectId/gate-check liczy decyzje/zablokowane zadania BEZ organization_id (ExecutionController.ts:290,313) — obcy identyfikator zwraca liczby z cudzej organizacji. CZWARTE: przycisk „Load Atelier Toys example" = główna akcja pustego rejestru KPI robi POST ×5 twardych danych testowych DO REALNEGO API (incydent danych, łamie „zero rekordów testowych"). PONADTO: kolejny cichy fallback klasy DEC-112 żywy (safeQueryAll z includes(does not exist) łapiącym też brak kolumny); EVM zeruje koszty po cichu; N+1 (30 realizacji = 61 równoległych żądań na każde otwarcie raportu); 11 martwych komponentów ~3800 linii + 4 flagi-fantomy + 3 pełne ekrany bez wejścia z menu; 53 przyciski klasami btn-* NIEZDEFINIOWANYMI w importowanym CSS; 53 klucze bez PL; generator raportów = formularz debugowy (użytkownik wymyśla „Identyfikator uruchomienia"); placebo „Otwarte raporty" = licznik useState. NA PLUS: dokumentacja dnia 11 UCZCIWA (sama obniżyła sześć własnych werdyktów), naprawa i18n Menu 3 z DEC-112 realna (50 kluczy istnieje), kanon list dotrzymany, model pomiaru workReportModel dobrze zbudowany (MetricValue UNKNOWN≠0 — wzorzec do rozciągnięcia). WERDYKTY: flaga execReportsIntelligence ZOSTAJE OFF; do Piotra z tego modułu idą wyłącznie 3 pytania decyzyjne (E-O3 taksonomia BSC, E-O4 wagi wpływu, E-O5 progi saturacji — blokują miary B3/B4). Partia A (A1-A10 + ukrycie przycisków 409) zlecona. |
| DEC-2026-08-26-121 | NO_MIGRATION — trzy gorące przypadki + permission_scope | **SUPERVISOR_ACCEPT + 2× DECYZJA WŁAŚCICIELA** | SCALONE (4 pliki: 3 migracje addytywne + 1 fix): (1) **async_jobs ŻYWE** — kolejka playbooków AI (Gateway→/api/ai/playbooks→AIPlaybooksController→enqueuePlaybookAdvance, żywy konsument frontowy PlaybookTemplatesListView); na świeżej bazie rzucało „relation does not exist"; migracja 20261121 + ZNALEZIONY SPRZĘŻONY BUG: asyncJobService ładował kolejkę przez require() w repo ESM — require nie istnieje w tym zasięgu ani w dev ani w dist, więc KAŻDE enqueue padało NIEZALEŻNIE od tabeli; naprawione wzorem await import() z aiQueue.ts, zweryfikowany pełny cykl życia joba (queued→running→failed→reset). (2) **organization_members.permission_scope** — kolumna SELECT-owana przez orgContextMiddleware nie istniała na świeżej bazie → dbGet(fallback:true) cicho zwracał null → allowed:false dla KAŻDEGO; skala zweryfikowana przez nadzorcę: montaż wyłącznie apiKeys.routes.ts:51, więc skutek = MARTWE zarządzanie kluczami API na świeżym środowisku (nie główna ścieżka logowania — korekta tezy robotnika); parsePermissionScope traktuje NULL jako valid → wystarczyła nullable kolumna (20261123); DOWÓD przed/po na realnym zamontowanym routerze: PRZED ważny członek dostaje „Access denied", PO dochodzi do właściwej bramki RBAC. (3) **consultant_org_links** — schemat dopisany (20261122, zero zmiany zachowania), kod nietknięty. DWIE DECYZJE WŁAŚCICIELA (obie strony wagi mają realne inwestycje, robotnik słusznie odmówił jednoosobowego rozstrzygnięcia): (a) **Consultant Mode** — middleware+serwis+testy istnieją, ale jedyny writer ma zero importerów, DDL leżał w never-ran/, a produkt mówi „multi-org niedostępne": dokończyć kiedyś czy formalnie skasować? (b) **Limity zasobów org** (organization_resource_usage) — middleware kwot NIGDY nie zamontowany, fail-open, a dane zużycia żyją już w organizations.* czytanych przez panel superadmina: (A) skasować martwy middleware (~1329 linii testów) czy (B) przepiąć na istniejące kolumny i REALNIE zamontować (zmiana zachowania produktu)? POZYCJA OTWARTA do rejestru: tabela consultants ma DWÓCH niezgodnych producentów (consultants.routes.ts INSERT z organization_id/specialization/hourly_rate — semi-żywy za enableStubRoutes z realnymi konsumentami frontowymi; consultantService.ts INSERT z display_name/status — martwy) — ta sama klasa buga co security_settings z DEC-116; kolizja nie materializuje się dziś, ale czeka na podłączenie serwisu. |
| DEC-2026-08-26-122 | Dzień 20 (Assessment backend) — FIX-y wykonane, SCALONY | **SUPERVISOR_ACCEPT** | Wszystkie warunki DEC-119 spełnione. FIX-1: tabela A.3 (kontrakt dla frontu) poprawiona — zły czasownik DELETE + dopisane 2 pominięte wywołania fallback (oba 404 na v1). FIX-2: kontrakt raportu niesie skips[] per pytanie; skipped obszaru = agregat true tylko przy komplecie pominięć (rozstrzygnięcie nadzorcy z DEC-119 wykonane; 2 nowe testy real-PG: 2 różne kody → oba w liście + skipped:false). FIX-3: tabela przed/po zmierzona na REALNEJ ŻYWEJ TRASIE (GET /api/assessment-reports/:id/drd-report, wyrenderowany HTML, nie na modelu): poziom 5 osi 5/6 był 100% → jest 83% (5/6), poziom 6 był OBCINANY do 5 (dowód: log AxisDataGuard „clamped actual=6 to [0,5]") → jest czytelny 100% (6/6); status B.1 podniesiony do ZROBIONE_WG_DoD zasadnie. FIX-4: test przeniesiony do dozwolonej lokalizacji, 11/11 PASS potwierdzone trzykrotnie. SCALONE do m03: model 7 osi ujednolicony klient↔serwer, słownik 4 kodów Pomiń (zgodny z frontem co do bajta), deterministyczny kontrakt raportu 7 rozdziałów, pakiet testów realnego routera. KOLEJNY KROK KOORDYNACJI (otwarty): przepięcie frontu z zapisu etykiety-przez-justification na POST kodu — TYLKO ekran HTTP, osobny zapis po recordAnswer z Idempotency-Key (warunki z DEC-119). Kontynuacja dyżurowa: A.2, B.2 (203 błędy TS), C.1, E.2 (wersjonowanie kontraktu), F.1, R.1. |
| DEC-2026-08-26-123 | Tools — tania partia 1-6 SCALONA + dwie korekty tez panelu | **SUPERVISOR_ACCEPT** | Gałąź codex/tools-batch1-20260826, 6 commitów, 366/366 testów (28 czerwonych zastanych potwierdzonych na baseline przez git stash). NAJWAŻNIEJSZE — DWIE KOREKTY TEZ PANELU (DEC-118), obie zweryfikowane empirycznie: (1) **migracja 946 WYKONUJE SIĘ w łańcuchu** — jej nagłówek „NIE uruchamiamy tej migracji na żywej bazie w tej fali" jest NIEAKTUALNY (runner wyklucza tylko ops/never-ran/rollback i sqlite-only; plik leży w server/migrations/ więc idzie normalnie; dowód: 847 migracji na świeżym PG, tabela tool_outputs z pełnym schematem istnieje) — warstwa outputów NIE jest martwa po stronie serwera, naprawa #1 miała sens; (2) **15 nie-bespoke narzędzi to NIE puste powłoki** — mają wspólne kroki uniwersalne + DZIAŁAJĄCY generyczny formularz (OperationalSectionStep z impact/effort zapisywanym do session.inputData); zagrodzenie im startu (jak sugerował panel) ZABRAŁOBY użytkownikom działające funkcje — robotnik SŁUSZNIE ODMÓWIŁ literalnego wykonania i wyeksponował podział SHIPPED(16)/DECLARED bez zmiany bramki (test re-derywuje 16 grep-em przy każdym uruchomieniu — kontrakt przestał kłamać i nie może się zestarzeć); decyzja o bramce = właściciel. WYKONANE: zakładka modułu woła wreszcie Api.listToolOutputs (za flagą ff_toolsInsightsWiring OFF; wiersze tool_output odróżnialne od Report/Assessment), słownik statusów uzupełniony (CONFIGURING/SENT_* — koniec „unknown status" na twarzy), etykieta Outputs→Insights/Insighty (DEC-32 WYKONANE co do etykiety; wewnętrzny id zostaje — deep-linki, świadomie), kebab z Reopen (jedyny realny endpoint poziomu wiersza; „zatwierdź/promuj" to akcje POZIOMU SESJI — nie dodane wg reguły „bez endpointu = nie dodawaj") i „Create report" → Report Builder ze źródłem TOOL (istniejący backend wreszcie osiągalny z modułu); akcje wyszarzone Z POWODEM na wierszu (kanon: nigdy ciche pominięcie). 8 zrzutów light+dark obejrzanych przez robotnika. ODŁOŻONE (do decyzji/osobnych bloków): migracja menu na actions/registry (4 powierzchnie, architektura), bramka SHIPPED/DECLARED, pełna zmiana id zakładki. Flaga OFF do akceptu właściciela na zrzutach. |
| DEC-2026-08-26-124 | Dzień 22 (Superadmin audyt) — odbiór i SCALENIE | **SUPERVISOR_ACCEPT** | MERGE całego diffu (6 plików), ZERO P0. Odbiorca odtworzył niezależnie: 9/9 realPG, deltę eksperymentu 0/0/2/0/1 (i UDOWODNIŁ, że wniosek „nogi projekcji = duplikacja" wynika z danych: neutralizacja pisarza semantycznego → test czerwony z audit_events 2→1), pomiar §0.4a w 100% (nie spot-check: 16 FAIL identycznych na markerze — zero wprowadzonych czerwonych), inwentarz 83 tras sumuje się co do jednego, MODULE_ACCEPTANCE podniesiony Z MIANOWNIKIEM (24/83, 28,9%) bez cichego podnoszenia bramek. Fail-closed z UCZCIWĄ kopertą: awaria audytu → 503 {code:AUDIT_UNAVAILABLE, operationApplied:true} — nie milczące 503. DOWÓD MUTACYJNY wykonał ODBIORCA (dyżur go nie dowiózł — C.3): cichy catch → 1 czerwony; usunięcie emitera → 3 czerwone. Testy mają zęby. FIX-y PRZED domknięciem TRI-MUST-08 (nie przed merge): FIX-1 (~5 linii) POST break-glass/sessions pisze audyt BEZ potwierdzenia mutacji — createSession połyka błąd INSERT-a do fallbacku „minimal schema" i potrafi zwrócić SYNTETYCZNY obiekt dla wiersza, którego nie ma (wzorzec Z22!), a fallback resourceId=actor wstawiłby id użytkownika zamiast sesji → readback po create + usunięcie fallbacku; FIX-2 (~4 linie) awaria audytu zostawia AKTYWNĄ sesję break-glass (awaryjny dostęp przyznany i nieodnotowany — dokładnie ryzyko TRI-MUST-08) → kompensacyjne revoke + operationApplied:false; FIX-3 (testy) C.1 dowiózł 3/20 scenariuszy → dołożyć 401/403-rola/?orgId/org-w-body ×4 trasy + fiveTableCounts w negatywach C.2; FIX-4 (dokument) STOP E.2 bez zmierzonej tabeli powierzchnia×widoczny i bez kontraktu frontowego + dopisać do kontraktu DUPLIKACJĘ wiersza w projekcji tenanta (semantyczny+generyczny, różne id — front to pokaże właścicielowi). NOWA POZYCJA REJESTRU (zastane, rodzina silent-green DEC-120): **activity_logs = MARTWY ZAPIS BEZ SYGNAŁU** — ActivityService.log ma catch tylko na rzut, a warstwa PG zwraca {success:false} zamiast rzucać, więc generyczne pokrycie 59 nieaudytowanych tras NIE ZAPISUJE NIC, po cichu; to sedno resztkowego ryzyka TRI-MUST-08. Ściąga dla przyszłych instrukcji: obraz postgres:15 nie przechodzi migracji (brak extension vector) — wymagany pgvector/pgvector. TRI-MUST-08: PARTIALLY PROVEN (24/83) — deklaracja dyżuru prawdziwa. |
| DEC-2026-08-26-125 | Audyty R1-R3 — blokery panelu rundy 2 ZAMKNIĘTE, SCALONE | **SUPERVISOR_ACCEPT** | Gałąź codex/audits-r1r3-20260826 (4 commity, 173/173 testy, tsc 0 błędów w dotkniętych). R1 (najcięższy — użytkownik zatwierdzał dokument, którego nie przeczytał): AuditReportDocumentView przepisany — DOMYŚLNIE renderuje audit_reports.payload (pełny 13-sekcyjny raport objęty contentHash, z obsługą sekcji group/keyValue, w tym macierz traceability niedostępna w starym widoku), a /presentation to jawny DRUGI tryb „Widok dla zarządu" ładowany leniwie z kebaba; etykieta „Rodzaj" mówi prawdę per tryb. R2: ustalenia z limit/offset/total i paginatorem (koniec cichego ucięcia do 50), kolumna Termin przez listAllActions (pętla stronicująca — decyzja opisana), filtr przeglądarki kryteriów działa na PEŁNYM zbiorze przed paginacją (test regresyjny na wierszu ze strony 2). R3: martwe getFindingStatistics/getSystemicFindings usunięte z klienta (backend zostaje dla przyszłych Wyników), kłamiący nagłówek poprawiony, window.prompt → NoteEntryModal z walidacją (wejście do niezmiennego śladu audytu), CAŁY pakiet dowodów odświeżony na tipie (16 zrzutów, w tym nowe: pełny raport z sekcjami group/keyValue/traceability, przełącznik trybów, modal notatki; criterion-build bez ISO wg DEC-99). Robotnik przy oględzinach własnego zrzutu złapał ostatni surowy enum (f.status w findings_by_severity) i naprawił przed oddaniem. Odnotowany zastany dług: NModeHeaderConfig.secondaryActions to martwy prop (4 przedistniejące czerwone testy) — przełącznik trybu poszedł do kebaba. Obie flagi nadal OFF — partia akceptacyjna dla właściciela w przygotowaniu. |
| DEC-2026-08-26-126 | Dzień 21 (Inicjatywy solver) — odbiór warstwy 1 | **SUPERVISOR_VERDICT** | RDZEŃ LEPSZY NIŻ DEKLARACJA, zero P0. Odbiorca zmierzył stan „przed" NA KODZIE BAZOWYM przez HTTP (dokładnie 1/1/1/47 — potwierdzona atrapa planowania z DEC-104) i „po" (13/13/12/12); determinizm SILNIEJSZY niż testowany (odwrócona kolejność wejścia → identyczna mapa); capacity realnie działa (podaż 5/okres → 20 przypisań + 30 KONFLIKTÓW zamiast zmyślonych dat); periodFor FIZYCZNIE zniknął (grep pusty); pułapka z erraty ominięta czystym authorizeProjectsMap (Map per projectId przez istniejące deps.authorize — effectiveAccessService nietknięty, Z16); licznik SQL odtworzony na DOMYŚLNYM okablowaniu: 5 wierszy 21→5, 50 wierszy 201→5; migracja = jedna linia CREATE INDEX, idempotentna; C-G uczciwie puste bez atrap; MODULE_ACCEPTANCE bez cichego podnoszenia bramek. P1-1 [BLOKUJE PROMOCJĘ]: **cicha regresja frontu** — trasa zwraca teraz domyślnie 50 z nextCursor, a runtimeApi.listRegisteredInitiatives NIE CZYTA kursora → Hub pokazałby maks. 50 inicjatyw bez błędu i bez śladu (Codex słusznie nie ruszał src/ — naprawa wewnętrzna zlecona). P1-2: nowy test 50×4 flake w pełnej suicie (zastane zanieczyszczenie: deadlocki+TRUNCATE sąsiadów) — izolacja zlecona. KOREKTY RAPORTU: „37 czerwonych zastanych" nieodtwarzalne w izolacji (2/3 spot-checków ZIELONE na markerze; cytowany config nie istnieje) — liczba z zanieczyszczonego przebiegu; 13/13/12/12 dowodzi zachowania targetów, nie capacity (to dowodzą osobne testy 8/8). P2: kursor to base64url JSON (nie nieprzezroczysty — klient może się przywiązać), pusta strona z nextCursor legalna (front musi iterować), test licznika SQL na ścieżce fail-closed nie asertuje wierszy (zastany defekt: resolveEffectiveAccess pada na kolumnie tworzonej przez pomijany DatabaseInitializer). MERGE zakresu A+B po FIX-1/FIX-2. |
| DEC-2026-08-26-127 | FIX-y dnia 22 — SCALONE | **SUPERVISOR_ACCEPT** | Warunki DEC-124 dla TRI-MUST-08 wykonane. FIX-1: break-glass create ma niezależny SELECT-readback (id/is_active/session_type) PRZED wpisem audytowym — brak wiersza lub zły typ → 409 bez audytu; fallback resourceId=actor USUNIĘTY (koniec audytowania nieistniejących sesji i wpisywania id użytkownika jako id sesji — wzorzec Z22 domknięty). FIX-2: awaria audytu → KOMPENSACYJNE revokeSession + 503 operationApplied:false — awaryjny dostęp nie może już zostać przyznany i nieodnotowany; dla service_account.created kompensacja świadomie pominięta (koperta operationApplied:true — dozwolona, revoke SA nie jest równie tanie). BONUS wymagany testami: obie trasy dostały strażnika ?orgId≠token → 403 ADMIN_BOUNDARY_VIOLATION (wcześniej w ogóle nie czytały query). FIX-3: realPG 9→27 (scenariusze a-d ×4 trasy: 401/403-rola/?orgId/org-w-body, C.2 na fiveTableCounts — mutacja odrzucona = zero wierszy w PIĘCIU tabelach, testy FIX-1/FIX-2). FIX-4: STOP E.2 dostał ZMIERZONY dowód — jedna mutacja: /api/admin/audit-logs WIDZI wiersz, /api/superadmin/admin/audit-logs NIE WIDZI (integrity.degraded:false = prawdziwy brak, nie awaria; przyczyna SuperAdminController:4638 czyta tylko admin_audit_logs) + pełny kontrakt frontowy z uwagą o duplikacji wiersza. Flakowość międzyplikowa przy łączeniu plików w jednym vitest = zastana (potwierdzona na czystym HEAD). TRI-MUST-08: pokrycie 24/83 z fail-closed NAPRAWDĘ fail-closed na 4 trasach najwyższego ryzyka; pełne domknięcie = kolejne dyżury wg rankingu A.3. |
| DEC-2026-08-26-128 | Realizacja partia A — SCALONA | **SUPERVISOR_ACCEPT** | Odpowiedź na panel 3,6/10 (DEC-120), gałąź codex/execution-batch-a-20260826 (11 commitów, 62 pliki, +1401/−5549, 78 testów PASS). **A6 BEZPIECZEŃSTWO — wyciek międzytenantowy POTWIERDZONY I ZAMKNIĘTY na żywym PG**: przed naprawą token org A + initiativeId z org B → gate-check zwracał liczby W CAŁOŚCI z danych org B (canAdvance:false, 2 błędy cudzej organizacji); po dopisaniu filtra organization_id z tokenu → canAdvance:true, errors:[] dla obcego id, właściciel nadal widzi swoje blokery; test regresyjny realdb w repo. **ŁAŃCUCH CICHEJ ZIELENI ROZCIĘTY NA KAŻDYM OGNIWIE** (tabela serwis→brama→klient→ekran→raport w raporcie robotnika): 17 cichych catchów w serwisach dostało logger.error z kontekstem (przy okazji wykryty ukryty bug: JSON.parse na uszkodzonym tasks_json też był cichy); workload z klasyfikacją DEC-112 (cichnie tylko brak tabeli); brama zwraca znacznik __sourceUnavailable zamiast nieodróżnialnej pustki; ekran dostał crimson Callout „Źródło sygnałów niedostępne" (fantomowe flagi controlTowerFailed/isLoadingControlSignals wreszcie CZYTANE); **stopka raportu: dowolna awaria źródła WYMUSZA Confidence≠high i Freshness=Degraded z jawnym wymienieniem źródła** — koniec raportów na komitet z „Confidence: high" przy padniętej usłudze ryzyk. A4: przycisk Atelier Toys (POST ×5 danych testowych do realnego API) USUNIĘTY. A11: 5 grup przycisków kończących się zawsze 409 wyłączonych z widocznym powodem PL/EN (DELETE budżetu — dozwolony wyjątek — aktywny). A2b: fałszywy lookup {id,name:id} → realny lookup po typie+id. A10: tryb demo nie kasuje już błędu — trwały baner + znacznik „Sample" na każdym wierszu demo. A7: **4232 linie martwego kodu usunięte** (13 plików + 3 flagi-fantomy; graf importów + trzy niezależne dowody z istniejących testów „retired/rejected/shadowed"). A9: btn-primary/secondary wreszcie ZDEFINIOWANE w importowanym CSS na tokenach c-* (neutralne, nie crimson), zweryfikowane getComputedStyle w realnej przeglądarce light+dark. A8: 15 kluczy PL dodanych; 38 pominiętych zasadnie (konsumenci usunięci w A7). POMINIĘTE JAWNIE: ~60 miejsc includes(does not exist) POZA Execution — należą do obszarów aktywnych dyżurów (scope-creep); e2e martwych paneli → osobne zadanie (task_fee9de6a, już w toku w sesji właściciela). |
| DEC-2026-08-26-129 | Dzień 21 — FIX-y wykonane, SCALONY | **SUPERVISOR_ACCEPT** | Warunki DEC-126 spełnione. FIX-1 (cicha regresja frontu): listRegisteredInitiatives konsumuje pętlą nextCursor (limit bezpieczeństwa 20 stron/4000 wierszy z console.warn; poprawna obsługa legalnej PUSTEJ strony z niepustym kursorem — autoryzacja filtruje po paginacji), test z mockiem 3 stron w tym pustą w środku; Hub znowu widzi komplet inicjatyw. FIX-2 (flake): describe.sequential + własny withDbFlakeRetry (re-seed + jittered backoff — natywny retry za wolny na burst TRUNCATE); DOWÓD METODYCZNY: diff listy czerwonych plików przed/po IDENTYCZNY poza naprawionym plikiem (zero regresji), a przebieg --no-file-parallelism pokazał planSolver zielony nawet BEZ fixa — potwierdzenie, że to zanieczyszczenie międzyplikowe, nie bug testu. KOREKTY RAPORTU wykonane (liczba z zanieczyszczonego przebiegu opisana uczciwie; 13/13/12/12 = zachowanie targetów; kursor = base64url JSON traktować jako nieprzezroczysty; pusta strona legalna). ZNALEZISKO ZASTANE do rejestru (świadomie NIE naprawiane w tym dyżurze): stabilny baseline 6 czerwonych plików suity integration/initiatives-execution ma JEDNĄ realną przyczynę — migracja dodała flow_accepted_classic_runtime_adoptions (FK → initiative_candidates), a starsze TRUNCATE initiative_candidates w kilku plikach jej nie uwzględnia; wahanie 12-15 czerwonych run-to-run to order:random + współdzielone tabele. SCALONE: realny solver planu (topologia+okna+pojemność+UNKNOWN+konflikty zamiast zmyślonych dat), lista z paginacją keyset i autoryzacją per projekt (201→5 zapytań), migracja indeksu, front z pełną konsumpcją kursora. Moduł Inicjatywy: dwie największe atrapy z panelu DEC-104 (solver po indeksie, burza autoryzacyjna) ZAMKNIĘTE. |
| DEC-2026-08-26-130 | Dzień 23 (Finance dowodowy) — STOP B.1 + ROZSTRZYGNIĘCIE | **SUPERVISOR_ACCEPT + SUPERVISOR_DECISION** | Dyżur wzorowo uczciwy: STOP na B.1 poziom CODE_PRESENT bez TECHNICAL_PASS — produkcyjny montaż blokuje WSZYSTKIE 5/5 kanonicznych kart Finance v2 przez v8FeatureGate (404 V8_DISABLED przy default-OFF); test dowodowy 6/6 PASS; Codex NIE ratował wyniku włączeniem flagi (dokładnie wg instrukcji); zero zmian flag/Gateway/middleware/src/migracji; inwentarz A dostarczony. SCALONE (raport + test dowodowy). ROZSTRZYGNIĘCIE NADZORCY otwartej decyzji „czy i jak karty mają być osiągalne": karty SĄ osiągalne przy KANONICZNEJ konfiguracji środowiska docelowego — DEC-102 (bramka stagingu) już ustanowił, że env stagingu MUSI mieć ENABLE_V8_GLOBAL=true (bez tego połowa produktu jest niewidoczna, nie tylko Finance). STOP formalnie zasadny (dyżur mierzył default-OFF zgodnie z literą), ale odpowiedź to KONFIGURACJA ENV zapisana już w rejestrze, nie zmiana kodu ani flag. Pozycje C-H dyżuru (równoważność strażników dwóch torów, CAS, idempotencja, audyt transakcyjny) pozostają NIE_ZACZĘTE — kandydat na kontynuację dyżurową po ustabilizowaniu env na stagingu, z §B.1 mierzonym przy ENABLE_V8_GLOBAL=true. |
| DEC-2026-08-26-131 | Dzień 17 (Results blok 2) — odbiór i SCALENIE | **SUPERVISOR_ACCEPT** | MERGE pełnego zakresu (25 plików — brief odbioru mylnie mówił 79; kod bezkolizyjny, addytywny, zero UI/flag). Odbiorca NIEZALEŻNIE potwierdził: zero nowych FAIL (własny pomiar na czystym markerze — 358/358 OKR i 421 baseline IDENTYCZNE przed/po; naprawił też wadę metody Codexa: „przed" było re-bazowane po commitach — wynik i tak czysty, wadliwa była metoda, nie delta), migracje 20261076/77 bez kolizji w skanie 3710 gałęzi, X.2 = PEŁNE DoD z realnym łańcuchem JWT→router→PG (eksport XLSX przez exceljs, org-scoping na WSZYSTKICH 3 formatach, obcy tenant → 404 nigdy 503 — kolejność tenant-przed-dependency skomentowana, bez wycieku istnienia; próba wstrzyknięcia ?organizationId odbita), X.1 uczciwa odmowa read-only z nazwanymi lukami, X.4 zero hardkodów (progi TYLKO z pustej tabeli polityk → DECISION_REQUIRED), K.2/K.3 kursor keyset bez gubienia zdarzeń + kody maszynowe + 404 dla cudzego KPI. Statusy CZĘŚCIOWO uczciwe, miejscami zaniżone. **P1-4 — NOWA POZYCJA BEZPIECZEŃSTWA (zastana, poza zakresem X.2)**: managementReports.routes.ts ma trasy CAŁKOWICIE nieoscopowane po organizacji, w tym MUTUJĄCE — POST /:id/finalize i POST /:id/unlock (unlockReport bez żadnego sprawdzenia org!), GET /:id/comments, GET /:id/audit-log — każdy uwierzytelniony użytkownik może sfinalizować/odblokować raport CUDZEJ organizacji znając ID; wzorzec naprawy gotowy (getReportByIdForOrganization z tego dyżuru) — FIX wewnętrzny zlecony. **P1-1 — STOP O.2 ZAKWESTIONOWANY przez odbiorcę**: semantyka CURRENT/DUE ISTNIEJE w repo (okrCheckInCommands.ts:205-245 — stale/next-due/current z window_end), ścieżka KR→kadencja też; uczciwą odpowiedzią było CZĘŚCIOWO + DECISION_REQUIRED na samej granicy, nie odrzucenie całej pozycji — O.2 wraca do kontynuacji dyżurowej z tym wskazaniem. P1-2: raport bez siedmiu tabel werdyktów z §9 (dla X.4 BRAK_ŹRÓDŁA pozostaje twierdzeniem bez dowodu) — do dopisania w kontynuacji. P2: zaszyty numer parametru $5 w okrAttentionRepository:97; reformat 55 linii MODULE_ACCEPTANCE zaszumia governance. |
| DEC-2026-08-26-132 | SZABLONY I GENERACJA DOKUMENTÓW — rekonesans (strach właściciela nr 1) | **SUPERVISOR_REPORT + DECYZJE** | Teza paneli „zero generacji Word/PPT" częściowo FAŁSZYWA: silniki ISTNIEJĄ i są dobre (docx 9.5 z 14 nazwanymi stylami Worda/nagłówkiem/stopką/PAGE-NUMPAGES — plik QA oceniony 7/10 po rozpakowaniu OOXML; pptxgenjs ×3 pipeline z 16 composites i NATYWNYM wykresem OOXML; ExcelJS z 10 szablonami finansowymi; system Deliverables/M17 z bramkami jakości i krytykiem decka). PRAWDZIWE PROBLEMY: (1) **KRYTYCZNE, dowód empiryczny — WSZYSTKIE serwerowe PDF-y łamią polskie znaki** („Zażółć gęślą jaźń"→„ZaÏ1Brq¶Á ja¡D"; Helvetica/WinAnsi bez ąęłńśźżć; zero registerFont, ZERO plików .ttf w repo; dotyczy 7 tras pdfkit) — polska firma doradcza nie odda klientowi ŻADNEGO z tych plików; (2) PIĘĆ niezależnych systemów stylowania eksportu (BRAND_EXPORT_CANON = zatwierdzony SSOT z 02.08, zadania VF3-2/3/4 NIE RUSZYŁY — klient dostaje różną navy zależnie od trasy); (3) najlepszy silnik (Deliverables/M17) za 2 flagami default OFF → router 404; (4) NIE ISTNIEJE ani jeden wizualnie zaprojektowany plik szablonu (.docx/.dotx/.potx) — wszystko programowe; docxtemplater niezainstalowany; (5) atrapy: assessment /export/pdf = 36 linii bez okładki/stopki/paginacji, /export/pptx = DOKŁADNIE 2 SLAJDY i zero konsumentów; PDF w Tools = raster html2canvas; drdReportHtml (najładniejszy) kończy w oknie drukowania przeglądarki — właściciel nie może wysłać PLIKU. Stan decyzji DEC-41..44: 41 dowiezione, 42/43 niezrobione (60 deklaracji vs 40 z decyzji; 6 starterów vs 24), 44 STOP. **REKOMENDACJA PRZYJĘTA: pierwszy dokument-wzorzec = RAPORT DRD → .docx** (dane gotowe i deterministyczne z day20; Assessment aktywny w MVP — Audits poza MVP wg FUNCTIONAL_DOCUMENTATION, wchodzi jako DRUGI na ten sam silnik ~2 dni; deck TRZECI). SILNIK: istniejący documentDocxRenderer (odrzucone: docxtemplater — szósty system stylowania + pętle zagnieżdżone + licencja PRO; HTML→PDF — puppeteer tylko w devDeps, klient chce Worda). FORMUŁA 3 KROKÓW (mocniejsza niż reguła zrzutów — przedmiotem akceptu jest PLIK): K1 złoty plik RĘCZNY z realnymi danymi PL do otwarcia we własnym Wordzie Piotra → K2 kontrakt parytetu (styl po stylu, wsad do VF3-2) → K3 budowa 1:1 z harnessem parytetu before/after, za flagą OFF. Liczba rozdziałów: 7 wg DEC-46 (rozstrzygnięcie z day20 obowiązuje; mapowanie 8D nietknięte jako pozycja otwarta). ZLECONE NATYCHMIAST: K1 (złoty plik) + osadzenie fontu OFL z polskimi znakami we wszystkich trasach pdfkit (DEC-133). DO DECYZJI WŁAŚCICIELA: ENABLE_DELIVERABLES_LIGHT (najlepszy silnik niedostępny), G00 Materials (potwierdzenie ograniczonego zakresu praw), los atrap eksportu assessment. Szacunek do akceptu wzorca: ~6-8 dni robotnika. |
| DEC-2026-08-26-134 | Dzień 24 (Meetings blok 3) — odbiór i SCALENIE | **SUPERVISOR_ACCEPT** | MERGE zakresu C/D/E/F, zero P0, zero regresji (odbiorca odtworzył pomiar 239/240 co do testu na własnym PG z MOCK_DB=false; jedyne czerwone zastane strukturalnie — src/ ma pusty diff). POTWIERDZONE: bramki occurrence dokładnie wg §E.3 z mutacją adwersaryjną (usunięcie bramki → 2/4 czerwone), PATCH /:id/status NIETKNIĘTY (DEC-58 uszanowane), zero wysyłki przy odmowie gwarantowane strukturalnie (return przed sendem), funnel notatka→zadanie na istniejącym idx_tasks_idempotency_org bez migracji z testem współbieżności, resolver materialTitle poprawny w kodzie (odebrany dostęp → null, nie snapshot), strażnicy wysyłki dnia 16 zielone, uczciwość WYSOKA (zero skip/todo w „pustych" pozycjach, odstępstwo commit-hygiene zgłoszone dwukrotnie). **P1-1 — ODBIORCA SAM WYKONAŁ POMIAR, KTÓREGO ZABRAKŁO (§C.3)**: recurrenceId BEZ sufiksu strefy przechodzi walidację i daje wynik zależny od strefy procesu serwera — rozjazd 1 h (jesień) / 2 h (wiosna), seria ucięta w złym momencie PO CICHU; raport deklarował „decyzja C.4(b) test+errata", ale nie ma ani testu, ani erraty — jedyne miejsce, gdzie raport wyprzedził dowód. P1-2: resolver D wchodzi BEZ ANI JEDNEGO testu (ścieżka bezpieczeństwa — wyciek tytułu; grep materialTitle po tests/ = zero) — D pozostaje CZĘŚCIOWO. P2: test DST wiąże tylko formułę UNTIL=recurrenceId−1s, NIE wiąże okazji z granicą czasu (mutacja: niezgodny z DST recurrenceId przechodzi); retry bez userId zeruje material_title (dziś nieszkodliwe — pola nikt nie czyta, co znaczy też, że kontrakt frontowy opisuje pole bez konsumenta); harness funnela maskuje mapowanie AuthorizationError własnym handlerem; flaga replayed niedokładna przy wyścigu. **FIX-y C.3/C.2/D/E-F SPAKOWANE do kontynuacji dyżurowej Meetings blok 4** (ekonomia: nie mnożymy wewnętrznych partii; walidacja jawnej strefy recurrenceId → 400 INVALID_OCCURRENCE jako preferowane rozstrzygnięcie C.3). |
| DEC-2026-08-27-135 | Management-reports P1-4 — SCALONE + odkrycie skali | **SUPERVISOR_ACCEPT** | Cztery trasy z DEC-131 doscopowane (GET /:id/comments, GET /:id/audit-log, POST /:id/finalize, POST /:id/unlock) przez nową assertReportInOrganization opartą o getReportByIdForOrganization (wzorzec z dnia 17); brak raportu i obcy raport = identyczne 404, org wyłącznie z JWT. **DOWÓD PRZED/PO metodą git stash**: bez naprawy 7 z 11 testów PADA — cross-org GET comments/audit-log 200 zamiast 404, cross-org finalize/unlock 200 (zero ochrony zapisu), wstrzyknięcie ?organizationId= i body.organizationId IGNOROWANE (atak przechodził), a comments zwracał 200 nawet dla NIEISTNIEJĄCEGO raportu. Po naprawie 11/11. Bonus P2: zaszyty $5 → $${values.length} w okrAttentionRepository (7/7 bez regresji). |
| DEC-2026-08-27-136 | Management-reports — DZIURA OBEJMUJE NIEMAL CAŁY ROUTER | **SUPERVISOR_ESCALATION** | Robotnik przy naprawie P1-4 zinwentaryzował CAŁY router i wykrył, że brak izolacji organizacyjnej to NIE cztery trasy, tylko KILKANAŚCIE. **NAJGROŹNIEJSZA: POST /:id/share — tworzy PUBLICZNY LINK do CUDZEGO raportu** (skutek wychodzi POZA system, nie tylko między tenantami — link można wysłać komukolwiek). Dalej: GET /pending-approvals zwraca zatwierdzenia WSZYSTKICH organizacji (zero filtra w SQL); GET /:id oddaje pełną treść obcego raportu; POST /:id/approve i /submit = ZAPIS na obcym raporcie; PATCH /:id nadpisuje tytuł cudzego; GET /:id/versions + /:versionNumber + /compare = historia cudzego; POST/PATCH/DELETE /:id/comments/:commentId — DELETE komentarza działa po samym ID BEZ związku z org; fallbacki || body.organizationId i || query.organizationId w /generate, /history, /analytics/*. NIE ZOSTAWIONE jako follow-up chip — zlecone NATYCHMIAST jako pełne domknięcie routera (gałąź codex/mgmtreports-full-orgscope-20260827) z wymogiem: KAŻDA trasa routera sklasyfikowana w raporcie, dowód przed/po metodą git stash, dla share kluczowy dowód „link utworzony przez obcego NIE POWSTAJE", zgłoszenie (bez wykonywania) czy istniejące share-linki w bazie wymagają rewizji. **WNIOSEK METODYCZNY: to CZWARTY obszar wycieku międzytenantowego wykryty w tej sesji (gate-check Realizacji, day25 AI trasy, management-reports ×2 fale). Wzorzec: routery pisane wcześnie, przed wprowadzeniem dyscypliny org-z-tokenu — do systematycznego audytu wszystkich routerów przed fazą 3.** |
| DEC-2026-08-27-137 | Dzień 25 (Assessment blok 2) — odbiór i SCALENIE | **SUPERVISOR_ACCEPT** | MERGE dowiezionego zakresu, ZERO P0. **PUŁAPKA A.2 NAWIGOWANA POPRAWNIE** — usunięte DOKŁADNIE 4 martwe handlery ról (my-role, GET/POST /roles, DELETE /roles/:targetUserId), access-requests ZOSTAWIONE (są w PUBLIC_DEMO_WRITE_ALLOWLIST — usunięcie zepsułoby publiczne demo), import AssessmentPermissionService przeniesiony bez zombie i duplikatu; pliki chronione (demoPrincipalGuard, publicDemoWriteAllowlist.test, h64-batch6, routes/index, assessment-ai.routes, drdStructure, src/, migracje) — ZERO zmian. **★ KOREKTA ADWERSARYJNA: rzekomy „wyciek międzytenantowy" z B.2 NIE JEST WYCIEKIEM** — getAssessmentData pyta WHERE id=? AND organization_id=?, a organizationId pochodzi z TOKENU (req.user), nie z żądania; obcy projekt → brak dopasowania → 200 z PUSTKĄ, cudze dane NIGDY nie wracają; getOutput też org-scoped. To gap SEMANTYCZNY (200 zamiast 404), zero wpływu na poufność — czwarty wyciek tej sesji NIE POWSTAŁ. Lekcja DEC-106 zadziałała drugi raz: znalezisko klasy „bezpieczeństwo" zweryfikowane w kodzie zanim trafiło do rejestru jako fakt. STOP B.2 zasadny (naprawa 200→404 w zakazanym pliku), severity zawyżone przez Codexa — skorygowane. **P1 REALNY (poprawność danych, NIE bezpieczeństwo): C.1 out-of-scale target** — event DECISION_APPROVED subject=target_level z level=8 przyjmowany 201 BEZ walidacji skali; skala DRD to 0-5, więc target 8 daje bezsensowną lukę (gap=8−2) i psuje wynik oceny oraz roadmapę u klienta; defekt istnieje w jądrze niezależnie od dyżuru, teraz scharakteryzowany testem — FIX ZLECONY. STOP F.1 zasadny i obejście NIEMOŻLIWE (h64-failsoft-batch7:54 importuje martwy plik BEZPOŚREDNIO, a produkcyjnie zamontowany jest inny — usunięcie wywala cudzy test poza licencją). POTWIERDZONE: supersededBy WYPROWADZANE podzapytaniem (fizyczna kolumna nigdy niezapisywana — append-only czyste), idempotencja 201/200/409 z ON CONFLICT DO NOTHING, migawka as-of przez listActiveAsOf z filtrem recorded_at (bez zapisu), C.1 przez produkcyjny router z realnym JWT (Z21). Odbiorca uruchomił 31/31 nowych testów, 0 SKIPPED, MOCK_DB=false. DO BACKLOGU (nie pilne): hardening semantyki tras AI 200→404 + zdjęcie @ts-nocheck; koordynowana czystka A.2(b)+F.1 z cudzymi testami; martwy barrel routes/index.ts. |
| DEC-2026-08-27-138 | KOREKTA DEC-137 — drugi niezależny odbiór obalił tezy pierwszego (i mój wpis) | **SUPERVISOR_CORRECTION** | Dwa niezależne odbiory dnia 25 dały różne wyniki w szczegółach; DRUGI jest lepiej udowodniony (grep + seed realnych danych zamiast lektury). **NADZORCA ZWERYFIKOWAŁ OSOBIŚCIE — drugi ma rację:** (1) **PUBLIC_DEMO_WRITE_ALLOWLIST NIE ZAWIERA access-requests** — ma DOKŁADNIE 4 wpisy (auth/logout, auth/refresh, demo/toggle, demo/record-event); `grep -c assessment demoPrincipalGuard.ts` = **0**, także na markerze. Wpisy z publicDemoWriteAllowlist.test.ts:382-383 należą do tablicy ROUTE_SURFACE = inwentarza tras, które test asertuje jako **ZABLOKOWANE** (test dosłownie: „intersects the allowlist in exactly the four documented rows"). **ERRATA §1.2 poz. 2 instrukcji dnia 25 BYŁA FAŁSZYWA, Codex ją „potwierdził" bez sprawdzenia, a nadzorca powtórzył zmyślenie w DEC-137 — niniejszym SKORYGOWANE.** COORDINATION_REQUIRED dla części (b) POZOSTAJE słuszne, ale z JEDNEGO powodu (h64-failsoft-batch6:220 bootuje realny router v1), nie z trzech; framing „produkcyjna allowlista bezpieczeństwa" był zawyżeniem. (2) Liczby handlerów: **32→28** (zweryfikowane osobiście grep-em), nie 31→27 jak w raporcie i DoD — znamię przepisania z instrukcji zamiast pomiaru. (3) batch6 = **18/18 PASS**, nie deklarowane zastane 16/18 — raport zawyżył zastaną czerwień. (4) **STOP B.2 NIEZASADNY**: instrukcja mówiła „bez zielonego harnessu NIE WOLNO ruszyć pliku serwisu" — harness JEST zielony (4/4), więc bramka się otworzyła, a aiAssessmentPartnerService.ts JEST w ramce WOLNO; obserwacja o statusie 200 dotyczy INNEGO pliku i niczego nie blokowała. Uczciwy STOP brzmiałby „budżet wyczerpany: 203 błędy typów (TS2339 ×81 brak deklaracji genAI/model/_injected, TS7006 ×68 implicit any, TS7053 ×29, TS18046 ×20), plik 1439 linii" — B.2 WRACA do wykonania. (5) Znalezisko AI PRZEKLASYFIKOWANE z BEZPIECZEŃSTWO na KONTRAKT/UX — drugi odbiorca zaseedował REALNY obcy assessment (SEKRET-ORG-B) i udowodnił: zero wycieku pól (name/industry/justification = false), obcy i nieistniejący projekt NIEODRÓŻNIALNE (brak nawet oracle istnienia); harness Codexa strzelał w string „foreign-project", który nie istniał w bazie — NIE dowodził tego, co deklarował. **LEKCJA METODYCZNA (do kodeksu): przy znaleziskach nośnych — zwłaszcza gdy raport POWOŁUJE SIĘ na plik/allowlistę — nadzorca weryfikuje grep-em PRZED wpisem do rejestru; erraty autorów instrukcji też podlegają weryfikacji, bo raz zmyślona teza propaguje się przez instrukcję → raport → rejestr.** |
| DEC-2026-08-27-139 | Polskie znaki w PDF + walidacja skali celu — SCALONE | **SUPERVISOR_ACCEPT** | (1) **POLSKIE ZNAKI NAPRAWIONE WE WSZYSTKICH 9 TRASACH PDF** (robotnik znalazł 2 więcej niż wskazano: report-builder-public — publiczny link do raportu! — oraz UnifiedExportService, co naprawia też partnerToolkitResources bez dotykania go). Font: Lato OFL 1.1 (Łukasz Dziedzic, tyPoland) z google/fonts, JUŻ obecny w kuratorowanej bibliotece DELIVERABLE_FORMATTING_SPEC §1 jako para nr 3. Moduł pdfFonts.ts rejestruje 4 wagi i ustawia domyślny font — naprawia też 4 trasy, które NIGDY nie wołały .font(); rzuca czytelny błąd zamiast cichego fallbacku do Helveticy. Kopiowanie assetów dodane w server/package.json ORAZ Dockerfile.api z twardym exit 1 przy braku 4 plików. **DOWÓD BIT-W-BIT**: przed „Za Ï1B r q ¶Á ja ¡D" → po „Zażółć gęślą jaźń"; embedding potwierdzony (/FontFile2, CIDFontType2, Identity-H = pełny Unicode, nie WinAnsi); 22/22 testy w 5 plikach, w tym 2 dymne na realnych trasach z ekstrakcją tekstu. Uwaga: faktury (InvoiceService) miały twardy polski adres — realnie łamany bug u klienta. Rozmiar fontów 2,6 MB (więcej niż szacowane 300-500 KB — google/fonts dystrybuuje pełniejszy zestaw glifów; zaakceptowane). (2) **WALIDACJA SKALI target_level** (P1 z DEC-137): check w method-core.routes.ts bramkowany DRD_METHOD_PACK_ID (precedens DRD-only w tym samym pliku) — jądro zdarzeń współdzielone z Audits/SIRI NIETKNIĘTE; skala per-oś przez istniejący getAxisForArea (7,5,5,7,6,6,5); poza [0,levelCount] / nie-integer / ujemny → 400 TARGET_LEVEL_OUT_OF_SCALE z allowedRange, zero zapisu. DOWÓD PRZED/PO: bez fixa 5/11 pada, w tym CIEKAWE — wartość nie-integer dawała **500** (crash na kolumnie integer), nie cichą akceptację. Po: 11/11; Audits/SIRI bez regresji 28/28. **ZASTANE DANE — DO SPRAWDZENIA PRZEZ NADZORCĘ Z DOSTĘPEM**: method_findings.target_level to numeric BEZ CHECK — robotnik dostarczył gotowe kwerendy diagnostyczne (w raporcie) do policzenia historycznych wartości poza skalą na demo/staging; decyzja korekta vs adnotacja należy do właściciela. |
| DEC-2026-08-27-140 | Management-reports — PEŁNE doscopowanie routera SCALONE | **SUPERVISOR_ACCEPT** | 34 trasy sklasyfikowane BEZ WYJĄTKU, testy 11→42, dowód przed/po: **16 czerwonych bez naprawy → 42/42 z naprawą**. **P0 ZAMKNIĘTY: POST /:id/share** — assert PRZED mintem tokenu i przed zapisem; obcy dostaje 404, share_token/share_expires_at zostają NULL, zero audytu. Naprawione też: GET /pending-approvals (zwracał zatwierdzenia WSZYSTKICH organizacji), GET /:id (pełna treść obcego), submit/approve (ZAPIS na obcym), versions ×3, PATCH /:id (nadpisanie tytułu), comments POST/PATCH/DELETE (działały po samym ID komentarza — teraz komentarz→raport→org), bulk-export (audyt na reportIds[0] bez kontroli), fallbacki || body/query.organizationId w /generate, /history, /analytics/* oraz w przesłoniętym routerze analytics. PRZY OKAZJI naprawione DEFEKTY FUNKCJONALNE: GET /:id/versions/compare był MARTWY (przesłaniany przez /:versionNumber → NaN → 404) — przeniesiony wyżej, test dowodzi że przed naprawą padał też właścicielowi; generyczne 400 maskowało 404 tenantowe. **UCZCIWOŚĆ ROBOTNIKA**: sam oznaczył 3 testy fallbacków jako NIE-dowód red→green (bypass E2E zawsze podstawia organizację, więc tożsamości bez org nie da się wytworzyć w harnessie; fallback odpalał się produkcyjnie przy pustym req.organizationId — auth.middleware.ts:881) i zapisał to w komentarzu testu i w commicie, żeby nikt nie odczytał tego jako dowodu eksploitu. **SHARE-LINKI ISTNIEJĄCE — NIE migrowane, zgłoszone**: token to goły uuidv4 bez org; getByShareToken NIE MA ANI JEDNEGO WYWOŁANIA i nie istnieje trasa realizująca token → dotąd był to UTAJONY P0 (zapis wchodził, link był nie do zrealizowania), ale (a) createShareLink NADPISUJE token, więc obcy mógł unieważnić/rotować legalny link cudzej organizacji, (b) w dniu powstania endpointu każdy podrzucony wcześniej token stałby się żywym wyciekiem. Kwerenda namierzająca w raporcie (z zastrzeżeniem: users.organization_id to org DZISIEJSZA, nie z chwili share) — do wykonania przez nadzorcę z dostępem, przed fazą 3. **NOWE ZNALEZISKO POZA ZLECENIEM (nie naprawiane, flagowane)**: POST /generate ze scope PROJECT przecieka TREŚĆ obcego projektu — getProjectById nie filtruje po organizacji (WHERE p.id = ?), więc raport zapisuje się już w org atakującego, ale budowany jest z cudzych tasków/inicjatyw/decyzji/ryzyk/insightów (dotyczy TEAM_MEETING, TEAM_WEEKLY, STEERING_COMMITTEE, RAID); robotnik świadomie NIE naprawił, bo naiwny filtr mógłby zerwać legalne przepływy konsultanckie (consultant_org_links) — DECYZJA PRODUKTOWA + osobne zlecenie. |
| DEC-2026-08-27-141 | Duplikat task_c30e0ce2 (mgmtreports) ROZSTRZYGNIĘTY — odrzucony z cherry-pickiem dwóch wartości | **SUPERVISOR_ACCEPT** | Audyt porównawczy (Opus, tabela tras, dowody plik:linia po obu stronach): gałąź `codex/mgmtreports-orgscope-20260826` (3ec3f3ea1c, zadanie task_c30e0ce2 uruchomione równolegle przez właściciela) jest w ~100% pokryta przez scaloną wersję pełną DEC-140, często ze słabszą ochroną (brak 401 na pending-approvals); NIE naprawia żadnego z dwóch otwartych P0 z DEC-140 (share-tokeny cross-org, wyciek PROJECT-scope /generate) — oba pozostają w kolejce bezpieczeństwa przed fazą 3. ODRZUCONA w całości POZA dwoma cherry-pickami scalonymi do m03 (merge d20477b988, gałąź fix/mgmtreports-dup-cherry-20260827): (1) defense-in-depth `createShareLink` UPDATE z `AND organization_id = ?` (jedyna mutacja w ManagementReportRepository.ts bez własnego filtra org; service i tak blokuje przez assertReportInOrganization); (2) helper `makeOrglessSignedToken()` + 3 testy realdb 401 na /history i /analytics/* z prawdziwie podpisanym tokenem BEZ claima org — ścieżka, której dotychczasowy harness e2e-bypass jawnie nie umiał zbudować (przyznane w komentarzu testu). Testy uruchomione na jednorazowym PG 5498: **45/45 PASS** (42 zastane + 3 nowe), kontener posprzątany. Werdykt nadzorcy po spot-checku diffu. |
| DEC-2026-08-27-142 | Akcept właściciela na zrzutach → 4 flagi domyślnie ON | **OWNER_ACCEPT** | Piotr 27.08: „obrazy akceptuję" (Audyty 4 PNG evidence/audits-gaps-20260826 06/10/11/13 + kpiRegistry zrzuty 23.08 zweryfikowane jako aktualne commit-po-commicie) oraz „ok - obrazy" (Tools Insights 3 PNG po naprawie i18n — 936842bd16). Flipy per-commit na fix/flag-flips-owner-accept-20260827: e4123d197b ff_auditsFindingsAndReportView→ON, 0930e21df2 ff_auditsScaleAndPolish→ON, b42d4eb34a kpiRegistry→ON na hostach nieprodukcyjnych (flaga okazała się niebinarna: wspólna funkcja kpi/roi/okr — roi/okr zostają OFF, wzorzec resultsVNextHostAllowsDefaultOn jak threePairs; 11 nowych testów), fb27580af1 ff_toolsInsightsWiring→ON. Fail-closed zachowany (catch→false), query/LS nadal nadpisują. Smoke: audits 10/10+17/17+181/181, results 11/11+16/16, tools 9/9+18/18+6/6; 9 testów zastanych zakładających default=false zaktualizowanych z komentarzem; kanon zielony ×4. Znalezisko uboczne: realna regresja mocków Api.listToolOutputs w 2 plikach testowych naprawiona; zastane 28 FAIL DiscoveryToolsHub.inactiveTools (brak Api.listAssessmentsLegacy w mocku, istnieje przed flipem — dowód git stash) wydzielone jako osobne zadanie. Scalone do m03. |
| DEC-2026-08-27-143 | Dzień 26 (front feedu sygnałów Chat) — odbiór CZERWONY→FIX-1..13→SCALONY | **SUPERVISOR_ACCEPT** | Odbiór warstwy 1 (Opus): łańcuch osiągalności Z20 KOMPLETNY (Sparkles→ChatSignalsPanel→ChatSignalsFeed→GET /api/signals), kanon zielony, flaga fail-closed potwierdzona; OBALONA nośna teza raportu „NO_ROUTE renderuje wyłączoną pigułkę" (7/8 typów nie pokazywało NIC — StandardPreview zwracał null bez onOpenFull), czerwona WPROWADZONA: +79 kluczy i18n bez de/es/ar/ja (1613→1692), zawężenie Z23 (zakres server/signals/__tests__ przemilczany). FIX-y wewnętrzne (12 commitów, gałąź codex/day26-fixes-20260827): licencja nadzorcy na ADDYTYWNY prop openDisabledReason w StandardPreview (test regresyjny: bez propów null jak dotąd) + KOREKTA wykonawcy FIX-ów: w produkcji nagłówek StandardPreview jest tłumiony przez embedded, pigułka przepięta przez renderPreviewActions TableWithPreviewLayout i zweryfikowana na żywo w przeglądarce; i18n wrócił dokładnie do bazy 1613; licencja na wyciszenie legacy-fetch w ChatSignalsPanel przy fladze ON (koniec podwójnych GET i sprzecznego toastu); presety drzemki z komunikatem „do kiedy" z snoozedUntil; chip „Tylko moje" jawnie „(z załadowanych)"; Relacje z typem+nazwą zamiast „Powiązany rekord"; minima testowe DoD dobrane (64/64: A.2 6, A.3 7, A.4 6, B.1 9, C.1 4); harness w realnych 1040px z realnym enum refType; raport dyżuru sprostowany (w tym pomiar serwera: realnie 25 PASS/1 FAIL/39 SKIP self-skip bez bazy — nie 39 FAIL). SCALONE do m03. Flaga ff_chatSignalsFeed NADAL OFF — czeka na akcept właściciela na zrzutach. ZNALEZISKA KANONU poza dyżurem: StandardModuleBar martwa lupa (onSearch ?? noop) oraz nachodzenie wiersza chipów na przycisk odświeżenia przy szerokości 1040px (zadanie task_a0bb8b6e). |
| DEC-2026-08-27-144 | Results O.2 (semantyka CURRENT/DUE) — DOKOŃCZONE wewnętrznie, SCALONE | **SUPERVISOR_ACCEPT** | STOP z dnia 17 („brak wiążącej semantyki CURRENT vs DUE") rozwiązany bez wymyślania progów: predykaty ISTNIAŁY w okrCheckInCommands.ts::loadSetCheckInFacts (~196-251, any_stale + next_checkin_due_at na poziomie Setu) — robotnik przeniósł je na poziom pojedynczego KR + dodał fakt „czy Cykl wygenerował okna" (UNKNOWN/NO_CADENCE_CONFIGURED). Nowy read-only endpoint GET /sets/:setId/check-in-summary (okr.routes.ts:3476+, commit c7ad2bc501) z bramką widoczności jak O.1: niewidoczny Set = 404, nigdy 200 z pustym agregatem (pusty agregat czytałby się jako „Set czysty"). Testy: HTTP 3/3 nowe + 118/118 regres routera; realdb na jednorazowym PG 5561 (sprzątnięty): 5/5 w tym OVERDUE/UNKNOWN/korekta/rekoncyliacja rollup↔KR/negatyw tenanta; pełny folder okr 363/363. Front jeszcze NIE woła (konsument = przyszły blok frontowy Results). Scalone do m03. |
| DEC-2026-08-27-145 | Akcept właściciela zrzutów feedu sygnałów → ff_chatSignalsFeed domyślnie ON | **OWNER_ACCEPT** | Piotr 27.08 („akceptuje jedziemy dalej") na 4 zrzutach po FIX-ach DEC-143 (pełny + podgląd z pigułką Otwórz, light+dark). Flip ac21d2fb66: default ?? true, fail-closed bez zmian, query/LS nadal nadpisują. Smoke 44/44 (w tym nowy test: przy domyślnym ON legacy /my-work/signals nie jest wołane — FIX-7 na ścieżce domyślnej; paczka legacy-UI testów trzyma jawny override =0). Kanon zielony 394=baseline. Zrzut stanu dławienia celowo NIE pokazany właścicielowi — znany defekt wspólnego StandardModuleBar (nachodzenie chipów, zadanie task_a0bb8b6e u właściciela w osobnej sesji) — do domknięcia po naprawie paska. Scalone do m03. |
| DEC-2026-08-27-146 | Dzień 27 (front raportu Oceny) — ZIELONY Z FIX-AMI, SCALONY | **SUPERVISOR_ACCEPT** | Odbiór warstwy 1 (Opus): łańcuch Z20 domknięty (zakładka Report→reportContent→AssessmentReportContractView→GET assessment-report-contract z day20), OFF = bitowo stary stan (dowód prettier-normalizacją vs marker), skip-code realny na POST z idempotencją (Z22 OK), zero dotknięć server/shared/standard/lustra; STOP wykonawcy na NModeHeader ZASADNY (wskaźnik zapisu bezwarunkowy). Przemilczenia wykryte: 13×TS18047 (esbuild nie łapie typów — luka procesu), obcięta kolumna Pominięcia, commit 7a6ff41c15 deklarował nieistniejące testy. FIX-y (8 commitów, codex/day27-fixes-20260827): typy 0 błędów; 6/6 testów skip-code; Pominięcia pod tabelą; LICENCJA NADZORCY: addytywny hideSaveState w NModeHeader (default false, 4 testy) — „Zapisano" zgaszone na read-only; Generuj w wyszarzonym wierszu „Planowane"; breadcrumb bez surowego id (nazwa sesji = §D dyżuru 29); i18n 1674→1613 (zmierzone uczciwie 61 kluczy, nie 62 z briefu). Konflikt locale przy merge (dni 26+27 w tym samym rejonie) rozwiązany 3-way z ustaleniem realnej przyczyny: reformat wcięć dawał fałszywy konflikt — reportView trafił do właściwego top-level assessment, aiChat przywrócony bit-w-bit, unia kluczy zweryfikowana programowo we wszystkich 6 locale (merge 7b7fdaba34, testy 37/37, i18n=1613 baza). RATYFIKACJA: wyjątek §27-exempt dla macierzy w dokumencie (archetyp B) — jednorazowo, nie precedens. Zastane czerwone potwierdzone stash-em (offlineRecovery 5, NModeHeader.ownerActions 4). Flaga ff_assessmentReportView NADAL OFF — czeka na akcept właściciela na zrzutach. |
| DEC-2026-08-27-147 | Dzień 28 (Meetings blok 4) — ZIELONY Z FIX-AMI, SCALONY | **SUPERVISOR_ACCEPT** | Odbiór warstwy 1 (Opus, pomiar własny 269/270 zasięg PEŁNY, 1 FAIL zastany bit-w-bit): A (strefa recurrenceId) POTWIERDZONE z pomiarem rozjazdu 1h odtworzonym na kodzie z markera i zero regresji na starych Z-owych ID; B siatka liczona niezależnie (rrule+Luxon); D bramka occurrence z dowodami mutacyjnymi (2+1 czerwone po usunięciu strażników); E 403 z produkcyjnego errorHandlerMiddleware (dowód mutacją handlera). OBALONE: „naprawa wyścigu replayed dowiedziona" — test przechodził też na kodzie sprzed naprawy. FIX-y (5 commitów, codex/day28-fixes-20260827): P0-1 wariant (a) — test różnicujący z wymuszonym 23505 WYKRYŁ REALNY REGRES scalonej wersji (advisory-lock bez retry → surowy błąd pg = nieobsłużone 500); naprawa = retry-on-23505 wokół transakcji z lockiem (zielony/czerwony/zielony na trzech stanach pliku, 7/7 regres funnela); P1-1 test ścieżki decision (8/8); P1-2 test OBALIŁ przesłankę zmiany retry (userId/roleKey inertne — setNoteStatus RETURNING * bez kolumn material_*) → revert linii + test dokumentujący uczciwy kontrakt, luka setNoteStatus jako osobne znalezisko; P1-4 scenariusz captured §F.3 pkt 7 dowieziony na realnym routerze+PG (3 wiersze captured, zero wywołań emailService.send) — F: STOP→CZĘŚCIOWO (pkt 4-6 STOP zasadny: sprzeczność §F.2↔Z10 do erraty). ZNALEZISKO P1 do decyzji właściciela: zadanie z funnela notatka→zadanie niewidoczne w My Work (funnel nie ustawia assignee_id, wszystkie odczyty My Work filtrują po nim). Erraty procesu: docker rm -fv zamiast volume prune (prune skasował cudzy anonimowy wolumen — szkoda ~zero, wina instrukcji); wartownik wiązania ZAWSZE «MARKER_SHA». Scalone do m03. |
| DEC-2026-08-27-148 | WYMÓG WŁAŚCICIELA: Assessment musi mieć MACIERZ i RAPORT OPISOWY równolegle | **OWNER_REQUIREMENT** | Piotr 27.08 (przy akcepcie kierunkowym ekranu raportu): „raport może i taki być, ale pamiętaj, że w assessmencie musimy mieć macierz i raport opisowy też". Stan: (1) macierz = osobny tryb warsztatu, nienaruszony przez dzień 27 (dowód w odbiorze DEC-146: OFF bitowo stary stan; ON dodaje zakładkę Raport obok trybów); (2) raport opisowy = sloty narracyjne kontraktu 7 rozdziałów (synteza 120-180 słów/oś, komentarz 110-170/obszar), dziś uczciwie puste — wypełnienie przez silnik narracji z bazy (tor dokumentów, wzorzec = złoty plik DRD, krok 2 formuły). KRYTERIUM ODBIORU KOŃCOWEGO modułu Ocena: obie powierzchnie żywe + treść opisowa generowana z danych sesji, nie ręczna. |
| DEC-2026-08-27-149 | Dzień 29 (Assessment blok 3) — CZĘŚCIOWY, dostarczone pozycje ZIELONE, SCALONE; dokończenie wewnętrznie | **SUPERVISOR_ACCEPT** | Uczciwy częściowy (bez fałszywego PASS — pochwała). Odbiór warstwy 1 (Opus, własny PG 5533, sondy na żywym routerze): §D sessionLabel {displayName,source,projectId} addytywnie, contractVersion NIEZMIENIONY (front dnia 27 nie pęka), źródło org-scoped z uczciwym null, 5/5 PG realne (Z21); §C.2 usunięty WŁAŚCIWY plik (assessments.routes.ts mnoga — pułapka jednej litery ominięta), dowód fail-soft przepięty na ŻYWY assessment-hub (pokrycie wzrosło); §B 11/26 tras AI odróżnia trzy stany (sonda: brak/obca→404 nieodróżnialne co do bajta, uczciwa pustka→200, awaria→503), 15 tras celowo nietkniętych (batch7 pinuje 500), commit c782a1b3af to WZMOCNIENIE testu dnia 25 (stary strzelał w nieistniejący string — wada z rodziny DEC-138), nie zamiecenie. Pomiary odbierającego: 222/222 batch6/7+allowlist, 41/41 PG Assessment, migracje czyste. FIX-y tylko P1 dokumentacyjne (fallback org-default ×12 bez wpisu w Znaleziskach; brak tabeli 15 tras). ZNALEZISKO do rejestru: suggest-justification z błędnym axisId zwraca 200 z error w ciele (cicha zieleń, zastane). NIEDOWIEZIONE → DOKOŃCZENIE WEWNĘTRZNE (nie wraca do Codexa): §A @ts-nocheck (203 błędy konfiguracji projektu, plan A-1/A-2/A-3), §B harness 7×3 tras + dowód mutacyjny stash, §C.1 czystka 6 handlerów v1 (allowlista czysta — potwierdzone grep 0; UWAGA: trzeci cudzy test red-assess-500s.e2e nieujęty w instrukcji), §R.1 mianowniki. Scalone do m03. |
| DEC-2026-08-27-150 | Akcept właściciela ekranu raportu Oceny → ff_assessmentReportView domyślnie ON | **OWNER_ACCEPT** | Piotr 27.08 („tak możemy") na 4 świeżych zrzutach post-FIX (evidence odświeżone 3631a94e9e po wykryciu, że gałąź FIX-ów nie zaktualizowała PNG — stare pokazywały stan sprzed napraw). Flip 88c3a042a7: default ?? true, fail-closed bez zmian, query/LS nadpisują. Smoke 36/36, w tym NOWY test DEC-148 reportMatrixCoexist: oba taby (Matrix i Raport) zawsze obecne w realnym MethodWorkspaceShell — flaga steruje tylko treścią zakładki Raport, nigdy nie ukrywa macierzy; Raport przy default ON renderuje widok kontraktowy. Zastane 5 czerwonych offlineRecovery potwierdzone stash-em jako niezwiązane. Kanon zielony. Scalone do m03. Wypełnia wymóg DEC-148 w części ekranowej; treść opisowa z silnika narracji = tor dokumentów (kryterium CLOSED_FINAL bez zmian). |
| DEC-2026-08-28-151 | ★ ZŁOTY PLIK RAPORTU DRD — AKCEPT WŁAŚCICIELA (strach nr 1 rozbrojony) | **OWNER_ACCEPT** | Piotr 28.08 (otworzył .docx+.pdf w Downloads): „mam ten raport i w pdf. sa zajebiste". PIERWSZY dokument w historii programu, który właściciel sam ocenił jako świetny — bezpośrednia odpowiedź na wielokrotnie powtarzany strach nr 1 (nigdy nie powstał ani jeden naprawdę dobry dokument). Wzorzec ZATWIERDZONY jako złoty standard typografii i struktury. KROK 2 FORMUŁY DOKUMENTÓW ODBLOKOWANY: ten sam raport DRD generowany Z BAZY dla innego klienta (dowód, że silnik pisze, nie tylko składa — data.cjs było ręczne). 1B (węższa kolumna/wypełnienie końcówek/Calibri) = opcjonalne dopieszczenie iteracji 2, nie blokuje. |
| DEC-2026-08-28-152 | Dostawca AI: NIE Anthropic teraz — używamy istniejącej konfiguracji | **OWNER_DECISION** | Piotr 28.08: „2 antropika nie używam na razie - używamy co mamy". Zastępuje część DEC-59/109 o kluczu Anthropic: warstwa AI działa na już skonfigurowanym dostawcy (Gemini/GoogleGenerativeAI w aiAssessmentPartnerService). „Analizuj AI" i playbooki nie czekają na klucz Anthropic. Nie usuwać obsługi ANTHROPIC_API_KEY (bramka ją przyjmuje) — po prostu nie jest to teraz wymagany tor. |
| DEC-2026-08-28-153 | Assignee zadania z funnela notatka→zadanie (delegacja: nadzorca decyduje) | **SUPERVISOR_DECISION** | Piotr 28.08: „3 zadecyduj sam". Znalezisko z odbioru dnia 28 (DEC-147): zadanie tworzone z notatki spotkania trafia do bazy bez assignee_id → niewidoczne w My Work (wszystkie odczyty filtrują po assignee_id). DECYZJA: assignee = właściciel działania wskazany w notatce; przy braku → twórca notatki (autor). Fix zlecony wewnętrznie. |
| DEC-2026-08-28-154 | Partia pięciu decyzji właściciela (a-e), w tym KOREKTA: eksport Assessment BUDOWAĆ | **OWNER_DECISION** | Piotr 28.08 dosłownie: (a) Consultant Mode skasować — „tak jeżeli tak uważasz"; (b) martwy middleware limitów zasobów org skasować — „tak jeżeli tak uważasz"; (c) atrapy eksportu Assessment — **„zbudować nie ukrywać"** (KOREKTA rekomendacji nadzorcy, który proponował ukrycie jako „Planowane" do czasu silnika — właściciel chce realnego eksportu; spójne z DEC-151: wzorzec dokumentu zaakceptowany, więc eksport Assessment = kolejny konsument silnika, osobny dyżur po dniu 32); (d) read-only zapytanie kontrolne do bazy demo/staging (to_regclass, 78 nazw, zero zapisów) — „jeżeli tak uważasz to tak"; (e) financeValueDemoAllowlist wyłączający demoWriteGuard — **„tak"**, naprawić przed deployem (bug, nie funkcja). TRYB: trzy odpowiedzi warunkowe („jeżeli tak uważasz") = delegacja pod warunkiem pewności → zgodnie z kodeksem uruchomiony SCEPTYK ADWERSARYJNY przed skasowaniem (a) i (b); kasowanie nieodwracalne, więc dowód martwoty musi być na runtime, nie z dokumentów. (d) wykonywane wyłącznie jako SELECT to_regclass, zero zapisów, bez zmian schematu. |
| DEC-2026-08-28-155 | P0 wyciek PROJECT-scope /generate ZAMKNIĘTY (druga luka DEC-140) | **SUPERVISOR_ACCEPT** | Karta decyzyjna (Opus) obaliła obawę produktową: consultant_org_links nie dotyczy tej trasy, cross-org działa przez switch-organization z nowym tokenem → to czysty P0, naprawa niczego nie psuje (getProjectById ma 2 callery, oba w tym pliku). KLUCZOWE ustalenie ponad DEC-140: filtr na getProjectById NIE wystarcza — RAID czyta projekt surowym SQL (SELECT * FROM project_insights WHERE project_id=?), omijając getProjectById. Naprawa (a57667a595): assertProjectInOrganization(projectId, organizationId) jako PIERWSZA instrukcja generateReport(), przed switch(reportType) — przed KAŻDĄ gałęzią (TEAM_MEETING/TEAM_WEEKLY/STEERING_COMMITTEE/RAID); organizationId wyłącznie z req, nigdy z body; obcy/nieistniejący projekt → 404. Nowa metoda repo getProjectByIdForOrganization (WHERE p.id=? AND p.organization_id=?). Testy realdb 56/56 (12 nowych: własny→200+zapis, obcy realny→404+zero zapisu z readback COUNT, per-typ obcy→404). Dowód mutacyjny: bramka wyłączona → 200+zapis wraca. Znalezisko poboczne (task_2baffc54, poza P0): STEERING_COMMITTEE/RAID scope=PROJECT mają zastany bug getBoardDecisions (TEXT vs liczba, join po nieistniejącej kolumnie) → 500 dla KAŻDEGO projektu. Scalone. DEC-140 (obie luki: share-token rotacja z DEC-141 + PROJECT-scope tutaj) ZAMKNIĘTE co do naprawy; share-token cross-org consumption pozostaje osobnym śledztwem (getByShareToken bez wywołań). |
| DEC-2026-08-28-156 | financeValueDemoAllowlist omijał demoWriteGuard — FURTKA ZAMKNIĘTA (DEC-154e) | **SUPERVISOR_ACCEPT** | Bug gorszy niż opis: Gateway.ts:564 wołał next() ZAMIAST guarda dla 4 tras POST /api/v8/finance/value/* — demoWriteGuard w ogóle się nie uruchamiał, niezależnie od org demo. Moduł deklarował w komentarzu „not imported by Gateway" — NIEPRAWDA (był importowany i użyty), dowód że włączono „propozycję" na żywo bez przeglądu bezpieczeństwa. Naprawa (7a201826e1): usunięty warunkowy wrapper + import isStatelessComputeDemoRoute; `app.use(demoWriteGuard)` bezwarunkowo = guard jedynym autorytetem. Allowlista ZOSTAJE w repo odłączona jako udokumentowany audyt (6 serwisów value-engine bezstanowych, zero importów DB — furtka nie chroniła realnej mutacji, otwierała tylko lukę). Test gatewayFinanceValueAllowlist przepisany (pinował bug!); dowód mutacyjny: cofnięcie Gateway.ts → 2/4 padają. Zachowanie: value-bridge+demo→403, bez demo→200, trasy spoza→403 (bez regresji). Scalone przed deployem. |
| DEC-2026-08-28-157 | Kasacja martwego kodu: resourceQuota + Consultant martwica (a1) — SCALONE | **SUPERVISOR_ACCEPT** | Po weryfikacji adwersaryjnej sceptyka: (B) resourceQuota.middleware.ts MARTWE potwierdzone (0 importerów, tabela organization_resource_usage nie ma DDL nigdzie w repo) → skasowane (28695509f3, -1726 linii: middleware+test 1329l+shim + 3 pliki config testów L1 w tym samym commicie by nie zerwać CI). Kasacja NIE zdejmuje realnej ochrony (middleware nigdy niezamontowany, działałby fail-open na nieistniejącej tabeli) — odnotowane wprost, by nie wyglądało na obniżenie poprzeczki L1. (A) Consultant Mode NIE martwy lecz ZEPSUTY end-to-end (front woła 3 ścieżki których router nie ma → zawsze 404; tabela consultants bez DDL w uruchamianych migracjach) → kasacja w 3 częściach: a1 CZYSTA MARTWICA skasowana (7f375144e0, -900 linii: consultantService 523l 0-importerów + pozorny test + never-ran 017 + TeamExpansionTrigger 178l 0-importerów + wpis consultant_mode w featureGate + shim); a2 lej wizualny (widoki + przycisk TrialEntryView) = ZMIANA WIZUALNA, czeka na zrzut do akceptu Piotra; a3 consultant_org_links w orgContext.middleware + migracja 20261122 = ŻYWY, NIE RUSZANE (testy orgContext 68/68 potwierdzają). Zastane 2 faile permissionService potwierdzone stash-em jako niezwiązane. Poboczny shim _backup/consultantService.js zgłoszony osobno (task_626362f5). Scalone. |
| DEC-2026-08-28-158 | Kontrola read-only bazy staging (DEC-154d) — WYKONANA, ujawniła DRYF SCHEMATU | **SUPERVISOR_ACCEPT** | Wykonana na Railway/staging w transakcji BEGIN READ ONLY, wyłącznie to_regclass + count, zero DDL/DML. Odpowiedź na pytanie sceptyka: `consultants`, `consultant_org_links`, `consultant_invites`, `organization_resource_usage` — ŻADNA NIE ISTNIEJE na żywej bazie → kasacja DEC-157 bezpieczna z perspektywy danych (brak nośnika), niezależne potwierdzenie commita 07dcd77b98. KOREKTA SZACUNKU: nie „78 nazw" (w repo nie ma listy CRITICAL_RELATIONS) — zbudowano listę z faktycznie uruchamianych migracji: 1656 unikalnych nazw, 1339 obecnych, **317 BRAKUJĄCYCH**. Wśród najczęściej odwoływanych z server/src: tool_outputs (65 odwołań!), inbox_items (69), analysis_financials (72), analysis_financial_scenarios (41), billing_alerts (35), approval_requests (18), user_profiles (16). RYZYKO WPROST: tool_outputs to źródło Tools Insights, którego flagę włączyliśmy dziś (DEC-142) — jeśli tabeli nie ma na staging, ekran pokaże pustkę udającą „brak danych" albo błąd. Zlecona analiza dryfu (świeży PG + pełne migracje = schemat docelowy vs staging vs kod) z klasyfikacją KRYTYCZNA/OPERACYJNA/FANTOM/SZUM — WYNIK BLOKUJE DEPLOY STAGINGU do czasu rozstrzygnięcia. Sieroty schematu nieustalone (wymagałyby enumeracji information_schema — poza autoryzowanym zakresem read-only). |
| DEC-2026-08-28-159 | webhook_events bez kolumny tenantowej — OPCJA A: endpoint superadminowy | **OWNER_DECISION** | Piotr 28.08: „A". Kontekst: GET /api/webhooks/events (trasa montowana BEZWARUNKOWO, żywa też na produkcji) zwracała adminowi każdego tenanta metadane 100 ostatnich webhooków całej platformy (kto z kim się integruje, typy zdarzeń, czasy). Zarzut o „surowe payloady" OBALONY pomiarem — SELECT nie zawiera kolumny payload. Tabela webhook_events nie ma i nigdy nie miała kolumny tenantowej; danych historycznych nie da się przypisać (GitHub/generic nieuwierzytelnione, brak klucza do tenanta; tylko Jira częściowo przez integration_sync_mappings). WYBRANO A: endpoint wyłącznie dla superadmina, zero migracji, luka zamknięta natychmiast; kolumna tenantowa do rozważenia w przyszłym bloku integracji. Trasy PRZYJMUJĄCE webhooki bez zmian (muszą być publiczne). Fix w toku. |
| DEC-2026-08-28-160 | ★ SYSTEMATYCZNY AUDYT WSZYSTKICH ROUTERÓW — 609 plików, 13 czerwonych, 8 potwierdzonych na runtime | **SUPERVISOR_ACCEPT** | Armia 4 audytorów (sektory A-E 210 plików, F-M 79, N-R 146, S-Z+v8 174) + 4 weryfikatory adwersaryjne na ŻYWYM PG (2 org, mutacja cudzego zasobu, readback). POTWIERDZONE I NAPRAWIONE: (1) raid_items IDOR — PUT/PATCH/DELETE po id, dowód „PWNED BY ORG A" + DELETE→null, naprawa 9f16b0d88b; (2) SCIM /Users /Groups — admin dowolnego tenanta sam sobie wystawia token (adminP32) i kasuje cudze grupy + dezaktywuje konta z unieważnieniem sesji; trasa montowana BEZWARUNKOWO (żywa na produkcji), naprawa a23551e4d9 (filtr org na ~35 statementach, token bez org→401, revokeUserSessions scoped, blokada wstrzyknięcia obcego usera do grupy, rozróżnienie kolizji e-maila w org vs cross-org); (3) webhooks/inbox FAIL-OPEN — anonimowy zapis do cudzej skrzynki, POTWIERDZONY NA ŻYWYM STAGINGU (INBOX_WEBHOOK_SECRET nie był ustawiony wśród 114 zmiennych); (4) stage-gates — zero izolacji, odczyt cudzych bramek + przełamana autoryzacja zapisu (dziś neutralizowana tylko dryfem schematu, nie kontrolą); (5) accessCodes — ESKALACJA: consultant tworzy kod na cudzą org, rejestracja przez /auth/register wpisuje ACTIVE członkostwo w org ofiary (dowód end-to-end); (6) enterprise-platform — odczyt I ZAPIS do cudzej org przez surowy nagłówek x-organization-id; (7) connectors + integration logs (żywe tylko dev/staging, prod zgaszony mountStub); (8) table-platform ×3 — zapis do cudzych tabel, najgorszy: masowa korupcja dat wszystkich rekordów cudzej tabeli jednym żądaniem. OBALONE (fałszywe alarmy — wartość procedury adwersaryjnej): rapidlean (2 warstwy blokują), report-builder org-default (bariera FK, kubełek nieosiągalny), webhooks „surowe payloady" (SELECT nie zawiera payload), ai-prompts (internalToolsGuard → 404 dla klienta), 5 z 6 plików drugiej fali (billing, finance-statements, settings, conversations, presentations — kontrola własności w innej warstwie). WZORZEC SYSTEMOWY: 95 plików/374 mutacji „WHERE id=? bez org" — próbka wykazała ~5-12% realnych, skupionych w NOWSZYCH modułach z surowym SQL. Sonda v8: 3/3 poprawnie izolowane — osobna fala audytu serwisów v8 NIE jest pilna. |
| DEC-2026-08-28-161 | webhooks/inbox — dziura zamknięta NA ŻYWO przez właściciela + fail-closed w kodzie | **OWNER_ACTION + SUPERVISOR_ACCEPT** | Piotr 28.08 ustawił INBOX_WEBHOOK_SECRET w Railway (staging, usługa consultify) — zweryfikowane przez nadzorcę: 115 zmiennych (było 114), długość 64. SONDA NA ŻYWYM STAGINGU po zmianie: POST /api/webhooks/inbox/slack z ciałem event_callback → **401 Unauthorized** z błędnym sekretem I bez sekretu (przed zmianą: 200 + wiersz w skrzynce cudzej org). UWAGA METODYCZNA: pierwsza sonda dała fałszywe 200 — ciało bez type='event_callback' wraca 200 ZANIM kod sprawdzi sekret (wcześniejszy return); poprawna sonda wymaga realnego kształtu zdarzenia. Naprawa kodu (fail-closed przy braku sekretu = 503 zamiast przepuszczania) w toku — sekret to obejście operacyjne, nie zamiennik poprawki. ZNALEZISKO DO PRZYSZŁEGO BLOKU: to JEDEN globalny sekret dla wszystkich najemców — jego posiadacz pisze do dowolnego ?orgId=; docelowo weryfikacja podpisu Slacka + wiązanie org z instalacji. |
| DEC-2026-08-28-162 | Naprawy z audytu SCALONE: SCIM, connectors, integration logs, raid, webhooks | **SUPERVISOR_ACCEPT** | Wszystkie z dowodem mutacyjnym (cofnięcie naprawy → atak wraca): SCIM a23551e4d9 (filtr org ~35 statementów, token bez org→401, revokeUserSessions scoped, blokada wstrzyknięcia obcego usera do grupy; dowód mutacyjny: bez fixu PATCH cudzej grupy→200 PWNED, DELETE→204, dezaktywacja usera org B unieważniała jego żywą sesję, GET /Users tokenem bez org→5 userów WSZYSTKICH organizacji); connectors 216504a94d (PUT/DELETE + AND organization_id, 404 przy changes=0, 6/6); integration logs 656c92942f (assert przynależności przed odczytem, 404, 4/4 — testowane rolą MEMBER, bo dziura była otwarta dla KAŻDEGO zalogowanego); raid 9f16b0d88b (4/4); webhooks/events superadmin 2800d08e12 (5/5). ZNALEZISKA POBOCZNE do osobnych zadań: (a) tabela `connectors` jest w never-ran/, mimo że routing i DatabaseInitializer.CRITICAL_TABLES jej wymagają — a atak się powiódł na sondzie, więc na dev/staging fizycznie istnieje (kolejny przypadek dryfu, patrz DEC-158); (b) ★ ZAGROŻENIE DLA WŁASNEJ PRACY: `tests/setup.ts` ma domyślny fallback DATABASE_URL=postgresql://iris:iris_test@localhost:5432/iris_test, a na 5432 nasłuchuje ŻYWA baza innej sesji — testy uruchamiane bez jawnego DATABASE_URL mogą trafić w cudzą bazę zamiast się pominąć; wszyscy robotnicy MUSZĄ uruchamiać testy z jawnym DATABASE_URL do własnego efemerycznego PG. Do wpisania w bezpieczniki przyszłych instrukcji jako Z25. |
| DEC-2026-08-28-163 | Zadania-widma z funnela notatek NAPRAWIONE (DEC-153) — z uczciwym ograniczeniem | **SUPERVISOR_ACCEPT** | Naprawa bf00af5a78: `createTaskFromMeetingNoteAction` ustawia `assigneeId` (wcześniej INSERT dawał NULL → zadanie niewidoczne w My Work, bo wszystkie odczyty filtrują po assignee_id). UCZCIWE OGRANICZENIE: pole `owner` w `MeetingNoteActionItem` to WOLNY TEKST z ekstraktora AI/heurystyki (ścieżka heurystyczna zawsze 'Unassigned'), NIE MA pola strukturalnego `ownerUserId` (kontrast: `meeting_follow_ups` takie pole MA) ani UI do przypisania właściciela przed konwersją. Robotnik świadomie NIE parsuje nazwisk heurystycznie (literówka/duplikat imienia → cichy błędny assignee = gorsze niż bug) — więc reguła DEC-153 w obecnym modelu danych ZAWSZE rozstrzyga się do fallbacku: assignee = twórca notatki, z kontrolą tenant (autor spoza org → fallback na aktora klikającego, już zweryfikowanego przez trasę). Tekst `owner` zostaje w opisie zadania. DO DECYZJI WŁAŚCICIELA: (1) backfill istniejących zadań z NULL — zapytanie `SELECT COUNT(*) FROM tasks WHERE assignee_id IS NULL AND source_type='meeting_note_action_item'` do odpalenia przez kogoś z dostępem; (2) czy dobudować pole strukturalne ownerUserId + UI wyboru właściciela (osobny blok — dopiero to zrealizuje DEC-153 w pełnym brzmieniu). Testy: 3/3 nowe end-to-end przez REALNĄ trasę GET /api/my-work/tasks, 8/8 regres day24, dowód mutacyjny (cofnięcie → assignee_id znów NULL). |
| DEC-2026-08-28-164 | Z26 — druga pułapka harnessu testowego (do bezpieczników przyszłych instrukcji) | **SUPERVISOR_NOTE** | Bez `RUN_DB_TESTS=1 MOCK_DB=false` przy `NODE_ENV=test` ODCZYTY (DbPromise→Database) idą cicho na in-memory mock, podczas gdy ZAPISY przez `withPgTransaction` trafiają do prawdziwego Postgresa — każdy test używający getMeetingNote/podobnych odczytów dostaje mylące fałszywe 404 BEZ żadnego logu błędu. Razem z Z25 (tests/setup.ts domyślny DATABASE_URL na cudzą bazę na 5432) do wpisania w §0 KAŻDEJ przyszłej instrukcji: testy realdb wyłącznie z jawnym DATABASE_URL własnego efemerycznego PG + RUN_DB_TESTS=1 MOCK_DB=false. Obie pułapki wyjaśniają część historycznych „zastanych czerwonych" w raportach dyżurów. |
| DEC-2026-08-28-165 | ★★★ ROZSTRZYGNIĘTE: aplikacja staging i bramka migracji używają DWÓCH RÓŻNYCH BAZ | **SUPERVISOR_ACCEPT — BLOKER DEPLOYU** | Dowód rozstrzygający (odczyt, zero zapisów): log bramki z ostatniego UDANEGO deployu (d43e52c0, 26.08 07:07) raportuje `RELEASE_MIGRATION_GATE_PASS pending=0 failed=0 skipped=0`; analityk odtworzył tę samą funkcję isExecutableMigration() na dokładnej liście plików z wdrożonego obrazu (e6ca206c, 1006 plików, required=794) i wynik 0/0/0 pasuje WYŁĄCZNIE do bazy Rehearsal. MAPA: aplikacja (`DATABASE_URL` usługi consultify) czyta `trolley.proxy.rlwy.net:28146` — 1753 tabele, 782 migracje, OSTATNIA 2026-08-13, `tool_outputs` BRAK, **106 migracji pending**; bramka migruje `Postgres-Rehearsal-20260820` (sakura) — 1766 tabel, 794 migracje, 0 błędów, `tool_outputs` JEST; usługa `Postgres` (thomas) — 1000 tabel, ostatnia migracja 2026-07-05 (martwa); `pgvector` — 0 tabel. TRZY BAZY O TEJ SAMEJ NAZWIE `railway` = bezpośrednia przyczyna pomyłki. DODATKOWO: (a) `DATABASE_URL` to URL WPISANY NA SZTYWNO (publiczne proxy), nie referencja `${{Postgres.DATABASE_URL}}` — dlatego rozjazd był bezgłośny; host `trolley` nie należy do ŻADNEJ z 3 usług bazodanowych stagingu ani do produkcji (prawdopodobnie osierocone proxy); (b) ostatni deploy 16f5744f (26.08 22:36) ma status FAILED bez logów — spójne z tym, że bramka trafiła już na trolley, znalazła 106 pending i fail-closed zablokowała deploy; (c) staging chodzi na buildzie e6ca206c z gałęzi BACKUPOWEJ (`github-backup/backup/detached-e6ca206`), który NIE jest przodkiem origin/demo; (d) `DB_MANAGED_SCHEMA=off` — aplikacja nie dorabia schematu sama, wszystko zależy od bramki. SPROSTOWANIE: migracja 946_tool_outputs powstała 2026-08-16 (nie 13.08), a trolley migrował ostatnio 13.08 — czyli ZANIM plik istniał; wcześniejszy pomiar „1656/317" nie odtwarza się na żadnej z 4 baz (trolley 1762, Postgres 1002, Rehearsal 1775) — do wyjaśnienia, na czym był robiony. TRZY DECYZJE WŁAŚCICIELA PRZED DEPLOYEM: (1) która baza jest bazą stagingu — Rehearsal (kompletny schemat) czy trolley (dane, schemat z 13.08); bazy mają rozbieżne zbiory migracji W OBIE STRONY (106 brakuje na trolley, 94 są TYLKO na trolley, m.in. seria 600-670_v4_*) → to decyzja o DANYCH, nie o schemacie; (2) przepięcie DATABASE_URL na referencję Railway + synchronizacja RELEASE_TARGET_DB_HOST_FINGERPRINT; (3) build z origin/demo zamiast z gałęzi backupowej. Obowiązkowy backup przed jakąkolwiek migracją. TANI TEST NA NAWRÓT: w logach deployu para linii `RELEASE_MIGRATION_GATE_PASS` i `[Postgres] Config: {host}` musi wskazywać TĘ SAMĄ bazę. |
| DEC-2026-08-28-166 | inbox fail-closed + stage-gates: izolacja i koniec fałszywego sukcesu — SCALONE | **SUPERVISOR_ACCEPT** | (1) inbox webhook 3eb9aa0dea: middleware na poziomie routera odmawia 503 gdy INBOX_WEBHOOK_SECRET nieustawiony, w KAŻDYM środowisku (świadomie ostrzej niż stripe.routes.ts) — sekret na Railway (DEC-161) był obejściem operacyjnym, teraz kod jest fail-closed sam z siebie; 5/5 realdb, dowód mutacyjny: pre-fix 200 + realny wiersz u ofiary. (2) stage-gates 64f572b9fe: wszystkie 4 handlery (evaluate/current/pass/history) rozstrzygają projekt przez `WHERE id=? AND organization_id=?` z req.user PRZED ewaluacją/zapisem, obcy → 404; sprawdzenie roli manage_stage_gates przeniesione ZA check przynależności (wcześniej PermissionService dostawał organizationId ATAKUJĄCEGO — bramka strukturalnie nie mogła chronić cross-tenant); INSERT w passGate z fallback:false → AppError(500) zamiast cichego {status:'PASSED'} przy błędzie bazy; 7/7 realdb, trzy dowody mutacyjne (wyciek evaluate, dotarcie zapisu na cudzy projekt, odtworzony sfabrykowany sukces). ZNALEZISKA POBOCZNE: (a) `projects.current_phase` nie istnieje w aktywnym schemacie (jest `phase`) — getCurrentGate zwraca 404 nawet dla WŁASNEGO projektu, bug zastany, osobne zgłoszenie; (b) ★ Z27 INCYDENT OPERACYJNY: `git stash` jest WSPÓŁDZIELONY między wszystkimi worktree repo (.git/refs/stash nie jest per-worktree) — robotnik przypadkowo zaaplikował cudzą zmianę z równoległej sesji; wykryte i naprawione bez wpływu na commit. DO BEZPIECZNIKÓW: przy pracy równoległej ZAKAZ `git stash` do dowodów mutacyjnych — używać lokalnych kopii plików (cp). |
| DEC-2026-08-28-167 | table-platform ×3 IDOR zamknięte + pełny audyt mutacji w pliku | **SUPERVISOR_ACCEPT** | Naprawa d31c64fd06: trzy handlery (PUT dependency-config :5170, POST date-dependencies/recalculate :5199, POST fields/reorder :5311) rozstrzygają `tp_tables.base_id` i wołają `PermissionsService.canAccessBase(userId, orgId, baseId)` PRZED mutacją — dokładnie wzorzec siostrzanych handlerów record-templates w tym samym pliku (asymetria = przeoczenie, nie zamiar). Odstępstwo świadome: 404-not-found / 403-denied wg konwencji PLIKU zamiast uniformnego 404 (nie wprowadzamy trzeciego wzorca). Audyt WSZYSTKICH 7 surowych mutacji w pliku: 4 były już chronione (requireFieldAccess ×1, canAccessBase ×3), 3 naprawione. Testy 13/13 realdb + dowód mutacyjny (cofnięcie → 6 asercji pada, oryginalna podatność się odtwarza), sąsiednie suity 75/75 bez zmian. POZOSTAWIONE ŚWIADOMIE (poza zakresem, do drobnego zgłoszenia): dwa handlery ODCZYTOWE bez kontroli — GET dependency-config i POST detect-cycle — nie mutują, ale mogą potwierdzić istnienie cudzej tabeli. DRUGI RAZ TEGO DNIA potwierdzony Z27 (git stash współdzielony między worktree — kolizja z równoległym robotnikiem, wykryta i naprawiona, commit czysty). |
| DEC-2026-08-28-168 | Analiza bramki fail-closed dla tokenu bez organizacji — REKOMENDACJA WARIANTU A | **SUPERVISOR_NOTE — do wykonania** | Trzy wycieki (enterprise-platform odczyt+ZAPIS przez surowy x-organization-id, assessment-workflow 22× `\|\| 'org-default'`, assessment-ai 12×) mają JEDEN mianownik: token uwierzytelniony bez rozstrzygniętej organizacji. ŹRÓDŁA SIEROT (dowód plik:linia): SCIM `scim.routes.ts:336` INSERT users BEZ organization_id i BEZ organization_members (TRWAŁE), OAuth `oauthService.ts:352-359` gałąź existingUser zwraca wiersz jak jest → `generateTokenPair` podpisuje pustą org bez walidacji. Łańcuch: SCIM tworzy sierotę → ten sam e-mail loguje przez Google → token bez org → enterprise-platform czyta nagłówek. ZAWĘŻENIE (ratunek w attachUser:844-875): req.organizationId jest puste WYŁĄCZNIE dla konta bez ANI JEDNEGO ACTIVE membership. SONDA: bramka 401 bez allowlisty na 4995 testach → **0 nowych porażek**; kontrola poprawności sondy (warunek zawsze-prawda) dała 93 porażki, więc zero jest realne, nie martwa łatka. LUKA POKRYCIA: 163 routery z verifyToken, tylko 37 z requireOrgAccess. DECYZJA NADZORCY: wariant A (globalna bramka + allowlista), ale **403 ORG_CONTEXT_REQUIRED zamiast 401** — `api.ts:528` latchuje globalną blokadę po serii 401 i wyrzuciłby sierotę w pętlę wylogowania; 403 jest też spójne z requireOrgAccess. Kroki 1-8 w raporcie analityka (m.in. usunięcie odczytu nagłówka w enterprise-platform, 34 fallbacki org-default, naprawa źródeł sierot w SCIM/OAuth, testy pinujące). WARUNEK WSTĘPNY: policzyć sieroty na żywej bazie (`SELECT id,email FROM users WHERE organization_id IS NULL OR organization_id=''`) — to jedyna liczba decydująca o realnym impakcie. |
| DEC-2026-08-28-169 | ★ E-O3 / E-O4 / E-O5 ROZSTRZYGNIĘTE + dyrektywa „budujemy wszystko, bez skrótów" | **OWNER_DECISION** | Piotr 28.08 na karcie decyzyjnej: „Wybierz wszędzie twoje rekomendacje - pamiętaj nigdzie nie idziemy na skróty i rozwijamy wszystko i budujemy. Aplikacja musi już być gotowa." PRZYJĘTE: **E-O3 = wariant C** (BSC 4 perspektywy: Finanse · Klient/Rynek · Procesy wewnętrzne · Ludzie/Kompetencje + PIĄTA warstwa „Ład i jakość danych" mówiąca, na ilu kompletnych danych raport stoi; etap przejściowy: dopóki cele nie mają przypisanych perspektyw, ekran nazywa się „raportem operacyjnym", nie strategicznym). **E-O4 = wariant B domyślnie** (skala 3-stopniowa przy inicjatywie: Krytyczna/Ważna/Wspierająca, zatwierdzana przez właściciela celu) **+ wariant C jako OPCJA** dla klientów z porządnym pomiarem (wkład liczbowy w jednostce miary z zamrożoną bazą i rozliczeniem obietnica-vs-wykonanie; mechanika już istnieje w rvn_kpi_initiative_impacts). Progi towarzyszące: **zagrożone = 7 dni przed terminem**, **decyzja opóźniona = 5 dni roboczych**. **E-O5 = wariant C** (trzy pasma: do 80% norma, 80-95% wysycony, >95% przeciążony; **bufor 15% mocy odjęty z góry** na pracę nieplanowaną; progi edytowalne per klient). KOREKTA MERYTORYCZNA: E-O5 w briefie nadzorcy było opisane jako „próg opłacalności inwestycji w inicjatywę" — w produkcie to WYSYCENIE MOCY LUDZI; karta i decyzja dotyczą realnego E-O5. Próg opłacalności nie istnieje nigdzie w kodzie ani kontrakcie — jeśli będzie potrzebny, to NOWA decyzja. WARTOŚCI LICZBOWE (7 dni / 5 dni roboczych / 80-95% / bufor 15%) to propozycja nadzorcy przyjęta przez właściciela, NIE znalezisko z repo (migracja polityki jawnie: „No default policy is seeded"). DYREKTYWA WYKONAWCZA: budujemy TAKŻE brakującą infrastrukturę, bez której decyzje są martwe — (1) ścieżka ZAPISU polityki (dziś execution_control_kpi_policies ma wyłącznie odczyt, zero pisarzy) + ekran polityki dla konsultanta; (2) pole `perspective` przy celu (nie istnieje w żadnej migracji) + ekran przypisania cel→perspektywa; ZAKAZ rozciągania heurystyki inferPerspective(kpi.name) z balancedScorecardService na raport zarządczy — perspektywa MUSI być deklaracją człowieka; (3) UI dla contribution_weight (kolumna istnieje od baseline, ZERO wywołań z UI — nikt jej nigdy nie wypełnił); (4) źródło danych o nieobecnościach/urlopach/stałych obowiązkach/rezerwacjach (system ich NIE ZNA — dziś porównuje tylko godziny zadań z nominalnym etatem); do czasu podłączenia raport ma pokazywać „nie wiem", nigdy ładne procenty. RYZYKO DO SPRAWDZENIA PRZY (3): `goal_initiative_links` nie ma kolumny organization_id (znany kształt wycieku RES-10, dziś strzeżony testem na JOIN po initiatives) — jeśli wagi mają tam zamieszkać, wymaga osobnej naprawy izolacji. |
| DEC-2026-08-28-170 | Consultant Mode część wizualna (a2) SKASOWANA — decyzja właściciela bez zrzutu | **OWNER_DECISION + SUPERVISOR_ACCEPT** | Piotr 28.08: „Kasuj bez zrzutu" (na pytanie, czy zmianę wizualną pokazać przed kasacją). Podstawa: funkcja NIGDY nie działała end-to-end — front wołał /consultants/orgs, /clients, /invites, a zamontowany router miał wyłącznie / i /:id → zawsze 404, także w dev; tabela `consultants` bez DDL w uruchamianych migracjach i NIE ISTNIEJE na żywej bazie (DEC-158). Kasacja f88716c3e6: 18 plików, -744 linie — przycisk „Zamów kody dostępowe" z TrialEntryView, oba widoki, trasy w AppRoutes/routeConfig, AppView.CONSULTANT_*, gałąź w useBreadcrumbs, 8 funkcji API (organizations.api + api), router consultants.routes.ts + montaż + wpis STUB_NAMES_WITH_LIVE_UI_ON_DEMO, osierocony re-export w routes/index.ts (znaleziony grepem — inaczej złamałby kompilację), klucze i18n pl/en (de/es/ar/ja nigdy ich nie miały). CUDZE TESTY PRZEPIĘTE (nie usunięte po cichu): d01HonestStub — wpis przeniesiony z LIVE_UI_STUB_PATHS do NO_LIVE_UI_CONTROL_PATHS z uzasadnieniem; routeConfig — asercje oczekują null jak dla innych niezmapowanych ścieżek; 31/31 zielone. ODSTĘPSTWO ZAAKCEPTOWANE: robotnik posprzątał dwa orphan-duplikaty poza listą (packages/shared/src/types/app-view.ts, config/viewToModuleMapping.ts w root) — zero konsumentów, inaczej finalna weryfikacja grepem nie dałaby zera. a3 POTWIERDZONE NIETKNIĘTE: zero zmian w orgContext.middleware/migracji 20261122/consultant-project-access; testy orgContext 25 passed/45 skipped. i18n bez pogorszenia (8/5 przed i po). Kanon zielony. Scalone. |
| DEC-2026-08-28-171 | ★★★ GRANICA OCHRONY DANYCH — zasada naczelna właściciela | **OWNER_DECISION** | Piotr 28.08 dosłownie: „jedyne czego bronimy za wszelką cenę to baza danych produkcyjnych" oraz „consultify.ai - to produkcja. A demo.consultify.ai i staging.consultify.ai ma tą samą bazę - ale ona nie jest wartościowa - raczej śmieci seedowane". USTALENIE WIĄŻĄCE: (1) **`consultify.ai` = PRODUKCJA = NIETYKALNA** — zero operacji, zero migracji, zero zapisów, żadnego kontaktu bez jawnej, osobnej zgody właściciela; (2) **`demo.consultify.ai` i `staging.consultify.ai` dzielą JEDNĄ bazę, której zawartość jest bezwartościowa (seed/śmieci)** — ta baza jest WYMIENNA: wolno ją migrować, przebudować od zera i przeseedować bez pytania o każdy krok. TO ODBLOKOWUJE DEC-165: wybór bazy stagingu przestaje być decyzją o danych (nie ma czego ratować) i staje się decyzją techniczną — bierzemy wariant dający KOMPLETNY, ODTWARZALNY schemat. KOREKTA WCZEŚNIEJSZEJ ZASADY: reguła z CLAUDE.md „origin/demo = święta baza, demo zamrożone" dotyczy GAŁĘZI KODU i procedury wdrożeń (nie force-push, nie masowe włączanie flag), NIE zawartości bazy danych demo — ta jest odbudowywalna. UWAGA UTRZYMANA: „dane demo = twarz produktu" (CLAUDE.md) pozostaje w mocy — po przebudowie baza demo musi dostać PORZĄDNY seed prezentacyjny, nie przypadkowe śmieci; probe'y i testy nadal sprzątają po sobie. WERYFIKACJA GRANICY w toku (mapa środowisk, izolacja produkcji, szczelność validate-deploy-target.sh, stan backupów) — wynik wpisany osobno. |
| DEC-2026-08-28-172 | ★ KOREKTA NADZORCY: backupy ISTNIEJĄ (moja wcześniejsza teza była FAŁSZYWA) + plan porządkowania środowisk | **SUPERVISOR_CORRECTION** | SPROSTOWANIE: powiedziałem właścicielowi „zero dowodów, że jakikolwiek backup kiedykolwiek powstał" — TO BYŁO NIEPRAWDĄ. Zweryfikowane własnym `ls`: `/Users/piotrwisniewski/Developer/Consultify/.tmp/incident-20260822/db-backups/` zawiera PIĘĆ zrzutów z 22.08.2026: production-postgres.dump 37 MB, demo-pgvector.dump 234 MB, staging-rehearsal.dump 7,6 MB, staging-postgres.dump 4,4 MB, dev-postgres.dump 4,2 MB. Przyczyna błędu: pierwszy analityk sprawdzał PITR w Railway + tabele-dowody `backup_run_receipts` (puste) + obecność `pg_dump` na maszynie (brak) i z braku tych trzech wyciągnął wniosek o braku backupów — nie sprawdził katalogu `.tmp/`. LEKCJA (do bezpieczników): brak mechanizmu ≠ brak artefaktu; przed twierdzeniem „nie ma X" sprawdź także miejsca poza mechanizmem. CO POZOSTAJE PRAWDĄ: PITR jest wyłączony na wszystkich 6 usługach, zrzuty mają 6 dni (nie obejmują zmian 23-28.08) i leżą wyłącznie lokalnie na maszynie właściciela — więc rekomendacja włączenia PITR na produkcji i zrobienia świeżego zrzutu przed operacjami POZOSTAJE W MOCY. DALSZE SPROSTOWANIA do DEC-165: (a) `trolley.proxy.rlwy.net:28146` NIE jest osieroconym proxy — to usługa `pgvector` środowiska `demo`; (b) środowisko `dev` nie stoi bezczynnie — jego DATABASE_URL wskazuje `thomas:28864`, czyli bazę `Postgres` środowiska STAGING (kolejny zlepek). ★★ ODKRYCIE KRYTYCZNE: **rename usługi `Postgres` ZABIŁBY PRODUKCJĘ** — prywatna domena `postgres.railway.internal` wywodzi się z nazwy usługi, a produkcja ma ją wpisaną NA SZTYWNO (żadna zmienna w 4 środowiskach nie jest referencją `${{ }}`). Ta sama pułapka dla `pgvector`↔demo. Wniosek: nazw usług bazodanowych NIE RUSZAMY, dopóki nie przejdziemy na referencje. ★ DRUGA FAŁSZYWA ZIELEŃ W CI: GitHub vars `STAGING_FRONTEND_URL`/`STAGING_API_HEALTH_URL` wskazują `stage.consultinity.ai` = domenę środowiska DEMO — CI wdraża na staging, a health-check robi na demo; `validate-deploy-target.sh:23` ma w allowliście „staging" wpisane `demo.consultify.ai` i NIE MA `staging.consultify.ai`. ★ TEST NA NAWRÓT ROZJAZDU JEST DZIŚ NIEMOŻLIWY: `release-migration-gate.ts:236` drukuje „host redacted", a linia `RELEASE_MIGRATION_GATE_PASS` (:294) nie zawiera hosta — potrzebna etykieta `DB_TARGET_LABEL` w obu liniach. PLAN 5 ETAPÓW przyjęty (szczegóły w raporcie architekta): E0 rozkrzyżowanie domen Railway (CLI, zero ryzyka), E1 poprawki w repo (bramka + denylist testów + dokumenty), E2 rename TYLKO `Postgres-Rehearsal-…`→`pg-staging` (istnieje wyłącznie w staging, zasięg lokalny, panel), E3 poprawka GitHub vars (PO scaleniu E1, inaczej CI zacznie padać), E4 rozcięcie zlepka staging↔demo (staging na sakura + fingerprint + redeploy, po świeżym zrzucie), E5 ★ WYMAGA OSOBNEJ ZGODY: fingerprint `centerbeam` w produkcji i/lub przejście produkcji na referencje. |
| DEC-2026-08-28-173 | Dzień 31 (Realizacja blok B) SCALONY + STOP na B.7 uznany za NIEZASADNY, licencja wydana | **SUPERVISOR_ACCEPT** | Odbiór warstwy 1 (Opus, własny pomiar): B.1-B.6, B.8-B.10 technicznie dostarczone — 5 komend runtime-v1 na ścieżce produkcyjnej z audytem W TEJ SAMEJ transakcji (materialCommand.ts:498-540), współbieżność potwierdzona własnym przebiegiem 17/17 ([201,409], 1 state, 1 audit, readback niezależnym poolem), capabilities addytywne, as-of działa na ie_audit_events, 5 rodzin KPI liczonych z danych. E-O3/4/5 POTWIERDZONE nietknięte (grep progów w diffie: zero poza INTERVAL '7 days'). Zero migracji, zero zmian w src/**, front i bramka 409 nietknięte. „Brak nowych czerwonych" = PRAWDA (2 zastane w plikach spoza diffu). ★ B.7 STOP NIEZASADNY — trzy dowody: (1) instrukcja dawała imienną licencję na postgresMaterialCommandUnitOfWork „jeśli nowa komenda wymaga nowego agregatu, z wpisem"; (2) bezpośredni precedens w tym samym mechanizmie: registerInitiative.ts:91-172 zapisuje w prepare do tabeli SPOZA rodziny ie_* (initiative_candidates, FOR UPDATE + kontrola wersji); (3) interfejs da się rozszerzyć BEZ dotykania chronionego materialCommand.ts (deklaracja `interface X extends MaterialCommandTransaction` w nowym pliku domenowym). Wykonawca SŁUSZNIE odrzucił alternatywę przez withPgTransaction (trzeci rejestr komend = złamanie AMD-EXE-SPINE-AUTHORITY-004) — błędem było zatrzymanie się, nie kierunek. ★★ DWA P0 PRODUKTOWE (nie blokują scalenia, blokują status „gotowe"): P0-1 pięć komend to ZAPIS BEZ CZYTELNIKA — każdy aggregateType występuje w repo DOKŁADNIE RAZ (w trasie zapisu); użytkownik zapisze pozycję budżetu i nigdzie jej nie zobaczy poza read-back; to znowu wzorzec „backend ma / front nie woła"; P0-2 rozjechany cykl życia budżetu: CREATE→ie_aggregate_state, DELETE/READ→budget_entries (nie da się usunąć tego, co się utworzyło). P1: expectedVersion z.literal(0) czyni komendy create-only (mitygacja RAID zapisywalna raz i nigdy poprawialna); okno KPI liczone po updated_at zamiast po dacie należnej (miara zmienia sens przy każdym UPDATE); SOURCE_NOT_EVENT_SOURCED nieosiągalny → kłamliwa etykieta powodu luki; ★ policy_id TEXT PRIMARY KEY NIE jest tenant-scoped — pierwsza organizacja, która zapisze politykę, zablokuje wszystkie pozostałe. Scalone do m03. |
| DEC-2026-08-28-174 | Dzień 32 (silnik dokumentu DRD) — MECHANIKA ZIELONA, PARYTET WIZUALNY CZERWONY | **SUPERVISOR_ACCEPT (mechanika) + ODRZUCENIE POKAZANIA WŁAŚCICIELOWI** | Odbiór warstwy 1 z audytem parytetu (Opus, wszystkie 19 stron obejrzane vs 29 stron wzorca). MECHANIKA — dowody TWARDSZE niż w raporcie: silnik w ISTNIEJĄCYM documentDocxRenderer (nie generator obok), wszystkie gałęzie za isDrdReportProfile; ★ bitowa niezmienność legacy potwierdzona NIEZALEŻNIE (odbierający zbudował worktree na markerze i porównał SHA word/document.xml, styles.xml, numbering.xml na 3 bogatych schematach — identyczne; snapshot w repo sam z siebie tego NIE dowodził, bo fixture powstał PO commicie); zero LLM, zero LibreOffice w server/src (tylko komentarze), zero zmian w src/**; trasa DOCX z org WYŁĄCZNIE z JWT (query.organizationId nieczytany), 7/7 realdb potwierdzone własnym przebiegiem w tym „obcy sesja nieodróżnialna od nieistniejącej"; dwa tenanty REALNIE różne (org-a oś 1, org-b oś 2 — nie ta sama treść z podmienioną nazwą); rFonts 638→0. ★★ PARYTET WIZUALNY = CZERWONY, właściciel NIE ZOBACZY tego pliku: (1) OKŁADKA DRD NIE ISTNIEJE — profil ustawia tylko coverPage:true i dziedziczy legacy renderCoverBlock; na twarzy dokumentu angielski ciąg „client final report · PL · comprehensive" (fallback documentTypeLabels bez PL), brak 9-wierszowej metryczki wzorca; (2) MATRYCA: DOCX_TABLE_MAX_COLS=8 + clampTableColumns wołany BEZWARUNKOWO (nie bramkowany profilem) → nagłówek „+3 more" (etykieta UI w dokumencie klienta), zjedzone kolumny Luka/Priorytet, nazwy łamane w środku wyrazów; (3) 195 PUNKTÓW RUSZTOWANIA REDAKCYJNEGO wyrenderowanych jako treść („stan faktyczny / ocena i wiarygodność / …" pod każdym z 39 obszarów) — instrukcja dla autora w dokumencie klienta; (4) 18 angielskich podpisów „Figure N"/„Table N" + PODWÓJNE podpisy; (5) radar 144dpi 16:9 domyślnym stylem Chart.js zamiast 300dpi z adnotacjami; (6) stopka dubluje poufność, brak znaku marki; (7) STOP „brak limitów słów" ZASADNY co do faktu, ale rozstrzygalny bez kontraktu v2 — stała 180-260 jest MIERZALNIE ZŁA (wzorzec: streszczenie ~160, wnioski ~410) i trafia też do czterech komórek linii decyzyjnej, gdzie wzorzec ma frazy 10-25 słów. SKALA UCZCIWOŚCI: 3304 słowa, z czego 2028 (61%) to placeholder/rusztowanie — bo w bazie jest 1 oceniony obszar na 39; wzorzec ma 8456 słów realnej prozy. WNIOSEK: nawet po naprawie wizualnej dokument z tych danych będzie ładnym pustym formularzem — potrzebna SESJA Z REALNIE WYPEŁNIONĄ OCENĄ jako dane demo (osobny dyżur). Wycena naprawy wizualnej: 3-4 dni. Scalone do m03 (trasa nowa, nieeksponowana w UI). |
| DEC-2026-08-27-175 | Dzień 35 (Realizacja: B.7 + projekcje odczytu) — ZIELONY Z FIX-AMI, SCALONY; ★ BRAMKA DYŻURU 33 ZDANA | **SUPERVISOR_ACCEPT** | Odbiór warstwy 1 (Opus, własny PG 5648, własne testy zamiast ufania wykonawcy). ★ BRAMKA BEHAWIORALNA DYŻURU 33 WYKONANA I ZDANA: POST polityki dla org A → 201, POST tego samego policyId dla org B → 201, GET /control-kpis jako A → `resolved:true, missingParameters:[]`, jako B → to samo; readback: dwa wiersze, każdy w swojej organizacji, rv=1. DYŻUR 33 DO WYDANIA. Chronione pliki: shasum identyczny z markerem dla materialCommand.ts i executionBudgetService.ts. D.1 migracja PK — strażnik sprawdzony WPROST (odbierający przekuł PK z powrotem, wstawił wiersz, uruchomił → RAISE EXCEPTION „needs an owner decision"), idempotencja 2. przebieg 0 pending. D.2 zapis przez kanoniczną transakcję (jeden z ~41 modułów-sióstr, zero trzeciego rejestru — grep BEGIN/COMMIT w nowych plikach pusty), walidacja WYŁĄCZNIE strukturalna, CAS realny także na INSERT. ★ ODBIERAJĄCY PODNIÓSŁ OCENĘ: trzy pozycje CZĘŚCIOWO (D.4/D.5/D.7) mają DZIAŁAJĄCE zachowanie — braki to pokrycie testowe, nie kod; napisał brakujące testy sam i PRZESZŁY (macierz dwóch org na tym samym aggregateId: każda widzi swój wiersz, cudza inicjatywa → items:[]; odczyt bez capability → 200 + items:[]). DWIE ODMOWY WYKONAWCY ZASADNE: (a) legacy summary budżetu — doliczenie kanonicznych wymagałoby edycji pliku chronionego ALBO przepisania zaszytych progów 80/100 do nowego modułu = drugi SSOT; (b) sourceVersion — `initiative_dependencies` NIE MA kolumny wersji (potwierdzone grepem po wszystkich migracjach), a stała `? 1 : 0` była JUŻ w markerze — wykonawca nic nie sfabrykował ani nie zamaskował. ROZBIEŻNOŚCI ZNALEZIONE PRZEZ ODBIERAJĄCEGO, NIEOBECNE W RAPORCIE: (1) dwa testy day31 padają bez ENABLE_V8_GLOBAL=true (404 zamiast 409) — raport podaje 34/34 nie wymieniając zmiennej, następny dyżur odtworzyłby fałszywą czerwień; (2) raport nazywa kod błędu `NO_EVENT_HISTORY_FOR_RECONSTRUCTED_REPORT`, którego NIE MA w kodzie (realny: `NO_EVENT_HISTORY_BEFORE_AS_OF`) — trafiłoby wprost do kontraktu dyżuru frontowego. FIX-y P1 (testy do repo, korekta kontraktu, przepis env) — wewnętrznie. DŁUG: initiative_dependencies bez wersji i bez daty biznesowej (dwie drogi domknięcia, wymaga decyzji bo dotyka tabeli legacy z danymi); koperta FULL nieudowodniona do czasu wpisania E-O; dwa kontrakty kasowania budżetu; ekran polityki progów nie istnieje (dyżur frontowy z prototypem). P2 DO DECYZJI WŁAŚCICIELA: progi 80/100 w podsumowaniu budżetu — zostają zaszyte czy stają się parametrem polityki. |
