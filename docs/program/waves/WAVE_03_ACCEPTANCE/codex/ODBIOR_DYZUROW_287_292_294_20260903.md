# ODBIÓR ADWERSARYJNY — dyżury 287, 292, 294 (03.09.2026)

Odbierający: agent odbioru C (Opus). Worktree `/private/tmp/ag-odbior-c`, gałąź `agent/odbior-c-20260903`,
linia integracyjna `416432abaf`. Port harnessu 5446. Wszystkie liczby poniżej zmierzone samodzielnie,
nie przepisane z raportów Codexa.

## Werdykty

| Dyżur | Werdykt | Jednym zdaniem |
| --- | --- | --- |
| 287 fokus `c-focus` | **SCALIĆ Z ZASTRZEŻENIEM** | Naprawa realna (193→84 w moim pomiarze), ale gałąź wnosi **czerwony test** i ma **6 konfliktów** z linią integracyjną. |
| 292 macierz akcji Wywiadu | **SCALIĆ Z ZASTRZEŻENIEM** | Kod R2–R4 jest realny i podłączony do istniejących handlerów, ale dyżur jest **niedokończony**: brak R5 (dowód) i R6 (raport), rejestr ma pustą sekcję „Stan PO”. |
| 294 Czat: trzy defekty | **SCALIĆ** | Dwa pliki dokumentacji, zero kodu produktu — i tak jest uczciwie: raport nazywa się pomiarem, dwa twierdzenia właściciela obalone, trzecie zatrzymane brakiem licencji. |

## Kontrola scalenia (`git merge-tree --write-tree` wobec `416432abaf`)

| Gałąź | merge-base | Konflikty | Pliki |
| --- | --- | --- | --- |
| `codex/day287-fokus-c-focus-20260903` | `35afcb15fd` | **6** | `scripts/check-focus-canon.baseline.txt`, `src/components/Execution/RolloutRegisterEditModal.tsx`, `src/components/MyWork/NotificationSettings.tsx`, `src/components/MyWork/NotificationsHub.tsx`, `src/components/MyWork/ProgressView.tsx`, `src/components/assessment/modals/ReportTemplatePickerModal.tsx` |
| `codex/day292-wywiad-menu-akcji-20260903` | `58ef0771d7` | **0** | — |
| `codex/day294-czat-naglowek-galezie-glos-20260903` | `58ef0771d7` | **0** | — |

Uwaga metodyczna: `git merge-tree <base> <our> <their>` (stara forma) wypisał dla 287 sześć wierszy
`changed in both` i **zero** `^++<<<<<<<`. Dopiero `--write-tree --messages` pokazał sześć realnych
`CONFLICT (content)`. Sam licznik markerów `<<<<<<<` na starej formie **nie wykrywa tych konfliktów** —
to pułapka przyrządu, nie własność gałęzi.

---

# DYŻUR 287 — fokus `c-focus` (4 commity, 63 pliki)

## Tabela R1…R6 → raport → commity

| Poz. | Instrukcja wymaga | Raport twierdzi | Commity | Mój pomiar |
| --- | --- | --- | --- | --- |
| R1 | pomiar PRZED + kanoniczna klasa + naprawa `VIOLATION_RE` | 193/94; kanon `focus-visible:…ring-c-focus`; RE nie łapie `crimson-*` | `9cb63d382e` | **POTWIERDZONE** 193 na markerze `35afcb15fd`; kanon istnieje (1730 użyć w 634 plikach) |
| R2 | komponenty współdzielone najpierw | 193→180, 94→86 | w `b91e835511` | Zmienione m.in. `shared/forms/{Select,MultiSelect,DatePicker,PriorityPicker}`, `ui/HelpButton` — TAK |
| R3+R4 | moduły 01–16, commit per moduł (16 commitów) | „pełny diff 193→0", zacommitowano podzbiór | **1 commit**, nie 16 | **ROZBIEŻNOŚĆ formalna** — jeden commit zamiast szesnastu |
| R5 | bezpiecznik na ZERO + test + mutacja | guard `--ci` zielony; baseline cofnięty do 83/35; mutacja RED→GREEN | `b6542f5238` | **CZĘŚCIOWO** — guard zielony, mutacja działa, ale **baseline ≠ 0** i **test jest CZERWONY** |
| R6 | 32 kadry PRZED/PO na 8 ekranach | `NOT_PROVEN` — uczciwie zadeklarowane | `7ab890a580` | **NIEWYKONALNE harnessem** (patrz niżej) |

## Dowody uruchomione samodzielnie

**(1) Pomiar naruszeń — wzorzec z zadania, trzy punkty odniesienia**

```
git grep -nE 'focus(-visible)?:(ring|outline)-(primary|crimson)|ring-offset-primary' <ref> -- src | wc -l
```

| ref | nowy wzorzec | stary wzorzec (`ring-primary-|outline-primary-|ring-offset-primary-`) |
| --- | --- | --- |
| `35afcb15fd` (marker) | 193 | 230 |
| `416432abaf` (linia integracyjna) | 174 | 211 |
| gałąź 287 | **84** | **123** |

Oczekiwane zadaniem 0 — **NIE osiągnięte**. Z 84 trafień jedno to asercja w teście
(`src/components/Organization/redesign/__tests__/OrganizationKnowledgeGraphScreen.test.tsx`),
poza zakresem bramki; stąd bramkowe 83 w 35 plikach. Moja liczba i liczba raportu zgadzają się
po odjęciu tego jednego trafienia.

Spadek jest REALNY, nie efektem zmiany przyrządu: mierzony STARYM wzorcem marker ma 230, gałąź 123
(−107); mierzony NOWYM: 193 → 84 (−109).

**(2) Bramka i mutacja**

```
bash scripts/check-focus-canon.sh --ci
  Naruszenia (crimson jako fokus):  35 plikow, 83 wystapien
  check-focus-canon --ci: OK (dlug nie rosnie w zadnym pliku, baseline 35 plikow / 83 wystapien)
rc=0
```

Mutacja (dodane `focus:ring-primary-500` w `src/components/ui/HelpButton.tsx`, plik z baseline 0):

```
check-focus-canon --ci: src/components/ui/HelpButton.tsx — NOWE naruszenie crimson-fokusa (1, baseline 0).
rc_mutacja=1
```

Po cofnięciu: `git diff --stat` pusty. **Bezpiecznik broni** — ratchet per plik działa.

**(3) ★ Zacommitowany test jest CZERWONY**

```
npx vitest run tests/unit/canon/focusCanonZero.test.ts --retry=0
 ❯ tests/unit/canon/focusCanonZero.test.ts:18:22
     18|     expect(baseline).toMatch(/RAZEM: 0 wystapien w 0 plikach/);
 Test Files  1 failed (1)
      Tests  1 failed (1)
```

Plik baseline na gałęzi ma w linii 12: `# RAZEM: 83 wystapien w 35 plikach`.
Test asertuje zero, baseline mówi 83 → test failuje na KAŻDYM przebiegu tej gałęzi.
**To jest blokada scalenia:** gałąź wnosi do drzewa test, który nie może przejść.

**(4) Zakres diffu — kontrola ZAKAZU z instrukcji (Z40: nic poza klasami fokusu)**

Przejrzałem diff 10 plików wybranych mechanicznie (co 6-ty z posortowanej listy 58 plików `src/`):
`DecisionInbox`, `PriorityCard`, `ReportDocumentView`, `KnowledgeLibrary`, `StatusTransitionDropdown`,
`ContentCategoriesManager`, `SubscriptionManager`, `DatePicker`, `InitiativeManagementView`,
`PromptTestBench`. **Zero zmian tła/tekstu/obramowania.** Wzorzec każdej zmiany:

```
- focus:outline-none focus:ring-2 focus:ring-primary-500
+ focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-c-focus
```

Efekt uboczny w granicach licencji, ale warty odnotowania: znika modyfikator przezroczystości
(`/20`, `/40`, `/50`) — pierścień dostaje pełną wartość tokenu `--c-focus`; oraz `focus:` → `focus-visible:`,
czyli pierścień przestaje się pokazywać po kliknięciu myszą. Instrukcja R3 kazała to zapisywać
w rejestrze przy elementach polegających na `focus:` dla myszy — **raport takiego zapisu nie zawiera**.

**(5) `ring-c-focus` to realna klasa, nie martwy napis** — `tailwind.config.js:94 focus: cTok('focus')`,
`src/index.css:74 --c-focus: rgba(37, 99, 235, 0.4)` (light), `:307` `rgba(91, 141, 239, 0.45)` (dark).

**(6) Dowód wizualny — pomiar koloru pierścienia w przeglądarce**

Harness na 5446 z gałęzi 287, 2 ekrany × 2 motywy × 8 przystanków Tab, odczyt `--tw-ring-color`
i `outline-color` z `document.activeElement`:

```
day238-ustawienia / light: ring=rgba(37, 99, 235, 0.4)  outline=solid 2px rgb(37, 99, 235)
day238-ustawienia / dark : ring=rgba(91, 141, 239, 0.45) outline=solid 2px rgb(91, 141, 239)
```

**Zero crimsonu na 16 zmierzonych przystankach fokusu.** Zrzuty kanonicznym narzędziem
(`--klawisze=Tab,Tab --a11y=1`) w `evidence/grafika/odbior-c-287/` — 4/4 OK, 0 błędów konsoli;
obejrzane wzrokiem: pierścień na „Profil” jest NIEBIESKI.

**(7) Bramki**: esbuild **58/58 plików OK**; `check-list-canon` rc=0 (369 = baseline 369);
`check-artefakt` rc=0 (8 wobec baseline 9 — dług SPADŁ); `initiativeRecordCanon` 2/2 PASS.

## Rozbieżności z cytatem — 287

**R-287-1.** Raport, sekcja „Wynik R2–R5”: *„Test: `focus canon zero guard keeps the baseline and
tracked src debt at zero` — GREEN na pełnym diffie roboczym."* — Zdanie jest prawdziwe i bezużyteczne:
opisuje drzewo, którego w gałęzi nie ma. **Na zacommitowanym HEAD ten test jest CZERWONY** (zmierzone:
`1 failed`). Raport nigdzie nie mówi wprost „gałąź, którą oddaję, ma failujący test”. To przemilczenie,
nie kłamstwo — ale nadzorca scalający po samym raporcie wpuściłby czerwień do linii integracyjnej.

**R-287-2.** Instrukcja R1: *„jeśli nie łapie `focus-visible:` albo `crimson-*`, **rozszerz** i zmierz
ponownie"*. Codex zmienił wzorzec z `ring-primary-|outline-primary-|ring-offset-primary-` na
`focus(-visible)?:(ring|outline)-(primary|crimson)-|ring-offset-(primary|crimson)-`. To jednocześnie
rozszerzenie (crimson) **i zwężenie** — nowy wzorzec wymaga prefiksu `focus:`/`focus-visible:`.
Zmierzyłem skutek: **39 wystąpień `ring-primary-*` wypadło spod strażnika** bez naprawy
(np. `ChatHistorySidebar.tsx:298 ring-1 ring-primary-400/50`, `CanvasVersionHistory.tsx:102`).
Merytorycznie to obrona zasadna — to pierścienie STANU zaznaczenia, nie fokusu, a kanon dotyczy fokusu —
ale jest to **redukcja zakresu bramki wykonana przy okazji i nienazwana liczbą w raporcie**.
Nadzorca musi to zatwierdzić świadomie, bo od teraz `ring-primary-*` poza fokusem nie jest pilnowane.

**R-287-3.** Instrukcja R3/R4: *„Commit po każdym module (osiem commitów)"* × 2 = 16 commitów.
Gałąź ma 4 commity, cała mechanika w jednym (`b91e835511`). Nie zmienia to jakości kodu, ale odbiera
możliwość cofnięcia pojedynczego modułu.

## Warunki scalenia — 287

1. **Rozwiązać 6 konfliktów** (pięć plików to równoległe naprawy G14 tej samej rodziny — rozstrzygnięcie
   powinno być mechaniczne; szósty to `check-focus-canon.baseline.txt`, który po scaleniu trzeba
   **wygenerować od nowa**, nie skleić ręcznie).
2. **Naprawić czerwony test.** Dwie uczciwe drogi: albo `focusCanonZero.test.ts` przestaje asertować
   zero i asertuje „baseline nie rośnie” (wtedy nazwa testu musi się zmienić), albo test zostaje
   dopiero razem z domknięciem długu do zera. **Nie scalać w obecnej postaci.**
3. Świadoma decyzja nadzorcy o zwężeniu `VIOLATION_RE` (39 wystąpień poza bramką).

## Zdanie do rejestru `D3` (crimson fokus)

> `D3` — **CZĘŚCIOWO ZAMKNIĘTE, POMIAR WŁASNY 03.09**: dyżur 287 zdjął 109 z 193 wystąpień crimson-fokusa
> (marker `35afcb15fd` 193 → gałąź 84; starym wzorcem 230 → 123, więc spadek nie pochodzi ze zmiany
> przyrządu). Bramka `check-focus-canon --ci` zielona przy baseline **83/35, nie 0**; mutacja
> `focus:ring-primary-500` daje rc=1, więc bezpiecznik realnie broni. Pozostałe 35 plików zablokował
> `check-triada` (zastane klasy crimson w tej samej linii przy zakazie Z40). Dowód wizualny: 16 przystanków
> Tab na 2 ekranach × 2 motywy — pierścień niebieski `rgba(37,99,235,0.4)` / `rgba(91,141,239,0.45)`,
> zero crimsonu. **Gałąź NIE nadaje się do scalenia bez poprawki**: wnosi czerwony test
> `tests/unit/canon/focusCanonZero.test.ts` i ma 6 konfliktów z `416432abaf`.

---

# DYŻUR 292 — macierz akcji Wywiadu (2 commity, 9 plików, BRAK raportu)

## Czy to dyżur niedokończony — TAK, i tak trzeba go nazwać

Instrukcja przewiduje commit po R1, R2, R3, R4, R5 i raport w R6 — sześć pozycji.
Gałąź ma **2 commity**. Rejestr `REJESTR_MENU_AKCJI_WYWIAD_20260903.md` kończy się dosłownie zdaniem:

> „## Stan PO — Do uzupełnienia po R2–R5 wraz z commitami, dowodem handlerów i zrzutami."

Sekcja nigdy nie została uzupełniona. Pliku `CODEX_DAY292_*_REPORT.md` nie ma w repo (sprawdzone:
`git show <gałąź>:…` → `does not exist`). **To dyżur zatrzymany po R2–R4 bez zamknięcia** —
prawo zatrzymania z instrukcji („R1–R3 wykonane z dowodem, R4 niewykonane z powodu X jest
pełnowartościowym wynikiem”) wymaga JAWNEGO powiedzenia tego. Nie powiedziano.

| Poz. | Wymaga | Stan | Dowód |
| --- | --- | --- | --- |
| R1 | macierz 6 typów × akcja × miejsce × trasa × i18n | **WYKONANE** | `aa8fdcc8bd`, rejestr 30 wierszy + 5 wierszy POMINIĘTE z powodem |
| R2 | jedna definicja macierzy dla kebaba i `PreviewActionBar` | **WYKONANE** | `interviewActionMatrix.ts` (320 linii), konsumowane w 5 plikach |
| R3 | akcje Sesje + Szablony w obu miejscach, i18n, test | **KOD TAK, zrzuty NIE** | `additionalActions` w `InterviewSessionPreview`, `InterviewTemplatePreview` |
| R4 | Skrzynka + Wnioski + Inicjatywy Wywiadu | **KOD TAK (5 z 6 typów)**, Inicjatywa świadomie pominięta | 4 miejsca wywołania `additionalActions` + osobne propsy Wniosku |
| R5 | zrzuty 6 typów × light/dark × pl 1440 / en 1024, a11y, wpisy w `g06-macierz-ekrany.json` | **NIEWYKONANE** | brak zrzutów, `g06-macierz-ekrany.json` nietknięty w diffie |
| R6 | raport, liście i18n PRZED/PO, zdanie do `INT-MENU-OWN-001` | **NIEWYKONANE** | brak pliku raportu |

## Dowody uruchomione samodzielnie

**(1) Czy akcje mają realne handlery, czy to atrapy — ATRAP NIE MA.**

Konstrukcja broni przed atrapą strukturalnie: przycisk powstaje **tylko** gdy przekazano handler
(`...(onFork ? [{ … onClick: onFork }] : [])`), a macierz świadomie nie trzyma callbacków
(komentarz w `interviewActionMatrix.ts`: *„The matrix deliberately stores no callbacks: callers bind
the existing, permission-aware handler”*). Sprawdziłem wiązania w `InterviewHub.tsx` — wszystkie
prowadzą do istniejących handlerów, m.in.:

```
onClick: () => handleApproveAssignment(linkedAssignment)
onClick: () => handleOpenSendBackModal(linkedAssignment)
onClick: () => handleSessionLifecycleAction(s, 'restore' | 'trash' | 'untrash' | 'archive')
onFork={() => handleForkInsight(item.id)}
onDelete={() => handleDeleteInsight(item.id)}
onClick: () => startInterviewAssignment(a)
onClick: () => void handleDelayAssignment(a, days)
```

`additionalActions` jest faktycznie przekazywane w **4 miejscach** `InterviewHub.tsx`
(linie 7132 Sesja, 8233 Szablon, 8810 Skrzynka, 8995 Przydział) — to nie jest sama zmiana etykiet.
Dla Przydziału zestaw renderowany (`reassign, remind, escalate, edit, archive, restore` + istniejące
`start/continue/fix/approve/send-back` + delay) **pokrywa się 1:1** z 12 akcjami deklarowanymi
w macierzy. Inicjatywa Wywiadu nie dostała `additionalActions` — zgodnie z tabelą POMINIĘTE
(„brak handlerów/backendu; nie dodajemy martwych przycisków”).

**(2) Liście i18n pl/en — pełne.** Wyciągnąłem 33 unikalne klucze z `interviewActionMatrix.ts`
i sprawdziłem oba pliki tłumaczeń: **33/33 obecne w pl i en, 0 braków, 0 przypadków `pl === en`**
(czyli żaden klucz nie trzyma angielskiego słowa po polsku — kształt „klucz istnieje ≠ przetłumaczony”
nie występuje). Dlatego brak zmian w `public/locales/**` jest tu POPRAWNY, nie luką.

**(3) Bramki.** esbuild 7/7 plików OK. `check-list-canon` rc=0 (368 = baseline 368).
`check-artefakt` rc=0 (9 = baseline 9). `initiativeRecordCanon` **6/6 PASS** (wymóg R4 wobec
`DEC-2026-09-03-346`). Test kontraktowy `interviewActionMatrix.contract.test.tsx` **4/4 PASS**.

**(4) Ocena wartości testu kontraktowego — słabszy niż wygląda.**
Test ma cztery przypadki: (a) 6 typów obecnych, (b) `operation` niepuste i `surfaces` = `['row','preview']`,
(c) klucze i18n realne w pl/en, (d) **`fs.readFileSync(file).toContain('interviewActionMeta')`** —
czyli grep po napisie w pięciu plikach. Przypadek (d) przejdzie także wtedy, gdy import zostanie,
a wszystkie przyciski zostaną wyrzucone. Test **nie renderuje żadnego komponentu** i nie sprawdza,
czy `additionalActions` w ogóle trafia do `PreviewActionBar`. Broni słownika, nie produktu.

**(5) Zrzuty — czego harness NIE udowodni.** Zrobiłem `karta-interview`, `karta-insight`,
`interview-preview-canon` (6/6 OK, 0 błędów konsoli, `evidence/grafika/odbior-c-292/`).
Podgląd sesji na zrzucie pokazuje **wyłącznie „Generuj wnioski AI” + kebab** — bo ekrany harnessu
montują `Interview*PreviewFooter` bezpośrednio i **nie przekazują `additionalActions`**.
Nowe akcje wstrzykuje `InterviewHub`, którego harness nie renderuje. To znany kształt
„przyrząd nie jest produktem”: **zrzut z harnessu nie może potwierdzić ani obalić zmiany 292**.
Dowód R5 wymaga rozszerzenia fikstury harnessu albo pełnego runtime — i tego brakuje po obu stronach.

## Rozbieżności z cytatem — 292

**R-292-1.** Rejestr, ostatnia linia: *„## Stan PO — Do uzupełnienia po R2–R5 wraz z commitami, dowodem
handlerów i zrzutami."* Kod R2–R4 powstał, a sekcja została pusta. Dyżur oddany bez zamknięcia
własnego dokumentu i bez raportu R6 — nie użyto też prawa zatrzymania, które instrukcja daje wprost.

**R-292-2.** Instrukcja R5 wymaga dołożenia brakujących typów do `g06-macierz-ekrany.json`
„w kształcie sąsiadów”. Plik nie występuje w diffie — macierz G06 nadal nie widzi tych powierzchni.

## Zdanie do rejestru `INT-MENU-OWN-001`

> `INT-MENU-OWN-001` — **MECHANIKA WYKONANA, ODBIÓR NIEDOMKNIĘTY (pomiar własny 03.09)**: powstał wspólny
> słownik `src/components/Interview/interviewActionMatrix.ts` (6 typów, 33 klucze i18n, 33/33 obecne
> w pl i en, zero `pl===en`), konsumowany przez kebab wiersza w `InterviewHub.tsx` i przez paski
> podglądu 5 z 6 typów (Inicjatywa Wywiadu świadomie POMINIĘTA — brak backendu, bez martwych przycisków).
> **Atrap nie ma**: przycisk powstaje wyłącznie przy przekazanym handlerze, a wszystkie sprawdzone
> wiązania prowadzą do istniejących handlerów Huba. Dla Przydziału zestaw renderowany pokrywa się 1:1
> z 12 akcjami macierzy. Bramki: esbuild 7/7, `check-list-canon` 368=368, `check-artefakt` 9=9,
> `initiativeRecordCanon` 6/6, kontrakt 4/4. **Otwarte**: R5 (zero zrzutów, `g06-macierz-ekrany.json`
> nietknięty) i R6 (brak raportu, „Stan PO” pusty). Dowód wizualny jest NIEOSIĄGALNY obecnym harnessem —
> ekrany `interview-*` montują stopki podglądu bez `additionalActions`, więc nie pokazują zmiany.
> Scalenie czyste (0 konfliktów), ale wiersz **nie może być zamknięty** przed R5/R6.

---

# DYŻUR 294 — Czat: nagłówek, gałęzie, głos (2 commity, 2 pliki)

## „2 pliki na trzy defekty” — czy to pomiar bez naprawy i czy uczciwie nazwane

**TAK, to jest sam pomiar — i TAK, jest nazwany uczciwie.** Oba pliki to dokumentacja
(`REJESTR_CZAT_TRZY_DEFEKTY_20260903.md`, `CODEX_DAY294_CZAT_TRZY_DEFEKTY_REPORT.md`);
**zero linii kodu produktu**. Raport nie udaje naprawy — jego pierwsze zdanie brzmi
`PARTIAL / DWA TWIERDZENIA OBALONE / JEDEN STOP MERYTORYCZNY`, R2 mówi wprost
*„Nie wykonano zmiany: mierzona wysokość była już stała"*, R5 deklaruje
*„Stan: NIE ZACOMMITOWANO kodu produktu"*, a sekcja „TWIERDZENIA NIEZWERYFIKOWANE” wymienia
trzy rzeczy, których nie sprawdzono. To jest wzorcowe zachowanie wobec kształtu „fałszywe gotowe”.

| Poz. | Wymaga | Stan | Mój pomiar |
| --- | --- | --- | --- |
| R1 | pomiar trzech defektów | WYKONANE, trzy tezy nadzorcy obalone | 2 z 3 potwierdzam, 1 prostuję (patrz R-294-1) |
| R2 | nagłówek — równe wysokości | BEZ ZMIANY, bo już równe | **POTWIERDZONE własnym pomiarem runtime** |
| R3 | zapis rozmowy — czy `localStorage` jest źródłem prawdy | Obalone: serwer jest źródłem | **POTWIERDZONE kodem** |
| R4 | gałęzie: podłączone albo usunięte | Obalone: podłączone, działają | **POTWIERDZONE kodem** |
| R5 | głos: jeden kontrakt | **STOP MERYTORYCZNY** (brak licencji w instrukcji) | STOP zasadny; diagnoza **niepełna** |
| R6 | raport | WYKONANE | — |

## Dowody uruchomione samodzielnie

**(1) Nagłówek — wysokość w 5 punktach czasu, realna przeglądarka.**
Playwright 1440×900 na harnessie 5446, ekran `chat-split-teresa-right`, selekcja po klasie `h-[42px]`:

```
t=0ms     naglowkow=0  []
t=500ms   naglowkow=0  []
t=1500ms  naglowkow=0  []
t=3000ms  naglowkow=2  [42,42]
t=5000ms  naglowkow=2  [42,42]
```

**Dwa nagłówki, oba 42 px, równe.** Zero rozjazdu. Kod potwierdza:
`UnifiedChatPanel.tsx:6718 flex h-[42px] flex-nowrap …` oraz
`WorkCanvasDocumentPanel.tsx:3410 flex h-[42px] shrink-0 flex-nowrap …`.
Uwaga: przed 3 s panele nie są jeszcze zamontowane — „0 nagłówków” to brak elementu, nie wysokość 0.
Prośba właściciela z `CHAT-OWN-002` („aby te wysokości były równe”) jest **spełniona na linii
integracyjnej**, więc brak diffu jest tu poprawnym wynikiem, nie zaniechaniem.

**(2) Zapis rozmowy — `localStorage` NIE jest źródłem prawdy.**
`git grep -n localStorage -- src/components/AIChat | wc -l` → **76** (raport: 76 — zgodne;
teza nadzorcy „kilkanaście” była błędna). Trasa serwerowa istnieje:
`server/src/routes/conversations.routes.ts:16 POST /api/conversations/:id/messages`.

**(3) Gałęzie — podłączone, mechanika serwerowa realna.**
`server/src/routes/conversations.routes.ts`: tabela `conversation_branches` (migracja 672),
`INSERT INTO conversation_branches` (:2808), `GET /api/conversations/:id/branches` (:3012).
Po stronie UI komponent **jest renderowany**, nie tylko importowany:
`UnifiedChatPanel.tsx:114` import, **`:6766 <BranchSelector`** — czyli nie zachodzi kształt
„wołacz istnieje ≠ renderuje się”. Rekomendacja raportu „nie usuwać UI” jest uzasadniona.

**(4) Głos — rozproszenie SZERSZE niż w raporcie.** Mój pomiar:

```
git grep -nE "new [A-Za-z]*(SpeechRecognition|webkitSpeechRecognition)" -- src   (bez testów)
src/components/AIChat/EnhancedChatInput.tsx:524
src/components/Interview/InterviewSingleQuestionRuntime.tsx:850
src/components/MyWork/mindmap/VoiceToNode.tsx:41
src/components/MyWork/notebook/AIChatInlinePanel.tsx:147
src/components/MyWork/table/VoiceImageInput.tsx:76
src/hooks/useSpeechToText.ts:339
src/hooks/useUniversalVoice.ts:348
```

**Siedem niezależnych konstruktorów w siedmiu plikach**, nie cztery.
`src/hooks/useTeresaVoice.ts` **nie jest** wspólnym kontraktem dyktowania — importuje
`GoogleGenAI` (Gemini Live), a nie Web Speech; `EnhancedChatInput.tsx` (wejście samego Czatu)
**nie używa** `useTeresaVoice` ani `TeresaVoiceContext` i buduje własny automat.
Istnieją też dwa dalsze hooki poza radarem raportu — `useSpeechToText.ts` i `useUniversalVoice.ts`
(konsumowane przez `UnifiedChatPanel`, `layout/ChatPanel`, `method-workspace/VoiceAnswerChannel`).

## Rozbieżności z cytatem — 294

**R-294-1.** Raport, „Zdania dla nadzorcy”: *„`CHAT-OWN-015`: … **Czat ma wspólny kontrakt**, trzy
wejścia Mojej Pracy nie"*. **Mój pomiar temu przeczy.** Czat wspólny kontrakt ma tylko dla
Gemini Live / TTS; dyktowanie w polu wejściowym Czatu (`EnhancedChatInput.tsx:524`) to **własny,
czwarty** `new SpeechRecognition()`, poza `useTeresaVoiceContext`. Sam raport w R1 wymienia
`EnhancedChatInput` wśród ścieżek wykonawczych — czyli **zdanie podsumowujące jest ostrzejsze
niż własny pomiar autora**. Kierunek rekomendacji (imienna licencja na refaktor) zostaje słuszny,
ale **promień rażenia jest większy**: 7 plików + 3 hooki, nie 3 miejsca Mojej Pracy.

**R-294-2.** Raport, R1: *„trzy niezależne wejścia Web Speech w Mojej Pracy"* — pominięto
`src/components/Interview/InterviewSingleQuestionRuntime.tsx:850`, czyli **piąte miejsce poza Moją
Pracą** (moduł Wywiad). Rekomendowana tabela licencji dla przyszłego dyżuru musi je objąć,
inaczej powtórzy się kształt „poprawne w dwóch z trzech”.

**R-294-3 (nie zarzut, ostrzeżenie proceduralne).** Raport: *„Tip bazowy uciekł do `984d3658fd`;
pracę wykonano zgodnie z instrukcją dokładnie z markera."* Ta sama decyzja co w 287 —
i w 287 kosztowała 6 konfliktów. Tu nie kosztowała nic, bo dyżur nie tknął kodu.
Reguła „start dokładnie z markera, bez rebase” jest bezpieczna dla pomiarów i kosztowna dla dużych
naprawek mechanicznych; przy kolejnym dyżurze rodzinowym warto marker odświeżyć.

## Zdania do rejestrów `CHAT-OWN-*`

> `CHAT-OWN-002` — **ZAMKNIĘTE, DEFEKT NIE ISTNIEJE (weryfikacja niezależna 03.09)**: nagłówki
> `UnifiedChatPanel` i `WorkCanvasDocumentPanel` mają twarde `h-[42px] flex-nowrap`; pomiar w realnej
> przeglądarce 1440 px w pięciu punktach czasu (0/500/1500/3000/5000 ms) daje `[42,42]` w obu punktach,
> w których panele są zamontowane. Zapis rozmowy idzie przez `POST /api/conversations/:id/messages`;
> 76 trafień `localStorage` w `src/components/AIChat` to token auth, preferencje i cache — **żadne
> nie jest magazynem wiadomości**. Teza nadzorcy „kilkanaście trafień” błędna. Brak diffu jest tu
> poprawnym wynikiem.

> `CHAT-OWN-003` — **ZAMKNIĘTE, PODŁĄCZONE POPRAWNIE (weryfikacja niezależna 03.09)**: tabela
> `conversation_branches` (migracja 672), `INSERT` w `conversations.routes.ts:2808`,
> `GET /api/conversations/:id/branches` (:3012), a `<BranchSelector` jest **renderowany**
> w `UnifiedChatPanel.tsx:6766` (nie tylko importowany). **UI nie usuwać.**

> `CHAT-OWN-015` — **OTWARTE, STOP MERYTORYCZNY ZASADNY, DIAGNOZA POSZERZONA**: wspólnego kontraktu
> dyktowania nie ma i **nie ma go także w samym Czacie** — `EnhancedChatInput.tsx:524` tworzy własny
> `new SpeechRecognition()` poza `useTeresaVoiceContext` (`useTeresaVoice.ts` to Gemini Live, nie
> Web Speech). Pomiar własny: **7 niezależnych konstruktorów w 7 plikach** — Czat, Wywiad
> (`InterviewSingleQuestionRuntime.tsx:850`), trzy miejsca Mojej Pracy oraz hooki `useSpeechToText.ts`
> i `useUniversalVoice.ts`. Licencja dla następnego dyżuru musi wymienić **wszystkie siedem plików
> plus trzy hooki**, inaczej powstanie „poprawne w dwóch z trzech”.

---

## Załączniki (w tym worktree, nie w repo)

- `evidence/grafika/odbior-c-287/` — 4 PNG (`day238-ustawienia`, `ustawienia-grupy`, light+dark, `--klawisze=Tab,Tab --a11y=1`)
- `evidence/grafika/odbior-c-292/` — 6 PNG (`karta-interview`, `karta-insight`, `interview-preview-canon`, light+dark)

Harness ubity po pomiarach (tylko własny PID na porcie 5446). Skrypty pomocnicze usunięte.
Żadnej gałęzi nie scalono i nie wypchnięto.
