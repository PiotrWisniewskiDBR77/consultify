# ODBIÓR ADWERSARYJNY — dyżury 295, 297, 298 — 2026-09-03

Odbiorca: sesja Opus „odbiór D”. Worktree `/private/tmp/ag-odbior-d`, harness vite `5448`,
Postgres własny `6313` (kontener `ag-odbior-d-pg`, usuwany po pracy). Linia odniesienia:
`/private/tmp/m03` tip `416432abaf`.

Zasada tego dokumentu: **każda liczba poniżej pochodzi z komendy, którą uruchomiłem sam.**
Tam, gdzie cytuję raport wykonawcy bez własnego pomiaru, piszę to wprost.

---

## WERDYKTY

| Dyżur | Werdykt | Jednym zdaniem |
|---|---|---|
| **295** | **SCALIĆ Z ZASTRZEŻENIEM** | Kod jest mały, zielony i kanoniczny; dwa nagłówkowe twierdzenia (dowód efektu kontrolek, wyścig 409 na realnej trasie) **nie bronią się** — nie zamykać `MYW-IDEAS-013` ani `MYW-PHOTO-010`. |
| **297** | **ODRZUCIĆ — NIEWYKONANY** | Jeden commit = sam raport STOP; `scripts/dev/reachability-from-root.mjs` nie istnieje. STOP był licencjonowany (§0.5, <5 GB) i uczciwy, ale R1–R6 to zero. |
| **298** | **SCALIĆ Z ZASTRZEŻENIEM** | Commity są **realne** (obalam obawę o profil read-only); wszystko, co zacommitowane, potwierdziłem własnym przebiegiem — ale silnik **nie ma ani jednego wołacza produkcyjnego**, a narrator za flagą OFF nie powstał. |

## KONTROLA SCALENIA (`merge-tree` vs `m03@416432abaf`)

| Gałąź | `<<<<<<<` / `CONFLICT` / `changed in both` | Pliki |
|---|---|---|
| `codex/day295-mojapraca-inicjatywy-dowody-20260903` | **2** (`changed in both`), znaczników `<<<<<<<` **0** | `public/locales/en/translation.json`, `public/locales/pl/translation.json` — git scala je automatycznie |
| `codex/day297-martwe-komponenty-od-korzenia-20260903` | **0** | — |
| `codex/day298-silnik-raportu-oceny-drd-20260903` | **0** | — |

> Uwaga metodyczna: pierwsze wywołanie `grep -cE` na wyniku `merge-tree` zwróciło w tej powłoce
> **pustkę zamiast `0`** przy pliku 9 398-liniowym. Przeliczyłem wszystko Pythonem. Pustka nie
> jest wynikiem, dopóki nie sprawdzisz, że polecenie się wykonało.

---

# DYŻUR 295 — Moja Praca + Inicjatywy

Marker `58ef0771d7` (potwierdzony), 6 commitów, 44 pliki (34 PNG + 10 plików kodu/dokumentów).

## Tabela R1…R6 → raport → commity

| Poz. | Czego żądała instrukcja | Co twierdzi raport | Co zmierzyłem SAM | Werdykt pozycji |
|---|---|---|---|---|
| **R1** | mianownik kontrolek, trasa 409, stan UI, lista czekowania B na REALNYM rekordzie | rejestr `REJESTR_MOJAPRACA_INICJATYWY_DOWODY_20260903.md` (46 linii, tabela punktowa) | rejestr istnieje, tabela ma mianowniki i plik:linia | **WYKONANE** |
| **R2** | dla **każdej** kontrolki dowód efektu **albo** wpis do tabeli MARTWE z plik:linia | „4/4 zielone; Whiteboard 53, Mind map 65, Process flow 81, Table 27; `MARTWE`: brak” | 4/4 PASS u mnie, mianowniki odtworzone **co do hasha**; ale efekt dowiedziony dla **12 z 226** sygnatur; tabela MARTWE pusta z braku pomiaru, nie z braku martwych; **czwarty ekran to nie to narzędzie** | **NIE BRONI SIĘ** |
| **R3** | wyścig dwóch klientów na realnej bazie + pełna ścieżka UI | „RealPG 3/3 PASS, `--retry=0`, Postgres 6299” | test wyścigu **nie jest wytworem 295** (ostatni commit pliku: `45753ed3bb integration(m02)`) i biega po trasie, o której własny nagłówek pliku mówi, że **nie ma frontendowego wołacza**; UI zrobione dobrze i broni się mutacją | **CZĘŚCIOWO — dowód na złej trasie** |
| **R4** | 4 narzędzia × 8 kadrów + `Tab*5`, a11y 0 | „32/32 kadry, axe 0” | **34 PNG w repo** (32 Idei + 2 Inicjatyw); 16 par light/dark **realnie różnych**; 4 pary pl/en **realnie różne** | **WYKONANE** (z zastrzeżeniem o manifestach) |
| **R5** | naprawy DROBNE + **PRZED/PO 8 kadrów** + `initiativeRecordCanon` 6/6 | „drobna naprawa, dwa kadry PO, brak PRZED” | `initiativeRecordCanon` **6/6 PASS** (mój przebieg); w repo **2 kadry PO** z wymaganych 8, **zero PRZED** | **CZĘŚCIOWO** |
| **R6** | raport z mianownikami i zdaniami do rejestrów | `COMPLETE_WITH_LIMITS`, sekcja „Granice dowodu” | granice wypisane uczciwie, ale nagłówek raportu jest mocniejszy niż treść | **WYKONANE** |

## Trzy dowody uruchomione przeze mnie + mutacje

**Dowód 1 — enumeracja kontrolek (własny harness na 5448).**
```
DAY295_IDEA_HARNESS_URL=http://127.0.0.1:5448 npx vitest run \
  src/components/MyWork/__tests__/ideaTools.controlEnumeration.test.tsx
→ Test Files 1 passed | Tests 4 passed | 1 skipped
→ DAY295_CONTROL_INVENTORY processflow-canvas 81 dfdd6efae65f...
→ DAY295_CONTROL_INVENTORY idea-table       27 3864b4540d73...
```
Mianowniki 53 / 65 / 81 / 27 i hashe kontraktu odtworzone **co do znaku**. Liczba jest prawdziwa.

**MUTACJA B — martwy handler istniejącej kontrolki.** W `IdeaTableTool.tsx:2311` zamieniłem
`onClick: () => setShowConnectorWizard(true)` na pusty `onClick: () => {}` (etykieta bez zmian):
```
→ Tests 1 passed | 4 skipped        (hash 3864b4540d73... bez zmian)
```
**Test pozostał ZIELONY.** Kontrolka przestała cokolwiek robić i bezpiecznik tego nie zauważył.
Wniosek: to jest **inwentarz sygnatur DOM + drut ostrzegawczy na hashu**, a nie dowód efektu.
Efekt dowiedziony jest wyłącznie dla wyzwalaczy menu — `expect(openedMenus).toBe(expected.menus)`,
czyli **2+2+3+5 = 12 kontrolek na 226 sygnatur (5,3%)**. Instrukcja żądała dowodu **albo** wpisu do
tabeli MARTWE dla każdej; zdanie raportu „`MARTWE`: brak w dowiedzionym zakresie interakcji menu”
jest formalnie prawdziwe i praktycznie puste.

**MUTACJA A — nowa martwa kontrolka.** Dodałem do menu narzędzia Tabela pozycję
`MUTACJA A martwa kontrolka` z `onClick: () => {}`:
```
→ Tests 1 passed        (hash bez zmian, unique nadal 27)
```
Też zielony — i to naprowadziło na **defekt cięższy niż sam test**.

### ★ ROZBIEŻNOŚĆ 1 — czwarty mianownik mierzy nie to narzędzie

Instrukcja nazywa cztery narzędzia Idei imiennie, w tym `IdeaTableTool.tsx`. Test enumeruje ekran
harnessu `idea-table`. Moja sonda Playwright na tym ekranie:

```
MUTACJA A w DOM: false
kontrolek w DOM: 21
["Zaznacz widoczne","Tytuł","Etap","Filter Etap","Tagi","Filter Tagi","Narzędzie",
 "Filter Narzędzie","Data↓","Ustawienia widoku","Oznacz gwiazdką","Akcje wiersza", …]
```
To jest **lista Idei** (`IdeasTableContent` — nagłówek `dev-render/screens/idea-table.tsx`:
„Renders the REAL `<IdeasTableContent>`”), a nie narzędzie Tabela. Realne narzędzie montuje
`IdeaMapWorkspace` z `initialTool="table"` — ekran `idea-table-timeline-stuck.tsx:194`.
Zmierzyłem oba tym samym filtrem widoczności:

| Ekran | co montuje | kontrolek widocznych | unikalnych (przejście bazowe) |
|---|---|---|---|
| `idea-table` (zmierzony przez 295) | `IdeasTableContent` — **lista** | 21 | 14 |
| `idea-table-timeline-stuck` (**realne narzędzie Tabela**) | `IdeaMapWorkspace initialTool="table"` → `IdeaTableTool.tsx` (5 325 linii) | **86** | **54** |
| `whiteboard-canvas` (kontrola) | `IdeaMapWorkspace initialTool="whiteboard"` | 45 | 44 |

Czyli mianownik czwartego narzędzia jest **zaniżony ~2×**, a plik `IdeaTableTool.tsx` — jedyny
z czterech wymieniony w instrukcji z nazwy — **nie został zinwentaryzowany ani razu**.
Pozostałe trzy ekrany (`whiteboard-canvas`, `mindmap-canvas`, `processflow-canvas`) montują
`IdeaMapWorkspace` w odpowiednich gałęziach i są zmierzone poprawnie.

**Dowód 2 — ścieżka konfliktu w UI.**
```
npx vitest run tests/components/MyWork/NotebookContent.ux-acceptance.test.tsx \
  src/components/MyWork/panel/__tests__/IdeaElementInspector.behavior.test.tsx
→ Test Files 2 passed | Tests 16 passed
```
**MUTACJA C** — wyłączyłem render panelu porównania (`{false && saveState === 'conflict' && …}`):
```
× shows a persistent conflict banner with keep/use/compare choices …
  → Unable to find an accessible element with the role "region" and name "Conflict comparison"
```
**CZERWONY.** Test broni realnej funkcji, nie samego napisu. Zmiana UI jest dobra: `role="region"`
z nazwą, tokeny `c-*`, `focus-visible:ring-c-focus`, zero crimson. Liście i18n kompletne
i **naprawdę przetłumaczone** (`conflictUseTheirs` = „Weź cudze”, `conflictKeepMine` =
„Zachowaj moje”, `conflictCompare` = „Porównaj”), oba `translation.json` parsują się.
Drobiazg: stare klucze `conflictReload`/`conflictRetry` zostały bez wołacza.

### ★ ROZBIEŻNOŚĆ 2 — wyścig 409 dowiedziony na trasie, której front nie woła

Raport: *„RealPG: `conflict-realpg.json` — 3/3 PASS, `--retry=0`, realny Postgres 6299”*.
Rejestr wskazuje trasę: *„`PUT /api/v8/notebook/pages/:noteId/content`”*.

Zmierzyłem pochodzenie i adres tej trasy:

1. Plik testu **nie występuje w diffie 295**; `git log` daje jeden commit:
   `45753ed3bb integration(m02): merge canonical My Work and fail-closed migration foundation`.
   295 **uruchomiło cudzy test**, nie napisało własnego.
2. Nagłówek tego pliku mówi wprost o testowanej trasie:
   *„distinct from the production write path `server/src/routes/v8/my-work.routes.ts` —
   no frontend caller reaches this endpoint today; it is contract/acceptance scaffolding”.*
3. Kody błędu się rozjeżdżają: testowana trasa zwraca `P07_CONCURRENT_EDIT_CONFLICT`
   (`server/src/routes/v8/notebook.routes.ts:496,558`), a front sprawdza
   `err?.code === 'NOTEBOOK_PAGE_CONFLICT'` (`NotebookContent.tsx:1700`), który produkuje
   `server/src/routes/v8/my-work.routes.ts:1485,1602,1722`.

Trasa produkcyjna jest sama w sobie zbudowana poprawnie — przeczytałem
`my-work.routes.ts:1585-1607`: atomowy predykat `date_trunc('milliseconds', updated_at) = …`
plus `if (expectedUpdatedAt && !update.changes) → 409` ze świeżym wierszem w `data`.
**Ale wyścig dwóch klientów na tej trasie nie został w tym dyżurze uruchomiony ani razu.**
Raport przyznaje pokrewne ograniczenie („nie dowodzi montażu przez `ApiGateway.initializeRoutes`”),
lecz nie przyznaje tego głównego: dowód dotyczy innego endpointu.

**Dowód 3 — Inicjatywy i bezpieczniki.**
```
npx vitest run tests/unit/initiatives/initiativeRecordCanon.test.ts
→ Test Files 1 passed | Tests 6 passed        ← 6/6, kanon rekordu zielony

npx vitest run src/components/Initiatives/__tests__/
→ Test Files 3 failed | 10 passed | Tests 1 failed | 167 passed | 8 skipped | 1 todo

bash scripts/check-list-canon.sh
→ ✓ naruszeń 368, baseline 368 — dług nie rośnie                       EXIT 0
bash scripts/check-artefakt.sh
→ ✓ crimson: aktualnie 9, baseline 9 — dług nie rośnie                 EXIT 0
esbuild × 6 zmienionych .ts/.tsx                                        → 6/6 OK
node -e "JSON.parse(pl); JSON.parse(en)"                                → OK
```
Jedyny realny czerwony (`financialNarrativeBlocks.test.ts` — „expected `=== [FIN:ANALYSIS]…`
to contain `Financial analysis (sizing + ROI)`”) **jest zastany**: uruchomiłem go także na
plikach z markera `58ef0771d7` i tam też jest `1 failed | 5 passed`. To nie jest wina 295.
Dwa dalsze „failed suites” to brak Postgresa dla `*.realpg`/`*.pg` — środowiskowe.

Uwaga do liczby: raport pisze „Testy preview/smoke/kanonu: 138/138 PASS”. Cały katalog modułu
to **177** przypadków, w tym jeden zastany czerwony. To wybór podzbioru, nie fałsz, ale meldunek
brzmi mocniej niż stan modułu.

**Zmiana w Inicjatywach.** `CanonicalInitiativeRegister.tsx` zamienia własną, dwuprzyciskową
stopkę na kanoniczny `PreviewActionBar` — zgodnie z kanonem podglądu. Znika widoczny przycisk
„Otwórz”; sprawdziłem, że zdolność zostaje: `onOpen` jest nadal wołane przez `onOpenFull`
(`:152-154`) i `onRowDoubleClick` (`:186`). Etykieta „Kopiuj link” pozostaje zaszyta po polsku
(bez `t()`) — tak było przed zmianą, nie jest to regres 295.

**Dowód wizualny — bezpiecznik par.** 32 kadry Idei w `evidence/grafika/idee-komplet-20260903/`
(4 ekrany × pl/en × 1024/1440 × light/dark) + 2 kadry Inicjatyw. Policzyłem średnią luminancję
64×64 dla 16 par:

```
delta light-dark: min 215,1 (whiteboard pl-1024)  max 228,6 (idea-table 1440)
podejrzanych par (delta ≤ 30): 0 z 16
pl vs en (sha256 1440/light): 4 z 4 różne
```
Zero duplikatów podanych pod dwiema nazwami — ani motywu, ani języka.

**Ale**: w repo jest **34 PNG i 0 plików JSON**. Sumy kontrolne (`idee-32-sha256.txt`), wyniki axe
(`r2-a11y-green.txt`), inwentarz (`idea-controls-live.json`, `idea-controls-unique.tsv`) i dowód
409 (`conflict-realpg.json`) leżą **wyłącznie** w `/private/tmp/cx-day295-…-artefakty/`.
Zweryfikowałem `ls` — dziś są. Jutro po sprzątaniu worktree ich nie będzie, a raport nadal będzie
je cytował.

## Zdania do rejestrów — 295

- **`MYW-IDEAS-013`** → `PARTIAL`. *Powstał powtarzalny inwentarz kontrolek trzech narzędzi Idei
  z mianownikami 53 / 65 / 81 i drutem na hashu sygnatur (odtworzone niezależnie 03.09), ale efekt
  dowiedziono dla 12 z 226 sygnatur — mutacja wypatroszenia handlera istniejącej kontrolki
  zostawia test zielonym — a czwarte narzędzie (`IdeaTableTool.tsx`, mianownik realny 54 unikalnych
  wobec zmierzonych 27) nie zostało zinwentaryzowane, bo zmierzono ekran listy `idea-table`
  zamiast `idea-table-timeline-stuck`. Tabela MARTWE jest pusta z braku pomiaru, nie z braku martwych.*
- **`MYW-PHOTO-010`** → `PARTIAL`. *Ścieżka użytkownika przy konflikcie jest kompletna i broniona
  testem (mutacja wyłączenia panelu „Porównaj” daje czerwień), pl/en przetłumaczone; natomiast
  dowód wyścigu dotyczy trasy `v8/notebook.routes.ts`, o której własny nagłówek pliku mówi
  „no frontend caller reaches this endpoint today”, a test ten istniał przed dyżurem
  (`45753ed3bb`). Wyścig na trasie produkcyjnej `v8/my-work.routes.ts` (`NOTEBOOK_PAGE_CONFLICT`)
  pozostaje `NOT_PROVEN`.*
- **`MYW-PHOTO-011`** → `DONE`. *32 kadry Idei w repo, 16 par light/dark realnie różnych
  (delta luminancji 215–229), 4 pary pl/en różne; sumy kontrolne i wynik axe pozostają poza repo.*
- **`INI-OWN-009`** → `PARTIAL`. *Stopka podglądu Inicjatyw przeszła na kanoniczny
  `PreviewActionBar`, `initiativeRecordCanon` 6/6 i `check-list-canon` zielone; w repo są 2 kadry PO
  z wymaganych 8 i zero kadrów PRZED, więc porównanie na tym samym markerze nie istnieje.*

---

# DYŻUR 297 — martwe komponenty od korzenia

Jeden commit `682375d322 docs(day297): STOP brak wymaganego miejsca na dysku`, jeden plik: raport.

## Tabela R1…R6

| Poz. | Żądano | Dostarczono | Werdykt |
|---|---|---|---|
| R1 graf osiągalności od korzeni | graf `src/index.tsx` + `dev-render/main.tsx` + testy | — | **NIEWYKONANE** |
| R2 usunięcie martwych poddrzew | commit per poddrzewo z liczbą plików/linii/kluczy | — | **NIEWYKONANE** |
| R3 cztery tabele (żywy z app / z harnessu / z testów / martwy) | — | — | **NIEWYKONANE** |
| R4 bezpiecznik `scripts/dev/reachability-from-root.mjs` + baseline + test | — | — | **NIEWYKONANE** |
| R5 dowód, 5 ekranów PRZED/PO | — | — | **NIEWYKONANE** |
| R6 raport | raport STOP, 56 linii | **WYKONANE** |

## Co sprawdziłem sam

```
git ls-tree -r --name-only codex/day297-… | grep -i reachability   → PUSTO
git log --all --diff-filter=A -- '**/reachability-from-root.mjs'   → PUSTO
```
Bezpiecznika **nie ma nigdzie w repo**, w żadnej gałęzi. Werdykt nie budzi wątpliwości.

## Co raport twierdzi bez commitów (do przeniesienia do nowego dyżuru, NIE do rejestru jako fakt)

Raport wypisuje cztery „korekty wobec instrukcji”. Jedną dało się zweryfikować od ręki i **jest
prawdziwa**:
```
python3 → wierszy zaczynających się od "|" w evidence/grafika/martwe-komponenty-r2-20260903.md = 267
```
instrukcja spodziewała się „~240”. Korekta 267 — **potwierdzona**.
Pozostałe (499 importów dynamicznych; `InboxTriage` żywy przez kebab `InboxContent.tsx:2859/:2946`;
`admin`/`Admin` = 1 nazwa w `src/components`) **nie zostały przeze mnie przemierzone** — traktować
jako hipotezy wykonawcy, nie jako pomiar. Przy ostatniej z nich przypominam pułapkę: na macOS
`admin` i `Admin` to ten sam inode, więc „1” jest odpowiedzią systemu plików, a nie repozytorium.

## STOP — czy był uprawniony

Tak. `§0.5` instrukcji 297 dopuszcza zatrzymanie całego dyżuru przy „mniej niż 5 GB wolnego dysku”.
Raport cytuje `df -h /` z `Avail 1.8Gi` po materializacji worktree (33 863 pliki) przy `5.4Gi`
przed. Dziś na tej samej maszynie `Avail 34Gi` — sytuacja minęła, bo worktree'e zostały sprzątnięte.
Wykonawca nie ruszył kodu produktu, nie obniżył progu i nie zaadoptował cudzego katalogu.
**To jest wzorcowe zatrzymanie, nie porażka wykonawcy — to porażka pojemności środowiska.**

## Zdanie do rejestru — 297

- **D12 (martwe komponenty)** → `NIEWYKONANE`. *Dyżur 297 zatrzymał się przed R1 na progu 5 GB
  wolnego dysku (`Avail 1.8Gi` po utworzeniu worktree); dostarczył wyłącznie raport STOP.
  Bezpiecznik `scripts/dev/reachability-from-root.mjs` nie istnieje w żadnej gałęzi repo,
  a inwentarz „238 komponentów bez importera” pozostaje niezrewidowany — jedyny zweryfikowany
  wynik to 267 wierszy tabeli w `evidence/grafika/martwe-komponenty-r2-20260903.md` zamiast ~240.
  Dyżur do wydania ponownie na maszynie z zapasem dysku.*

---

# DYŻUR 298 — silnik raportu Oceny DRD

## Najpierw obawa właściciela: „sesja 298 była zablokowana profilem read-only”

**Obalone.** Zmierzyłem:
```
git merge-base HEAD codex/day298-…  →  ebfcf3d580d58734d1c2eeabcc4aa90dbfd16943   ← właściwy marker
6 commitów, 29 plików, 527 wstawek
ffa6e8e449 docs(day298): measure DRD report engine gaps
2c9be2b1b8 feat(day298): build accepted DRD report model from session data
939792e397 feat(day298): persist DRD report session metadata
c770ebd0e0 feat(day298): share accepted DOCX layout with report model
412d3beb96 docs(drd): correct native scales for axes 5 and 6
7d2cda6a8f docs(day298): finalize partial evidence report
```
To nie są commity-atrapy: trzy z nich niosą realny kod (`acceptedDrdReportModel.ts` 133 linie,
`methodSessionReportMetadataService.ts` 105 linii, migracja 21 linii, test 67 linii), a ja ten kod
uruchomiłem. Sesja pisała do dysku i do bazy.

## Tabela R1…R6

| Poz. | Żądano | Raport | Mój pomiar | Werdykt |
|---|---|---|---|---|
| **R1** | generator, trasa, mapa pole→źródło, magazyn | wykonane, rejestr 52 linie | rejestr istnieje i wskazuje `method-core.routes.ts` | **WYKONANE** |
| **R2** | `ReportModel` współdzielony + budowa **z realnej sesji** + test równości z `SAMPLE_DRD_SCORES` | wykonane, 3/3 zielone | test 3/3 PASS **u mnie**; mutacja zaokrąglenia → czerwień; **ale budowa z realnej sesji nie istnieje** — builder przyjmuje całą narrację jako wejście | **CZĘŚCIOWO** |
| **R3** | migracja addytywna + **karta UI „Metryka badania”** + tłumaczenia poziomów + reguła E0–E4 | `PARTIAL` + STOP na braku licencji na kartę | migracja **przeszła u mnie od zera na 6313**, idempotentna, FK+unikat tenantowy; karta UI i tłumaczenia — brak | **CZĘŚCIOWO** |
| **R4** | DOCX+PDF+wyciąg 4 str., **podpięcie pod „Generuj raport”**, zapis w magazynie, zimny odczyt, **narrator LLM za flagą OFF + test fail-safe** | `PARTIAL` + STOP na braku licencji na handler i magazyn | DOCX z modelu silnika **złożyłem sam** — `word/document.xml` identyczny bajt w bajt; **flagi LLM nie ma w diffie**; podpięcia nie ma | **CZĘŚCIOWO** |
| **R5** | 21 stron obok prototypu z **tabelą różnic**, 8 kadrów runtime, `check-artefakt` | `PARTIAL`, „21/21 mean_abs_rgb=0.0000”, brak kadrów runtime | **odtworzyłem cały łańcuch sam**: 21/21 = 0.0000; `check-artefakt` zielony | **CZĘŚCIOWO** (brak 8 kadrów) |
| **R6** | raport, STOP-pytania, zdanie do `ASM-OWN-024/025` | wykonane, `PARTIAL`, „nie promować do demo” | najuczciwszy raport z całej trójki | **WYKONANE** |

## Dowody uruchomione przeze mnie + mutacje

**Dowód 1 — test równości modelu.**
```
npx vitest run server/src/services/report/__tests__/acceptedDrdReportModel.test.ts
→ Test Files 1 passed | Tests 3 passed
```
**MUTACJA D** — `asIs: round2(score.actual)` → `round1(...)` w `acceptedDrdReportModel.ts:113`:
```
× reproduces the accepted prototype model for SAMPLE_DRD_SCORES
    -  "asIs": 4.22,
    +  "asIs": 4.2,
→ Tests 1 failed | 2 passed
```
**CZERWONY dokładnie na tej liczbie, którą raport opisuje jako powód poprawki.** Test broni precyzji
osi. Bezpiecznik działa.

**Zastrzeżenie do R2, którego raport nie nazywa wprost.** Test podaje builderowi **cały model
prototypu jako wejście** (`buildAcceptedDrdReportModel({ ...expected, … })`) i sprawdza
`expect(actual).toEqual(expected)`. Builder przelicza z kernela wyłącznie
`nazwa · skala · obszary · asIs · toBe · procent`; META, wnioski, tabele, rekomendacje i mapa drogowa
**przechodzą przez niego bez zmiany**. Test dowodzi więc warstwy liczbowej i braku psucia
przelotu — nie dowodzi, że model da się zbudować z realnej sesji. Raport przyznaje to w sekcji
„Twierdzenia niezweryfikowane” (`zgodność modelu z realnej sesji: NOT_PROVEN`) i to przyznanie
podtrzymuję.

**Dowód 2 — skład: DOCX z modelu SILNIKA vs DOCX prototypu (mój własny przebieg).**
Zbudowałem model przez `tsx` z produkcyjnej funkcji i zapisałem go jako moduł:
```
model z SILNIKA: osie 1:4.22/7  2:3/5  3:2.4/5  4:4/7  5:3.6/6  6:3.4/6  7:2/5
```
(przy okazji potwierdza to poprawkę `412d3beb96`: osie 5 i 6 mają w kernelu `levelCount = 6`,
więc `data/sample-reports/drd-full-diagnostic-report.md` słusznie zmieniono z „skala 1-5” na „1-6”.)

```
node scripts/prototypes/build-raport-oceny-prototyp.mjs /tmp/ag-domyslny.docx
node scripts/prototypes/build-raport-oceny-prototyp.mjs /tmp/ag-silnik.docx /tmp/ag-model-silnik.mjs

sha256 word/document.xml (domyślny) = 9e39b38f6831650af90eceb1f627ee3cef366d7fefd070eb786c632b99a7444b
sha256 word/document.xml (silnik)   = 9e39b38f6831650af90eceb1f627ee3cef366d7fefd070eb786c632b99a7444b
```
**Identyczne.** Twierdzenie „prototyp i silnik używają dokładnie jednego kodu składu” potwierdzone
niezależnie, moim własnym uruchomieniem, a nie odczytem cudzego artefaktu.

**Dowód 3 — 21 stron strona po stronie, mój render.**
`soffice --convert-to pdf` → `pdftoppm -r 110 -png` → 21 stron A4 910×1287, porównane z
`evidence/prototypy/raport-oceny-20260903/strona-NN.png`:
```
strona 01…21   mean_abs_rgb vs PROTOTYP = 0.0000   (21/21)
stron z różnicą > 1.0: 0 / 21
```
To samo dla kadrów zacommitowanych przez wykonawcę
(`evidence/prototypy/raport-oceny-silnik-20260903/page-NN.png`): 21/21 = 0.0000, rozmiary zgodne.

**Tabela różnic strona po stronie: pusta — zero różnic na 21 stronach.**

Obejrzałem strony **własnymi oczami**, nie tylko licznikiem. Strona 1: okładka „Raport z Oceny
Dojrzałości Cyfrowej”, TechProd Manufacturing, wynik ogólny 54,6% skali, benchmark 46%.
Strona 5: tabela 9 obszarów osi 1 z AS-IS/TO-BE/Luka i etykietami dowodu
(`Potwierdzony` / `Deklarowany` / `Niepełny` — reguła E0–E4 działa), wnioski, tabela rekomendacji
z kolumnami **Priorytet · Horyzont · Właściciel** (pola z migracji R3), blok „Sufit rekomendowany”
z uzasadnieniem. To jest realny dokument doradczy, nie makieta.

**Crimson.** `C.accent = '85182F'` używany wyłącznie przy `isCrit`, `o.ryzyko` i nagłówku `RYZYKO`
(`build-raport-oceny-prototyp.mjs:114, 234, 248, 250`) — semantyka krytyczna, zgodnie z kanonem.
```
bash scripts/check-artefakt.sh → ✓ crimson: aktualnie 9, baseline 9 — dług nie rośnie
```

**Dowód 4 — migracja strict od zera na MOJEJ bazie 6313.**
```
docker run … -p 127.0.0.1:6313:5432 pgvector/pgvector:pg17        → gotowy po 3 s
DATABASE_URL=…6313/cx298 npx tsx server/scripts/migrate.postgres.ts
→ ✅ Postgres migrations complete           (pełny łańcuch na PUSTEJ bazie)
\d method_session_report_metadata
→ 13 kolumn; PK(session_id); UNIQUE(organization_id, session_id);
  FK organization_id → organizations(id) ON DELETE CASCADE
  FK session_id      → method_sessions(id) ON DELETE CASCADE
drugi przebieg → Applying migrations: 0     ← idempotentna
```
Migracja jest addytywna i **nie wywraca odtworzenia bazy od zera**: `method_sessions` powstaje
w `20260813_method_core_1_kernel.sql`, czyli przed `20260903_…` — kolejność nazw jest zgodna
z kolejnością zależności.

**Dowód 5 — izolacja tenantowa serwisu (na tę ścieżkę NIE MA testu w repo).**
Zasiałem `org-A`, `org-B` i sesję `sess-A` należącą do `org-A`, po czym wołałem produkcyjny serwis:
```
WŁAŚCICIEL widzi zapis: true
OBCY tenant: ZABLOKOWANY: report metadata save refused: session not found in organization
wiersz po próbie obcego: {"organization_id":"org-A","study_period":"P"}   ← nietknięty
OBCY get() = null
```
Para wymagana przez metodykę — „obcy nie widzi” **i** „właściciel widzi” — spełniona na realnym
łańcuchu, nie na atrapie.

**MUTACJA E — kasacja warunku tenantowego** w `WHERE EXISTS (SELECT 1 FROM method_sessions
WHERE id = ? AND organization_id = ?)`:
```
WŁAŚCICIEL widzi zapis: true
OBCY tenant: ZABLOKOWANY: report metadata save refused: session not found in organization
wiersz po próbie obcego: {"organization_id":"org-A","study_period":"PRZEJETE"}   ← ★ NADPISANY
OBCY get() = null
```
Bezpiecznik jest **nośny** — po jego usunięciu obcy tenant realnie nadpisał cudzy wiersz.

### ★ ROZBIEŻNOŚĆ 3 — komunikat „zablokowano” pochodzi z odczytu PO zapisie, nie z odmowy zapisu

Zwróć uwagę na drugą linię obu przebiegów: **`save()` rzuciło ten sam wyjątek również wtedy, gdy
zapis się powiódł.** Wyjątek pochodzi z `if (!saved) throw new Error('report metadata save refused…')`,
czyli z tenantowego `get()` **po** `INSERT`. Nie ma transakcji i nie ma weryfikacji, co faktycznie
zapisano. Skutki:

1. Gdyby warunek tenantowy kiedykolwiek zregresował, wołający dostanie wyjątek i uzna „odmówiono”,
   podczas gdy dane cudzej organizacji **już są nadpisane**. To jest kształt „bezpiecznik nagradza
   defekt” — im cichszy błąd, tym spokojniejszy komunikat.
2. W repo **nie ma ani jednego testu** dla `methodSessionReportMetadataService`. Trzy nowe
   przypadki dotyczą wyłącznie `acceptedDrdReportModel`. Usunięcie `AND organization_id = ?`
   przeszłoby cały pakiet na zielono.

**Zalecenie dla nadzorcy:** przed scaleniem dopisać jeden test `.pg.test.ts` na tę parę
(właściciel zapisuje / obcy nie nadpisuje) i owinąć `save()` w transakcję albo zamienić
`INSERT … WHERE EXISTS` na jawny `RETURNING` z kontrolą liczby wierszy.

### ★ Silnik nie ma ani jednego wołacza produkcyjnego

```
git grep -n "buildAcceptedDrdReportModel" -- server src scripts
→ wyłącznie server/src/services/report/__tests__/acceptedDrdReportModel.test.ts

git grep -n "methodSessionReportMetadataService" -- server src
→ PUSTO (zero wołaczy)
```
To jest **biblioteka bez wywołania**: kod jest poprawny, przetestowany i udowodniony, ale żaden
przycisk, trasa ani serwis go nie uruchamia. Raport nazywa to sam („przycisk »Generuj raport«
nadal nie uruchamia tego silnika”) i za tę uczciwość należy mu się kredyt — ale wartość dla
właściciela na dziś wynosi **zero kliknięć**.

### Czego nie ma, choć instrukcja żądała imiennie

```
git diff ebfcf3d580..HEAD | grep -iE '^\+.*(ENABLE_|FEATURE_|narrat|llm)'
→ tylko zdania w dokumentach; ZERO kodu flagi
```
- **narrator LLM za flagą domyślnie OFF + test fail-safe (błąd modelu → raport i tak powstaje)**
  — nie powstał. Raport przyznaje: „Flaga LLM default OFF nie została dodana”.
- **karta „Metryka badania” w UI** (formularz kanoniczny, i18n pl/en) — nie powstała.
- **polskie tytuły poziomów osi 1–4 i 7** — nie powstały.
- **8 kadrów runtime kanonicznym narzędziem** — nie powstały (ekran nie ma podpiętego silnika,
  więc fotografowanie starego UI byłoby przyrządem pokazującym nie produkt; zgadzam się z tą decyzją).
- **realny HTTP → ApiGateway → JWT → PostgreSQL → plik → zimny odczyt** — nie powstał.

Dwa STOP-y (R3 karta UI, R4 handler i magazyn) są **merytoryczne i uzasadnione**: sprawdziłem,
że instrukcja 298 rzeczywiście **nie zawiera tabeli licencji**, a to ona nazywa walidator · trasę ·
kontroler · serwis · repozytorium. Wykonawca postąpił zgodnie z bezpieczniejszą interpretacją
zamiast improwizować magazyn plików. To jest właściwa reakcja.

### Kontrola higieny 298
```
esbuild acceptedDrdReportModel.ts / methodSessionReportMetadataService.ts / test  → 3/3 OK
node --check scripts/prototypes/build-raport-oceny-prototyp.mjs                    → OK
bash scripts/check-artefakt.sh                                                     → zielony (9/9)
liście i18n                                                                        → 298 nie dodało kluczy
merge-tree vs m03                                                                  → 0 konfliktów
```

## Zdanie do rejestrów — 298

- **`ASM-OWN-024` / `ASM-OWN-025`** → `PARTIAL`. *Zaakceptowany układ raportu DRD jest odtwarzany
  1:1 z wymiennego modelu — odbiorca złożył DOCX z modelu silnika i z modelu prototypu i otrzymał
  identyczny `word/document.xml` (`sha256 9e39b38f…`), a własny render 21 stron A4 pokrył się
  z zaakceptowanym prototypem na wszystkich stronach (`mean_abs_rgb = 0,0000`, 21/21). Migracja
  `20260903_assessment_report_metadata.sql` przeszła strict od zera na pustej bazie i jest
  idempotentna (drugi przebieg `Applying migrations: 0`), a izolacja tenantowa serwisu potwierdzona
  na realnym Postgresie parą „właściciel zapisuje / obcy nie nadpisuje i nie widzi”. Mimo to obie
  pozycje pozostają otwarte: `buildAcceptedDrdReportModel` i `methodSessionReportMetadataService`
  nie mają ani jednego wołacza produkcyjnego, przycisk „Generuj raport” nadal nie uruchamia silnika,
  narrator LLM za flagą domyślnie OFF wraz z testem fail-safe nie powstał, karta „Metryka badania”
  i polskie tytuły poziomów nie powstały, a ścieżka tenantowa serwisu nie ma żadnego testu —
  po skasowaniu warunku `AND organization_id = ?` obcy tenant nadpisał cudzy wiersz, a `save()`
  i tak zgłosiło „zablokowano”, bo komunikat pochodzi z odczytu wykonanego PO zapisie.*

---

## PODSUMOWANIE DLA NADZORCY — co wolno zrobić dziś

| Gałąź | Wolno scalić | Czego NIE wolno zrobić |
|---|---|---|
| 295 | tak — kod jest zielony, kanoniczny, `check-list-canon` i `check-artefakt` bez przyrostu długu, 0 znaczników konfliktu | **nie zamykać** `MYW-IDEAS-013` ani `MYW-PHOTO-010`; nie przepisywać do rejestru zdania „wszystkie kontrolki działają”; przenieść sumy kontrolne i wynik axe z `/private/tmp` do `evidence/` **przed** scaleniem |
| 297 | scalenie samego raportu STOP jest nieszkodliwe (0 konfliktów) | **nie oznaczać D12 jako ruszonego**; dyżur wydać ponownie po zwolnieniu dysku |
| 298 | tak, jako fundament — model, migracja, serwis, wspólny skład | **nie promować na demo**, **nie włączać narratora**, **nie zamykać** `ASM-OWN-024/025`; dopisać test tenantowy serwisu przed scaleniem albo natychmiast po |

## Trzy rzeczy, które w tym odbiorze wyszły dzięki mutacjom, a nie dzięki czytaniu

1. **295:** test enumeracji jest zielony po wypatroszeniu handlera kontrolki — inwentarz udaje dowód.
2. **295:** czwarty mierzony ekran to lista Idei, nie narzędzie Tabela — mianownik zaniżony 27 wobec 54.
3. **298:** po skasowaniu warunku tenantowego zapis obcej organizacji **przechodzi**, a mimo to
   `save()` melduje „zablokowano” — bo komunikat pochodzi z odczytu po zapisie, nie z odmowy.

## Twierdzenia, których NIE zweryfikowałem

- Wyścigu 409 nie uruchomiłem na własnym Postgresie (mój port 6313 zajęła praca 298); ocena R3
  opiera się na pochodzeniu pliku testu, nagłówku tego pliku i rozjeździe kodów błędu, nie
  na własnym przebiegu wyścigu.
- Trzech z czterech „korekt wobec instrukcji” dyżuru 297 (499 importów dynamicznych, wołacze
  `InboxTriage`, `admin`/`Admin` = 1) nie przemierzyłem.
- Nie sprawdzałem 4-stronicowego wyciągu zarządczego 298 (raport podaje jego SHA-256; plik jest
  poza repo).
- Nie oceniałem listy czekowania część B punkt po punkcie na żywym ekranie Inicjatyw — opieram się
  na `initiativeRecordCanon` 6/6, `check-list-canon` i dwóch kadrach PO.
