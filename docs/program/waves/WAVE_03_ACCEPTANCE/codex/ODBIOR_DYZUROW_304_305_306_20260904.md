# ODBIÓR ADWERSARYJNY — dyżury 304, 305, 306 (fala 2) — 2026-09-04

Odbiorca: Opus, sesja odbioru F. Baza: `/private/tmp/m03` HEAD `3ce22596ed`, worktree
`/private/tmp/ag-odbior-f`, gałąź `agent/odbior-f-20260904`. Baza pomiarowa gałęzi: `416432abaf`.
Środowisko: Postgres 6320 (`pgvector/pgvector:pg16`, pełny łańcuch strict od zera), vite 5454.
Niczego nie scalono, niczego nie wypchnięto, żadnej flagi nie włączono na stałe.

## 0. Werdykty jednym słowem

| Dyżur | Samoocena Codexa | Werdykt odbioru | Podstawa |
|---|---|---|---|
| 304 | CZĘŚCIOWE | **SCALIĆ Z ZASTRZEŻENIEM** (dokument pomiarowy, zero kodu) | 2 pliki docs; rdzeń R2 i R5 niewykonany; R4 domknąłem sam — wypada pozytywnie |
| 305 | CZĘŚCIOWE | **SCALIĆ Z ZASTRZEŻENIEM** — *dokument, nie prototyp* | 2 pliki docs; R3/R4/R5 STOP, a STOP okazał się nieuzasadniony |
| 306 | CZĘŚCIOWE | **SCALIĆ Z ZASTRZEŻENIEM** (za flagą, bez liczenia jako funkcja) | kod bezpieczny i fail-closed, ale leży w warstwie **bez ani jednego konsumenta** |

Wspólna cecha trzech gałęzi: **żadna nie daje właścicielowi niczego do akceptu wizualnego.**
Materiał do oglądania wytworzyłem dopiero ja, w tym odbiorze.

## 1. „2 pliki na 6 commitów” — nazwane wprost

| Dyżur | Commity | Pliki | Kod produktu | Nowy test |
|---|---|---|---|---|
| 304 | 6 (wszystkie `docs(day304)`) | 2 dokumenty | **0 linii** | **0** |
| 305 | 6 (5× `docs`, 1× `test`) | 2 dokumenty | **0 linii** | **0** — commit `test(day305)` nie dodaje pliku testu, zapisuje wynik uruchomienia w dokumencie |
| 306 | 7 (6× `docs`, 1× `feat`) | 6, w tym 4 kodu | 68 linii + 4 zmienione | 1 plik, 3 przypadki |

304 i 305 to **notatki pomiarowe podzielone na sześć commitów**, nie implementacje.
Zero ryzyka regresji (nie dotykają ani jednego pliku wykonywalnego), ale też zero funkcji.
Tylko 306 dołożył kod.

Żadna z trzech gałęzi nie dodaje migracji ani nie rusza `public/locales`
(zmierzone: `git diff --name-only <baza>..<gałąź> -- server/migrations server/scripts` = 0,
`-- public/locales src/i18n` = 0). Liście i18n bez zmian we wszystkich trzech.

## 2. Dyżur 304 — historia Czatu prywatna / organizacyjna

### 2.1 Tabela R1…R6

| Poz. | Czego wymagała instrukcja | Stan faktyczny | Werdykt |
|---|---|---|---|
| R1 | pomiar: tabela tras · porównanie definicji tabeli · co umie panel | 5 tras w tabeli, porównanie 3 definicji, opis panelu | **WYKONANE** |
| R2 | reguła widoczności w **jednym** miejscu kodu + testy jednostkowe | reguła opisana **słowem** w dokumencie; kod nietknięty; nadal ≥2 wykonania ACL | **NIEWYKONANE** (Codex pisze CZĘŚCIOWE — łagodniej niż stan) |
| R3 | migracja addytywna + łańcuch strict od zera | uznał, że pola już są, i **nie dodał migracji** | **WYKONANE PRZEZ POMIAR** (potwierdzam, patrz 2.2) |
| R4 | para izolacji na realnej bazie + **dowód mutacyjny** + Gateway | para real-PG jest (zastany test); mutacji i Gateway brak | **CZĘŚCIOWE** — mutację domknąłem ja, wynik pozytywny |
| R5 | prototyp panelu za flagą OFF + 4 kadry | świadomie nie zrobiony | **NIEWYKONANE (świadomie, zgodnie z prawem zatrzymania)** |
| R6 | raport z tabelą, dowodami, twierdzeniami niezweryfikowanymi | raport pełny, z sekcją TWIERDZENIA NIEZWERYFIKOWANE | **WYKONANE** |

Czy „CZĘŚCIOWE” jest uczciwe: **TAK, a w R4 wręcz zaniżone.** Codex nie przypisał sobie
niczego, czego nie zrobił, i sam odmówił zbudowania etykiety „Prywatne / Organizacja”
bez izolacji — dokładnie tak, jak nakazywało prawo zatrzymania. To jest zachowanie wzorcowe.
Rezerwa: raport nie mówi wprost, że **cały wysiłek zmieścił się w dwóch plikach dokumentacji**.

### 2.2 Moje dowody (5, nie 3)

**D1 — łańcuch migracji strict od zera, PUSTA baza 6320.** `✅ Postgres migrations complete`.
Drugi przebieg: `Applying migrations: 0` — idempotentny. Uwaga: to jest stan **HEAD**,
bo gałąź nie dodaje ani jednej migracji. Addytywność jest tu trywialna (nie ma czego dodawać).

**D2 — model na ŻYWEJ bazie, nie w kodzie.** `\d conversations` po pełnym łańcuchu daje
`user_id`, `organization_id`, `visibility_scope` (default `'private'`), `access_policy_version`,
oraz indeksy `idx_conversations_private_list (organization_id, user_id, deleted_at, …)` i
`idx_conversations_team_list`. `chat_projects` ma `scope`, `organization_id`, `user_id`.
**Teza Codexa „nie trzeba migracji” potwierdzona pomiarem na żywej bazie.**

**D3 — trzy definicje tabeli, nie dwie.** `073_conversations.sql`,
`20260331_p35b_canonical_model_completion.sql`, `fix_conversations_table.sql`.
Instrukcja twierdziła „dwie”. **Korekta Codexa jest poprawna** — potwierdzam własnym grepem.
Trzeci słownik schematu zostaje jako dług.

**D4 — para izolacji na realnej bazie.** `conversations.search.realdb.test.ts`,
komplet zmiennych z §0.2c, `--retry=0`, PG 6320: **12/12 PASS**. Para obecna w nazwach:
`a real team member (checkChatPermission allowed) DOES see the team conversation` (właściciel widzi)
+ `a user in a completely different organization gets zero leakage` (obcy nie widzi)
+ `a personal-only user … cannot see another user's personal conversation`.

**D5 — DOWÓD MUTACYJNY, którego Codex nie wykonał.** Zmieniłem w
`server/src/routes/conversations.routes.ts:589` filtr organizacji na zawsze prawdziwy:
`OR (cp.scope = 'team' AND (cp.organization_id = ? OR TRUE))`. Wynik: **1 failed | 11 passed**,
i czerwienieje **dokładnie** `a user in a completely different organization gets zero leakage`,
podczas gdy pozytywna połówka („członek widzi”) zostaje zielona. Filtr przywrócony,
`git status` czysty. **To jest para nie do podrobienia i test celuje w samo zabezpieczenie,
nie w mechanizm.** Kształt „zamknięte przez wygaszenie” tu nie występuje.

Konsekwencja: **izolacja historii Czatu na trasie `/api/conversations/search` jest realna
i udowodniona.** Ale zasługa należy do kodu zastanego — dyżur 304 nie dołożył do tego
ani jednej linii.

### 2.3 Czego nadal nie ma (dla dyżuru kontynuacji)

- **R2** — centralny resolver widoczności; dziś reguła żyje w `conversations.routes.ts`
  (`findAccessibleConversation`), `chat-projects.routes.ts` i `chatPermissionService`.
- **R4 (reszta)** — dowód przez `ApiGateway.initializeRoutes` (test montuje router w gołym
  Express, więc nie dowodzi produkcyjnego montażu) oraz zimny readback po przeniesieniu.
- **R5** — prototyp panelu za flagą OFF + 4 kadry. Uwaga do zlecenia: `ChatHistorySidebar.tsx`
  (1365 linii — liczba potwierdzona) **już zna zakresy** `personal`/`team`, więc dyżur
  kontynuacji ma polerować istniejące, nie budować od zera.

## 3. Dyżur 305 — standard kart 7 typów

### 3.1 Tabela R1…R6

| Poz. | Czego wymagała instrukcja | Stan faktyczny | Werdykt |
|---|---|---|---|
| R1 | tabela **różnic** pole/zachowanie × 7 typów, z kodu; czy wspólny kontrakt ma konsumentów | macierz 7 wierszy × 6 kolumn opisowych; kolumna „wspólny `KanonicznaKarta`” = 7× „tak”; **ani jednej konkretnej różnicy pola**; pytanie o konsumentów wspólnego pliku **bez odpowiedzi** | **CZĘŚCIOWE** — inwentarz, nie tabela różnic |
| R2 | wspólny kontrakt **z cytatami z kanonu i numerem paragrafu**, zgodny z §13 archetyp C | 6 wymagań powłoki, **zero cytatów, zero numerów paragrafów**; rozjazd wobec §13.1 nieodnotowany | **CZĘŚCIOWE** |
| R3 | prototyp jednego typu za flagą OFF | STOP; flaga `VITE_VF1_STANDARD_CARD_INITIATIVE` **nie istnieje w repo** (sprawdzone grepem) | **NIEWYKONANE — STOP nieuzasadniony, patrz 3.3** |
| R4 | 4 kadry + para PRZED/PO | STOP | **NIEWYKONANE — STOP nieuzasadniony** |
| R5 | lista odbioru §18.1 literalnie + `check-artefakt` zielony | listy §18.1 **nie ma**; raport podaje wyniki bramek | **NIEWYKONANE** (raport nazywa to CZĘŚCIOWE) |
| R6 | plan przejścia sześciu typów | plan 5-punktowy z kolejnością | **WYKONANE** |

### 3.2 Moje dowody

**D1 — liczby Codexa zweryfikowane co do linii.** 385 / 299 / 281 / 885 / 846 / 346 / 221
oraz wspólny `cardContract.types.ts` = 301. Suma 3263, ze wspólnym 3564. **Zgadza się z pomiarem
Codexa i z tezą nadzorcy.** Ten dyżur nie ma choroby „hipoteza nadzorcy staje się faktem”.

**D2 — bramki, zweryfikowane przeze mnie, nie przepisane.**
`check-list-canon` ✓ (naruszeń 368, baseline 368 — dług nie rośnie);
`check-artefakt` ✓ (crimson 9, baseline 9; karty N R2+R3 = 0/0);
`initiativeRecordCanon.test.ts` **8/8**. Wszystkie liczby zgadzają się z raportem 305.
Zastrzeżenie: skoro gałąź nie zmienia ani jednego pliku kodu, te bramki mierzą HEAD, nie dyżur.

**D3 — rozjazd „8 ekranów vs 7 typów” ROZWIĄZANY.** Codex zostawił to jako „jawny rozjazd
pomiarowy”. Przyczyna: `karta-task.tsx` i `karta-task-pelna.tsx` to **dwa ekrany jednego typu
Task**. Ósmego typu nie ma; rozjazdu nie ma.

**D4 — odpowiedź na pytanie, które R1 zadał, a raport pominął.** Wspólny
`cardContract.types.ts` **ma konsumentów**: 17 plików, m.in. `taskCardContract.ts`,
`decisionCardContract.ts`, `notificationCardContract.ts`, `initiativeCardContract.ts`,
`useCardLayout.ts`. Znaleziska „biblioteka bez wywołania” tu **nie ma** — ale odpowiedź
powinna była paść w raporcie, bo instrukcja pytała wprost.

### 3.3 ★ STOP w R3/R4 był NIEUZASADNIONY — dowód

Codex uzasadnił STOP tak: „nie wprowadzono zmiany wizualnej bez bezpiecznego, lokalnego harnessu
pokazującego rzeczywisty `InitiativeDocumentView`”. Zmierzyłem — **harness istnieje, flaga
istnieje, para PRZED/PO jest wykonalna bez pisania ani jednej linii kodu**:

1. `dev-render/screens/karta-initiative.tsx` montuje **realny** `InitiativeDocumentView`
   (import w linii 42), nie atrapę.
2. `src/components/Initiatives/sections/initiativeCardContract.ts:841`
   `isInitiativeCardContractEnabled()` — realna flaga, **domyślnie OFF**, przełączalna
   query-paramem `?cardContract=1` (klucz `ff_initiativeCardContract`, alias `cardContract`).
3. `InitiativeDocumentView.tsx` używa jej i wyprowadzonych z niej list **8 razy**;
   komentarz w linii 213-214 mówi wprost: „kompozycja kart Initiative wyprowadzona
   z WIĄŻĄCEGO kontraktu karty … Za flagą (default OFF)”.

Czyli prototyp jednego typu za flagą OFF — dokładnie to, czego żądał R3 — **był już w repo**,
dla dokładnie tego typu, który Codex wybrał. Dyżur zatrzymał się przed sprawdzeniem stanu zastanego.

### 3.4 ★★ ZNALEZISKO, które ten STOP przykrył: włączona flaga KASUJE 11 z 15 sekcji

Wykonałem parę PRZED/PO kanonicznym `scripts/dev/grafika-zrzuty.mjs` (vite 5454, ekran
`karta-initiative`, light+dark, `?cardContract=0` vs `?cardContract=1`), obejrzałem każdy kadr.

- SHA-256 są **różne** w obu motywach → flaga realnie zmienia widok (to nie „duplikat zamiast motywu”).
- **OFF (dziś w produkcie):** lewa nawigacja ma 15 pozycji w 4 grupach — ZAKRES I PLAN
  (Zakres inicjatywy, Zadania 9, Harmonogram, Zależności, Produkty i kamienie 5),
  DECYZJE I RYZYKO (Decyzje 3, Ryzyko i RAID 5, Bramy, Sugerowane zmiany, Dziennik zmian),
  LUDZIE (Zespół, RACI 5, Właściciele strumieni), REZULTATY (Kryteria sukcesu, …).
- **ON:** zostają **4 pozycje w 2 grupach** — ZAKRES I PLAN (Zakres inicjatywy, Zadania 9)
  i REZULTATY (Kryteria sukcesu, KPI i korzyści). **Grupy DECYZJE I RYZYKO oraz LUDZIE
  znikają w całości.** Prawy panel i centrum bez zmian.

Zwężenie jest **zamierzone w kodzie** (`INITIATIVE_MINIMAL_BOARD_VISIBLE_IDS`, role
`rdzen`/`domyslna`), a komentarz w tym samym pliku (linia 39) mówi:
„DO POTWIERDZENIA PIOTRA (który board = rdzeń)”. **Kod sam oznacza to jako czekające na decyzję
właściciela — i właśnie po to istniał dyżur 305.** Pytanie nie trafiło do właściciela, bo kadrów
nie zrobiono. Robię je teraz.

### 3.5 Rozbieżność z §13 archetyp C — macierz 7 typów NIE odpowiada kanonowi

`Harvard/wdrozenie-100/ARTIFACT_ANATOMY_STANDARD.md` §13.1 „Archetyp C — REKORD” wymienia
**jedenaście** artefaktów: Initiative, Task, Decision, KPI, Insight, Idea, RAID, Milestone,
Change Request, Stage Gate, Action Proposal.

Siódemka z dokumentu 305: Task, Decision, **Notification**, Initiative, Insight,
**Interview**, **Tool**.

- przecięcie: **4** (Initiative, Task, Decision, Insight);
- w 305, a **nie ma** ich w §13.1: Notification, Interview, Tool (3);
- w §13.1, a **nie ma** ich w 305: KPI, Idea, RAID, Milestone, Change Request, Stage Gate,
  Action Proposal (**7**).

To dwie różne osie podziału (rejestr kart N vs archetypy artefaktów). R2 wymagał kontraktu
„zgodnego z §13 archetyp C” — dokument nie cytuje §13.1, nie odnotowuje rozjazdu i nie
rozstrzyga, która lista obowiązuje. **To jest pierwsza rzecz do domknięcia w dyżurze kontynuacji.**

### 3.6 Co zostało do dokończenia (305)

- **R1** — tabela **różnic** pole po polu (dziś 7× „tak” w kolumnie wspólnego kontraktu
  nie pokazuje żadnej różnicy, choć dyżur istnieje właśnie z powodu rozjazdu 3263 linii).
- **R2** — cytaty z numerami paragrafów; rozstrzygnięcie 7 vs 11 typów wobec §13.1.
- **R3** — nie budować od zera: mechanizm stoi; zadanie to **rozstrzygnięcie zakresu boardu**.
- **R4** — kadry są; brakuje decyzji właściciela na nich.
- **R5** — lista odbioru §18.1 literalnie, z „n/d + powód”.

## 4. Dyżur 306 — dwa etapy SWOT

### 4.1 Tabela R1…R6

| Poz. | Czego wymagała instrukcja | Stan faktyczny | Werdykt |
|---|---|---|---|
| R1 | pomiar faz i bram; konsumenci klocków `src/config/swot/` | tabela 7 etapów; **obalił tezę nadzorcy** (nie „wszystkie po pięć”: 14×5, 1×8, 4×4); konsumenci policzeni | **WYKONANE** |
| R2 | które dwa etapy brakują, **cytatem z numerem paragrafu** | §6.15 (Recommendations), §6.16 vs §6.B (sprzeczność „ostatni”), Review; jawne pytanie do właściciela | **WYKONANE — najlepsza pozycja z trzech dyżurów** |
| R3 | dwa etapy za flagą OFF, brama dla każdego, testy | flaga fail-closed + 2 fazy + 2 bramy + 3 testy; **tylko warstwa deskryptora** | **CZĘŚCIOWE** |
| R4 | sesja przez 7 etapów **wznawialna**, realna baza, zimny odczyt | STOP — store sesji ma 5 kroków | **NIEWYKONANE (STOP uzasadniony)** |
| R5 | 4 kadry warsztatu sesji | STOP — ekran montuje 5-etapowy runtime | **NIEWYKONANE (STOP uzasadniony)** — kadry zrobiłem ja |
| R6 | raport + koszt dla 18 paczek liczbą | raport z SHA artefaktów, 18×2 = 36 deskryptorów + 36 bram | **WYKONANE** |

Które **2 etapy** dołożono: `recommendations` (przed `outputs`) i `review` (po `outputs`),
każdy z własną bramą (`swot-recommendations-direction`, `swot-review-decision`).
Pliki: `src/toolPacks/packs/dynamicSwot.pack.ts`, `src/utils/dynamicSwotSevenStagesFlag.ts`,
`src/toolPacks/registry.ts`. **Nie ma ich w `src/method-core/methods/swot/**`** — ten katalog
nie został tknięty; zlecenie odbioru wskazywało go, faktyczna zmiana leży w `src/toolPacks/`.

### 4.2 Moje dowody

**D1 — testy pack 7 vs 5 na HEAD.** Test flagi **3/3 PASS**; pełny pakiet
`src/toolPacks/__tests__/` **370/370 PASS** (`--retry=0`). OFF zwraca **tę samą referencję**
obiektu co HEAD (`toBe(dynamicSwotPack)`), ON daje
`mission, input, swot, insights, recommendations, outputs, review`.

**D2 — DOWÓD MUTACYJNY fail-closed (mój).** Zamieniłem `parse()` z listy dozwolonych
(`'1','true','on'`) na negację listy zakazanych (fail-open). Wynik: **1 failed | 2 passed**,
czerwienieje `brak zmiennej zachowuje dokładnie pięć dotychczasowych faz`.
**Test broni samego zabezpieczenia, nie mechanizmu.** Plik przywrócony.

**D3 — kadry SWOT flaga ON/OFF × light/dark, kanonicznym narzędziem, obejrzane.**
`scripts/dev/grafika-zrzuty.mjs`, ekran `tools-swot-session-workspace`, vite 5454;
OFF bez zmiennej, ON z `VITE_VF1_DYNAMIC_SWOT_SEVEN_STAGES=true` (restart harnessu).
Wynik: **SHA-256 identyczne co do bitu** dla pary ON/OFF w obu motywach
(`4cb0d5ba…` dark, `a490c4a5…` light). Light i dark różne między sobą — motyw działa.
Oglądnięte: lewe drzewo sesji pokazuje **pięć** etapów (Misja i kontekst · Wejście i eksploracja ·
Budowa SWOT · Synteza i wnioski · Wyniki i działania) **także przy fladze ON**.
„Brak zmiany bez flagi” — potwierdzony. „Zmiana z flagą” — **nie istnieje**.

**D4 — ★ ZNALEZISKO: `src/toolPacks/` nie ma ANI JEDNEGO konsumenta.** Sprawdzone czterema
sposobami: (a) grep `getToolPack|listToolPacks|TOOL_PACKS` w całym repo poza `node_modules`
— trafienia **wyłącznie** w `src/toolPacks/registry.ts` i w jego własnych testach;
(b) grep importów `toolPacks` z `src`, `server`, `dev-render` z wyłączeniem samego katalogu
— **zero trafień**; (c) grep importu dynamicznego `import(...toolPacks)` — **zero**;
(d) kanoniczny ekran `tools-swot-session-workspace.tsx` montuje `DiscoveryToolsHub`, który
tego katalogu nie dotyka; realny runtime pięciu kroków siedzi w
`src/components/DiscoveryTools/toolCompletion.ts:15` (twardy union
`'mission' | 'input' | 'swot' | 'insights' | 'outputs'`), nietknięty.

**D5 — ★ kruchość, która dziś śpi.** `getDynamicSwotPackForCurrentFlags()` składa fazy przez
`slice(0, outputsIndex)` + `outputs` + `review`. Każda faza występująca **po** `outputs`
zostaje **cicho zgubiona** przy ON. Symulacja: wejście
`mission,input,swot,insights,outputs,przyszla-faza` → wyjście
`mission,input,swot,insights,recommendations,outputs,review`, zgubione: `przyszla-faza`.
Dziś `outputs` jest ostatnia, więc defekt jest uśpiony — ale bez asercji i bez testu.

### 4.3 Rozbieżność wagi ciężkiej

Dokument 306 stwierdza: „Wszystkie pięć modułów `src/config/swot/` ma konsumentów poza własnym
katalogiem … **Nie ma »biblioteki bez wywołania«**”. To zdanie jest prawdziwe o `src/config/swot/`
— i **nieprawdziwe o warstwie, w której Codex wykonał zmianę**. Zmierzył konsumentów sąsiedniego
katalogu i uogólnił wniosek na własną robotę. Jedenasty kształt fałszywego „gotowe” występuje —
tyle że o jeden katalog dalej, niż patrzył pomiar.

Skutek praktyczny: **flagi 306 nie da się „włączyć właścicielowi”, bo nie ma czego włączyć.**
Zrzuty ON i OFF są identyczne co do bitu.

### 4.4 Co zostało do dokończenia (306)

- **Przewód** — podłączyć `toolPacks/registry` do runtime albo przenieść oba etapy do
  `src/components/DiscoveryTools/toolCompletion.ts` (union 5 id) i store sesji. **Bez tego
  reszta pozycji jest bezprzedmiotowa.**
- **R4** — sesja przez 7 etapów + wznawialność + zimny odczyt osobnym klientem na realnej bazie.
- **R5** — 4 kadry, które **pokażą różnicę** (dzisiejsze pokazują jej brak).
- **D5** — asercja/test na fazy po `outputs`.
- Napisy pl/en nowych faz są **inline w packu**, poza `public/locales` — bramki i18n ich nie
  obejmą; do rozstrzygnięcia przy podłączaniu przewodu.

## 5. Kontrola scalenia — wszystkie trzy gałęzie

| Kontrola | 304 | 305 | 306 |
|---|---|---|---|
| `git merge-tree --write-tree --messages` → `grep -c CONFLICT` | **0** | **0** | **0** |
| esbuild zmienionych plików | n/d (0 plików kodu) | n/d (0 plików kodu) | **4/4 OK** |
| liście i18n (`public/locales`, `src/i18n`) | 0 zmian | 0 zmian | 0 zmian |
| `initiativeRecordCanon` | 8/8 | 8/8 | 8/8 |
| `check-artefakt` | ✓ 9/9 baseline | ✓ 9/9 baseline | ✓ 9/9 baseline |
| `check-list-canon` | ✓ 368/368 baseline | ✓ 368/368 baseline | ✓ 368/368 baseline |
| migracje strict od zera + idempotencja | ✓ / `Applying migrations: 0` | ✓ | ✓ |

Ryzyko regresji: **304 i 305 zerowe** (nie dotykają plików wykonywalnych).
**306 zerowe przy fladze OFF** — dowiedzione podwójnie: identyczna referencja obiektu w teście
i identyczny SHA-256 zrzutów.

## 6. Zrzuty do pokazania właścicielowi

Wszystkie w `evidence/grafika/`, wykonane kanonicznym `scripts/dev/grafika-zrzuty.mjs`,
każdy obejrzany przeze mnie przed wpisaniem do raportu.

**★ PIERWSZORZĘDNE — wymaga decyzji Piotra (to jest pytanie, po które istniał dyżur 305):**
- `odbior-f-305-kontraktOFF/karta-initiative__PRZED__pl__1440__light.png` — 15 sekcji, 4 grupy
- `odbior-f-305-kontraktON/karta-initiative__PO__pl__1440__light.png` — **4 sekcje, 2 grupy**
- oraz para `…__dark.png` obu katalogów.

Pytanie do właściciela, jednym zdaniem: *czy karta inicjatywy ma po włączeniu kontraktu kart
pokazywać cztery sekcje (Zakres, Zadania, Kryteria sukcesu, KPI) zamiast dzisiejszych piętnastu
— znikają całe grupy „Decyzje i ryzyko” i „Ludzie”?* Bez tej odpowiedzi flaga nie powinna
zostać włączona nigdy.

**DRUGORZĘDNE — dowód braku efektu, nie materiał do akceptu:**
- `odbior-f-306-flagaOFF/tools-swot-session-workspace__PRZED__pl__1440__{light,dark}.png`
- `odbior-f-306-flagaON/tools-swot-session-workspace__PO__pl__1440__{light,dark}.png`
  — identyczne co do bitu z parą OFF; pokazać wyłącznie jako dowód, że dyżur 306
  nie zmienia produktu.

**304: brak materiału wizualnego** — R5 świadomie nie wykonany, i słusznie.

## 7. Rozbieżności między raportami Codexa a moim pomiarem

**R1 (306, najcięższa).** Cytat z `docs/program/prototypy/SWOT_SIEDEM_ETAPOW_20260903.md`:
„Wszystkie pięć modułów `src/config/swot/` ma konsumentów poza własnym katalogiem … **Nie ma
»biblioteki bez wywołania«**”. Pomiar: katalog, w którym Codex wykonał zmianę —
`src/toolPacks/` — nie ma **ani jednego** konsumenta poza własnymi testami (4 niezależne
sprawdzenia, §4.2 D4). Zdanie jest prawdziwe o sąsiednim katalogu i fałszywe o własnej robocie.

**R2 (305).** Cytat z `CODEX_DAY305_STANDARD_KART_7_TYPOW_REPORT.md`: „R3 — STOP — nie
wprowadzono zmiany wizualnej **bez bezpiecznego, lokalnego harnessu pokazującego rzeczywisty
`InitiativeDocumentView`**”. Pomiar: harness istnieje (`dev-render/screens/karta-initiative.tsx`
montuje realny komponent), flaga istnieje i jest domyślnie OFF
(`isInitiativeCardContractEnabled`, `?cardContract=1`), a para PRZED/PO wychodzi **różna** —
wykonałem ją w kilkanaście minut bez pisania kodu. STOP był nieuzasadniony, i przykrył
znalezisko z §3.4.

**R3 (305).** Cytat z tego samego raportu: „**7 wpisów typu w rejestrze; 8 ekranów `karta-*`
w dev-render (jawny rozjazd pomiarowy)**”. Pomiar: rozjazdu nie ma — `karta-task.tsx`
i `karta-task-pelna.tsx` to dwa ekrany tego samego typu Task. Rozbieżność zgłoszona jako
otwarta, a rozstrzygalna jednym `ls`.

**R4 (305, wobec kanonu).** Macierz „7 typów” nie pokrywa się z §13.1 archetypu C, który
wymienia 11 artefaktów rekordowych; przecięcie wynosi 4. R2 wymagał zgodności z §13 —
dokument nie cytuje ani jednego paragrafu (§3.5).

**R5 (304, na korzyść Codexa).** Raport zapisuje R4 jako CZĘŚCIOWE z powodu braku dowodu
mutacyjnego. Wykonałem ten dowód: wypada **pozytywnie** — skasowanie filtra organizacji
czerwieni dokładnie test wycieku cross-org, przy zielonej połówce pozytywnej.
Ocena Codexa była **zaniżona**, nie zawyżona.

## 8. Higiena

Kontener `cx-odbior-f-pg` usunięty z wolumenem. Procesy vite (PID 32362, 35105, 38644)
zamknięte po numerze — bez `pkill`/`killall`. Mutacje w
`server/src/routes/conversations.routes.ts` i `src/utils/dynamicSwotSevenStagesFlag.ts`
przywrócone, `git status` czysty przed commitem raportu. `junit.xml` (produkt uboczny
vitest w katalogu repo) usunięty. Katalog `/Users/piotrwisniewski/Developer/Consultify`
nietknięty poza symlinkiem `node_modules` (odczyt). `/private/tmp/m03` nietknięty.
Nic nie scalono i nic nie wypchnięto.
