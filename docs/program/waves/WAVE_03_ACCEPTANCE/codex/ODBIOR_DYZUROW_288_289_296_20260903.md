# ODBIÓR ADWERSARYJNY — dyżury 288, 289, 296 (2026-09-03)

Odbierający: Opus, sesja odbioru B. Baza odbioru: `/private/tmp/m03` HEAD `416432abaf`.
Worktree odbioru: `/private/tmp/ag-odbior-b` (gałąź `agent/odbior-b-20260903`), worktree per gałąź:
`/private/tmp/ag-ob-288`, `/private/tmp/ag-ob-289`, `/private/tmp/ag-ob-296`.
Baza pomiarowa: kontener `ag-odbior-b-pg` (`pgvector/pgvector:pg16`), port hosta **6312**,
bazy `cx288`, `cx289`, `cx289m` — każda z PEŁNEGO łańcucha migracji od zera, strict, bez `--safe`.
Zmienne każdego przebiegu: `NODE_ENV=test DB_TYPE=postgres MOCK_DB=false RUN_DB_TESTS=1
ENABLE_TEST_AUTH_BYPASS=false ENABLE_V8_GLOBAL=true JWT_SECRET=<lokalny>` + `--retry=0`.

**Nie scalałem i nie pushowałem niczego. Nie dotykałem katalogu właściciela.**

---

## WERDYKTY

| Dyżur | Werdykt | Jednym zdaniem |
| --- | --- | --- |
| **288** finanse — bramka modułu | **SCALIĆ Z ZASTRZEŻENIEM** | Naprawa działa i jest przeze mnie potwierdzona na 8 trasach z 7 prefiksów w obie strony, ale gałąź zostawia 2 testy czerwone, które na HEAD są zielone. |
| **289** martwe trasy / help | **SCALIĆ** | Jedyny z trójki, który po moim własnym pomiarze spełnia rdzeń instrukcji: schemat po pełnym łańcuchu od zera zgadza się z kodem, dwie niezależne mutacje dają czerwień. |
| **296** wycieki błędów tras | **SCALIĆ Z ZASTRZEŻENIEM — DYŻUR NIEWYKONANY** | R1–R2 wykonane, R3–R6 nie: **0 z 294** miejsc zamienione, bezpiecznika nie ma, raportu nie ma, mapper ma **zero wołaczy**. |

---

## Kontrola scalenia (wspólna)

| Gałąź | `merge-tree \| grep -cE '^(<<<<<<<\|CONFLICT\|changed in both)'` | `merge-tree --write-tree` | Pliki gałęzi tknięte też na HEAD | Zmiany w `src/` + `public/locales` |
| --- | ---: | --- | ---: | ---: |
| `codex/day288-finanse-bramka-modulu-20260903` | **0** | rc=0, drzewo `024a808600` | 0 | 0 |
| `codex/day289-martwe-trasy-watchers-help-20260903` | **0** | rc=0, drzewo `4b6fb4c87b` | 0 | 0 |
| `codex/day296-wycieki-bledow-tras-20260903` | **0** | rc=0, drzewo `4eaaaa03bd` | 0 | 0 |

- Baza 288/289 = `17dfbc0c8a` (HEAD o **101** commitów do przodu), baza 296 = `984d3658fd` (HEAD o **27**).
- **Konflikt z usunięciem `NotificationSettingsV2` przez nadzorcę nie wystąpi.** Nadzorca usunął katalog
  commitem `e6c236c0dd` („usuń: NotificationSettingsV2/ (niepodłączony) — decyzja A5, 8 plików, 1382 linii");
  gałąź 289 nie dotyka ani jednego pliku w `src/`. Werdykt 289 („MARTWY komponent, zero kodu A")
  i akcja nadzorcy są zgodne.
- **Liście i18n**: żadna z trzech gałęzi nie rusza `public/locales`. Stan bazowy HEAD:
  `en=32314`, `pl=34303`, brak w `pl`=16, brak w `en`=2005 — bez zmian po stronie gałęzi.
- **`initiativeRecordCanon`**: 12/12 PASS na HEAD; żadna gałąź nie dotyka `src/`, więc bez wpływu.
- **esbuild**: 9 z 9 zmienionych plików TS/MJS kompiluje się bez błędu
  (jedno ostrzeżenie `assign-to-define` w `appErrorMapper.test.ts` — przypisanie do `process.env.NODE_ENV`
  w teście, nieszkodliwe).

---

# DYŻUR 288 — bramka `MODULE_ECONOMICS` na `/api/v8/finance*`

Commity: `bfce508b8a` (rejestr) → `e9a3cfb983` (naprawa) → `bc67874551` (raport). 6 plików, 564 linie, 0 usunięć.

## Tabela R1…R6 → raport → commity

| Poz. | Czego żądała instrukcja | Co jest w commitach | Co potwierdziłem WŁASNYM pomiarem | Ocena |
| --- | --- | --- | --- | --- |
| R1 | Inwentarz KAŻDEJ trasy rodziny + łańcuch montowania + wołacze w `src/` + źródło statusu | `REJESTR_TRAS_FINANSE_BRAMKA_20260903.md`, 270 wierszy, łańcuch z numerami linii | Policzyłem sam: `finance` 34/51, `finance-value` 9/16, `financeValueRoutes` 0/6, `finance-intelligence` 2/11, `finance-planning` 0/17, `finance-valuation` 0/19, `finance-v2/**` 46/59 → **91 GET + 179 zapis = 270**. Mianownik nadzorcy potwierdzony co do jednego | **ZALICZONE** |
| R2 | Para kodów USER/OWNER **dla każdej trasy** z realnego serwera, PRZED | Raport podaje PRZED tylko dla 3 tras, reszta `N/Z`; sam autor pisze „reprezentatywny, nie tabela wszystkich 270" | Odtworzyłem PRZED mutacyjnie (patrz niżej): bez bramki USER dostaje **200 z treścią** na `/finance/settings`, `/finance/models`, `/finance/statements` | **CZĘŚCIOWE — uczciwie nazwane** |
| R3 | Jeden middleware na wzorcu ścieżki, `verifyToken` PRZED `createModuleGate`, przed pierwszym mountem finansów; to samo na powierzchni sprawozdań | `v8/index.ts` linia 123 (przed mountami 129–140), `financeStatementMountedSurface.ts` `moduleGate` tuż po `verifyToken` | `v8Router.use(verifyToken)` jest w linii **61**, bramka w **123**, pierwszy mount finansów w **129** — kolejność poprawna, kształt „zamknięte przez wygaszenie" **nie wystąpił** | **ZALICZONE** |
| R4 | Para PO dla każdej trasy + dowód mutacyjny | Test 4/4; mutacja 1/4 PASS | Uruchomiłem sam: 4/4 PASS; własna sonda 8 tras/7 prefiksów **8/8 par poprawnych**; mutacja obu bramek → rejestr **45/50** (5 czerwonych) i sonda pokazuje przeciek | **ZALICZONE PONAD RAPORT** |
| R5 | (a) rejestr `betaGateMountRegistry.test.ts` rozszerzony, mutacja → czerwony; (b) nowy `.pg.test.ts` USER 403 / OWNER 200 | Dwa wpisy `MODULE_ECONOMICS` + test wzorca; nowy `financeRoutes.moduleGate.pg.test.ts` | Rejestr 50/50 PASS; po mutacji 45/50 → bezpiecznik **realnie broni**. Zastrzeżenie do (a) niżej | **ZALICZONE Z WADĄ** |
| R6 | Raport z rozbieżnościami, STOP-pytaniami, wyjściem mutacji, zdaniem do D7 | `CODEX_DAY288_FINANSE_BRAMKA_MODULU_REPORT.md`, werdykt własny **PARTIAL**, sekcja TWIERDZENIA NIEZWERYFIKOWANE, zdanie D7 | Raport jest uczciwy: sam odmawia `VERIFIED`. Brakuje jednej rzeczy — patrz zastrzeżenie Z1 | **ZALICZONE Z BRAKIEM** |

## Trzy dowody uruchomione przeze mnie

**Dowód 1 — para USER/OWNER na 8 trasach z WSZYSTKICH 7 prefiksów** (własny plik sondy, `cx288`, realny `ApiGateway`,
podpisane JWT, dwaj użytkownicy w jednej organizacji; plik sondy usunięty po pomiarze):

```
GET  /api/v8/finance/settings                        | USER=403/BETA_LOCKED | OWNER=200/-
GET  /api/v8/finance/models                          | USER=403/BETA_LOCKED | OWNER=200/-
GET  /api/v8/finance/statements                      | USER=403/BETA_LOCKED | OWNER=200/-
GET  /api/v8/finance-intelligence/packs/probe/tie-out| USER=403/BETA_LOCKED | OWNER=404/-
POST /api/v8/finance-planning/cash-forecast          | USER=403/BETA_LOCKED | OWNER=400/CASH_FORECAST_INVALID_PERIODS
POST /api/v8/finance-valuation/monte-carlo-npv       | USER=403/BETA_LOCKED | OWNER=400/-
POST /api/v8/finance/value-tracking/ledger/baselines | USER=403/BETA_LOCKED | OWNER=400/VALUE_LEDGER_BAD_INPUT
POST /api/v8/finance-v2/models/probe/approve         | USER=403/BETA_LOCKED | OWNER=404/-
```

Instrukcja żądała pary na **co najmniej 5** trasach `/api/v8/finance*`. Zmierzyłem **8**, po jednej z każdego
z siedmiu prefiksów. **Na żadnej trasie OWNER nie dostał 403 `BETA_LOCKED`** — kształt „zamknięte przez
wygaszenie" nie wystąpił. Trzy trasy zwróciły OWNER-owi **200 z treścią**, pozostałe 400/404 z handlera,
czyli bramka przepuściła i odbił się dopiero walidator.

**Dowód 2 — mutacja obu bramek** (usunąłem blok `v8Router.use((req,res,next) => …FINANCE_MODULE_PATH…)`
w `v8/index.ts` ORAZ `dependencies.moduleGate,` w `financeStatementMountedSurface.ts`):

```
rejestr betaGateMountRegistry.test.ts:  50/50 PASS  →  45 PASS / 5 FAIL
sonda 8 tras po mutacji:
GET  /api/v8/finance/settings   | USER=200/- | OWNER=200/-
GET  /api/v8/finance/models     | USER=200/- | OWNER=200/-
GET  /api/v8/finance/statements | USER=200/- | OWNER=200/-
POST /api/v8/finance-planning/cash-forecast | USER=400/CASH_FORECAST_INVALID_PERIODS | ...
```

To jest zarazem brakujący **dowód PRZED**: zwykły `USER` bez uprawnienia do zamkniętego modułu czytał
ustawienia finansowe, listę modeli i sprawozdania organizacji **z kodem 200 i treścią**. Po przywróceniu
plików rejestr znów 50/50. Pliki przywrócone (`git status --short` pusty).

**Dowód 3 — jedyny konsument spoza modułu nie został odcięty.** Rola `ADMIN`, realna baza, realny Gateway:

```
GET /api/v8/finance/settings  → 200
PUT /api/v8/finance/settings  → 400 IDEMPOTENCY_KEY_REQUIRED   (bramka przepuściła, odbił walidator)
```

Dodatkowo statycznie: `MODULE_ECONOMICS: 'closed'` w `server/src/sharedRuntime/utils/betaMenuStatus.ts:52`,
**bez jakiegokolwiek nadpisania z `process.env`** w tym pliku — bramka jest deterministyczna, nie ma
tu pułapki „flaga OFF w kodzie ≠ wyłączona".

## Zastrzeżenia (warunki przed scaleniem)

**Z1 — gałąź zostawia DWA testy czerwone, które na HEAD są zielone. To jest warunek blokujący.**

```
HEAD (bez zmian 288):  server/src/routes/v8/__tests__/financeStatementMountedSurface.test.ts  →  23/23 PASS
gałąź 288:             ten sam plik                                                            →  21 PASS / 2 FAIL
```

Czerwone przypadki:
- `reaches only exact flags and Statement handlers when global and org V8 gates are disabled` — `expected 200 "OK", got 403 "Forbidden"`
- `uses the real Finance membership/editor wall before handlers while V8 gates are disabled` — `expected { …(2) } to match object { code: 'FINANCE_EDIT_FORBIDDEN' }`

Oba wynikają z tego, że zastany test montuje powierzchnię ze stubem użytkownika **bez roli**, a nowa bramka
słusznie odbija taki byt 403 `BETA_LOCKED`. Raport 288 wspomina o tym jednym zdaniem („71/73… nie osłabiłem
asercji"), ale **nie nazywa tego blokerem CI ani nie zostawia zadania**. Naprawa jest mała (stub ma dostać
rolę `OWNER`/`ADMIN` albo oczekiwać 403), ale **musi wejść przed scaleniem**, bo inaczej nadzorca scala
czerwony pakiet.

**Z2 — test wzorca w bezpieczniku kopiuje literał zamiast importować stałą produkcyjną.**
`tests/unit/backend/security/betaGateMountRegistry.test.ts` deklaruje u siebie
`const financeModulePath = /^\/finance(?:-|\/|$)/;` zamiast zaimportować `FINANCE_MODULE_PATH`
z `server/src/routes/v8/index.ts` (a ta stała jest wyeksportowana właśnie po to). Skutek: gdyby ktoś
zawęził wzorzec w kodzie produkcyjnym, ten test zostanie **zielony**. Bezpiecznikiem realnym pozostaje
wpis kotwicowy `MODULE_SURFACES` (mój dowód 2 pokazuje, że on działa) — ale test wzorca w obecnej postaci
niczego nie broni. Do poprawienia przy okazji, nie bloker.

**Z3 — 266 z 270 tras nadal bez indywidualnego pomiaru.** Autor to deklaruje. Moja sonda podnosi pokrycie
z 4 do 12 tras (8 moich + 4 jego) i pokrywa wszystkie 7 prefiksów, ale D7 **nie wolno zamykać** jako
„270/270 zweryfikowane".

---

# DYŻUR 289 — martwe trasy watchers + schemat pomocy

Commity: `820b8d0a5b` (kod + migracja + test) → `fe3d43cb03` (rejestr) → `097e8f4317` (raport). 5 plików, +302/−114.

## Tabela R1…R6 → raport → commity

| Poz. | Czego żądała instrukcja | Co jest w commitach | Co potwierdziłem WŁASNYM pomiarem | Ocena |
| --- | --- | --- | --- | --- |
| R1 | A: inwentarz watcherów i wołaczy; B: `\d` trzech tabel po pełnym łańcuchu + tabela rozjazdu + lista cichych `catch` | `REJESTR_MARTWE_TRASY_20260903.md` + tabela 5 kolumn BRAK w raporcie | Odtworzyłem `\d` na własnej bazie od zera (patrz dowód 1) | **ZALICZONE** |
| R2 | Dowód PRZED z realnego serwera, kody + ciała + log z połkniętym błędem | Tabela 5 żądań; korekta tezy: handler nie zwracał `stored:false`, tylko `stored:true` przy odrzuconym INSERT | Odtworzyłem PRZED mutacyjnie: bez migracji log serwera pokazuje `column "article_id" of relation "help_events" does not exist` | **ZALICZONE** |
| R3 | Migracja ADDYTYWNA pięciu kolumn, usunięcie DDL w locie w TYM SAMYM commicie, `:274-278` przestaje zwracać `200 stored:false`, wyrównanie kontraktu `HelpContext.tsx:299` po stronie serwera, każdy cichy `catch` → log albo 5xx | `20260904_help_shape_alignment.sql` (5× `ADD COLUMN IF NOT EXISTS` + backfill + 2 indeksy); `ensureHelpSchema` usunięty w całości w commicie `820b8d0a5b`; 4× `.catch(() => [])` → log + `throw`; `/events` przyjmuje `playbookKey/context`, sprawdza `result.success`, zwraca 500 przy porażce | Wszystkie 5 kolumn są w bazie po łańcuchu od zera; `help_categories` istnieje z migracji, więc usunięcie DDL w locie **nie zostawia dziury**; front faktycznie wysyła `{playbookKey, eventType, …}` (`src/contexts/HelpContext.tsx:299-307`) | **ZALICZONE** |
| R4 | A: potwierdzenie, że to martwy komponent, ZERO kodu | Werdykt MARTWY, STOP-pytanie merytoryczne, zero zmian w `src/` | `git diff --name-only … -- src` = **0 plików**. Niezależne potwierdzenie: nadzorca usunął ten katalog commitem `e6c236c0dd` | **ZALICZONE** |
| R5 | Te same wywołania PO → 200; zimny odczyt osobnym `pg`; mutacja: świeża baza bez migracji → test czerwony | Test `help.routes.schema.pg.test.ts` z zimnym readbackiem przez własny `Pool`; mutacja przez usunięcie jednej linii `ADD COLUMN … article_id` | Wykonałem **dwie niezależne mutacje**, obie czerwone (dowód 2 i 3) | **ZALICZONE PONAD RAPORT** |
| R6 | Raport z rozbieżnościami, tabelami PRZED/PO, STOP z kosztem, TWIERDZENIA NIEZWERYFIKOWANE, zdania D5/D6 | Wszystko obecne, w tym korekta tezy nadzorcy | — | **ZALICZONE** |

## Trzy dowody uruchomione przeze mnie

**Dowód 1 — `\d` po PEŁNYM łańcuchu migracji od zera** (`cx289`, strict, bez `--safe`, drugi przebieg 0 migracji):

| Tabela | Kolumny czytane przez `help.routes.ts` | Stan po łańcuchu |
| --- | --- | --- |
| `help_articles` | `category_id`, `body`, `status`, `updated_at` | **wszystkie obecne** (`status` z `DEFAULT 'published'`, indeksy `idx_help_articles_category_id`, `idx_help_articles_status`) |
| `help_events` | `user_id`, `organization_id`, `event_type`, `article_id`, `metadata`, `playbook_key`, `event_data`, `route` | **wszystkie obecne** |
| `help_categories` | `id`, `name`, `sort_order` | **obecne** — tabela pochodzi z migracji, nie z DDL w locie |

Test `server/src/routes/__tests__/help.routes.schema.pg.test.ts`: **2/2 PASS** na mojej bazie.

**Dowód 2 — mutacja migracji (świeża baza `cx289m` BEZ pliku `20260904_help_shape_alignment.sql`, cały łańcuch od zera):**

```
× stores the live HelpContext payload and exposes it to a cold pg readback
× returns a migrated legacy article instead of silently converting a schema error to an empty list
   → column "category_id" of relation "help_articles" does not exist
Test Files  1 failed (1)   Tests  2 failed (2)
```

To jest mutacja **mocniejsza niż ta z raportu** (autor usunął jedną kolumnę, ja cały plik) i przy okazji
pokazuje naprawę cichych `catch` w działaniu — w logu serwera stoi teraz:
`[Help] Failed to persist help event {"error":"column \"article_id\" … does not exist", …}`,
a nie milcząca pusta lista.

**Dowód 3 — mutacja KODU (baza z migracją, `help.routes.ts` przywrócony do wersji z bazy `17dfbc0c8a`):**

```
× stores the live HelpContext payload …
   → expected { playbook_key: null, …(3) } to match object { playbook_key: 'day289', …(2) }
✓ returns a migrated legacy article …
Tests  1 failed | 1 passed (2)
```

Ta mutacja rozstrzyga zarzut „test broni tylko migracji, nie kodu": pierwszy przypadek broni **wyrównania
kontraktu** (`playbookKey/context` → `playbook_key/event_data/route`), drugi broni **migracji**. Każdy
z dwóch składników naprawy ma swój czerwony.

## Zastrzeżenie (nieblokujące)

**Z4 — brak testu wprost na „cichy `catch` już nie milczy".** Cztery `.catch(() => [])` zamieniono na
log + `throw`, ale żaden test nie asertuje, że `GET /api/help/articles` przy błędzie schematu zwraca **5xx**
zamiast `200` z pustą listą. W moim dowodzie 2 widać to w logu, nie w asercji. Do dołożenia jednym `it`
przy okazji — nie warunek scalenia.

---

# DYŻUR 296 — wycieki treści błędów z tras

Commity: `59204e2ad8` (rejestr) → `338b14aa19` (mapper + test + codemod). 4 pliki, +602/−0.
**Raportu `CODEX_DAY296_…` NIE MA na gałęzi ani nigdzie w repo.**

## Tabela R1…R6 → raport → commity

| Poz. | Czego żądała instrukcja | Co jest w commitach | Mój pomiar | Ocena |
| --- | --- | --- | --- | --- |
| R1 | Tabela plik · linia · klasa · co może wyciec; istniejący mapper AI; kto na froncie czyta `error` | `docs/program/waves/WAVE_03_ACCEPTANCE/REJESTR_WYCIEKI_BLEDOW_TRAS_20260903.md` — 341 wierszy, klasyfikacja konserwatywna, sekcje „Kody do decyzji" i „Front czyta `error`" (91 trafień) | Rejestr istnieje i jest rzetelny; **każdy** wiersz w kolumnie PO ma napis `oczekuje na codemod` | **ZALICZONE** |
| R2 | Mapper: `errorCode`, komunikaty pl/en po `Accept-Language`, `correlationId` w odpowiedzi i logu, pełna treść tylko do logu, `debug` wyłącznie w dev, klasy bezpieczne (`AppError`) przechodzą, testy + negatywny | `server/src/middleware/appErrorMapper.ts` (104 linie) + `__tests__/appErrorMapper.test.ts` (5 przypadków, w tym `never includes debug detail in production`) | Uruchomiłem: **5/5 PASS**. Test negatywny jest i realnie sprawdza `production` | **ZALICZONE** |
| R3 | Codemod w repo **oraz zastosowanie** w grupach po 10 plików, commit per grupa | `scripts/dev/codemod-error-mapper.mjs` istnieje; **zero commitów grupowych**, zero zmienionych tras | `git grep -cE "error: \(err(or)? as Error\)\.message\|error: err(or)?\.message" -- server/src/routes`: **gałąź = 62 pliki / 294 wystąpienia**, **HEAD = 62 pliki / 294 wystąpienia**. Różnica: **0** | **NIEWYKONANE** |
| R4 | `tests/unit/backend/security/noRawErrorMessage.test.ts` z linią bazową 0 + osiem tras `ai.routes.ts` z testem kontraktu | Pliku nie ma | `ls tests/unit/backend/security/ \| grep -i raw` → pusto | **NIEWYKONANE** |
| R5 | Kontener 6300, drugi worktree na `984d3658fd` = PRZED, 10 tras, para PRZED/PO z `correlationId`, JSON w artefaktach | Brak jakiegokolwiek śladu | — | **NIEWYKONANE** |
| R6 | Raport: tabela 305 → 0, klasy, kody do decyzji, TWIERDZENIA NIEZWERYFIKOWANE | **Raportu nie ma** | `find` po `CODEX_DAY296` → 0 trafień | **NIEWYKONANE** |

## Trzy dowody uruchomione przeze mnie

**Dowód 1 — ile z rodziny faktycznie zamieniono.** Tą samą komendą na obu stronach:

```
gałąź 296 : plikow=62  wystapien=294
HEAD      : plikow=62  wystapien=294
```

**Zamienionych: 0 z 294.** Prawo zatrzymania z instrukcji dopuszczało „codemod zastosowany na 40 z 69 plików,
reszta wypisana". Zastosowano na **0**.

**Dowód 2 — mapper nie ma ani jednego wołacza produkcyjnego.**

```
git grep -n "appErrorMapper\|mapAppError\|toAppErrorResponse" -- server src scripts
  scripts/dev/codemod-error-mapper.mjs:21,50,52,53   ← wyłącznie jako STRING w codemodzie
(zero trafień w server/src poza samym plikiem mappera i jego testem)
```

To jest **„biblioteka bez wywołania"** (jedenasty kształt fałszywego gotowe): zielone testy, dowód
jakości kodu, zero konsumentów. Funkcja `mapAppErrorResponse` jest wyeksportowana i martwa.

**Dowód 3 — testy mappera przechodzą i test negatywny jest realny.**

```
✓ keeps raw database detail in the log and returns a safe English response
✓ selects Polish safe messages from Accept-Language
✓ preserves an explicit operational AppError message and code
✓ never includes debug detail in production
✓ includes debug detail only in development
Tests  5 passed (5)
```

Sam mapper jest dobrze zrobiony: klasyfikuje po `statusCode`/`code`, ma `correlationId`, `debug` tylko
poza produkcją, przepuszcza własny `AppError`. **Wada nie jest w mapperze, tylko w tym, że nikt go nie woła.**

## Zastrzeżenie

**Z5 — dyżur 296 jest NIEWYKONANY i nie wolno nim zamykać żadnego wiersza wycieków.** Gałąź nadaje się
do scalenia jako **materiał wejściowy** (rejestr 341 pozycji + przetestowany mapper + codemod), bo jest
czysto addytywna, merge-clean i nic nie psuje. Ale samo jej scalenie **nie zamyka ani jednego z 294 miejsc**.
Dyżur wymaga wznowienia od R3.

---

# ROZBIEŻNOŚCI Z CYTATEM

## R-1 (dyżur 288) — teza nadzorcy wskazuje plik, w którym nie ma finansów

Instrukcja 288, nagłówek zakresu, cytat:

> „jedyny konsument spoza modułu to `GET/PUT /api/v8/finance/settings` z `AdminSettingsModule.tsx:497`
> (rola admin, którą bramka przepuszcza)"

Mój pomiar: `git grep -n "finance" -- src/views/admin/AdminSettingsModule.tsx` → **0 trafień**, tak samo
na HEAD. Wołanie jest **pośrednie**: `AdminSettingsModule.tsx` renderuje w okolicy linii 497
`<AdminOrganizationDefaultsPanel organizationId={currentUser.organizationId} />`, a dopiero
`src/components/Admin/AdminOrganizationDefaultsPanel.tsx:5` importuje `V8FinanceApi`.
**Skutek tezy jest poprawny** (potwierdziłem: ADMIN → GET 200, PUT dochodzi do walidatora), ale
zacytowana linia nie zawiera wołacza. Rejestr 288 nie wymienia `AdminSettingsModule` ani razu, więc
Codex tej rozbieżności nie rozliczył — potwierdził tylko ogólnie, że „nie znalazł wołacza z modułu otwartego".

Przy okazji znalazłem drugiego importera `V8FinanceApi` poza modułem:
`src/components/settings/OrganizationSettings.tsx:21` (woła `V8FinanceApi.getSettings()` i `updateSettings()`).
Ten plik ma **zero importerów** w `src/` — to kolejny martwy komponent tej samej klasy co
`NotificationSettingsV2`. Nie jest zagrożeniem dla bramki (nikt go nie renderuje), ale wart osobnego wpisu
w rodzinie „martwe rodziny do usunięcia".

Trzy pliki `src/components/Benefits/*Workspace.tsx` też importują klientów finansów — sprawdziłem,
kto je renderuje: **wyłącznie** `src/components/Economics/FinanceHub.tsx` i ekrany `src/components/Finance/**`.
Mimo nazwy katalogu należą do powierzchni Finansów, nie do otwartego `MODULE_BENEFITS` (Results/M15).
**STOP-pytania nie ma.**

## R-2 (dyżur 288) — raport nie nazywa blokera CI

Raport 288, cytat:

> „Połączony przebieg z zastanym `financeStatementMountedSurface.test.ts`: 71/73; dwa stare przypadki
> oczekują 200 lub `FINANCE_EDIT_FORBIDDEN` dla stubu bez roli, a po nowej bramce prawidłowo dostają
> 403 `BETA_LOCKED`. Nie osłabiłem asercji."

Zdanie jest prawdziwe, ale **prezentuje regresję jako ciekawostkę**. Mój pomiar tego samego pliku:
**HEAD 23/23 PASS**, **gałąź 288 21 PASS / 2 FAIL**. Gałąź wprowadza dwa nowe czerwone testy do pakietu.
W raporcie nie ma tego ani w sekcji STOP, ani w „TWIERDZENIA NIEZWERYFIKOWANE", ani w zdaniu do D7 —
a nadzorca scalając na ślepo dostanie czerwony pakiet.

## R-3 (dyżur 296) — trzy różne liczby dla tej samej rodziny, żadna nie uzgodniona

| Źródło | Liczba |
| --- | --- |
| Instrukcja 296 / pomiar nadzorcy w `G20_BLOKERY_P0P1_20260903.md` | **305 miejsc w 69 plikach** |
| Rejestr 296, linia 3, cytat: „Pomiar markera `984d3658fd`: **341 miejsc w 71 plikach**" | **341 / 71** |
| Mój pomiar komendą wprost z instrukcji odbioru, na gałęzi i na HEAD | **294 / 62** |

Rozrzut 294–341 przy tym samym markerze oznacza, że **każda strona liczy innym wzorcem**, a żaden dokument
nie podaje użytej komendy obok liczby. Dopóki to nie zostanie uzgodnione, zdanie „305 → 0" w przyszłym
raporcie będzie niemierzalne. Rekomendacja: wpisać do rejestru dosłowną komendę pomiarową obok liczby.

## R-4 (dyżur 289, drobna) — instrukcja cytuje nieistniejący plik

Instrukcja 289, R4, cytat:

> „`sed -n '425,440p' src/components/settings/SettingsView.tsx`"

Tego pliku nie ma. Realny plik to `src/views/SettingsView.tsx` (linia 433 renderuje `NotificationSettings` v1).
Codex użył właściwej ścieżki i podał ją w raporcie, ale **nie zgłosił rozbieżności w sekcji korekt** —
mimo że instrukcja tego wymaga. Wpływu na werdykt brak.

## R-5 (dyżur 288, na korzyść nadzorcy) — mianownik 270 potwierdzony co do jednego

Rzadki przypadek: policzyłem sam i teza nadzorcy („270 tras = 91 GET + 179 zapisowych w 7 prefiksach")
zgadza się dokładnie. Rozbicie w tabeli R1 wyżej.

---

# GOTOWE ZDANIA DO WKLEJENIA

## Do rejestru znalezisk — wiersz D5

> Dyżur 289 potwierdził na realnej bazie 3 martwe wołacze `/api/settings/watchers` w `src/`, 0 tras po stronie
> serwera i realny 404, a werdykt „martwy komponent" niezależnie potwierdziło usunięcie katalogu
> `NotificationSettingsV2` przez nadzorcę (`e6c236c0dd`); odbiór adwersaryjny nie znalazł żywego importera,
> zero kodu w obszarze A — wiersz zamykalny.

## Do rejestru znalezisk — wiersz D6

> Dyżur 289 wyrównał schemat pomocy do kodu migracją addytywną `20260904_help_shape_alignment.sql`
> (5 kolumn + backfill + 2 indeksy), usunął DDL w locie i fałszywy `stored:true` przy odrzuconym INSERT;
> odbiór adwersaryjny potwierdził własnym pełnym łańcuchem migracji od zera obecność wszystkich kolumn
> czytanych przez `help.routes.ts`, 2/2 PASS oraz DWIE niezależne mutacje na czerwono (bez migracji 2/2 FAIL,
> ze starym kodem 1/2 FAIL) — wiersz zamykalny.

## Do rejestru znalezisk — wiersz D7

> Dyżur 288 objął wszystkie 270 tras `/api/v8/finance*` (91 GET + 179 zapisowych, 7 prefiksów) wspólną bramką
> `createModuleGate('MODULE_ECONOMICS')` po `verifyToken`, plus osobną bramką na powierzchni sprawozdań omijającej
> `v8FeatureGate`; odbiór adwersaryjny zmierzył parę USER/OWNER na 8 trasach z 7 prefiksów na realnym Postgresie
> (USER 403 `BETA_LOCKED` w 8/8, OWNER ani razu 403 — brak wygaszenia), potwierdził mutacyjnie, że bez bramki
> `USER` czytał `/finance/settings`, `/finance/models` i `/finance/statements` z kodem 200 i treścią, oraz że
> jedyny konsument spoza modułu (panel admina) nadal dostaje 200; **266 z 270 tras pozostaje bez indywidualnego
> runtime i D7 nie wolno zamykać jako 270/270**.

## Do `G20_BLOKERY_P0P1_20260903.md`

> **T1 (finanse, `/api/v8/finance*`) — ZAMKNIĘTY CO DO MECHANIZMU, otwarty co do pokrycia.** Bramka modułu stoi
> na wszystkich 7 prefiksach i na przedglobalnej powierzchni sprawozdań (gałąź `codex/day288-…`, commit `e9a3cfb983`).
> Dowód odbioru na realnym Postgresie: 8 tras z 7 prefiksów, USER 403 `BETA_LOCKED` 8/8, OWNER 0/8 zablokowany,
> mutacja obu bramek → rejestr 45/50 i USER znów czyta ustawienia finansowe z kodem 200. **Warunek scalenia:
> naprawić 2 przypadki `financeStatementMountedSurface.test.ts`, które gałąź zmienia z zielonych (23/23 na HEAD)
> na czerwone (21/23).** Pozostaje otwarte: 266 tras bez indywidualnego pomiaru oraz 64 zapisy bez ponownego
> odczytu członkostwa (niezweryfikowane w tym dyżurze).

> **T-help (schemat pomocy) — ZAMKNIĘTY.** Gałąź `codex/day289-…`, commit `820b8d0a5b`. Pięć kolumn czytanych
> przez `help.routes.ts` obecnych po pełnym łańcuchu migracji od zera, DDL w locie usunięty, ciche `catch`
> logują i propagują, `POST /api/help/events` nie zwraca już sukcesu przy odrzuconym zapisie. Dwie niezależne
> mutacje na czerwono.

> **T-wycieki (`error: err.message` w trasach) — NIEZMIENIONY, dyżur 296 NIEWYKONANY.** Zamienionych **0 z 294**
> miejsc w 62 plikach (pomiar odbioru: gałąź = HEAD, różnica zero). Powstał poprawny, przetestowany mapper
> `server/src/middleware/appErrorMapper.ts` (5/5 PASS, w tym test negatywny dla `production`) — ale **bez ani
> jednego wołacza produkcyjnego**. Bezpiecznika `noRawErrorMessage.test.ts` nie ma, dowodu runtime nie ma,
> raportu dyżuru nie ma. Rodzina pozostaje otwarta w całości; dyżur do wznowienia od R3.

---

# TWIERDZENIA NIEZWERYFIKOWANE PRZEZ TEN ODBIÓR

- Zachowanie po **scaleniu** gałęzi 288/289 z HEAD oddalonym o 101 commitów: `merge-tree` czysty i zero
  pokrycia plików, ale **nie zbudowałem drzewa scalonego i nie uruchomiłem na nim testów**.
- Pełen `tsc` i pełen pakiet vitest repozytorium: **nie uruchamiane**. Mierzyłem wskazane pliki i pakiety.
- Stan staging / demo / produkcji: **nie dotykany**.
- Teza „64 zapisy bez ponownego odczytu członkostwa" (dyżur 288): **niezweryfikowana**, tak samo jak w raporcie autora.
- `POST /finance-planning/nl-to-model` woła LLM bez limitu: **niezweryfikowane** — poza zakresem odbioru.
- Rejestr 296 (341 wierszy): sprawdziłem wyrywkowo kształt i sumę, **nie weryfikowałem każdej z 341 linii**.
