# G20 — pomiar dwóch blokerów „zero open P0/P1” (03.09.2026)

Pomiar **statyczny** (bez uruchamiania serwera, bez ataków na żywe środowiska) na gałęzi
`agent/g20-p0p1-20260903` z `/private/tmp/m03` HEAD `17dfbc0c8a`.
Wszystkie ścieżki plików liczone od korzenia repo.

Zasada: **jeśli mój pomiar przeczy liczbie z rejestru, obowiązuje mój** — rozbieżności
wypisane jawnie w każdej sekcji.

---

## T1 — bramka modułu na `/api/v8/finance/*`

### T1.0 Jak wygląda „bramka modułu” w tym repo

Nie ma middleware o nazwie `requireModule` / `moduleGate` / `requireModuleAccess` /
`ensureModule` / `checkModule`. Kanoniczna bramka modułu to:

`server/src/middleware/betaGate.middleware.ts:28` → `createModuleGate(moduleId)`

- czyta SSOT `BETA_MENU_STATUS` z `server/src/sharedRuntime/utils/betaMenuStatus.ts`
  (generowany mirror `src/utils/betaAccess.ts` — TA SAMA mapa, którą czyta menu klienta);
- `status === 'open'` → `next()`;
- w przeciwnym razie przepuszcza role `OWNER | ADMIN | ADMINISTRATOR | SUPERADMIN`
  (odpowiednik `BETA_ADMINS_EXEMPT = true` po stronie klienta);
- pozostałym zwraca `403 { code: 'BETA_LOCKED' }`.

**Pułapka konstrukcyjna (udokumentowana w repo, zmierzona 02.09):** `createModuleGate`
czyta `req.user.role`. Jeśli stanie PRZED `verifyToken`, widzi pustą rolę i wygasza moduł
dla WSZYSTKICH, właściciela włącznie (kształt „zamknięte przez wygaszenie”).
Dlatego przy każdym mouncie w `Gateway.ts` stoi `gatewayVerifyToken` PRZED bramką.

`betaGate` (bez `create…`) to **atrapa** — całe ciało to `next()`
(`betaGate.middleware.ts:18-20`). Tak samo `createBetaGate(skipPaths)` (linia 60).
Obecność `betaGate` na mouncie **nie jest bramką**.

### T1.1 Jak są zabezpieczeni sąsiedzi

| Mount | Plik:linia | Bramka modułu |
| --- | --- | --- |
| `/api/economics` | `server/src/Gateway.ts:1228-1233` | `createModuleGate('MODULE_ECONOMICS')` — naprawione 02.09 |
| `/api/finance-statements` | `server/src/Gateway.ts:1444-1454` | `createModuleGate('MODULE_ECONOMICS')` — naprawione 02.09 |
| `/api/financial-modeling` | `server/src/Gateway.ts:1463-1468` | `createModuleGate('MODULE_ECONOMICS')` — naprawione 02.09 |
| `/api/conclusions` | `server/src/Gateway.ts:894` | `createModuleGate('MODULE_CONCLUSIONS')` |
| `/api/meeting` | `server/src/routes/meeting.routes.ts:304` | `closedBetaModuleGate` = `createModuleGate('MODULE_MEETING')` |
| `/api/v8/case-workspace` | `server/src/routes/v8/index.ts:115` | `createModuleGate('MODULE_CASE_WORKSPACE')` — naprawione 02.09 |
| `/api/v8/teresa/case-intake` | `server/src/routes/v8/teresa.routes.ts:451` | `createModuleGate('MODULE_CASE_WORKSPACE')` |
| `/api/v8/chat` (podścieżka case-intake) | `server/src/routes/v8/chat.routes.ts:336` | `caseIntakeModuleGate` |
| **`/api/v8/finance*` (7 prefiksów)** | `server/src/routes/v8/index.ts:120-131` | **BRAK** |
| `/api/v8/finance/statements*` (mount przedrolloutowy) | `server/src/routes/v8/financeStatementMountedSurface.ts:88-96` | **BRAK** |

Uwaga o „sąsiadach v8”: `/api/v8/initiatives` **nie istnieje** — Inicjatywy siedzą na
`/api/initiatives` (`Gateway.ts:680-693`). `/api/v8/execution` i `/api/v8/execution-control`
nie mają bramki modułu, ale ich moduły są w SSOT `open`, więc bramka nie jest im potrzebna.
**Jedynym modułem `closed` bez bramki na swojej powierzchni API jest `MODULE_ECONOMICS`
w przestrzeni `/api/v8`.**

`MODULE_ECONOMICS: 'closed'` — `server/src/sharedRuntime/utils/betaMenuStatus.ts:52`.

### T1.2 Łańcuch middleware, który realnie stoi przed finansami v8

`server/src/Gateway.ts:1545-1546`:

```
app.use('/api/v8', mountedFinanceStatementRouter);      // ← OMIJA v8FeatureGate
app.use('/api/v8', v8FeatureGate, v8Router);
```

`server/src/routes/v8/index.ts:59-89`, kolejność dla `/finance*`:

```
verifyToken → requireV8OrgContext → v8OrgGate → attachV8Context
            → v8MetricsMiddleware → mutationAbortCanary → [finance routers]
```

Czyli przed finansami stoją: **uwierzytelnienie**, **kontekst organizacji z tokena**,
**globalna flaga wdrożenia V8** i **flaga V8 per-organizacja**. Żadne z tych czterech
nie mówi nic o statusie modułu Finanse.

Dwa dodatkowe obejścia zmierzone w kodzie:

1. `mountedFinanceStatementRouter` (`financeStatementMountedSurface.ts:78-98`) jest
   zamontowany PRZED `v8FeatureGate` i celowo go omija dla **5 tras dokładnych + 7 wzorców
   parametrycznych** (linie 12-35). Skutek: nawet przy `ENABLE_V8_GLOBAL=false` cała
   ścieżka Sprawozdań (`GET /finance/statements`, `GET /finance/statements/:id`,
   `…/analytics`, `…/ratios`, `…/source-receipt`, `…/document-intelligence/search`,
   `GET /finance/statement-packs[/:id]`, `GET /finance/canonical-lines`,
   `POST …/upload-and-analyze`, `POST …/{detect,extract,map,confirm}`,
   `POST …/manual-mapping-decisions`, `PUT …/values`) jest osiągalna dla każdego
   zalogowanego, aktywnego członka organizacji. Ten sam zbiór danych, który na
   `/api/finance-statements` dostał 02.09 bramkę `MODULE_ECONOMICS`, na
   `/api/v8/finance/statements` bramki nie ma. **To jest ta sama dziura pod inną ścieżką.**
2. `v8OrgGate` (`v8FeatureGate.middleware.ts:38-47`) ma fallback:
   gdy organizacja nie ma ANI JEDNEGO wiersza flag i `NODE_ENV !== 'production'`,
   przepuszcza z ostrzeżeniem w logu. Na demo/stagingu (`NODE_ENV` ≠ `production`)
   to znaczy: brak wierszy = otwarte.

### T1.3 Inwentarz tras — liczby

Skanowane pliki: wszystkie routery zamontowane pod prefiksami finansowymi w
`server/src/routes/v8/index.ts:120-131`.

| Prefiks (pełna ścieżka) | Plik | GET | ZAPIS (POST/PUT/PATCH/DELETE) | Razem |
| --- | --- | ---: | ---: | ---: |
| `/api/v8/finance` | `server/src/routes/v8/finance.routes.ts` | **34** | 51 | 85 |
| `/api/v8/finance/value` + `/api/v8/finance-value` (ten sam router, 2 mounty) | `server/src/routes/v8/financeValueRoutes.ts` | 0 | 6 | 6 |
| `/api/v8/finance/value-tracking` | `server/src/routes/v8/finance-value.routes.ts` | 9 | 16 | 25 |
| `/api/v8/finance-v2` | `server/src/routes/v8/finance-v2/*.routes.ts` (15 plików) | 46 | 59 | 105 |
| `/api/v8/finance-valuation` | `server/src/routes/v8/finance-valuation.routes.ts` | 0 | 19 | 19 |
| `/api/v8/finance-planning` | `server/src/routes/v8/finance-planning.routes.ts` | 0 | 17 | 17 |
| `/api/v8/finance-intelligence` | `server/src/routes/v8/finance-intelligence.routes.ts` | 2 | 11 | 13 |
| **SUMA** | | **91** | **179** | **270** |

**Rozbieżność z moją tezą D7 — zapisana:**

- Teza: „34 trasy odczytowe `/api/v8/finance/*` bez bramki modułu”.
- Pomiar: **34 to dokładnie liczba tras GET w jednym pliku** `finance.routes.ts`.
  Liczba się zgadza co do jednego, ale zakres tezy był zawężony.
- Literalnie pod `/api/v8/finance/*` (razem z podprefiksami `value` i `value-tracking`)
  jest **43 GET i 73 zapisy = 116 tras**.
- Cała powierzchnia finansowa `/api/v8` (7 prefiksów) to **91 GET + 179 zapisów = 270 tras**,
  **wszystkie bez bramki modułu**.
- **Teza zaniżała skalę ~8× i pomijała, że problem dotyczy w większości ZAPISÓW, nie odczytów.**

### T1.4 Tabela per trasa — `/api/v8/finance` (plik `finance.routes.ts`)

Legenda kolumn dla całej sekcji T1.4/T1.5:
- **auth** — `verifyToken` na `v8Router` (`routes/v8/index.ts:59`) obejmuje wszystko: TAK wszędzie.
- **bramka modułu** — `createModuleGate('MODULE_ECONOMICS')`: NIE wszędzie.
- **org scoping** — czy `organizationId` pochodzi z tokena (`getV8Context(req)` →
  `attachV8Context` → `req.organizationId` z JWT), a nie z parametru żądania.

Pomiar org scopingu: `git grep -nE "req\.(query|params|body)\.(organizationId|organization_id|orgId)"`
po wszystkich plikach finansowych v8 → **0 trafień**. Żadna trasa finansowa v8 nie przyjmuje
identyfikatora organizacji od klienta. **Org scoping jest poprawny na całej powierzchni.**

34 trasy odczytowe (`GET`), wszystkie: auth TAK · bramka modułu **NIE** · org scoping z tokena TAK:

| # | linia | ścieżka |
| ---: | ---: | --- |
| 1 | 339 | `/settings` |
| 2 | 895 | `/dashboard` |
| 3 | 907 | `/models` |
| 4 | 1016 | `/models/:modelId` |
| 5 | 1080 | `/models/:modelId/validations` |
| 6 | 1105 | `/models/:modelId/outputs` |
| 7 | 1168 | `/models/:modelId/appraisal` |
| 8 | 1531 | `/models/:modelId/case` |
| 9 | 1583 | `/models/:modelId/versions` |
| 10 | 1596 | `/models/:modelId/versions/diff` |
| 11 | 1620 | `/models/:modelId/events` |
| 12 | 1739 | `/models/:modelId/outputs/download` |
| 13 | 1765 | `/models/:modelId/export` |
| 14 | 1902 | `/valuations` |
| 15 | 1914 | `/budgets` |
| 16 | 2178 | `/statement-packs` |
| 17 | 2194 | `/statement-packs/:packId` |
| 18 | 2244 | `/statements` |
| 19 | 2743 | `/statements/:statementId` |
| 20 | 2759 | `/statements/:statementId/analytics` |
| 21 | 2785 | `/statements/:statementId/ratios` |
| 22 | 2802 | `/statements/:statementId/document-intelligence/search` |
| 23 | 3566 | `/statements/:statementId/source-receipt` |
| 24 | 3679 | `/canonical-lines` |
| 25 | 3699 | `/analyses` |
| 26 | 3789 | `/analyses/:analysisId/ratios` |
| 27 | 3807 | `/analyses/:analysisId/initiative-proposals` |
| 28 | 4034 | `/lane/:runId` |
| 29 | 4048 | `/lane` |
| 30 | 4087 | `/lane/:runId/mutation-audit` |
| 31 | 4138 | `/versions` |
| 32 | 4149 | `/lane/:runId/kpi-coherence` |
| 33 | 4493 | `/exports/packages` |
| 34 | 4523 | `/exports/packages/:artifactId/download` |

Pozycje 16-23 i 24 są DODATKOWO osiągalne przy `ENABLE_V8_GLOBAL=false` przez
`mountedFinanceStatementRouter` (T1.2 pkt 1).

### T1.5 ZAPISY bez bramki modułu (gorsze niż odczyt) — 179 tras

Ściany, które istnieją i realnie działają **wewnątrz** routerów (to NIE jest bramka modułu,
ale trzeba to policzyć uczciwie, żeby nie zawyżyć ryzyka):

| Router | `requireActiveMembership` | `requireFinanceEditorMembership` |
| --- | --- | --- |
| `finance.routes.ts` | TAK, router-level (linia 326) | TAK: per-trasa na `/digitization-analyses/*`, `/settings` PUT + router-level dla wszystkich mutacji `/statements*` (linie 330-334) |
| `finance-v2/index.ts` | TAK, router-level (linia 60) | TAK: `requireCanonicalFinanceMutation` (linie 44-59) dla mutacji na `models\|artifacts\|versions\|compute\|statements\|analysis\|baseline\|prediction\|valuation` + `/import/apply` |
| `finance-value.routes.ts` | częściowo — per-trasa na 5 z 25 (linie 117, 172, 320, 360, 662) | NIE |
| `financeValueRoutes.ts` | **NIE — zero** | NIE |
| `finance-valuation.routes.ts` | **NIE — zero** | NIE |
| `finance-planning.routes.ts` | **NIE — zero** | NIE |
| `finance-intelligence.routes.ts` | **NIE — zero** | NIE |

Zapisy, które **nie mają ANI bramki modułu, ANI ponownego odczytu członkostwa** — czyli
dostępne dla każdego użytkownika z ważnym JWT i kontekstem organizacji:

| Prefiks | liczba zapisów | charakter |
| --- | ---: | --- |
| `/api/v8/finance-valuation` | 19 | kalkulatory bezstanowe (Monte Carlo, VaR, opcje realne, wrażliwość, scenariusze, decyzje kapitałowe) — body → wynik, brak DB |
| `/api/v8/finance-planning` | 17 | kalkulatory bezstanowe (cash forecast, rolling forecast, headcount, run-rate, driver tree) + **`POST /nl-to-model` (LLM)** |
| `/api/v8/finance-intelligence` | 11 | 9 bezstanowych + **`POST /pipeline/run` i `POST /pipeline/document` — jedyne dwie z `aiRateLimiter`** (linie 435, 477) |
| `/api/v8/finance-value` + `/finance/value` | 6 | bezstanowe (value-bridge, prioritize, ration, appraise, variance-bridge, value-assurance) |
| `/api/v8/finance/value-tracking` | 11 z 16 | **DOTYKAJĄ BAZY** (attribution/rollup, reconciliation, banking, lineage/early-warnings, ratios) bez ponownego czytania członkostwa |
| **RAZEM** | **64** | |

**Najgorsza pozycja pomiaru:** 11 zapisów w `finance-value.routes.ts`, które piszą/czytają
bazę (rejestr wartości, uzgodnienia, banking) bez bramki modułu i bez ponownego odczytu
członkostwa. Ryzyko: token użytkownika, któremu odebrano członkostwo, działa do wygaśnięcia JWT
— dokładnie ten scenariusz, dla którego `requireActiveMembership` postawiono w
`finance.routes.ts:326` (komentarz FIN-MVP-CUTOVER-001).

**Druga pozycja:** 47 kalkulatorów bezstanowych to nie wyciek danych, ale niekontrolowane
zużycie CPU/LLM — z 47 tylko 2 mają `aiRateLimiter`. `POST /finance-planning/nl-to-model`
woła model językowy bez limitu.

### T1.6 Kto to woła po stronie klienta (czy bramka czegoś nie wygasi)

`git grep -ln "v8/finance" -- src` → 74 pliki. Po odjęciu testów, konsumenci to:

- `src/components/Economics/**` (FinanceHub + 20 paneli + hooki) — moduł Finanse, `closed`;
- `src/components/Finance/**` (Workspace'y Sprawozdań/Modeli/Wyceny) — ten sam moduł;
- `src/components/Benefits/{BudgetWorkspace,FinancialAnalysisWorkspace,ValuationWorkspace}.tsx`
  — mimo katalogu `Benefits` te trzy są renderowane **wyłącznie z `FinanceHub.tsx`**
  (linie 163-168, 3486, 3629, 3652), czyli też moduł Finanse;
- `src/services/conversionService.ts` — woła `createDigitizationAnalysis` i `createBudget`;
- **`src/views/admin/AdminSettingsModule.tsx:497` → `AdminOrganizationDefaultsPanel`
  → `V8FinanceApi.getSettings()` / `updateSettings()`** = `GET/PUT /api/v8/finance/settings`.
  To moduł Admin (otwarty), ale panel z definicji widzą tylko role admin/owner,
  które `createModuleGate` przepuszcza. **Bez ryzyka wygaszenia.**
- `src/components/settings/OrganizationSettings.tsx` też woła `/finance/settings`, ale ten
  komponent **nie ma ani jednego importera** (`git grep` po `src` → tylko własny plik i
  niezwiązana lokalna nazwa typu w `AISecuritySettings.tsx`). Martwy kod (kształt
  „biblioteka bez wywołania”). Nie liczy się jako konsument.

**Wniosek: bramka `MODULE_ECONOMICS` na finansach v8 nie odcina żadnego otwartego modułu.**
Jedyna trasa spoza Finansów (`/finance/settings`) jest wołana z ekranu admina, a admin jest
zwolniony przez `BETA_ADMINS_EXEMPT`.

### T1.7 Proponowana naprawa (NIE wdrożona — dyżur Codexa 288)

Zgodnie z sąsiadami: **jedna linia montowania na routerze, nie per trasa.**

**Miejsce 1 — `server/src/routes/v8/index.ts`, WSTAWIĆ przed obecną linią 118**
(czyli przed pierwszym mountem finansowym `/finance/value` w linii 120):

```ts
// M16 Finanse — `MODULE_ECONOMICS` = 'closed' w SSOT
// (`server/src/sharedRuntime/utils/betaMenuStatus.ts:52`). Odpowiedniki legacy
// (`/api/economics`, `/api/financial-modeling`, `/api/finance-statements`) dostały
// bramkę 02.09; przestrzeń `/api/v8` została pominięta — te same dane, inna ścieżka.
// ★ `verifyToken` stoi na `v8Router` wyżej (linia 59), więc bramka widzi realną rolę
//   i NIE wygasza modułu dla właściciela.
const economicsModuleGate = createModuleGate('MODULE_ECONOMICS');
v8Router.use(
  ['/finance', '/finance-v2', '/finance-value', '/finance-valuation',
   '/finance-planning', '/finance-intelligence'],
  economicsModuleGate
);
```

`'/finance'` jako prefiks pokrywa `/finance/value` i `/finance/value-tracking`.
`createModuleGate` jest już zaimportowany w tym pliku (linia 4) — bez nowego importu.

**Miejsce 2 — `server/src/routes/v8/financeStatementMountedSurface.ts:88-94`**, dołożyć
bramkę do łańcucha PO `verifyToken` (inaczej pusta rola → wygaszenie dla wszystkich):

```ts
  router.use(
    dependencies.verifyToken,
    dependencies.requireOrgContext,
    dependencies.moduleGate,        // ← nowa zależność, domyślnie createModuleGate('MODULE_ECONOMICS')
    dependencies.attachContext,
    dependencies.metrics,
    dependencies.mutationCanary
  );
```
z dopisaniem `moduleGate` do `MountedSurfaceDependencies` (linia 51-59) i `defaults` (61-69),
żeby test mógł ją nadpisać.

**Miejsce 3 (osobne, mniejsze, do decyzji) —** dołożyć `router.use(requireActiveMembership)`
na `finance-value.routes.ts`, `financeValueRoutes.ts`, `finance-valuation.routes.ts`,
`finance-planning.routes.ts`, `finance-intelligence.routes.ts` (5 plików, po jednej linii),
oraz `aiRateLimiter` na `POST /finance-planning/nl-to-model`.
To NIE jest częścią bramki modułu — osobna pozycja, może iść osobnym dyżurem.

### T1.8 Jak wygląda dowód mutacyjny (dla dyżuru 288)

Wzorzec już istnieje w repo: `server/src/scripts/economics-gate-probe.ts` (sonda z 4 pozycjami,
uruchamiana `npx tsx`, NIE vitest — `tests/setup.ts:858-896` podmienia `global.fetch` i dałby
fałszywy sukces). Dyżur 288 **dokłada pozycje 5-6**, nie pisze własnej sondy.

Para dowodów wymagana dla KAŻDEJ pozycji (sam negatyw nie wystarczy — bramka postawiona przed
`verifyToken` daje zielony negatyw i 403 również dla właściciela = wygaszenie):

| | żądanie | oczekiwane PRZED naprawą | oczekiwane PO naprawie |
| --- | --- | --- | --- |
| **NEG odczyt** | `GET /api/v8/finance/statements` jako rola `USER` (aktywny członek organizacji, wiersz sprawozdania zasiany na zimno w JEGO organizacji) | `200` + widoczne sprawozdanie | `403 { code: 'BETA_LOCKED' }` |
| **POS odczyt** | to samo jako rola `OWNER` | `200` | `200` + to samo sprawozdanie (właściciel NIE stracił dostępu) |
| **NEG zapis** | `POST /api/v8/finance/models` jako `USER` | `201` + wiersz w `financial_models` potwierdzony odczytem na zimno | `403 BETA_LOCKED`, **zero nowych wierszy** w odczycie na zimno |
| **POS zapis** | to samo jako `OWNER` | `201` | `201` + wiersz potwierdzony odczytem na zimno |
| **NEG obejście** | `GET /api/v8/finance/statements` jako `USER` przy `ENABLE_V8_GLOBAL=false` (ścieżka `mountedFinanceStatementRouter`) | `200` — obejście działa | `403 BETA_LOCKED` |
| **POS obejście** | to samo jako `OWNER` przy `ENABLE_V8_GLOBAL=false` | `200` | `200` |

Odczyt „na zimno” = osobny klient `pg` poza pulą aplikacji. Powód: `Database.ts:686` zwraca
`changes:1` dla każdego UPDATE niezależnie od `WHERE` — kod nie jest dowodem.

**Mutacja celująca w zabezpieczenie (nie w mechanizm):** usunąć samą linię
`v8Router.use([...], economicsModuleGate)` i uruchomić sondę ponownie. Sonda MUSI zrobić się
czerwona na pozycjach NEG. Jeśli po skasowaniu bramki testy dalej są zielone — test broni
mechanizmu (routingu), a nie zabezpieczenia, i nie liczy się jako dowód.

**Drugi wariant mutacji (na pułapkę wygaszenia):** przenieść `economicsModuleGate` PRZED
`v8Router.use(verifyToken)`. Pozycje NEG dalej będą zielone (403), ale pozycje **POS muszą
się zrobić czerwone** (właściciel też dostanie 403). Bez tej pary sonda przepuściłaby
wygaszenie modułu dla wszystkich.

---

## T2 — `WatchingTab` / `/api/settings/watchers` (D5) i rozjazd `help_*` (D6)

### T2.1 `WatchingTab` — teza D5 jest w POŁOWIE fałszywa

Teza D5: „`WatchingTab` w Ustawieniach woła trasę `/api/settings/watchers`, która nie istnieje”.

**Co potwierdziłem:**

| Fakt | Dowód |
| --- | --- |
| Front woła 3 warianty `/api/settings/watchers` | `src/hooks/useUserNotificationPreferences.tsx:192` (`GET`), `:422` (`POST`), `:448` (`DELETE /:objectType/:objectId`) |
| Trasy `/api/settings/watchers` NIE MA po stronie serwera | `git grep -n "settings/watchers" -- server/src` → **0 trafień**. Pełny spis 118 tras w `server/src/routes/settings.routes.ts` (jedyny router pod `app.use('/api/settings', settingsRoutes)` — `Gateway.ts:760`) nie zawiera żadnej ścieżki `/watchers` |
| Błąd jest cichy | `useUserNotificationPreferences.tsx:180` — `if (response.ok)`, bez `else`; 404 nie ustawia żadnego stanu błędu, lista zostaje pusta |

**Co OBALIŁEM własnym pomiarem — poprawka do D5:**

`WatchingTab` **nie jest w Ustawieniach**. Cały moduł `src/components/settings/NotificationSettingsV2/**`
(7 plików: `index.tsx` + 6 zakładek, w tym `WatchingTab.tsx`) **nie ma ani jednego importera**.

```
grep -rln "NotificationSettingsV2" --exclude-dir=node_modules --exclude-dir=.git .
→ wyłącznie pliki .md/.json (dokumentacja i baseline'y skryptów); zero plików kodu
git grep -rn "NotificationSettingsV2" -- src dev-render tests
→ wyłącznie 4 wystąpienia we WŁASNYM pliku src/components/settings/NotificationSettingsV2/index.tsx
```

Realnie renderowany ekran powiadomień to **v1**: `src/views/SettingsView.tsx:433` →
`src/components/settings/NotificationSettings.tsx`, w którym `git grep -in "watch"` daje **0 trafień**.
**W Ustawieniach nie ma i nigdy nie było zakładki „Watching”.**

To niezależnie potwierdza korektę już zapisaną 02.09 w
`docs/program/waves/WAVE_03_ACCEPTANCE/modules/15_SETTINGS/MODULE_ACCEPTANCE.md` (wiersz G05) —
mój pomiar jest z nią zgodny, a sprzeczny z zapisem D5 w rejestrze.

**Klasyfikacja kształtu:** to NIE jest „zbudowane, ale niepodłączone” po stronie serwera.
Serwerowe „watchers” istnieją, ale **per obiekt**, nie generycznie:

| Powierzchnia | Plik:linia | Migracja |
| --- | --- | --- |
| `GET/POST /api/initiatives/:id/watchers`, `DELETE /api/initiatives/:id/watchers/:watcherId` | `server/src/routes/pmo/initiatives.routes.ts:3685-3694` (+ `InitiativeController.getWatchers/addWatcher/deleteWatcher`) | `server/migrations/334_initiative_watchers.sql` |
| `GET /api/table-platform/records/:recordId/watchers` | `server/src/routes/table-platform.routes.ts:1395-1406` (+ `server/src/services/tablePlatform/RecordWatchService.ts`) | `server/migrations/716_record_watches.sql` |

Nie ma i nigdy nie było **agregatu wielotypowego** („wszystko, co obserwuję”), którego oczekuje
`WatchingTab` (grupuje po `objectType`: `task` / `initiative` / `project` — trzy różne magazyny).

**Drugi defekt tego samego wołacza, dorzucony przy okazji:** wszystkie trzy `fetch` do
`/api/settings/watchers` wysyłają `credentials: 'include'` i **żadnego nagłówka `Authorization`**,
podczas gdy reszta aplikacji uwierzytelnia się Bearer JWT
(np. `src/contexts/HelpContext.tsx:301`). Nawet gdyby trasa powstała, ten wołacz dostałby 401.
Naprawa musi objąć oba: trasę i nagłówek.

**Najmniejsza naprawa (dyżur 289) — trzy warianty, decyzja produktowa nie techniczna:**

- **(A) najtańszy, zalecany:** usunąć `src/components/settings/NotificationSettingsV2/**`
  (7 plików) razem z `src/hooks/useUserNotificationPreferences.tsx`, jeśli nie ma innych
  konsumentów. Zysk: znika martwy wołacz nieistniejącej trasy i drugi, konkurencyjny ekran
  powiadomień. Ryzyko: zero (nikt tego nie renderuje). To jest ta sama rodzina co D11
  (`OrganizationV8CanonPanel.tsx` martwy).
- **(B) jeśli właściciel CHCE ekran „Obserwowane”:** dopisać agregat
  `GET /api/settings/watchers` w `server/src/routes/settings.routes.ts`, który sumuje
  `initiative_watchers` i `record_watches` per `req.user.id` + `req.organizationId`,
  plus `POST` i `DELETE /:objectType/:objectId` delegujące do istniejących serwisów;
  równolegle poprawić nagłówek `Authorization` w hooku i podpiąć `NotificationSettingsV2`
  pod `SettingsView.tsx` **za flagą domyślnie OFF** (reguła 7 CLAUDE.md — Piotr nie jest
  pierwszym testerem wizualnym).
- **(C) minimum bez decyzji:** zostawić kod, ale zamienić ciche `if (response.ok)` na jawny
  stan błędu, żeby defekt przestał być niewidzialny.

**Dowód dla dyżuru 289 (wariant A):** `git grep -rn "NotificationSettingsV2\|useUserNotificationPreferences" -- src dev-render tests`
po usunięciu → 0 trafień; `npm run build` (albo esbuild na `SettingsView.tsx`) przechodzi.
Mutacja: przywrócić jeden import — build musi się wywrócić, inaczej test nie mierzy usunięcia.
**Dowód dla wariantu B:** para NEG/POS na realnym Postgresie — użytkownik A obserwuje inicjatywę
X, `GET /api/settings/watchers` jako A zwraca X (POS); jako użytkownik B z innej organizacji
zwraca pustą listę i NIE zwraca X (NEG, org scoping); odczyt na zimno osobnym klientem `pg`.

### T2.2 `help_articles` / `help_events` — rozjazd migracja ↔ kod (D6 POTWIERDZONE)

Ostrzeżenie o rozjeździe jest już wpisane w kodzie: `server/src/routes/help.routes.ts:33-41`
(komentarz „measured 2026-09-03 … out of scope for this fix”, commit `72d49c9dbf`).
Ten commit naprawił WYŁĄCZNIE `help_categories` (dodał migrację
`server/migrations/20260903_help_categories.sql` i przestał połykać błąd w `ensureHelpSchema`).
**`help_articles` i `help_events` zostały nietknięte.**

Mechanizm: `ensureHelpSchema()` (`help.routes.ts:43-108`) używa `CREATE TABLE IF NOT EXISTS`.
Obie tabele już istnieją z migracji o INNYM kształcie, więc `CREATE` jest **no-opem** —
deklaracja w kodzie nigdy nie dochodzi do skutku, a zapytania niżej odwołują się do kolumn,
których w bazie nie ma.

#### `help_articles`

| Kolumna | w migracji? | w kodzie tras? | uwaga |
| --- | --- | --- | --- |
| `id` | TAK (`255_help_system.sql:9`) | TAK | zgodne |
| `title` | TAK (`:11`) | TAK | zgodne |
| `category` | **TAK** (`:10`, `NOT NULL`) | **NIE** | migracja ma, kod nie zna |
| `content` | **TAK** (`:14`, `NOT NULL`) | **NIE** | migracja ma, kod nie zna |
| `slug`, `subcategory`, `excerpt`, `video_url`, `tags`, `sort_order`, `is_featured`, `view_count`, `helpful_count`, `not_helpful_count`, `last_updated_by`, `search_vector` | TAK | NIE | 12 kolumn nieużywanych |
| `is_published` | **TAK** (`:24`, `BOOLEAN`) | **NIE** | kod używa zamiast tego `status` |
| **`category_id`** | **NIE** | **TAK** — `help.routes.ts:182, 194, 220, 222` | **42703** |
| **`body`** | **NIE** | **TAK** — `help.routes.ts:196` (`body LIKE ?`) | **42703** |
| **`status`** | **NIE** | **TAK** — `help.routes.ts:182, 184, 194, 195, 220, 222` | **42703** |
| `created_at` / `updated_at` | TAK (`TIMESTAMP`) | TAK | typ różny (`TIMESTAMPTZ` w kodzie), ale `CREATE` jest no-opem |

Trzeci konsument tej samej tabeli — `server/src/services/helpService.ts:103, 188, 193, 232` —
używa kształtu **z migracji** (`is_published`, `view_count`, `slug`). Czyli w jednym repo
dwa czytniki tej samej tabeli mają dwa różne kontrakty. (Uwaga poboczna, nie mierzona do końca:
`helpService.ts:103` pisze `WHERE is_published = 1` na kolumnie `BOOLEAN` — na Postgresie to 42804.
`helpService` nie ma dziś żadnego importera po stronie serwera: `git grep -n "helpService" -- server/src`
poza własnym plikiem → 0 trafień.)

#### `help_events`

| Kolumna | w migracji? | w kodzie tras? | uwaga |
| --- | --- | --- | --- |
| `id` | TAK (`000_initdb_core_tables.sql:1313`) | TAK | zgodne |
| `user_id` | TAK (`:1314`, FK do `users`) | TAK | zgodne |
| `event_type` | TAK (`:1315`) | TAK | zgodne |
| `event_data` | **TAK** (`:1316`, default `'{}'`) | **NIE** | kod używa zamiast tego `metadata` |
| `organization_id` | TAK (`551_help_playbooks_onboarding.sql:48`, `20260719_baseline_gap.sql:4744`) | TAK | zgodne |
| `playbook_key`, `step_id`, `route` | TAK (`551:49-51`) | NIE | kod ich nie zapisuje, choć front je wysyła |
| **`article_id`** | **NIE** | **TAK** — `help.routes.ts:262` (INSERT) | **42703** |
| **`metadata`** | **NIE** | **TAK** — `help.routes.ts:262` (INSERT) | **42703** |

#### Gdzie błąd jest łapany po cichu

| Plik:linia | Co połyka | Skutek dla użytkownika |
| --- | --- | --- |
| `server/src/routes/help.routes.ts:187` | `.catch(() => [])` — lista artykułów bez zapytania | `200 { success: true, data: [] }` — „brak artykułów” zamiast błędu |
| `server/src/routes/help.routes.ts:199` | `.catch(() => [])` — wyszukiwanie artykułów | `200` z pustą listą przy każdym zapytaniu |
| `server/src/routes/help.routes.ts:216` | `.catch(() => [])` — lista kategorii | `200` z pustą listą |
| `server/src/routes/help.routes.ts:224` | `.catch(() => [])` — liczniki artykułów per `category_id` | ciche zero |
| `server/src/routes/help.routes.ts:274-278` | `try/catch` → `logger.warn(...)`, **odpowiedź nadal `200 { success: true, stored: false }`** | **klient dostaje sukces, zdarzenie nie zostaje zapisane** |

Pozycja `:274-278` jest najgorsza: to **jedyna z tych tras, która ma żywego wołacza**
(`src/contexts/HelpContext.tsx:299`, `POST /api/help/events` przy każdej interakcji z playbookiem).
Sukces w odpowiedzi maskuje trwałą utratę danych. Pole `stored:false` jest w odpowiedzi,
ale front go nie czyta.

**Zakres realnego wpływu (żeby nie zawyżyć):** `git grep -rn "help/articles\|help/categories" -- src dev-render`
→ **0 trafień**. Trasy `/api/help/articles` i `/api/help/categories` nie mają dziś żadnego
wołacza w aplikacji — psują się, ale nikogo nie boli. Boli tylko `/api/help/events`.

**Trzeci, niezależny rozjazd na tym samym endpoincie:** front wysyła
`{ playbookKey, eventType, context }` (`HelpContext.tsx:308-317`), a trasa czyta
`{ eventType, articleId, metadata }` (`help.routes.ts:250`). Nawet po naprawie schematu
`playbookKey` i `context` wylądowałyby jako `NULL`. Naprawa musi objąć kontrakt, nie tylko kolumny.

**Najmniejsza naprawa (dyżur 289):** jedna migracja addytywna
`server/migrations/20260904_help_shape_alignment.sql`:

```sql
ALTER TABLE help_articles ADD COLUMN IF NOT EXISTS category_id TEXT;
ALTER TABLE help_articles ADD COLUMN IF NOT EXISTS body TEXT;
ALTER TABLE help_articles ADD COLUMN IF NOT EXISTS status TEXT DEFAULT 'published';
ALTER TABLE help_events   ADD COLUMN IF NOT EXISTS article_id TEXT;
ALTER TABLE help_events   ADD COLUMN IF NOT EXISTS metadata TEXT;
```
plus w `help.routes.ts:250` dołożyć odczyt `playbookKey`/`context` i zapisać je do
istniejących kolumn `playbook_key` / `event_data`, plus zamienić `stored: false`
z `200` na `500` albo przynajmniej wystawić to jako błąd, którego front nie zignoruje.

★ **Pułapka migracji przyrostowej (bezpiecznik z pamięci nadzorcy):** nazwa
`20260904_*` sortuje się PO `20260719_baseline_gap.sql` i `551_*`, więc kolumny
istnieją, zanim `ALTER` je doda — dobrze. Ale dyżur MUSI zweryfikować to na bazie
od zera (dwa przebiegi migracji, drugi ma dać `Applying migrations: 0`), a nie
na bazie zastanej.

**Dowód mutacyjny dla D6:** na świeżym Postgresie (pgvector, port jednorazowy),
migracje dwa razy, realny `ApiGateway` przez `npx tsx` (NIE `vitest` —
`tests/setup.ts:858-896` podmienia `global.fetch`):

| | żądanie | PRZED | PO |
| --- | --- | --- | --- |
| NEG (dziś) | `POST /api/help/events` z `{eventType:'x', articleId:'a1', metadata:{k:1}}` | `200 {stored:false}`, `SELECT COUNT(*) FROM help_events` na zimno = **0** | `200 {stored:true}`, wiersz na zimno z `article_id='a1'` i `metadata` |
| NEG kontrakt | `POST /api/help/events` z realnym payloadem frontu `{playbookKey:'p1', eventType:'x', context:{route:'/r'}}` | wiersz brak / `playbook_key` NULL | wiersz z `playbook_key='p1'` i `event_data` zawierającym `route` |
| Artykuły | `GET /api/help/articles` po zasianiu 1 opublikowanego artykułu na zimno | `200 { data: [] }` (cichy 42703) | `200` z tym artykułem |
| Mutacja | usunąć z migracji linię `ADD COLUMN … article_id` | — | test NEG musi zrobić się CZERWONY; jeśli zostaje zielony, mierzy mechanizm (HTTP 200), nie zabezpieczenie |

---

## T3 — pozostałe otwarte P0/P1 wobec bramki G20

Bramka G20 = „Final 16/16 replay” (finalny przebieg wszystkich szesnastu modułów na jednym SHA).
Warunek wejścia „Zero open P0/P1 across all registers” jest niezaznaczony w
`docs/program/waves/WAVE_03_ACCEPTANCE/FINAL_16_MODULE_REPLAY.md` (lista „Entry gate”).

**Pomiar wyjściowy:** `G20` ma status `NOT_STARTED` w **16 z 16** kart modułowych
(`docs/program/waves/WAVE_03_ACCEPTANCE/modules/*/MODULE_ACCEPTANCE.md`, wiersz `| G20 |`).
Zgodne z `docs/program/SCIEZKA_WYJSCIA_V2.md:26` („G17, G19 i G20 — mechanizm formalnego
zamknięcia nie został uruchomiony ani razu”).

### T3.1 Liczniki P0/P1 z `MASTER_STATUS_REGISTER.md` (stan dokumentu `2026-09-02`)

| Moduł | Open P0 wg dokumentu | Open P1 wg dokumentu |
| --- | ---: | ---: |
| `ASM` Assessment | 7 | 1 |
| `INI` Initiatives | 3 | 6 |
| `MYW` My Work / Agent | 11 | 18 |
| `CHAT` Chat | 1 | 13 |
| pozostałe 12 modułów | 0 | 0 |
| **SUMA** | **22** | **38** |

**Mój pomiar kontrolny — dokument NIE jest odtwarzalny z kart modułowych:**

| Moduł | Co policzyłem w karcie modułu | Zgodność z tabelą master |
| --- | --- | --- |
| `MYW` | 11 wierszy `\| P0 \|` (6 `CZĘŚCIOWE`, 2 `WYMAGA_DECYZJI`, 2 `NIEZROBIONE`, 1 `READY_FOR_OWNER_SCREENSHOT_REVIEW`) | **P0 zgadza się: 11** |
| `MYW` | 23 wiersze `\| P1 \|`, z tego 6 ma status `ZROBIONE_W_KODZIE` → **17 realnie otwartych** | **P1 NIE zgadza się: master 18, mój pomiar 17 otwartych / 23 wszystkich** |
| `ASM` | karta zawiera 3 wystąpienia napisu `P0` i **żadnej kolumny ważności** — 7 P0 / 1 P1 z tabeli master **nie da się odtworzyć z karty** | **NIEODTWARZALNE** |
| `INI` | karta ma inny format tabeli, bez kolumny ważności per wiersz; 6 wystąpień `P0`, 5 `P1` w tekście | **NIEODTWARZALNE** (master: 3 / 6) |
| `CHAT` | karta agreguje 17 uwag właściciela w JEDEN wiersz `CHAT-OWN-001–017` z odsyłaczem „See detailed report” | **NIEODTWARZALNE** (master: 1 / 13) |

To jest ten sam kształt co „dwa rejestry — licznik mierzy rozjazd”: **suma 22 P0 / 38 P1 jest
liczbą z pulpitu, a nie sumą policzalnych obiektów.** Karta master sama to przewiduje
(sekcja „Update rule”: *„The module register is authoritative for finding detail”*), ale
dla 3 z 4 modułów karta modułowa tej informacji nie niesie. **Przed G20 trzeba przywrócić
odtwarzalność liczby albo świadomie zapisać, że jej nie ma.**

### T3.2 Tabela pozycji

| ID | Co | Stan wg dokumentu | Mój pomiar (grep / plik) | Blokuje G20? | Szacunek |
| --- | --- | --- | --- | --- | --- |
| `XMOD-SEC-001` | Poświadczenie Railway (jawny URL z hasłem) w historii repo; 5 skryptów wymaga dziś `DATABASE_URL` | `CODE_FIXED_AND_CURRENT_TREE_SCAN_PASS / EXTERNAL_ROTATION_UNVERIFIED`, `P0_SECURITY` (`CROSS_MODULE_FINDINGS.md:8`); `FINAL_16_MODULE_REPLAY.md:29` nazywa go „explicit P0 acceptance blocker” | **Kod czysty — potwierdzam.** `git grep -nE "postgres(ql)?://[user]:[hasło]@"` po całym drzewie → jedyne trafienia to `tests/unit/deploy/validate-deploy-target.test.mjs:27-29`, gdzie hasło jest zmienną testową (`${PW_AT}`), nie literałem. **Śladu rotacji NIE MA** — ale jest coś więcej: `OWNER_DECISION_LEDGER_2026-08-24.md:277`, `DEC-2026-08-28-226`: **właściciel ŚWIADOMIE ODROCZYŁ rotację haseł do przejścia na produkcję** („zostaw hasła jak są”). Rejestr `CROSS_MODULE_FINDINGS.md` tej decyzji nie odnotował | **TAK, ale wyłącznie formalnie** — pozycja jest otwarta w rejestrze, choć decyzja właściciela ją odracza. To rozjazd dokumentów, nie dziura w kodzie | **0,5 dnia dokumentacyjnie** (dopisać `DEC-2026-08-28-226` do `CROSS_MODULE_FINDINGS.md` i przeklasyfikować na `OWNER_DEFERRED`), albo **1 h + działanie właściciela** jeśli rotacja ma nastąpić przed G20 |
| `D7` / T1 | 270 tras finansowych `/api/v8/*` bez bramki modułu `MODULE_ECONOMICS` | `REJESTR_ZNALEZISK_20260903.md:66` — „34 trasy odczytowe”, otwarte | **Zmierzone w T1: 91 GET + 179 zapisów = 270; teza zaniżała ~8×.** Dwa miejsca naprawy, jedna linia + jedna zależność | **TAK** — powierzchnia zamkniętego modułu otwarta dla każdego zalogowanego użytkownika | **1 dyżur** (2 pliki, ~10 linii + rozbudowa istniejącej sondy `economics-gate-probe.ts` o 2 pozycje) |
| `D5` | `WatchingTab` / `/api/settings/watchers` | `REJESTR_ZNALEZISK_20260903.md:64` + `PRZEKAZANIE_20260904.md:75` — „funkcja martwa w produkcji” | **Teza w połowie fałszywa** (T2.1): trasy faktycznie nie ma, ale `NotificationSettingsV2` **nie ma ani jednego importera** — ekran nigdy się nie renderuje. Potwierdza to niezależnie `modules/15_SETTINGS/MODULE_ACCEPTANCE.md` (wiersz G05, korekta z 02.09) | **NIE** — martwy kod bez powierzchni użytkownika; nie jest P0 ani P1, jest porządkiem (rodzina D11) | **2 h** (wariant A: usunięcie 8 plików) |
| `D6` | `help_articles` / `help_events` — rozjazd migracja ↔ kod | `REJESTR_ZNALEZISK_20260903.md:65`, otwarte | **POTWIERDZONE i policzone** (T2.2): 5 kolumn rozjechanych (`category_id`, `body`, `status`, `article_id`, `metadata`), 5 miejsc cichego połykania, 1 żywy wołacz (`HelpContext.tsx:299`), 0 wołaczy dla artykułów/kategorii. Trzeci, niezależny rozjazd: kontrakt payloadu (`playbookKey`/`context` vs `articleId`/`metadata`) | **P1, nie P0** — cicha utrata zdarzeń pomocy przy `200 success:true`; brak wpływu na dane klienta | **pół dyżuru** (1 migracja addytywna + 10 linii w trasie) |
| `videos` cross-org | Trzecia z „3 dziur cross-org” z `SCIEZKA_WYJSCIA_V2.md:45` | `CODEX_DAY242_UPRAWNIENIA_REPORT.md` — `EVIDENCE_MISSING / NOT VERIFIED LIVE`, bo „pełne migracje nie tworzą tabeli `videos`” | **Potwierdzam i pogłębiam:** tabeli `videos` NIE tworzy ani jedna migracja (`grep -rln "CREATE TABLE IF NOT EXISTS videos" server/migrations/` → 0), ani żaden runtime-DDL w `server/src` (→ 0). Router jest zamontowany (`Gateway.ts:1352`, `mountStub('/api/videos', …)`), strażnik org jest napisany (`videos.routes.ts:18-23`), ale **żadne żądanie nie może przejść — tabela nie istnieje nigdzie**. Front nie ma wołacza (potwierdzone w raporcie 242) | **NIE jako wyciek** (nie da się wyciec z nieistniejącej tabeli). **TAK jako rodzina „schemat mieszka poza migracjami”** | **1 h** — decyzja: albo migracja tworząca `videos`, albo odmontowanie `/api/videos` |
| Permission Requests + AI Context cross-org | Pozostałe dwie z „3 dziur” | `SCIEZKA_WYJSCIA_V2.md:45` — „wciąż otwarte”; dyżur 242 zamknięty jako `PARTIAL` | **ZAMKNIĘTE w kodzie na HEAD.** `8f07001d3c` „fix(permissions): block cross-org request resolution” i `6aa92b3eb6` „fix(context): block cross-org AI context mutations” są przodkami HEAD; strażnik widoczny w `server/src/routes/permissionRequests.routes.ts:21-27` (`SELECT organization_id … WHERE id = ?` + porównanie z tokenem). Test na realnym Postgresie istnieje: `server/src/routes/__tests__/day242-permission-requests-org-isolation.realpg.test.ts` | **NIE** — `SCIEZKA_WYJSCIA_V2.md:45` jest w tym punkcie **nieaktualna**; wymaga poprawki | **15 min** (poprawić wpis w planie) |
| `D4` | 9 zastanych czerwieni testów jednostkowych | `REJESTR_ZNALEZISK_20260903.md:62` — dyżur 286 (G15) | Nie mierzyłem (zakaz pełnego `vitest`) | **TAK** — G15 jest przed G20 w kolejce | **wydany** (dyżur 286) |
| Kolejka 285/287/290 | `ResultsHub` (decyzja), fokus `c-focus`, staging W5 | `KOLEJKA_CODEX_INTEGRACJA.md:87,180,198` | Nie mierzyłem | **TAK dla 290** (staging przed G19/G20 wg `PRZEKAZANIE_20260904.md:68`); 285/287 nie | — |

### T3.3 Kolizja numeracji dyżurów — do poprawienia PRZED wydaniem

Instrukcja tego pomiaru mówi o „dyżurze Codexa 288” (finanse) i „289” (help/watchers).
**Oba numery są już zajęte** w `docs/program/KOLEJKA_CODEX_INTEGRACJA.md`:

- `### 288 · Rodzina „surowe ID zamiast etykiety”` (linia 186)
- `### 289 · Rozliczenie 77 uwag z rejestru odbioru` (linia 191)

Pierwsze wolne numery to **292 i dalej** (`290 · W5 staging`, `291 · G19/G20` też zajęte).
Wydanie pod numerem 288/289 nadpisałoby dwa istniejące zlecenia.

### T3.4 Rekomendowana kolejność przed G20

1. **Finanse v8 (T1)** — jedyna pozycja z tej trójki, która jest realną dziurą dostępu.
2. **`XMOD-SEC-001`** — 0,5 dnia pracy dokumentacyjnej; bez tego wpisu bramka „zero open P0/P1”
   nie może być zaznaczona, mimo że kod jest czysty i właściciel podjął decyzję.
3. **Odtwarzalność liczników P0/P1 (T3.1)** — dopóki 22/38 nie da się policzyć z kart,
   „zero open P0/P1” jest twierdzeniem, nie pomiarem.
4. **`help_*` (T2.2)** i **usunięcie `NotificationSettingsV2` (T2.1)** — mogą iść po G20,
   nie blokują.

## Czego NIE dało się zmierzyć statycznie

1. **Czy `ENABLE_V8_GLOBAL` jest dziś `true` na demo i stagingu.** `.env.example:211` ma `true`,
   `docs/program/evidence/closure/decisions/STAGING_G4_16_20260820.json:17` ma `true`,
   ale to nie jest odczyt z żywego środowiska. Gdy jest `false`, część `/api/v8/finance/*`
   i tak przechodzi obejściem `mountedFinanceStatementRouter` — więc dziura z T1 istnieje
   w OBU stanach flagi, tylko o różnej szerokości.
2. **Czy organizacje demo/stagingu mają wiersze w `v8_feature_flags`.** Fallback
   `allowImplicitOrgRowsFallback()` (`v8FeatureGate.middleware.ts:7`) otwiera `v8OrgGate`
   dla organizacji bez wierszy poza produkcją. Stan wierszy to odczyt z żywej bazy.
3. **Czy hasło Railway zostało kiedykolwiek zrotowane.** To zdarzenie poza repo; jedyny
   ślad w repo to decyzja o ODROCZENIU (`DEC-2026-08-28-226`).
4. **Czy 22 P0 / 38 P1 to liczby aktualne.** Ustaliłem, że dla 3 z 4 modułów nie są
   odtwarzalne z kart, ale nie mogłem ustalić prawdziwej liczby bez raportów szczegółowych
   i bez uruchomienia produktu.
5. **Realne odpowiedzi HTTP (403 vs 200) dla którejkolwiek tezy.** Cały pomiar jest
   statyczny; żadna liczba tutaj nie jest dowodem runtime. Dowody mutacyjne opisane
   w T1.8 i T2.2 wykonuje dyżur na realnym Postgresie.

## Aktualizacja po odbiorach 03.09 noc

Poniższe dwa akapity to zdania odbiorcy B (`ODBIOR_DYZUROW_288_289_296_20260903.md`,
sekcja „GOTOWE ZDANIA DO WKLEJENIA"), przeniesione dosłownie po dyżurach Codexa 288 i 289
wydanych na podstawie pomiaru T1/T2 wyżej. Werdykt runtime zastępuje statyczny pomiar
z reszty tego pliku dla tych dwóch pozycji; T3 (pozostałe P0/P1) pozostaje bez zmian.

**T1 (finanse, `/api/v8/finance*`) — W NAPRAWIE, nie zamknięty.** Cytat: „**T1 (finanse,
`/api/v8/finance*`) — ZAMKNIĘTY CO DO MECHANIZMU, otwarty co do pokrycia.** Bramka modułu
stoi na wszystkich 7 prefiksach i na przedglobalnej powierzchni sprawozdań (gałąź
`codex/day288-…`, commit `e9a3cfb983`). Dowód odbioru na realnym Postgresie: 8 tras z 7
prefiksów, USER 403 `BETA_LOCKED` 8/8, OWNER 0/8 zablokowany, mutacja obu bramek → rejestr
45/50 i USER znów czyta ustawienia finansowe z kodem 200. **Warunek scalenia: naprawić 2
przypadki `financeStatementMountedSurface.test.ts`, które gałąź zmienia z zielonych (23/23
na HEAD) na czerwone (21/23).** Pozostaje otwarte: 266 tras bez indywidualnego pomiaru oraz
64 zapisy bez ponownego odczytu członkostwa (niezweryfikowane w tym dyżurze)." Gałąź
`codex/day288-finanse-bramka-modulu-20260903` **nie jest scalona na HEAD** — czeka na
naprawę tych dwóch przypadków (odbiór B: „SCALIĆ Z ZASTRZEŻENIEM").

**T2 (`WatchingTab`/D5 + `help_*`/D6) — ZAMKNIĘTE.** Cytat (D5): „Dyżur 289 potwierdził na
realnej bazie 3 martwe wołacze `/api/settings/watchers` w `src/`, 0 tras po stronie serwera
i realny 404, a werdykt „martwy komponent" niezależnie potwierdziło usunięcie katalogu
`NotificationSettingsV2` przez nadzorcę (`e6c236c0dd`); odbiór adwersaryjny nie znalazł
żywego importera, zero kodu w obszarze A — wiersz zamykalny." Cytat (D6/T-help): „**T-help
(schemat pomocy) — ZAMKNIĘTY.** Gałąź `codex/day289-…`, commit `820b8d0a5b`. Pięć kolumn
czytanych przez `help.routes.ts` obecnych po pełnym łańcuchu migracji od zera, DDL w locie
usunięty, ciche `catch` logują i propagują, `POST /api/help/events` nie zwraca już sukcesu
przy odrzuconym zapisie. Dwie niezależne mutacje na czerwono." Gałąź
`codex/day289-martwe-trasy-watchers-help-20260903` **jest scalona na HEAD** (`a905bce0aa`,
odbiór B: „SCALIĆ").
