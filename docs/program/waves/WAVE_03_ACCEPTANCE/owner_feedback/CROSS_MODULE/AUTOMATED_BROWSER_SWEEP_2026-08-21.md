# Wave 3 automated browser sweep — 2026-08-21

Status: `TECHNICAL_BROWSER_SWEEP_COMPLETE / OWNER_REVIEW_PENDING`

## Qualification boundary

- Browser: authenticated local session at `http://127.0.0.1:3940`.
- Runtime-reported build: `ad0766ac4c10`; this is not the exact current candidate.
- Viewport observed by the browser: `1055 × 998`.
- Coverage: 16 module entry routes and 31 child tabs/surfaces.
- Evidence: PNG screenshots and `browser-sweep.json` in `evidence/2026-08-21_AUTOMATED_SWEEP/`.
- The first exact runtime at `3972/3973` was qualified at HEAD `72a590b0b6d3daf06ac1e607c1e31faca0a0a2af`, 817 migrations, health/ready/frontend `200`, exact server/readiness SHA, client marker verified, V8 enabled, and all auth/test bypasses disabled. After explicit action-time authorization, a real bcrypt login was completed and the authenticated product was exercised.
- A second fresh runtime was then qualified at HEAD `3d61730fd8ad18d19cf9967cb5513697659003cc`, again with 817 migrations and the same fail-closed auth posture. It uses a deterministic UUID-shaped local OWNER persona, because the historical `cw-local-user` text identity is incompatible with Partner's UUID-bound participant tables.
- Exact authenticated coverage now includes all 16 entry routes plus the same 31 child surfaces. Every child surface reached a stable rendered state after its bounded load window; none retained a loading state, visible source error, or horizontal page overflow at `1280 × 720`.
- Final frozen browser candidate: HEAD `3d61730fd8ad18d19cf9967cb5513697659003cc`; its non-self-referential dirty fingerprint is recorded in the external 0600 runtime manifest. Server `3972`, client `3973`, 817 migrations, authoritative readiness green, UUID-shaped OWNER persona authenticated with real bcrypt/JWT, and auth/test bypasses disabled. Fingerprint equality was rechecked after the complete `16 + 31` replay.
- This report does not claim owner acceptance or release readiness: fixture-backed mutations, cold durable readbacks and Piotr's guided decisions remain open.

## Executive verdict

The authenticated exact-current sweep materially changes the earlier verdict: all 16 module entry points were exercised, and all 16 modules now have a retained deterministic fixture-backed technical browser replay. Organization, Admin and Settings were replayed sequentially on exact `3d61730fd8ad` after the original sweep; their technical findings remain open and the concurrent Organization/Admin rebuild is not frozen by this evidence. This is not a 16/16 owner acceptance result: Piotr's walkthrough, visual decisions, responsive/theme/a11y coverage and named policy/provider gates remain separate from technical browser completion.

### Release-blocking integration findings

1. **Interview — FIXED LOCALLY / PROVIDER BOUNDARY.** Authoring and canonical V8-only surfaces are explicitly separated; manager and public fixture journeys cold-open and revoked access returns `410`. With the provider unavailable, evaluation now makes exactly one canonical request and returns typed `503 INTERVIEW_EVALUATION_UNAVAILABLE`; there is no legacy retry, fabricated score or persistence.
2. **Assessment — P1 request-contract mismatch FIXED LOCALLY.** The Hub requested `limit=200`, while Method Core intentionally refuses limits above 100. The exact browser returned 400 and showed a truthful DRD warning. The Hub now requests 100; focused coverage pins the contract and live browser readback shows the canonical catalog plus the honest `No DRD sessions` state without the warning. The earlier `Tools / Licensed` identity defect also remains fixed.
3. **Initiatives — FIXED LOCALLY / OWNER DATA BOUNDARY.** The canonical initiative and candidate cold-open with complete read models and correct scoring. Portfolio/Plan/Capacity are honest empty states because the fixture intentionally has no scenarios.
4. **Execution — FIXED LOCALLY / OWNER DATA BOUNDARY.** Realizacje and the exact Case workbench cold-open; the four other tabs are honest empty canonical states because the fixture has no task/allocation/report/intervention rows.
5. **Tools — FIXED LOCALLY.** Guided and approved Dynamic SWOT sessions cold-open and derive `80%`/`100%` from canonical content; the previous fixture contradiction was corrected.
6. **Finance — P1 fixture/evidence mismatch, not a broken route.** All five tabs now render without a source error on the fresh authenticated runtime. A cold first request briefly showed `Real finance source needs attention`; the same canonical endpoint subsequently returned the correct empty envelope and all five tabs stabilized. Server evidence tied the cold failure to startup/database contention rather than a frontend envelope mismatch. The computed owner fixture still has to be mounted for BV/WR/hash parity.
7. **Partner — FIXED LOCALLY / POLICY BOUNDARY.** The operational fixture journey now covers profile, certification, referral and participant lineage; economics remains explicitly disabled by approved policy.

### Visual and UX findings

1. **Shared table empty states — P1 responsive clipping — FIXED LOCALLY.** Finance, Results, Execution, Meetings and Audits used the 980 px minimum table canvas. At a 1055 px window with the application sidebar, the empty-state card was centered in the off-screen table width and visibly clipped on the right. `FilterableTable` now measures the actually visible horizontal viewport (including window clipping), pins the empty card inside it, and preserves the wide scrollable header geometry. Browser readback proves the cards fully visible in Finance, Results, Meetings and Audits; focused regression is `12/12 PASS`.
2. **Admin — P2 narrow content composition.** At this width the Membership card compresses explanatory copy and controls into very narrow columns. The rebuilt shell is usable but not visually ready at laptop/tablet widths.
3. **Organization — P2 header/breadcrumb truncation.** The three-column layout is coherent, but the breadcrumb and header copy truncate aggressively at 1055 px. Mixed Polish/English remains visible.
4. **Materials Document Studio — P2 header collision — FIXED LOCALLY.** The back label, module label, editable title and generation controls competed for the same compact top-bar width. Below the `xl` breakpoint the two redundant labels now collapse while retaining their accessible names; browser geometry proves a visible 118 px title area with no action-cluster overlap. Executive shell regression is `17/17 PASS`.
5. **Partner — P2 journey mismatch.** `/partner` lands on a long marketing/program page. It is visually polished, but it does not prove the operational owner fixture journey (profile, certification, referral lineage, tenant denial).
6. **Module labels — P2 truth mismatch — FIXED LOCALLY.** Audits and Meeting were marked `Wkrótce` in the global sidebar even though their workspaces are routable and render. Both now carry the truthful `beta` badge; Meeting remains protected by its closed-beta authorization gate. Browser readback shows `Audits beta` and `Meeting beta`; focused contract is `2/2 PASS`.

## Module matrix

| Module | Browser result | FE↔BE result on 3940 | Next acceptance action |
|---|---|---|---|
| Chat | Fixture-backed pass | Exact source message/citations and governed proposal cold-open; OWNER approval materializes one document and cold reopen preserves the receipt | Piotr owner walkthrough; live-provider quality remains outside this deterministic replay |
| My Work | Fixture-backed pass | Inbox decision, task, three-step requester plan and exactly-once materialization receipt cold-open; completed tasks now remain visible after reload | Piotr owner walkthrough; retained DB intentionally contains the post-review completed task |
| Interview | Fixture-backed technical pass | Manager sees two canonical sessions, 3/3 submitted answers and public/revoked token truth; provider-off evaluation is an honest single `503` with no fallback | Piotr owner walkthrough; decide whether archived/escalation capabilities enter Wave 3 |
| Tools | Fixture-backed pass | Guided and approved Dynamic SWOT sessions cold-open from canonical `tool_sessions`; computed progress now matches persisted content at `80%` and `100%` | Piotr owner walkthrough; mutation/CAS journey remains a separate owner action |
| Assessment | Fixture-backed technical pass | Hub lists both canonical Method Core sessions; active interview, immutable frozen Output and Initiative Draft cold-open with zero V8/legacy `my-role` probes, zero 404s and a clean console | Piotr owner walkthrough; report/reopen and full visual matrix remain explicit separate gates |
| Initiatives | Fixture-backed pass | Canonical register, 42% pending candidate, accepted candidate, Initiative Card and stable deep link cold-open; amend/cancel contract proved in RealPG | Piotr owner walkthrough; Portfolio/Plan/Capacity scenarios and provider-free manual create remain open |
| Execution | Fixture-backed pass | Realizacje shows the accepted delivery; exact Case v1 workbench cold-opens and all five canonical surfaces mount without crash | Piotr owner walkthrough; task/allocation/report/intervention fixture rows remain open |
| Meetings | Fixture-backed pass | Pending/rejected/materialized notes cold-open by meetingId with receipts 0/0/1 and exact rejection reason | Piotr owner walkthrough; participant-name UX remains open |
| Results | Fixture-backed pass | KPI, ROI and OKR canonical screens expose seeded measurement/receipt, approved-vs-Actual realization, reconciliation/PIR, and KR/check-in/review readbacks | Piotr owner walkthrough; keep browser/owner gate open |
| Finance | Fixture-backed pass | Retained exact-six fixture exposes approved Statement, Analysis, Baseline, Prediction and Valuation workspaces with canonical lineage and computed readbacks | Piotr owner walkthrough; keep browser/owner gate open |
| Materials | Fixture-backed pass | Document Studio cold-opens 2 canonical sections; Deck Builder cold-opens 4 slides; Excele resolves the saved workbook and its XLSX preview | Piotr owner walkthrough; keep export/share/provider and owner gates open |
| Audits | Fixture-backed pass | Internal pack, program, criterion/evidence/finding/remediation, draft report and proposal cold-open; program preview crash and program deep link fixed | Piotr policy/owner decision; finding auto-selection and responsive/a11y remain open |
| Organization | Fixture-backed technical pass with finding | Exact OWNER profile, `27` claims, truthful readiness `UNKNOWN`, cold reopen and anonymous redirect passed | Piotr owner walkthrough; mixed-language/IA, responsive/theme/a11y and mobile remain open |
| Admin | Fixture-backed technical pass / rebuild boundary | Roster and pending failed-delivery invite cold-opened; retained Audit replay shows list `3`, stats `3/3/3` and CSV/list parity `3/3` after the canonical+legacy projection fix | Replay invite/role/revoke after the external rebuild stabilizes; owner/policy/backup/responsive gates remain open |
| Settings | Fixture-backed technical partial / UI retest pending | Profile, light theme, MFA deferral, OAuth unavailable states, Data Controls, cold deep links and anonymous redirect passed; canonical digest/regional/export/deletion-history backend contracts are fixed | Wire/retest the presenters for weekly digest, date format, pending export and cancelled deletion; preserve owner direction and policy gates |
| Partner | Fixture-backed pass | Profile, certification `1/10`, referral `W3PARTNER`, participant ledger and explicit economics policy gate cold-open; foreign/revoked/unbound boundaries verified | Piotr owner walkthrough; economics remains policy-disabled |

## Recommended order before Piotr resumes owner review

1. Resolve the remaining Organization localization and Settings presenter work without treating the active rebuild as accepted: `ORG-PF-005`, `SET-PF-005..007` UI portions.
2. Re-run the affected exact retained-fixture browser seams after the verified backend fixes, including Admin Audit/invite/role/revoke and harmless Settings preference/status actions.
3. Resume Piotr's guided owner walkthrough across all 16 populated retained fixtures; keep owner, provider, policy, responsive/theme/a11y and mobile gates separate.

Staging data was not copied. Deterministic disposable fixtures are safer, reproducible, tenant-scoped, and avoid importing production-like personal or confidential data into the review environment.

## Local remediation evidence

- `FilterableTable.filterA11y.test.tsx`: `12/12 PASS`.
- `ExecutiveModuleShell.test.tsx`: `17/17 PASS`.
- `AppRoutes.assessment-identity.test.ts`: `1/1 PASS`.
- `menuConfig.moduleStatusTruth.test.ts`: `2/2 PASS`.
- `AssessmentHub.method-core-cutover.test.tsx`: `4/4 PASS`, including the exact Method Core `limit=100` contract.
- `AuthView.fail-closed-errors.contract.test.tsx`: PASS with label-addressable Email and Password fields; live browser selection by visible label also passes.
- Root TypeScript check with 8 GB heap: `PASS` (no diagnostics).
- `git diff --check`: `PASS`.
- Exact authenticated browser replay: `16/16` entry routes plus `31/31` child surfaces reached stable render; `0` retained loading states, `0` visible source errors and `0` horizontal page overflows after bounded warm-up.
- Finance fixture-backed browser replay: approved Statement exact-six, computed Analysis (`CURRENT_RATIO=3`), 12-period Baseline (`372` outputs), Prediction passthrough (`372` rows) and DCF FCFF Valuation (`READY`) all open from canonical lists with their persisted lineage. The adopted runtime was then stopped without dropping its database.
- Results fixture-backed browser replay: KPI `DELIVERY_ON_TIME` exposes two verified measurements (`92`, `78`), critical deviation and `execution_receipt` source; OKR `Company Results Q3` exposes its review state, objective, one KR (`84/90`, `70%`), last check-in and reviewer. The ROI fixture now pins its immutable approval and Actual snapshots, so the organization view cold-reads `200,000` approved, `125,000` Actual and `62.5%` realization. The full tool exposes the verified Actual, 100% coverage snapshot, finalized `Benefits partially realized` PIR and accepted Finance divergence (`125,000` vs `110,000`). A separate frontend slot-mapping defect that rendered `[object Object]%` was fixed and browser-replayed as `62.5%`.
- Materials fixture-backed browser replay: the document fixture was migrated from the obsolete generic `content` payload to canonical `content_json_native` `DocumentSchema`, and cold reload now renders `Cel` plus `Rekomendacje`. The presentation fixture now carries a complete Unified Report `meta` envelope and four canonical slide intents, eliminating the backend `reading 'template'` 500 and rendering all four slides, notes entry and Builder controls. The workbook manifest now uses the product's canonical `artifactId` query key rather than ignored `workbookId`; the deep link resolves `Budżet pilotażu`, one sheet and the XLSX preview. Export/share/provider states remain deliberately unclaimed.
- Assessment fixture-backed browser replay: the Hub lists the exact canonical Method Core frozen and active DRD sessions (no legacy DRD substitution). The frozen route renders the immutable server Output with hash `12851515dc5b772a…`, two findings and the governed Initiative Draft; report generation and HTTP reopen remain visibly unavailable rather than simulated. The active route cold-opens the exact session/version, its two answered units and two evidence records, with the server-truth badge and role-gated freeze action. The adopted runtime was stopped while preserving its 817-migration database and FINAL marker manifest.
- Interview fixture-backed browser replay: Inbox and Sessions both expose the public in-progress and manager-review submitted sessions, the submitted editor cold-reads all three realistic answers, and the public token route renders all three questions without authentication while the revoked token displays an explicit expiry/revocation state. A same-screen progress defect was fixed: organization-defined question categories had been omitted from the properties calculation, producing `0% (0/0)` beside a truthful `3/3`; the rail now renders `100% (3/3)` and focused regression is `2/2 PASS`.
- Meetings fixture-backed browser replay: stable `meetingId` deep links cold-open pending, rejected and approved/materialized notes; the exact rejection reason is visible and durable receipt counts remain `0/0/1`. Focused UI is `12/12 PASS`, mounted/RealPG is `22/22 PASS`; the retained database and FINAL manifest remain available after identity-verified runtime stop.
- Tools fixture-backed browser replay: the guided and approved Dynamic SWOT records cold-open by exact `docId`. The fixture was corrected to include the canonical mission and output sections required by the current five-phase model; the UI now independently derives `80%` for the guided session and `100%` for the approved session instead of exposing the former contradictory `70/100` database claims beside `60%` content progress. Backend typecheck and diff-check pass; the adopted runtime was stopped with its database preserved.
- Audits fixture-backed browser replay: the internal unlicensed pack, program, TA.1 criterion, evidence/finding/remediation chain, draft report and draft initiative proposal render from the retained canonical database. A canonical detail without `members` no longer crashes the program preview, and `?tab=processes&programId=...` now cold-opens that preview. Focused Audits is `17/17 PASS`, runtime guard `20/20 PASS`; owner policy and finding auto-selection remain open.
- Partner fixture-backed browser replay: profile, certification progress `1/10`, referral code `W3PARTNER`, participant ledger and the explicit `AMD-PRT-ECONOMICS-002` policy gate all render from the tenant-bound fixture. Demo campaign/click seeding is disabled in the owner runtime and remains `0/0` after cold restart; foreign, revoked and unbound personas fail closed. Focused regressions are `29/29 PASS`.
- My Work fixture-backed browser replay: OWNER sees the pending `Approve pilot` decision and `Review pilot` task, while the requester sees the canonical three-step plan and one `MATERIALIZED / SUCCEEDED` target receipt. A real completion followed by cold OWNER login exposed and fixed a frontend omission: the personal-task query excluded terminal tasks even on the `Wszystkie` view. It now requests `includeDone:true`, and the completed task remains visible as `Zrobione`. Focused regression is `5/5 PASS`; stale task CAS returns `409 TASK_VERSION_CONFLICT`.
- Initiatives fixture-backed browser replay: the canonical register renders one complete `IN_EXECUTION` aggregate and the discovery inbox renders the alternate pending candidate at the correct `42%`; the accepted candidate is absent from the pending inbox and the Initiative Card preserves its `open/mode=doc` URL after cold load. The fixture was corrected from invalid `4.6/2.1` fit scores to the documented `0..1` scale and completed with the source/owner/problem/outcome fields required by the frontend read model. Focused UI/fixture is `13/13 PASS`; canonical unit plus isolated RealPG is `18/18 PASS`.
- Chat fixture-backed browser replay: the exact conversation renders the source message, URL citation, two source references and the pending governed document proposal with its pinned content hash/version. OWNER approval followed by `Create document` produced one successful materialization receipt; a new-tab cold reopen retained `Document created`. Independent DB readback confirms the proposal is `materialized`, decided by the fixture OWNER, with one succeeded mapping receipt (`attempt_count=1`, no error). No frontend defect was observed in this deterministic provider-free journey.
- Execution fixture-backed browser replay: `Realizacje` renders the accepted `Customer pilot delivery`, and Praca, Zasoby, Sterowanie and Raporty mount with honest empty canonical states. A real routing defect was fixed: `/execution/:caseId` no longer redirects to Chat and now cold-opens the exact Case `v1` workbench. API/SQL parity confirms Initiative `v3`, ACTIVE Case `v1`, closed BVP lineage and one immutable Results receipt. Deep-link/runtime spine is `4/4 PASS`; the retained 817-migration database and FINAL manifest remain available for owner review.
- Organization final fixture replay: exact `3d61730fd8ad` on `4080/4081` authenticated the deterministic OWNER, rendered the Professional Services profile and `27` claims, cold-opened readiness as truthfully `UNKNOWN`, and proved anonymous redirect. Mixed Polish/English remains visible; Piotr's owner/IA decision is not inferred.
- Admin final fixture replay: exact `3d61730fd8ad` retained replay on `4082/4083` authenticated the real OWNER and rendered Audit total/unresolved/high-risk `3/3/3` with exactly three canonical rows (`member_removed`, `role_change`, `role_change`). Authenticated list, stats and CSV endpoints all returned `200`; list and CSV each contained exactly three rows. The three-member roster and pending failed-delivery invitation with resend/revoke controls still cold-opened. No IAM mutation was attempted during the external rebuild. The owned runtime stopped with ports free and DB preserved; protected `3940/3941` was untouched.
- Settings final fixture replay: exact `3d61730fd8ad` on `4080/4081` rendered OWNER profile, Warsaw timezone, light theme, truthful MFA deferral, mostly unavailable OAuth providers and Data Controls; cold deep links and anonymous redirect passed. The fixture/backend contracts are now corrected for canonical weekly digest, full regional preferences, pending export and latest cancelled deletion (`7/7` + `4/4` RealPG PASS); React presenter wiring and browser retest remain pending. No destructive or provider action was attempted.
- Runtime hygiene: every Organization/Admin/Settings replay ran sequentially. Each identity-verified adopted runtime was stopped while preserving its retained DB/FINAL manifest; ports `4080/4081` were free afterward and protected `3940/3941` were not touched.
- Browser geometry after the shared-table fix:
  - Audits card `321..958` inside visible scroll viewport `289..990`.
  - Finance card `305..966` inside visible scroll viewport `273..998`.
  - Meetings card `321..968` inside visible scroll viewport `289..1000`.
  - Results card `350.5..1022.5` inside window `0..1055` despite its retained 980 px table canvas.
- Document Studio after the top-bar fix: title width `118.5 px`, action cluster starts at `448.5 px`, overlap `false`.
