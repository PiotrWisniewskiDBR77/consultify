# Analiza G13 — moduły 09_RESULTS…16_PARTNER — 2026-09-03

Zakres: bramka `G13` („analiza rozwiązania i wpływu") ośmiu modułów: 09_RESULTS,
10_FINANCE, 11_MATERIALS, 12_AUDITS, 13_CHAT, 14_ADMIN, 15_SETTINGS, 16_PARTNER.
**Wyłącznie analiza — zero napraw w `src/`/`server/src/`.**

Marker startowy: `26d905569a`. Worktree: `/private/tmp/ag-g13-b`, branch
`agent/g13-modules-09-16-20260903`. Zero serwera, zero bazy — źródła: dziewięć
rejestrów z `docs/program/waves/WAVE_03_ACCEPTANCE/` (`REJESTR_G06_JEZYKI_MOTYWY`,
`REJESTR_G05_PRZELOT`, `REJESTR_PODGLAD_RODZINA`, `REJESTR_SUROWE_ID`,
`REJESTR_CRIMSON`, `TRIAZ_UWAG`, `REJESTR_CROSSORG`, `REJESTR_SCHEMAT_OD_ZERA`,
`REJESTR_RESULTSHUB`) + `MAPA_GRAFIKA_MODULY_20260902.md` (mapowanie ekranów →
moduły) + wyrywkowa weryfikacja bezpośrednio w kodzie tej gałęzi.

## Metoda

Dla każdego znaleziska z rejestrów: (1) ustal, którego z moich ośmiu modułów
dotyczy (wg `MAPA_GRAFIKA_MODULY_20260902.md`, pole `gdzie`); (2) tam gdzie
rejestr cytuje plik:linię — **odczytaj tę linię na tej gałęzi dziś**, bo
rejestry mają 1 dzień i gałąź miała w tym czasie duże zmiany (usunięcie
`ResultsHub`, naprawa kontraktu Finansów, naprawa bramki dostępu Ekonomii,
kilka napraw z rejestru surowych ID). Znaleziska, które zdążyły się już
naprawić, oznaczam `NIEAKTUALNE` z dowodem (commit lub cytat kodu). Reszta
niżej jest zweryfikowana na dziś, nie przepisana z rejestru bez sprawdzenia.

**Już potwierdzone `NIEAKTUALNE` w trakcie tej analizy** (więcej niż zapowiadał
brief — patrz sekcje modułów):
1. `src/components/Results/ResultsHub.tsx` i 25 martwych dzieci — **usunięte**
   (potwierdzone: `test -f ...ResultsHub.tsx` → brak pliku; `git log` pokazuje
   commit `b1ccf551be "chore(results): warstwa 4 — korzeń: ResultsHub.tsx"`).
2. Bramka API Ekonomii (Finanse) — **naprawiona**. `REJESTR_G05` (02.09) opisywał
   `betaGate` jako atrapę przepuszczającą każdego zalogowanego użytkownika mimo
   `MODULE_ECONOMICS='closed'`. Dziś `server/src/Gateway.ts:1226-1229` montuje
   `app.use('/api/economics', gatewayVerifyToken, createModuleGate('MODULE_ECONOMICS'), economicsRoutes)`
   — komentarz nad wywołaniem wprost cytuje ten sam pomiar i datuje naprawę na
   `2026-09-02`. Bramka API i bramka menu klienta czytają dziś tę samą mapę.
3. Ustawienia → Powiadomienia, martwy wołacz frontu — **naprawiony**.
   `REJESTR_G05` (02.09) opisywał `src/hooks/useUserNotificationPreferences.tsx:211`
   wołające `PUT /api/settings/notifications/preferences` (odwrócone segmenty
   względem trasy backendu `/preferences/notifications`) → `404`, zapis nigdy
   nie trafiał do bazy. Dziś (linie 153 i 214 tego samego pliku) wołacz używa
   poprawnej kolejności `/api/settings/preferences/notifications` — zgodnej z
   trasą backendu. Commit: `02b6d03f9a "fix(settings): correct reversed URL
   segments in notification preferences hook"`.
4. `InitiativeTeamSection.tsx` (surowy `userId`) i `WatchingTab.tsx` (surowy enum
   `notifyOn`) z `REJESTR_SUROWE_ID` — **naprawione**, commity `67c09e5273` i
   `6d7eac6306` obecne na tej gałęzi.

Te cztery pozycje NIE wchodzą do tabel niżej jako otwarte znaleziska — zostały
tylko odnotowane jako kontrola jakości źródeł (rejestry starzeją się w dniach,
nie tygodniach, zgodnie z regułą CLAUDE.md).

---

## 09_RESULTS — Wyniki

22 ekrany zmapowane (19 w mianowniku odbioru), 10 z otwartą uwagą właściciela.
Katalog źródłowy: `08-wyniki` → `src/components/ResultsVNext/**`. Martwy
`ResultsHub.tsx` (2228 linii + 26 martwych dzieci, ~27 tys. linii łącznie z
poddrzewem) usunięty wczoraj — patrz `NIEAKTUALNE` #1 wyżej; poniższe znaleziska
dotyczą wyłącznie żywego `ResultsVNext`.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| `ResultsVNextRegistryShell.tsx` (widoki KPI/ROI/OKR registry) buduje własny `<aside>` zamiast używać `TableWithPreviewLayout` i nie ma ANI JEDNEGO `data-preview-pane` w kodzie — kanoniczny przyrząd pomiaru wysokości podglądu (`measure-preview-canon.mjs`) jest dla niego ślepy. Ręczna inspekcja DOM sugeruje ~0 px luki, ale to NIE jest zmierzone kanonicznym narzędziem. | `REJESTR_PODGLAD_RODZINA_20260902.md` §K2; zweryfikowane dziś: `grep -n data-preview-pane src/components/ResultsVNext/ResultsVNextRegistryShell.tsx` = pusto | Dziś prawdopodobnie brak realnego wpływu (ręczny pomiar OK), ale moduł nie ma dowodu — jeśli regresja wjedzie, żaden automat jej nie złapie na tym ekranie | Pojedynczy plik, ale wzorzec „własny `<aside>` zamiast kanonu" to ten sam błąd co reszta rodziny TableWithPreviewLayout (20 innych ekranów w programie, opisane w tym samym rejestrze) | DROBNE — dodać `data-preview-pane` do istniejącego `<aside>`, ~5-10 linii | 3 — tanie i odblokowuje pomiar na przyszłość, ale nie ma dziś widocznego defektu dla użytkownika |
| 4 ekrany mają realne naruszenia axe (nie artefakt harnessu, zweryfikowane skanem `#dev-render-root`): `results-vnext-kpi-scorecards`, `results-vnext-teresa-kpi-deviation`, `results-vnext-teresa-okr-reflection`, `results-zestawienia` — color-contrast, label, select-name, scrollable-region-focusable | `REJESTR_G06...md`, „Dopisek — dyżur 2026-09-03" | Użytkownicy czytników ekranu i klawiatury nie rozróżnią pól formularza / nie odczytają kontrastu na 4 z ~20 mierzonych ekranów | Jeden z 12 modułów z realnymi naruszeniami axe w tym samym dyżurze (rodzina systemowa, nie unikat Wyników) | ŚREDNIE — 4 ekrany, różne pliki źródłowe (formularze OKR/KPI), nie jeden komponent | 4 |
| Formularz „Refleksja OKR" (`results-vnext-teresa-okr-reflection`) i „Odchylenie KPI" (`results-vnext-teresa-kpi-deviation`) w stanie OTWARTYM wyglądają jak sprzed lat — płaskie niestylowane pola HTML, szare przyciski. Sam właściciel wskazał to jako prawdopodobne źródło skargi „grafika jak sprzed 5 lat"; stan ZAMKNIĘTY tej samej sprawy był już naprawiany w innej fali, stan OTWARTY nie | `TRIAZ_UWAG_20260902.md` UW-09-05, UW-09-07 | Konsultant wypełniający ocenę widzi rażąco gorszy standard wizualny niż reszta aplikacji w kluczowym, częstym przepływie | Rodzina 2 (ten sam wzorzec formularza w obu ekranach — prawdopodobnie współdzielony komponent formularza refleksji/odchylenia) | DUŻE — przeprojektowanie wizualne całego formularza, nie punktowa poprawka CSS | 2 — wysoki, powtarzalny wpływ wizualny na częsty ekran |
| ROI („N-karta"): 3 ekrany (`results-vnext-roi-full-tool`, `results-vnext-roi-model`, `results-vnext-roi-pir-outcomes`) mają jedno, wspólne zgłoszenie właściciela o przebudowie z poziomego menu zakładek na jedną kartę artefaktu z sekcjami; to samo dotyczy `results-vnext-okr-workspace` (6 zakładek → sekcje karty N) | `TRIAZ_UWAG_20260902.md` UW-09-01/02/03/09; plan nazwany w `docs/program/grafika/ODLOZONE.md` | Nawigacja rozjeżdża się z resztą aplikacji, która już ma wzorzec N-karty gdzie indziej — spójność produktu, nie usterka pojedynczego ekranu | Rodzina 4 (3 podekrany ROI + 1 OKR workspace, jedna decyzja architektoniczna) | DUŻE — przebudowa powłoki, wymaga decyzji produktowej (już nazwana, czeka na realizację) | 5 — ważne, ale to praca architektoniczna poza jednym dyżurem, nie zator |
| `results-vnext-attention` i `results-vnext-search-registry` są za domyślnie WYŁĄCZONYMI flagami (`attentionEntry`, `resultsSearch`) — właściciel zgłosił uwagę do ekranu, którego większość użytkowników nie widzi w ogóle | `TRIAZ_UWAG_20260902.md` (tabela „ZA FLAGĄ") | Zero wpływu na dzisiejszych użytkowników produkcyjnych (funkcja ukryta); wpływa tylko na to, co widzi właściciel przy przeglądzie | Rodzina — wzorzec „za flagą" powtarza się w 4 miejscach programu (patrz CLAUDE.md reguła 7); w tym module 2 ekrany | DROBNE (przełącznik) do DUŻE (jeśli decyzja to realne otwarcie funkcji dla wszystkich) | 6 — decyzja właściciela, nie defekt do naprawy |
| `results-vnext-teresa-okr-reflection`: `GET /api/vnext/results/okr/objectives/:id/reflection` zwraca 404 w pomiarze — serwer MA trasę (`server/src/routes/resultsVnext/okr.routes.ts:2491`), ale fixture dev-render stubuje tylko `POST`, nie `GET`. Zweryfikowane w rejestrze jako luka NARZĘDZIA pomiaru, nie produktu | `REJESTR_G06...md`, „Dopisek" | Brak — to defekt harnessu (`dev-render/screens/results-vnext-teresa-okr-reflection.tsx`), nie produktu | Pojedynczy przypadek | DROBNE (poza `src/`/`server/src/` — plik w `dev-render/`, poza zakazem tego dyżuru, ale też poza priorytetem produktu) | 7 — nie blokuje nic realnego |
| `:projectId=all` na `POST/GET /api/results-strategic/:projectId/okr/objectives` — SQL filtruje tylko po `organization_id`, ignoruje `projectId`; `requireProjectCapability(...,{shadow:true})` nie egzekwuje dziś żadnej bramki roli/flagi (tryb cieniowy = tylko loguje, nie blokuje) | `REJESTR_G05_PRZELOT_20260902.md`, wiersz „Wyniki"; zweryfikowane dziś: `requireProjectCapability('okr.objective.create', ...)` nadal zamontowane bez wymuszenia (shadow) w `server/src/routes/resultsStrategic.routes.ts` | Dziś brak izolacji per-projekt na poziomie zapisu OKR (każdy użytkownik organizacji pisze do „wszystkich" projektów jednym wywołaniem) — nie cross-org (izolacja organizacji działa), ale cross-project w tej samej organizacji | Wzorzec „shadow capability = brak realnej bramki" może występować w innych trasach `results-strategic`, nie tylko tej jednej — niezweryfikowane poza tym jednym punktem wejścia | ŚREDNIE — włączenie egzekwowania (`shadow:false`) wymaga decyzji, czy istniejące dane/flow są gotowe na twardą blokadę | 3 — nie jest to dziura cross-tenant (bardziej dotkliwa kategoria), ale wymaga świadomej decyzji przed jakąkolwiek pracą nad uprawnieniami w tym module |

**Kolejność modułu:** 1) formularze Teresa OKR/KPI (wizualny dług na częstym
ekranie, DUŻE ale precyzyjnie zlokalizowane) i decyzja „shadow capability" —
oba mają realny wpływ na użytkownika dziś; 2) `data-preview-pane` (tanie,
odblokowuje pomiar); 3) axe na 4 ekranach; 4) przebudowa N-karty ROI/OKR — duża
praca architektoniczna, już nazwana, nie pilna względem powyższych.

---

## 10_FINANCE — Finanse

16 ekranów zmapowanych (13 w mianowniku odbioru), 4 z otwartą uwagą właściciela.
Katalog źródłowy: `09-finanse` → `src/components/Economics/**`. Bramka API
modułu (`createModuleGate('MODULE_ECONOMICS')`) naprawiona wczoraj — patrz
`NIEAKTUALNE` #2 na górze dokumentu; poniższe znaleziska są zweryfikowane jako
wciąż otwarte dzisiaj.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| `FinanceHub.tsx` renderuje `{content}` (czyli `<TableWithPreviewLayout>`) jako bezpośrednie dziecko `<StandardModuleBar>` bez opakowania `<div className="flex-1 min-h-0">` — luka pod kartą podglądu **263 px, największa zmierzona w całym programie**. Zweryfikowane dziś: `src/components/Economics/FinanceHub.tsx:4106` — `{content}` nadal bez wrappera | `REJESTR_PODGLAD_RODZINA_20260902.md` §K4/K5 (wzorzec naprawy opisany, nie wykonany); zweryfikowane w kodzie 2026-09-03 | Karta podglądu wygląda ucięta/za krótka dla każdej pozycji w tabelach Finansów — najbardziej widoczna instancja tego defektu w całym produkcie | Ten sam, jednozdaniowy root cause (brak `flex-1 min-h-0` na najbliższym przodku) powtarza się w **6 ekranach w całym programie** (Finanse 263px, AIChat/ChatSignalsFeed 58px, Execution-standalone 216px*, Initiatives ×2 67px/92px, Vault 200px) — jedna naprawiona referencja (`ExecutionResourcesSurface.tsx`) pokazuje dokładny wzorzec do powtórzenia | DROBNE — jedna linia (`<div className="flex-1 min-h-0">` wokół `{content}`), wzorzec naprawy już istnieje w kodzie (referencja) | 1 — najtańsza, najbardziej widoczna, wzorzec gotowy do skopiowania |
| Kolumna „Initiative" w panelu Value Capture pokazuje surowy `{gate.initiativeId}` zamiast nazwy inicjatywy — `ValueCaptureGate` nie ma pola z nazwą | `REJESTR_SUROWE_ID_20260902.md` poz. #4; zweryfikowane dziś: `src/components/Economics/panels/ValueCapturePipelinePanel.tsx:344` nadal `{gate.initiativeId}` | Konsultant czytający tabelę powiązań finanse↔inicjatywy widzi UUID zamiast nazwy — nie wie, o którą inicjatywę chodzi bez przełączania się do innego ekranu | Pojedynczy przypadek w tym module, ale część większej, systemowej rodziny „surowy id zamiast etykiety" (≥35 wcześniejszych napraw w historii repo) | ŚREDNIE — wymaga zmiany kontraktu serwera (dociągnięcie nazwy razem z `initiativeId`, analogicznie do już wykonanej naprawy `base_period_id`) | 3 — wymaga kontraktu, nie jest to poprawka samego UI |
| `finance-baseline-workspace` ma decyzję właściciela `poprawka` (jedyna `poprawka` w mapie grafiki tego modułu) z otwartą uwagą; `finance-analysis-workspace`, `finance-compare-panel`, `finance-valuation-workspace` też mają otwarte uwagi | `MAPA_GRAFIKA_MODULY_20260902.md` §10_FINANCE | Właściciel oznaczył konkretny ekran jako wymagający korekty, nie tylko akceptacji — treść poprawki nierozliczona w tym dyżurze (poza zakresem: to G17, nie G13) | Pojedynczy ekran z formalną `poprawka`; pozostałe 3 to zwykłe otwarte uwagi, częściowo już rozliczone w `TRIAZ_UWAG` (patrz niżej) | Nieznany — zależy od treści poprawki, nie odczytanej w tym dyżurze | 5 — wymaga najpierw przeczytania treści poprawki (G17), nie jest to defekt do analizy kosztowej bez tego kroku |
| `ValuationWorkspace.tsx` deklaruje `WorkspaceBarConfig` (poprawnie wg kanonu, nie rysuje własnego nagłówka), ale nie ma w kodzie potwierdzenia, że wewnętrzne przyciski akcji są kanonicznymi pigułkami CTA — właściciel: „przyciski u góry są słowami, nie okrągłymi przyciskami" | `TRIAZ_UWAG_20260902.md` UW-10-02 (`DROBNE`, niepotwierdzone jako zrobione) | Niespójność wizualna z kanonem StandardModuleBar na jednym z ekranów Finansów o wysokiej częstości użycia (Wycena) | Pojedynczy ekran, wzorzec kanonu pigułek CTA jest już zdefiniowany gdzie indziej w aplikacji | DROBNE — prawdopodobnie punktowa poprawka CSS/komponentu przycisku, wymaga najpierw zrzutu do potwierdzenia | 4 — tanie, ale wymaga jednego zrzutu weryfikacyjnego przed wyceną |
| 2 ekrany z realnymi naruszeniami axe (nie artefakt harnessu): `finance-model-workspace` (color-contrast, label) i `finance-statement-pack-workspace-v2` (color-contrast); `finance-model-workspace` dodatkowo ma `404 GET /api/v8/finance/models/model-dbr77-demo-1/versions` — prawdopodobna luka fixture dev-render, nie zweryfikowana jako trasa serwera w tym dyżurze | `REJESTR_G06...md`, „Dopisek — dyżur 2026-09-03" | Czytelnicy klawiatury/kontrastu tracą informację na 2 z 13 ekranów w mianowniku odbioru | Jeden z 12 modułów z realnymi axe w tej samej fali pomiarowej — rodzina systemowa | ŚREDNIE — 2 różne ekrany/komponenty | 4 |
| `components/Economics` niesie **46 wystąpień** klas koloru crimson (`primary-*`/`crimson-*`) w tle i tekście — 34 primary + 12 crimson, wszystkie w próbce K4 oceniono jako 100% dekoracja (ikony, tła odznak, CTA drugorzędne), zero semantyki krytycznej | `REJESTR_CRIMSON_20260902.md` §K2/K4 | Czerwień poza semantyką krytyczną w module, który akurat naprawił dziś bramkę dostępu — wizualny dług nie związany z funkcją | Część systemowego długu 5325 wystąpień w 609 plikach; Finanse to średniej wielkości nośnik (46 z 5325, ~0,9%) | DUŻE jako projekt (46 wystąpień, 6 plików) — pojedyncze podmiany są DROBNE, ale wymagają przejścia całego pliku żeby nie zostawić resztek | 6 — niżej niż funkcjonalne defekty, ale tańsze niż przebudowy architektoniczne w innych modułach |

**Kolejność modułu:** 1) luka podglądu 263px (najtańsza, wzorzec gotowy,
największa w programie); 2) raw `initiativeId` (wymaga małego kontraktu, ale
ma jasny wzorzec z naprawy `base_period_id`); 3) `finance-baseline-workspace`
poprawka (wymaga najpierw przeczytania treści od właściciela); 4) axe na 2
ekranach i przycisk-jako-słowo w Wycenie; 5) crimson (46 wystąpień, projekt
kosmetyczny, nie blokujący).

---

## 11_MATERIALS — Materiały

40 ekranów zmapowanych (35 w mianowniku odbioru), **15 z otwartą uwagą
właściciela — najwięcej ze wszystkich moich ośmiu modułów**. Katalog
źródłowy: `10-materialy` → `src/components/DocumentStudio/**`,
`src/components/AIChat/KimiWorkspace/**` (Excel/arkusze), `src/components/Materials/**`.
Zgodnie z briefem: **to najgorszy moduł w produkcie** — 12 ekranów z
naruszeniami dostępności (w tym krytyczne w kreatorach szablonów) i 6 ekranów
z realnie zwiniętymi sekcjami.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| 3 kreatory szablonów (`template-builder-deck`, `template-builder-doc`, `template-builder-table`) mają naruszenia axe **KRYTYCZNE** (nie tylko poważne): `label` (critical), `select-name` (critical), `nested-interactive` (serious, do 6 węzłów) — pola formularza bez etykiet i selecty bez nazwy dostępnej | `REJESTR_G06...md`, „Dopisek — dyżur 2026-09-03" (3/3 kadrów każdy) | Użytkownik na czytniku ekranu lub klawiaturze nie jest w stanie wypełnić kreatora nowego szablonu — to nie jest kosmetyka, to blokada funkcji dla części użytkowników w KLUCZOWYM przepływie tworzenia treści | Rodzina 3 — te same trzy klasy naruszeń (`label`+`select-name`+`nested-interactive`) powtarzają się identycznie na wszystkich trzech kreatorach → wspólny formularz/komponent pól kreatora, nie trzy niezależne defekty | ŚREDNIE — jeśli to jeden współdzielony komponent formularza kreatora (prawdopodobne, biorąc pod uwagę identyczny wzorzec naruszeń), naprawa w jednym miejscu zamyka 3 ekrany; DUŻE jeśli pola są zduplikowane per-typ artefaktu | 1 — krytyczna dostępność w funkcji tworzenia, najwyższy priorytet modułu |
| 6 ekranów mają REALNIE zwinięte sekcje, które automat rozwijania nie potrafi rozwinąć (nie fałszywy alarm — sprawdzone też przez wysokość `aria-controls`): `document-artifact` (5/10 nadal zwinięte), `document-studio-menu-pliku` (1/6), `document-studio-save-as-template` (2/7), `excele-edytowalna-siatka` (3/12), `excele-prawy-panel-standard` (3/12), `sheet-artifact` (3/12) | `REJESTR_G06...md`, „Dopisek", sekcja „Realne znaleziska" | Użytkownik klika, żeby rozwinąć sekcję (np. panel właściwości dokumentu/arkusza) i część treści zostaje ukryta mimo interakcji — funkcja wygląda na zepsutą | Rodzina 6 — dwie grupy: dokumenty (`document-artifact`, `document-studio-*`, 2 ekrany) i arkusze/deck (`excele-*`, `sheet-artifact`, 3 ekrany) — sugeruje 2 różne komponenty accordion, nie 1 | ŚREDNIE — prawdopodobnie 1-2 komponenty accordion współdzielone w obrębie każdej grupy | 1 (razem z kreatorami) — realna, widoczna usterka interakcji na 6 ekranach |
| Deck/Excel: brak narzędzi ręcznej edycji w prawym panelu artefaktu — właściciel wprost: „to trzeba dorobić" (deck), „nie mam tutaj w ogóle narzędzia Excelowego... W Wordzie mogę, w PowerPoincie też nie mogę" (Excel) | `TRIAZ_UWAG_20260902.md` UW-11-12/13/14 (**DUŻE** ×3) | Użytkownik nie może ręcznie doedytować wygenerowanego arkusza/prezentacji — funkcja edycji istnieje dla Worda, nie dla pozostałych dwóch typów artefaktów | Rodzina 3 (deck + excel prawy panel + sheet layout restrukturyzacja — właściciel sam połączył te 3 uwagi jako jedną pracę) | DUŻE — brakująca funkcjonalność, nie defekt wizualny; wymaga projektu narzędzia edycji per typ artefaktu | 2 — realna luka funkcjonalna zgłoszona wprost przez właściciela, wyższy priorytet niż kosmetyka |
| CRUD cudzych dokumentów Studio (silnik DocumentStudio) — strażnik cross-org **potwierdzony ZAMKNIĘTY** dowodem mutacyjnym (po skasowaniu strażnika obcy odczytuje/nadpisuje/kasuje cudzy dokument; z przywróconym strażnikiem — `404` na każdej z 6 tras) | `REJESTR_CROSSORG_20260902.md`, rodzina #5 | Brak — to POTWIERDZENIE bezpieczeństwa, nie defekt. Odnotowane dla kompletności analizy G13 (moduł ma jedną z 6 rodzin cross-org w programie i wypada czysto) | n/d — pozytywny wynik | n/d | n/d — nie wymaga kolejki naprawczej |
| `excele-jeden-widok-recent` — właściciel chce USUNIĘCIA ekranu jako zbędnego duplikatu (otwarcie arkusza od razu pyta o źródło: szablon/czysty/Teresa, więc osobny ekran „Recent" jest nadmiarowy) | `TRIAZ_UWAG_20260902.md` UW-11-04 | Brak negatywnego wpływu dziś (ekran działa), ale to decyzja o usunięciu funkcji, nie naprawa | Pojedynczy ekran | DROBNE jako usunięcie trasy/ekranu, ale wymaga decyzji produktowej najpierw | 4 — tanie wykonawczo, ale czeka na decyzję właściciela (nie jest to defekt do naprawy z automatu) |
| `report-builder-library-template` — ekran renderował się „pusty" (13 znaków tekstu) w pomiarze z dyżuru 280; przyczyna zlokalizowana: komponent wymaga dodatkowych parametrów URL (`&new=true&templateArtifactId=...`), bez których `ReportBuilderView` nie rozpoznaje trybu — z poprawnymi parametrami ekran renderuje się w pełni (402 znaki, realny formularz) | `REJESTR_G06...md`, „Korekta metodologiczna" | Brak realnego wpływu na użytkownika — to błąd METODOLOGII POMIARU (brakujące query param w harnessie), nie defekt produktu | n/d | n/d | n/d — `NIEAKTUALNE` jako znalezisko produktowe; wymaga tylko przemierzenia dyżuru 280 z poprawnymi parametrami, nie naprawy kodu |
| `components/DocumentStudio`+arkusze/deck nie są w top 30 nośników crimson wprost pod jedną nazwą katalogu w rejestrze (rozproszone pod różnymi ścieżkami), ale 12 z 40 ekranów modułu ma realne naruszenia axe (patrz wyżej) — moduł ma też szeroki ślad crimson rozproszony po wielu plikach edytora (nie zmierzony jako jeden wpis w top 30 — wymaga osobnego zliczenia per plik, poza budżetem tego dyżuru) | `REJESTR_CRIMSON_20260902.md` §K2 (moduł nie w top 30 jako pojedynczy katalog) | Nieustalone — wymaga dodatkowego zliczenia | Nieustalone | Nieznany | 5 — oznaczone jako luka pomiarowa do domknięcia w kolejnym dyżurze crimson, nie duplikować pracy tego dyżuru bez pomiaru |

**Kolejność modułu:** 1) axe krytyczny w 3 kreatorach szablonów + 6 ekranów z
realnie niedziałającym rozwijaniem sekcji — oba dotykają PODSTAWOWEJ
funkcjonalności (tworzenie/przeglądanie treści), nie kosmetyki; 2) brakujące
narzędzia ręcznej edycji Deck/Excel — zgłoszone wprost przez właściciela,
duża praca ale jasno zdefiniowana; 3) `data-preview-pane`/decyzje kosmetyczne
(usunięcie `excele-jeden-widok-recent`); 4) crimson — wymaga osobnego
pomiaru per plik, nie oszacowany w tym dyżurze.

---

## 12_AUDITS — Audyty

Najmniejszy z moich modułów: 4 ekrany zmapowane, wszystkie w mianowniku
odbioru, 1 z otwartą uwagą właściciela. Katalog źródłowy: `11-audyty` →
`src/components/Audit/**`.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| Cały edytor raportu DRD (`audyty-drd-report`) — panel AI, akcje per sekcja, eksport PDF, żywy backend — istnieje od dawna, ale nie ma ŻADNEGO wejścia w produkcie poza flagą `ff.drdReport`/`VITE_DRD_REPORT_ENABLED` domyślnie WYŁĄCZONĄ. Zweryfikowane dziś: `src/utils/drdReportFlag.ts:41-49` (`readEnvFlag`) zwraca `false` gdy zmienna nie jest ustawiona, bez override w localStorage/query — flaga jest OFF z automatu | `TRIAZ_UWAG_20260902.md` UW-12-01 („Co zaskoczyło" #1 — „najbardziej dobitny przypadek hipotezy z briefu"); zweryfikowane w kodzie 2026-09-03 | Właściciel pytał „gdzie to jest" — bo dosłownie nigdzie nie prowadzi. Gotowa, działająca funkcja jest niewidoczna dla każdego użytkownika, który nie zna dokładnej flagi | Wzorzec „za flagą" (CLAUDE.md reguła 7) — ten sam kształt co 2 ekrany w 09_RESULTS, ale tu to CAŁY moduł raportowania, nie podekran | DROBNE jako przełącznik (zmiana wartości domyślnej flagi), ale wymaga decyzji właściciela: czy funkcja jest gotowa do pokazania wszystkim, czy potrzebuje najpierw wizualnego/funkcjonalnego doszlifowania | 1 — najtańsza możliwa naprawa (przełącznik) o największym pojedynczym wpływie w tym module — gotowa funkcja czeka na odkrycie |
| `audyty-warsztat-kryterium` ma realne, poważne naruszenia axe: `color-contrast` (15-25 węzłów na kadr!), `heading-order` — jedyny z 4 ekranów modułu z realnym długiem dostępności | `REJESTR_G06...md`, „Dopisek", sekcja „Realne znaleziska" | Ekran warsztatu kryteriów (praca robocza audytora) ma masowo nieczytelny kontrast na wielu elementach jednocześnie — nie pojedynczy element, tylko systemowy problem palety na tym ekranie | Pojedynczy ekran, ale skala naruszenia (15-25 węzłów) sugeruje problem na poziomie komponentu/tokenu koloru używanego wielokrotnie na stronie, nie 15 osobnych literówek | ŚREDNIE — wysoka liczba węzłów zwykle oznacza jeden zły token/klasę użytą w pętli (tabela/lista), więc naprawa może być tańsza niż liczba sugeruje, ale wymaga potwierdzenia w kodzie | 2 — realny, poważny dług dostępności na jedynym dotkniętym ekranie |
| Ustalenie audytowe: pełny łańcuch zapisu (pakiet → zatwierdzenie eksperckie → publikacja → program → member → finding) **PRZEŻYWA** realny HTTP i odczyt na zimno — potwierdzone bez zastrzeżeń | `REJESTR_G05_PRZELOT_20260902.md`, wiersz „Audyty" | Brak — pozytywne potwierdzenie, moduł ma działający, złożony przepływ zapisu | n/d | n/d | n/d — nie wymaga kolejki naprawczej |

**Kolejność modułu:** 1) odkrycie/otwarcie `audyty-drd-report` (tani
przełącznik, duży pojedynczy zysk — cała funkcja już istnieje); 2) axe na
`audyty-warsztat-kryterium`. Moduł ma najmniej otwartych znalezisk z
ośmiu — proporcjonalnie tani do domknięcia.

---

## 13_CHAT — Czat

7 ekranów zmapowane, wszystkie w mianowniku odbioru, 4 z otwartą uwagą
właściciela. Katalog źródłowy: `01-czat` → `src/components/AIChat/**`.
**Ten moduł już miał częściową pracę G13** (stan zastany
`DRAFT_COMPLETE`, 17 nazwanych zadań z kryteriami akceptacji — patrz
`G13` w pliku modułu). Poniżej DOKŁADAM, nie duplikuję: nowe znaleziska z
rejestrów wczorajszych + rozstrzygnięcie zlecenia „sprawdź, co zostaje z
dwóch zgłoszonych crashy jako realne".

### Rozstrzygnięcie crashy (zlecenie: „jeden okazał się harnessem, drugiego nie odtworzono — sprawdź co zostaje")

Żaden z dziewięciu rejestrów tego dyżuru ani plik modułu nie zawiera
narracji „jeden harness, drugi nieodtworzony" dosłownie — to musiało
paść w innej, nieudostępnionej mi rozmowie. Co JEST udokumentowane i
**świeże (dzisiejsza data, ten sam dzień co ten dyżur)**: `G06` modułu
(`modules/13_CHAT/MODULE_ACCEPTANCE.md:26`, dyżur `agent/g06-modules-13-16-20260903`)
zmierzył pełną macierz 8 kadrów × 7 ekranów i znalazł **dwa inne, nowe,
realne crashe produktu**, niezależne od axe:

1. `teresa-chipy-sugestii` — `TypeError: feed.signals is not iterable`,
   `ChatSignalsFeed.tsx:35`, przechwycony przez `ErrorBoundary`, 8/8 kadrów,
   jasny i ciemny motyw dają IDENTYCZNY obraz (`ΔL=0.00` — ekran nie
   renderuje treści w ŻADNYM motywie). Zweryfikowane dziś w kodzie:
   `useSignalsFeed.ts:20` inicjalizuje `signals` jako `[]`, ale
   `setSignals(response.signals)` (linia 42) nadpisuje stan wprost
   odpowiedzią API bez sprawdzenia, czy `response.signals` istnieje —
   jeśli odpowiedź (prawdziwa lub z fixture harnessu) nie ma pola
   `signals`, kolejny `[...feed.signals]` (`ChatSignalsFeed.tsx`, `sorted`)
   rzuca dokładnie ten zgłoszony błąd. Nie rozstrzygnąłem w tym dyżurze,
   czy to fixture dev-render czy realny kontrakt API zwraca czasem
   odpowiedź bez `signals` — wymaga jednego dodatkowego sprawdzenia przed
   wyceną naprawy.
2. `chat-split-teresa-right` — w motywie light dodatkowy crash
   `Invalid hook call`/`TypeError: Cannot read properties of null
   (reading 'useEffect')`, NIEOBECNY w dark (25 vs 22 błędów konsoli, ta
   sama klatka 1440×900) — niestabilność zależna od motywu, nie fałszywy
   alarm automatu (4/8 kadrów, wszystkie light, zostają z widocznymi
   zwiniętymi sekcjami mimo automatu rozwijania; dark tego samego ekranu
   rozwija się poprawnie).

**Wniosek dla zlecenia:** nie mogę potwierdzić ani zaprzeczyć narracji o
DWÓCH WCZEŚNIEJSZYCH zgłoszeniach bez dostępu do źródła tej narracji — nie
znalazłem go w dziewięciu rejestrach ani w historii commitów modułu z
frazami „harness”/„crash” w kontekście Czatu poza tymi dwoma. Traktuję
powyższe DWA znaleziska G06 z dzisiejszej daty jako **aktualny, udokumentowany
stan realnych crashy modułu** — to one wchodzą do tabeli niżej.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| `teresa-chipy-sugestii` nie renderuje się w ŻADNYM motywie — `feed.signals is not iterable` w `ChatSignalsFeed.tsx`, przechwycone przez ErrorBoundary (użytkownik widzi pusty/błędny stan zamiast treści) | G06 modułu (`MODULE_ACCEPTANCE.md:26`); zweryfikowane w kodzie `useSignalsFeed.ts:20,42` | Ekran sugestii chipów Teresy jest praktycznie martwy dla każdego użytkownika, który do niego trafi — nie kosmetyka, brak funkcji | Pojedynczy ekran, ale dzieli komponent `ChatSignalsFeed`/`useSignalsFeed` z ekranem `chat-signals-feed` (osobny wpis w mapie grafiki) — wymaga sprawdzenia, czy `chat-signals-feed` ma ten sam błąd | ŚREDNIE — zależy od tego, czy defekt jest w fixture harnessu (DROBNE, poza `src/`) czy w kontrakcie API/hooku (`useSignalsFeed.ts`, brak walidacji `response.signals`) | 1 — crash blokujący cały ekran, najwyższy priorytet modułu |
| `chat-split-teresa-right` crashuje TYLKO w motywie light (`Invalid hook call`) — dark tego samego ekranu działa poprawnie; 4/8 kadrów (wszystkie light) mają też sekcje, które nie rozwijają się mimo automatu | G06 modułu (`MODULE_ACCEPTANCE.md:26`) | Użytkownicy w jasnym motywie (prawdopodobnie większość) trafiają na niestabilny ekran; w ciemnym motywie ten sam ekran działa — defekt niewidoczny, jeśli testuje się tylko w jednym motywie | Pojedynczy ekran, ale wzorzec „działa w jednym motywie, nie w drugim" to znany kształt z korpusu ustaleń programu („Duplikat zamiast motywu" / przyrząd-motyw) — wymaga sprawdzenia, czy to izolowany przypadek czy hook renderujący się warunkowo per-motyw | DUŻE — crash `Invalid hook call` zwykle oznacza naruszenie reguł hooków (warunkowe wywołanie) głębiej w drzewie komponentów, nie punktową literówkę | 1 (razem z powyższym) — crash blokujący, ale trudniejszy do zlokalizowania niż #1 |
| `canvas-new-doc` ma 8/8 kadrów z `404 GET /api/work-canvas/drafts` — nierozstrzygnięte, czy to fixture harnessu czy defekt produktu (trasa istnieje w `server/src/routes/` pod inną nazwą pliku, nie zweryfikowano dokładnego dopasowania w tym dyżurze) | G06 modułu (`MODULE_ACCEPTANCE.md:26`) | Nieustalone — wymaga jednego sprawdzenia trasy przed wyceną | Nieustalone | Nieznany | 3 — wymaga rozstrzygnięcia przed dalszą pracą, nie jest to potwierdzony defekt |
| `canvas-kebab-restructure` — właściciel prosi o zamianę pozycji menu z gołych słów na przyciski w ramkach/półokrągłe; sam podkreśla „to bardzo ważna zmiana, bo dotyczy WSZYSTKICH idei" | `TRIAZ_UWAG_20260902.md` UW-13-03 (**DUŻE**) | Menu kebab canvas jest współdzielone przez każdą kanwę idei w produkcie — zmiana wizualna o szerokim promieniu rażenia | Rodzina — jeden komponent menu używany przez wszystkie kanwy idei (liczba ekranów poza tym dyżurem, nie policzona tutaj) | DUŻE — restrukturyzacja wizualna współdzielonego komponentu menu, wymaga uwagi na regresję we wszystkich miejscach użycia | 2 — szeroki promień rażenia, ale to zmiana stylu, nie crash |
| `components/AIChat` niesie **739 wystąpień** klas crimson (`primary-*`/`crimson-*`/`brand-*`) — **największy pojedynczy nośnik długu w całej aplikacji, 14% z 5325** — 412 w tle, 327 w tekście/ikonach, próbka K4 (z tego modułu) pokazała 100% dekoracja, w tym 2 naruszenia wprost reguł CLAUDE.md (pierścień fokusu `focus:ring-primary-500` zamiast `c-focus`; gradient/ikona AI na crimson zamiast tokenu `c-ai`) | `REJESTR_CRIMSON_20260902.md` §K2/K4, rekomendacja kolejności #1 | Wizualny dług niezwiązany z funkcją, ale to NAJWIĘKSZY pojedynczy nośnik w produkcie — jeśli właściciel widzi crimson gdziekolwiek najczęściej, to tutaj | Systemowy dług 5325 wystąpień; Czat to 14% całości w jednym katalogu | DUŻE jako projekt (69 plików) — pojedyncze podmiany DROBNE, ale wolumen wymaga zorganizowanej pracy, nie dyżuru ad-hoc | 4 — najwyższy priorytet w REKOMENDACJI CRIMSON ogólnie, ale niżej niż 2 crashe blokujące funkcję w priorytecie TEGO modułu |
| `teresa-confirm-chip` — prośba o delikatniejszą formułę graficzną (mniej „toporną") istniejącej, działającej kontrolki | `TRIAZ_UWAG_20260902.md` UW-13-01 (**DROBNE**) | Kosmetyka na działającej kontrolce | Pojedynczy przypadek | DROBNE — prawdopodobnie lokalna zmiana CSS/komponentu | 5 |
| `teresa-chipy-sugestii` (funkcja, nie ekran-crash powyżej) — właściciel chce kontekstowego przełącznika włącz/wyłącz dla chipów sugestii — nowa preferencja użytkownika, nie usunięcie ani poprawka wyglądu | `TRIAZ_UWAG_20260902.md` UW-13-02 (**DUŻE**) | Brak dziś negatywnego wpływu (funkcja działa, gdy się renderuje — patrz jednak crash #1 na tym samym ekranie) | Pojedynczy przypadek | DUŻE — nowa funkcja preferencji, nie defekt | 6 — nowa funkcja czeka za crashem na tym samym ekranie |

**Kolejność modułu:** 1) dwa crashe blokujące (`teresa-chipy-sugestii`,
`chat-split-teresa-right` light) — oba wymagają lokalizacji przyczyny przed
wyceną, ale blokują funkcję całkowicie, więc idą przed kosmetyką; 2)
restrukturyzacja menu kebab (szeroki promień, ale nie crash); 3) rozstrzygnięcie
404 na `canvas-new-doc`; 4) crimson (największy pojedynczy nośnik w
programie, ale to dług wizualny, nie funkcja zepsuta); 5-6) drobna
kosmetyka i nowa funkcja przełącznika chipów. Stan zastany G13
(`DRAFT_COMPLETE`, 17 zadań) pozostaje ważny jako plan remediacji funkcji
Canvas/API/RBAC — ta analiza go uzupełnia o crashy i crimson, nie zastępuje.

---

## 14_ADMIN — Administracja

68 ekranów zmapowanych (42 w mianowniku odbioru — reszta C/D poza
odbiorem), 3 z otwartą uwagą właściciela. Katalog źródłowy:
`13-administracja` → `src/components/Admin/**`, `src/views/superadmin/**`,
`src/components/SuperAdmin/**`. Zgodnie z briefem: **najlepszy moduł
programu** (336 kadrów, zero błędów konsoli/żądań, 32 czyste ekrany na 42).
Ale wygląd czysty ≠ backend bez landmin — najważniejsze znalezisko tego
modułu jest niewidoczne na żadnym zrzucie.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| **`aiSettingsService.ts:167` (`ai_user_tiers`) tworzy tabelę w RUNTIME z dialektem SQLite (`DATETIME DEFAULT CURRENT_TIMESTAMP`), BEZ `try/catch` i BEZ migracji tworzącej tę tabelę.** Wołane bezwarunkowo z `getOrgUserTiers`/`assignUserTier` (linie 599, 607) — czyli przy KAŻDYM wejściu na ekran przypisań poziomów AI (`model-catalog-table`/„Model Tier Assignments"). Zweryfikowane dziś w kodzie: brak `try {}` wokół `dbRun(...)` w `ensureUserTiersTable`. To DOKŁADNIE ta sama rodzina błędu, która ubiła cały proces serwera przy rejestracji (`email_verification_tokens`, naprawione dopiero po tygodniach — patrz `REJESTR_SCHEMAT_OD_ZERA_20260902.md` §D) — na świeżym Postgresie (nowe środowisko, staging, katastrofa i odtworzenie) PIERWSZE wejście admina na ten ekran rzuci `42704 type "datetime" does not exist` NIEOBSŁUŻONE | `REJESTR_SCHEMAT_OD_ZERA_20260902.md` §B/D (5 plików z pozostałym runtime DDL, w tym `aiSettingsService.ts` i `ai/llmConfigService.ts`); zweryfikowane bezpośrednio w kodzie 2026-09-03 (brak try/catch, brak migracji dla `ai_user_tiers`) | Dziś: zero — ścieżka nie była wywołana na mierzonym przelocie rejestracji, więc żaden użytkownik jej dziś nie trafił. Przy odtworzeniu na świeżej bazie (dokładnie ten scenariusz, który już raz kosztował tygodnie wg CLAUDE.md): PADA CAŁY PROCES serwera dla WSZYSTKICH użytkowników, nie tylko admina na tym ekranie — `Gateway.ts` nie ma error middleware łapiącego nieobsłużone odrzucenia | Ten sam wzorzec (`CREATE TABLE` z `DATETIME`, bez migracji) występuje w 5 plikach wg rejestru schematu (`integrationHubService.ts`, `notificationOutboxService.ts`, `llmConfigService.ts`, `aiSettingsService.ts`, `demoTrialTelemetryService.ts`) — ten jeden zweryfikowany bezpośrednio jako BEZ try/catch (najgorszy przypadek), reszta niezweryfikowana w tym dyżurze | ŚREDNIE — dodanie migracji dla `ai_user_tiers` (kilka linii SQL) LUB opakowanie `ensureUserTiersTable` w try/catch (wzorzec z `chatTraceService.ts`, już istnieje w kodzie) | **1 — niewidoczny na żadnym zrzucie, ale to jedyne znalezisko w tym module z realnym ryzykiem CAŁKOWITEJ awarii serwera; ten sam wzorzec błędu już raz kosztował tygodnie** |
| 5 ekranów pod Admin → Command Center (`admin-command-agent-trace`, `admin-command-ai-policy`, `admin-command-audit`, `admin-command-residency`, `admin-command-retention`) mają identyczne naruszenie axe `label` — pole formularza (filtr/wyszukiwarka) bez etykiety dostępnej, powtórzone na 5 z 6 kart pochodnych tego samego shellu | `REJESTR_G06...md` (surowe wiersze 14_ADMIN); zweryfikowane: wszystkie 5 komponentów (`CommandCenterAgentTraceTab.tsx` itd.) mają własne `<input>`/`<select>` bez wspólnego pliku filtra — to samo pominięcie powtórzone niezależnie w 5 rodzeństwach, nie jeden plik | Użytkownik czytnika ekranu nie wie, do czego służy pole filtra na 5 kart Command Center | Rodzina 5 — ten sam typ pominięcia (etykieta pola filtra) powtórzony w 5 komponentach-rodzeństwie tego samego shellu, nie jeden współdzielony plik do naprawy | DROBNE per ekran (~5 linii `aria-label`/`<label>`), ale to 5 osobnych miejsc, nie jedna naprawa — razem ŚREDNIE | 2 |
| `landmark-one-main`/`page-has-heading-one`/`region` — te same 3 reguły axe pojawiają się na WIĘKSZOŚCI z 42 zmierzonych ekranów Administracji. Dokładnie ten sam wzorzec (te same 3 nazwy reguł razem) został **udowodniony jako artefakt harnessu** (axe skanujący `document` zamiast `#dev-render-root`, reguły poziomu strony nigdy nie występują przy skanie fragmentu) w równoległym dyżurze modułów 09-12 tego samego dnia. Dyżur 13-16 (który zmierzył Administrację) **nie potwierdził**, czy użył tej samej korekty zakresu skanu | `REJESTR_G06...md`, „Dopisek" (dowód dla 09-12) vs. brak analogicznej notatki dla 13-16; brak plików pomiarowych w repo do weryfikacji (worktree usunięty po scaleniu, zgodnie z instrukcją) | Nieustalone — jeśli to harness, wpływ = zero; jeśli to realny brak `<main>`/nagłówka w layoucie Administracji, wpływ = spory (nawigacja czytnikiem ekranu) | Potencjalnie CAŁA reszta z ~30 ekranów oznaczonych `NOT_PROVEN` w tym module — jedno rozstrzygnięcie metody pomiaru zmieniłoby ocenę większości modułu naraz | Nieznany do czasu ponownego pomiaru ze skopowanym axe | 3 — **musi być rozstrzygnięte PRZED jakąkolwiek wyceną naprawy dostępności w tym module**, inaczej ryzyko naprawiania nieistniejącego problemu (albo przeoczenia prawdziwego) |
| `admin-command-center-panel` — widoczny nagłówek „Centrum dowodzenia", ale w menu nazywa się „Centrum administracyjne" (rozjazd nazw). Dodatkowo `src/utils/commandCenterFlag.ts` deklaruje `isCommandCenterEnabled()` — zweryfikowane dziś: **zero wołających w całym `src/`** poza własną deklaracją — martwa flaga-fantom, sekcja jest zawsze widoczna niezależnie od niej | `TRIAZ_UWAG_20260902.md` UW-14-02 (**DROBNE**); zweryfikowane dziś `grep -rn isCommandCenterEnabled src/` = 1 trafienie (deklaracja) | Właściciel i użytkownicy widzą dwie różne nazwy tej samej funkcji w dwóch miejscach nawigacji — źródło zamieszania, potwierdzone jako przyczyna pytania właściciela | Pojedynczy przypadek nazwy + pojedynczy przypadek martwej flagi, oba w tym samym miejscu | DROBNE — ujednolicenie nazwy (~1 linia) + opcjonalne usunięcie martwej flagi (~10 linii, osobna decyzja czy warto) | 4 |
| Pierścień fokusu na crimson (`focus:ring-primary-500`/`focus:border-primary-500`) zamiast kanonicznego `c-focus` (CLAUDE.md reguła 3) — próbka K4 znalazła to w 2 plikach, ale pełny grep dziś pokazuje **9 wystąpień w `FeatureFlagsPanel.tsx` i 15+ w `AdminKnowledgeView.tsx`** — skala większa niż sugerowała próbka | `REJESTR_CRIMSON_20260902.md` §K4 (próbka: 2 pliki); zweryfikowane dziś pełnym grepem — realna skala większa | Fokus klawiaturowy w panelach SuperAdmin jest wizualnie niespójny z resztą aplikacji (crimson zamiast niebieskiego) — dotyczy użytkowników nawigujących klawiaturą | Rodzina — nie 2 linie jak sugerowała próbka, tylko ~24 wystąpienia w 2 plikach; prawdopodobnie wzorzec skopiowany raz i powielony w obu plikach | DROBNE per wystąpienie, ŚREDNIE łącznie (2 pliki, ~24 miejsca, mechaniczna podmiana klasy) | 5 — jest to wprost nazwany błąd w CLAUDE.md, ale kosmetyczny, nie funkcjonalny |
| `views/superadmin`+`components/SuperAdmin`+`components/Admin` = **873 wystąpienia** crimson (478+275+120) — drugi co do wielkości blok w całej aplikacji po Czacie, rekomendowany jako priorytet #2 w rejestrze crimson | `REJESTR_CRIMSON_20260902.md` §K2, rekomendacja #2 | Ekrany, które właściciel i zespół widzą najczęściej przy pracy operacyjnej, niosą drugi co do wielkości wizualny dług crimson w produkcie | Systemowy dług, 102 pliki łącznie w 3 katalogach | DUŻE jako projekt — wolumen wymaga zorganizowanej pracy per plik | 6 — kosmetyka, niżej niż landmina bazy danych i realne naruszenia dostępności |

**Kolejność modułu:** 1) landmina `ai_user_tiers` bez try/catch i bez migracji
— jedyne ryzyko CAŁKOWITEJ awarii procesu w tym module, tego samego rodzaju
co defekt, który już raz kosztował tygodnie; niewidoczne wizualnie, dlatego
łatwo pominąć mimo że moduł „wygląda najlepiej"; 2) etykieta filtra
powtórzona na 5 kart Command Center; 3) rozstrzygnięcie, czy
landmark-one-main/page-has-heading-one/region to harness czy produkt —
warunkuje ocenę reszty modułu; 4) rozjazd nazwy + martwa flaga (tanie); 5-6)
crimson (kosmetyka, duży wolumen, niżej niż funkcja).

---

## 15_SETTINGS — Ustawienia

12 ekranów zmapowanych (9 w mianowniku odbioru), 1 z otwartą uwagą
właściciela. Katalog źródłowy: `18-ustawienia` → `src/components/settings/**`,
`src/views/SettingsView.tsx`. **Moduł już miał częściową pracę G13**
(`PARTIAL_DAY55_MEASURED` — szerokość, typografia, breadcrumb, slot Zapisz,
system wizualny, zgodność komponentów, copy integracji, 4 długi post-MVP;
przebudowa cross-module i pełna macierz per-ekran otwarte, patrz raport
§E.1/F.1). Poniżej dokładam nowe znaleziska, w tym jedną istotną korektę
własnego pomiaru z wczoraj.

| Znalezisko | Źródło | Wpływ na użytkownika | Rodzina | Koszt | Kolejność |
| --- | --- | --- | --- | --- | --- |
| **KOREKTA rejestru — „PL=EN" na 8 z 9 ekranów Ustawień to defekt HARNESSU, nie produktu.** `REJESTR_G06` zgłasza identyczny tekst PL/EN na prawie każdym ekranie (`ustawienia-ai-automatyzacja`, `-dane-prywatnosc`, `-integracje`, `-personalne`, `-powiadomienia`, `-workflow`, `-wyglad`, `-zaawansowane`). Zweryfikowane dziś: `dev-render/screens/ustawienia-grupy.tsx:53` woła `void i18n.changeLanguage('pl')` **bezwarunkowo, w module top-level**, ignorując parametr `?lang=en` harnessu. Kontrola: klucz `settings.sidebar.groups.mySettings` ma realne, różne tłumaczenia (`public/locales/pl/translation.json:15560` = „MOJE USTAWIENIA", `public/locales/en/translation.json:16096` = „MY SETTINGS") | `REJESTR_G06_JEZYKI_MOTYWY_20260902.md` (8 wierszy 15_SETTINGS, kolumna PL/EN); zweryfikowane w kodzie i w obu plikach tłumaczeń 2026-09-03 | Brak — to fałszywy alarm pomiaru. Prawdziwy stan i18n Ustawień jest nieznany z tego rejestru (harness nigdy realnie nie przełączył języka), nie „zepsuty" | n/d — korekta metodologii, nie defekt produktu | n/d | n/d — `NIEAKTUALNE` jako znalezisko i18n produktu. Wymaga PONOWNEGO pomiaru z poprawionym harnessem (usunięcie hardkodowanego `changeLanguage('pl')`), zanim ktokolwiek uzna i18n Ustawień za sprawdzone albo zepsute |
| `WatchingTab.tsx:88` pokazuje surowy `{watcher.objectId}` (obserwowany task/inicjatywa/projekt) zamiast tytułu — `Watcher` nie ma pola `title`/`name`, wymaga dociągnięcia z API właściwego obiektu. (Sąsiedni defekt na tej samej linii — surowy enum `notifyOn` — już naprawiony, commit `6d7eac6306`, patrz `NIEAKTUALNE` #4) | `REJESTR_SUROWE_ID_20260902.md` poz. #3; zweryfikowane dziś: linia 88 nadal `{watcher.objectId}`, linia 89 (enum) już naprawiona | Użytkownik zarządzający obserwowanymi obiektami widzi UUID zamiast nazwy taska/inicjatywy/projektu — nie wie, co obserwuje bez przełączania ekranu | Pojedynczy przypadek w tym module, część systemowej rodziny surowych ID (≥35 wcześniejszych napraw) | ŚREDNIE — wymaga kontraktu serwera (dociągnięcie tytułu razem z `objectId`, ten sam wzorzec co już wykonana naprawa `base_period_id` w Finansach) | 2 |
| Kalendarz: integracja Outlook widoczna na liście dostawców, ale realne OAuth nieukończone — `server/src/routes/calendarIntegrations.routes.ts:26` zwraca „Google/Outlook calendar OAuth integration is not available yet" | `TRIAZ_UWAG_20260902.md` UW-15-01 (część 2, ikona jabłka już naprawiona — część 1 `ZROBIONE`) | Użytkownik widzi opcję „Połącz Outlook", klika, i nie da się realnie podłączyć — obietnica bez realizacji | Pojedynczy dostawca w module integracji kalendarza | DUŻE — dokończenie integracji OAuth, nie punktowa poprawka | 3 |
| `components/settings` niesie **65 wystąpień** crimson — **100% `brand-*`** (zero `primary-*`/`crimson-*` w tym katalogu), 30 w tle / 35 w tekście, w 17 plikach | `REJESTR_CRIMSON_20260902.md` §K2 | Wizualny dług skoncentrowany wyłącznie w jednej nazwie aliasu (`brand`) — jeśli ktoś już „naprawił" ten moduł grepując tylko `primary`/`crimson`, przeoczy WSZYSTKO tutaj (dokładnie pułapka opisana dla Partnera w tym samym rejestrze) | Systemowy dług; moduł jest jednym z niewielu, gdzie CAŁY ślad to jedna nazwa — ryzyko fałszywego „zamknięte" wysokie | DUŻE jako projekt (17 plików), ale jednorodny (jedna nazwa do podmiany, nie trzy) | 4 |
| Stan zastany G13 (`PARTIAL_DAY55_MEASURED`) nazywa 4 długi post-MVP i otwiera „przebudowę cross-module + pełną macierz per-ekran" bez konkretnej listy — nie rozliczone w tym dyżurze (poza budżetem: wymagałoby ponownego czytania raportu Day 55 w całości) | Stan zastany pliku modułu, `G13` przed tą analizą | Nieustalone bez przeczytania pełnego raportu Day 55 | Nieznana | Nieznany | 5 — odziedziczone zadanie, nie nowe znalezisko tego dyżuru; wymaga osobnego czasu na przeczytanie raportu źródłowego |

**Kolejność modułu:** 1) korekta pomiaru PL=EN (nie defekt, ale musi być
zakomunikowana zanim ktoś zacznie „naprawiać" nieistniejący problem
tłumaczeń — priorytet komunikacyjny, nie inżynierski); 2) `WatchingTab`
surowy `objectId` (jasny wzorzec naprawy, mały kontrakt); 3) Outlook OAuth
niedokończony (duża, ale nazwana praca); 4) crimson `brand-*` w całości
modułu (jednorodny, tani do zaplanowania mimo wolumenu); 5) odziedziczone
zadanie Day 55 (wymaga osobnego przeczytania raportu).
