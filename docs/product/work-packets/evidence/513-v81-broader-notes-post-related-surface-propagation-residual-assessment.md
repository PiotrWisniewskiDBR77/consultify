# 513 - Broader Notes - post related-surface propagation residual assessment

Date: 2026-03-28
Lane: broader `Notes` adjunct / object-linked outputs breadth
Status: assessed

## Assessment

The active broader-notes lane was re-checked after `512` landed.

What is now honestly closed:

- notebook upload-source and converted-output truth no longer stops at `NotebookContent`,
- nearby `Task`, `Decision`, and `Initiative` note suggestion/readback surfaces now carry the same notebook metadata forward,
- and the immediate adjacent note-card ecosystem no longer flattens notebook truth back to title-only entries.

## Resulting residual

The next honest broader-notes step is no longer another tiny badge-propagation patch on one more nearby note card.

What still remains broader than this packet:

- wider notebook attachment management beyond the upload-origin source file,
- or a materially wider notebook-output propagation seam on additional distant surfaces.

## Decision

Keep broader `Notes` active.

Do not reopen:

- title-only note-card propagation on `Task`, `Decision`, and `Initiative` nearby surfaces,
- or another one-off metadata badge patch on a single adjacent note list.

The next packet must come from:

- wider notebook attachment management,
- or broader notebook-output propagation beyond the now-closed nearby related/suggested surfaces.
