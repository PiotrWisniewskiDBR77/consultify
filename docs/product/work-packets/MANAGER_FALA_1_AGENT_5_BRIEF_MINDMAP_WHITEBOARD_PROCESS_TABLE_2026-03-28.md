# Manager Brief - Agent 5

> Date: 2026-03-28
> Status: active manager brief
> Scope owner: Agent 5
> Canonical standard: `docs/product/work-packets/MANAGER_FALA_1_AGENT_STANDARD_2026-03-28.md`

---

## 1. Scope

You own only:

- `Mind map`
- `Whiteboard`
- `Proces flow`
- `Tabele`

You do not own:

- broad `Idea founder / Idea maker`
- `Notatki`
- `Outputs`

---

## 2. Why this cluster exists

This cluster owns the visual thinking and structured-work canvas layer.

The main product question is whether these tools already form a believable workspace for:

- thinking,
- mapping,
- organizing,
- and turning structure into durable work.

The standard is not “many features exist.”

The standard is whether users feel a coherent visual operating environment.

---

## 3. Required source emphasis

Prioritize:

- the active manager map
- wave split docs
- phase reviews for `Idea / Mindmap` and `Whiteboard / Proces flow / Tabele`
- the strongest module SSOT docs
- core workspace components and backend/runtime paths

You should treat the earlier `AGENT_5_WHITEBOARD_PROCESS_FLOW_TABLE_EXECUTION_PLAN.md` as valuable source material, but not as final authority.

---

## 4. Required benchmark lens

Benchmark against patterns similar to:

- `Miro`
- `Lucidchart`
- `Airtable`
- `Coda`

Focus on:

- coherence of visual grammar,
- quality of conversion from exploration to structure,
- trust in persistence and transformation,
- and whether tables behave like a real operating surface rather than a demo capability bundle.

---

## 5. Key questions to answer

You must answer:

- Does `Mind map` feel like a real ideation tool or only a branch editor?
- Is `Whiteboard` a finished workshop surface or still a capability collection?
- Is `Proces flow` credible for operational modeling?
- Are `Tabele` one coherent product, or two competing mental models?
- Which bounded packets would most quickly transform this cluster into a calm, trustworthy workspace system?

---

## 6. Output requirement

Return one final execution-grade markdown plan for this scope only.

The final plan must say clearly where product coherence is missing and which packets would create the fastest rise in perceived quality.
