# T4 Charter - Landing Anna shared-shell placement

Date: 2026-03-26
Lane: `Landing Anna shared-shell placement`
Taxonomy: `T4`
Tranche: `Parking lot`
Status: `done`

## Why now

The landing SSOT still records Anna embedding on the broader landing IA as incomplete. After closing bounded Anna behavior
packets, the next smallest real residual was placement breadth: the public assistant existed on canonical `/`, but not on the
shared `MarketingLayout` used by several other public landing pages.

## Goal

Promote one bounded `Landing Anna shared-shell placement` slice that reduces mixed truth across:

1. landing IA placement intent in `LANDING_V8_SSOT.md`
2. the shared public `MarketingLayout` shell
3. shared CTA handoff continuity for Anna on secondary marketing pages

## In scope

1. one bounded shared-shell placement packet
2. split-brain map for Anna placement between canonical `/` and `MarketingLayout`
3. embed `AnnaAssistantWidget` in the shared `MarketingLayout`
4. wire shared demo/trial/contact handoffs through the layout authority
5. focused regression proving Anna is present on a marketing page using the shared shell
6. tracker/program/evidence updates

## Explicitly out of scope

1. full Anna rollout to every custom public page
2. `Resources`, `Tools`, or `Audits` bespoke-shell placement work
3. Anna analytics, prompt-quality, or deeper voice implementation work
4. broader landing IA / copy / visual-system redesign

## Packet 1

Completed:

- place `AnnaAssistantWidget` into the shared `MarketingLayout`
- route Anna handoffs through the layout's shared demo/trial/contact authority
- add focused regression proving shared-shell pages now expose Anna

Recorded in:

- `evidence/248-v81-landing-anna-shared-shell-placement-split-brain-map.md`
- `evidence/249-v81-landing-anna-shared-shell-placement-seam.md`

## Acceptance

Accepted in:

- `evidence/250-v81-landing-anna-shared-shell-placement-t4-acceptance.md`

Residual visible backlog:

1. full Anna placement breadth on bespoke public pages such as `Resources`, `Tools`, and `Audits`
2. Anna analytics, prompt-quality, multilingual expansion, or deeper voice implementation work
