# Manager Brief - Agent 1

> Date: 2026-03-28
> Status: active manager brief
> Scope owner: Agent 1
> Canonical standard: `docs/product/work-packets/MANAGER_FALA_1_AGENT_STANDARD_2026-03-28.md`

---

## 1. Scope

You own only:

- `Anna`
- `Radar`
- `Notatki`

You do not own:

- broad `Landing`
- `Kalendarz`
- `Integracja`
- `Komunikacja`
- `Outputs`

---

## 2. Why this cluster exists

This cluster owns the core user journey from:

- first guided entry (`Anna`),
- through situational understanding (`Radar`),
- into captured working material (`Notatki`).

The goal is to decide whether this trio already forms a believable front-door-to-working-memory flow, and what still must be delivered to make it feel like a serious product.

---

## 3. Required source emphasis

Prioritize:

- the active manager map
- wave split docs
- phase review docs for `Landing / Anna`, `MyWork / Radar`, and `Notatki`
- strongest UI surfaces and API/runtime paths for these modules
- any docs that define notebook behavior, radar truth, and Anna positioning

If the repo has conflicting truths, prefer:

- strongest current SSOT
- then real repo behavior
- then older summary docs

---

## 4. Required benchmark lens

Benchmark against products or patterns similar to:

- `Perplexity` or guided AI front doors for `Anna`
- `Notion` or `Mem.ai` for structured note capture and synthesis
- `Linear` / `Asana` / executive cockpit patterns for `Radar`

Do not copy brand expression.

Compare:

- clarity of first-use experience,
- quality of next-step guidance,
- how notes become actionable,
- and whether the insight surface actually helps decision-making.

---

## 5. Key questions to answer

You must answer:

- Is `Anna` a real guided entry point or just a branded surface?
- Does `Radar` help a user orient and decide, or only display information?
- Do `Notatki` form a real working memory with promotion and continuity, or just a content area?
- Where does the trio currently break as one flow?
- What are the smallest packets that would make the trio feel product-grade now?

---

## 6. Output requirement

Return one final execution-grade markdown plan for this scope only.

The file must be strong enough that the manager can later merge it into one implementation order without re-interpreting your intent.
