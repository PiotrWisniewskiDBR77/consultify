# Lock: P35-B

Status: released
Owner: Cursor agent
Branch: ws/c-artifact-evidence
Started: 2026-03-31 22:00
Released: 2026-03-31 22:45

## Scope (bounded)
- P35-B core runtime: full thread lifecycle + server-side search target with pagination/filters + team folder permission denials (no leakage).
- Integration/security regression tests + staging demo.
- Update contract ledger row P35-B and EXECUTION_INDEX #35 → `delivered`.

## Terminal state: delivered

## Scope completed (v2 — gap closure)

### Wave 1 (initial delivery)
1. **G1+G2 Server-side search target**: `/api/conversations/search` with team scope, cursor pagination, full filter set, message content search, pinned boost + recency ranking.
2. **G3+G4 Soft-delete with grace**: `deleted_at` column, soft-delete by default, `?force=true` for purge, bulk soft-delete.
3. **G5 Deep-link state handling**: `_state` field (`active`/`archived`/`deleted`/`not_found`).
4. **G6 Team folder permission denial**: 403 responses, frontend handles by clearing active conversation.
5. **Frontend server search wiring**: `serverSearch()` + debounced sidebar integration.
6. **Delete confirmation UX**: Explicit destructive confirmation with 30-day grace messaging.

### Wave 2 (gap closure audit)
7. **BUG FIX: Summarize endpoint** — was querying `messages` table instead of `conversation_messages`. Fixed.
8. **List cursor pagination** — added `cursor` param to GET /api/conversations for deterministic, duplicate-free pagination.
9. **hasAttachments filter** — wired into search query (was defined in schema but unused).
10. **P34 policy gateway integration** — `checkChatPermission('read')` called before list and search for team scope resolution.
11. **Canonical model completion** (migration `20260331_p35b_canonical_model_completion.sql`):
    - `conversation_sessions` table (runtime snapshot: model, preset, locale, tools)
    - `conversation_message_attachments` table (kind, targetId, targetUrl, displayName, mime, sizeBytes, provenancePointer)
    - Message fields: `session_id`, `edited_at`, `redaction_state`, `safety_flags`, `language`, `delivery_state`
    - Governance metadata on conversations: `visibility_scope`, `access_policy_version`, `client_info`, `retention_policy`
    - `conversation_purge_audit` table (minimal audit trail after hard delete)
    - Composite indexes for private and team listing
12. **Audit purge trail** — force-delete now records `conversation_purge_audit` entry (who, when, message count, title hash) before purging content.
13. **Degraded posture** — `serverSearch` returns `partial` flag on errors; sidebar shows amber "Results may be incomplete" banner; rate limit detection.
14. **Tests rewritten** — strict status assertions (no 500 acceptance), 18 integration tests, 10 store tests.

## Known limits (honest)
- Session entity is schema-only (no runtime population yet — needs AI chat engine integration)
- Attachment model is schema-only (no UI for adding/viewing attachments yet)
- Write conflict detection not implemented (no ETag/version/optimistic locking)
- Export feature not implemented
- 4 of 10 degraded scenarios partially covered; 3 remain not covered (E6 attachment missing, E7 partial retrieval, E10 export too large)

## Links
- Contract: `docs/product/work-packets/cursor-work/final_master/final-v8-contracts/FINAL_IMPLEMENTATION_PLAN_35_HISTORIA_CZATOW_2026-03-29.md`
- Evidence ledger row: §10 in the contract
- Migrations: `server/migrations/20260331_p35b_conversation_soft_delete.sql`, `server/migrations/20260331_p35b_canonical_model_completion.sql`
