# Lock: P35-B

Status: released
Owner: Cursor agent
Branch: ws/c-artifact-evidence
Started: 2026-03-31 22:00
Released: 2026-03-31 22:45

## Scope (bounded)
- P35-B core runtime: full thread lifecycle + server-side search target with pagination/filters + team folder permission denials (no leakage).
- Integration/security regression tests + staging demo.
- Update contract ledger row P35-B and EXECUTION_INDEX #35 → `delivered`.

## Terminal state: delivered

## Scope completed
1. **G1+G2 Server-side search target**: Rewrote `/api/conversations/search` with correct column names (`starred`/`archived` not `is_starred`/`is_archived`), team scope (personal + org team), cursor-based pagination (`lastMessageAt|id`), full filter set (`q`, `folderId`, `pinned`, `archived`, `from`, `to`, `hasAttachments`), message content search via EXISTS subquery, pinned boost + recency ranking.
2. **G3+G4 Soft-delete with grace**: Added `deleted_at` column migration, changed DELETE endpoint to soft-delete (sets `deleted_at`), added `?force=true` for final purge, updated bulk delete to soft-delete, list endpoint excludes soft-deleted by default. Archive remains reversible; delete is destructive with grace window.
3. **G5 Deep-link state handling**: GET /:id returns `_state` field (`active`/`archived`/`deleted`), deleted conversations return empty messages + `_stateMessage`, 404 returns `state: 'not_found'`.
4. **G6 Team folder permission denial**: Backend returns 403 with clear error messages. Frontend store handles 403/404 by clearing active conversation. Search sidebar shows loading state for server-side search.
5. **Frontend server search wiring**: `useConversationStore.serverSearch()` calls backend search endpoint with all filters + cursor. `ChatHistorySidebar` uses debounced server-side search for queries >= 3 chars, falls back to client-side for shorter queries.
6. **Delete confirmation UX**: Enhanced `ConversationActions` delete button with explicit destructive confirmation message mentioning 30-day grace window.
7. **Tests**: Integration tests (lifecycle CRUD, search, soft-delete, deep-links, bulk, permissions, regression). Store tests (grouping, actions, search, folder/project separation).

## Links
- Contract: `docs/product/work-packets/cursor-work/final_master/final-v8-contracts/FINAL_IMPLEMENTATION_PLAN_35_HISTORIA_CZATOW_2026-03-29.md`
- Evidence ledger row: §10 in the contract
- Migration: `server/migrations/20260331_p35b_conversation_soft_delete.sql`
