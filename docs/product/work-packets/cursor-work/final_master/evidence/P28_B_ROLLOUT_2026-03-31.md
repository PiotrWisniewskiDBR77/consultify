# P28-B — wdrożenie (rollout) workbench

**Data:** 2026-03-31  
**Zakres:** FINAL 28 §8.1 P28-B — E2E metodologia na jednym workbench + degraded „what next”.

## Artefakty runtime

- Kolumna `assessments.p28_workbench_v1` (migracja `server/migrations/20260331_p28_workbench_p29_partner_program_ledger.sql` + `ensureAssessmentSchema`).
- Serwis: `server/src/services/assessment/AssessmentWorkbenchService.ts` — `buildWhatNextGuidance`, preset `P28_METHODOLOGY_PRESETS.DRD`, `applyMethodologyPreset`.
- API V8: `GET/POST /api/v8/assessment/:id/workbench/*`, w tym `.../methodology-preset` (`{ "preset": "DRD" }`).
- Odpowiedź `GET .../workbench`: `{ workbench, whatNext }`. Błąd `409` na `score-proposal` przy braku evidence: `missingEvidenceKinds`, `whatNext`.

## Testy

- E2E (mock DB): `server/src/services/assessment/__tests__/assessmentWorkbench.p28b-e2e.test.ts`
- Kontrakt: `tests/integration/p28-p29.program-assessment.contract.test.ts`
- Smoke statyczny: `npx tsx server/scripts/smoke-p28-workbench-b.ts`

## Kroki operatorskie (staging / produkcja)

1. Zastosuj migrację na docelowej bazie (Postgres), zgodnie z pipeline Railway / runbookiem DB organizacji.
2. Wdróż backend zawierający commity workbench (branch `ws/c-artifact-evidence` lub merge do deploy branch).
3. Zweryfikuj: organizacja z włączonym V8, token użytkownika, `GET /api/v8/assessment/{id}/workbench` zwraca `whatNext`.
4. Skrypt demo (P28-B §5.3): preset DRD → `running` → próba score (409 + whatNext) → evidence `document` + `interview_note` → score → review → interpretacja → review → `completed` → `promotion-payload` + `promotion`.

## Rollback (§8.3)

- Wyłączenie wywołań interpretacji/promocji po stronie klienta; dane w `p28_workbench_v1` pozostają (read-only po `completed`).
