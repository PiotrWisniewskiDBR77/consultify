# Wave 1B and Wave 1C Cut

Date: 2026-03-29
Owner: Cursor agent
Purpose: explicitly cut non-P0 work out of `Wave 1A`

## Rule

Anything not required to close a `P0` gap stays out of `Wave 1A`.

`Wave 1A` ends with `P0`.

`Wave 1B` starts the audited `P1` set.

`Wave 1C` carries the audited `P2` and market-fit strengthening layer.

## Wave 1B

Modules and packets assigned to `Wave 1B`:

- `Radar` - decision-support readback
- `Notatki` - adjunct breadth and provenance
- `Teresa` - workspace handoffs
- `Ankiety` - operator workflow and submission governance
- `Wnioski w Interview` - insight structure and actionability
- `Inicjatywy` - write-family clarity and schema resilience
- `KPI` - report workflow and reconciliation depth
- `Finanse` - broader mutation parity outside the active analysis lane

Rule for `Wave 1B`:

- deepen product usefulness where bounded lanes already work
- do not reopen `Wave 1A` P0 definitions unless a P0 packet regresses

## Wave 1C

Modules and packets assigned to `Wave 1C`:

- `Mind map` - interaction calmness and collaboration confidence
- `Whiteboard` - facilitation maturity and template depth
- `Proces flow` - semantic/BPMN/governance maturity
- `Tabele` - relational grammar, interface/form/governance, docs-plus-data quality
- `Anna` - commercial-strengthening layer

Rule for `Wave 1C`:

- raise market standard fit and perceived product coherence
- do not pretend this layer is required to validate the `Wave 1A` operating-credibility goal

## Carried modules

Remain outside `Wave 1A`, `Wave 1B`, and `Wave 1C` unless separately reopened:

- `Help / Baza wiedzy`
- `Program partnerski`

They remain context-only dependencies for planning.

## Why this cut matters

Without this cut, the program will keep collapsing:

- `closed`
- `usable`
- `credible`
- `commercially complete`

into one undefined stream of work.

This document prevents that.
