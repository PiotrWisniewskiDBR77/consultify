{
  "name": "l3-axe-completion-evidence",
  "private": true,
  "description": "Isolated npm project boundary so `npm install axe-core` in build.sh stays confined to this evidence directory's own node_modules and never touches the repo root's node_modules or package-lock.json.",
  "devDependencies": {}
}
