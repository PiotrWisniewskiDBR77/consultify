# PKG-G Prediction — niezależna weryfikacja (gate-e)

Weryfikator: sesja niezależna, worktree `/Users/piotrwisniewski/consultify-wt/fv3p-g-prediction`,
gałąź `codex/fv3p-g-prediction` @ `c2a9b7febd`, baza `49071c3e2d`.
Nastawienie: zakładam zawyżenie, dopóki sam nie zmierzę. Poniżej tylko pomiary wykonane w tej sesji.

STATUS: W TOKU — dokument aktualizowany etapami, commit po każdym etapie (zalecenie koordynatora
po awarii sieci innej sesji weryfikacyjnej równoległej).

## Tabela wyników (aktualizowana)

| # | Twierdzenie autora | Mój niezależny pomiar | Werdykt |
|---|---|---|---|
| 1 | 75/75 testów vitest, exit 0 (70 logiki + 5 renderu) | `npx vitest run src/components/Finance/Prediction/__tests__/predictionScenarioModel.test.ts src/components/Finance/Prediction/__tests__/PredictionWorkspace.test.tsx --maxWorkers=2` z korzenia repo. Wynik: **75 passed (75)**, 2 pliki testowe, podział dokładnie 70+5. Exit code zmierzony jawnie osobnym przebiegiem: `VITEST_EXIT_CODE=0`. | **POTWIERDZONE** |
| 2 | ★ Pełny `tsc --noEmit` NIE został ukończony (przerwany przy load 362, zastąpiony esbuild per plik) | Uruchomiłem SAM `NODE_OPTIONS=--max-old-space-size=12288 npx tsc --noEmit -p tsconfig.json` z korzenia repo (ten sam tsconfig co skrypt `type-check` w `package.json:28`, scope `src`). Proces ukończył się po ok. 8 minutach (10:38→10:46) mimo skrajnego obciążenia maszyny (load 311–470 z dziesiątek równoległych `tsc` innych sesji widocznych w `ps aux`). **Wyjście: ZERO linii diagnostyki, `TSC_EXIT_CODE=0`.** Brak output dla `tsc --noEmit` przy braku błędów jest zachowaniem NORMALNYM (nie fałszywym sukcesem) — potwierdziłem to niezależnie sprawdzając zawartość logu (16 bajtów, tylko linia exit code) i czas trwania procesu (proces żył ~8 min, zjadł realny CPU, nie padł natychmiast). To NIE jest exit 134 (OOM) — dostałem czysty exit 0. **Luka autora jest ZAMKNIĘTA przeze mnie: pełny tsc --noEmit przechodzi bez błędów.** | **LUKA ZAMKNIĘTA / POTWIERDZONE (0 błędów, exit 0)** |
| 3 | ★ Base == Baseline jest STRUKTURALNE — `STANDARD_BASE` fizycznie zabrania własnego wiersza wyniku | Znalazłem `trg_finance_prediction_forbid_standard_base_outputs` (BEFORE INSERT OR UPDATE na `finance_prediction_outputs`, `server/migrations/20260809_finance_v3_d07_prediction_02_integrity.sql:145-159`) — RAISE EXCEPTION gdy `scenario_mode='STANDARD_BASE'`. To NIE jest tylko konwencja w kodzie aplikacji — sprawdziłem to **na żywym, izolowanym Postgresie** (nowa jednorazowa baza `pkg_g_verify_standard_base_<ts>`, usunięta po teście), transkrybując DOSŁOWNIE funkcję/trigger z pliku migracji na minimalny schemat (bez FK do tabel zewnętrznych, nieistotnych dla badanego mechanizmu). 4 podtesty: (1) bezpośredni `INSERT` do `finance_prediction_outputs` dla scenariusza `STANDARD_BASE` → **odrzucony** wyjątkiem SQL identycznym z komunikatem w migracji; (2) kontrola negatywna — identyczny `INSERT` dla scenariusza `STANDARD_UPSIDE` → **przyjęty**; (3) próba złamania: promocja istniejącego scenariusza do `STANDARD_BASE` przez `UPDATE scenario_mode`, potem `UPDATE` na już istniejącym wierszu wyniku → **nadal odrzucony** (dowód, że sprawdzenie jest LIVE przy każdym UPDATE, nie tylko przy INSERT ani cache'owane); (4) potwierdzenie braku wycieku wierszy po nieudanym INSERT (0 wierszy). Wszystkie 4/4 PASS. | **POTWIERDZONE (zweryfikowane na żywym Postgresie, nie tylko czytaniem SQL)** |
| 9 | ★ Brak HTTP CRUD do utrwalania scenariuszy/driver overrides/inicjatyw/impact chain/financing — BLOCKED_EXTERNAL | Sprawdziłem GŁĘBOKOŚĆ luki, nie tylko jej istnienie. **Tabele ISTNIEJĄ** (`server/migrations/20260809_finance_v3_d07_prediction_01_tables.sql`: `finance_prediction_scenarios`, `_driver_overrides`, `_initiatives`, `_impact_chain`, `_financing`, plus `_outputs`/`_driver_line_map`/`_preflight_runs`/`_preflight_findings`/`_conflict_resolutions`). **Trasy HTTP**: `server/src/routes/v8/finance-v2/prediction.routes.ts` ma WYŁĄCZNIE `POST /prediction/:id/preflight` i `POST /prediction/:id/calculate` — zero `POST/PUT/PATCH/DELETE` dla samych tabel konfiguracji scenariusza (potwierdzone grepem po całym `server/src/routes/v8/finance-v2/`). **Serwisy (warstwa niżej)**: `grep -rn "INSERT INTO finance_prediction_scenarios\|_driver_overrides\|_initiatives\|_impact_chain\|_financing" server/src` **nie zwraca ANI JEDNEGO wyniku poza plikami testowymi** (`*.pg.test.ts`) — czyli ani jeden serwis produkcyjny nie zapisuje do tych pięciu tabel. `predictionPreflightService.ts`/`predictionComputeService.ts` WYŁĄCZNIE je czytają (`SELECT`). Jedynym miejscem, gdzie te tabele są zapełniane, są ręczne fixture'y SQL w testach. **Wniosek: luka jest GŁĘBSZA niż "brakuje tras" — brakuje RÓWNIEŻ całej warstwy serwisowej zapisu.** Tabele/schemat są gotowe (koszt naprawy: napisać serwisy + trasy + walidację + testy kontraktowe, nie tylko dopisać routery nad istniejącą logiką zapisu, bo takiej logiki nie ma). Frontend (`PredictionWorkspace.tsx:36-39`) trzyma cały scenariusz w lokalnym `useState<ScenarioDraft>` — potwierdzone, praca użytkownika NIE przeżywa odświeżenia strony. To jest **potencjalny P1** zgodnie z notatką autora. | **POTWIERDZONE, luka GŁĘBSZA niż zgłoszono (brak serwisów zapisu, nie tylko tras)** |
| 10 | Allowlista respektowana — `financeV2.api.ts`/`.types.ts` tylko DODANE bloki `// --- PKG-G Prediction ---` | `git diff --stat 49071c3e2d..HEAD`: 14 plików, **WYŁĄCZNIE insercje** (`3306 insertions(+)`, zero delecji, zero plików spoza `src/components/Finance/Prediction/**`, hooka flagi, `dev-render/**`, `.claude/launch.json` i tego raportu). Ręczna inspekcja `git diff` obu plików API potwierdza: w `financeV2.api.ts` jedyna zmiana istniejącego kodu to 3 nowe importy typów w istniejącym bloku `import type {...}` — reszta to czysto nowy blok na końcu pliku. `financeV2.types.ts` — czysto nowy blok na końcu pliku. `.claude/launch.json` (plik WSPÓŁDZIELONY między sesjami) — dodany dokładnie JEDEN nowy wpis `fv3p-g-prediction` na porcie 58028, zero zmian w istniejących wpisach. | **POTWIERDZONE** |
| 11 | Brak osłabień testów (skip/only/usunięte asercje) | `grep -n "\.skip\|\.only\|xit(\|xdescribe(\|it\.todo\|describe\.todo"` na obu plikach testowych PKG-G → **zero wyników**. | **POTWIERDZONE (brak skip/only)** |

## Notatki w toku (pozostałe twierdzenia 4,5,6,7,8,12) — kontynuacja w kolejnym etapie
